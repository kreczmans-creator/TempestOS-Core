Toggle that takes effect immediately — the only pill-shaped element in Tempest UI.

```jsx
<Switch checked={live} onChange={() => setLive(!live)} label="Live telemetry" />
```

For choices that need an explicit save, use Checkbox instead.
