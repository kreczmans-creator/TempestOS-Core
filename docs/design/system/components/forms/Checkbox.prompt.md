Multi-select control, also used for settings toggles that need a description line.

```jsx
<Checkbox label="Alert on threshold breach" description="Pages the on-call rotation" checked onChange={fn} />
<Checkbox label="All sites" indeterminate onChange={fn} />
```
