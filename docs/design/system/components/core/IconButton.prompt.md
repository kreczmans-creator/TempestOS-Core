Icon-only square button — toolbars, row actions, panel headers, sidebar rails.

```jsx
<IconButton icon="refresh-cw" label="Refresh" />
<IconButton icon="filter" label="Filter" active />
<IconButton icon="settings" label="Settings" variant="outline" size="sm" />
```

`label` is mandatory. Use `active` for toggles, never for hover.
