Mono-type metadata token — filters, applied facets, key/value labels.

```jsx
<Tag onRemove={() => drop('region:us-east-2')}>region:us-east-2</Tag>
<Tag onClick={toggle} selected>turbine-array-4</Tag>
```

Set in Space Mono because tags usually carry identifiers. Badge is the non-interactive counterpart.
