The Tempest panel — every grouped block of content sits in one.

```jsx
<Card eyebrow="Array 04" title="Turbine output" actions={<IconButton icon="more-horizontal" label="More" size="sm" />}>
  …
</Card>
```

Flat fill + one hairline; add `accent` for a status rule on the top edge. Do not stack shadows on dark surfaces.
