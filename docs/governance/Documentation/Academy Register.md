# Academy Register

## Register Metadata

| Field | Value |
|---|---|
| **Register Name** | Academy Register |
| **Purpose** | The complete index of every Academy article — its category, subject, and originating Work Package — verifying the Academy's own maintenance obligation (Engineering Governance §6) is being met in practice, not just in principle. |
| **Scope** | Every file under `docs/academy/`. |
| **Owner** | Project Maintainer. |
| **Source of Truth** | `docs/academy/` itself; `docs/academy/Academy Index.md` (the reader-facing navigation index this register cross-checks against). |
| **Review Frequency** | Updated whenever a new Academy article is created — in practice, every Work Package (Engineering Governance §6). |
| **Last Reviewed** | 2026-08-28 (Engineering Object Rehydration, `TD-85`) — one article added, `02 Runtime Architecture/34-engineering-object-rehydration.md`, covering `ADR-0113`: why a Kind→constructor map could never have worked (the constructor arguments had never been persisted), the `CaptureTypeState`/`IRehydratable<TSelf>` pattern that lets each canonical type own both directions of its own persistence with no central switch, the two defects only a restart could expose (never overwrite a live object; restoring an index can make it *more* complete and break an order-dependent consumer), and why the `Projects.Index` workaround was deleted rather than kept "for safety". **Five pre-existing omissions corrected along the way**: articles 29–33 (`WP 10.6A`, `WP 10.7A`, `WP 10.8A`, `TD-70`/`TD-71`, `TD-84`) were written and committed but never registered here; the section header had drifted stale at 31. 02 Runtime Architecture now lists 34 of 34, cross-checked against `ls` (34 files). Previously reviewed 2026-08-18 (`WP 13.12.2`, `v0.13.0` Release Documentation Closure) — three rows added for `WP 13.12.0`/`13.12.1`/`13.12.2`, completing this release's coverage at 28 of 28 delivered Work Packages, each with exactly one retrospective; counts re-derived by direct file count (129 → 142; 194 → 197). Previously reviewed 2026-08-18 (`WP 13.12.0`, `v0.13.0` Academy Retrospective Completion) — sixteen backfilled retrospectives closing the Release Blocking Academy-completeness finding. Previously reviewed 2026-08-17 (`WP 13.9.1`, Governance & Documentation Remediation) — closes the single most-corroborated finding in `WP13.9.0 Engineering Release Report.md` (found independently by four of that review's six disciplines): `WP 13.3B` (Plugin Platform Integration & End-to-End Validation Review), real and already committed (`88e41a2`), had no Academy retrospective anywhere. Added `03 Work Packages/WP13.3B-plugin-platform-integration-and-end-to-end-validation-review.md`, reconstructed directly from that commit's own real, complete message and independently cross-checked against real, current source wherever directly checkable — mirroring exactly how `WP 13.3A` itself reconstructed `WP 13.2B`'s own `WorkPackages.md` row from a commit message one Work Package earlier, but, unlike that precedent, writing the full retrospective this time rather than declining to. One new row added to the `## 03 Work Packages` table above, immediately after `WP 13.3A`'s own. File counts re-derived directly by `find`, not carried forward: `03` 112 → 113, `Total` 177 → 178. Zero `src/`/`tests/` files touched by this Governance & Documentation Remediation pass. Previously reviewed 2026-08-14 (`WP 13.3A`, Plugin Platform Integration & End-to-End Validation) — added `03 Work Packages/WP13.3A-plugin-platform-integration-and-end-to-end-validation.md` (a 13-section retrospective, mirroring `WP13.1A`'s/`WP13.2A`'s own format), one new row added to the `## 03 Work Packages` table above, immediately after `WP 13.2A`'s own. File counts re-derived directly by `find`, not carried forward: `03` 111 → 112, `Total` 176 → 177. Zero `src/`/`tests/` files touched by this Governance & Documentation pass. Previously reviewed 2026-08-13 (`WP 13.2A`, Plugin Trust & Capability Enforcement Implementation) — added `03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md` (a 13-section implementation retrospective, mirroring `WP13.1A`'s own format), one new row added to the `## 03 Work Packages` table above, immediately after `WP 13.1B`'s own. File counts re-derived directly by `find`, not carried forward: `03` 110 → 111, `Total` 175 → 176. Zero `src/`/`tests/` files touched by this Governance & Documentation pass. Previously reviewed 2026-08-13 (`WP 13.1B`, Plugin Runtime & Composition Root Implementation Review) — added `03 Work Packages/WP13.1B-plugin-runtime-and-composition-root-implementation-review.md` (a six-section governance-correction retrospective, mirroring `WP13.0B`'s own lighter format). **Found and fixed, this Work Package's own independent count, not `WP 13.1A`'s**: `WP 13.1A` added its own retrospective file to disk but never incremented this register's own counts or added its own table row — both header (108) and Total-paragraph (173) figures had silently carried `WP 13.0B`'s own stale values forward. Corrected directly by independent file recount (`find`, not carried-forward arithmetic): both `WP 13.1A`'s own missing row and `WP 13.1B`'s own new row added to the `## 03 Work Packages` table; `03` 108 → 110 (a two-file jump, not one), `Total` 173 → 175. Zero `src/`/`tests/` files touched. Previously reviewed 2026-08-13 (`WP 13.0B`, Plugin & Trust Isolation Architecture Review and Baseline) — added `03 Work Packages/WP13.0B-plugin-and-trust-isolation-architecture-review-and-baseline.md` (a six-section governance-correction retrospective, mirroring `WP13.0.0A-release-register-reconciliation.md`'s own lighter format — a review/audit Work Package with no genuine architectural either/or choice of its own), one new row added to the `## 03 Work Packages` table above, immediately after `WP 13.0A`'s own. File counts re-derived directly: `03` 107 → 108, `Total` 172 → 173, this register's own header/Total lines both corrected in the same pass rather than left for a future Work Package to find stale, mirroring the exact drift class `WP 13.0A` itself corrected here one Work Package ago. 107 → 108 Work Packages retrospectives; 172 → 173 total. Zero `src/`/`tests/` files touched. Previously reviewed 2026-08-13 (`WP 13.0A`, Plugin & Registration Trust Isolation Architecture) — added `03 Work Packages/WP13.0A-plugin-and-registration-trust-isolation-architecture.md` (a 13-section architecture-phase retrospective, mirroring `WP4.2-plugin-manifest-architecture.md`'s own format), one new row added to the `## 03 Work Packages` table above, immediately after `WP 12.9.3B`'s own. `02 Runtime Architecture/07-plugin-architecture.md` updated in place a second time (not a new file) — a new Trust Boundary section citing `ADR-0107`–`ADR-0112`, plus the already-implemented Plugin Discovery/Loading/Module Discovery pipeline's own factual Mermaid sequence diagram; that article's own "Last Materially Updated" column corrected below. `03 Work Packages/WP6.1-permissions-and-identity-implementation.md` §12 Future Evolution also updated in place — a connecting note stating `TD-09`/`TD-10`/`TD-11`'s retrofit is now fully designed (not implemented), without rewriting that retrospective's own historical content; `ADR-0044` itself is Accepted/historical and was correctly left unedited. File counts re-derived directly (`ls`, every subfolder): `00`: 1, `01`: 11, `02`: 31 (unchanged — `07` updated in place, no new file), `03`: 107 (106 → 107, this Work Package's own +1), `04`–`05`: 5 each, `06`: 8 — this register's own `## 03 Work Packages` header corrected from a long-stale "94" to the true, current 107; the `Total` line below corrected to 172 (was 164, last corrected `WP 12.1B`, silently un-updated by seven further real Work Packages added since — `WP 12.2A`, `WP 12.9.0`, `WP 12.9.3B`, `WP 13.0.0`, `WP 13.0.0A`, and this Work Package's own two files — an undercount of the same recurring shape this field has disclosed and fixed several times before, corrected here directly). **Disclosed, not fixed, out of this Work Package's own narrowly-scoped brief**: `WP 13.0.0` (`v0.13.0` Branch Establishment) and `WP 13.0.0A` (Release Register Reconciliation) are both real, already-committed Academy retrospectives (`03 Work Packages/WP13.0.0-v0.13.0-branch-establishment.md`, `03 Work Packages/WP13.0.0A-release-register-reconciliation.md`) but were never added as their own table rows here — the identical class of gap this register has found and disclosed-without-fixing several times before (`WP 12.9.3B`'s own entry, `WP 10.2B`, `WP 9.4A`'s own entries, further above); the file-count correction above already reflects their real presence on disk, only the table rows themselves remain unbackfilled. Not attempted here, whose own brief is explicit: `WP 13.0A`'s own Academy deliverables only, not a general table reconciliation pass. Architecture only; zero `src/`/`tests/` files touched. Previously reviewed 2026-08-13 (WP 12.9.3B, Governance Health Check Consistency Remediation) — added `03 Work Packages/WP12.9.3B-governance-health-check-consistency-remediation.md` (an implementation retrospective), one new row added to the `## 03 Work Packages` table above, immediately after `WP 12.9.0`'s own. **Disclosed, not fixed, deliberately out of this Work Package's own narrowly-scoped brief**: `WP 12.9.1` (Governance Health Check Remediation) and `WP 12.9.2` (Engineering Readiness Review Re-Execution) both already exist as real, committed Academy retrospectives (`03 Work Packages/WP12.9.1-governance-health-check-remediation.md`, `03 Work Packages/WP12.9.2-engineering-readiness-review-re-execution.md`) but were never added as their own table rows here — the identical class of gap this register has found and disclosed-without-fixing several times before (`WP 10.2B`, `WP 9.4A`'s own entries, above). Not backfilled by this Work Package, whose own brief is explicit: implement `WP 12.9.3A`'s approved recommendation only, do not broaden scope into repository cleanup. File counts not re-derived in full this pass (out of scope); the register's own `## 03 Work Packages` header count and Total line, below, remain exactly as `WP 12.9.0` last left them, now further stale by three real, undercounted files (`WP 12.9.1`, `WP 12.9.2`, `WP 12.9.3B` itself) — named here, not silently absorbed. Fixes a checker-architecture inconsistency in `scripts/governance-healthcheck.ps1` (`Test-ProjectStatusReferences` did not honour the same "tags unavailable → disclosed, skip" behaviour `Test-ReleaseRegisterMatchesTags` already had, producing a false `Fail` against real historical version tokens whenever a shallow checkout left zero git tags visible — confirmed directly against a real GitHub Actions run, `WP 12.9.3A`). Fix scoped to exactly the affected sub-check; path-reference validation, `Get-RepoTags`, and Check 3 itself all unmodified. Four behavioural cases verified against isolated fixtures. Full Debug+Release regression re-run: 2,255/2,255 passing both, 0 Warnings/0 Errors both. Previously reviewed 2026-08-12 (WP 12.9.0, Release Preparation & Engineering Sign-Off — Architecture phase, including its own same-day architecture review and follow-up — two blocking findings closed, see `WP12.9.0`'s own retrospective §8 Addendum) — added `03 Work Packages/WP12.9.0-release-preparation-and-engineering-sign-off-architecture.md` (an architecture-phase retrospective), one new row added to the `## 03 Work Packages` table below, immediately after `WP 12.1B`'s own (this branch's own tree does not yet include `WP 12.2A`'s own still-uncommitted row, cut as it was from `main`'s current tip, not that branch). File counts re-verified directly: `03`: 100 (99 → 100, this Work Package's own +1) — 161, +4 top-level = 165 total. Formalises the permanent TempestOS Engineering Readiness Review (`ADR-0106`, `Engineering Readiness Review Architecture.md`) — five readiness categories, a written blocking taxonomy, a fixed four-value verdict vocabulary, executed via the six-discipline Programme Review `WP 10.9A`/`WP 11.9.0` each already used. Architecture only; zero `src/`/`tests/` files touched; no release engineering performed. Previously reviewed 2026-08-12 (WP 12.2A, Presentation Strategy Execution Architecture) — added `03 Work Packages/WP12.2A-presentation-strategy-execution-architecture.md` (an architecture-phase retrospective whose own conclusion is disposition, not new design), one new row added to the `## 03 Work Packages` table below, immediately after `WP 12.1B`'s own. File counts re-verified directly: `03`: 100 (99 → 100, this Work Package's own +1) — 161, +4 top-level = 165 total. Finds `WP 12.2A`'s entire roadmap-predicted scope already delivered by `WP 11.3A`/`WP 11.3B` in `v0.11.0`; no new ADR, no new/updated architecture document — Engineering Governance §5's own ADR-creation criteria genuinely not met. Architecture only; zero `src/`/`tests/` files touched. Previously reviewed 2026-08-12 (WP 12.1B Architecture Review Follow-Up, documentation only) — corrects this field's own declaring-class count of "12" figure, propagated from the Engineering Vocabulary Register's own "Last Reviewed" narrative; re-derived directly from that register's three Entries tables, the true count is 11 (`VerificationService` had been double-counted — one class appearing in both the Kind and RelationshipKind tables). Same correction applied to the `03 Work Packages` table row below, `Engineering Vocabulary Register.md`, `PROJECT_STATUS.md`, `WorkPackages.md`, `Documentation Register.md`, `ADR Register.md`, and the `WP 12.1B` Academy retrospective; confirmed by repository-wide search that no occurrence remains. Zero `src/`/`tests/` files touched; `ADR-0105` not reopened. Previously reviewed 2026-08-12 (WP 12.1B, Classification & Relationship Vocabulary Safety Net Implementation) — added `03 Work Packages/WP12.1B-classification-and-relationship-vocabulary-safety-net-implementation.md` (an implementation retrospective, mirroring `WP12.4B`'s own format), one new row added to the `## 03 Work Packages` table below, immediately after `WP 12.1A`'s own. File counts re-verified directly: `03`: 99 (98 → 99, this Work Package's own +1) — 160, +4 top-level = 164 total. Implements `ADR-0105` exactly: every `FactoryRegistry` retrofitted with named Kind constants (Mechanical's own eight, the largest confirmed gap); `DigitalThreadGraphModel`'s own confirmed duplicate closed (found to be three local constants, not one); new Engineering Vocabulary Register populated (46 values, 11 declaring classes); new `EngineeringVocabularyConsistencyTests` (four tests, verified to actually catch a deliberately-reintroduced rogue duplicate). Full Debug+Release regression 2,255/2,255 passing, 0 Warnings/0 Errors. Previously reviewed 2026-08-12 (WP 12.1A, Classification & Relationship Vocabulary Safety Net Architecture) — added `03 Work Packages/WP12.1A-classification-and-relationship-vocabulary-safety-net-architecture.md` (an architecture-phase retrospective, mirroring `WP12.4A`'s own whole-review format), one new row added to the `## 03 Work Packages` table below, immediately after `WP 12.4B`'s own. File counts re-verified directly: `03`: 98 (97 → 98, this Work Package's own +1) — 159, +4 top-level = 163 total. `ADR-0105` produced — realises `WP11.0A` Finding `A-6`, confirming a real, present cross-layer vocabulary duplicate (`VerifiedByRelationshipKind`) and Mechanical's own complete absence of declared Kind constants; the roadmap-defined Work Package (`A-6`), not renumbered, not merged with any other WP, on its own new branch. Previously reviewed 2026-08-12 (WP 12.4B, Desktop Command & Event Wiring Implementation) — added `03 Work Packages/WP12.4B-desktop-command-and-event-wiring-implementation.md` (an implementation retrospective, mirroring `WP12.0B`'s own format), one new row added to the `## 03 Work Packages` table below, immediately after `WP 12.4A`'s own. File counts re-verified directly: `03`: 97 (96 → 97, this Work Package's own +1) — 158, +4 top-level = 162 total. Implements `ADR-0104` exactly, on the same branch as `WP 12.4A`: `RibbonObjectActionHandlers`'s own 16 duplicated report-then-refresh tails consolidated into one local `ReportAsync` function; `WP 12.0B`'s own architecture review Finding 5 closed — `UndoRedoCoordinator`/`WorkspaceViewCoordinator` both replace a two-phase-constructed `CockpitView` object reference with a plain `Action refreshCockpit` delegate. Three characterization tests added before the refactor, closing two real, confirmed coverage gaps (Mechanical discipline Ribbon handlers; the shared `statusHandler` factory's own failure branch). No Desktop mediator, command dispatcher, or event dispatcher introduced; `ADR-0103` ownership rules preserved; all public APIs preserved; behaviour verified identical via the same test assertions passing against both pre- and post-refactor code. Full Debug+Release regression: 2,248/2,248 passing, 0 Warnings/0 Errors, both configurations. No new ADR — `ADR-0104` implemented as designed, no factual correction exposed. Previously reviewed 2026-08-12 (WP 12.4A, Desktop Command & Event Wiring Architecture) — added `03 Work Packages/WP12.4A-desktop-command-and-event-wiring-architecture.md` (an architecture-phase retrospective, mirroring `WP8.0A`'s/`WP12.0A`'s own whole-review format), one new row added to the `## 03 Work Packages` table below, immediately after `WP 12.0B`'s own. File counts re-verified directly: `03`: 96 (95 → 96, this Work Package's own +1) — 157, +4 top-level = 161 total. `ADR-0104` produced — a formal, six-option evaluation of Desktop cross-collaborator communication mechanisms (this Work Package's own initial "zero new ADR" conclusion revised, in the same Work Package, once its own fuller brief's evaluation was carried out, disclosed directly); a second directly-commissioned Work Package (`WP 12.3A`/`WP 12.3B`'s own precedent), on its own branch. Previously reviewed 2026-08-12 (WP 12.0B, Desktop Composition Root Decomposition Implementation) — added `03 Work Packages/WP12.0B-desktop-composition-root-decomposition-implementation.md` (an implementation retrospective, mirroring `WP10.0B`'s own format — a full concept guide already exists separately at `docs/architecture/Desktop Composition Architecture.md`, so this file focuses on the implementation narrative, the nine + six real collaborators produced, and the two genuine construction-order cycles found and resolved), one new row added to the `## 03 Work Packages` table below, immediately after `WP 12.0A`'s own. File counts re-verified directly: `03`: 95 (94 → 95, this Work Package's own +1; also corrects a pre-existing drift — this row had stayed stale at 93 even through `WP 12.0A`'s own review, despite that Work Package's own retrospective already existing on disk) — 156, +4 top-level = 160 total. No new ADR — `ADR-0103` applied exactly, not amended; closes `WP11.0A` Finding `A-1` in full. Previously reviewed 2026-08-12 (WP 12.0A, Desktop Composition Root Decomposition Architecture) — added `03 Work Packages/WP12.0A-desktop-composition-root-decomposition-architecture.md` (an architecture-phase retrospective, mirroring `WP8.0A`'s own whole-review format), one new row added to the `## 03 Work Packages` table below. File counts re-verified directly: `03`: 94 (93 → 94, this Work Package's own +1) — 155, +4 top-level = 159 total. Architecture only, `ADR-0103`; realises `WP11.0A` Finding `A-1`. Previously reviewed 2026-08-12 (WP 12.3B, Fault-Injection Validation Framework Implementation) — added `03 Work Packages/WP12.3A-fault-injection-validation-framework-architecture.md` and `WP12.3B-fault-injection-validation-framework-implementation.md` (an architecture-then-implementation retrospective pair, mirroring `WP4.3`'s/`WP5.2`'s own precedent), both new rows added to the `## 03 Work Packages` table below. File counts re-verified directly against every subfolder (`find`/`ls`, not carried forward): `00`: 1, `01`: 11, `02`: 31, `03`: 93 (91 → 93, this Work Package's own +2), `04`–`05`: 5 each, `06`: 8 — 154, +4 top-level = 158 total. **This Work Package closes two of the four `v0.11.0` Work Packages this field's own prior entry named as lacking Academy coverage entirely (`WP 11.0A`, `WP 11.0B`, `WP 11.3A`, `WP 11.3B`) — no, it does not**: `WP 12.3A`/`WP 12.3B` are this Work Package's own retrospectives, not backfill for the still-open `v0.11.0` gap, which **remains open, unchanged, disclosed again rather than silently implied closed by this entry's own proximity to it** — still recommended as a `v0.12.0` fast-follow, still not attempted here (outside this Work Package's own scope, a directly-commissioned pair with its own separate subject matter). **Also disclosed**: `scripts/governance-healthcheck.ps1`, re-run against this Work Package's own branch, confirms `WP12.3A`/`WP12.3B`'s two new files join the already-tracked, already-disclosed "`03 Work Packages` — Work Package Walkthroughs" index gap in `Academy Index.md` (stalls at `WP 7.3A`, `WP 11.2A`'s own finding) — two more instances of a pre-existing, named, ~50-file gap, not a new one; not fixed here for the same reason the other ~50 were not. Previously reviewed 2026-08-11 (WP 11.9.0, `v0.11.0` Release Preparation & Engineering Sign-Off) — this field had gone unreviewed across the entire `v0.11.0` release (last touched at `v0.10.0`'s own close, `WP 10.9A`), found and corrected here rather than deferred a further release. **Real, disclosed gap, worse in shape than the `v0.10.0`-era finding this same field already once carried, not a recurrence of the identical one**: zero of `v0.11.0`'s own seven completed Work Packages (`WP 11.0A`–`WP 11.3B`) added a `03 Work Packages` retrospective article. Three (`WP 11.1A`, `WP 11.1B`, `WP 11.2A`) have substantive equivalent coverage instead, via three new `06 Engineering Standards` articles (`04-continuous-integration.md`, `05-release-engineering.md`, `06-governance-automation.md`) — mirroring the `v0.10.0` precedent's own "coverage exists, format differs" mitigation. Four (`WP 11.0A`, `WP 11.0B`, `WP 11.3A`, `WP 11.3B`) have **no dedicated Academy coverage of any kind**, only passing citations inside other documents — a materially wider gap than `v0.10.0`'s own five-Work-Package, fully-mitigated version. **Assessed as real but non-blocking**, per this register's own established precedent for a comparable finding, and named here explicitly rather than silently carried: recommended as an immediate `v0.12.0` fast-follow, specifically the four uncovered Work Packages first. The already-standing "`03 Work Packages` — Work Package Walkthroughs" index gap (stalls at `WP 7.3A`, ~50 real retrospectives unlinked, found by `WP 11.2A`'s own governance health-check) remains open, independently reconfirmed still true. See `WP11.9.0 Engineering Release Report.md` §5. Previously reviewed 2026-08-11 (WP 10.9A, `v0.10.0` Release Candidate & Engineering Sign-Off) — reviewed, zero new files added, file counts re-confirmed directly unchanged (`01`: 11, `02`: 31, `03`: 91, `04`–`06`: 5 each, `00`: 1 — 149, +4 top-level = 153, matching the total this field already carried since `WP 10.5C`). **Real, disclosed gap found**: five Work Packages completed since `WP 10.5C`'s own last row-adding entry (`WP 10.6B`, `WP 10.6C`, `WP 10.6D`, `WP 10.7A`, `WP 10.8A`) added **no new `03 Work Packages` retrospective article** — the per-WP convention every prior Work Package back to `WP 10.0A` had followed. `WP 10.6B` is partially covered by its own two dedicated audit documents (`docs/releases/v0.10.0/`, not `docs/academy/`); `WP 10.6C`/`WP 10.6D` gained retrospective `docs/releases/v0.10.0/` documents only during this same `WP 10.9A` audit (see `Documentation Register.md`); `WP 10.7A`/`WP 10.8A` both made real, substantive in-place amendments to existing `02 Runtime Architecture` concept guides (`21`, `23`, `25`, `26` — Cockpit, Workspace Modernisation, Object Editors, Ribbon — each gaining an accurate Future Evolution update), which is why the `02` count did not change and why "Academy updates" genuinely did happen for both, just not via the `03` per-WP retrospective sub-format. **Assessed as a real but non-blocking gap** — substantive coverage exists for every one of the five; only the dedicated retrospective format is missing. Recommended as an immediate post-`v0.10.0` fast-follow, not a release blocker (`WP10.9A Engineering Release Report.md` §5). Previously reviewed 2026-08-10 (WP 10.5C, Commercial User Experience & Application Completion) — added `02 Runtime Architecture/31-commercial-user-experience-and-application-completion.md` (a new concept guide describing this Work Package's own required-first runtime audit methodology, its own honest positive finding, and the two genuine `TD-39`-class defects found instead) and `03 Work Packages/WP10.5C-commercial-user-experience-and-application-completion.md` (an implementation retrospective mirroring `WP10.6A`'s own format). This is `v0.10.0`'s own thirteenth Work Package by completion order; 30 → 31 Runtime Architecture, 90 → 91 Work Packages (file count: 1+11+31+91+5+5+5 = 149, +4 top-level = 153); 151 → 153 total. This Work Package's own row is added to the `## 03 Work Packages` table; the eleven-row backfill gap `WP 10.2B` first disclosed remains open, unchanged. **Disclosed numbering note**: this Work Package's own number (`WP 10.5C`) sorts before `WP 10.6A` (above) despite being commissioned and completed after it — recorded here plainly, not reordered, mirroring `WP 9.3A`'s own identical precedent. Previously reviewed 2026-08-10 (WP 10.6A, Command Execution & Productivity Experience) — added `02 Runtime Architecture/30-command-execution-and-productivity-experience.md` (a new concept guide describing Undo/Redo as a Desktop-local delegate stack, `ADR-0098`; a Macro as a registered Command, `ADR-0099`; and the External Controller abstraction proven against a real Keyboard provider and a test-only stub, `ADR-0100`) and `03 Work Packages/WP10.6A-command-execution-and-productivity-experience.md` (an implementation retrospective mirroring `WP10.5B`'s own format). This is `v0.10.0`'s own twelfth Work Package; 29 → 30 Runtime Architecture, 89 → 90 Work Packages (file count: 1+11+30+90+5+5+5 = 147, +4 top-level = 151); 149 → 151 total. This Work Package's own row is added to the `## 03 Work Packages` table. **Disclosed, found-and-fixed drift**: this register's own `## 02 Runtime Architecture` section header read "28 articles" — stale since `WP 10.5B` itself added the 29th file without updating this header (the Total line's own figure was already correct; only the header had drifted, the identical class of staleness this register's own `WP 10.1B`/`WP 10.0A` entries already found and fixed twice before) — corrected here to the true, current count (30, after this Work Package's own addition). The eleven-row `## 03 Work Packages` table backfill gap `WP 10.2B` first disclosed remains open, unchanged, out of this Work Package's own scope. Previously reviewed 2026-08-10 (WP 10.5B, Desktop Workflow & Professional Interaction) — added `02 Runtime Architecture/29-desktop-workflow-and-professional-interaction.md` (a new concept guide describing the platform's own first unified Dialog Framework, why no shared dialog base class was used, and the two genuine implementation findings) and `03 Work Packages/WP10.5B-desktop-workflow-and-professional-interaction.md` (an implementation retrospective mirroring `WP10.5A`'s own format). This is `v0.10.0`'s own eleventh Work Package; 28 → 29 Runtime Architecture, 88 → 89 Work Packages (file count: 1+11+29+89+5+5+5 = 145, +4 top-level = 149); 147 → 149 total. This Work Package's own row is added to the `## 03 Work Packages` table; the eleven-row backfill gap `WP 10.2B` first disclosed remains open, unchanged. Previously reviewed 2026-08-10 (WP 10.5A, Workspace Visual Polish & Engineering User Experience) — added `02 Runtime Architecture/28-workspace-visual-polish.md` (a new concept guide describing the platform's own first theme-reactive brush infrastructure, why a global focus-visible style override was deliberately deferred rather than shipped unverified, and the four genuine Avalonia-specific implementation findings) and `03 Work Packages/WP10.5A-workspace-visual-polish.md` (an implementation retrospective mirroring `WP10.4A`'s own format). This is `v0.10.0`'s own tenth Work Package; 27 → 28 Runtime Architecture, 87 → 88 Work Packages (file count: 1+11+28+88+5+5+5 = 143, +4 top-level = 147); 145 → 147 total. This Work Package's own row is added to the `## 03 Work Packages` table; the eleven-row backfill gap `WP 10.2B` first disclosed remains open, unchanged. Previously reviewed 2026-08-09 (WP 10.4A, Digital Thread Visualisation) — added `02 Runtime Architecture/27-digital-thread-visualisation.md` (a new concept guide describing the Digital Thread graph's own progressive, on-demand expansion design, the deliberate choice to reuse `ObjectEditorView`'s own bidirectional relationship read over `IEvidenceComposer`, and how `TD-32` became visible as a real graph node for the first time) and `03 Work Packages/WP10.4A-digital-thread-visualisation.md` (an implementation retrospective mirroring `WP10.3B`'s own format). This is `v0.10.0`'s own ninth Work Package; 26 → 27 Runtime Architecture, 86 → 87 Work Packages (file count: 1+11+27+87+5+5+5 = 141, +4 top-level = 145); 143 → 145 total. This Work Package's own row is added to the `## 03 Work Packages` table; the eleven-row backfill gap `WP 10.2B` first disclosed remains open, unchanged. Previously reviewed 2026-08-09 (WP 10.3B, Ribbon, Toolbar & Command Experience) — added `02 Runtime Architecture/26-ribbon-and-command-experience.md` (a new concept guide describing the Engineering Ribbon's own "one engine over `ICommandRegistry.Items`" design, the discovery that zero commands anywhere in this platform's history ever set `CreateDefault`, and the two genuine defects — one pre-existing in `CommandPaletteOverlay`, one new in `RibbonView` itself — found and fixed as a direct consequence) and `03 Work Packages/WP10.3B-ribbon-and-command-experience.md` (an implementation retrospective mirroring `WP10.3A`'s own format). This is `v0.10.0`'s own eighth Work Package; 25 → 26 Runtime Architecture, 85 → 86 Work Packages (file count: 1+11+26+86+5+5+5 = 139, +4 top-level = 143); 141 → 143 total. This Work Package's own row is added to the `## 03 Work Packages` table; the eleven-row backfill gap `WP 10.2B` first disclosed remains open, unchanged. Previously reviewed 2026-08-09 (WP 10.3A, Engineering Object Editors) — added `02 Runtime Architecture/25-engineering-object-editors.md` (a new concept guide describing the Object Editor Framework's own "one generic engine, six disciplines" design, `ADR-0097`'s own real Content-revise dispatch, and how `ADR-0063` shaped every design decision) and `03 Work Packages/WP10.3A-engineering-object-editors.md` (an implementation retrospective mirroring `WP10.2B`'s own format). This is `v0.10.0`'s own seventh Work Package; 24 → 25 Runtime Architecture, 84 → 85 Work Packages (file count: 1+11+25+85+5+5+5 = 137, +4 top-level = 141); 139 → 141 total. This Work Package's own row is added to the `## 03 Work Packages` table; the eleven-row backfill gap disclosed by `WP 10.2B` (above) remains open, unchanged, still named as a candidate for a future dedicated governance Work Package. Previously reviewed 2026-08-09 (WP 10.2B, Docking & Workspace Layouts) — added `02 Runtime Architecture/24-docking-and-workspace-layouts.md` (a new concept guide describing the Docking Framework's own Bottom-dock realisation, Collapse vs. Auto-Hide, and the three predefined layout presets, all built without touching any of the twelve frozen `WP8.0B` Workspace contracts) and `03 Work Packages/WP10.2B-docking-and-workspace-layouts.md` (an implementation retrospective mirroring `WP10.2A`'s own format). This is `v0.10.0`'s own sixth Work Package; 23 → 24 Runtime Architecture, 83 → 84 Work Packages (real, on-disk file count — both this register's own `## 02`/`## 03` section headers and the "total" figure below are file counts, re-verified directly by `find` against every subfolder plus the four loose top-level files, `Academy Index.md`/`Academy Audit Report.md`/`Academy Masterclass Roadmap.md`/`Contributor Learning Path.md`: 1+11+24+84+5+5+5 = 135, +4 = 139); 137 → 139 total. **New finding, disclosed, not fixed here (out of this Work Package's own scope):** the `## 03 Work Packages` **table's own rows** are a genuinely separate thing from the file count above — a direct count of `| WP...` table rows found only 72 present, while 84 real files now exist on disk. Eleven real, already-shipped retrospective files (`WP9.3A`/`WP9.4A`/`WP9.5A`/`WP9.8B`/`WP9.9.0`-first-pass/`WP9.9.0`-second-pass/`WP10.0A`/`WP10.0B`/`WP10.1A`/`WP10.1B`/`WP10.2A`) are each named exactly once in this very field's own "Last Reviewed" narrative history, but were never actually added as their own table row — the identical class of gap `WP 9.8B` was commissioned specifically to close for the Platform Services Register/Map, here found in a sibling register instead. This Work Package's own row (`WP 10.2B`, below) is added; backfilling the other eleven is named here as a candidate for a future dedicated governance Work Package, not attempted in the middle of an unrelated implementation Work Package's own scope. Previously reviewed 2026-08-07 (WP 10.2A, Workspace Modernisation) — added `02 Runtime Architecture/23-workspace-modernisation.md` (a new concept guide describing `ADR-0096`'s own real object Rename/Delete dispatch and the modernised Workspace shell built around it) and `03 Work Packages/WP10.2A-workspace-modernisation.md` (an implementation retrospective mirroring `WP10.1B`'s own format). This is `v0.10.0`'s own fifth Work Package; 22 → 23 Runtime Architecture, 82 → 83 Work Packages, 135 → 137 total (re-verified directly by `find` count against every subfolder). Previously reviewed 2026-08-07 (WP 10.1B, Runtime Host & Module Discovery Hardening) — added `02 Runtime Architecture/22-runtime-host-restart-stability.md` (a new concept guide describing `TD-26`/`TD-37`'s own true root causes and fixes) and `03 Work Packages/WP10.1B-runtime-host-and-module-discovery-hardening.md` (an implementation retrospective mirroring `WP10.1A`'s own format). This is `v0.10.0`'s own fourth Work Package, and its first pure hardening/root-cause pass; 21 → 22 Runtime Architecture, 81 → 82 Work Packages, 133 → 135 total (re-verified directly by `find` count against every subfolder). Also corrects this register's own stale `## 02 Runtime Architecture` section header (read "18 articles," last correct at `WP 8.2A`/`WP 8.2B`/`WP 8.2C`'s own updates-in-place, silently un-updated by `WP 10.0A`/`WP 10.0B`/`WP 10.1A` despite each having added a new file — the Total line itself had stayed correct throughout; only this section header had drifted, mirroring the identical class of staleness `WP 9.2A`/`WP 10.0A` each previously found and fixed in the sibling `## 03 Work Packages` header) — disclosed and fixed here, with the four missing rows (19–22) backfilled into the table itself, not merely the header count. Previously reviewed 2026-08-07 (WP 10.1A, Engineering Cockpit Implementation) — added `02 Runtime Architecture/21-engineering-cockpit-graphical-implementation.md` (a new concept guide describing the complete graphical Engineering Cockpit and its own placeholder-audit discipline) and `03 Work Packages/WP10.1A-engineering-cockpit-implementation.md` (an implementation retrospective mirroring `WP8.1A`'s/`WP10.0B`'s own format). This is `v0.10.0`'s own third Work Package, and its second real implementation this release; 20 → 21 Runtime Architecture, 80 → 81 Work Packages, 131 → 133 total (re-verified directly by `find` count against every subfolder). Previously reviewed 2026-08-07 (WP 10.0B, Desktop Application Framework) — added `02 Runtime Architecture/20-desktop-application-framework.md` (a new concept guide, written at implementation stage, describing `Tempest.Desktop` — this platform's first graphical desktop application) and `03 Work Packages/WP10.0B-desktop-application-framework.md` (an implementation retrospective, mirroring `WP8.1A`'s own format — a full concept guide already exists separately, so this file focuses on the implementation narrative, files added, and Architectural Debt Assessment). This is `v0.10.0`'s own second Work Package, and its first to write real implementation code this release; 19 → 20 Runtime Architecture, 79 → 80 Work Packages, 129 → 131 total (re-verified directly by `find` count against every subfolder). Previously reviewed 2026-08-07 (WP 10.0A, User Experience Architecture) — added `02 Runtime Architecture/19-user-experience-and-desktop-application.md` (a new concept guide, written at the architecture stage per this folder's own established precedent, describing this project's first two ADR supersessions — `ADR-0092` superseding `ADR-0066`, `ADR-0093` superseding `ADR-0065`) and `03 Work Packages/WP10.0A-user-experience-architecture.md` (a whole-review retrospective mirroring `WP8.0A`'s own format exactly). This is Programme 10's own opening Work Package, `v0.10.0`'s first, on the new `feature/v0.10.0-user-experience` branch — architecture and specification only, zero `src/`/`tests/` change; 18 → 19 Runtime Architecture, 78 → 79 Work Packages, 127 → 129 total (re-verified directly by `find` count against every subfolder). Also corrects this register's own stale `## 03 Work Packages` section header (read "74 retrospectives," last correct at `WP 9.9.0` First Pass, silently un-updated by four subsequent Work Packages that each added a file — the Total line itself had stayed correct throughout; only this section header had drifted) — disclosed and fixed here, the same class of staleness `WP 9.2A` previously found and fixed in this identical field. Previously reviewed 2026-08-07 (WP 9.9.0, Release Preparation & Product Baseline — Second Pass) — added `03 Work Packages/WP9.9.0-release-preparation-and-product-baseline-second-pass.md`, a second whole-review retrospective for the same release, commissioned by the Product Owner after `WP 9.8B` closed the first pass's own top standing recommendation — the first "verify, remediate, re-verify" cycle this project has performed in full; 126 → 127 total. Previously reviewed 2026-08-07 (WP 9.8B, Platform Service Register Reconciliation) — added `03 Work Packages/WP9.8B-platform-service-register-reconciliation.md`, a whole-review retrospective mirroring `WP 7.1F`'s own governance-backfill format, adapted for a narrower, single-purpose reconciliation Work Package (no separate architecture/UX/contract phase, since none applies). Commissioned after `WP 9.9.0` despite carrying an earlier number, closing the four-Engineering-Foundation-framework Platform Service gap `WP 7.3A` first disclosed; 125 → 126 total. Previously reviewed 2026-08-07 (WP 9.9.0, Release Preparation & Product Baseline) — added `03 Work Packages/WP9.9.0-release-preparation-and-product-baseline.md`, a whole-review retrospective mirroring `WP 5.4`'s/`WP 6.8`'s/`WP 7.4.0`'s/`WP 8.9.0`'s own format — also this Work Package's own formal "Academy Retrospective" deliverable. This is `v0.9.0`'s own closing Work Package, the fifth release-preparation closing review this project has performed; 124 → 125 total. Previously reviewed 2026-08-07 (WP 9.5A, Manufacturing Workspace) — added `03 Work Packages/WP9.5A-manufacturing-workspace.md`, the same two-part (Concept Guide, Implementation Retrospective) structure every prior real-discipline Work Package established. This is `v0.9.0`'s own seventh Work Package by completion order, and the sixth to give a real Engineering Discipline real Workspace presentation; 123 → 124 total. Re-verified directly against the file system before this edit (123 present, matching the register's own stated total exactly — no drift found this time). This Work Package's own controlling instruction skips `WP 9.6A`–`WP 9.8A` and moves directly to `WP 9.9.0` Release Preparation — recorded plainly, not silently absorbed. Previously reviewed 2026-08-07 (WP 9.3A, Verification Management Workspace) — added `03 Work Packages/WP9.3A-verification-management-workspace.md`, the same two-part (Concept Guide, Implementation Retrospective) structure every prior real-discipline Work Package established. This is `v0.9.0`'s own sixth Work Package by completion order (fourth by intended number), and the fifth to give a real Engineering Discipline real Workspace presentation; 122 → 123 total. Closes the disclosed `WP 9.3A` numbering gap `WP 9.2A` left open; completed, in real time, after `WP 9.4A` despite its own earlier number — recorded here plainly, not reordered. Previously reviewed 2026-08-06 (WP 9.4A, Engineering Documents Workspace) — added `03 Work Packages/WP9.4A-engineering-documents-workspace.md`, the same two-part (Concept Guide, Implementation Retrospective) structure `WP 9.0A`/`WP 9.0B`/`WP 9.1A`/`WP 9.2A` established. This is `v0.9.0`'s own fifth Work Package, and the fourth to give a real Engineering Discipline real Workspace presentation; 121 → 122 total. Commissioned under a disclosed `WP 9.3A` numbering gap (see `PROJECT_STATUS.md`), recorded plainly in this new file's own Introduction, not silently absorbed. Previously reviewed 2026-08-05 (WP 9.2A, Engineering Calculations Workspace) — added `03 Work Packages/WP9.2A-engineering-calculations-workspace.md`, the same two-part (Concept Guide, Implementation Retrospective) structure `WP 9.0A`/`WP 9.0B`/`WP 9.1A` established. This is `v0.9.0`'s own fourth Work Package, and the third to give a real Engineering Discipline real Workspace presentation; 120 → 121 total. Also corrects this register's own stale `03 Work Packages` section header (read "68 retrospectives," last correct at `WP 8.9.0`, silently un-updated by `WP 9.0A`/`WP 9.0B`/`WP 9.1A` despite each having added a file — the Total line's own "71" figure was already correct; only the section header itself had drifted) — disclosed and fixed here, not silently. Previously reviewed 2026-08-05 (WP 9.1A, Requirements Management Workspace) — added `03 Work Packages/WP9.1A-requirements-management-workspace.md`, the same two-part (Concept Guide, Implementation Retrospective) structure `WP 9.0A`/`WP 9.0B` established. This is `v0.9.0`'s own third Work Package, and the second to give a real Engineering Discipline real Workspace presentation; 119 → 120 total. Also corrects this register's own Coverage Note total, stale at "117"/"68 Work Packages" since `WP 8.9.0` and never updated by `WP 9.0A`/`WP 9.0B` despite each having added a file — disclosed and fixed there, not silently. Previously reviewed 2026-08-05 (WP 9.0B, Product Configuration & BOM Management) — added `03 Work Packages/WP9.0B-product-configuration-and-bom-management.md`, the same two-part (Concept Guide, Implementation Retrospective) structure `WP 9.0A` established. This is `v0.9.0`'s own second Work Package; 118 → 119 total. Previously reviewed 2026-08-05 (WP 9.0A, Mechanical Product Structure) — added `03 Work Packages/WP9.0A-mechanical-product-structure.md`, a two-part file (Concept Guide, Implementation Retrospective) satisfying this Work Package's own two distinct Academy deliverable names within this folder's existing one-file-per-Work-Package convention — a disclosed documentation-structure decision. This is `v0.9.0`'s own first Work Package, and the first to give a real Engineering Discipline real Workspace presentation; 117 → 118 total. Previously reviewed 2026-08-04 (WP 8.9.0, Release Preparation & Product Baseline) — added `03 Work Packages/WP8.9.0-release-preparation-and-product-baseline.md` (a whole-review retrospective mirroring `WP 5.4`/`WP 6.8`/`WP 7.4.0`'s own format). This is `v0.8.0`'s own tenth and closing Work Package — release verification only, no `src/`/`tests/` change of any kind; 116 → 117 total. Previously reviewed 2026-08-04 (WP 8.2C, Engineering Domain Implementation) — added `03 Work Packages/WP8.2C-engineering-domain-implementation.md` (a standard 13-section implementation retrospective) and updated `02 Runtime Architecture/18-engineering-domain-architecture.md` in place a third time (not a new file) to reflect the now-compiled implementation, `ADR-0077`, `ADR-0078`, and `ADR-0079`. This is `v0.8.0`'s own ninth Work Package, and its fourth implementation — 1631 tests passing (1592 → 1631). Previously reviewed 2026-08-04 (WP 8.2B, Engineering Domain Contracts) — added `03 Work Packages/WP8.2B-engineering-domain-contracts.md` (a whole-review retrospective mirroring `WP7.2C`/`WP8.0B`'s own format) and updated `02 Runtime Architecture/18-engineering-domain-architecture.md` in place a second time (not a new file) to reflect the now-frozen contract layer, `ADR-0075`, and `ADR-0076`. This is `v0.8.0`'s own eighth Work Package, and its second Engineering-Core-facing contract-review Work Package since `WP 7.2C` — no production code, no `src/`/`tests/` change of any kind. Previously reviewed 2026-08-04 (WP 8.2A, Engineering Domain Architecture) — added `02 Runtime Architecture/18-engineering-domain-architecture.md` (a new concept guide — the canonical Engineering Object shape) and `03 Work Packages/WP8.2A-engineering-domain-architecture.md` (a whole-review retrospective mirroring `WP7.2B`/`WP8.0A`'s own format). This is `v0.8.0`'s own seventh Work Package, and its first Engineering-Core-facing architecture Work Package since `WP 7.2B` — no production code, no `src/`/`tests/` change of any kind. Previously reviewed 2026-08-04 (WP 8.1C, Engineering Cockpit) — added `WP8.1C-engineering-cockpit-implementation.md` (a standard 13-section implementation retrospective) and updated `02 Runtime Architecture/17-engineering-workspace.md` in place a sixth time (not a new file) to reflect the now-implemented Engineering Cockpit and its Command Palette integration. This is `v0.8.0`'s own sixth Work Package, and its third implementation — zero new ADRs, the first implementation Work Package this release to need none. Previously reviewed 2026-08-04 (WP 8.1B, Navigation & Project Explorer) — added `WP8.1B-navigation-and-project-explorer-implementation.md` (a standard 13-section implementation retrospective) and updated `02 Runtime Architecture/17-engineering-workspace.md` in place a fifth time (not a new file) to reflect the now-implemented Navigation system/Project Explorer, `ADR-0071`, and the first real Kind-keyed registration. This is `v0.8.0`'s own fifth Work Package, and its second implementation. Previously reviewed 2026-08-04 (WP 8.0C, Engineering Workspace UX Specification) — added `WP8.0C-engineering-workspace-ux-specification.md` (a whole-review retrospective mirroring `WP8.0A`/`WP8.0B`'s own format) and updated `02 Runtime Architecture/17-engineering-workspace.md` in place a third time (not a new file) to reflect the full target UX specification, `ADR-0069`, and `ADR-0070`. This is `v0.8.0`'s own fourth Work Package, and the second product/UX-only milestone in the release (after `WP 8.0A`). Previously reviewed 2026-07-30 (WP 8.1A, Workspace Shell) — added `WP8.1A-workspace-shell-implementation.md` (a standard 13-section implementation retrospective) and updated `02 Runtime Architecture/17-engineering-workspace.md` in place a second time (not a new file) to reflect the now-compiled shell and `ADR-0068`. This is `v0.8.0`'s own first implementation Work Package. Previously reviewed 2026-07-30 (WP 8.0B, Workspace Contracts) — added `WP8.0B-workspace-contracts.md` (a whole-review retrospective mirroring `WP7.2C`'s own format) and updated `02 Runtime Architecture/17-engineering-workspace.md` in place (not a new file) to reflect the now-frozen twelve contracts and both resolved ADRs. This is `v0.8.0`'s own second Work Package. Previously reviewed 2026-07-30 (WP 8.0A, Engineering Workspace Architecture) — added `WP8.0A-engineering-workspace-architecture.md` (a whole-review retrospective mirroring `WP7.2B`'s own format) and `02 Runtime Architecture/17-engineering-workspace.md` (the Workspace's own concept guide, written at the architecture stage, to be updated at implementation, mirroring `10-shell-and-application-composition.md`'s own precedent). This is `v0.8.0`'s own first Work Package. Previously reviewed 2026-07-30 (WP 7.4.0, Release Preparation & Product Baseline) — added `WP7.4.0-release-preparation-and-product-baseline.md`, a whole-review retrospective mirroring `WP 5.4`/`WP 6.8`/`WP 7.1F`'s own format; no production code, no new concept guide (release preparation only). This is `v0.7.0`'s own closing activity, reviewing all twelve prior `v0.7.0` Work Packages. Previously reviewed 2026-07-30 (WP 7.3A, Requirements Engine) — added `WP7.3A-requirements-engine-implementation.md` and `02 Runtime Architecture/16-requirements-engine.md` (the three-layer Requirement-as-Document pattern and the relationship-kind/traceability vocabulary, per `WP7.2C Academy Plan.md`'s own recommended two sections). This is the first implementation Work Package of the Systems Engineering Foundation phase. Previously reviewed 2026-07-30 (WP 7.2C, Requirements & Verification Platform Contract Review) — added `WP7.2C-requirements-and-verification-platform-contract-review.md`, a whole-review retrospective mirroring `WP7.0C`/`WP 7.2A`/`WP 7.2B`'s own format; no production code, no new concept guide (`WP7.2C Academy Plan.md` recommends two for the owning implementation Work Package instead, once real code exists). Previously reviewed 2026-07-30 (WP 7.2B, Requirements & Verification Platform Architecture) — added `WP7.2B-requirements-and-verification-platform-architecture.md`, a whole-review retrospective mirroring `WP 7.0A`/`WP 7.0B`/`WP 7.2A`'s own format; no production code, no new concept guide (architecture only — `WP7.2B Academy Plan.md` recommends one for the owning implementation Work Package instead). Previously reviewed 2026-07-30 (WP 7.2A, Strategic Roadmap Selection & Programme Architecture) — added `WP7.2A-strategic-roadmap-selection-and-programme-architecture.md`, a whole-review retrospective mirroring `WP 7.0A`/`WP 7.0B`/`WP 6.8`/`WP 7.1F`'s own format; no production code, no new concept guide (a planning/governance milestone, not a feature). Previously reviewed 2026-07-30 (WP 7.1F, Engineering Core Integration Review & Certification) — added `02 Runtime Architecture/15-engineering-data-model.md`, the Engineering Data Model's own concept guide, four Work Packages later than `WP7.0C Academy Plan.md` originally called for it as this programme's own "highest-priority new Academy content" — found missing by this Work Package's own closing certification review, never disclosed by `WP 7.1A` or any of `WP 7.1B`–`WP 7.1E`. Also added `WP7.1F-engineering-core-integration-review-and-certification.md`, the closing certification retrospective, mirroring `WP 6.8`'s own whole-release review format. Previously reviewed 2026-07-30 (WP 7.1E, Verification Framework) — added `WP7.1E-verification-framework-implementation.md` and `02 Runtime Architecture/14-verification-framework.md` (distinguishing Verification from Audit and Calculation Record, per `WP7.0C Academy Plan.md`'s own required output). This completes the Engineering Foundation programme's own Academy obligations. Previously reviewed 2026-07-30 (WP 7.1D, Engineering Calculation Framework) — added `WP7.1D-engineering-calculation-framework-implementation.md` and `02 Runtime Architecture/13-calculation-framework.md` (the Calculation-vs-Command distinction, per `WP7.0C Academy Plan.md`'s own required output). Previously reviewed 2026-07-30 (WP 7.1C, Materials Framework) — added `WP7.1C-materials-framework-implementation.md`. No new concept guide — per `WP7.0C Academy Plan.md`'s own finding, Materials is a worked example of the Data Model, not a new pattern. Previously reviewed 2026-07-30 (WP 7.1B, Units & Quantities Framework) — added `WP7.1B-units-and-quantities-framework-implementation.md` and `04 Design Patterns/05-phantom-type-dimension-safety.md`. Previously reviewed 2026-07-30 (WP 7.1A, Engineering Data Model) — added `WP7.1A-engineering-data-model-implementation.md`. |
| **Related Documents** | `docs/academy/Academy Index.md`; `docs/academy/Academy Audit Report.md`; `Engineering Standards Register.md`; `Feature Register.md`. |
| **Related ADRs** | None directly — the Academy documents ADRs, it is not itself governed by one. |
| **Related Academy Articles** | This register's entire scope. |
| **Coverage Status** | Complete. |

---

## 00 Introduction (1 article)

| Article |
|---|
| Welcome to the TempestOS Academy |

## 01 Engineering Principles (11 articles)

| # | Article |
|---|---|
| 01 | SOLID |
| 02 | Separation of Concerns |
| 03 | Immutability |
| 04 | Composition Over Inheritance |
| 05 | Dependency Injection |
| 06 | Fail Fast |
| 07 | Deterministic Systems |
| 08 | State Machines |
| 09 | Defensive Programming |
| 10 | Single Responsibility Principle |
| 11 | Atomic Phase Principle |

## 02 Runtime Architecture (34 articles)

| # | Article | Last Materially Updated |
|---|---|---|
| 01 | The Module Pipeline | WP 2.x era |
| 02 | The Startup Sequence | WP 2.6/2.7 era |
| 03 | Building a Module | WP 4.1/4.4B, WP 5.3 (scaffolding template added) |
| 04 | Building an Event-Driven Module | WP 4.4E |
| 05 | Working with the TempestOS Host | WP 4.5 (expanded) |
| 06 | Platform Layering | WP 5.0A (Navigation worked example added) |
| 07 | Plugin Architecture | WP 4.2, WP 13.0A (Trust Boundary section added — `ADR-0107`–`ADR-0112`, Mermaid pipeline diagram, architecture only, `WP 13.0B` not yet implemented) |
| 08 | Failure Isolation Across TempestOS | WP 5.1A (Case 5 — Command Dispatch — added; WP 5.0A's Navigation "no new case needed" finding also present) |
| 09 | Navigation Architecture | WP 5.0A (new), WP 5.0B (implementation confirmed) |
| 10 | Shell & Application Composition | WP 5.0C (new), WP 5.0D (implementation confirmed; `const`-field/assembly-loading finding added) |
| 11 | Command Framework | WP 5.1A (new), WP 5.1B (implementation confirmed; `CommandHandlerTable` sharing finding added) |
| 12 | Diagnostics & Composite Logging | WP 5.2 (new) |
| 13 | Calculation Framework | WP 7.1D (new) |
| 14 | Verification Framework | WP 7.1E (new) |
| 15 | Engineering Data Model | WP 7.1A (contract), WP 7.1F (concept guide written — disclosed as four Work Packages late) |
| 16 | Requirements Engine | WP 7.3A (new) |
| 17 | Engineering Workspace | WP 8.0A (new — architecture only; to be updated at implementation) |
| 18 | Engineering Domain Architecture | WP 8.2A (new), WP 8.2B (updated in place — contracts added), WP 8.2C (updated in place — implementation added) |
| 19 | User Experience & Desktop Application | WP 10.0A (new) |
| 20 | Desktop Application Framework | WP 10.0B (new) |
| 21 | Engineering Cockpit — Graphical Implementation | WP 10.1A (new) |
| 22 | Runtime Host — Restart Stability & Readiness Signalling | WP 10.1B (new) |
| 23 | Workspace Modernisation — Real Dispatch Behind a Modern Shell | WP 10.2A (new) |
| 24 | Docking & Workspace Layouts | WP 10.2B (new) |
| 25 | Engineering Object Editors | WP 10.3A (new) |
| 26 | Ribbon & Command Experience | WP 10.3B (new) |
| 27 | Digital Thread Visualisation | WP 10.4A (new) |
| 28 | Workspace Visual Polish | WP 10.5A (new) |
| 29 | Desktop Workflow & Professional Interaction | WP 10.6A (new) |
| 30 | Command Execution & Productivity Experience | WP 10.7A (new) |
| 31 | Commercial User Experience & Application Completion | WP 10.8A (new) |
| 32 | Responsive Workspace & Ribbon Minimisation | `TD-70`/`TD-71` (new) |
| 33 | The Product Spine | `TD-84` (new) |
| 34 | Engineering Object Rehydration | `TD-85` (new) |

## 03 Work Packages (142 retrospectives)

| Retrospective | Type |
|---|---|
| WP 2.1 — Module Discovery | Implementation |
| WP 2.2 — Runtime Registration | Implementation |
| WP 2.3 — Runtime Lifecycle | Implementation |
| WP 2.4 — Dependency Injection | Implementation |
| WP 2.5 — Configuration Framework | Implementation |
| WP 2.6 — Logging & Diagnostics Framework | Implementation |
| WP 2.7 — Runtime Host Architecture Review | Architecture |
| WP 2.7B — Runtime Host Implementation | Implementation |
| WP 4.0 — Platform Contracts | Implementation (contracts only) |
| WP 4.1 — Module SDK | Implementation |
| WP 4.2 — Plugin Manifest Architecture | Architecture |
| WP 4.2A — Runtime Platform Version | Implementation |
| WP 4.2B — Plugin Failure Classification | Architecture (ADR-0025) |
| WP 4.2C — Plugin Discovery Lifecycle Placement | Architecture (ADR-0026) |
| WP 4.2 — Plugin Manifest Implementation | Implementation |
| WP 4.2D — Platform Services Architecture Review | Review/Audit |
| WP 4.3 — Sample Module Architecture | Architecture |
| WP 4.3 — Sample Module Implementation | Implementation |
| WP 4.4A — Dependency Injection for Discovered Modules | Architecture (ADR-0027 design) |
| WP 4.4B — ADR-0027 Implementation | Implementation |
| WP 4.4 — Event Bus Architecture | Architecture (ADR-0028; also covers WP 4.4C) |
| WP 4.4D — Event Bus Implementation | Implementation |
| WP 4.4E — Sample Module Event Integration | Implementation |
| WP 4.5 — Background Services Architecture | Architecture (ADR-0029/0030) |
| WP 4.5 — Background Services Implementation | Implementation |
| WP 5.0A — Navigation Framework Architecture | Architecture (ADR-0031/0032) |
| WP 5.0B — Navigation Framework Implementation | Implementation |
| WP 5.0C — Shell & Composition Framework Architecture | Architecture (ADR-0033/0034/0035) |
| WP 5.0D — Shell & Composition Framework Implementation | Implementation |
| WP 5.0S — Platform Security Baseline Audit | Review/Audit |
| WP 5.1A — Command Framework Architecture | Architecture (ADR-0036/0037/0038) |
| WP 5.1B — Command Framework Implementation | Implementation |
| WP 5.2 — Diagnostics Improvements | Implementation (combined design + implementation, ADR-0039) |
| WP 5.3 — Developer Experience Improvements | Implementation (scaffolding/tooling, no preceding architecture phase) |
| WP 5.4 — v0.5.0 Release Candidate & Engineering Sign-Off | Release verification/sign-off — not a feature Work Package; a departure from the standard 13-section template, disclosed in its own retrospective's "What This Document Is" |
| WP 6.1 — Permissions & Identity Implementation | Implementation (combined design + implementation, ADR-0043/0044 — implemented directly against the already-approved `v0.6.0` architecture and Contract Review packages, no separate architecture-phase retrospective) |
| WP 6.4 — Settings Framework Implementation | Implementation (combined design + implementation, ADR-0041/0042 — implemented directly against the already-approved `v0.6.0` architecture and Contract Review packages, ahead of `WP 6.0`–`WP 6.3` per `Platform Service Implementation Order.md`) |
| WP 6.5 — Audit Framework Implementation | Implementation (combined design + implementation, ADR-0045 — reuses `WP 6.4`'s own Persistence abstraction, validated as sufficient without speculative extension) |
| WP 6.2 — Notification Framework Implementation | Implementation (combined design + implementation, ADR-0046 — built on the existing Event Bus's own proven dispatch model rather than a second, parallel publish/subscribe implementation) |
| WP 6.0 — Reporting Framework Implementation | Implementation (combined design + implementation, ADR-0040 — orthogonal to Export/Import; first of the five implemented `v0.6.0` Work Packages to match its own nominal numeric position) |
| WP 6.3 — REST API Implementation | Implementation (combined design + implementation, ADR-0047/0048/0049/0052 — this platform's first substantial pre-built framework dependency, first genuinely concurrent per-request scenario, resolved without modifying `WP 6.1`'s own already-shipped `CurrentPrincipalAccessor`) |
| WP 6.7 — Export/Import Framework Implementation | Implementation (combined design + implementation, ADR-0051 — completes the `WP 6.0` orthogonality decision; resolves the approved contract's own multi-destination-import gap via a `Kind`-routed, dual-registered `ImportService`, reusing `WP 6.1`'s own `CurrentPrincipalAccessor` registration pattern) |
| WP 6.6 — Licensing Framework Implementation | Implementation (combined design + implementation, ADR-0050 — the release's final production implementation Work Package; resolves `Risk Register.md`'s own `R5`, a missing license file is a valid, unrestricted default, never Host-fatal, while a broken one is) |
| WP 6.8 — Platform Services Integration Review & Release Certification | Closing certification review — mirrors `WP 5.4`'s own whole-release format (What Was Achieved, Architectural Lessons, Implementation Lessons, Repository Maturity, Recommendations, Key Takeaways), not the standard 13-section per-feature template; no production code written |
| WP 7.0A — Future Capability Register & Product Vision | Architecture-and-governance milestone Work Package — mirrors `WP 6.8`'s own whole-review format, not the standard 13-section per-feature template; no production code written; established `VISION.md`, `Future Capability Register.md`, `Capability Categories.md`, `Product Roadmap.md` |
| WP 7.0B — Engineering Foundation Planning & Capability Architecture | Architecture-and-planning milestone Work Package — mirrors `WP 6.8`/`WP 7.0A`'s own whole-review format; no production code written; added `FCR-0029`–`FCR-0033` (Engineering Foundation Programme) and eight planning deliverables (dependency graph, foundation architecture, discipline assessment, release roadmap, Work Package catalogue, platform consumption matrix, roadmap risk register) |
| WP 7.0C — Engineering Foundation Contract Review | Contract-review milestone Work Package — mirrors `WP 6.8`/`WP 7.0A`/`WP 7.0B`'s own whole-review format; no production code, no compiled interface; proposed public C# contracts for all five Engineering Foundation frameworks and eight review deliverables (contracts, cross-framework dependency report, standards mapping, platform integration matrix, testing strategy, academy plan, governance confirmation, required ADR catalogue `ADR-0053`–`ADR-0057`) |
| WP 7.1A — Engineering Data Model | Implementation (standard 13-section template, `ADR-0053`) — the first implementation Work Package of the Engineering Foundation phase; implements `Tempest.Core.EngineeringData` exactly as `WP 7.0C` proposed, one disclosed minor deviation (exception base class modifier); 13 new production files, 36 new tests, `docs/engineering/Engineering Principles.md` established |
| WP 7.1B — Units & Quantities Framework | Implementation (standard 13-section template, `ADR-0054`) — the second implementation Work Package of the Engineering Foundation phase; implements `Tempest.Core.UnitsAndQuantities` exactly as `WP 7.0C` proposed, extended (not changed) with arithmetic, comparison, formatting, parsing, and JSON serialization; 20 new production files, 67 new tests, zero DI registration, `docs/engineering/Engineering Principles.md` extended (Principles 7-12), new Design Patterns concept guide added |
| WP 7.1C — Materials Framework | Implementation (standard 13-section template, `ADR-0055`) — the third implementation Work Package of the Engineering Foundation phase; implements `Tempest.Core.Materials` exactly as `WP 7.0C` proposed, extended (not changed) with a structured, provenance-carrying property type resolving `ADR-0055`'s own reserved property-typing question; 14 new production files, 55 new tests, consumes both `Tempest.Core.EngineeringData` and `Tempest.Core.UnitsAndQuantities`, `docs/engineering/Engineering Principles.md` extended (Principles 13-16), no new concept guide (a worked example of the Data Model, per `WP7.0C Academy Plan.md`) |
| WP 7.1D — Engineering Calculation Framework | Implementation (standard 13-section template, `ADR-0056`) — the fourth implementation Work Package of the Engineering Foundation phase, and the first to include a dedicated Security Review; implements `Tempest.Core.Calculations` exactly as `WP 7.0C` proposed, substantially extended (metadata, assumptions, constraints, a validation model, an execution context, material references) to satisfy this Work Package's own "engineering evidence, not merely a numerical answer" requirement, resolving `ADR-0056`; 17 new production files, 52 new tests, consumes both `Tempest.Core.EngineeringData` and `Tempest.Core.UnitsAndQuantities`, `docs/engineering/Engineering Principles.md` extended (Principles 17-23), new concept guide (`13-calculation-framework.md`, the Calculation-vs-Command distinction) |
| WP 7.1E — Verification Framework | Implementation (standard 13-section template, `ADR-0057`) — the fifth and final implementation Work Package of the Engineering Foundation phase, completing the entire programme; implements `Tempest.Core.Verification` exactly as `WP 7.0C` proposed, extended (not changed to `subjectDocumentId`/`outcome`/`method`) with a structured `VerificationContext` (criteria, evidence, linked documents, linked calculation records, material references) resolving `ADR-0057`; 9 new production files (the smallest of the five frameworks), 49 new tests, consumes only `Tempest.Core.EngineeringData` (no direct Persistence dependency needed — verification history is queried via the Data Model's own existing `LinkAsync`/`GetReferencesAsync` mechanism), `docs/engineering/Engineering Principles.md` extended (Principles 24-28), new concept guide (`14-verification-framework.md`, distinguishing Verification from Audit and Calculation Record) |
| WP 7.1F — Engineering Core Integration Review & Certification | Closing certification review — mirrors `WP 6.8`'s own whole-release format (What Was Achieved, Architectural Lessons, Implementation Lessons, Repository Maturity, Recommendations, Key Takeaways), not the standard 13-section per-feature template; no production code written. Found and closed a repeat of `WP 6.8`'s own exact governance-drift pattern: `Interface Register.md`, `Dependency Injection Register.md`, and `Module Register.md` had gone stale since `WP 6.8` itself, undetected across all five Engineering Foundation Work Packages (11 interfaces, 4 registrations, 4 sample modules unrecorded). Also found and wrote the Engineering Data Model's own missing concept guide (`15-engineering-data-model.md`), required by `WP7.0C Academy Plan.md` since `WP 7.1A` and never produced. |
| WP 7.2A — Strategic Roadmap Selection & Programme Architecture | Architecture, governance, and roadmap-planning milestone Work Package — mirrors `WP 7.0A`/`WP 7.0B`'s own whole-review format; no production code written. Evaluated seven candidate programmes against eleven criteria using repository evidence; recommended Programme A (Requirements & Verification Platform, `FCR-0027`), scoring 46/55, over Programme F (Platform Hardening, 36/55, sequenced second) and five programmes with no identified capability (`WP7.0B Engineering Discipline Assessment.md`'s own finding, re-confirmed unchanged). Ten completion deliverables, prefixed `WP7.2A`. |
| WP 7.2B — Requirements & Verification Platform Architecture | Architecture-only milestone Work Package, continuing the Engineering Foundation into Systems Engineering — mirrors `WP7.0C Engineering Foundation Contracts.md`'s own format; no production code written. Designed the complete architecture for the Requirements & Verification Platform: twelve domain concepts, a three-layer Engineering Core → Systems Engineering Foundation → Engineering Discipline Modules model, a digital thread traversal design, eleven-service dependency analysis, security classification (found one new Technical Debt item — no concurrency-conflict detection on `ReviseAsync`), and an industry-neutral standards mapping. Reserved `ADR-0058`–`ADR-0060`. Eleven completion deliverables, prefixed `WP7.2B`. |
| WP 7.2C — Requirements & Verification Platform Contract Review | Contract-review-only milestone Work Package, mirroring `WP7.0C Engineering Foundation Contracts.md`'s own twelve-question format extended to seventeen questions per concept; no production code written. Defined full proposed C# contracts for all thirteen named domain concepts (`IRequirementsService`, `IRequirement`, and eleven companions), a seven-state Requirement Lifecycle Model, a six-of-seven-relationship Relationship Model (the seventh, "Verified By," already owned by `Tempest.Core.Verification`), a Traceability Contract (disclosing one genuine reverse-allocation-traceability limitation), and a Verification Integration Contract re-confirming `ADR-0057`'s own circular-dependency avoidance unmodified. Reserved `ADR-0061` (new) alongside `ADR-0058`–`ADR-0060` (carried forward, unanswered). Twelve completion deliverables, prefixed `WP7.2C`. |
| WP 7.3A — Requirements Engine | Implementation (standard 13-section template, `ADR-0058`–`ADR-0061`) — the first implementation Work Package of the Systems Engineering Foundation phase; implements `Tempest.Core.Requirements` exactly as `WP 7.2C` proposed, zero architectural deviation (the sole disclosed narrowing — Guid-only allocation targets — originated at the `WP 7.2C` contract-review stage itself, not this Work Package); 20 new production files, 131 new tests (1275 → 1406), consumes both `Tempest.Core.EngineeringData` and `Tempest.Core.Verification`, `docs/engineering/Engineering Principles.md` extended (Principles 29-32), new concept guide (`16-requirements-engine.md`, the three-layer Requirement-as-Document pattern and the relationship-kind/traceability vocabulary). Third Work Package overall to include a dedicated Security Review; found and formally registered `TD-25`. Eight completion deliverables, prefixed `WP7.3A`. |
| WP 7.4.0 — Release Preparation & Product Baseline | Closing release-preparation review — mirrors `WP 5.4`/`WP 6.8`/`WP 7.1F`'s own whole-release format (What Was Achieved, Architectural Lessons, Implementation Lessons, Repository Maturity, Recommendations, Key Takeaways), not the standard 13-section per-feature template; no production code written. Complete release readiness review across seventeen named areas; found and corrected five governance/documentation staleness findings (`Documentation Register.md`, `Governance Register.md` Compliance Matrix — twelve Work Packages backfilled); disclosed, not fixed, one further gap (`Platform Services Register.md`/`Platform Service Map.md` missing four Engineering Foundation framework rows). Populated `docs/releases/v0.7.0/ReleaseNotes.md` and `Retrospective.md`, both previously stale skeletons. Recommended `v0.7.0` **APPROVED**. |
| WP 8.0A — Engineering Workspace Architecture | Architecture-only milestone Work Package, `v0.8.0`'s own first — mirrors `WP7.2B`'s own whole-review format; no production code written, no implementation. Designed the complete Engineering Workspace across all twelve named areas (philosophy, user journeys, main window layout, navigation model, Project Explorer, engineering object hierarchy, docking strategy, view architecture, digital thread visualisation, state management, extensibility model, interaction patterns) — a graphical, multi-panel evolution of `Tempest.App`'s own composition root, additive to console `TempestShell`, introducing zero new Platform Service. Four new ADRs (`ADR-0062`–`ADR-0065`); `ADR-0066`/`ADR-0067` reserved. Five completion deliverables, prefixed `WP8.0A`. |
| WP 8.0B — Workspace Contracts | Contract-review-only milestone Work Package, `v0.8.0`'s own second — mirrors `WP7.2C`'s own whole-review format; no production code, no compiled interface. Defined full proposed C# contracts for all twelve named interfaces (`IWorkspace`, `IWorkspaceManager`, `IWorkspaceView`, `IWorkspacePanel`, `IWorkspaceLayout`, `INavigationService`, `ISelectionService`, `IWorkspaceContext`, `IWorkspaceState`, `IProjectExplorer`, `IPropertyInspector`, `IWorkspaceCommand`) plus supporting types. Resolved both reserved ADRs: `ADR-0066` (terminal-based presentation, not a graphical desktop framework) and `ADR-0067` (Kind-keyed registration for views and explorer nodes, mirroring `IReportDefinition`/`IReportRenderer<T>`). Four completion deliverables, prefixed `WP8.0B`. |
| WP 8.1A — Workspace Shell | Implementation (standard 13-section template, `ADR-0068`) — `v0.8.0`'s own first implementation Work Package, and the third overall; implements the Workspace shell only (no engineering functionality) exactly as `WP 8.0B` approved — all twelve contracts compiled in a new `Tempest.App.Workspace` namespace, zero signature change. `WorkspaceManager`/`WorkspaceShell` are now `Tempest.App`'s own default launch target (`ADR-0068`); console `TempestShell` remains, untouched, no longer default. 27 new production files, 91 new tests (1406 → 1497). Two disclosed implementation-phase findings (`ISettingsProvider`'s own `string`-only contract; `ITempestHost`'s own single-use constraint), neither requiring an architectural revisit. Zero new Technical Debt. Academy concept guide updated in place a second time. |
| WP 8.0C — Engineering Workspace UX Specification | Product-and-UX-design-only milestone Work Package, `v0.8.0`'s own fourth — mirrors `WP8.0A`/`WP8.0B`'s own whole-review format; no production code, no `src/`/`tests/` change of any kind. Fully specified the target Workspace experience across all 28 named scope areas: nine deliverables (UX Specification, Screen Catalogue, User Journey Maps, Interaction Specification, Navigation Maps, Wireframe Sketches, Workspace Behaviour Specification, Engineering Cockpit Specification), each disclosing "Today vs. Target" against `WP 8.1A`'s own already-shipped shell — a genuine sequencing departure from this project's usual architecture-before-implementation order, disclosed rather than absorbed silently. Two new ADRs (`ADR-0069`, `ADR-0070`); named, not resolved, a Rendering Feasibility tension between rich UX ambitions and `ADR-0066`'s terminal-based decision. Nine completion deliverables, prefixed `WP8.0C`. |
| WP 8.1B — Navigation & Project Explorer | Implementation (standard 13-section template, `ADR-0071`) — `v0.8.0`'s own fifth Work Package, and its second implementation; implements the Navigation system and Project Explorer specified across `WP 8.0A`/`WP 8.0B`/`WP 8.0C` — navigation history, breadcrumbs, an Areas panel, Kind-keyed node providers, selection synchronisation, context menus, filtering, and recent items — against representative sample content only (`Tempest.App.Workspace.Samples`), no Requirements/Calculations/Documents. Two genuine capabilities `WP 8.0B` never anticipated (history/recent items; breadcrumbs/filtering) added as same-assembly-only extensions to the concrete `NavigationService`/`ProjectExplorer` classes, not to the twelve public interfaces. Found and corrected, in a new ADR, a worked example inside `ADR-0067` that does not hold against the real Host/Workspace boundary (`ADR-0062`) — a discovered module cannot reach `IWorkspaceManager` directly; registration now happens in `Program.cs`. 7 new production files, 5 modified, 55 new tests (1497 → 1552). Zero new Technical Debt. Academy concept guide updated in place a fifth time. |
| WP 8.1C — Engineering Cockpit | Implementation (standard 13-section template) — `v0.8.0`'s own sixth Work Package, and its third implementation; implements the Engineering Cockpit as the Workspace's own default landing screen (`ADR-0069`), consuming only existing Workspace services (`NavigationService`, and — newly resolved — the existing `ICommandRegistry` Platform Service for Command Palette integration, `ADR-0070`). Controlling instruction expanded mid-Work-Package to name many more cards than `WP8.0C Engineering Cockpit Specification.md`'s own seven regions (Continue Where I Left Off, Recent/Favourite Projects, a five-discipline Project Health Dashboard, Risk Summary, Open Decisions, Blocked Items, Overdue Actions, Quick Actions) — followed in full, disclosed explicitly as a product-scope expansion, not an architectural one. Every card with a real Workspace-service backing is a live read; every other card is fixed, disclosed, representative placeholder content — no Requirements/Calculations/Verification/Digital-Thread-traversal logic anywhere. 5 new production files, 3 modified, 40 new tests (1552 → 1592). **Zero new ADRs** — the first implementation Work Package this release to need none. Zero new Technical Debt. Academy concept guide updated in place a sixth time. |
| WP 8.2A — Engineering Domain Architecture | Architecture-only milestone Work Package, `v0.8.0`'s own seventh — mirrors `WP7.2B`/`WP8.0A`'s own whole-review format; no production code, no `src/`/`tests/` change of any kind. Defines the complete canonical Engineering Domain Architecture — ~49 named Engineering Objects across thirteen families, twenty relationship kinds, a canonical eight-state lifecycle vocabulary, configuration management, the full Digital Thread, common metadata, and validation rules — every fact grounded in, and reconciled against, the four Engineering Core frameworks that already ship (`Requirements`/`Verification`/`Materials`/`Calculations`), never designed in isolation from them. Three new ADRs (`ADR-0072`–`ADR-0074`), each formalising a pattern those four frameworks had already independently converged on, now stated once as binding platform architecture. New concept guide (`18-engineering-domain-architecture.md`). Nine completion deliverables, prefixed `WP8.2A`. |
| WP 8.2B — Engineering Domain Contracts | Contract-review-only milestone Work Package, `v0.8.0`'s own eighth — mirrors `WP7.0C`/`WP7.2C`/`WP8.0B`'s own whole-review format; no production code, no compiled interface, no `src/`/`tests/` change of any kind. Converts `WP 8.2A`'s canonical Engineering Domain Architecture into complete proposed C# contracts: `IEngineeringObject` plus ten facet interfaces (`ADR-0075`); all ~49 canonical object interfaces composed from them across thirteen families; one generic `IEngineeringRelationship` interface carrying a `RelationshipCategory` enum as descriptive metadata realising all seventeen named relationship categories without reopening `ADR-0073`'s own closed-enum prohibition (`ADR-0076`); full Lifecycle, Relationship, Validation, and Digital Thread contract specifications; eight sequence diagrams; a complete dependency-rules/layering analysis. Two new ADRs (`ADR-0075`, `ADR-0076`), `ADR-0076` explicitly resolving a tension between this Work Package's own controlling instruction and `ADR-0073`'s prior, binding decision. Concept guide updated in place a second time. Eight completion deliverables, prefixed `WP8.2B`. |
| WP 8.2C — Engineering Domain Implementation | Implementation (standard 13-section template) — `v0.8.0`'s own ninth Work Package, and its fourth implementation; compiles every WP8.2B frozen contract exactly, and gives 38 of the ~49 canonical objects a real, tested concrete class (corrected from an initially-claimed 39 by `WP 8.9.0`'s own release-readiness verification) over a new shared `EngineeringObjectBase`, two generic factory types, and a new in-memory repository layer — the five already-Implemented canonical Kinds (`Requirement`, `RequirementCollection`/`Group`, `VerificationRecord`, `CalculationRecord`, `MaterialSpecification`) are deliberately not given a competing concrete realisation. Every canonical object's own real storage reuses the existing, shared `IEngineeringDocumentStore` in production — zero new persistence introduced, resolving a tension against `ADR-0072` at the implementation stage the same way `ADR-0076` resolved one at the contract stage. Three new ADRs (`ADR-0077`–`ADR-0079`). New sample module (`EngineeringDomainSampleModule`, the platform's twenty-second, building a sixteen-object representative graph including a real cross-framework Material reference). 39 new tests (1592 → 1631). Concept guide updated in place a third time. |
| WP 8.9.0 — Release Preparation & Product Baseline | Release-verification-only Work Package, mirroring `WP 5.4`/`WP 6.8`/`WP 7.4.0`'s own whole-review format — `v0.8.0`'s own tenth and closing Work Package; no production code, no `src/`/`tests/` change of any kind. Complete release readiness review across repository/build/test/version/governance/architecture verification for all nine `v0.8.0` Work Packages (`WP 8.0A`–`WP 8.2C`). Independently re-verified every headline figure directly against the repository; found and corrected one genuine arithmetic error (`WP 8.2C`'s own claimed 39 concrete object classes, corrected to the verified 38, in every living document, left unedited in `WP 8.2C`'s own dated historical artifacts per "never silently modify historical records"); disclosed, not fixed, two further findings (the four-Engineering-Foundation-framework Platform Service gap, now open across two consecutive release-closing reviews; `WP8.2B`'s own `IRelease` interface-inheritance-depth inconsistency); disclosed, weighed explicitly, a genuine process gap (zero dedicated Security Reviews this release). Recommendation: **APPROVED**. Zero new ADRs. Nine completion deliverables, prefixed `WP8.9.0`, plus `ReleaseNotes.md`/`Retrospective.md`/`WorkPackages.md` fully populated. |
| WP 9.0A — Mechanical Product Structure | Implementation — `v0.9.0` ("Mechanical Foundation")'s own first Work Package, and the first to wire a real Engineering Discipline into the real Engineering Workspace. `Project`/`Assembly`/`SubAssembly`/`Part`/`Component`/`Configuration` were already real, tested `WP8.2C` concrete classes (the Canonical Object Catalogue's own "Conceptual" marking for all six was stale, corrected here, not silently); this Work Package adds three new, additive Domain facets (`IRenamable`/`IHasParent`/`IDeletable`, `ADR-0080`/`ADR-0081`) and the platform's first real (non-sample) Kind-keyed Workspace provider implementations across all three categories, including a new third one, `IPropertyFacetProvider` (`ADR-0082`). Six new `IWorkspaceCommand`/`ICommand` implementations close `WP8.1B`'s own disclosed "no concrete `IWorkspaceCommand` is implemented" gap. Representative data: a Project, two Assemblies, a three-level-deep Sub-Assembly chain, five Parts (one soft-deleted), one Component cross-referenced from two Parts, one Configuration baseline. Three new ADRs (`ADR-0080`–`ADR-0082`). One genuine, pre-existing platform finding disclosed (`TD-26`, confirmed via a disposable `git worktree` comparison against the unmodified `v0.8.0` tag, not introduced by this Work Package). First dedicated Security Review since `v0.7.0`, closing `WP8.9.0`'s own disclosed gap. 12 new production files under `Tempest.App.Workspace.Mechanical`, plus new files in `Tempest.Core.EngineeringDomain`/`Tempest.Samples`; 64 new tests (1631 → 1695). Single academy file satisfies both named Academy deliverables ("Concept Guide"/"Implementation Retrospective") as two clearly headed parts — a disclosed documentation-structure decision, preserving this folder's own one-file-per-Work-Package convention. |
| WP 9.0B — Product Configuration & BOM Management | Implementation — `v0.9.0`'s own second Work Package. Adds one new additive Domain facet, `IHasBomLine` (Quantity/Unit of Measure/Find Number/Item Number/Reference Designator, `ADR-0083`), the fourth in the `ADR-0080` composition family; `Configuration`/`Baseline`/`Release` needed zero new Domain code (already real since `WP8.2C`). First real use of two previously-unused `WP8.2C` extension points: `ValidationRuleSet.Register` (five new `IValidationRule`s) and `IReferenceIntegrityChecker.CheckBaselineMembersAsync`. Three new Workspace commands (Set BOM Line, Compare Baselines — a real diff, not placeholder — Validate Configuration), bringing the Mechanical discipline to nine commands total. The existing Product Structure tree became the BOM hierarchy by extension, never a second structure. Product Variants remain placeholder architecture only, exactly as scoped — a design note, no code. Found and fixed, in place, two genuine implementation defects from earlier in this same, not-yet-committed session (a `TEMPEST-VAL` validation-code collision `WP 9.0A` itself introduced; `ReviseAsync` silently discarding structural/BOM state across a revision) — both disclosed fully, neither registered as debt, since neither was ever part of a commit or tagged release. One new Technical Debt item (`TD-27`, a flaky-test/documentation finding, already largely remediated). One new ADR (`ADR-0083`). 43 new tests (1695 → 1738). Single academy file, same two-part structure as `WP 9.0A`'s own. |
| WP 9.1A — Requirements Management Workspace | Implementation — `v0.9.0`'s own third Work Package, and the second to wire a real Engineering Discipline into the real Engineering Workspace — the Requirements Framework (`WP 7.3A`), genuinely different in shape from Mechanical's `EngineeringObjectBase`-composed objects. Seven new additive `IRequirementsService` methods plus `ListCollectionsAsync`/`ListGroupsAsync` (added mid-implementation once the Explorer tree's own real need for them surfaced), a `RequirementGroupDto` parent-resolution fix, and a new `IRequirementValidationService` (`ADR-0084`); `ISelectionService`/`IWorkspaceContext` gain multi-selection, additively, resolving `FCR-0039` (`ADR-0085`). New `Tempest.App.Workspace.Requirements` namespace mirrors `.Mechanical`'s own shape: node provider, view/factory, facet provider, 18 commands. Search needed zero new code (`ProjectExplorer.FilterAsync`, `WP8.1B`, already generic). Real Engineering Cockpit Requirements KPIs replace every placeholder card. Representative data: three-level Group hierarchy (one root-created-then-moved, proving the storage fix directly), ten Requirements across six statuses, real allocations to the Mechanical sample data's own Wing Assembly/Spar Web Plate — the first sample module ever to depend on another sample module's own instance, disclosed directly. Found and fixed, in place, two genuine implementation defects from earlier in this same, not-yet-committed session (a permission-gated `GetEvidenceAsync` read reachable from three passive surfaces; the `RequirementGroupDto` resolution ambiguity) — both disclosed fully, neither registered as debt. One new Technical Debt item (`TD-28`, a bulk-command view-refresh disclosure). Two new ADRs (`ADR-0084`, `ADR-0085`). 70 new tests (1738 → 1808). Single academy file, same two-part structure as `WP 9.0A`/`WP 9.0B`'s own. |
| WP 9.2A — Engineering Calculations Workspace | Implementation — `v0.9.0`'s own fourth Work Package, and the third to wire a real Engineering Discipline into the real Engineering Workspace — the Calculation Framework (`WP 7.1D`) and its Engineering Domain counterpart (`WP 8.2C`), two already-real frameworks connected to each other and to the Workspace for the first time. Zero Domain-layer changes — `Calculation`/`CalculationSet` were already sufficient. New `CalculationTemplateRegistry` (`ADR-0086`), a Workspace-layer, JSON-marshalled type-erasure adapter making every registered Calculation Template dispatchable through one non-generic Execute/Recalculate command. Ten commands, including a `SetCalculationStatusCommand` that Lock/Unlock/Request Review/Approve/Archive all alias to (`ADR-0087`), since no `IApprovalGate`/`IApproval` implementation exists anywhere in the platform. New `Tempest.App.Workspace.Calculations` namespace, extended by one synthetic, non-Domain `"CalculationTemplate"` Kind. Search needed zero new code. Real Engineering Cockpit Calculations KPIs replace the placeholder card. Representative data: five real engineering calculations (Bolt Shear, Beam Bending, Bearing, Pressure Vessel, Material Selection), one Calculation Set, one calculation-to-calculation dependency chain, real Digital Thread links to the Mechanical and Requirements sample data, one genuine `Conditional` outcome, one calculation revised after execution. Two new Technical Debt items (`TD-29`, `TD-30`, both pre-existing absences, not defects). Two new ADRs (`ADR-0086`, `ADR-0087`). 57 new tests (1808 → 1865). Single academy file, same two-part structure as `WP 9.0A`/`WP 9.0B`/`WP 9.1A`'s own. |
| WP 10.2B — Docking & Workspace Layouts | Implementation — `v0.10.0`'s own sixth Work Package. A professional dockable workspace over the existing `Tempest.Desktop` shell: `WorkspaceDockPosition.Bottom` (a real enum member since `WP 8.0B`, never wired to a dock surface before now) realised via a third `DockingGrid` row hosting a new, fourth `IWorkspacePanel` implementer, `OutputPanel` (live `IDiagnosticsProvider` module/hosted-service status, not a captured log feed — disclosed explicitly, `TD-39`'s own sibling scope note); Collapse (manual, in-place thin-strip shrink) and Auto-Hide (unpinning, handing the dock slot back to the Document Area, click-to-peek/click-away-or-`Escape`-to-dismiss flyout overlay) are both new, deliberately distinct `PanelHostControl` header affordances; three predefined layout presets (Engineering/Review/Documentation, `PredefinedLayouts`) and Reset Layout are both thin `IWorkspaceLayout.SetPlacement`/`ResetToDefault` callers, introducing no new Workspace-layer capability of their own. Saved-layout persistence, restore-on-startup, and persistent panel visibility/splitter positions were **already fully implemented since `WP 8.1A`** (`WorkspaceState`/`ADR-0064`) — this Work Package's own genuinely new Desktop-local state (Collapse/Auto-Hide/Output) persists through a second, sibling `ISettingsProvider` key (`DesktopPanelUiState`), the identical established pattern applied a second time. **Zero changes to any of the twelve frozen `WP8.0B` Workspace contracts** — a stronger claim than `WP 10.2A`'s own one disclosed, additive `IWorkspaceManager` extension; zero Engineering Domain/Runtime files touched, zero new ADRs. One genuine, disclosed, low-severity finding: `PanelHostControl`'s own opaque overlay background is a fixed, non-theme-variant-aware brush, mirroring `CommandPaletteOverlay`'s own identical, previously-unregistered limitation — both surfaced together for the first time (`TD-39`). Also found, disclosed, and named (not fixed, out of scope) a real gap in this very register's own `## 03 Work Packages` table — see this field's own "Last Reviewed" entry above. 28 new tests (29 → 57 `Tempest.Desktop.Tests`; 2040 `Tempest.Core.Tests` unchanged), zero regressions, Debug and Release both clean. Single academy file, same two-part structure as `WP 9.0A` onward. |
| WP 10.3A — Engineering Object Editors | Implementation — `v0.10.0`'s own seventh Work Package. Replaced `DocumentAreaView`'s own three-line placeholder (disclosed future work since `WP 10.0B`) with a real Object Editor Framework: one generic `ObjectEditorView` engine, applied uniformly across all six disciplines' own real Kinds via `EngineeringObjectBase`'s already-uniform facet composition (`ADR-0075`), never six duplicated classes. Two real, dispatched editable fields — Name (`RenameObjectAsync`, `ADR-0096`) and Content (new `ReviseObjectAsync`, `ADR-0097`, a sixth Kind-keyed `IWorkspaceManager` provider category, additive only) — both honouring `ADR-0063` throughout (reads direct, writes through Commands only). Mechanical, the one discipline of six with no Revise command before this Work Package, gets a new `ReviseMechanicalObjectCommand`, closing a genuine, pre-existing asymmetry. Real Validation feedback (`IValidatable.ValidateAsync`, `ADR-0075`) surfaced at the Desktop layer for the first time — informational only, never blocking Save. Real, flat, bidirectional Relationship summary with click-to-navigate rows; real Lifecycle display (Status + recent History); new, Desktop-local dirty-state tracking (`ObjectEditorView.IsDirty`, distinct from and never redefining the permanently-`false` `IWorkspaceView.IsDirty`), reflected in the tab header via `DocumentAreaView`'s own new `MarkDirty` method; Save/Cancel workflow; Read-Only mode. Integrated with the Cockpit and Property Inspector via the identical `ActionCompleted`-triggers-`Refresh()` pattern every prior Desktop View already establishes. One pre-existing test corrected, disclosed (`MechanicalWorkspaceIntegrationTests`' own hardcoded Mechanical-command count, nine → ten, this Work Package's own new `"mechanical.edit"` descriptor). 22 new tests (2040 → 2048 `Tempest.Core.Tests`; 57 → 71 `Tempest.Desktop.Tests`), zero regressions, Debug and Release both clean. One new Technical Debt item (`TD-40`, no close-confirmation on a dirty tab) and one new Future Capability entry (`FCR-0068`, discipline-specific editor enhancements), both disclosed, neither release-blocking. Single academy file, same two-part structure as `WP 9.0A` onward. |
| WP 10.3B — Ribbon, Toolbar & Command Experience | Implementation — `v0.10.0`'s own eighth Work Package. A professional Engineering Ribbon over `Tempest.Desktop`: `RibbonView` groups every real `CommandDescriptor` by `Category` into one tab per discipline — zero per-discipline Desktop code, mirroring `ObjectEditorView`'s own "one engine, six disciplines" precedent a second time. A direct `grep` across this platform's entire history found zero `CommandDescriptor` registrations ever set `CreateDefault`, meaning `ICommandRegistry.InvokeAsync` cannot invoke a single real command by Id alone — rather than generalising the Command Framework (explicitly excluded, "No new command framework"), the Ribbon reuses the three already-Kind-keyed dispatch verbs (`ADR-0096`/`ADR-0097`) for Delete (immediate) and Rename/Edit (routes to the real Object Editor input surface, `WP 10.3A`); every other command honestly reports it cannot be dispatched from a button click alone, rather than silently doing nothing. Command grouping/icons are both deterministic, verb-suffix-derived heuristics (`CommandDescriptor.Icon` is likewise never populated anywhere — confirmed by the same `grep`); real, authored per-command icons remain disclosed future work (`FCR-0069`). Large/small buttons, tooltips, a new seventh Status Bar segment (Hint, real `PointerEntered`/`PointerExited`-driven), a bounded Recently-Used list, and Command Search reusing `CommandPaletteOverlay` directly (zero duplicate search) round out the scope. The old, minimal two-button Toolbar and the standalone Navigation Framework button row are both retired, consolidated into a new Quick Access Toolbar and the Ribbon's own tab-click-switches-area behaviour respectively — disclosed consolidations, confirmed to lose zero existing capability. **Two genuine defects found and fixed before commit**: `CommandPaletteOverlay.InvokeSelectedAsync`'s own silent no-op (pressing Enter on any real command, ever, since `WP 8.1A`, did nothing, with zero feedback — a real, six-Work-Package-latent defect, found while building the Ribbon's own identical honest-failure case, fixed via a new `CommandUnavailable` event); `RibbonView.Rebuild()`'s own missed initial `RefreshEnablement()` call (every selection-aware button defaulted to enabled with no real selection — found by this Work Package's own test suite, before any commit). **Zero files touched under `src/Tempest.Core/` or `src/Tempest.App/Workspace/` at all** — the cleanest layering-compatibility result of any Work Package this release, stronger than `WP 10.2B`'s own "zero of twelve contracts" (this Work Package touches neither layer, not merely zero contract members); zero new ADRs. 10 new tests (2048 `Tempest.Core.Tests` unchanged; 71 → 81 `Tempest.Desktop.Tests`), zero regressions, Debug and Release both clean. Single academy file, same two-part structure as `WP 9.0A` onward. |
| WP 10.4A — Digital Thread Visualisation | Implementation — `v0.10.0`'s own ninth Work Package. A graphical Digital Thread viewer realising `ADR-0093` as real code for the first time: `DigitalThreadGraphModel` (pure, Avalonia-free) constructs a node-link graph via the identical bidirectional relationship read `ObjectEditorView`'s own Relationship summary already established (`WP 10.3A`) — a deliberate choice over `IEvidenceComposer`, which is outgoing-only and whose Verification/Calculation-result enrichment resolves structurally empty today (`TD-30`). Expand/collapse issues a fresh, live, one-hop read every time (never cached or precomputed); collapsing removes exactly what an expansion added unless another still-expanded path keeps a node reachable, via a general fixpoint reachability sweep. Three real, deterministic layouts (Hierarchical, Engineering — concentric rings, Force-Directed — a seeded spring simulation); zoom, pan, a real click-to-pan mini-map, a combined legend/category-filter panel, node search, single-click-opens-editor vs. double-click-re-centres, a Relationship Inspector, and a breadcrumb trail. Rendered as a Document Area tab (`IWorkspaceView` implemented directly) rather than a new dockable panel — `DockingGrid`'s own three slots are all already occupied, and a fourth would itself be a Workspace docking change, out of scope. **Closes the loop on `TD-32`**, exactly as `WP10.0A Digital Thread & Relationship Visualisation.md` §4 asked: a Verification Activity's own `"verifiedBy"` link, invisible to `RelationshipRepository` since `WP 9.3A`, is merged in via `VerificationRecordReader` and rendered as a real, visible leaf node — the first place in the Workspace/Desktop layer this link is shown graphically. In the process, found and disclosed that `ObjectEditorView`'s own Relationship summary shares the identical, previously-unnoticed blind spot. **One genuine defect found and fixed before commit**: `JumpToBreadcrumb`'s own breadcrumb-not-emptying bug (the shared graph-rebuild helper unconditionally re-pushed the centre being left, even on a "back" jump) — fixed via an explicit `pushCurrentCentreToBreadcrumb` parameter. **Zero files touched under `src/Tempest.Core/` or `src/Tempest.App/Workspace/` at all** — matching `WP 10.3B`'s own cleanest layering-compatibility result exactly; zero new ADRs, zero new traversal mechanism. 24 new tests (2048 `Tempest.Core.Tests` unchanged; 81 → 105 `Tempest.Desktop.Tests`), zero regressions, Debug and Release both clean. One new Future Capability entry (`FCR-0070`, dense-graph clustering/pruning, already disclosed and accepted by `ADR-0093` itself). Single academy file, same two-part structure as `WP 9.0A` onward. |
| WP 10.5A — Workspace Visual Polish & Engineering User Experience | Implementation — `v0.10.0`'s own tenth Work Package. A professional visual/UX polish pass across `Tempest.Desktop`: `ApplicationPalette`/`ThemeReactiveBrush` (new) — the platform's own first real theme-reactive brush infrastructure, closing `TD-39` directly (`PanelHostControl`/`CommandPaletteOverlay`'s own fixed-brush overlay backgrounds) and fixing a same-session regression in `DigitalThreadGraphView`'s own hardcoded node colours (`WP 10.4A`). `IconRegistry` Phase 2 — every Kind glyph now a consistent, monochrome, theme-reactive Unicode symbol, replacing a mixed full-colour-emoji set; `IconGeometry` (new) — the platform's first real hand-authored vector icon set, four glyphs, deliberately curated not comprehensive. Four new reusable controls (`ToastHost`/`ToastNotification`, `BusyOverlay`, `ConfirmationDialog`, `EmptyStateView`), each wired to a real consumer — `ConfirmationDialog` closes `TD-40` directly (a dirty Object Editor tab now prompts before discarding unsaved edits). `ObjectEditorView` gained a coloured lifecycle status badge (reusing `LifecycleColors`, `WP 10.4A`) and severity-coded validation rows (`SeverityColors`, new — a fourth colour-language mapping). **Two genuine Avalonia-specific implementation findings**: the conventional `GetResourceObservable`/`Bind` code-behind pattern does not reliably re-push a value once a control that subscribed while unattached is later attached to a real visual tree (fixed via a new `ThemeReactiveBrush` helper resolving directly against `Application.Current`); a process-wide `static bool` registration guard intermittently left a later headless test's own fresh `Application` instance completely unregistered, since Avalonia's own test host does not guarantee one instance per process (fixed by removing the guard). **A third, smaller finding**: a new test touching `StreamGeometry.Parse` as a plain `[Fact]` rather than `[AvaloniaFact]` poisoned its own type for the rest of the test process on failure — fixed by correcting the attribute. **A fourth finding, in `WP 10.4A`'s own test**, found only by this Work Package's own repeated "check for flakes" re-runs: an over-broad assertion intermittently failed depending on `InMemoryEngineeringObjectRepository`'s own unspecified iteration order (`TD-27`'s identical risk class) — fixed by narrowing it to the specific edge `TD-32` names; `WP10.4A`'s own already-published documents deliberately left untouched. Independently assessed and confirmed: no ADR required (`WP10.5A Architecture Review.md` §1), zero Engineering Domain/Runtime/Workspace-contract behaviour changed. `TD-39`/`TD-40` both Resolved; two new Future Capability entries (`FCR-0071`, comprehensive vector icons; `FCR-0072`, split-document view); `FCR-0067` marked Implemented. 25 new tests (2048 `Tempest.Core.Tests` unchanged; 105 → 130 `Tempest.Desktop.Tests`), zero regressions confirmed across two full-suite re-runs, Debug and Release both clean. This platform's own first dedicated Accessibility Review and Technical Debt Review. Single academy file, same two-part structure as `WP 9.0A` onward. |
| WP 10.5B — Desktop Workflow & Professional Interaction | Implementation — `v0.10.0`'s own eleventh Work Package. Professional workflow interactions across `Tempest.Desktop`: a unified, four-dialog Dialog Framework (`InputDialog`/`MessageDialog`/`SettingsDialog`, new, joining `ConfirmationDialog` from `WP 10.5A`), sharing styling via tokens directly rather than a common base class (each dialog's own layout genuinely differs enough that a shared base would cost more than it saves). Real window-geometry persistence (`WindowUiState`, new, mirroring `DesktopPanelUiState`'s own established shape) with a `ClampToVisibleScreen` multi-monitor safety net, applied before the first frame renders. `MainWindow.Closing` is a single, consolidated graceful-shutdown gate — checks `DocumentAreaView.HasAnyDirtyTab`, prompts once if anything is unsaved, then persists both window and panel state before the real close, using the standard async-`Closing` pattern (Avalonia does not await an async `Closing` handler). Delete Confirmation wired once (`ConfirmDeleteAsync`, a single delegate) into all three existing Delete paths (Ribbon, context menu, `Delete` key), gated by the new `UserSettings.ConfirmBeforeDelete`. One real, complete, end-to-end object-creation workflow (`RibbonView.ObjectCreationHandlers`, new) — Mechanical Create (`InputDialog` name prompt → `ICommandDispatcher.DispatchAsync`) and Duplicate (`ConfirmationDialog` only) — deliberately not extended to the other five disciplines' own genuinely different command shapes (`FCR-0075`). `PlatformNotificationToastBridge` (new) gives the pre-existing `IEventBus`/`IPlatformNotification` mechanism its first real Desktop consumer, confirmed by a whole-repository search to have had zero consumers before this Work Package. `UserSettings` (new) gives the platform its first real, separate user-preferences surface (Theme, Toast duration, Confirm-before-delete), completely apart from Engineering data. `MainWindow.ShowUnexpectedErrorAsync`, wired from `App.cs`'s own `TaskScheduler.UnobservedTaskException` handler, gives genuinely unexpected exceptions a real, visible surface — deliberately not wired to the already-fatal `AppDomain.UnhandledException`. **Two genuine findings, both fixed before commit**: `ProjectExplorerView`'s own filter `TextBox.TextChanged` does not reliably fire for a programmatic `.Text =` assignment, the identical class `ObjectEditorView` (`WP 10.3A`) already fixed — found while building Recent Searches, fixed via `PropertyChanged` filtered to `TextBox.TextProperty`; a test-precision gap (an incorrect assumption that `IDeletable.DeleteAsync` removes an object from its repository — it is a deliberate soft delete — plus a `TD-27`-class non-deterministic "first node found" test helper) — both fixed, production code was always correct. Independently assessed and confirmed: no ADR required (`WP10.5B Architecture Review.md` §1-§2), zero Engineering Domain/Runtime/Workspace-contract behaviour changed, zero files touched under `src/Tempest.Core/` or `src/Tempest.App/Workspace/`. Five new Future Capability entries (`FCR-0073`–`FCR-0077`: Copy/Move destination picker, Export/Import commands, uniform Create/Duplicate across all six disciplines, a startup splash screen, customisable shortcuts/Ribbon/toolbar preferences). 23 new tests (2048 `Tempest.Core.Tests` unchanged; 130 → 153 `Tempest.Desktop.Tests`), zero regressions confirmed across two full-suite re-runs, Debug and Release both clean. Single academy file, same two-part structure as `WP 9.0A` onward. |
| WP 10.6A — Command Execution & Productivity Experience | Implementation — `v0.10.0`'s own twelfth Work Package. The professional engineering workflow layer over the existing, unmodified Command Framework: Undo/Redo (`UndoableAction`/`IUndoRedoStack`, new `ADR-0098`) — a Desktop-local delegate stack, not a new `ICommand` contract, wired real and broad for Rename across all six disciplines from `ObjectEditorView`'s own one shared commit path (reusing `RenameObjectAsync`'s own already-Kind-agnostic dispatch, `ADR-0096`) plus the new Favourite/Un-favourite toggle. A Macro foundation (`ICommandMacro`/`IMacroManager`/`RunMacroCommand`, new `Tempest.Core.Macros`, `ADR-0099`) realising a macro as an ordinary registered `CommandDescriptor` — the Command Palette invokes it through the identical, unmodified `ICommandRegistry.InvokeAsync` path every other command already uses, zero macro-specific branching anywhere; a direct `grep` confirmed only `Tempest.Samples` commands are `CreateDefault`-eligible today, the real, disclosed bound on which commands a macro can sequence. An External Controller/Input Binding abstraction (`IInputBindingProvider`/`IExternalControllerProvider`/`IInputBindingRegistry`, new `Tempest.Core.Input`, `ADR-0100`) mirroring `IEventBus`'s own established subscriber-isolation shape — one real `KeyboardCommandBindingProvider` and one test-only `StubExternalControllerProvider`, zero vendor SDK, per this Work Package's own explicit Out-of-Scope. Recent/Favourite Objects (`RecentObjectsState`/`FavouriteObjectsState`, new, mirroring `UserSettings`'s own persistence shape, surfaced as two new `ProjectExplorerView` flyout sections) — deliberately distinct from the still-unbuilt `EngineeringCockpit.FavouriteProjects`. Command History (`CommandHistoryLog`, new) and a Background Task Framework (`IBackgroundTaskRunner`, new, coarse state only, wired real to Macro invocation) both surface as two new `OutputPanelView` sections — no new dock panel. **Zero changes to `Tempest.Core.Commands` and every frozen `WP8.0B` Workspace contract**, confirmed by direct diff — every new capability is additive. **One genuine, pre-existing governance-register drift found and fixed**: `ADR Register.md`'s own Entries table was missing its own `ADR-0097` row (the file existed, the narrative already named it, no table row had ever been added) — added, with the Register's own stale `Total`/`Related ADRs` fields corrected too. Six new Future Capability entries (`FCR-0078`–`FCR-0083`) paired with six new Accepted Trade-offs (`AT-18`–`AT-23`), each disclosing exactly one of this Work Package's own real scope boundaries. 37 new tests (2048 → 2062 `Tempest.Core.Tests`; 153 → 176 `Tempest.Desktop.Tests`), zero regressions, Debug and Release both clean. Single academy file, same two-part structure as `WP 9.0A` onward. |
| WP 10.5C — Commercial User Experience & Application Completion | Implementation — `v0.10.0`'s own thirteenth Work Package by completion order (commissioned and completed after `WP 10.6A` despite its own earlier number — a disclosed numbering note, mirroring `WP 9.3A`'s own identical precedent). A required-first runtime UX audit (`WP10.5C Runtime UX Traceability Matrix.md`) — a real `dotnet run` process launch plus direct source-level reachability tracing of every `WP 10.0B`–`WP 10.5B` claimed feature, honestly disclosing that no screenshot/accessibility-tree tool exists in this environment. **Found no genuinely unreachable feature** — every named capability traces to a real, wired control. **Two genuine `TD-39`-class defects found instead** — `CockpitCardControl`'s own neutral border and `RibbonView.BuildSectionWithLabel`'s own group divider, both a fixed `Brushes.Gray` since `WP 10.1A`/`WP 10.3B` respectively, both fixed via `ThemeReactiveBrush.Bind` — the fourth and fifth real instance of this exact defect class this project has now found and closed. `DisciplineColors` (new, `Tempest.Desktop.Theming`) — a fifth colour-mapping class, keyed on the real `CommandDescriptor.Category` string (confirmed exhaustive against all six real registered disciplines by direct `grep`), applied to real, coloured Ribbon tab accents. The existing `LifecycleColors` mapping (`WP 10.4A`) newly applied to two further surfaces — real, coloured lifecycle-status dots on Project Explorer object nodes and Property Inspector lifecycle rows — via two additive, defaulted trailing parameters (`ProjectExplorerNode.Lifecycle`, `CockpitKpiCard.PercentValue`) on data records the Workspace layer already owned, zero extra reads. Real `ProgressBar` rendering for the Cockpit's own four existing coverage KPIs, coloured by the same Healthy/Attention/Blocked thresholds `HealthColors` already established. **Disclosed, not attempted**: Object Editor discipline-aware layouts (`FCR-0068`, reconfirmed still open — a Kind→Discipline colour mapping was considered and rejected as a real risk of encoding a wrong mapping with no way to visually verify it here); Requirements' own tree nodes receive no lifecycle dot (`RequirementStatus` is a genuinely distinct taxonomy from `LifecycleState`, not force-mapped); Digital Thread received no code changes (reviewed directly, confirmed already real and complete since `WP 10.4A`/`WP 10.5A`). Zero Workspace contract changes, zero new ADRs, zero files touched under `src/Tempest.Core/`. 8 new tests (2062 → 2064 `Tempest.Core.Tests`; 176 → 184 `Tempest.Desktop.Tests`), zero regressions, zero flakes confirmed across a targeted repeat re-run, Debug and Release both clean. Single academy file, same two-part structure as `WP 9.0A` onward. |
| WP 12.3A — Fault Injection & Validation Framework Architecture | Architecture — `v0.12.0`'s own first commissioned Work Package, directly commissioned outside `WP11.0B Architecture Roadmap.md`'s own predicted `12.0`–`12.2` slots (mirroring `WP 11.3A`/`WP 11.4A`'s own precedent). Traces the real production composition path (`EngineeringWorkspaceComposer.Build()` → unrestricted `TempestHostBuilder()` → `WorkspaceHost`) to find a genuine, previously-undisclosed defect: `DuplicateNavigationSampleModule`, a deliberately-always-failing module proving `ModuleLifecycleManager`'s isolation (ADR-0013), was discovered and initialised on every real `Tempest.App`/`Tempest.Desktop` launch, permanently leaving one module `ModuleState.Failed` — confirmed directly via `ModuleLifecycleStabilityTests.cs`'s own pre-existing special-case exclusion. Designs `ADR-0102`: a new project, `Tempest.Validation` (chosen over the brief's own suggested `Tempest.Diagnostics` naming specifically to avoid colliding with `IDiagnosticsProvider`/`DiagnosticsSampleModule`), plus a default-excluded discovery-time marker (`IFaultInjectionModule`) — project isolation alone found insufficient, since `AppDomain`-wide reflection scanning is process-wide, not reference-scoped. Architecture only; no code. |
| WP 12.3B — Fault Injection & Validation Framework Implementation | Implementation — realises `WP 12.3A`'s design unchanged, with one disclosed addition found during implementation: `Tempest.Validation` also references `Tempest.Samples` (downward, no cycle), so the moved module can reference `NavigationSampleModule.NavigationItemId` directly rather than duplicating it as a second string literal. `DuplicateNavigationSampleModule` moved, renamed `DuplicateNavigationModule`; `IFaultInjectionModule` (new marker interface); `ReflectionFrameworkDiscoveryService`/`ITempestHostBuilder`/`TempestHostBuilder` each gain one small, additive, defaulted member. The actual defect closed, verified directly: `ModuleLifecycleStabilityTests.cs`'s special-case exclusion deleted, not merely updated — a real `WorkspaceHost` now genuinely reaches `Running` with zero modules `Failed`. Zero behavioural change for any existing caller; full regression re-verified directly both configurations, 2,233/2,233 passing (2,026 → 2,031 `Tempest.Core.Tests`, net +5; 202 `Tempest.Desktop.Tests` unchanged in count, tightened in assertion), 0 Warnings/0 Errors. |
| WP 12.0A — Desktop Composition Root Decomposition Architecture | Architecture — `v0.12.0`'s own third completed Work Package, the roadmap's own first predicted `v0.12.0` item (`WP11.0B Architecture Roadmap.md` §3), realising Finding `A-1`. Re-verifies `MainWindow.cs` (1,556 lines, a ~1,000-line constructor) and `EngineeringCockpit.cs` (1,398 lines, a flat six-discipline read-model) unchanged since `WP11.0A`. Designs `ADR-0103` — "Composition Roots Own Collaborators," a general, platform-wide pattern (not scoped to either file) extending `ADR-0009`'s own composition-root principle one layer down: composition-root/collaborator responsibilities, ownership/lifetime, construction/dependency rules, and an explicit prohibition on DI-registering an extracted collaborator (reusing `ADR-0013`'s own classification test); rejects both a `partial class` split (`ADR-0017`'s own structural-over-conventional precedent) and a declarative/reflective composition framework (`ADR-0032`'s own "no genuine extensibility trigger" precedent). `Desktop Composition Architecture.md` records the pattern with `MainWindow`/`EngineeringCockpit` as motivating, non-binding illustrative evidence — the real, final collaborator boundaries are `WP 12.0B`'s own Implementation Report. Architecture only; no code. |
| WP 12.0B — Desktop Composition Root Decomposition Implementation | Implementation — `v0.12.0`'s own fourth completed Work Package, closing Finding `A-1` in full. Implements `ADR-0103` exactly, refactor only: zero public API changes, zero behavioural changes, zero new Platform Services, zero new DI registrations, no `partial class`. `MainWindow.cs` (1,556 → 544 lines; ~1,000 → ~370-line constructor) decomposes into nine `Tempest.Desktop.Composition` collaborators — `DesktopCompositionRoot`, `DesktopSessionState`, `WorkspaceDockingComposer`, `RibbonObjectActionHandlers` (the ~450-line, ~29%-of-file per-discipline handler population, the single largest, most mechanical extraction), `WorkspaceViewCoordinator`, `UndoRedoCoordinator` (now fully reactive via `IUndoRedoStack.Changed`, replacing several explicit refresh call sites with one), `MainMenuFactory`/`QuickAccessToolbarFactory` (stateless static build functions — `ADR-0103`'s own named "pure construction" collaborator shape), `WorkspaceLayoutPresetCoordinator`. `EngineeringCockpit.cs` (1,398 → 575 lines) decomposes into six per-discipline read-model collaborators, one per existing `Tempest.App.Workspace.{Discipline}` namespace; genuinely cross-discipline aggregation (`Health`, `KpiCards`, `AttentionItems`, `OpenActions`, `BlockedItems`) and the Governance & Risk family (Decisions/Risks/Milestones/Tasks — none of the six named disciplines) stay directly on the composition root, per `ADR-0103`'s own Composition-root Responsibility #4 — a real, disclosed judgement call, not a gap in the decomposition (the illustrative sketch in `Desktop Composition Architecture.md` was explicitly non-binding for exactly this reason). Every collaborator `new`-constructed, never DI-registered, never a direct reference to a sibling collaborator — two genuine construction-order cycles (`DocumentAreaView`/`CockpitView` needing `WorkspaceViewCoordinator`'s own `BuildDocumentContent`/`NavigateToObject`, which in turn need them back) resolved via the identical "field assigned after construction, read lazily by a deferred delegate" two-phase sequencing the pre-decomposition source already used — one collaborator boundary away, not a new pattern; `UndoRedoCoordinator`'s equivalent `CockpitView` cycle resolved the same way. 12 new characterization tests added before their own respective extractions (5 `EngineeringCockpit`; 7 `MainWindow` Menu System/Quick Access Toolbar), both real, confirmed-by-direct-search coverage gaps, not speculative additions. Full Debug+Release regression: 2,245/2,245 passing, 0 Warnings/0 Errors, both configurations. No new ADR; `ADR-0103` unmodified — no factual correction was exposed by implementation. |
| WP 12.4A — Desktop Command & Event Wiring Architecture | Architecture — a second directly-commissioned `v0.12.0` Work Package (`WP 12.3A`/`WP 12.3B`'s own precedent), reviewing the Desktop layer's own command/event/UI-wiring architecture after `WP 12.0B`'s decomposition, and formally evaluating how Desktop collaborators should communicate going forward. Catalogues every event in `Tempest.Desktop` directly (25, all plain `Action`/`Action<T>` delegates; zero unsubscription, zero `IDisposable` anywhere — found safe today, not merely unexamined, since exactly one `MainWindow` is ever constructed per process, `ADR-0103`'s own "Ownership and lifetime" section already establishing why). Documents four distinct, already-individually-ADR-governed command mechanisms (`ICommandRegistry`/`ICommandDispatcher`, `RibbonView.ObjectCreationHandlers`, `KeyboardShortcuts`, `KeyboardCommandBindingProvider`) now composing together for the first time in one visible place, confirming none is redundant. Finds and quantifies real duplication — a four-statement "report via StatusBar/Toast, then refresh Explorer/Cockpit" tail repeated 42/25/25/19 times respectively across six files, concentrated in `RibbonObjectActionHandlers` and `WorkspaceViewCoordinator` — named as a real implementation-hygiene opportunity for a future `WP 12.4B`, deliberately not designed in full here. Re-confirms `WorkspaceViewCoordinator` as the strongest remaining decomposition candidate (`WP 12.0B`'s own architecture review Finding 4, unchanged, not rediscovered). Evaluates six candidate cross-collaborator communication mechanisms on their own individual merits (advantages/disadvantages/layering/ownership/lifetime/testing each): direct delegates, typed callback interfaces, a Desktop-local Mediator, a Desktop-local Command Dispatcher, a Desktop-local Event Dispatcher, and reuse of the existing platform Command/Event Framework. **`ADR-0104` produced** — direct delegates remain the default; typed callback interfaces sanctioned narrowly, at three or more bundled callbacks (directly answering `WorkspaceViewCoordinator`'s own 18-parameter constructor); the Mediator, a second Command Dispatcher, and a second Event Dispatcher are each explicitly rejected as Desktop composition mechanisms, on mechanism-specific grounds, not merely inferred from `ADR-0103`'s more general text — meets Engineering Governance §5's own ADR-creation criteria, applying `ADR-0103`'s own principles more specifically rather than replacing or narrowing them (this Work Package's own first-pass conclusion of "zero new ADR" was revised in the same Work Package once its own fuller brief's evaluation was carried out — disclosed directly, not silently corrected). **Disclosed naming correction**: initially proposed under the label `WP 12.1A`, already the roadmap's own name for an unrelated item (Classification & Relationship Vocabulary Safety Net); corrected before any document was written. Architecture only; zero `src/`/`tests/` files touched, confirmed directly. |
| WP 12.4B — Desktop Command & Event Wiring Implementation | Implementation — a third directly-commissioned `v0.12.0` Work Package, on the same branch as `WP 12.4A` (`feature/v0.12.0-desktop-command-event-wiring`), realising `ADR-0104` exactly as approved. Three characterization tests added before any refactor (`MechanicalCreate_OnARealPart_ActuallyCreatesARealObject`, `MechanicalDuplicate_OnARealPart_ActuallyCreatesARealCopy`, `DocumentsApprove_OnAFreshDraftDocument_IsRejectedByRealValidation_NeverThrows`), closing two real, confirmed-by-direct-search gaps: the entire Mechanical discipline had zero direct Ribbon-handler test coverage anywhere, and the shared `statusHandler` factory's own failure branch (report `result.Message`, skip refresh) had never been exercised by any existing test. **Two ADR-0104-compliant changes, both direct delegates, neither a typed callback interface.** Before this Work Package, `WorkspaceViewCoordinator`'s own genuine, delegate-shaped callback-parameter count was 2 (`refreshStatusBar`, `recordHistory`) — below `ADR-0104`'s own 3-callback threshold. This Work Package introduces a third callback, `refreshCockpit`, bringing the count to 3 and reaching that threshold; despite reaching it, introducing a typed callback interface in the same change that first crosses the threshold was assessed and deferred as an explicit, disclosed engineering judgement — not applied speculatively — even though `WorkspaceViewCoordinator` is `ADR-0104`'s own named motivating example: (1) `RibbonObjectActionHandlers`'s own 16 duplicated "report via StatusBar/Toast, then refresh Explorer/Cockpit" tails (`WP 12.4A`'s own quantified finding) consolidated into one local function, `ReportAsync`, defined inside the existing constructor — pure extract-method, zero new abstraction, zero constructor-signature change; every handler's own success/failure message text preserved unchanged, and `mechanical.create`/`mechanical.duplicate`'s own previously-double-computed message ternary normalised to compute once, matching the other 14 sites. (2) `WP 12.0B`'s own architecture review Finding 5 closed: `UndoRedoCoordinator` and `WorkspaceViewCoordinator` both previously carried a two-phase-constructed `CockpitView` object reference (a nullable field plus an `Attach`/`AttachCockpitView` method) purely to call its own `Refresh()` — replaced in both with a plain `Action refreshCockpit` constructor parameter, wired from `MainWindow` via the identical field-closure lazy-capture pattern already established there for `DocumentAreaView` (`() => _cockpitView!.Refresh()`, safe because `_cockpitView` is `readonly` and the lambda is only ever invoked after construction fully completes). `WorkspaceViewCoordinator`'s own two-phase `Attach` now takes only `DocumentAreaView` — the one genuine remaining construction-order cycle; `UndoRedoCoordinator`'s own `AttachCockpitView` method is removed entirely, since no cycle remained once the delegate replaced the reference. **Zero Desktop mediator, Desktop-local command dispatcher, or Desktop-local event dispatcher introduced; the platform Command/Event Framework left untouched** — all four exactly as instructed. `ADR-0103`'s ownership rules preserved throughout: every collaborator remains `new`-constructed by `MainWindow`, never DI-registered, never referencing a sibling collaborator directly. All public APIs preserved; behaviour verified identical, not merely asserted unchanged — the three new characterization tests (plus the full pre-existing suite) pass against both the pre-refactor and post-refactor code, byte-for-byte the same assertions. Full Debug+Release regression: 2,248/2,248 passing (2,031 `Tempest.Core.Tests` + 217 `Tempest.Desktop.Tests`, net +3 from the new characterization tests, zero regressions), 0 Warnings/0 Errors, both configurations. No new ADR — `ADR-0104` unmodified, implemented as designed with no factual correction exposed. |
| WP 12.1A — Classification & Relationship Vocabulary Safety Net Architecture | Architecture — the roadmap-defined Work Package (`WP11.0B Architecture Roadmap.md` §3, `A-6`), on its own new branch (`feature/v0.12.0-classification-relationship-vocabulary-safety-net`), realising `WP11.0A Platform Architecture Review.md` Finding `A-6` ("Domain classification/relationship vocabulary is entirely stringly-typed, by deliberate design, with no compile-time safety net"). Audits every Kind/`Classification`/`RelationshipKind` vocabulary across `Tempest.Core`/`Tempest.App`/`Tempest.Desktop`. Confirms five prior ADRs (`ADR-0072`/`ADR-0073`/`ADR-0076`/`ADR-0088`/`ADR-0091`) already, independently, settle the open-string-vs-closed-type question — none reopened — while `ADR-0090` provides direct evidence `Classification`-tagging is not universally the right sub-typing axis (Verification deliberately reuses `LifecycleState` instead). Finds the confirmed gap is precisely bounded, not platform-wide: four `Tempest.Core` frameworks and two Workspace disciplines (Documents/Manufacturing, `ADR-0088`/`ADR-0091`) already declare their own vocabulary values as named constants; Mechanical declares none. Quantifies real, present duplication — the literal Kind string `"Part"` written out 14 times across 5 files with no shared constant, and a confirmed cross-layer duplicate (`Tempest.Desktop.DigitalThread.DigitalThreadGraphModel`'s own `VerifiedByRelationshipKind` independently redeclaring `Tempest.Core.Verification.VerificationService`'s own identical constant). Finds three already-shipped, independently-proven instances of the eventual safety-net shape (`RequirementRelationshipKinds`, `RelationshipKindCategoryMap`, `DocumentCategory.Of`/`ManufacturingCategory.Of`) and generalises them. **`ADR-0105` produced**: every live vocabulary value declared once, as a named constant, on its owning class; a new Engineering Vocabulary Register tracks every value platform-wide; one additive xUnit test (not a new tool) catches exact-value collisions and register/code drift — never validation at write time, every prior ADR's own "never validated" guarantee left completely intact. Four alternatives evaluated and rejected on real evidence: a closed enum (already rejected five times independently), a strongly-typed value object, a runtime DI-resolved registry service (the identical "misclassifies a coordination concern as a Platform Service" reasoning `ADR-0104` just applied to Desktop wiring), and a fully free-form metadata model. Digital Thread roadmap compatibility confirmed low-risk directly — `DigitalThreadGraphModel` is fully Kind-agnostic except for its own two hand-typed local duplicates, the confirmed defect above. Architecture only; zero `src/`/`tests/` files touched, confirmed directly. `WP 12.1B`'s own minimum scope named directly: populate the Engineering Vocabulary Register, add the consistency test, retrofit the two confirmed defects. |
| WP 12.1B — Classification & Relationship Vocabulary Safety Net Implementation | Implementation — realises `ADR-0105` exactly, on the same branch as `WP 12.1A`. Every `FactoryRegistry` retrofitted: `MechanicalObjectFactoryRegistry` (the largest confirmed gap) gains all eight Kind constants (`Project`/`Assembly`/`SubAssembly`/`Part`/`Component`/`Configuration`/`Baseline`/`Release`), used internally throughout; `DocumentObjectFactoryRegistry`/`ManufacturingObjectFactoryRegistry` gain their own base Kind constants alongside their already-disciplined `Classification` sub-values; `CalculationObjectFactoryRegistry` gains `CalculationKind`/`CalculationSetKind`. `DigitalThreadGraphModel`'s own confirmed duplicate closed — found, while directly reading the file, to be three local constants, not one (`VerificationActivityKind`/`VerificationRecordKind`/`VerifiedByRelationshipKind`), all now referencing their real owning constants (`VerificationActivityFactoryRegistry.SupportedKind`; `VerificationService.VerificationRecordDocumentKind`/`VerifiedByRelationshipKind`). New Engineering Vocabulary Register (`docs/governance/Engineering/`) populated from a full repository scan — 46 declared values across 11 declaring classes, plus 7 further conventional values honestly marked Undeclared; one genuine, pre-existing dual-ownership case found and disclosed (`references`, independently, legitimately owned by both `RequirementRelationshipKinds` and `VerificationService`), not treated as a defect, mirroring `ADR-0073`'s own already-accepted vocabulary-drift risk. New `EngineeringVocabularyConsistencyTests` (`Tempest.Desktop.Tests`, per the corrected `WP 12.1A` architecture review) — four tests covering register/code drift, cross-owner duplication, register self-consistency, and a rogue-duplicate scan across all four layer assemblies; **verified to actually work**, not merely assumed — a rogue duplicate was deliberately, temporarily reintroduced mid-implementation, confirmed to fail the test with a precise message, then reverted. Three characterization tests added before refactoring (`MechanicalCommandsTests`'s own `Configuration`/`Baseline`/`Release` `InlineData` cases — a real, confirmed gap `WP 12.1A`'s own grep-based estimate had missed entirely, since the existing coverage used `[Theory]`/`[InlineData]`, invisible to a literal-string grep). Full Debug+Release regression: 2,255/2,255 passing (2,034 `Tempest.Core.Tests` + 221 `Tempest.Desktop.Tests`, net +7 new tests, zero regressions), 0 Warnings/0 Errors, both configurations. No new ADR — `ADR-0105` unmodified, implemented as designed. |
| WP 12.2A — Presentation Strategy Execution Architecture | Architecture — the roadmap-defined Work Package (`WP11.0B Architecture Roadmap.md` §3), on its own new branch (`feature/v0.12.0-presentation-strategy-execution`), scoped as "Presentation Strategy Execution (realises `WP 11.2A`'s decision)." Discovery (Phase A) finds `WP 11.2A` itself was never commissioned as predicted — its label was reused for Governance Health-Check Automation instead — and the real Desktop & Console Presentation Strategy Decision (`WP11.0A` finding `A-2`) was ratified and executed under a different pair, `WP 11.3A` (review, `Complete`) and `WP 11.3B` (implementation, `Complete`), both in `v0.11.0`, before `v0.12.0` even branched. Every claim independently re-verified directly against live source, not carried forward: `TempestShell`/`IPage`/`PlaceholderPage` confirmed retired (zero matches anywhere in `src/`/`tests/`); `ADR-0101` confirmed Accepted and unmodified, formally classifying `Tempest.App`/`WorkspaceShell` as TempestOS's Internal Engineering Harness, not a shipped product; `README.md`/`Contributor Learning Path.md` confirmed corrected; `ci.yml`/`release.yml` confirmed packaging `Tempest.Desktop` and `Tempest.App` as two separate, clearly-labelled artifacts; `Shell & Composition Framework Architecture.md` confirmed already carrying the final, current state via its own `WP 11.3B` addendum. `WP11.3A`'s own five-stage roadmap's Stage 5 ("further trimming of `WorkspaceShell`'s own test coverage or feature scope") was deliberately not executed by `WP 11.3B` — confirmed, not assumed, to be a formal, disclosed deferral (`WP11.9.0 Engineering Release Report.md`'s own "Product Debt... deferred by design" entry), gated on "a real, demonstrated cost problem" that a direct search of both the Technical Debt Register and the Future Capability Register confirms has still not occurred. Phase B (Architectural Analysis) evaluates four disposition alternatives (execute Stage 5 speculatively; re-run the investigation from scratch; formally close as delivered, adopted; leave the Status silently stale) and concludes, honestly, neither a new ADR nor a new/updated architecture document is justified — Engineering Governance §5's own ADR-creation criteria is not met (no new either/or choice exists to record), and `Shell & Composition Framework Architecture.md` already documents the current state correctly. Phase C's own conclusion is disposition, not new design: `docs/releases/v0.12.0/WorkPackages.md`'s own `WP 12.2A` row updated from "Not started — scope set by `WP 11.2A`" to "Delivered by `WP 11.3A`/`WP 11.3B` — no remaining scope," mirroring this same register's own directly-precedented disposition pattern for the sibling `WP 11.1B`/`WP 11.2A` renumbering case, above. Architecture only; zero `src/`/`tests/` files touched, confirmed directly; no ADR produced. |
| WP 12.9.0 — Release Preparation & Engineering Sign-Off Architecture | Architecture — the final roadmap-defined `v0.12.0` Work Package, on its own new branch (`feature/v0.12.0-release-preparation-and-signoff`). Phase A repository assessment, independently re-derived from source, not carried forward: `main` state confirmed via `git log` (`WP 12.0A`/`12.0B`, `12.1A`/`12.1B`, `12.3A`/`12.3B`, `12.4A`/`12.4B` all merged; `WP 12.2A`'s own disposition complete but not yet committed — its branch tip is byte-identical to `main`'s own, its changes existing only as an uncommitted stash); Debug+Release builds and full regression freshly re-run (0 Warnings/0 Errors both, 2,255/2,255 passing both); `scripts/governance-healthcheck.ps1` run directly (3 passed, 1 warned, 4 failed of 8 — one genuine, previously-undisclosed, `v0.12.0`-caused gap found: `Engineering Vocabulary Register.md` never linked from `Governance Index.md`; the remaining failures are the Academy Index's own large, pre-existing gap and mostly tool-level path-check false positives); Technical Debt (48 entries) and Future Capability (84 entries) registers both independently re-derived by direct count, matching their own claimed totals exactly, nothing newly blocking. Phase B designs the permanent Engineering Readiness Review: five categories (Architecture/Implementation/Verification/Governance/Release readiness), each with defined required evidence, pass criteria, and blocking conditions, reviewed by the proven six-discipline Programme Review (`WP 10.9A`/`WP 11.9.0`'s own shape, formalised rather than replaced) — plus two genuinely new pieces neither prior sign-off had: a written blocking taxonomy (Release Blocking / Disclosed Non-Blocking / Pre-Existing Unaffected) and a fixed, four-value verdict vocabulary (`CERTIFIED`; `CERTIFIED WITH ACCEPTED TECHNICAL DEBT`; `ACCEPT WITH OBSERVATIONS`; `NOT READY`). **`ADR-0106` produced** — three alternatives evaluated and rejected (the ad hoc status quo; a single-reviewer sign-off; a purely automated gate), each on this project's own concrete evidence, most directly `WP 11.9.0`'s own independently-found `TD-42`/`TD-43` as the counter-evidence against collapsing multi-discipline review. **Found and fixed, this Work Package's own Governance-readiness review**: `Architecture Document Register.md`'s own `Classification & Relationship Vocabulary Safety Net Architecture.md` row was still marked Designed despite `WP 12.1B` having implemented `ADR-0105` and merged it into `main` — corrected directly, plus that register's own stale `Related ADRs` field. This is explicitly not release engineering — no verdict is rendered for `v0.12.0` itself; that is a distinct, later, separately-commissioned execution. **Same-day architecture review and follow-up, before any commit**: found and closed two blocking findings — the ADR Register's own Entries table missing `ADR-0106`'s row, and an internally contradictory relationship between category status, the blocking taxonomy, and the verdict vocabulary (`CERTIFIED WITH ACCEPTED TECHNICAL DEBT` was, as originally written, an unreachable verdict) — resolved with the minimum change making all four verdicts reachable, mutually exclusive, and objectively derivable; `governance-healthcheck.ps1` re-confirmed `[PASS]` for the ADR Register after correction (105/105). Architecture only; zero `src/`/`tests/` files touched, confirmed directly. |
| WP 12.9.3B — Governance Health Check Consistency Remediation | Implementation — a narrowly-scoped fix to `scripts/governance-healthcheck.ps1` itself, correcting a checker-architecture inconsistency `WP 12.9.3A`'s own review found (not a repository defect): `Test-ProjectStatusReferences` and `Test-ReleaseRegisterMatchesTags` both consume `Get-RepoTags`, but only the latter treated an empty tag list (git missing, not a repository, or a shallow checkout — confirmed directly against a real GitHub Actions run) as a disclosed environmental limitation (`Warn`); the former treated it as a content defect, producing a false `Fail` against every real historical version token whenever tags happened to be unavailable. Fix scoped to exactly the sub-check that depends on tag availability — version-token validation skips with a disclosed `Warn` when zero tags are available; path-reference validation, which never depended on tags, continues unconditionally and can still fail the check on its own. `Get-RepoTags`, `Test-ReleaseRegisterMatchesTags`, and every other check left unmodified. Four behavioural cases verified directly against isolated fixtures (never the real repository): tags available (unchanged `Pass` behaviour); tags unavailable with a genuine missing path (still `Fail`); tags unavailable with no path issues (`Warn`, no false `Fail`); Release Register behaviour unchanged throughout. Full Debug+Release regression re-run: 2,255/2,255 passing both, 0 Warnings/0 Errors both — expected, since this change touches only a PowerShell governance script, no `src/`/`tests/` file. |
| WP 13.0A — Plugin & Registration Trust Isolation Architecture | Architecture — `v0.13.0`'s own first roadmap-predicted Work Package (`A-3`/`FCR-0001`), on `feature/v0.13.0`. Two composed architecture documents, from parallel sibling workstreams, integrated into one coherent package: `docs/architecture/Plugin Platform Architecture.md` (manifest v2 — `Dependencies`, `RequestedCapabilities`, `Publisher`, `Signature`; dependency graph resolution; a new Host-owned `IPluginRegistry` projected read-only via `IDiagnosticsProvider.Plugins`; lifecycle load/upgrade/uninstall, no live unload; DI boundaries unchanged — `ADR-0107`–`ADR-0109`) and `docs/security/Plugin Trust & Isolation Architecture.md` (a four-tier trust model; a capability model extending `IPermissionEvaluator` via a new `ICurrentComponentAccessor`; a detached SHA-256 signature verified at Plugin Discovery; the isolation-boundary decision — capability-scoped, not `AssemblyLoadContext`, not process separation; a trust-ordered registration rule — `ADR-0110`–`ADR-0112`). Directly resolves, at the design level, `TD-09`/`TD-10`/`TD-11` (`ADR-0044`'s own deferred retrofit) and closes `TD-06`/`FCR-0010` (configurable plugins root) at the design level. Six new ADRs, nineteen new Rejected Designs entries (`RD-0046`–`RD-0064`). `02 Runtime Architecture/07-plugin-architecture.md` extended in place (new Trust Boundary section, the already-implemented pipeline's own Mermaid sequence diagram) rather than a new article — Engineering Governance §6's "fit into an existing document first" discipline applied directly; `03 Work Packages/WP6.1-permissions-and-identity-implementation.md` §12 Future Evolution updated in place with a connecting note (not rewritten) — `ADR-0044` itself is Accepted/historical and was not edited. Architecture only; zero `src/`/`tests/` files touched, confirmed directly (`git status`). `TD-09`/`TD-10`/`TD-11` move from "mechanism exists, not yet applied" to "design resolved, retrofit remains open until `WP 13.0B`" — stated plainly, not overclaimed. |
| WP 13.0B — Plugin & Trust Isolation Architecture Review and Baseline | Governance/Process — an independent review/verification Work Package auditing `WP 13.0A`, on `feature/v0.13.0`. Three parallel, genuinely independent audit sub-agents (Architecture Audit; Governance Audit; Documentation Audit) — none of which authored any `WP 13.0A` material — each read-only, each reporting file-and-line findings rather than editing anything themselves. Architecture Audit: PASS, one blocking fix (a mis-cited failure-category range) and one non-blocking citation gap (`ADR-0111` now cites `ADR-0032`/`ADR-0037`, whose already-Accepted registration behaviour it additively revises). Governance Audit: FAIL, two blocking fixes (`ADR Register.md`'s own separate `Total: 104 ADRs` paragraph, stale independently of the fields `WP 13.0A` had already corrected; a governance Brief's own closing verdict left asserting incompleteness after its underlying findings were already resolved). Documentation Audit: FAIL, one blocking fix (the `WP 13.0A` retrospective's own §12 falsely claimed `Host Lifecycle.md`/`Failure Behaviour.md` were untouched, when both carry real, already-merged "Extended, `WP 13.0A`" content — corrected to state the true documentation-vs-code distinction). Every fix applied was a defect `WP 13.0A` itself introduced; nothing pre-existing was touched or re-flagged (`Rejected Designs Register.md`/`Governance Index.md`'s own long-stale counts, already disclosed by `WP 13.0A` as out of scope, confirmed untouched here too). `governance-healthcheck.ps1` re-run clean after every fix: 7 passed, 1 warned, 0 failed, exit 0 — identical shape to `WP 13.0A`'s own closing baseline. No new ADR, no new architecture, no broadened scope. Zero `src/`/`tests/` files touched. `WP 13.0A`'s and this Work Package's combined material committed as a single architecture baseline immediately following. |
| WP 13.1A — Plugin Runtime & Composition Root Implementation | Implementation — the real Trust Isolation Implementation successor `WP 13.0B`'s own row disclosed as "not yet commissioned, and does not yet have a number," commissioned and assigned the `WP 13.1A` label directly by the Product Owner, colliding with — and repurposing — that same label's own roadmap-predicted "REST API Authentication & TLS Architecture" row (disclosed, not silently reconciled; see `WorkPackages.md`). Four parallel workstreams built the mechanical, non-trust half of `WP 13.0A`'s design (`ADR-0107`–`ADR-0109`) only: the fixed-point dependency-graph resolution (missing/incompatible-version removal, three-colour cycle detection, repeat-until-stable, Kahn's-algorithm topological sort) and three new isolated failure categories; `PluginRegistry`/`IPluginRegistry`/`IPluginRegistryRecorder` (Host-owned, never DI-public — `ADR-0017`); `Runtime:Plugins:RootDirectory`/`ManifestFileName`/`Disabled` wired in `TempestHost.cs`; `IDiagnosticsProvider.Plugins` backed by a direct (non-`Func<T>`) registry reference. 45 new test methods; Debug/Release both 0 Warnings/0 Errors; full regression Debug/Release 2,305/2,305 passing; zero genuine production-code defects found at the time. Closed `TD-06`/`FCR-0010`; `TD-09`/`TD-10`/`TD-11` (trust) untouched, exactly as scoped. **Found and fixed by `WP 13.1B`'s own review, not this Work Package**: this register's own `## 03 Work Packages` header/Total counts were updated to 108/173 for this Work Package's own retrospective, but its table row itself — this row — was never actually added; added here directly rather than left as a further, silent gap. |
| WP 13.1B — Plugin Runtime & Composition Root Implementation Review | Governance/Process — an independent review of `WP 13.1A`, mirroring `WP 13.0B`'s own review-before-baseline discipline, applied to real production code for the first time. Four parallel, genuinely independent audit sub-agents (Code Review; Architecture Compliance; Verification & Test Audit; Governance & Documentation Audit) — none of which authored any `WP 13.1A` material, each instructed to assume defects exist until personally disproven. Architecture Compliance: PASS, zero blocking findings, confirmed `ADR-0107`–`ADR-0109` conformance point-by-point and the `ADR-0017`/`ADR-0109` DI-boundary (`PluginRegistry` never registered into the container). Code Review: FAIL, **two genuine, empirically-confirmed production bugs** — a duplicate-dependency-entry bug in `PluginManifestDiscoveryService.TopologicalSort` silently dropping a valid plugin with zero diagnostic trace, and a blank-configured-value bug in `TempestHost.cs` faulting the entire Host instead of falling back to default — both fixed at their exact root cause. Verification & Test Audit: independently converged on the same two gaps via test-completeness judgment, plus correctly diagnosed a genuinely pre-existing, unrelated `CompositeLogSinkTests` flake (confirmed non-reproducible on rerun, not a `WP 13.1A` regression) and two non-blocking coverage gaps in `DiagnosticsProviderTests.cs`, both closed. Governance & Documentation Audit: PASS, zero defects, twenty specific claims fact-checked. Eight new regression tests added closing every real gap found. `governance-healthcheck.ps1` re-run clean: 7 passed, 1 warned, 0 failed. Debug/Release both 0 Warnings/0 Errors; full regression, both configurations, 2,313/2,313 passing (2,305 + 8 new). No new ADR, no new architecture, no broadened scope. `WP 13.1A`'s and this Work Package's combined material committed as a single reviewed implementation baseline immediately following. |
| WP 13.2A — Plugin Trust & Capability Enforcement Implementation | Implementation — the trust-half implementation successor to `WP 13.1A`/`WP 13.1B`'s own mechanical half, implementing `ADR-0110`–`ADR-0112` exactly as `WP 13.0A` designed and `WP 13.0B` independently audited them, zero architectural deviation. Trust tier assignment and detached SHA-256/RSA-PSS signature verification (`PluginSignatureEnvelope`, `PluginSignatureVerifier`) run entirely within the existing Plugin Discovery phase, from file bytes, before any `Assembly.LoadFrom` call; a new `Plugins:AllowUnsignedLoad` configuration key defaults to `false`, fail-closed. Capability enforcement (`PluginAssemblyLoader.EnforceTrust`) checks `RequestedCapabilities` against trust-tier ceiling and reflects over discovered `IModule` constructors for baseline/grant conformance, denying at category 17 (`PluginTrustDeniedException`, `PluginRegistryState.TrustDenied`, the sixth registry state) before `Loaded`. A new `ICurrentComponentAccessor`/`CurrentComponentAccessor` (`AsyncLocal<T>`-backed, mirroring `ICurrentPrincipalAccessor`'s shape, a second identity axis per `ADR-0111`) is pushed around each module lifecycle call by `ModuleLifecycleManager`'s new, plugin-unaware `componentScopeProvider` hook. `NavigationService`, `CommandRegistry`, `CommandHandlerTable`, and `IEventBus` each gained a trust-ordered registration/capability-gated-publish retrofit — a contested Id resolved by trust-tier rank, `NavigationService.Unregister` now performs a real ownership check, every check skipped for `null`/First-Party actors, zero behavioural change for every registrant that exists today. **Directly closes `TD-09`/`TD-10`/`TD-11`** (Open → Resolved) and implements `Security Roadmap.md` items 1, 2, 10. 15 new + 11 modified production files; 2 new test files (`PluginSignatureVerifierTests.cs`, 15 methods; `PluginSigningTestHelper.cs`) plus 31 modified — 23 pre-existing fixtures needed a two-line `ICurrentComponentAccessor`/`IPermissionEvaluator` dual-registration update, the direct consequence of this platform's DI container requiring every constructor parameter type independently resolvable. One genuine found-and-corrected test-fixture finding, never a production defect: `PluginManifestV2FieldsTests.cs`'s own placeholder `Signature` string, previously stored-and-forwarded verbatim, now correctly fails envelope parsing once `AssignTrustTier` actively verifies it — corrected to omit the field and pass `allowUnsignedLoad: true`. Confirmed absent from this Work Package's own diff: certificate chains, online revocation, a marketplace, remote trust services, a REST API, authentication, authorisation beyond plugin capabilities, dynamic trust changes, live plugin reload. No new ADR — `ADR Register.md`'s own Originating Work Package field for `ADR-0110`–`ADR-0112` updated to cite this Work Package's implementation alongside `WP 13.0A`'s own design, mirroring `ADR-0105`'s own precedent. |
| WP 13.2B — Plugin Trust & Capability Enforcement Review | Governance/Process — an independent review of `WP 13.2A` by four parallel read-only sub-agents, on `feature/v0.13.0`, committed with it as `25df570`. Fixed a Critical privilege-escalation path (`NeverEligibleServiceResolveTypes`), a blocking missing component-scope push in `CommandHandlerTable.DispatchAsync`, and a Medium `IsFirstParty` exemption-guard inconsistency across four registries; two disclosed-not-fixed findings were later registered as `TD-49`/`TD-50` by `WP 13.3A`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.3A — Plugin Platform Integration & End-to-End Validation | Governance/Process (this workstream) — the plugin platform's own closing integration/validation pass for `v0.13.0`, run alongside three parallel technical sub-agents (End-to-End Integration; Fault Injection & Resilience; Performance & Scalability, each scoped to `src/`/`tests/`, none touched by this workstream). Independently re-verifies `ADR-0107`–`ADR-0112` fully implemented against real, current source — not any prior Work Package's own citation — confirming `TD-09`/`TD-10`/`TD-11` remain genuinely Resolved. Checked `WP 13.2B`'s own two disclosed-but-unfixed findings (a Discovery-to-Loading TOCTOU window; filename-based, not certificate-attribute-based, first-party trust-store detection) directly against each ADR's own literal Decision text: neither is an ADR conformance gap (`ADR-0112` promises verification "before any assembly is loaded," satisfied literally; it never promises re-verification at the load call site, and never commits to attribute-based first-party detection) — both are real, disclosed engineering debt, formally registered for the first time as `TD-49`/`TD-50`. Four new Future Capability entries raised (`FCR-0085`–`FCR-0088`: certificate-chain validation/revocation checking; per-plugin collectible-`AssemblyLoadContext` hot/live reload and unload; process-separated marketplace isolation; a per-plugin `AllowUnsignedLoad` allow-list), each named out of scope directly by `ADR-0108`/`ADR-0110`/`ADR-0112` and by `WP 13.2A`'s own Future Evolution section, none previously given a roadmap-facing entry. **Found and fixed, this Work Package's own independent audit**: `ADR-0107`/`ADR-0108`/`ADR-0109`'s own Originating Work Package field in `ADR Register.md` still read "WP 13.0A (architecture only)" despite `WP 13.1A` having implemented all three — `WP 13.2A`'s own citation fix reached only the sibling `ADR-0110`–`ADR-0112` row; corrected here to the same two-phase citation shape. **Disclosed, not fixed**: `WP 13.2B` (Plugin Trust & Capability Enforcement Review) has no Academy retrospective, `WorkPackages.md` row, or `PROJECT_STATUS.md` entry of its own — its only prior trace in this project's governance documentation was one citation inside the Technical Debt Register's own narrative; named here as a real, pre-existing gap this Work Package's own `docs/`-only scope did not attempt to backfill with an unverified, reconstructed retrospective. Zero `src/`/`tests/` files touched. This retrospective's own §12 ("What the Three Parallel Technical Sub-Agents Validated") is explicitly written from independently-verifiable ADR/code inspection alone, since the three parallel sub-agents' own results were not yet available when it was written — flagged directly, not silently presented as complete. |
| WP 13.3B — Plugin Platform Integration & End-to-End Validation Review | Governance/Process — an independent review of `WP 13.3A` by four parallel, genuinely independent audit sub-agents (Architecture & Integration Review; Security & Reliability Review; Verification Review; Governance Review), instructed to assume defects exist until disproven. Architecture & Integration Review and Security & Reliability Review independently converged on a genuine, blocking registry-Id-spoofing defect in `WP 13.3A`'s own `InvalidPluginManifestException.PluginId` fix (`DuplicatePluginIdException` fires only for candidates that pass complete manifest validation, so a manifest failing on any other field could carry its own self-declared, uniqueness-unverified `Id` into `PluginRegistryEntry.Id`, spoofing a genuine, unrelated, already-`Loaded` plugin's registry entry) — fixed, the declared `Id` now surfaced only as free text inside `Detail`, never as the structured `Id`; a dedicated regression test proves the spoof is no longer possible. Verification Review independently re-verified the `TopologicalSort` performance claim and added further regression tests (the commit message's own two paragraphs disclose an internal count inconsistency, 15 vs. 12, not resolved by this register); also flagged, not fixed, that four distinct signature failure causes collapse to one generic `Detail` message. Governance Review fixed one stale `Academy Register.md` count and reconfirmed `TD-49`/`TD-50`/`FCR-0085`–`FCR-0088`. Committed together with `WP 13.3A` as a single commit (`88e41a2`) — the direct reason this Work Package's own governance footprint (this row, its retrospective, its `PROJECT_STATUS.md` entry) went missing until `WP 13.9.1` closed the gap, the most-corroborated finding in `WP13.9.0 Engineering Release Report.md` (found independently by four of that review's six disciplines). **This retrospective and this row were reconstructed directly from `88e41a2`'s own commit message by `WP 13.9.1`**, independently cross-checked against real, current source wherever directly checkable (`InvalidPluginManifestException.PluginId`'s own use confirmed by direct source read; the regression test confirmed present by name). Also discloses, rather than silently repeats, that `WP13.9.0`'s own Implementation-readiness review (Finding F2) searched the repository directly and found no "20,000-trial differential fuzz harness" corresponding to Architecture & Integration Review's own claim of that name. `governance-healthcheck.ps1`: 7 passed, 1 warned, 0 failed. Debug/Release both 0 Warnings/0 Errors; full regression both configurations 2,519/2,519 passing (2,298 `Tempest.Core.Tests` + 221 `Tempest.Desktop.Tests`). No new ADR; no new Technical Debt beyond what `WP 13.3A` itself raised. |
| WP 13.9.0 — `v0.13.0` Engineering Readiness Review | Verification/Governance — the release-readiness review opening the `WP 13.9.x` chain, returning a **NOT READY** verdict and producing `docs/releases/v0.13.0/WP13.9.0 Engineering Release Report.md`. Its Security/Trust discipline empirically demonstrated, against the compiled binary, that `EnforceTrust` scanned only the one manifest-declared assembly. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.9.1 — `v0.13.0` Readiness Remediation | Implementation/Governance — closes `WP 13.9.0`'s governance and documentation findings, including reconstructing `WP 13.3B`'s own missing Academy retrospective directly from commit `88e41a2`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.9.2 — `v0.13.0` Readiness Re-Execution | Verification/Governance — a fresh re-execution of the readiness review against the remediated state. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.9.3 — Multi-Assembly Trust-Boundary Remediation | Implementation — forces constructor-parameter `ParameterType` resolution inside the scan's own before/after `AppDomain` diff window, so an assembly reachable only through a constructor parameter can no longer escape trust checking. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.9.4 — Trust-Denial Execution Boundary Remediation | Implementation — makes trust denial an execution boundary rather than a bookkeeping outcome: a Host-owned `IPluginDeniedTypeRecorder`/`IPluginDeniedTypeRegistry` plus two `TempestHost` filters at Module Registration and Hosted Service Registration; broadened mid-Work-Package, by its own Adversarial Review, to cover `IHostedService` as well as `IModule`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.9.5 — `v0.13.0` Trust & Security Final Adversarial Review | Verification/Governance — found that `WP 13.9.4`'s registration-time filters, though correct, act too late: an unattributed denied module was still constructed during Module Discovery itself. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.9.6 — Module Discovery Trust Boundary Remediation | Implementation — closes that gap with one optional, generic `Func<Type, bool>? isTypeExcluded` predicate on `ReflectionFrameworkDiscoveryService`, wired by `TempestHost` to the unmodified `WP 13.9.4` registry, preserving `ADR-0110`'s plugin-unawareness at the type-reference level; verified non-vacuous by mutation testing. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.9.7 — Trust Boundary Integration & Commit | Governance/Process — sole integrating agent, no parallel writers; reconciled, independently verified and committed the entire `WP 13.9.1`–`WP 13.9.6` chain as one commit, `d7d19d4`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.10A — Plugin Platform Hardening Architecture | Architecture/Security — a read-only hardening review finding two genuine, live-PoC-confirmed gaps in `ADR-0111`'s own enforcement mechanism, raising `TD-51` and `TD-52`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.10B — Plugin Trust Hardening Implementation | Implementation — closes `TD-51`/`TD-52`: `HasCompliantConstructor` and the never-eligible denylist now run against `hostedServiceTypes` as well as `moduleTypes`, and `HostedServiceManager` gained an optional `componentScopeProvider` hook mirroring `ModuleLifecycleManager`'s established `ADR-0111` hook; its own Adversarial Security review found and fixed a compounding regression mid-implementation, before commit. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.10C — Plugin Trust Hardening Review & Integration | Governance/Process — four fresh read-only reviewers, each with its own live proof-of-concept, non-vacuousness confirmed by reverting the fix and observing the targeted tests genuinely fail, then restoring; committed the `WP 13.10A`/`WP 13.10B` chain as `79e453f`. Its characterisation of `TD-51`'s residual gap as safely isolated was later found false for the `IModule` axis by `WP 13.11A`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.11A — Plugin Platform Final Hardening Architecture Review | Architecture/Security — read-only, six parallel disciplines; **reopened `TD-51`** (the "unresolvable constructor parameter type" residual gap genuinely crashes the whole Host on the `IModule` axis) and raised `TD-53`/`TD-54`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.11B — TD-51 Trust-Denial Crash Remediation | Implementation — closes reopened `TD-51` (`8341438`): the denial is surfaced to `EnforceTrust` through an `out PluginTrustDeniedException?` so `RecordDenied` runs before the throw, and the fixed-point scan is allowed to **complete** rather than abort, deliberately declining `WP 13.11A`'s own recommended partial-list shape, which would have traded a Host crash for a silent trust bypass. Added `TD-55`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.11C — TD-51 Remediation Review & Trust-Boundary Verification | Architecture/Security — found that `WP 13.11B`'s completed-fixed-point-scan decision, the security-critical half of that fix, had **no regression coverage at all** (reverting it left all 2,561 tests green); closed with one new test and one new builder helper, proven non-vacuous in both directions, and corrected a factually wrong inherited "three other call sites" claim. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.11D — `v0.13.0` Plugin Platform Exit Review | Governance/Process — a read-only exit review across six disciplines, declaring **READY FOR WP14** with zero `src/`/`tests/` files changed; added `TD-56` (a plugin's own constructor executes outside its component scope, so plugin code runs during construction with a `null` ambient principal every capability gate treats as first-party — **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support**) and extended `TD-34`. Retrospective backfilled by `WP 13.12.0`. |
| WP 13.12.0 — `v0.13.0` Academy Retrospective Completion | Governance/Process — closes the release-blocking Academy completeness finding: sixteen of `v0.13.0`'s twenty-five delivered Work Packages had shipped with no retrospective, against Engineering Governance §6's unconditional requirement. All sixteen backfilled to the 13-section template from primary evidence; the exemption claimed twice in `PROJECT_STATUS.md` shown to cite a non-existent rule and corrected, originals retained. |
| WP 13.12.1 — `v0.13.0` Engineering Readiness Re-Execution | Verification/Governance — re-ran the Engineering Release Readiness Review mechanically under `ADR-0106` §4 across six read-only disciplines, reporting committed `HEAD` and working tree separately. **NOT READY** for both; two disputed classifications adjudicated centrally against the governing text. Changed no file of any kind. |
| WP 13.12.2 — `v0.13.0` Release Documentation Closure | Governance/Process — produced `WP13.12.2 Engineering Release Report.md` (this release's authoritative readiness record, superseding `WP13.9.0`'s) and `Release Notes.md`; added the `WP 13.12.x` rows, retrospectives, and register entries; corrected eight documentation-drift findings by annotation while deliberately preserving pre-existing debt. Zero `src/`/`tests/` files touched. |
| WP 13.12.3 — `v0.13.0` Commit Preparation & Final Branch Audit | Governance/Process — six read-only audits over the uncommitted `WP 13.12.0`–`13.12.2` documentation set; determined the `ADR-0110` amendment needed no architectural approval, and found and fixed eight defects that documentation pass had itself introduced before committing `f58b475`. |
| WP 13.12.4 — VERSION 0.12.0 → 0.13.0 | Implementation — the release-time version bump, isolated in its own sequential commit (`30b0f45`) so the pre-tag release candidate is a single minimal diff; byte format preserved. |
| WP 13.12.5 — PR & Main-CI Release Gate | Governance/Process — stopped at a tooling boundary rather than inventing CI evidence, then merged PR #1 under explicit Product Approval as `6089a218`; real CI green on `main` at that exact pre-tag commit cleared the release's sole §4 Release Blocking finding. |
| WP 13.12.6 — `v0.13.0` Tag & Release | Implementation — ran `new-release.ps1` without `-Push`, creating annotated tag `15aedec` → `6089a218`, verified independently rather than trusting the script's own banner (`TD-42`). |
| WP 13.12.7 — `v0.13.0` Tag Push & Release Completion | Implementation — pushed only the tag; `release.yml` then failed at `Test (Release)`, so packaging and publication were skipped and no GitHub Release or asset exists. Stopped without amending, retagging, or retrying. |
| WP 13.12.8 — `v0.13.0` Release Test Failure Investigation | Verification/Governance — four parallel read-only agents converging on a genuine intermittent test flake, not a product defect; `ci.yml` passed while `release.yml` failed on the same SHA, image and minute. Local reproduction attempted and failed, disclosed. |
| WP 13.12.9 — Desktop Async Test Determinism Remediation | Implementation — replaced a fixed `Task.Delay(50)` with the bounded repository poll `TD-46` established, test-only, one method, `+28/−2` (`7449756`); deliberately not generalised to the other 28 delays. |
| WP 13.12.10 — `v0.13.0` Release Register Closure | Governance/Process — added `v0.13.0`'s row as *tagged and merged, GitHub Release not published*, restoring `governance-healthcheck.ps1` to its pre-tag-push baseline. Documentation-only (`ea3fe07`). |
| WP 13.13.0 — `v0.13.0` Release Failure Disposition & `v0.13.1` Planning | Architecture/Security — analysis only, changing no file; established that re-tagging is barred by §7.4, that `WP 11.4B`'s exception cannot reach this case, and that a new patch version is the only permitted remedy. |
| WP 13.13.1 — `v0.13.1` Release Preparation | Governance/Process — prepares `v0.13.1`, the corrected and publishable form of `v0.13.0`'s content, on branch `release/v0.13.1` cut from `ea3fe07`. Bumps `VERSION`, authors the release notes, adds the `v0.13.1` Release Register row as a fourth "In preparation" status, and brings `WorkPackages.md` up to date with the ten Work Packages delivered since `WP 13.12.2`. `v0.13.0`'s tag left immutable per §7.4. Zero production or test-code changes. |
**Note.** `WP 4.4C` produced no code and no separate retrospective — its
story is told inside the `WP 4.4` architecture retrospective's own
Background section (Verified — this is stated explicitly in that
document and in `Academy Index.md`). `WP 4.4F` (Academy & Documentation
Baseline Audit) is tracked as `docs/academy/Academy Audit Report.md`
itself, below, rather than as a `03 Work Packages/` retrospective — its
own deliverable *is* the audit report.

## 04 Design Patterns (5 articles)

| # | Article |
|---|---|
| 01 | The Registry Pattern |
| 02 | Descriptor and Snapshot Types |
| 03 | Minimal Interface, Extension-Method Sugar |
| 04 | Reflection-Based Discovery (expanded WP 4.5 — third real-world application) |
| 05 | Phantom-Type Dimension Safety (new, WP 7.1B — this platform's first phantom-type pattern) |

## 05 Case Studies (5 articles)

| # | Article | Paired ADR |
|---|---|---|
| 01 | Why RuntimeModule Is Immutable | ADR-0001 |
| 02 | Why Lifecycle State Lives Externally | ADR-0002 |
| 03 | Why Dispose Is Always Legal | ADR-0004 |
| 04 | Why Discovery Is Isolated | ADR-0008 |
| 05 | Why Isn't Configuration Mutable? | (general principle, no single paired ADR) |

## 06 Engineering Standards (5 documents)

See `Engineering Standards Register.md` for the detailed index — not
duplicated here. Grew from 3 to 4 documents during `WP 4.5A` (which added
`03-governance-registers.md`), and from 4 to 5 during `WP 4.5B` (which
added `Engineering Lifecycle.md`).

## Top-Level Academy Meta-Documents (4)

| Document | Purpose |
|---|---|
| `Academy Index.md` | Reader-facing navigable table of contents |
| `Academy Masterclass Roadmap.md` | Candidate long-form synthesis subjects, none yet written |
| `Academy Audit Report.md` | `WP 4.4F`'s own audit deliverable |
| `Contributor Learning Path.md` | Repository-wide onboarding sequence for a new contributor, added `WP 4.5B` |

**Total, corrected `WP 13.12.0`, independently re-derived by direct file
count** (`find "docs/academy/03 Work Packages" -iname "*.md" | wc -l`
→ 132; `find docs/academy -iname "*.md" | wc -l` → 207 — sixteen retrospectives backfilled by `WP 13.12.0`, plus three added by `WP 13.12.2` for `WP 13.12.0`/`13.12.1`/`13.12.2` themselves, since §6 applies to every Work Package including the ones closing this finding; nineteen files, incrementing both counts by exactly nineteen, not carried forward from any prior arithmetic). `WP 13.12.0` (v0.13.0 Academy Retrospective Completion)
closed the Release Blocking Academy-completeness finding `WP 13.12.0`'s
own release-gate review raised: sixteen of this release's own twenty-five
delivered Work Packages — `WP 13.2B`, `WP 13.9.0`–`WP 13.9.7`,
`WP 13.10A`–`WP 13.10C`, and `WP 13.11A`–`WP 13.11D`, the whole
trust-boundary remediation chain and the security substance of
`v0.13.0` — had shipped with no retrospective at all, against
Engineering Governance §6's unconditional "created for every future work
package". Each was reconstructed from primary evidence (its own commit
message, its own `WorkPackages.md` row, `PROJECT_STATUS.md`'s retained
strata, the Technical Debt Register, and direct reads of real source),
using the full 13-section template, with every figure attributed to its
source and every uncorroborated claim disclosed rather than resolved.

**Disclosed, pre-existing and deliberately not closed here.** This
`## 03 Work Packages` table carries **117** rows against **132** files on
disk — a fifteen-row deficit that predates `WP 13.12.0` and is unchanged
by it (the table was 98 rows against 113 files immediately before `WP 13.12.0`; nineteen rows were added for exactly nineteen new files across `WP 13.12.0` and `WP 13.12.2`, leaving the deficit unchanged at fifteen).
`WP 13.0.0` and `WP 13.0.0A` are among the files with no row. That gap is
a register-coverage issue, not an Academy-completeness one — every file
it concerns exists — and closing it was outside this Work Package's own
commissioned scope, which was the sixteen missing retrospectives
themselves. Recorded here rather than left for a future reader to
rediscover.

**Previous total, corrected `WP 13.9.1`, independently re-derived by direct file
count** (`find "docs/academy/03 Work Packages" -iname "*.md" | wc -l`
→ 113; `find docs/academy -iname "*.md" | wc -l` → 178 — `WP 13.3B`'s own
retrospective, reconstructed directly from its commit message
(`88e41a2`) and added here for the first time, one file, incrementing
both counts by exactly one, not carried forward from any prior
arithmetic).

**Previous total, corrected `WP 13.3A`, independently re-derived by direct file
count** (`find "docs/academy/03 Work Packages" -iname "*.md" | wc -l`
→ 112; `find docs/academy -iname "*.md" | wc -l` → 177 — this Work
Package's own retrospective, one file, incrementing both counts by
exactly one, not carried forward from any prior arithmetic).

**Previous total, corrected `WP 13.2A`, independently re-derived by direct
file count** (`find "docs/academy/03 Work Packages" -iname "*.md" | wc -l`
→ 111; `find docs/academy -iname "*.md" | wc -l` → 176 — this Work
Package's own retrospective, one file, incrementing both counts by
exactly one, not carried forward from any prior arithmetic).

**Previous total, corrected `WP 13.1B`, independently re-derived by direct
file count** (`find "docs/academy/03 Work Packages" -iname "*.md" | wc -l`
→ 110; `find docs/academy -iname "*.md" | wc -l` → 175 — not carried
forward from any prior arithmetic): `WP 13.1A` had, in fact, added its
own retrospective file to disk, but its own Governance & Documentation
pass never incremented this register's counts at all for it — this
paragraph and the header above both still read `WP 13.0B`'s own
108/173 figures until this Work Package's independent count caught the
gap (a genuine, two-file undercount — `WP 13.1A`'s own file plus this
Work Package's own — not the one-file gap a naive "add one for this
retrospective" edit would have produced; disclosed plainly, corrected
directly). 1 (Introduction) + 11 (Engineering Principles) + 31 (Runtime
Architecture) + 110 (Work Packages) + 5 (Design Patterns) + 5 (Case
Studies) + 8 (Engineering Standards) + 4 (top-level meta) = 175 files
under `docs/academy/` —
re-verified by direct `find`/`ls` count against every subfolder
individually, and independently re-summed against the seven subfolder
counts themselves (they add up exactly, both ways). **Disclosed
correction, `WP 9.1A`**: this figure had read "117" (Work Packages "68")
since at least `WP 8.9.0`, never updated across `WP 9.0A`/`WP 9.0B`
despite each adding one new Work Package file (69, then 70) — the
"118 → 119 total" and "119 total" figures the "Last Reviewed" field
itself already correctly stated for those two Work Packages were never
carried into this summary block, an undercount of the same shape `WP
5.4`'s own correction below already found once before. Re-derived
directly from the file system, not carried forward. Subfolder list
(`WP 6.1`, `WP 6.4`, `WP 6.5`, `WP 6.2`, `WP 6.0`, `WP 6.3`, `WP 6.7`,
`WP 6.6`, `WP 6.8`, `WP 7.0A`, `WP 7.0B`, `WP 7.0C`, `WP 7.1A`, `WP 7.1B`,
`WP 7.1C`, `WP 7.1D`, `WP 7.1E`, `WP 7.1F`, `WP 7.2A`, `WP 7.2B`,
`WP 7.2C`, `WP 7.3A`, `WP 7.4.0`, `WP 8.0A`, `WP 8.0B`, `WP 8.1A`,
`WP 8.0C`, `WP 8.1B`, `WP 8.1C`, `WP 8.2A`, `WP 8.2B`, `WP 8.2C`,
`WP 8.9.0`, `WP 9.0A`, `WP 9.0B`, `WP 9.1A`), not just the grand total,
continuing the standing practice `WP 5.4`'s own repository review
recommended. `02
Runtime Architecture/17-engineering-workspace.md` (introduced by
`WP 8.0A`) was updated in place five times — by `WP 8.0B`, `WP 8.1A`,
`WP 8.0C`, `WP 8.1B`, then `WP 8.1C` — none of the five counted as a new
file. `02 Runtime Architecture/18-engineering-domain-architecture.md`
(introduced by `WP 8.2A`) was updated in place twice, by `WP 8.2B` then
`WP 8.2C` — neither counted as a new file.
`02 Runtime Architecture/18-engineering-domain-architecture.md`
(`WP 8.2A`) is a genuinely new concept guide, counted as new.
**Correction, `WP
5.4`**: the "03 Work Packages" count had read "33" (`WP 5.2`) then "34"
(`WP 5.3`) while the section's own table already listed 34 and then 35
rows respectively — the stated count had undercounted its own table by
one for at least two consecutive Work Packages, and the "76" grand
total inherited the same undercount rather than being independently
re-derived. Both figures are now re-verified directly against the real
file system, not carried forward from the previous register's own
arithmetic.**

## Governance Maintenance Check (Engineering Governance §6)

Every Work Package from `WP 2.1` onward produced or updated Academy
documentation as part of its own delivery — **Verified** by direct
correspondence between `Feature Register.md`'s Work Package list and this
register's own "03 Work Packages" table: no Work Package that shipped
code or an ADR is missing a retrospective, and no retrospective exists
for a Work Package that never happened. No stale "Future Evolution"
prediction was found still describing a since-resolved gap as open — the
most recent example checked directly, Background Services' own
"designed, not yet implemented" markers across `02 Runtime Architecture/
05-the-runtime-host.md`, `06-platform-layering.md`, and
`08-failure-isolation.md`, were updated to reflect implementation as part
of `WP 4.5`'s own delivery.

## Cross-Reference Check

Every article above is cross-checked directly against
`docs/academy/Academy Index.md`'s own listing — no article exists in one
but not the other, and `Academy Index.md`'s own section headings
(Welcome, Learning Path, Engineering Principles, Platform Architecture,
Runtime, Dependency Injection, Modules, Plugins, Events, Background
Services, Design Patterns, Engineering Governance, Case Studies, Work
Package Walkthroughs, Reference Material) match this register's own
category grouping one-for-one.
