# Release Register

## Register Metadata

| Field | Value |
|---|---|
| **Register Name** | Release Register |
| **Purpose** | The index of every TempestOS release (tagged or in progress), its scope, and its documentation status. |
| **Scope** | Every version referenced under `docs/releases/` or the root `VERSION` file. |
| **Owner** | Project Maintainer. |
| **Source of Truth** | `docs/releases/`; the root `VERSION` file; git commit history. |
| **Review Frequency** | Updated at every release tag, and whenever release-scoped planning documents change materially. |
| **Last Reviewed** | 2026-08-28 (`WP 14.0A`, TempestOS Companion — Mobile Companion Application) — `v0.14.0` added as **In preparation**, opening the `v0.14.x` train with the Companion as its first Work Package. No tag exists and `VERSION` deliberately remains `0.13.1` until release preparation (`WP 13.12.4` precedent). Total 14 → 15 versions referenced; released count unchanged at 12. No historical row touched. Previously reviewed 2026-08-18 (`WP 13.13.2`, `v0.13.1` Final Release & Closure) — `v0.13.1` moved **In preparation → Released, published, merged to `main`**, closing the `v0.13.x` release train. Tag `b647e2c` → `6eca5fde`, verified locally and against the remote peel; `release.yml` run `32158618270` succeeded end to end and published the GitHub Release with both required assets, independently confirmed via `gh release view` rather than inferred from workflow success. Released count 11 → 12. `v0.13.0`'s own row is untouched and its tag remains immutable at `6089a218` — superseded in content, never in tag. Previously reviewed 2026-08-18 (`WP 13.13.1`, `v0.13.1` Release Preparation) — `v0.13.1` added as **In preparation**, the corrected, publishable form of `v0.13.0`'s own content, prepared on branch `release/v0.13.1` cut from `ea3fe07`. `v0.13.0`'s own row is left exactly as `WP 13.12.10` wrote it: its tag stays immutable at `6089a218`, unamended and unretried, per Engineering Governance §7.4, and `WP 11.4B`'s tag-position exception is explicitly out of reach here (bounded to a tag's mechanical position, and to the window before the release branch closes). Total 13 → 14 versions referenced; a fourth status category added — "in preparation" — rather than overloading "released". No historical row rewritten. Previously reviewed 2026-08-18 (`WP 13.12.10`, `v0.13.0` Release Register Closure) — `v0.13.0` added, closing the same check that first surfaced `v0.12.0`'s own omission: `governance-healthcheck.ps1` reported `[FAIL] Git tag(s) with no Release Register row: v0.13.0` the moment `WP 13.12.7` pushed the tag. Recorded deliberately as **Tagged and merged to `main` — GitHub Release not published**, not as "Released, published" like `v0.11.0`/`v0.12.0` above: the tag-triggered `release.yml` run failed at its `Test (Release)` step, so packaging and publication never ran and no GitHub Release or asset exists for this version. Documentation-only; no tag created, amended or repointed, no `VERSION` change, no `Release Notes.md` change, zero `src/`/`tests/` files touched. Previously reviewed 2026-08-13 (`WP 13.0.0A`, Release Register Reconciliation) — `v0.12.0` added: found missing by `governance-healthcheck.ps1`'s own Release Register check (`[FAIL] Git tag(s) with no Release Register row: v0.12.0`), first surfaced running that check on `feature/v0.13.0` (`WP 13.0.0`) — a pre-existing gap dating to `v0.12.0`'s own release/close, confirmed via `git show 13a6ce3:"docs/governance/Delivery/Release Register.md"` to already be missing at the exact commit the tag itself points to, not introduced by any later Work Package. All eleven real git tags (`v0.3.0`–`v0.12.0`) independently re-verified present in the Entries table exactly once each, cross-checked directly against `git tag -l "v*"` and each tag's own resolved commit/date via `git rev-list -n1`/`git log -1`, not carried forward from any prior figure; `v0.1.0`/`v0.2.0` reconfirmed to remain the same two disclosed non-tag exceptions this register has always carried (`v0.1.0` cited by commit, `v0.2.0` genuinely Unknown), neither newly investigated this pass — their own status is unchanged and was not this Work Package's own subject. Total corrected 11 → 12 versions referenced (10 → 11 confirmed released). Governance/documentation only; no `src/`/`tests/` file touched, no architecture, implementation, or release behaviour changed — confirmed directly. Previously reviewed 2026-08-11 (`WP 11.4B`, Merge to `main` & Release Process Correction) — `v0.11.0`'s own row corrected: tag now cites `4b1fb16` (the merge commit `main` actually resolves to), not `fda4172` (the pre-merge feature-branch commit the tag briefly, incorrectly, pointed to) — see the row's own full account of the one-time governance exception this correction required. Previously reviewed 2026-08-11 (`WP 11.9.0`, `v0.11.0` Release Preparation & Engineering Sign-Off) — found, on independent re-verification, carrying **zero row at all for `v0.11.0`** despite that release being seven Work Packages deep at the time of review — corrected directly: `v0.11.0` added as "In progress — sign-off complete, not yet tagged," the first non-Released/non-Unknown status this register has ever carried, reflecting that tagging remains a Product Approval action not yet taken as of this review. This register's own "Last Reviewed" field itself last pointed to `v0.10.0`'s own close — an eighth recurrence of the exact drift pattern `FCR-0005` exists to prevent, now itself corrected in the same review that also fixed `FCR-0005`'s own status field (see `Future Capability Register.md`). Previously reviewed 2026-08-11 (`WP 10.9A`, `v0.10.0` Release Candidate & Engineering Sign-Off) — **major, disclosed drift found and corrected**: this register had gone unreviewed since `v0.6.0` (2026-07-30) despite three further releases genuinely tagging in the interim — `v0.7.0`, `v0.8.0`, `v0.9.0` all confirmed Released directly against real git tags (`git tag --list`, `git show <tag>`) during this Work Package's own re-verification-from-source discipline, not assumed from any prior document. All three added below; `v0.10.0` added as the release this Work Package is actively gating (see its own row for the live pass/fail state). This is the second time this exact register has gone stale across multiple releases without a correcting review (first disclosed for `v0.5.0`, `WP 6.0`-era) — a recurring risk pattern, not a one-off; `FCR-0005` (Governance Register Health-Check Tooling) is directly, repeatedly evidenced by this finding. Previously reviewed 2026-07-30 (`v0.6.0` Release Engineering) — `v0.5.0` corrected from "in progress" to Released (had gone unreviewed since `WP 5.0D`, missing its own final three Work Packages and tag); `v0.6.0` added as Released; `v0.7.0` added as not yet scoped. |
| **Related Documents** | `docs/releases/FOUNDATION.md`; `docs/releases/v0.5.0/ReleasePlan.md`, `WorkPackages.md`; `docs/releases/v0.6.0/WorkPackages.md`, `WP6.8 Platform Certification Report.md`; `docs/releases/v0.7.0/WorkPackages.md`; `docs/releases/v0.4.0/WorkPackages.md`, `CHANGELOG.md`, `Risks.md`, `ReleaseChecklist.md`; `Feature Register.md`; `Validation Register.md`. |
| **Related ADRs** | None directly — releases are delivery milestones, not architectural decisions. |
| **Related Academy Articles** | See `Academy Register.md` for the retrospectives each release's Work Packages produced. |
| **Coverage Status** | Partial. |

---

## Coverage Note

Marked **Partial**, not Complete, because two referenced versions
(`v0.1.0`, `v0.2.0`) have **Unknown** or only partially reconstructable
detail — see each entry below. This is disclosed explicitly rather than
worked around.

## Entries

| Version | Status | Evidence | Detail |
|---|---|---|---|
| v0.1.0 | Released (pre-Claude era) | Commit `e4fcd58`, "v0.1.0 Repository Stabilisation", 2026-07-21 | **Unknown** full scope — this commit is the last of the 5 pre-Claude commits; no `docs/releases/v0.1.0.md` exists to describe it in detail. |
| v0.2.0 | **Unknown** | `docs/releases/v0.2.0/` directory exists but is empty | No release notes, no commit message referencing "v0.2.0" was found in `git log`. **Unknown** whether this version was ever actually released, skipped, or reserved for a purpose not yet realised. See `Documentation Register.md`'s own note on this same gap. |
| v0.3.0 | Released | `docs/releases/v0.3.0.md`; commit `f2176d7`, "Prepare v0.3.0 release" | "Runtime Foundation Complete" — the six platform services (Configuration, Logging, Discovery, Registration, Dependency Injection, Lifecycle) plus the Runtime Host, Composition Root, and startup/shutdown orchestration. 164 tests at this baseline (per `docs/releases/v0.4.0/Testing.md`'s own stated starting point). |
| v0.4.0 | **Released, 2026-07-27** | `docs/releases/v0.4.0.md`; `docs/releases/v0.4.0/Release Notes.md` | **"Platform Foundation"** — rescoped, during Release Engineering, from its own original, broader plan down to exactly the Foundation-phase scope: Platform Contracts, Module SDK, Plugin Manifest, Sample Module, Event Bus, Background Services, the Governance Register Baseline, and the Foundation phase closeout (`WP 4.5B`). Navigation, Command Framework, Diagnostics, and Developer Experience were part of this release's original scope list but are formally deferred to the next milestone, not shipped here (see `docs/releases/v0.4.0/ReleasePlan.md`'s "Scope" section and `Feature Register.md`). 355 tests, 0 failures. |
| v0.5.0 | **Released** | `docs/releases/v0.5.0/Release Notes.md`; tag `v0.5.0` | **"Developer Experience"** — renumbers `v0.4.0`'s own deferred `WP 4.6A`–`WP 4.9` as `WP 5.0A`–`WP 5.3` (see `ReleasePlan.md`'s "A Note on Renumbering"), and grew a new `WP 5.0C`/`WP 5.0D` pair beyond that original scope once `WP 5.0C`'s own Repository Investigation confirmed `Tempest.App` still had no composition root, plus `WP 5.0S` (Security Baseline Audit, not originally scoped) and `WP 5.4` (Release Candidate & Engineering Sign-Off). All ten Work Packages (`WP 5.0A`–`WP 5.4`) complete — Navigation, the Shell, the Command Framework, Diagnostics improvements, and Developer Experience tooling all shipped. |
| v0.6.0 | **Released, 2026-07-30** | `docs/releases/v0.6.0/ReleaseNotes.md`; `docs/releases/v0.6.0/WP6.8 Platform Certification Report.md`; tag `v0.6.0` (`99ed285`) | **"Platform Services"** — `CERTIFIED WITH ACCEPTED TECHNICAL DEBT`. Nine Work Packages (`WP 6.0`–`WP 6.8`): Reporting, Permissions & Identity, Notifications, REST API, Settings, Audit, Licensing, Export/Import, and the closing `WP 6.8` Integration Review & Release Certification. Merged into `main` non-fast-forward; six of eight feature Work Packages implemented ahead of their own nominal numeric order, per `Platform Service Implementation Order.md`'s own recommendation. 1016 tests, 0 failures; 52 ADRs, no gaps; zero Release Blocking technical debt (16 tracked items, 13 disclosed trade-offs, all classified Resolved/Accepted/Deferred). |
| v0.7.0 | **Released** | Tag `v0.7.0` (`61fb2db`, 2026-08-03), "TempestOS v0.7.0 - Engineering Foundation"; `docs/releases/v0.7.0/WP7.4.0 Release Readiness Report.md`, `WP7.4.0 Product Approval Report.md` | **"Engineering Foundation"** — the five cross-cutting Engineering Foundation frameworks (`FCR-0029`–`FCR-0033`: Engineering Data Model, Units & Quantities, Materials, Calculation, Verification) plus the Requirements & Verification Platform architecture/contract phases (`FCR-0027`). Exact combined full-suite total not independently re-derived within `WP 10.9A`'s own time budget — disclosed rather than guessed; every later release's own re-verified total (`v0.8.0` 1631/1631, `v0.9.0` 2026/2026) confirms no regression reached those baselines. |
| v0.8.0 | **Released** | Tag `v0.8.0` (`183d2ef`, 2026-08-04), "TempestOS v0.8.0 - Engineering Workspace"; `docs/releases/v0.8.0/WP8.9.0 Release Readiness Report.md`, `Product Approval Report.md` | **"Engineering Workspace"** — the Workspace Shell, Navigation & Project Explorer, Command Palette, Engineering Cockpit, and the full Engineering Domain (`ADR-0075` composition model: `EngineeringObjectBase` implementing every facet interface uniformly). 1631/1631 tests passing per its own Release Readiness Report. |
| v0.9.0 | **Released** | Tag `v0.9.0` (`9f64700`, 2026-08-07); `docs/releases/v0.9.0/ReleaseNotes.md`, `WP9.9.0 Release Readiness Report (Second Pass).md`, `Retrospective.md` | **"Mechanical Foundation"** — six real Engineering Disciplines end-to-end (Mechanical Product Structure, Requirements, Calculations, Documents, Verification, Manufacturing), each with a browsable Project Explorer area, real Property Inspector, full command set, real Digital Thread links, real Cockpit KPIs. Two independent release-readiness passes (first pass, then a second after `WP 9.8B` closed the first pass's own top recommendation). 2026/2026 tests passing at the second pass. |
| v0.10.0 | **Released, 2026-08-11** | Tag `v0.10.0`; `docs/releases/v0.10.0/WP10.9A Engineering Release Report.md` (the release decision, full gate-by-gate evidence); `docs/releases/v0.10.0/Release Notes.md` (the narrative record); `docs/releases/v0.10.0/` (~100 documents, sixteen Work Packages `WP 10.0A`–`WP 10.9A`) | **"User Experience & Desktop Application"** — the entire graphical Desktop application (`Tempest.Desktop`, Avalonia 11.2.3, `ADR-0094`) built over the `v0.9.0` terminal-based Engineering Workspace: Ribbon, Object Editors, Docking, Digital Thread graph, Command Palette, Undo/Redo, Macros, Cockpit dashboard, and this release's own closing Feature Completion / Existing-Capability-Exposure passes (`WP 10.7A`/`WP 10.8A`). 2266/2266 tests passing (Debug and Release both independently re-run from a clean build by `WP 10.9A`), 0 Warnings/0 Errors both configurations. |
| v0.11.0 | **Released, published, merged to `main`, 2026-08-11** | Tag `v0.11.0` — **corrected under a one-time, Product-Owner-approved governance exception, `WP 11.4B`**: originally cut from the feature branch (`fda4172`), repeating verbatim the `v0.10.0` tag-position defect `WP 11.1B` disclosed; found before the release branch closed, the tag (never the commit, never any release content) was deleted and recreated on `main`'s own merge commit (`4b1fb16`), reusing its original annotation verbatim — independently re-verified to resolve exactly there, and to be a real ancestor of `main`; [GitHub Release](https://github.com/kreczmans-creator/TempestOS-Core/releases/tag/v0.11.0) (a fresh, correctly-tagged, non-draft object republished by `release.yml`'s own independent rebuild/retest against the corrected tag — the original release was orphaned into an undeleted draft by the tag deletion, disclosed, not cleaned up within `WP 11.4B`'s own authorised scope), two correctly-separated assets; `docs/releases/v0.11.0/WorkPackages.md`; `docs/releases/v0.11.0/Release Notes.md`; `docs/releases/v0.11.0/WP11.9.0 Engineering Release Report.md`; `docs/releases/v0.11.0/Release Publication Report.md`; `docs/releases/v0.11.0/WP11.4A Release Engineering Corrections.md`; `docs/releases/v0.11.0/WP11.4B Release Process Correction Report.md`; `docs/releases/v0.11.0/` (ten Work Packages `WP 11.0A`–`WP 11.4B`) | **"Release Engineering & Architecture Governance"** — TempestOS's first CI pipeline (`WP 11.1A`), its first complete documented engineering workflow (`WP 11.1B`), its first automated governance health-check tooling (`WP 11.2A`, delivering `FCR-0005`), and a fully reviewed and implemented Desktop/console presentation-strategy decision (`WP 11.3A`/`WP 11.3B` — `ADR-0101`, `TempestShell` retired). Zero new Engineering Discipline or product feature — the first release since `v0.4.0` to add no new production `.cs` files. 2228/2228 tests passing, 0 Warnings/0 Errors both configurations — independently re-verified three times: locally (`WP 11.9.0`), on real GitHub-hosted infrastructure (`Release Publish`), and **on `main` itself, immediately before the corrected tag** (`WP 11.4B`, genuinely satisfying Governance §7.3 as literally written for the first time this release). First real CI execution found and `WP 11.4A` fixed three genuine release-engineering defects, none a product regression; `WP 11.4B` then found and corrected the tag-position defect — `Build & Test`/`CI Gate` now genuinely pass on real infrastructure for `main` itself, confirmed on four consecutive real CI runs. |
| v0.12.0 | **Released, published, merged to `main`, 2026-08-13** | Tag `v0.12.0` (annotated object `f430f6f`, tagger date 2026-08-13), resolves to commit `13a6ce3` — independently re-verified directly, `git rev-parse v0.12.0^{commit}` and `git merge-base feature/v0.13.0 v0.12.0` both confirm this exact SHA, no tag-position defect (unlike `v0.10.0`/`v0.11.0`'s own disclosed history above — this tag was cut directly from `main`'s own tip, never from a feature branch); [GitHub Release](https://github.com/kreczmans-creator/TempestOS-Core/releases/tag/v0.12.0), published automatically by `release.yml` (tag-triggered, independently re-built and re-tested Release configuration against the tagged commit, real GitHub Actions-confirmed success), two correctly-separated assets (`ADR-0101`); `docs/releases/v0.12.0/WorkPackages.md`; `docs/releases/v0.12.0/Release Notes.md`; `docs/releases/v0.12.0/WP12.9.0 Engineering Release Report.md`, `WP12.9.2 Engineering Release Report.md`, `WP12.9.4 Engineering Release Report.md` (three real Engineering Readiness Review executions, in order; the last is this release's own final, authoritative verdict); `docs/releases/v0.12.0/` (thirteen Work Packages `WP 12.3A`–`WP 12.9.3B`) | **"Desktop Composition & Domain Vocabulary Hardening"** — `MainWindow`/`EngineeringCockpit` decomposed from two monolithic composition roots into eighteen focused collaborators (`ADR-0103`, closing `WP11.0A` Finding `A-1`); every live Kind/`Classification`/`RelationshipKind` vocabulary value declared exactly once as a named constant, tracked in a new Engineering Vocabulary Register (`ADR-0105`, closing Finding `A-6`); a permanently-exposed false-positive in `IDiagnosticsProvider`'s own health reporting eliminated by isolating the one deliberately-always-failing sample module (`ADR-0102`); Desktop command/event wiring rules formalised (`ADR-0104`); this project's first formal, permanent Engineering Readiness Review model produced and exercised for real three times (`ADR-0106`, `WP 12.9.0`/`WP 12.9.2`/`WP 12.9.4`); a governance-tool checker-architecture inconsistency found and fixed via real CI, the first time this release's own Governance Health Check job actually ran on real GitHub-hosted infrastructure (`WP 12.9.3B`). Zero new Engineering Discipline — the second consecutive engineering-hardening release, mirroring `v0.11.0`'s own character. **Final Engineering Readiness verdict: `CERTIFIED WITH ACCEPTED TECHNICAL DEBT`** (`ADR-0106` §4, mechanically derived — zero Release Blocking finding, zero Disclosed Non-Blocking finding, `TD-42`/`TD-43`/`TD-45` the operative Pre-Existing, Unaffected finding, §4's own row 3). 2,255/2,255 tests passing both configurations, 0 Warnings/0 Errors both — independently re-verified from source repeatedly throughout; real, GitHub Actions-confirmed CI evidence for the tagged commit (`Build & Test (Debug)`, `Build & Test (Release)`, `CI Gate`, `Governance Health Check` all succeeded), not assumed. |
| v0.13.0 | **Tagged and merged to `main`, 2026-08-18 — GitHub Release not published** | Tag `v0.13.0` (annotated object `15aedec`, tagger date 2026-08-18), resolves to commit `6089a218` — independently re-verified directly, `git rev-parse v0.13.0^{commit}` and the remote's own `refs/tags/v0.13.0^{}` peel both confirm this exact SHA; cut from `main`'s own tip by `scripts/new-release.ps1 -Version 0.13.0` (run without `-Push`), never from a feature branch, so no tag-position defect of the `v0.10.0`/`v0.11.0` kind disclosed above. Merge commit `6089a218` is a genuine merge commit (parents `13a6ce3` + `30b0f45`), created by merging PR [#1](https://github.com/kreczmans-creator/TempestOS-Core/pull/1) under explicit Product Approval, no squash and no rebase; its tree is byte-identical to branch tip `30b0f45`. **Real GitHub Actions on `main` at this exact pre-tag commit: `Build & Test (Debug)`, `Build & Test (Release)`, `CI Gate`, and `Governance Health Check` all green** (run `32145226354`) — the mandatory verification `Engineering Readiness Review Architecture.md` §2.3 requires, satisfied for the first time in this release. **`release.yml` (run `32146823154`, tag-triggered) FAILED** at its `Test (Release)` step — `Tempest.Core.Tests` passed 2,341/2,341, `Tempest.Desktop.Tests` failed 1 of 221 (`ObjectEditorViewTests.Save_RevisedContent_ActuallyAdvancesTheRealRevisionNumber`, `Assert.True() Failure`) — so `Package Release build output`, `Resolve release notes`, and `Publish GitHub Release` were all **skipped**: there is **no GitHub Release, no `TempestOS-v0.13.0.zip`, and no engineering-harness asset** for this version. Diagnosed by `WP 13.12.8` as a genuine intermittent test flake, not a product defect — a fixed `Task.Delay(50)` racing a real disk-write chain, the same idiom and failure mode `TD-46` already records for a sibling test in the same file; the concurrent `ci.yml` run on the identical SHA, runner image and minute passed, and the failing run was ~51% slower end to end. Remediated test-only by `WP 13.12.9`. `docs/releases/v0.13.0/WorkPackages.md`; `docs/releases/v0.13.0/Release Notes.md`; `docs/releases/v0.13.0/WP13.12.2 Engineering Release Report.md` (this release's authoritative readiness record, superseding `WP13.9.0 Engineering Release Report.md`'s now-historical `NOT READY`); `docs/releases/v0.13.0/` (twenty-eight Work Packages, `WP 13.0.0`–`WP 13.12.2`, each with exactly one Academy retrospective) | **"Trust & Deployment Hardening"** — TempestOS's plugin platform and its trust boundary, the scope `FCR-0001` has named since `v0.7.0` and `Security Roadmap.md` items 1, 2 and 10 since `v0.6.0`. Six ADRs (`ADR-0107`–`ADR-0112`): plugin dependency-graph resolution and extended failure classification; a load/upgrade/uninstall lifecycle with live in-process unload retained as a named non-goal; plugin service registration adding no new DI capability; **capability-scoped isolation rather than `AssemblyLoadContext` or process separation**; a trust-capability model extending `IPermissionEvaluator` via a component principal and trust-ordered registration; and a detached manifest-and-assembly hash signature verified at Plugin Discovery. **Closes `TD-09`/`TD-10`/`TD-11`** and implements `Security Roadmap.md` items 1/2/10. The release's centre of gravity is the four adversarial remediation chains that followed the initial build: `WP 13.9.0`'s readiness review returned `NOT READY` and demonstrated, against the compiled binary, that trust enforcement scanned only the one manifest-declared assembly; eleven Work Packages then closed that and three further trust-boundary defects (`TD-51` twice, `TD-52`), each independently re-verified, several by reverting the fix and confirming the original failure returned. Technical Debt 48 → 56 tracked (18 Resolved, 1 Partially resolved, 37 Open); tests 2,255 → 2,562 both configurations; Academy 104 → 132 retrospectives. **No third-party plugin ships and third-party plugin support is neither enabled nor advertised** — `src/Plugins/` holds only a `README.md`, `FCR-0002` remains not started, and `TD-56` (a plugin's constructor executing outside its component scope) is a mandatory precondition to enabling it. |
| v0.13.1 | **Released, published, merged to `main`, 2026-08-18** | Tag `v0.13.1` (annotated object `b647e2c`, tagger date 2026-08-18), resolves to commit `6eca5fde` — independently re-verified, `git rev-parse v0.13.1^{commit}` and the remote's own `refs/tags/v0.13.1^{}` peel both confirm this exact SHA; cut from `main`'s own tip by `scripts/new-release.ps1 -Version 0.13.1` (run without `-Push`), the tag object verified directly rather than from the script's own success banner (`TD-42`). Merge commit `6eca5fde` is a genuine merge commit (parents `6089a218` + `9707fe7f`) from PR [#2](https://github.com/kreczmans-creator/TempestOS-Core/pull/2), merged under explicit Product Approval, no squash and no rebase, tree byte-identical to branch tip `9707fe7f`. **Real GitHub Actions on `main` at this exact pre-tag commit — `Build & Test (Debug)`, `Build & Test (Release)`, `CI Gate`, `Governance Health Check` all green** (run `32157115399`). **`release.yml` run `32158618270` succeeded end to end**: Build (Release), Test (Release), Package, Resolve release notes, and Publish GitHub Release all passed. [GitHub Release](https://github.com/kreczmans-creator/TempestOS-Core/releases/tag/v0.13.1) published 2026-08-18T16:14:42Z, not a draft and not a prerelease, with **both required assets** — `TempestOS-v0.13.1.zip` (49,877,891 bytes) and `TempestOS-v0.13.1-engineering-harness.zip` (642,363 bytes). Branch `release/v0.13.1`, cut from local `main` at `ea3fe07`, carrying `v0.13.0`'s own tagged commit `6089a218` in its ancestry unchanged plus exactly two commits: `7449756` (Desktop async test determinism remediation, test-only, one method in one file, `+28/−2`) and `ea3fe07` (`v0.13.0` Release Register closure, documentation-only, `+7/−3`). Combined delta over the `v0.13.0` tree: **2 files, +35/−5, zero `src/` files** — no product change of any kind. `docs/releases/v0.13.1/Release Notes.md`; the authoritative readiness record remains `docs/releases/v0.13.0/WP13.12.2 Engineering Release Report.md`, whose six category assessments apply unchanged to this release. **The `v0.13.0` tag is deliberately untouched** — not amended, moved, deleted or recreated, and `release.yml` was not retried against it — per Engineering Governance §7.4; `WP 11.4B`'s tag-position exception explicitly cannot reach this case, being bounded to a tag's mechanical position and to the window before the release branch closes, neither of which applies. Not yet tagged, not merged to `main`, not published; `origin/main` remains `6089a218` until the sanctioned PR merge. | **"Trust & Deployment Hardening" (corrected release)** — the published release of `v0.13.0`'s content, unchanged in scope: the plugin platform and its trust boundary (`ADR-0107`–`ADR-0112`), closing `TD-09`/`TD-10`/`TD-11` and implementing `Security Roadmap.md` items 1, 2 and 10. See `docs/releases/v0.13.0/Release Notes.md` for the full per-Work-Package scope account, deliberately not duplicated. This release exists solely because `v0.13.0`'s tag-triggered `release.yml` run (`32146823154`) failed at its `Test (Release)` step on a single Desktop test, so packaging and publication never ran — diagnosed by `WP 13.12.8` as a genuine intermittent flake (a fixed `Task.Delay(50)` racing a real disk-write chain, the same idiom `TD-46` records for a sibling test in the same file), remediated test-only by `WP 13.12.9`. **The project's first non-zero patch version**; `05-release-engineering.md` defines PATCH as "a hotfix, and only a hotfix, never a vehicle for new capability", which this release honours in substance. |

| v0.14.0 | **In preparation** | Branch `claude/tempestos-companion-mobile-ubznt3` (feature branch cut from `main` at `00f7f39`), carrying `WP 14.0A` (TempestOS Companion — Mobile Companion Application). No tag exists; `VERSION` remains `0.13.1` per the `WP 13.12.4` precedent (the bump lands as its own isolated commit at release preparation). `docs/releases/v0.14.0/WorkPackages.md` opened with the train. | **"TempestOS Companion"** — the platform's first non-desktop client (`ADR-0113`–`ADR-0115`): a separate-process Avalonia single-view mobile app over a new late-bound REST query-and-action surface, with explicit-freshness offline caching, the first concrete TempestOS visual identity, and a full-stack integration test suite. Scope beyond `WP 14.0A` not yet planned. |

**Total: 15 versions referenced — 12 confirmed released (v0.1.0, v0.3.0,
v0.4.0, v0.5.0, v0.6.0, v0.7.0, v0.8.0, v0.9.0, v0.10.0, v0.11.0,
v0.12.0, v0.13.1), 1 tagged and merged but not published (v0.13.0 — its
tag-triggered `release.yml` run failed before the publish step, so no
GitHub Release and no assets exist; see its own row above; superseded in
content, though never in tag, by v0.13.1), 1 in preparation (v0.14.0), 1 Unknown status (v0.2.0).**

## Cross-Reference Check

v0.4.0's shipped-vs-deferred scope above is cross-checked directly
against `Feature Register.md`'s own "Complete" vs. "Deferred to next
milestone" rows — consistent, no discrepancy. v0.5.0's own scope is
cross-checked against `docs/releases/v0.5.0/WorkPackages.md` directly —
consistent. The v0.2.0 gap is cross-checked against `Documentation
Register.md`'s identical finding — both registers disclose the same
Unknown independently, from different angles, and agree.
