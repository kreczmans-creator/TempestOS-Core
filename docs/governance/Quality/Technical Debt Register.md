# Technical Debt Register

## Register Metadata

| Field | Value |
|---|---|
| **Register Name** | Technical Debt Register |
| **Purpose** | The single, consolidated list of every disclosed debt item across the entire Claude-developed history of TempestOS — pulled from each Work Package retrospective's own "Architectural Debt Assessment" section and `WP 4.2D`'s own consolidated review, updated to current status as of this baseline. |
| **Scope** | Every debt item explicitly named in a Work Package retrospective's "Architectural Debt Assessment" section, `docs/releases/v0.4.0/Platform Services Architecture Review.md`'s "Remaining Technical Debt" section, or an ADR's own disclosed, accepted trade-off. |
| **Owner** | Project Maintainer. |
| **Source of Truth** | Every retrospective under `docs/academy/03 Work Packages/`; `docs/releases/v0.4.0/Platform Services Architecture Review.md`. |
| **Review Frequency** | Updated whenever a Work Package resolves, worsens, or discloses a new debt item — every retrospective's own "Architectural Debt Assessment" section is the trigger. |
| **Last Reviewed** | 2026-09-05 (`WP 16.4B-R1`, Architecture remediation) — **`TD-126` is Resolved**: `docs/architecture/Platform Service Map.md` now carries "At a Glance" rows and full detail sections for the three `v0.14.0` Platform Services this row named (Plugin Trust & Capability Enforcement, Engineering Object Durability & Rehydration, Attachment Content Store); see this row's own Status cell and `docs/releases/v0.16.0/WP16.4B-R1 Migration Collision Guard and Platform Service Registration.md`. Counts re-derived directly from this file, row by row: **136 tracked — 54 Resolved, 6 Closed, 70 Open, 5 Partially resolved, 1 Deferred** (`TD-126` moved from Open to Resolved; every other row unchanged by this Work Package). `TD-01`–`TD-136` contiguous, no duplicates, re-verified directly 2026-09-05 (the figure read `TD-01`–`TD-128` until the final release review recounted it: the 135 total and its breakdown were always right, only this contiguity range lagged behind `TD-129`–`TD-135`'s own addition). Previously reviewed 2026-09-04 (`WP 16.2A`, Register and Status Currency) — **`TD-57` is Resolved**: every governance register it named as drifted (Interface, Exception, DI, Namespace, Platform Services, Validation) was re-derived directly against source at this Work Package's own base commit; full derivation and residuals in `docs/releases/v0.16.0/WP16.2A Register and Status Currency Report.md` and in `TD-57`'s own Status cell above. **`TD-45` gains a pointer** to `WP 16.1A`'s report (the CI-workflow half of the fix landed; the GitHub-settings half remains Open, unchanged status). Counts re-derived directly from this file, row by row (`# TD-\d+` rows, Status cell of each): **128 tracked — 50 Resolved, 6 Closed, 66 Open, 5 Partially resolved, 1 Deferred** (`TD-57` newly among the Resolved; `TD-116` resolved by `WP 16.5B`; at the Wave 2/3 closure on 2026-09-04 `TD-34`, `TD-83`, `TD-87`, `TD-100`, `TD-119` moved to Resolved and `TD-65` to Partially resolved — counts re-derived by the `WP 16.1B` summary-vs-rows check) (`TD-82`, "Deferred to v1.1 per `D-022`, Proposed, `WP 16.0A`; ratification pending PR #6" — a status wording this review found already in place, not introduced here; its own separate "Deferred" bucket, not folded into "Open," is itself new to this summary line and accounts for the apparent one-row shift against `WP 15.2A`'s own last-stated "73 Open" — disclosed, not silently reconciled). `TD-01`–`TD-127` contiguous, no duplicates, re-verified directly. **Re-derived again at the `v0.16.0` review board, 2026-09-05:** `TD-120` moved to Resolved (it had read "Open" since `WP 15.2A`'s merge while four other documents said otherwise — see `TD-124`), `TD-116`'s claim was hedged to match `WP 16.5B`'s own report and its `D-025` citation marked Proposed, and five new rows were opened for debt the board itself found: `TD-123` (three registers no machine check derives), `TD-124` (a stale individual TD row is invisible to Check 12), `TD-125` (Rejected Designs Register 45 indexed vs 65 real), `TD-126` (`Platform Service Map.md` missing three `v0.14.0` service sections), `TD-127` (`v0.16.0`'s own retrospectives). **Re-derived again at the `WP 16.4B` integration, 2026-09-05:** `TD-62`, `TD-68`, `TD-69` and `TD-97` moved to Resolved and `TD-67` to Partially resolved (three of its five named defects closed, one found not to be a defect and referred to Technical Review, one genuinely still open — its own row says which); `TD-128` opened for the Digital Thread graph's keyboard-unreachable relationship edges, found by the review board and re-confirmed by `WP 16.5A-R1`. Previously reviewed 2026-09-04 (`WP 15.2A`, Desktop Test Suite Persistence Root Cleanup) — **`TD-120` is Resolved**: every isolated persistence root `WorkspacePersistenceCollection.NewIsolatedPersistenceRootPath()` returns now lives under one shared, per-test-run parent directory, deleted once by a new `ICollectionFixture` when every test in the "Tempest.Desktop WorkspaceHost persistence" collection finishes — not one directory per test, forever. `ResponsiveWorkspaceTests` (three `WorkspaceHost` instances of its own) found not to carry the collection attribute at all — joined it, closing both the disk-accumulation gap and the unserialised-parallel-execution hazard the collection exists to prevent. Verified empirically, not just by inspection: `/tmp` held 6,786 stray directories from prior, pre-fix runs in this same session before this Work Package began; a full 412-test run after the fix left zero new ones. Counts re-derived from this file: **122 tracked — 39 Resolved, 6 Closed, 73 Open, 4 Partially resolved**, unchanged in total; `TD-01`–`TD-122` contiguous, no duplicates. Previously reviewed 2026-09-04 (`WP 15.1A`, `v0.15.0` Release Preparation & Governance Closure) — reconciled this register against the actual `v0.14.0..main` implementation: two real, already-fixed defects from that range had no register entry at all — **`TD-121`** (a real Windows startup crash, `WP 15.0A`) and **`TD-122`** (the Ribbon horizontal scrollbar never rendering at compact widths, `WP 15.0D`) — both added directly as **Resolved**, for traceability, not because either needed further work. Counts re-derived from this file: **122 tracked — 38 Resolved, 6 Closed, 74 Open, 4 Partially resolved**, plus 26 accepted trade-offs (`AT-01`–`AT-26`), unchanged; `TD-01`–`TD-122` contiguous, no duplicates. No `src/` or `tests/` file was modified by this review. Previously reviewed 2026-09-02 (`WP-Z4` Stage 15, TD-119 remediation closure). **`TD-119` is Partially resolved** — 51 of its 52 fixed waits are gone, converted test-only across Stages 8, 11 and 13 with zero `src/` changes, and the one that remains (`WorkflowInteractionTests.cs:335`) is a deliberate retention whose assertions are negative and whose cancelled path raises no completion signal to join. Verified dual-event at `f0fcad6`: the `push` and `pull_request` runs both passed all four checks against the identical SHA, the first agreement in the sequence after four consecutive commits where the events disagreed. Its row now also records the six CI occurrences across seven distinct tests, the third legitimate synchronisation pattern (attempt-bounded `for`, six existing uses), the synchronous production paths whose waits were removed outright, and — kept rather than rewritten — two classification errors of this programme's own that the test suite caught: the original 3/61 miscount (truly 10/54) and the Command Palette Class-B misclassification. **`TD-120` is added**: the Desktop suite never deletes its isolated persistence roots, so repeated local runs fill the disk — ≈9,000 directories and ≈12 GB observed at Stage 13, which took the container to 0 bytes free mid-verification. Test-cleanup debt, not a product defect, not a CI failure, and not a merge blocker. Counts re-derived from this file: **120 tracked — 36 Resolved, 6 Closed, 74 Open, 4 Partially resolved**, plus **26** accepted trade-offs (`AT-01`–`AT-26`), unchanged; `TD-01`–`TD-120` contiguous, no duplicates. No `src/` or `tests/` file was modified by this review. Previously reviewed 2026-09-01 (`WP-Z4` Stage 5, the PR #5 CI recurrence). **`TD-119` is added** — `Tempest.Desktop.Tests` uses fixed `Task.Delay` waits as synchronisation against asynchronous work the tests never join; the `v0.14.0` PR CI run failed one Desktop test in Debug and a different one in Release while the push-triggered run at the **identical SHA** passed both, making this the **third documented incident** of the mechanism `TD-46`/`WP 11.4A` named and `WP 13.12.8`/`WP 13.12.9` met again at `v0.13.0`. It is **test-suite synchronisation debt, not a product defect**, and its remediation is **deferred — explicitly not part of the `v0.14.0` release implementation**. Two figures were re-derived rather than carried forward from the investigation that prompted this row, and one was wrong: the scope is **64 executable `Task.Delay` occurrences across 16 files, of which 61 are fixed waits** (three are legitimate — two bounded-poll back-offs from `WP 13.12.9`, one deliberate yield from `WP-Z2`), not the 63 first reported. Counts re-derived from this file: **119 tracked — 36 Resolved, 6 Closed, 74 Open, 3 Partially resolved**, plus **26** accepted trade-offs (`AT-01`–`AT-26`), unchanged. No `src/` or `tests/` file was modified by this review. Previously reviewed 2026-09-01 (`WP-Z2`, `TD-117` Undo/Redo threading). **`TD-117` is Resolved** — the Undo/Redo toolbar refresh now marshals to the UI thread in the Desktop subscriber (`ADR-0119`), leaving `Tempest.App` untouched. Counts re-derived: **118 tracked — 36 Resolved, 6 Closed, 73 Open, 3 Partially resolved**, plus **26** accepted trade-offs. Two corrections were made to `TD-117`'s own row in the course of fixing it: the defect dates to `v0.10.0`, not `v0.13.1`, and its real symptom was a silent half-completed Undo rather than the error dialog first reported. Previously reviewed 2026-09-01 (`WP-Z1`, Governance Correction — closing the pre-release audit). **The eleven-work-package remediation programme (`WP-C`, `WP-B1`, `WP-D2`, `WP-A1`, `WP-H`, `WP-D1`, `WP-F`, `WP-B2`, `WP-G`, `WP-A2`, `WP-E`) is recorded here for the first time as a whole**, together with `WP-REVIEW`. Counts **re-derived from this file rather than carried forward**, and they had drifted badly: **104 tracked -> 118** (the previous figure predated `TD-105`-`TD-118`), now **35 Resolved, 6 Closed, 74 Open, 3 Partially resolved**, plus **26** accepted trade-offs (`AT-01`-`AT-26`). **Eight rows had a contradictory Status cell** and are corrected: `TD-105`/`TD-106`/`TD-107` (Closed) and `TD-110`/`TD-111`/`TD-112`/`TD-113`/`TD-114` (Resolved) each led with "Open" while ending with their own closure, so a reader or script taking the leading token - which is what the Status column is - read eight finished rows as outstanding. Each keeps its own closure verb and its full body; only the leading token moved, following `TD-31`/`TD-72`/`TD-80`'s precedent. **`TD-108`'s per-assembly split was wrong and is corrected** (26/37 -> 28 Desktop / 35 App at `7de6290`), with the post-`WP-E` figures added - an error introduced by `WP-E` in the amendment that existed to correct wrong figures, found by the pre-release audit. **`TD-109`'s `MainWindow` figure** corrected 1,042 -> 1,052 lines. Three new rows arrived during the programme: `TD-116` (Linux/X11 launch), `TD-117` (undo/redo UI-thread defect, found while testing `WP-E`, pre-existing), `TD-118` (deferred async Cockpit conversion); and `AT-26` (the synchronous `IWorkspaceViewFactory.Create` contract, retained as a deliberate survivor). No production code was changed by this review. Previously reviewed 2026-08-30 (`WP — Project Timeline & Milestones`). **`TD-81` narrows again**: the Timeline/Milestone clause is delivered and struck from its scope. The row stays **Partially resolved** — Commercial, Resources, Knowledge, Administration and Gantt/scheduling proper are untouched, and it now lists them as the whole of what remains. `ProjectArea.Timeline` moves from `Declared` to `Implemented` and drops its `TD-81` tracking reference, which leaves **Reports** and **Settings** as the only project areas still Declared. Counts unchanged at 104 tracked — 35 Resolved, 5 Closed, 61 Open, 3 Partially resolved. Six mutations run, six killed. The load-bearing decision was a restraint rather than an addition: nothing in the product created a `Milestone` before this, so the surface needed creation to be honest, but the model's own `TargetDate` is immutable and the brief asked for what the model supports — so rescheduling is disclosed as absent instead of being added or faked. Previously reviewed 2026-08-30 (`WP — Project Risks, Issues & Decisions`). **`TD-81` narrows again**: its Risks/Issues/Decisions clause is delivered and struck from its scope, and `FCR-0056` moves to Implemented. The row stays **Partially resolved** — Commercial, Resources, Knowledge, Administration, Gantt/Timeline and managed Milestone surfaces are untouched, and it now lists them as the whole of what remains. Counts unchanged at 104 tracked — 35 Resolved, 5 Closed, 61 Open, 3 Partially resolved: this work resolves a Future Capability row, not a debt row. Eight mutations run, eight killed. Two new tests failed first time and both were worth having: one found a real defect in the register — an issue's originating risk was read by filtering the issue's *outgoing* relationships for a link pointing at itself, which can never match, so every issue silently reported no originating risk; the other was the test's own wrong premise, asserting a rewritten description would grow `CaptureState().History`, which records lifecycle transitions rather than document revisions. A third finding came from mutation testing rather than a failure: dropping the persist call from `ScoreAsync` was caught only by the store-reading test, because the restart test's *later* persisting call re-captured the whole state and masked it — which is exactly why both kinds of test exist. Previously reviewed 2026-08-30 (`TD-75` phase 2: Complete Samples Boundary). **`TD-75` moves to Resolved and the row closes.** The boundary is now a property that can be checked rather than described: the sample harness is deletable, verified by removing `src/Samples` from the tree and building the remaining solution clean. Counts move by one status: 104 tracked — **35 Resolved**, 5 Closed, **61 Open**, 3 Partially resolved. Seven mutations run, six killed first time. The survivor was in the new test rather than the product and was worth having: a raw `<Reference Include="Tempest.Samples"/>` slipped past the boundary check, because `Path.GetFileNameWithoutExtension("Tempest.Samples")` strips `.Samples` as though it were a file extension and yields `Tempest`. The same pass also corrected the phase-1 assertion, which matched the assembly name anywhere in a project file's raw text and so failed when a comment merely mentioned it — both now read the declared `ProjectReference`/`Reference` includes. Previously reviewed 2026-08-30 (Product Gap Reconciliation audit — no feature work). Two rows corrected, both demonstrated stale by evidence rather than by argument, and **no status was changed to make any count agree**. **`TD-75` re-scoped**: measured by removing the `ProjectReference` to `Tempest.Samples` and building, which leaves 70 errors across three files. It is not the packaging problem this row has always described — the product's six discipline explorer areas take their navigation identity from `*WorkspaceExplorerModule` types in the sample assembly, and the whole engineering calculation catalogue (Bolt Shear, Beam Bending, Bearing Load, Pressure Vessel, Material Selection Margin) is declared there. Removing the reference today would delete real product capability. The row's Severity is unchanged but its *shape* is now recorded honestly, because a "packaging chore" and a "product content is misfiled" item deserve very different places in a queue. Separately, the **shell's Tasks module descriptor** was found stating, in shipped UI text, that "no task surface, assignment workflow or board has been built" — false since the Tasks work package; corrected to describe the capability that is genuinely missing (a cross-project view), with `Availability` left `Declared` because the cross-project module really is unbuilt. That descriptor is the same defect class as `TD-102` and is now guarded by `ProductGapReconciliationAuditTests`, which checks every declared/implemented area against what the running shell actually renders and kills three mutations that re-introduce it. Counts unchanged at 104 tracked — 34 Resolved, 5 Closed, 61 Open, 4 Partially resolved. Previously reviewed 2026-08-29 (Project Tasks & Delivery Workflow — `ADR-0117`). `TD-81` moves to **Partially resolved**, stated as one module delivered and the rest explicitly not: Tasks is a real project surface; Commercial, Resources, Knowledge, Administration, Gantt/Timeline and managed Milestone surfaces are untouched, and the row now says so item by item rather than reading as a module programme in progress. No new debt row was opened: the task work is complete for the scope delivered, and what is missing is the same `TD-81` list it always was. Thirteen mutations run and thirteen killed, one of which (a task state change that never persisted) survived first time and exposed a real gap — every assertion read the in-memory object rather than the store — closed by a test that reads back what the state store actually holds. **Tracked-count correction.** Re-derived by directly parsing every `| TD-nnn |` row's own status cell rather than carrying the previous entry's arithmetic forward: **104 tracked — 34 Resolved, 5 Closed, 61 Open, 4 Partially resolved** (sum 104). The previously recorded figures (102 / 28 / 3 / 68 / 3) had drifted over several passes and are corrected here; no row's own status was changed to make the totals agree, only the narrative count. Previously reviewed 2026-08-29 (Production Rehydration & Principal Boundary — `ADR-0116`). `TD-103` **Resolved** for the local single-user desktop product TempestOS currently is: one explicit principal boundary, `ISessionPrincipalSource`, established by the real shell, with authentication and a permissions model left as declared future Administration scope rather than quietly invented. `TD-75` **Partially resolved**, deliberately stated as two halves: production rehydration no longer touches `Tempest.Samples` (the sample rehydrator file is deleted and all 21 canonical Kinds are registered by `CanonicalObjectKinds`), but `Tempest.App` still project-references `Tempest.Samples`, so sample modules still ship and still show — claiming full closure would have overstated it. New `TD-104` **Resolved**: nine persistable types were registered for rehydration nowhere at all and were silently discarded on restart, found by reflecting over the domain rather than trusting the registration list. Seven mutations run and seven killed, including one that reproduced a real defect this work had introduced — publishing only a non-null principal, leaving a sample's principal standing. 101 → 102 tracked, 28 Resolved, 3 Closed, 68 Open, 3 Partially resolved, re-derived by direct row count. Previously reviewed 2026-08-29 (Drawing / Document Viewer — `TD-80`, `ADR-0115`). `TD-80` is **Closed for the scope delivered**, and deliberately not more than that: its own original text lists markup, annotation and rotation, none of which was in this pass's scope, so they are carried as `TD-98` rather than absorbed into a closure that would have overstated it. The viewer is real — PDFium rasterises pages on demand at the current zoom, so zooming into a drawing shows more of the drawing; a text-extraction approach was rejected because it serves a specification and fails at the vector drawings these mock-ups are about. Missing, Corrupt and Unsupported are three distinct states, because telling a user their intact file is damaged is a false accusation about their data. The viewer is an ordinary `TD-72` panel, so multiple documents open with no fixed slots, and opening one never navigates. Eight mutations run and eight killed. Four new rows disclose what was not built or not verified: `TD-98` (markup/annotation/rotate), `TD-99` (DWG/SVG), `TD-100` (the headless platform's stub image decoder, found by probing after a correct test failed against correct code), `TD-101` (no tiled rendering). 97 → 101 tracked, 26 Resolved, 3 Closed, 67 → 70 Open, 2 Partially resolved, re-derived by direct row count. Previously reviewed 2026-08-29 (Attachment Content Storage — `TD-31`, `ADR-0114`). `TD-31` is **Closed**: an attached file is a file this platform holds, durable across restart and rehydration, verified on read. The byte shape is the same `PersistenceStore` instance over the same root, sharing `TD-59`'s naming, the per-key lock, the exact-name resolution and the atomic rename, so no second persistence architecture was introduced and none of those properties was re-implemented. Metadata and content stay separated: object state carries a hash, never a byte, so rehydration loads no files. Seven mutations run and seven killed, including "store the metadata but not the bytes", which only the restart journey catches. `FCR-0054` is implemented. Three new rows disclose what was deliberately not attempted: `TD-95` (no deduplication), `TD-96` (whole-file reads), `TD-97` (no orphan collection). 94 → 97 tracked, 26 Resolved, 2 Closed, 65 → 67 Open, 2 Partially resolved, re-derived by direct row count. Previously reviewed 2026-08-29 (TD-72 final closure and cross-platform cleanliness). `TD-72` is **Closed**: the shell's own composition was traced to the new layout host, all nine required capabilities to production wiring, and no fixed-grid type survives. Six mutations were run against the closure claims and one survived — deleting the host's `SizeChanged` subscription left all 260 Desktop tests green, because every responsive test invoked the rule directly and nothing proved it was wired. That is `TD-83`'s own smell one level up; it is closed with a test that drives a real layout pass, and the mutant then died. `TD-94` records a genuine production defect found while clearing the last two environment-dependent test failures: `PluginTrustStore` let the file system's case rules decide whether a `.CER` certificate was a trusted publisher, so a trusted publisher was silently untrusted on Linux. It had been carried for three commits as a "pre-existing environment case" — it never was one. The suite now passes in full on this platform for the first time (Core 2572/2572, Desktop 261/261) with no environment carve-outs. 93 → 94 tracked, 26 Resolved, 1 Closed, 65 Open, 2 Partially resolved, re-derived by direct row count. Previously reviewed 2026-08-29 (CI closure — `TD-93`). Making `EngineeringVocabularyConsistencyTests` deterministic exposed a real, pre-existing gap: the check reflected over `AppDomain.CurrentDomain.GetAssemblies()`, so it examined `Tempest.Samples` only on the runs where an earlier test had already loaded it, and passed quietly on the runs where it had not. The scan now names and force-loads the three production assemblies it covers, with a guard test that fails if its reach ever shrinks, and `Tempest.Samples` a disclosed exclusion — it references only `Tempest.Core`, so it structurally cannot reference the `Tempest.App` constants it would need to satisfy `ADR-0105`. `TD-93` records that residual decision. 92 → 93 tracked, 26 Resolved, 64 → 65 Open, 2 Partially resolved, re-derived by direct row count — which also corrects the previous entry's own arithmetic, where the four figures summed to 91 rather than 92 because one of the two Partially-resolved rows was not counted. Previously reviewed 2026-08-29 (Full Workspace Layout & Docking, `TD-72`, `ADR-0095`). The compile-time docking geometry is **replaced**, not decorated: the workspace arrangement is now an immutable tree of arbitrarily nested splits and tab groups plus floating windows, with drag-to-dock, tabbing, splitting, floating, collapse, auto-hide, resize, persistence and responsive behaviour all implemented, and the document area demoted to an ordinary panel so future surfaces participate by registration alone. `ADR-0095` — reserved and left unwritten by `ADR-0092` since 2026-08-05 for precisely this question — is now written. Eleven mutations run and killed, one of which exposed a real coverage gap first. Three new rows disclose what was deliberately not attempted: `TD-90` (focus across re-render), `TD-91` (the lossy edge-based projection), `TD-92` (no live drag adorner). 89 → 92 tracked, 25 → 26 Resolved, 62 → 64 Open, re-derived by direct row count. Previously reviewed 2026-08-29 (Project-Centric Convergence, `TD-89`). The product spine now expresses the authoritative product model in full: projects are the primary organisational context, **and** standalone calculation sets / quick calculations are a first-class workflow requiring no project. `TD-89` added and resolved for the spine itself; `TD-84`'s "Engineering is reachable only from an open project" is superseded, and the two tests that encoded it were inverted and strengthened rather than removed. `ProjectMembership` gives the platform one transitive definition of project ownership; `ShellAreas`/`ProjectAreas` make "declared but not yet implemented" checkable application state rather than a caption. 88 → 89 tracked, 24 → 25 Resolved, re-derived by direct row count. Previously reviewed 2026-08-28 (Engineering Object Rehydration — `TD-85`, `ADR-0113`). The persistence boundary is closed at its real source: engineering object state is now durable (`EngineeringObjectState` through `IEngineeringObjectStateStore`, written at creation and after every state-changing mutation, into the same `IPersistenceStore` the document store already uses), each of the 30 concrete canonical types owns both directions of its own persistence (`CaptureTypeState` / `IRehydratable<TSelf>.Rehydrate`) with no central switch over Kind anywhere, a Domain-layer Kind→rehydrator registry is populated by each Kind's own declaring class (29 Kinds — every Kind with a live write path in `src/`), and `EngineeringObjectRehydrationService` rebuilds the object graph and the relationship index at startup in both the desktop and console entry points. The `Projects.Index` workaround was **removed**, leaving one object model and one persistence authority. Proven by an 18-step acceptance journey through the real `MainWindow` across three application lifetimes plus 24 focused tests; four mutations run and killed. A ten-point closure audit followed the implementation and found one material defect — `ReviseAsync` carrying only half an object's state, which `TD-85` had turned from an in-memory reset into durable lifecycle-history loss — fixed, regression-tested and mutation-proven in the same pass; `TD-32` was amended (not resolved, not absorbed) to record that its asymmetry is now session-dependent. Counts re-derived by direct row count (88 tracked — 24 Resolved, 2 Partially resolved, 62 Open). Previously reviewed 2026-08-28 (Product Convergence & Recovery Programme — implementing the product decision that TempestOS is a project-centric engineering operating environment, `TempestOS → Module → Project → Workspace → Engineering Object → Evidence`). The programme's first and load-bearing objective, the **Product Spine**, is built: projects are real engineering objects with a durable index; the current project is real, persisted application state with an event; navigation is one immutable, persisted, testable location value enforcing that a project-scoped location and the current project can never disagree; and the Desktop shell renders whichever module the navigator reports, with Engineering reachable only from an open project. Proven by 32 tests including the full Definition-of-Done journey through the real `MainWindow` (launch → Projects → open → Project Workspace → Engineering → back → close → reopen → recovered) and the three commissioned engineering traces; four mutations run and killed. Two register changes of note: `TD-84` groups `TD-74`/`TD-76`/`TD-79`/`TD-81` as one P0 Product Spine deficiency rather than four features, and `TD-85` records the in-memory object-graph durability boundary the restart journey exposed. Counts re-derived by direct row count (85 tracked — 23 Resolved, 2 Partially resolved, 60 Open). Previously reviewed 2026-08-28 (Product / UX / Functional Compliance Audit — a full-system audit of the implementation against the original product/design brief, every established architectural decision, the recorded UX non-negotiables, and the five supplied TempestOS mock-ups; requirement set reconstructed repository-wide, then verified adversarially against running code, including a headless probe that booted the real `MainWindow` and measured shell composition and layout at seven window sizes). Verdict: **PARTIALLY compliant — the platform is strong, the product shell is not the product in the mock-ups.** Two items closed with implementation + tests + mutations (`TD-70`, `TD-71`); twelve raised (`TD-72`–`TD-83`), four of them P0: no project context, no global navigation architecture, no drawing/document viewer, and no Companion application on this branch. Counts re-derived by direct row count (83 tracked — 23 Resolved, 1 Partially resolved, 59 Open). Previously reviewed 2026-08-28 (independent finding-closure verification pass — eight fresh, read-only review sub-agents: closure verification, persistence/data integrity, UI/threading/lifecycle, plugin/security boundaries, accessibility/UI semantics, test/mutation quality, architecture/dependency, governance/source reconciliation; every claim re-verified against real source centrally before this register was touched, and every claimed closure re-checked with named mutations). Outcome: `TD-58`/`TD-59`/`TD-60` closed with implementation + real-production-path tests + mutation evidence (see their rows); all previously-Resolved v0.13.x closures re-verified against source — `TD-06`/`TD-09`/`TD-10`/`TD-11`/`TD-44`/`TD-46`/`TD-47`/`TD-48`/`TD-51` CLOSED-VERIFIED (two named surviving mutations recorded for `TD-51`: exception-filter narrowing, and the untested assembly-enumeration entry branch), `TD-40` and `TD-52` graded SUSPECT for test-pinning gaps now tracked as `TD-63`/`TD-64`; ten new items added (`TD-57`, `TD-61`–`TD-69`); counts re-derived by direct row count (69 tracked — 21 Resolved, 1 Partially resolved, 47 Open). Full report: `docs/governance/Quality/Finding Closure Verification (2026-08-28).md`. Previously reviewed 2026-08-18 (`WP 13.11D`, `v0.13.0` Plugin Platform Exit Review — six fresh, read-only sub-agents: Security/Adversarial, Architecture/Runtime, API/Compatibility, Verification/Test and RAM-concurrency, Governance/Documentation, WP14 UI/UX Dependency; every finding independently re-confirmed against real source centrally before this register was touched). Scope was deliberately narrow: determine only whether anything genuinely prevents starting WP14 UI/UX. **All six disciplines returned no blocker, and the platform is declared READY FOR WP14.** Confirmed against real code: `TD-51` remains closed (all four elements of the `WP 13.11B` mechanism intact — the `out` seam, `RecordDenied` before every throw, the completed fixed-point scan, the four-exception `CreateDescriptor` guard); `TD-52` remains closed (hosted-service constructor conformance plus `HostedServiceManager`'s own `componentScopeProvider` hook); `TD-55` correctly classified non-blocking, with an escalation attempt made and failed; no public API, dependency, or ADR regression across the whole of `v0.13.0` (111 ADRs, zero dependency changes, nothing packaged for external consumption); multi-assembly/multi-module behaviour sound; and plugin-facing UI dependencies clearly identified as WP14's own greenfield design scope rather than prerequisites — no existing seam is wrong in a way WP14 would have to break, and every extension WP14 needs is additive. **One genuine new finding, added here as `TD-56` and deliberately not fixed** (this Work Package is read-only by its own instruction): a plugin's own constructor executes outside its component scope, so plugin code runs during construction with a `null` ambient principal, which every capability gate treats as first-party and skips — defeating `TD-52`'s own gate for a signed plugin, and, via `IEventBus.Subscribe`'s owner capture, persisting first-party dispatch for the life of the process. Graded **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support** exactly as `TD-51` was, and explicitly not blocking WP14 or `v0.13.0` as shipped: dormant, unreachable below `VerifiedSigned`, no crash, orthogonal to UI work. Minimum follow-up is defined in its own row and is a statement-ordering change within `ADR-0111`'s existing hook, not a new seam. Three documentation-accuracy defects also corrected: `WP 13.9.0` was genuinely complete yet had no row in `WorkPackages.md`'s delivered table (the only completed `v0.13.0` Work Package missing from it); that table's own preamble still claimed "two ... completed so far" and that `WP 13.0A` "has not yet begun"; and `Plugin Trust & Isolation Architecture.md`'s closing pointer named only `TD-53`/`TD-54`, omitting `TD-55` and now `TD-56`, the two that are actually plugin-trust-specific. A fourth reported defect was **rejected on inspection**: `WP 13.9.0`'s `Not started` cell in the roadmap-*predicted* table is correct and deliberately unchanged, that table being a frozen branch-cut snapshot that is explicitly never re-derived. 55 → 56 tracked items, 18 Resolved unchanged, 1 Partially resolved unchanged, 36 → 37 Open; no existing status changed, `TD-53`/`TD-54` untouched. No ADR added or amended and `ADR Register.md` correctly untouched, mirroring `WP 13.11A`–`WP 13.11C`'s own precedent; no Academy article, mirroring the unbroken precedent since `WP 13.3B`. Zero `src/`/`tests/` files touched. `TD-34` extended, not re-classified: this Work Package's own Release regression reproduced its intermittent failure live (2,561/2,562, then 2,562/2,562 clean on an immediate re-run of identical binaries), and its Verification reviewer named the untracked sibling hazard behind the same root cause — `Logging/ConsoleLogSinkTests.cs` redirects process-global `Console.Out` while sitting outside the collection that serialises the fifty-odd other classes doing the same. Neither blocks WP14; both are test-harness isolation, not product behaviour. Previously reviewed 2026-08-18 (`WP 13.11C`, TD-51 Remediation Review & Trust-Boundary Verification — four fresh, read-only sub-agents: Security/Adversarial, Architecture/Runtime, Verification/Test and RAM-concurrency, Governance/Documentation; all revert experiments and every edit performed centrally). Reviewed `WP 13.11B` against `TD-51`, `ADR-0110`/`ADR-0111`, and the real implementation. **The production fix is clean** — an unresolvable constructor-parameter type cannot crash the Host on any traced path; `RecordDenied` provably precedes every denial `EnforceTrust` surfaces; the scan terminates and its per-step `AppDomain` diff runs on every path; both `IModule` and `IHostedService` denied types are excluded from Discovery, Registration, and Lifecycle, including a type implementing both; the discovery backstop preserves `ModuleDiscoveryException`/`DuplicateModuleIdException`/`TargetInvocationException` propagation (the duplicate-ID throw is outside the guarded block entirely); legitimate multi-assembly plugins are byte-identical on the non-failing path; and no public API, ADR, dependency, or architecture-boundary regression exists. **Two genuine findings, both closed here.** First, and materially: `WP 13.11B`'s own completed-fixed-point-scan decision — the security-critical half of the fix, and the one its rationale argued hardest for — had **no regression coverage whatsoever**. Reverting it to `WP 13.11A`'s own recommended partial-list shape left all 2,561 tests green, so a future "simplification" back to the unsafe shape would have passed the entire suite. Closed by one new test plus one new builder helper, proven non-vacuous in both directions: it fails on its own load-bearing assertion under the partial-list revert and passes with the fix. Second: the "guard already used at three other call sites in this exact codebase" claim, inherited verbatim from `WP 13.11A` and softened rather than corrected by `WP 13.11B`, is factually wrong — exactly **one** other call site carries that four-exception filter (`DiscoverModuleTypes`' own); `LoadOne`'s neighbouring guard is a different three-exception filter and the rest are single-`ReflectionTypeLoadException` catches. Corrected in every document that repeated it and in the production comment that asserted it. `TD-55` extended to name the complementary **under**-recording half of the same attribution defect, which its original wording concealed and which is *not* fail-closed: the per-step `before` snapshot is taken after `Assembly.LoadFrom` returns, so an assembly already resident when a plugin is scanned is never attributed to it at all — adversarial review could not construct a privilege escalation from this, so it is recorded as attribution imprecision of the same root cause, closed by the same per-plugin denial scoping. `ADR-0013` reviewed and found not to require amendment (its "Discovery throws → `Faulted`" clause is narrowed by the backstop, but same-category and fail-closed, and `ADR-0025`'s own category 8 already ratifies this isolation; the residue — per-candidate rather than per-assembly, and covering first-party candidates — is disclosed in `Plugin Trust & Isolation Architecture.md` rather than amended into either ADR). One pre-existing Academy staleness corrected in place: `07-plugin-architecture.md`'s "with zero change to ... Module Discovery, Registration" claim has been false since `WP 13.9.6`, not `WP 13.11B`, and now carries a supersession marker in that article's own established strikethrough style. No new ADR; `ADR Register.md` correctly untouched, mirroring `WP 13.11A`/`WP 13.11B`'s own precedent for a Work Package amending no ADR. 55 tracked items unchanged, 18 Resolved, 1 Partially resolved, 36 Open — no status changed, `TD-53`/`TD-54` deliberately left untouched. Debug and Release both 0 Warnings / 0 Errors; full regression 2,562/2,562 both configurations (2,561 + 1 new). Previously reviewed 2026-08-18 (`WP 13.11B`, Trust-Denial Crash Remediation — four parallel sub-agents, the analysis three read-only: Security/Adversarial, Runtime/Lifecycle, Verification/Test, Governance/Documentation; all production and documentation edits integrated centrally, one owner per file) — closes the single Release-blocking finding `WP 13.11A` raised, by both of that review's own recommended minimum-fix options and no wider. (a) `PluginAssemblyLoader.DiscoverModuleTypes`'s own unresolvable-constructor-parameter-type failure no longer throws from inside the scan; it is surfaced to `EnforceTrust`, which records every discovered `IModule` and `IHostedService` type (`RecordDenied`, `IPluginDeniedTypeRecorder`, `WP 13.9.4`) before throwing — so `WP 13.9.6`'s own `isTypeExcluded: deniedTypeRegistry.IsDenied` Module Discovery filter genuinely excludes the offending type and `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own `type.GetConstructor(Type.EmptyTypes)` call is never reached for it. Deliberately implemented as a **complete** fixed-point scan rather than the partial type list `WP 13.11A` suggested: this Work Package's own Security/Adversarial analysis established that aborting mid-traversal would have left an already-resident, unscanned assembly's module types unrecorded yet fully visible to Module Discovery's plugin-unaware `AppDomain` scan, trading the Host crash for a silent First-Party-treated trust bypass — strictly worse, and not fail-closed. (b) `ReflectionFrameworkDiscoveryService.DiscoverModules` gained the same narrow four-exception reflection guard around `CreateDescriptor` already used at the one other call site in this codebase (`DiscoverModuleTypes`' own; corrected from "other call sites", `WP 13.11C`): excluded and logged, never a crash and never a silent inclusion, with `ModuleDiscoveryException` (including the `WP 5.3` guidance) still propagating unchanged. `TD-51` moved **Reopened → Resolved** accordingly; 54 → 55 tracked items, 17 → 18 Resolved, 1 Partially resolved unchanged, 36 Open unchanged in count but changed in membership (`TD-51` out, `TD-55` in). `TD-55` was added by this Work Package's own independent Adversarial Review, which confirmed the core fix clean on every axis it attacked but found one genuine consequence worth tracking: `RecordDenied`'s all-discovered-types shape combined with `PluginDeniedTypeRegistry`'s `Type`-only keying lets one plugin's denial suppress another assembly's module types, a hazard latent since `WP 13.9.4` on the two existing denial paths and newly reachable on this third one, since it previously recorded nothing at all. Disclosed rather than silently accepted; closing it requires a per-plugin denial-scoping mechanism, which is an architectural change beyond this Work Package's own explicitly minimal remit. `TD-53`/`TD-54` deliberately untouched — both low-severity, non-blocking, and neither naturally nor directly implicated by this fix. No ADR added or amended: `ADR-0111`'s own Decision text already mandates the behaviour (a) restores, verbatim, so the defect was an implementation divergence from an already-correct decision — independently re-confirmed by direct read of that Decision section before this register was updated. No public API change, no new dependency. Seven permanent regression tests added; non-vacuousness confirmed by reverting each half of the fix independently, observing the targeted tests genuinely fail, and — with both reverted — reproducing the exact `Expected: Running, Actual: Faulted` Host crash end-to-end, then restoring. Debug and Release both 0 Warnings / 0 Errors; full regression 2,561/2,561 both configurations (2,554 + 7 new). Committed together with `WP 13.11A`'s own documentation corrections, which stopped before commit by their own explicit instruction. Previously reviewed 2026-08-17 (`WP 13.11A`, Plugin Platform Final Hardening Architecture Review — six parallel, read-only sub-agents: Security/Adversarial, Runtime/Lifecycle Architecture, API/Compatibility, Verification/Test/RAM, Governance/Documentation, UI/UX Dependency) — one **Release-blocking finding** (for third-party plugin support, not `v0.13.0`'s own shipped state): `TD-51`'s own already-disclosed "unresolvable constructor parameter type" residual gap, previously characterised by `WP 13.10C` as safely isolated (no crash), re-examined by this review's Security/Adversarial sub-agent and found to genuinely crash the whole Host on the `IModule` axis — `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own unguarded `type.GetConstructor(Type.EmptyTypes)` call throws uncaught when an unattributed module type's constructor references an unresolvable parameter type, because `WP 13.9.6`'s own crash-prevention filter (`isTypeExcluded: deniedTypeRegistry.IsDenied`) never triggers for a type this specific denial path never records. Independently re-confirmed by direct source read (not taken on the sub-agent's own citation alone) before this register was updated. `TD-51` reopened accordingly; a minimal follow-up Work Package (`WP 13.11B`, recommended, not yet scheduled) is the correct next step, not implemented here per this review's own explicit "review only" scope. Two further, genuinely new, low-severity items found and added directly by the same sub-agent, neither plugin-trust-specific: `TD-53` (`HostedServiceManager`'s own construction-throw critical-service misclassification) and `TD-54` (`ITempestServiceProvider`'s own incidental, not explicit, DI-escape closure). Zero WP14-blocking findings from any of the six disciplines — the UI/UX Dependency sub-agent found two genuine plugin-facing UI/diagnostics gaps (no capability-gated Workspace view/panel extension point exists yet, only Navigation/Commands; no plugin-ownership/trust-tier/capability projection exists on any DI-public diagnostics surface yet), both assessed as WP14's own first-order design scope to build, not a prerequisite hardening gap blocking WP14 from starting. `TD-49`/`TD-50` reconfirmed unchanged. One documentation-accuracy correction made directly by this review, unrelated to the plugin trust boundary: `Future Capability Register.md`'s `FCR-0001`/`FCR-0002` entries had drifted stale since `WP 13.0A`, still citing `WP 13.0B` as "implementation pending" when `WP 13.0B` was in fact repurposed to an independent architecture review and the real implementation shipped in `WP 13.2A` — corrected to cite `WP 13.2A` and `Status: Implemented`, matching this same register's own already-correct `WP 13.3A` narrative. 52 → 54 tracked items; 18 → 17 Resolved (`TD-51` reopened), 1 Partially resolved unchanged, 33 → 36 Open (`TD-51` reopened plus `TD-53`/`TD-54` newly added). Not yet committed at time of writing — this Work Package stops before commit per its own explicit instruction. Previously reviewed 2026-08-17 (`WP 13.10C`, Plugin Trust Hardening Review & Integration — the same integration-and-commit role `WP 13.9.7` established for the `WP 13.9.1`–`WP 13.9.6` chain, applied here to the `WP 13.10A`/`WP 13.10B` chain; four fresh, read-only sub-agents — Security/Adversarial, Architecture/API, Verification/Test + RAM/Concurrency, Governance/Documentation) — `TD-51`/`TD-52` both independently re-confirmed **Resolved** by direct re-read of the real, current source (`PluginAssemblyLoader.EnforceTrust`/`DiscoverModuleTypes`, `BackgroundServices.HostedServiceManager`, `Identity.IdentityService.EstablishCurrentPrincipal`), not taken on either prior Work Package's own citation — each finding's own targeted regression test independently reverted and confirmed to genuinely fail, then restored. One permanent-coverage gap found and closed directly by this review: the twice-found, twice-fixed Host-crash regression (an unresolvable constructor-parameter type on an `IHostedService`-only plugin faulting the whole Host) had only ever been proven fixed via throwaway proof-of-concept code across two independent `WP 13.10B` reviewers — a new permanent test, `LoadPlugins_FirstHostedServiceOnlyPluginHasUnresolvableConstructorParameterType_IsolatesFailure_SecondLegitimatePluginStillLoads`, closes it, non-vacuousness independently confirmed (reverted, observed to fail, restored). One small, low-severity, pre-existing gap disclosed but deliberately not fixed here (a redundant, safely-isolated second construction attempt for the "unresolvable constructor parameter" denial path, since `RecordDenied` is never reached on that specific throw site) — see `TD-51`'s own updated Status cell. One governance-only note disclosed, not a code defect (`plugin.identity.establish`'s own broad blast radius at any tier above `UnsignedLocal`) — see `TD-52`'s own updated Status cell. `ADR-0111` gained a "Corrected, `WP 13.10B`" note; `Plugin Trust & Isolation Architecture.md`'s own "Found, `WP 13.10A`" Risks entry corrected to "Closed, `WP 13.10B`"; `ADR Register.md`'s own `ADR-0111` citation extended to a seventh phase; `WorkPackages.md`/`PROJECT_STATUS.md` gained `WP 13.10A`/`WP 13.10B`/`WP 13.10C` entries. 52 tracked items unchanged; 16 → 18 Resolved, 35 → 33 Open (the two `WP 13.10A` additions moved to Resolved), 1 Partially resolved unchanged. Committed as a single commit alongside the full `WP 13.10B` implementation. Previously reviewed 2026-08-17 (`WP 13.10A`, Plugin Platform Hardening Architecture — a read-only architecture/security review of the complete v0.13.0 plugin platform, `WP13.0A`–`WP13.9.7`, six parallel disciplines: Plugin Runtime Architecture, Security/Trust, Lifecycle/Composition, API/Compatibility, Verification/Testability, Governance/Documentation) — **two new items added, `TD-51`/`TD-52`**, both live-PoC-confirmed against the current, committed code, both a genuinely distinct finding from anything the `WP 13.9.1`–`WP 13.9.6` remediation chain closed: `TD-51` (the static `IModule` constructor-conformance/denylist check was never extended to `IHostedService`, and `HostedServiceManager` has no ambient-identity scope hook at all) and `TD-52` (`IIdentityService.EstablishCurrentPrincipal`, an ordinary DI-public interface, reaches the exact ambient-identity write surface the denylist exists to keep out of plugin hands). Both classified **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support** — not for `v0.13.0`'s own already-shipped, already-signed first-party modules, which this finding does not affect, but for the plugin trust platform's own stated purpose. A dedicated remediation Work Package is recommended before third-party plugin support is considered production-ready; neither item blocks WP14 UI/UX work itself, which does not touch the plugin trust boundary. 50 → 52 tracked items; zero items resolved or worsened beyond the two additions. Full detail: this Work Package's own report (delivered directly in-session, no standalone document) and `Plugin Trust &amp; Isolation Architecture.md`'s own "Found, `WP 13.10A`" Risks entry. Previously reviewed 2026-08-14 (`WP 13.3A`, Plugin Platform Integration & End-to-End Validation) — the plugin platform's own closing integration/validation pass for `v0.13.0`. `TD-09`/`TD-10`/`TD-11` independently re-confirmed **Resolved** by direct re-reading of the real, current code (`PluginAssemblyLoader.EnforceTrust`, `NavigationService.Unregister`'s ownership check, trust-tier-ordered eviction across `NavigationService`/`CommandRegistry`/`CommandHandlerTable`), not taken on `WP 13.2A`'s own citation. **Two new items added, `TD-49`/`TD-50`** — both sourced from `WP 13.2B`'s own commit message, which disclosed each as a genuine, plausible finding it deliberately did not fix ("genuinely out of scope for a defect-fixing pass"), and neither of which had, until now, ever reached a formal register entry: `TD-49` (a TOCTOU window between Discovery-time signature verification and Loading-time `Assembly.LoadFrom`) and `TD-50` (filename-based, not certificate-attribute-based, first-party trust-store detection). Both confirmed still present by direct source re-read (`PluginAssemblyLoader.LoadOne`; `PluginTrustStore`'s own `FirstPartyCertificateFileName` constant and its class-level remarks, which already self-disclose the filename convention). Neither is an `ADR-0112` conformance gap — see `ADR Register.md`'s own updated `Last Reviewed` entry for the clause-by-clause check. 48 → 50 tracked items; zero items worsened, zero resolved by this pass beyond the two re-confirmations above. Full detail: `docs/academy/03 Work Packages/WP13.3A-plugin-platform-integration-and-end-to-end-validation.md`. Previously reviewed 2026-08-13 (`WP 13.2A`, Plugin Trust & Capability Enforcement Implementation) — `TD-09`, `TD-10`, and `TD-11` all moved **Open → Resolved**: this Work Package implements `ADR-0110`–`ADR-0112` exactly as `WP 13.0A` designed and `WP 13.0B` independently audited them — real, tested, working capability enforcement (`PluginAssemblyLoader.EnforceTrust`, closing `TD-09`'s DI-container-trust gap), a real `NavigationService.Unregister` ownership check (closing `TD-10`), and real trust-tier-ordered registration replacing unconditional "first registration wins" across `NavigationService`/`CommandHandlerTable`/`CommandRegistry` (closing `TD-11`). 48 tracked items unchanged in count; zero new items added, zero worsened; 13 → 16 Resolved, 34 → 31 Open (1 Partially resolved item, `TD-05`, unaffected). Full detail: `docs/academy/03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md`. Previously reviewed 2026-08-13 (`WP 13.1A`, Plugin Runtime & Composition Root Implementation) — `TD-06` moved **Open → Resolved**: `Runtime:Plugins:RootDirectory`/`ManifestFileName`/`Disabled` are now real, tested configuration keys, closing the gap `WP 13.0A` had only designed. `TD-09`/`TD-10`/`TD-11` **confirmed unchanged, deliberately** — this Work Package implements only the mechanical, non-trust half of `WP 13.0A`'s design (`ADR-0107`–`ADR-0109`); the trust retrofit those three items still need (`ADR-0110`–`ADR-0112`) remains a genuinely separate, still-unassigned future Work Package's task, not touched here. 48 tracked items unchanged in count; zero new items added, zero worsened; 12 → 13 Resolved, 35 → 34 Open. Zero `docs/` files touched by any of the three production/testing workstreams — only this Work Package's own Governance & Documentation pass, applied after implementation and independent verification both closed (Debug: 2,305/2,305 passing, 0 Warnings/0 Errors; Release: run in progress at time of writing, expected to match). Full detail: `docs/academy/03 Work Packages/WP13.1A-plugin-runtime-and-composition-root-implementation.md`. Previously reviewed 2026-08-13 (`WP 13.0A`, Plugin & Registration Trust Isolation Architecture) — `TD-06`, `TD-09`, `TD-10`, `TD-11` all updated in place, none resolved: each moves from "mechanism exists, not yet applied" to "design resolved, retrofit remains open until implemented" (`TD-09`/`TD-10`/`TD-11`) or "architecture designed, not yet implemented" (`TD-06`), citing the six new ADRs (`ADR-0107`–`ADR-0112`) this Work Package produced. No item moved to Resolved — this Work Package is architecture only; the actual code retrofit is `WP 13.0B`'s own, separately-scoped task. 48 tracked items unchanged in count; zero new items added, zero worsened. Zero `src/`/`tests/` files touched. Full detail: `docs/academy/03 Work Packages/WP13.0A-plugin-and-registration-trust-isolation-architecture.md`. Previously reviewed 2026-08-11 (WP 11.4A, Release Engineering Corrections) — three new items added and immediately resolved (`TD-46`–`TD-48`, the specific defects found on `v0.11.0`'s first real CI execution, per `WP11.9.0`'s own Release Publication Report); `TD-44` moved Open → Resolved in the same pass, its own precondition (CI genuinely executing on real infrastructure) now independently confirmed on two real runs (branch push, tag push). 45 → 48 total; 6 → 10 Resolved; 38 → 37 Open. No production, Domain, or architecture change; three test/tooling/CI-configuration files touched, each narrowly, per `WP11.4A Release Engineering Corrections.md`. Previously reviewed 2026-08-11 (WP 11.9.0, `v0.11.0` Release Preparation & Engineering Sign-Off) — full re-verification as part of this release's own closing sign-off, not merely a currency refresh: an independent QA re-verification of `v0.11.0`'s own two new release-tooling scripts, run against real fixtures (not taken on either script's own prior claim), found two genuine, previously-undisclosed defects — `scripts/new-release.ps1`'s `git tag`/`git push` steps silently swallow a failure and still report `RELEASE SUCCESSFUL` (`TD-42`); `scripts/governance-healthcheck.ps1`'s generic exception handler loses the failing check's own identity on an empty-register or link-less-index input (`TD-43`). Two further items already disclosed in prose across `WP 11.1A`/`WP 11.1B`/`WP 11.2A`'s own reports, but never before consolidated into a formal register entry, are now registered directly: CI has never executed on real GitHub-hosted infrastructure (`TD-44`); branch protection is documented, not configured (`TD-45`). This register itself had drifted stale relative to `v0.11.0`'s own prior seven Work Packages before this review — `WP 10.8A` was its last genuine review, none of `v0.11.0`'s own findings had been entered — corrected here directly rather than deferred again. 41 → 45 total; Open count 34 → 38. `v0.11.0`'s own eighth and closing Work Package. Previously reviewed 2026-08-11 (WP 10.8A, Desktop Feature Completion & Existing Capability Exposure) — reviewed, zero new items added: this Work Package's own `PropertyInspectorView` real-Validation section (reusing `ObjectEditorView.BuildSeverityRow`, an already-established resolution to reuse rather than duplicate) is a second, previously-undisclosed consumer of `TD-41`'s own gap — a Requirement's own Validation now correctly reports "Real validation is not available for this object here" rather than a fabricated result, the identical honest degradation `TD-41`'s original entry already named for the Object Editor, now confirmed to extend to this second surface too; `TD-41`'s own Consequence text updated to name both surfaces. A second, genuine pre-existing defect was found and fixed in place, never reaching Technical Debt status: `RibbonView`'s own final fallback message (reached by every genuinely-unwired verb, including Copy) claimed a command was reachable via "the Command Palette (Ctrl+K)" or "Project Explorer's own context menu" — confirmed false for every command still reaching that branch, since `CreateDefault` is `null` on every real `CommandDescriptor` this platform has ever registered (`26-ribbon-and-command-experience.md` §11, a `WP 10.3B` finding this Work Package's own audit re-confirmed still true), so the Command Palette cannot invoke any real discipline command by Id at all, and the Project Explorer's own context menu offers only Open/Rename/Delete/Favourite. Corrected to name no false alternative — the identical "found and fixed, not merely disclosed" discipline this project has followed since `WP 10.2A`. No genuine new implementation defect found beyond this. 41 tracked items unchanged; Open count 34 unchanged. `v0.10.0`'s own fifteenth Work Package — zero Command Framework/frozen Workspace contract changes; `EngineeringDomainContext` threaded into `PropertyInspectorView` as a third optional constructor parameter, mirroring exactly how `ObjectEditorView` gained `ICommandDispatcher` the Work Package prior. Combined test suite 2266/2266 passing, Debug and Release both clean, 0 Warnings/0 Errors. See `WP10.8A Implementation Report.md`. Previously reviewed 2026-08-10 (WP 10.7A, Feature Completion) — reviewed, one new item added: `TD-41` (`ObjectEditorView.TryCreate` never resolves a real Requirement via `EngineeringDomainContext.Repository`, a genuine, pre-existing, previously-undetected gap since `WP 10.3A`, found while adding this Work Package's own new Requirements Owner/Priority Object Editor section — Open, not caused by this Work Package, not fixable within its own "no new architecture" scope). A second, genuine implementation-time bug was found and fixed in place before any commit, never reaching Technical Debt status: every one of this Work Package's own five new Object Editor sections had its own success/failure status message immediately overwritten by the very `Refresh()` call that reported it, since `Refresh()` re-runs `PopulateFrom`, which unconditionally clears each section's own status text — reordered (`Refresh()` first, status message set after) in all five, the identical "found and fixed, not merely disclosed" discipline this project has followed since `WP 10.2A`. This Work Package closes the WP10.6D-audited placeholder gap directly: real Ribbon status-transition/Create/Duplicate dispatch for the five disciplines beyond Mechanical (`FCR-0075`), real drag-and-drop reparenting (`FCR-0066`), and all five discipline-specific Object Editor sections (`FCR-0068`) — see `WP10.7A Implementation Report.md`. 40 → 41 tracked items; Open count 33 → 34. `v0.10.0`'s own fourteenth Work Package — zero Command Framework/frozen Workspace contract changes (confirmed directly): every new capability dispatches an already-existing, already-registered command through `ICommandDispatcher` exactly as `MainWindow`'s own pre-existing Mechanical Create/Duplicate handlers already did, or raises a new plain event mirroring `ProjectExplorerView`'s own existing `ObjectOpened`/`ToggleFavouriteRequested` shape. Combined test suite 2263/2263 passing (2064 `Tempest.Core.Tests` + 199 `Tempest.Desktop.Tests`), Debug and Release both clean, 0 Warnings/0 Errors. Previously reviewed 2026-08-10 (WP 10.5C, Commercial User Experience & Application Completion) — reviewed, zero new items added: this Work Package's own required-first runtime audit found two genuine, real `TD-39`-class defects (`CockpitCardControl`'s own neutral border, `RibbonView.BuildSectionWithLabel`'s own group divider — both a fixed `Brushes.Gray`, never theme-reactive) — both fixed at their own true source (`ThemeReactiveBrush.Bind`/`ApplicationPalette.PanelBorderBrushKey`, the identical, already-proven fix) before commit, the fourth and fifth real instance of `TD-39`'s own risk class this project has now found and closed. 40 tracked items unchanged; Open count unchanged. `v0.10.0`'s own thirteenth Work Package by completion order — zero `src/Tempest.Core/` files touched; `ProjectExplorerNode`/`CockpitKpiCard` (`Tempest.App.Workspace`) both gained only additive, defaulted trailing parameters. See `WP10.5C Technical Debt Review.md`. Previously reviewed 2026-08-10 (WP 10.6A, Command Execution & Productivity Experience) — zero new `TD-##` items added (no genuine pre-existing defect found; every new capability this Work Package built is genuinely new code, not a fix against already-shipped behaviour); six new Accepted Trade-offs added instead (`AT-18`–`AT-23`): Undo/Redo coverage limited to Rename and Favourite toggle; Background Task Runner reports coarse state only, never a percentage; a Macro step must be `CreateDefault`-eligible (today, only `Tempest.Samples` commands qualify); Command History records only what reaches existing UI `ActionCompleted` surfaces, with a disclosed "contains 'fail'" heuristic for `succeeded`; Undo/Redo and Command History are both session-only; `KeyboardCommandBindingProvider` ships with zero default bindings and no remapping UI, and no real external controller vendor integration exists. 40 tracked `TD` items unchanged; Open count 31 unchanged; 17 → 23 Accepted Trade-offs. `v0.10.0`'s own twelfth Work Package — zero Engineering Domain/Runtime files touched beyond `TempestHost.cs`'s own two new `services.Singleton` lines; every frozen `WP8.0B` Workspace contract and both public Command Framework contracts (`ICommandDispatcher`/`ICommandRegistry`) unmodified. Combined test suite 2238/2238 passing (2062 `Tempest.Core.Tests` + 176 `Tempest.Desktop.Tests`), Debug and Release both clean, 0 Warnings/0 Errors. See `WP10.6A Technical Debt Review.md`. Previously reviewed 2026-08-10 (WP 10.5B, Desktop Workflow & Professional Interaction) — reviewed, zero new items added: two genuine implementation-time findings — a real, pre-existing (`WP 10.2A`) `ProjectExplorerView` filter reliability gap (`TextChanged` not firing for programmatic `.Text =`, the identical class `ObjectEditorView`/`WP 10.3A` already fixed) and a `TD-27`-class test-precision gap in this Work Package's own new Delete-Confirmation tests (a soft-delete assertion error, a non-deterministic "first node found" helper) — both fixed at their true source before commit, neither reaching Technical Debt status, mirroring `WP 10.2A`/`WP 10.3B`'s own "found and fixed, not merely disclosed" precedent. Five new Future Capability entries recorded instead (`FCR-0073`–`FCR-0077`), all disclosed, deliberate scope reductions, none a defect. 40 tracked items unchanged; Open count 31 unchanged. `v0.10.0`'s own eleventh Work Package — zero `src/Tempest.Core/`/`src/Tempest.App/Workspace/` files touched. See `WP10.5B Technical Debt Review.md`. Previously reviewed 2026-08-10 (WP 10.5A, Workspace Visual Polish & Engineering User Experience) — `TD-39` and `TD-40` both moved Open → Resolved, each fixed at exactly the mechanism its own "Revisit trigger" column named (a real `DynamicResource`-bound overlay background; a real "Discard unsaved changes?" confirmation before a dirty tab closes), both proven by dedicated tests, re-run five consecutive times, zero failures. Zero new items added. One genuine, real test-precision defect found in `WP 10.4A`'s own `DigitalThreadGraphModelTests` (an over-broad assertion, intermittently failing depending on `InMemoryEngineeringObjectRepository`'s own unspecified iteration order, `TD-27`'s identical class of risk) — fixed in place, not registered as a new item, since the underlying production behaviour was always correct; `WP10.4A`'s own already-published documents deliberately left untouched, this finding disclosed in `WP10.5A Technical Debt Review.md` §3 instead. 40 tracked items unchanged; Open count 33 → 31. `v0.10.0`'s own tenth Work Package — zero `src/Tempest.Core/`/`src/Tempest.App/Workspace/` files touched. Previously reviewed 2026-08-09 (WP 10.4A, Digital Thread Visualisation) — reviewed, zero new items added: one existing item, `TD-32`, revisited and re-disclosed exactly as `WP10.0A Digital Thread & Relationship Visualisation.md` §4 explicitly called for once a graph view was actually built — disposition unchanged (still Open, no data-correctness issue), but a second, previously-undisclosed consumer of the same underlying gap was found in the process: `ObjectEditorView`'s own Relationship summary (`WP 10.3A`) silently misses the same `"verifiedBy"` link `DigitalThreadGraphModel` now explicitly merges in via `VerificationRecordReader`, made visible for the first time as a real graph node (`WP10.4A Engineering Review.md` §3). 40 tracked items unchanged (33 Open). `v0.10.0`'s own ninth Work Package — zero `src/Tempest.Core/`/`src/Tempest.App/Workspace/` files touched; the new `DigitalThreadGraphModel`/`DigitalThreadGraphView` pair lives entirely in `Tempest.Desktop`, reading exclusively through already-existing, already-permitted reads (`EngineeringDomainContext.Repository`/`RelationshipRepository`, `VerificationRecordReader`) — zero new traversal mechanism, `ADR-0093` honoured directly. Previously reviewed 2026-08-09 (WP 10.3B, Ribbon, Toolbar & Command Experience) — reviewed, zero new items added: this Work Package found two genuine defects (`CommandPaletteOverlay`'s own pre-existing silent no-op on Enter for any command with no `CreateDefault`; `RibbonView.Rebuild()`'s own missed initial `RefreshEnablement()` call) — both fixed in place, before any commit, proven by dedicated tests, never reaching a state warranting a Technical Debt entry (`WP10.3B Engineering Review.md` §2–3), the identical "found and fixed, not merely disclosed" discipline `WP 10.2A`'s own Property Inspector fix established. 40 tracked items unchanged (33 Open). `v0.10.0`'s own eighth Work Package — zero `src/Tempest.Core/`/`src/Tempest.App/Workspace/` files touched. Previously reviewed 2026-08-09 (WP 10.3A, Engineering Object Editors) — one new item added: `TD-40` (`ObjectEditorView`'s own new, real, buffered dirty-state is not wired into `DocumentAreaView`'s own tab-close path — closing a dirty editor tab silently discards unsaved edits, no confirmation prompt, a disclosed, deliberate scope decision). 39 → 40 total (33 Open). Zero pre-existing items worsened; every one of the thirty-nine reviewed and confirmed unaffected by this Work Package's own diff. `v0.10.0`'s own seventh Work Package. Previously reviewed 2026-08-09 (WP 10.2B, Docking & Workspace Layouts) — one new item added: `TD-39` (`PanelHostControl`'s own fixed-brush overlay background, not theme-variant-aware — required once a panel can be reparented as an Auto-Hide flyout, mirroring `CommandPaletteOverlay`'s own identical, previously-unregistered limitation, both surfaced together for the first time). 38 → 39 total (32 Open). Zero Workspace contract changes of any kind (`IWorkspaceLayout`/`WorkspacePanelPlacement`/`IWorkspacePanel` all unmodified — Bottom dock, Collapse, Auto-Hide, predefined/saved/reset layouts, and the new Output panel are all realised additively, entirely in `Tempest.Desktop`/a new `OutputPanel : IWorkspacePanel` implementer, plus a second, sibling `DesktopPanelUiState` Settings key alongside `WorkspaceState`'s own — never a change to any of the twelve frozen `WP8.0B` contracts). Zero Engineering Domain/Runtime files touched. `v0.10.0`'s own sixth Work Package. Previously reviewed 2026-08-07 (WP 10.2A, Workspace Modernisation) — reviewed, zero new items added: one genuine, low-severity implementation defect (Property Inspector Lifecycle-section facet duplication) was found by this Work Package's own required Engineering Review and fixed in place, before any commit, proven by a dedicated regression test — never reaching a state warranting a Technical Debt entry (`WP10.2A Engineering Review.md` §3). Two disciplines' own honestly-incomplete Rename/Delete coverage (Requirements' three Kinds register Delete only; Calculations' synthetic `"CalculationTemplate"` Kind registers neither) are permanent, correct, disclosed-by-design absences, not defects with a future fix trigger — documented directly in `ADR-0096` and the Implementation Report, not registered here. 38 tracked items unchanged. Previously reviewed 2026-08-07 (WP 10.1B, Runtime Host & Module Discovery Hardening) — `TD-26` and `TD-37` both fully investigated and Resolved, not merely re-mitigated: `TD-26` fixed at its own source (`WorkspaceManager.StartAsync` now waits for `IDiagnosticsProvider.HostState == HostState.Running`, replacing the two prior Work Packages' own `Tempest.Desktop`-side mitigations, now removed as redundant); `TD-37`'s true root cause determined via direct file-system evidence — a durable, cross-launch `IPersistenceStore`-backed uniqueness index (`ADR-0041`) colliding with fixed, literal sample-module identifiers on a second real launch, never a double-invocation as its own prior entry had speculated — fixed via idempotent seeding in all four affected modules plus genuine per-test persistence isolation in `Tempest.Desktop.Tests`. `EngineeringCockpitTests`' own three previously honest-empty assertions now read real, stable, live data. One new item added and disclosed, not fixed: `TD-38` (`EngineeringObjectFactory<T>` enforces no business-identifier uniqueness of its own — a genuine Engineering Domain change, explicitly out of this Work Package's own scope). 37 → 38 total (two Open items, `TD-26`/`TD-37`, moved to Resolved; one new Open item, `TD-38`, added): 6 Resolved, 1 Partially resolved, 31 Open. `v0.10.0`'s own fourth Work Package, and its first pure hardening/root-cause pass — zero Engineering Domain files touched, confirmed directly; combined test suite 2051/2051 passing (2029 `Tempest.Core.Tests` + 22 `Tempest.Desktop.Tests`), Debug and Release both clean. Previously reviewed 2026-08-07 (WP 10.1A, Engineering Cockpit Implementation) — TD-37 added, Open: four `Tempest.Samples` modules (`engineeringdomain`, `materials`, `requirements`, `requirementsworkspace`) each fail their own `InitialiseAsync` against their own literal Id, confirmed reproducible, confirmed not caused by this Work Package's own code, root cause not fully diagnosed — found via `IDiagnosticsProvider.Modules` while diagnosing an incomplete `WP 10.0B`-era `TD-26` mitigation. TD-26 reconfirmed Open a third time (`WP 9.0A`, `WP 10.0B`, `WP 10.1A`); its own `Tempest.Desktop` mitigation strengthened from a narrow six-item `NavigationItem` poll to the Runtime Host's own authoritative `IDiagnosticsProvider.HostState == HostState.Running` signal. 36 → 37 total. `v0.10.0`'s own third Work Package, and its second real implementation this release — zero Engineering Domain files touched, confirmed directly. Previously reviewed 2026-08-07 (WP 10.0B, Desktop Application Framework) — TD-35 added and immediately marked Resolved (`ProjectExplorer`/`PropertyInspector`'s own non-deterministic panel Ids broke cross-restart session persistence — a genuine, pre-existing `v0.9.0` defect, found by this Work Package's own real, two-independent-process test before any commit, fixed in the same Work Package); TD-36 added, Open (the Settings persistence store's own working-directory-relative default root path — a new consideration surfaced by a graphical, non-terminal launch path). TD-26 reconfirmed Open, its own "Revisit trigger" now directly, empirically met for the first time — `Tempest.Desktop`'s own headless tests hit its real consequences directly, mitigated one layer up in `Tempest.Desktop` itself, not fixed at the `WorkspaceManager` source; priority reconsideration recommended. 34 → 36 total (34 → 35 → 36, one addition immediately Resolved). This is `v0.10.0`'s own second Work Package, and the first to write real implementation code this release — zero Engineering Domain/discipline files touched, confirmed directly; two small, disclosed, non-contract changes made in `Tempest.App.Workspace` (§TD-35, plus an `InternalsVisibleTo` grant). Previously reviewed 2026-08-07 (WP 9.9.0, Release Preparation & Product Baseline — Second Pass) — TD-34 added: `CompositeLogSinkTests`'s own previously-informally-disclosed, non-reproducible `Console.Out`-capture flake (first named `WP 6.3`, referenced by every subsequent release-closing review's own flake-check, never formally registered) — actually observed for the first time during this Work Package's own fresh test-suite verification (1 failure across 5 full-suite runs, 0 failures across 5 further isolated runs of the same test), formally registered now that real evidence exists. 33 → 34 total. Previously reviewed 2026-08-07 (WP 9.9.0, Release Preparation & Product Baseline — First Pass) — reviewed, zero new items added: verification-only Work Package. All 33 tracked items and 17 disclosed trade-offs re-verified directly against this register's own Entries tables, both counts matching exactly — zero arithmetic drift found this time (unlike `WP 9.3A`'s own disclosed correction). Eight items added across `v0.9.0` (`TD-26`–`TD-33`, one per Work Package), all reconfirmed still Open, zero Release Blocking, zero disposition worsened. See `WP9.9.0 Release Readiness Report.md` §15 (Technical Debt Review). Previously reviewed 2026-08-07 (WP 9.5A, Manufacturing Workspace) — TD-33 added (`EngineeringCockpit.FormatCoverage`'s own zero-denominator text is hardcoded Requirements-specific, already inaccurately reused by two prior Work Packages' own coverage cards — found by direct inspection while designing `ManufacturingKpiCards`, worked around locally, not fixed at the shared source). Seventh consecutive dedicated Security Review (`WP9.5A Security Review Report.md`); zero Domain-layer files touched; one genuine finding (`TD-33`), caught by design-time inspection, not a failing test this time, and not a bug in already-shipped code. Previously reviewed 2026-08-07 (WP 9.3A, Verification Management Workspace) — TD-32 added (`VerificationService.RecordAsync`'s own subject→record link is never visible via `EngineeringDomainContext.RelationshipRepository`, only via the raw document store — found by nine failing tests, fully worked around at the read side, no functional gap remains). Sixth consecutive dedicated Security Review (`WP9.3A Security Review Report.md`); zero Domain-layer/Verification-Framework files touched; one genuine implementation-time finding (`TD-32`), caught by tests before any commit, not a bug in already-shipped code. Previously reviewed 2026-08-06 (WP 9.4A, Engineering Documents Workspace) — TD-31 added (`Attachment`/`IAttachment` carry descriptive metadata only — no file/URL storage capability exists anywhere in this platform, disclosed, not fixed); TD-30 confirmed still open and its consequences extended to a second discipline (no live Verification object exists for Documents↔Verification Digital Thread traceability, a direct consequence of the disclosed `WP 9.3A` numbering gap). Fifth consecutive dedicated Security Review (`WP9.4A Security Review Report.md`); zero Domain-layer files touched, zero genuine implementation defects found. Previously reviewed 2026-08-05 (WP 9.2A, Engineering Calculations Workspace) — TD-29 added (`CalculationRecord`/`CalculationRecordDto` never retain the input that produced them — `RecalculateCalculationCommand` cannot resume from stored input, disclosed, not fixed); TD-30 added (`ICalculationResult`/`IVerificationResult`/`IApprovalGate` family — declared `WP 8.2B` Domain contracts with zero concrete implementations anywhere in the platform, a pre-existing gap now formally registered for the first time; `Evidence`/`Approval` both worked around via direct relationship/status reads, not fixed). Fourth consecutive dedicated Security Review (`WP9.2A Security Review Report.md`); zero Domain-layer files touched, zero genuine implementation defects found — this Work Package's own disclosed items are both pre-existing absences, not bugs in already-real code. Previously reviewed 2026-08-05 (WP 9.1A, Requirements Management Workspace) — TD-28 added (Bulk Requirements commands — `BulkSetRequirementStatusCommand`/Owner/Priority — are deliberately plain `ICommand`, so no open view for any touched requirement is automatically refreshed after a bulk operation succeeds; disclosed, not fixed). Two further, genuine implementation defects (a permission-gated `GetEvidenceAsync` read reachable from three passive surfaces — `RequirementValidationService`, `RequirementsPropertyFacetProvider`, `EngineeringCockpit`; a `RequirementGroupDto` parent-resolution ambiguity once `MoveGroupAsync` made a second `groupedUnder` link possible) were found and fully fixed in place, not registered as debt — neither had ever been part of a commit or tagged release. Previously reviewed 2026-08-05 (WP 9.0B, Product Configuration & BOM Management) — TD-27 added (`InMemoryEngineeringObjectRepository` iteration order is unspecified; a test incorrectly assumed insertion order, found via an intermittent full-suite failure, fixed at the test/documentation level). Second consecutive dedicated Security Review (`WP9.0B Security Review Report.md`). Two further, genuine implementation defects (a `TEMPEST-VAL` validation-code collision `WP 9.0A` itself introduced; `ReviseAsync` silently discarding structural/BOM state) were found and fully fixed in place, not registered as debt — neither had ever been part of a commit or tagged release. Previously reviewed 2026-08-05 (WP 9.0A, Mechanical Product Structure) — TD-26 added (`WorkspaceManager.StartAsync` does not await Runtime Host module initialisation before returning — confirmed pre-existing on the unmodified `v0.8.0` tag, not introduced by this Work Package). First dedicated Security Review since `v0.7.0` (`WP9.0A Security Review Report.md`), closing `WP8.9.0`'s own disclosed "zero dedicated Security Reviews this release" finding; sourced no new debt item of its own beyond `TD-26` (found during Workspace integration verification, not the Security Review itself). Previously reviewed 2026-07-30 (WP 7.3A, Requirements Engine) — TD-25 added (`IRequirementsService.ReviseAsync`/`SetStatusAsync` carry no concurrency-conflict detection). Third Work Package to perform a dedicated Security Review (`WP7.3A Security Review Report.md`), sourcing this new debt item directly from it. Previously reviewed 2026-07-30 (WP 7.1E, Verification Framework) — TD-23 added (`RecordAsync`'s own multi-link sequence is not transactional); TD-24 added (`VerificationContext` imposes no bound on recorded data volume; `GetVerificationHistoryAsync` scales with total reference count); AT-17 added (no dependency on Materials for material-reference validation). Second Work Package to perform a dedicated Security Review (`WP7.1E Security Review Report.md`), sourcing both new debt items directly from it. This is the fifth and final Engineering Foundation framework Work Package. Previously reviewed 2026-07-30 (WP 7.1D, Engineering Calculation Framework) — TD-21 added (no cancellation reaches into `Calculate` once execution has started); TD-22 added (`CalculationContext` imposes no bound on recorded data volume or type fidelity); AT-16 added (no dependency on Materials for material-reference validation — open, unvalidated string references only). First Work Package to also perform a dedicated Security Review (`WP7.1D Security Review Report.md`), sourcing both new debt items directly from it. Previously reviewed 2026-07-30 (WP 7.1C, Materials Framework) — TD-20 added (`MaterialCatalog` reads a material's own full revision history to reconstruct current state — no dedicated "latest revision only" lookup exists on `IEngineeringDocumentStore`); AT-15 added (no permission-gating inside `IMaterialCatalog` itself — calling-layer enforcement expected, mirroring Reporting/Navigation precedent). Previously reviewed 2026-07-30 (WP 7.1B, Units & Quantities Framework) — TD-19 added (no affine/offset unit conversion support — Temperature deliberately deferred); AT-14 added (compile-time dimension-safety guarantee verified by direct inspection, not an automated compiler-error test). Previously reviewed 2026-07-30 (WP 7.1A, Engineering Data Model) — TD-17 added (`Content` remains string-only, no structured/typed payload); TD-18 added (no native concurrent-reference-write isolation test at scale for `LinkAsync`); TD-12 confirmed not inherited for this framework's own revision-history/reference-lookup access pattern (see `ADR-0053`). Previously reviewed 2026-07-29 (WP 6.6, Licensing) — TD-16 added (no cryptographic license file signature verification); AT-13 added (no remote validation/activation, floating/seat-based licensing, or renewal/grace-period model, all explicit approved-contract scope decisions); no existing Technical Debt item required annotation. |
| **Related Documents** | `docs/releases/v0.4.0/Risks.md`; `Validation Register.md`; `Hosted Services Register.md`; `Plugin Register.md`; `docs/security/Platform Security Review v0.5.0.md`; `docs/security/Security Roadmap.md`; `docs/architecture/Command Framework Architecture.md`; `docs/architecture/Diagnostics Architecture.md`; `docs/releases/v0.7.0/WP7.1A Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1B Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1C Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1D Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1D Security Review Report.md`; `docs/releases/v0.7.0/WP7.1E Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1E Security Review Report.md`; `docs/releases/v0.7.0/WP7.3A Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.3A Security Review Report.md`; `docs/releases/v0.9.0/WP9.0A Technical Debt Assessment.md`; `docs/releases/v0.9.0/WP9.0A Security Review Report.md`; `docs/releases/v0.9.0/WP9.0B Technical Debt Assessment.md`; `docs/releases/v0.9.0/WP9.0B Security Review Report.md`. |
| **Related ADRs** | ADR-0009, ADR-0021, ADR-0024, ADR-0025, ADR-0026, ADR-0028, ADR-0029, ADR-0032, ADR-0037, ADR-0038, ADR-0039, ADR-0040, ADR-0044, ADR-0046, ADR-0047, ADR-0048, ADR-0049, ADR-0052, ADR-0053, ADR-0054, ADR-0055, ADR-0056, ADR-0057, ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0107, ADR-0108, ADR-0109, ADR-0110, ADR-0111, ADR-0112. |
| **Related Academy Articles** | Every Work Package retrospective's own "Trade-offs" and "Architectural Debt Assessment" sections. |
| **Coverage Status** | Complete as of this baseline — every debt item disclosed by any retrospective through `WP 4.5`'s implementation is represented below, either as still Open or Resolved with the resolving Work Package named. |

---

## Governing Distinction

This project consistently distinguishes **debt** (something that should
eventually be fixed, currently costing nothing to leave alone) from a
**disclosed, accepted trade-off** (a deliberate design exclusion, not
expected to ever need fixing unless a real consumer need emerges). Both
are tracked below, in separate tables, because conflating them would
misrepresent deliberate, reasoned scope decisions as unaddressed problems.

## Entries — Technical Debt (Expected to Eventually Be Addressed)

| # | Debt Item | Since | Owning Work Package | Status |
|---|---|---|---|---|
| TD-01 | Two logging mechanisms coexist (`ILogger` vs. legacy `LoggingService`) | WP 2.6 | WP 5.2 assessed this (formerly WP 4.8); decided **not** to migrate — re-scoped forward again, no new owning Work Package named | **Open — reassessed, not resolved.** `WP 5.2`'s own investigation confirmed `Program.cs` has not called the legacy path since `WP 5.0D`; migrating code with zero live callers was judged pure risk with no behavioural benefit (`D-020`). Revisit trigger: the legacy bootstrap code is either genuinely revived or deliberately deleted. **Trigger fired, Architecture & Dead-Code Audit (2026-08-31):** the independent audit re-confirmed zero references to `LoggingService` from anywhere except `BootstrapService`, which is itself unreferenced — and found the same pattern across four further retired types (`ConfigurationService`, `HostingService`, `ProjectService`, `ProjectNumberGenerator`) plus `IProjectRepository`/`JsonProjectRepository`. `WP 5.2` declined to *migrate* this code (`D-020`); it never ruled on *deleting* it, which is this trigger's own second branch. **Approved disposition: deletion, via `WP-C`** (see `TD-110`). This row closes when `WP-C` lands. |
| TD-02 | Single-sink logging (no composite `ILogSink` fan-out) | WP 2.6 | WP 5.2 (formerly WP 4.8) | **Resolved** — `CompositeLogSink` (`ADR-0039`, `Diagnostics Architecture.md`) fans a log entry out to any number of child sinks, isolating one child's own write failure from every other, with no change to `Logger`, `ILoggerFactory`, or any existing `ILogger` consumer. |
| TD-03 | No disposal tracking for `AddInstance`-registered or reflection-constructed singletons implementing `IDisposable` | WP 2.4 / ADR-0009 | None named yet | Open — not urgent; no current platform service is disposable |
| TD-04 | `IHostedService` naming proximity to `Microsoft.Extensions.Hosting.IHostedService` | WP 4.0 / ADR-0024 | Revisit trigger: real usage evidence | **Open — trigger arguably now met, not yet acted on.** `WP 6.3` added a genuine, direct dependency on the ASP.NET Core ecosystem (`ADR-0049`) within the very same codebase that defines this platform's own, differently-shaped `IHostedService` (`Tempest.Core.BackgroundServices.IHostedService`, still unrelated to `Microsoft.Extensions.Hosting.IHostedService` in shape or namespace) — the two same-named-but-unrelated interfaces now coexist in one solution for the first time, exactly the "real usage evidence" this item's own revisit trigger named. No confusion has actually been reported; `RestApiHostedService` itself implements only `Tempest.Core.BackgroundServices.IHostedService`, never the ASP.NET Core one. Still Open, not Resolved — renaming remains a judgment call for a future Work Package, not forced by this one. |
| TD-05 | Parameterless-constructor-only constraint on discovered modules | WP 4.1 | Partially addressed — WP 4.4A/4.4B (ADR-0027) | **Partially resolved.** A module carrying `[ModuleMetadata]` may now declare a DI-resolvable constructor; a module without the attribute remains constrained exactly as before. This is a deliberate, additive lift, not a full removal of the constraint. |
| TD-06 | Plugins root directory (`Plugins/`) and manifest file name (`plugin.manifest.json`) are fixed conventions, not configurable | WP 4.2 | `WP 13.0A` (architecture — `Runtime:Plugins:RootDirectory`/`Runtime:Plugins:ManifestFileName` configuration keys designed, `Plugin Platform Architecture.md`); `WP 13.1A` (implementation, complete) | **Resolved — implemented, `WP 13.1A`.** `Runtime:Plugins:RootDirectory`/`Runtime:Plugins:ManifestFileName`/`Runtime:Plugins:Disabled` are now real, tested configuration keys, consumed in `TempestHost.cs`'s own Plugin Discovery wiring exactly as `WP 13.0A`'s `Plugin Platform Architecture.md` designed, defaulting to the original `WP 4.2` fixed conventions when absent, consulted at the seam ADR-0026 already anticipated — no Host Lifecycle phase change. See `docs/academy/03 Work Packages/WP13.1A-plugin-runtime-and-composition-root-implementation.md`. |
| TD-07 | Navigation's `Tempest.Core` placement is an open architectural question | WP 4.2D (named), pre-existing since v0.4.0 planning | WP 5.0A (formerly WP 4.6A) | **Resolved** — WP 5.0A's own Repository Investigation and `ADR-0031` settle this: the Navigation *model* belongs in `Tempest.Core`; rendering is `Tempest.App`'s own responsibility. Stale "Open"/`WP 4.6A` labels found and corrected here, `WP 5.0D`, having survived unnoticed through three prior Work Packages (`WP 5.0A`–`WP 5.0C`) that each resolved or built on this exact question without this register being updated to match — a disclosed governance-debt finding in its own right, not a new architectural gap. |
| TD-08 | Background Services would need to extend `Host Lifecycle.md`'s phase table a second time | WP 4.2D (named as future work) | WP 4.5 | **Resolved** — WP 4.5 implemented Phases 8.1/10.1 exactly per ADR-0029/ADR-0030, no renumbering. See `Risks.md` R1/R4, both now Retired. |
| TD-09 | No isolation boundary exists between a loaded plugin and a first-party module — a plugin gets identical DI-container trust once its assembly loads. **Scope widened, WP 5.1A**: this same gap now also applies to the Command Framework — a plugin's own command handler/descriptor is indistinguishable from a first-party one once registered (`Command Framework Architecture.md`'s own Security Review). One root cause, two affected surfaces, not two separate debt items. | Implicit since WP 4.2/ADR-0025/ADR-0026; named explicitly as security debt by WP 5.0S; scope widened WP 5.1A | `WP 6.1` built the mechanism (`IPermissionEvaluator`, `ADR-0044`); `WP 13.0A` designed the retrofit (`ADR-0110`/`ADR-0111`/`ADR-0112`, `Plugin Trust & Isolation Architecture.md`); `WP 13.2A` (implementation, complete) | **Resolved — implemented, `WP 13.2A`.** `PluginAssemblyLoader.EnforceTrust` checks every plugin's `RequestedCapabilities` against its assigned `PluginTrustTier`'s ceiling and reflects over every discovered `IModule` implementer's constructor for conformance against a fixed always-allowed baseline (`ILogger`/`IConfigurationProvider`/`IDiagnosticsProvider`) plus its own granted `plugin.services.resolve:*` declarations, denying at category 17 (`PluginTrustDeniedException`, recorded `PluginRegistryState.TrustDenied`) before the plugin is ever `Loaded` — real, tested, working code, not merely a design. See `docs/security/Platform Security Review v0.5.0.md` Finding SEC-01, `Security Roadmap.md` item 1, `ADR-0110`–`ADR-0112`, `docs/academy/03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md`. |
| TD-10 | `NavigationService.Unregister` performs no ownership check — any caller can unregister any other component's navigation item by ID | WP 5.0B/ADR-0032; named explicitly as security debt by WP 5.0S | `WP 6.1` built the mechanism (`IPermissionEvaluator`, `ADR-0044`); `WP 13.0A` designed the retrofit (`ADR-0111`); `WP 13.2A` (implementation, complete) | **Resolved — implemented, `WP 13.2A`.** `NavigationService.Register` now captures the registering component's own principal (`ICurrentComponentAccessor.Current`) out-of-band, alongside `_itemsById`, with no change to `NavigationItem`'s public shape; `Unregister(id)` now compares the caller's current component principal against the stored owner, rejecting a mismatch unless the caller holds the reserved `navigation.unregister.any` permission (held only by First-Party by construction) — real, tested, working code. `null`/First-Party callers proceed unconditionally, exactly as before, zero behavioural change for every actor that existed prior to this Work Package. See `docs/security/Platform Security Review v0.5.0.md` Finding NAV-1, `Security Roadmap.md` item 2, `docs/academy/03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md`. |
| TD-11 | Command and Navigation registration-order squatting: "first registration wins" rejects a *later* duplicate but does not establish the *first* registrant was the intended owner of a well-known Id — a plugin whose module Id sorts earlier (by `ModuleLifecycleManager`'s existing ascending-Id initialisation order) can legitimately claim a well-known command or navigation Id before its real owner registers | Pre-existing for Navigation since WP 5.0B/ADR-0032 (undetected until now); newly designed-in for Commands, WP 5.1A; both disclosed together by WP 5.1A's own Security Review (Finding CMD-1) | `WP 6.1` built the mechanism (`IPermissionEvaluator`, `ADR-0044`); `WP 13.0A` designed the retrofit (`ADR-0111`); `WP 13.2A` (implementation, complete) | **Resolved — implemented, `WP 13.2A`.** `NavigationService.Register`, `CommandHandlerTable.Register`, and `CommandRegistry.RegisterDescriptor` all now apply the trust-tier-ordered rule for real: first-wins only among registrants of the same tier (`PluginTrustPermission.Rank`); a higher-tier registration always evicts and replaces a lower one, regardless of order, logged loudly at `Warning` as an "ownership override," never silent. An Id-namespace-prefix convention was considered and rejected (`RD-0059`) in favour of reusing the trust-tier comparison this design already needs. Real, tested, working code — every registrant that exists today is First-Party, so zero behavioural change was observed. See `docs/architecture/Command Framework Architecture.md` Finding CMD-1, `docs/security/Security Roadmap.md` item 10, `docs/academy/03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md`. |
| TD-12 | `IPersistenceStore` has no native query or filter capability — only key lookup and full-collection key enumeration. `WP 6.5` (Audit)'s own `IAuditQuery` filters client-side, over `ListKeysAsync` plus a per-key `ReadAsync`; this is fully correct but scales linearly with the total number of stored records | WP 6.4/ADR-0041 (the shape itself); confirmed as a real, not merely anticipated, limitation by WP 6.5's own explicit Persistence Validation (`ADR-0045`) | None named yet — trigger: a real, measured performance problem or a concrete scale requirement, not speculative pre-emptive extension | Open — deliberately not extended by `WP 6.5`; see `docs/releases/v0.6.0/Risk Register.md` `R8` and `ADR-0045`'s own Persistence Validation reasoning |
| TD-13 | The REST API's own identity resolution carries no real authentication — a caller-supplied `X-Identity-Id` header is trusted outright, with no password/token/credential verification of any kind | WP 6.3/ADR-0052 (extends this release's own local-only identity model, `ADR-0043`, over a network boundary for the first time) | A genuine authentication mechanism (API keys, OAuth/OIDC, mutual TLS) being designed for a future release, once this platform is exposed beyond a trusted local/loopback boundary | Open — disclosed explicitly, mitigated (not fixed) by binding to the loopback address only by default (`ADR-0049`); this is this release's own first network-facing attack surface, and `Platform Service Contracts.md`'s own Security Considerations name it as the highest-security-sensitivity service alongside Identity & Permissions itself |
| TD-14 | No TLS is configured for the REST API's own Kestrel listener | WP 6.3/ADR-0049 | A concrete deployment scenario beyond local development naming a real TLS requirement | Open — `Platform Service Contracts.md`'s own Versioning/Security dimensions name TLS as an expectation "beyond local development," not a current-release requirement; disclosed, not silently omitted |
| TD-15 | A command handler that relies on `IAuditRecorder.RecordAsync`'s own ambient-current-principal auto-attribution will record `AuditRecorder.UnknownActorId` ("unknown") when invoked via the REST API, since `ApiRequestHandler` deliberately never establishes the ambient current principal (`ADR-0052`) | WP 6.3/ADR-0052 | A future command handler needing precise per-command Audit attribution specifically when invoked via REST | Open — the caller's own real identity is not lost, only mis-routed: it is carried correctly in the REST API's own `api.request` audit entry's `Detail[CallerIdentityId]`, just not automatically visible to a *different* command handler's own, separate `RecordAsync` call for the same request. See `WP6.3 Technical Debt Assessment.md` and `WP6.3 Future Capability Recommendations.md`. |

| TD-16 | License file contents are trusted at face value — no cryptographic signature or tamper-resistance verification of any kind | WP 6.6/ADR-0050 | A concrete license distribution scenario naming a real tamper/forgery threat model | Open — disclosed explicitly, mirroring `TD-13`'s own precedent for the REST API's undisclosed-authentication gap; extends this release's own local-trust posture (`ADR-0043`) to a second surface. No concrete distribution channel or threat model exists yet in this release's own approved scope. |
| TD-17 | `IDocumentRevision.Content` (Engineering Data Model) is a plain, opaque `string` — no structured/typed payload support; every future consumer must define and enforce its own content schema outside this framework's own contract | WP 7.1A/`WP7.0C Engineering Foundation Contracts.md`'s own disclosed Extension Point | A real, demonstrated need for framework-enforced content structure | Open — disclosed explicitly, an approved-contract scope decision, not a defect. No concrete consumer with a structured-content requirement exists yet. |
| TD-18 | `IEngineeringDocumentStore.LinkAsync`'s own concurrency behaviour under many simultaneous calls against the *same* source document is not tested at the same depth as `ReviseAsync`'s own 20-concurrent-call regression test — only sequential `LinkAsync` calls are tested | WP 7.1A | A real, demonstrated need for high-concurrency reference writes against the same source document | Open — disclosed explicitly; no atomicity concern is believed to exist (each `LinkAsync` call writes an independent, randomly-keyed entry), but this has not been proven under real concurrent load the way `ReviseAsync`'s own atomicity has. |
| TD-19 | `Unit<TDimension>` supports only a single multiplicative `ToBaseUnitFactor` — no offset term. `Temperature` (Celsius↔Fahrenheit, a genuine affine conversion) is therefore deliberately absent from `WP 7.1B`'s own starting catalogue (`Length`, `Mass`, `Duration`, `Force`, `Pressure`, `Area`, `Volume` — all purely multiplicative) | WP 7.1B/`ADR-0054`'s own "Temperature Deliberately Deferred" | A real, demonstrated need for a Temperature dimension (any future Mechanical/HVAC/Materials capability) | Open — disclosed explicitly, an approved-contract-compatible scope decision, not a defect. Resolving it requires either extending `Unit<TDimension>` with an optional offset term (a change to every existing dimension's own conversion arithmetic) or a parallel affine-unit type — a genuine design decision for a future Work Package, not assumed here. |
| TD-20 | `MaterialCatalog.FindAsync`/`ListAsync` read a material's own complete revision history (`IEngineeringDocumentStore.GetRevisionHistoryAsync`) and take only the last entry, since no dedicated "latest revision only" lookup exists on that interface — a material with many revisions pays for reading all of them on every lookup | WP 7.1C/`ADR-0055` | A real, measured performance problem with a many-revision material, or `IEngineeringDocumentStore` gaining a dedicated latest-revision lookup of its own | Open — disclosed explicitly, mirroring `TD-12`'s own "no measured performance problem yet" disclosure discipline; not believed to be a current correctness or performance concern at this framework's own current scale. |
| TD-21 | `ICalculationDefinition.Calculate` carries no `CancellationToken` — a long-running or blocking calculation definition cannot be cancelled once `ExecuteAsync` has dispatched to it | WP 7.1D/`WP7.1D Security Review Report.md` | A real, demonstrated need for cancelling an in-flight calculation | Open — disclosed explicitly; calculation definitions are trusted, first-party, in-process code, the same trust boundary the Command Framework already operates under without cancellation reaching into a handler either. |
| TD-22 | `CalculationContext` imposes no bound on how many intermediate results, constraint checks, or material references a single execution may record; separately, `CalculationIntermediateResult.Value` is not guaranteed to deserialize back to its exact original CLR type if read from durable storage later, only fully inspectable from the in-memory record `ExecuteAsync` returns immediately | WP 7.1D/`ADR-0056`, `WP7.1D Security Review Report.md` | A real, demonstrated need to bound recorded data volume, or to deserialize intermediate results back into their original CLR type from durable storage | Open — disclosed explicitly; no current consumer needs either capability. |
| TD-23 | `VerificationService.RecordAsync`'s own multi-step linking sequence (create the verification record, link it to its subject, link it to each additional document/calculation record) is not transactional — a failure partway through leaves a partially-linked record | WP 7.1E/`ADR-0057`, `WP7.1E Security Review Report.md` | A real, demonstrated need for transactional multi-document operations (`FCR-0036`) | Open — disclosed explicitly; `IEngineeringDocumentStore` itself offers no transactional multi-write primitive to build on. |
| TD-24 | `VerificationContext` imposes no bound on how many criteria, evidence entries, or links a single verification may record; separately, `GetVerificationHistoryAsync` reads every reference from a subject document (not only verification references) plus a full revision history per matching record, scaling linearly with both | WP 7.1E/`WP7.1E Security Review Report.md` | A real, measured performance problem, or a real need for bounded recording | Open — disclosed explicitly; mirrors `TD-12`/`TD-20`/`TD-22`'s own identical "no measured problem yet" disclosure discipline. |
| TD-25 | `IRequirementsService.ReviseAsync`/`SetStatusAsync` carry no compare-and-swap or expected-prior-revision parameter — two concurrent editors of the same requirement can each succeed, with the second silently becoming current and no conflict signalled to either caller. The underlying store's own per-document lock still guarantees no two revisions ever claim the same revision number | WP 7.3A/`ADR-0060`, `WP7.3A Security Review Report.md` | A real, demonstrated multi-author collaborative-editing incident against a shipped requirement set | Open — disclosed explicitly; implementing a fix now would deviate from the approved `WP7.2C Requirements Platform Contracts.md` signature without a genuine implementation defect to justify it. More consequential than the equivalent absence elsewhere, since the Requirements Engine's own target users are more likely to edit the same artefact concurrently as ordinary practice. |
| TD-26 | `WorkspaceManager.StartAsync`'s own `WaitForServicesAsync` returns as soon as `ITempestHost.Services` is non-null, which `TempestHost.cs` sets *before* running module `InitialiseAsync` — a Workspace read taken immediately after `StartAsync` returns can occur before a module-registered `NavigationItem` or seeded Engineering Domain object exists. Confirmed pre-existing on the unmodified `v0.8.0` tag, not introduced by `WP 9.0A`. Mitigated one layer up in `Tempest.Desktop` twice (`WP 10.0B`, `WP 10.1A`) before being fixed at its own true source | WP 9.0A/`WP9.0A Technical Debt Assessment.md`; consequences directly reproduced, `WP 10.0B`/`WP10.0B Technical Debt Review.md` §1; resolved, `WP 10.1B`/`WP10.1B Technical Debt Review.md` §1 | N/A — already resolved | **Resolved, `WP 10.1B`** — `WorkspaceManager.StartAsync`'s own `WaitForServicesAsync` now also waits for `IDiagnosticsProvider.HostState == HostState.Running` before returning, fixed at its own source for the first time; `Tempest.Desktop`'s own now-redundant `WorkspaceHost.WaitForHostRunningAsync` mitigation was removed. Proven by `ModuleLifecycleStabilityTests` (`tests/Tempest.Desktop.Tests`). |
| TD-27 | `InMemoryEngineeringObjectRepository`'s own backing `ConcurrentDictionary`'s `.Values` iteration order is not guaranteed (insertion order or otherwise) — `MechanicalProductStructureNodeProvider.OrderForBom`'s own fallback behaviour for a partially-numbered BOM sibling group is therefore "whatever order the repository happens to return," honest but not especially useful. Found via an intermittent full-suite test failure (passed in isolation, failed roughly one run in several as part of the full 1738-test suite) | WP 9.0B/`WP9.0B Technical Debt Assessment.md` | A real user complaint about unstable/confusing ordering for a partially-numbered BOM, or the in-memory repository being replaced by one with a real ordering guarantee | Open — the test-level and documentation-level inaccuracies that first surfaced this are already fixed (re-run four consecutive times with zero failures since); the underlying "no stable order" characteristic itself is accepted, not solved, since no ordering rule beyond "sorting"/"filtering" generally was ever specified. |
| TD-28 | `IWorkspaceCommand`'s own generic post-dispatch `RefreshAsync` targets exactly one `TargetObjectId` — `BulkSetRequirementStatusCommand`/`BulkSetRequirementOwnerCommand`/`BulkSetRequirementPriorityCommand` are deliberately plain `ICommand` (many targets, none more "the" target than another, mirroring `CompareBaselinesCommand`'s own precedent), so no open view for any requirement touched by a bulk operation is automatically refreshed. Underlying data is always correct immediately; only an already-open view's own cached display can lag | WP 9.1A/`WP9.1A Technical Debt Assessment.md` | A real user report of a stale view after a bulk operation, or a future Work Package extending `IWorkspaceCommand` (or a sibling contract) with a multi-target refresh shape (`FCR-0050`) | Open — disclosed explicitly; no data-correctness issue exists, only a display-freshness one. |
| TD-29 | `CalculationRecord<TResult>`/`CalculationRecordDto<TResult>` (`WP 7.1D`, unmodified) never retain the `TInput` that produced them — `RecalculateCalculationCommand` therefore still requires a fresh `InputJson` from the caller, identical in shape to `ExecuteCalculationCommand`; it cannot offer a parameterless "run it again with the same numbers" gesture. No data-correctness issue — every execution, first or repeated, is a fresh, correctly-recorded, fully evidentiary record | WP 9.2A/`WP9.2A Technical Debt Assessment.md` | A real UI consumer of the Calculations Workspace surface demonstrating a genuine need to re-run with the last input unchanged, or a future Work Package extending `CalculationRecordDto`/`CalculationRecord` to retain it (`FCR-0053`) | Open — disclosed explicitly; no data-correctness issue exists. |
| TD-30 | `ICalculationResult`/`IVerificationResult` (`Contracts/Calculations.cs`/`Contracts/RequirementsVerification.cs`, `WP 8.2B`) and `IApprovalGate`/`IApproval`/`IReview`/`IReviewGate` (`Contracts/Lifecycle.cs`, `WP 8.2B`) have zero concrete implementations anywhere in this platform, confirmed by a direct, whole-repository search — a pre-existing gap across two releases and three Engineering-discipline Workspace integrations, formally registered here for the first time. `EvidenceComposer`/`ITraceable.GetEvidenceAsync` resolves structurally empty for every object as a direct consequence; "Approval State" is necessarily read from `IHasLifecycle.Status` alone, never a governed sign-off record | WP 9.2A/`WP9.2A Technical Debt Assessment.md` | A future Work Package needing governed, queryable Approval/Review records, or needing `IEvidence.CalculationResults`/`VerificationResults` to actually resolve non-empty (`FCR-0051`, `FCR-0052`) | Open — disclosed explicitly; worked around via direct relationship/status reads throughout, not fixed. Confirmed still open by `WP 9.4A`, and additionally consequential for it — no live Verification Domain object exists anywhere in the platform for Documents↔Verification Digital Thread traceability to be demonstrated against (a direct consequence of the disclosed `WP 9.3A` numbering gap, see `PROJECT_STATUS.md`), so `WP9.4A`'s own Test Report instead references the closest real, live anchor available. |
| TD-31 | `Attachment`/`IAttachment` (`Tempest.Core.EngineeringDomain`, `WP 8.2C`, unmodified) carry descriptive metadata only (`FileName`/`ContentType`/`SizeInBytes`) — no actual file bytes, no resolvable file path, no URL-fetch or storage capability exists anywhere in this platform. `AttachDocumentCommand` therefore only ever records metadata about a file, never the file itself; an `"External Reference"` Document's own Content field holds a descriptive placeholder, never resolved or fetched | WP 9.4A/`WP9.4A Technical Debt Assessment.md` | 2026-08-29 attachment content storage (`ADR-0114`) | **Closed** (`ADR-0114`, implementing `FCR-0054`). An attached file is now a file this platform holds. `IBinaryPersistenceStore` is the byte shape of the **same** `PersistenceStore` — same instance, same root, sharing `TD-59`'s reserved-name-safe naming, the per-key lock, the exact-name resolution and the atomic rename outright, so no second persistence architecture exists and none of those properties was re-implemented. `IAttachmentContentStore` writes one record per attachment, keyed by the attachment's own Id, in its own collection — the same relationship to the substrate `EngineeringObjectStateStore` has (`ADR-0113`), one level down. Metadata and content stay separated: object state carries the file name, type, size and a SHA-256, and never a byte of the file, so rehydrating a whole graph loads no attachment content and a 40 MB drawing is read only when opened. Size and hash are derived from the bytes stored rather than accepted from the caller, so metadata cannot describe content the store does not hold; content is written before metadata, so a crash leaves unreferenced bytes rather than an attachment promising a file nobody stored. Reads verify hash and length and report `Available`, `Missing` or `Corrupt` — damaged bytes are never returned, and neither absence nor damage throws (`TD-60`'s discipline). Metadata-only attachments remain fully supported and are what every pre-`ADR-0114` record is. Proven by a create → attach → restart → retrieve journey through the real `WorkspaceHost` and the same `AttachDocumentCommand` the Documents workspace dispatches, with structurally real PDF/PNG/ZIP/JPEG/CSV payloads and an all-256-byte-values blob; 47 tests, seven mutations run and seven killed. Residual, disclosed and all new: `TD-95` (no deduplication), `TD-96` (whole-file reads, no streaming), `TD-97` (no orphaned-content collection). |
| TD-32 | `VerificationService.RecordAsync` (`Tempest.Core.Verification`, `WP 7.1E`, unmodified) links its own subject to a newly-created record via `IEngineeringDocumentStore.LinkAsync` directly, never through `IHasRelationships.LinkAsync` on an `EngineeringObjectBase`-derived object — so, unlike every other relationship a Verification Activity's own subject creates, the Activity→Record `"verifiedBy"` link is invisible to `EngineeringDomainContext.RelationshipRepository`, though fully durable and correctly readable via the raw document store. Found by nine failing tests during `WP 9.3A`'s own implementation, before any commit; fully worked around at the read side (`VerificationRecordReader` reads `IEngineeringDocumentStore.GetReferencesAsync` directly) — no functional gap remains in the shipped Workspace. **Disposition revisited, `WP 10.4A`, as `WP10.0A Digital Thread & Relationship Visualisation.md` §4 explicitly called for once a graph view was actually built**: `DigitalThreadGraphModel`'s own relationship read is the same `RelationshipRepository`-based bidirectional read `ObjectEditorView` (`WP 10.3A`) already established — which, confirmed by direct test (`Recentre_VerificationActivityWithARecordedResult_AddsTheResultAsAVisibleLeafNode`), does **not** surface the `"verifiedBy"` link either, an identical, previously-undisclosed gap already silently present in `ObjectEditorView`'s own Relationship summary since `WP 10.3A`, only now found. `DigitalThreadGraphModel` additionally merges in `VerificationRecordReader`'s own raw-store read for any `"VerificationActivity"` node it expands, rendering the record as a real, visible, non-editable leaf node — the first place in the Workspace/Desktop layer this link is ever shown graphically | WP 9.3A/`WP9.3A Technical Debt Assessment.md`; disposition revisited, `WP 10.4A`/`WP10.4A Engineering Review.md` §3 | A future Work Package extending `VerificationService.RecordAsync` itself to additionally link through `IHasRelationships` (`FCR-0057`), or any new code querying `RelationshipRepository` directly for a Verification result link (now confirmed to include `ObjectEditorView`'s own Relationship summary, `WP 10.3A`, disclosed here for the first time — out of `WP 10.4A`'s own scope to fix) | Open — disclosed explicitly; no data-correctness issue exists — the underlying link is always durable and always correctly read back by `VerificationRecordReader`, now reachable from two consumers (`DigitalThreadGraphModel`, and — indirectly, as the workaround — `VerificationRecordReader` itself) instead of one. **Amended 2026-08-28 (`TD-85`/`ADR-0113`), factual scope only — this row is not resolved and was deliberately not absorbed into `TD-85`:** "invisible to `RelationshipRepository`" is now *session-dependent* rather than absolute. Startup rehydration rebuilds the in-memory relationship index from the durable `DocumentReference`s the document store already held, and those include this raw-store link — so after a relaunch the Activity→Record `"verifiedBy"` edge **is** present in `RelationshipRepository`, while in the session that recorded it, it still is not. The underlying asymmetry (`VerificationService.RecordAsync` linking through the raw store rather than `IHasRelationships.LinkAsync`) is unchanged and remains this row's own subject, with `FCR-0057` still the fix. `DigitalThreadGraphModel` was made discovery-order-independent so a Verification record renders as a result leaf either way (proven by `ObjectRehydrationAcceptanceTests.AfterRelaunch_AVerificationRecord_IsStillALeaf_NotAnExpandableNeighbour`); `ObjectEditorView`'s own Relationship summary, named above, now shows the edge after a relaunch and not before — still out of scope to fix here, and disclosed rather than silently carried. |
| TD-33 | `EngineeringCockpit.FormatCoverage` (`Tempest.App.Workspace`, `WP 9.1A`, unmodified) returns a hardcoded, Requirements-specific zero-denominator string (`"— (no requirements yet)"`) regardless of what is actually being measured — already inaccurately reused by `CalculationsKpiCards`'s (`WP 9.2A`) and `VerificationKpiCards`'s (`WP 9.3A`) own "Verification Coverage" cards for a zero-Calculation/zero-Activity denominator. Found by direct inspection while designing `ManufacturingKpiCards`, before any code was written — not by a failing test. Worked around locally: `ManufacturingKpiCards`'s own "Manufacturing Readiness"/"Supplier Status" cards define a local `FormatShare` function with an accurate empty-state message instead of reusing the shared helper, avoiding a third inaccurate instance; the two pre-existing instances are left unfixed | WP 9.5A/`WP9.5A Technical Debt Assessment.md` | A future Work Package touching `EngineeringCockpit.cs` for any reason, which could parameterise `FormatCoverage`'s own empty-state message and retire `ManufacturingKpiCards`'s own local `FormatShare` duplication (`FCR-0061`) | Open — disclosed explicitly; no data-correctness issue exists — every numerator/denominator value is always correct, only the all-zero display text is discipline-mismatched for two pre-existing call sites. |
| TD-34 | `CompositeLogSinkTests.Write_AllSinksThrow_ExceptionNeverPropagatesToTheCaller` (`Tempest.Core.Tests.Logging`, `WP 5.2`, unmodified) intermittently fails when run as part of the full suite, though never in isolation — `CompositeLogSink.Write`'s own per-sink failure handler (`Console.Error.WriteLine`, line 84) races against a concurrently-running test class that temporarily redirects the process-wide `Console.Out`/`Console.Error` streams (`Console.SetOut`/`SetError` — every `[Collection("Console output capture")]`-tagged Workspace integration test does this, `WP 9.0A` onward), since xUnit parallelises test classes by default and `Console.Error` is a single, static, shared stream. First informally disclosed in narrative form since `WP 6.3` ("this project's own previously-disclosed, non-reproducible `Console.Out`-capture flake"), referenced by name in every subsequent release-closing review's own dedicated flake-check run — every one of which, until now, reported zero instances observed. **First actually observed and captured, `WP 9.9.0`'s own second verification pass**: 1 failure across 5 full-suite runs (Debug ×2, Release ×2 plus one re-run), reproduced zero times across 5 further runs scoped to the failing test class alone — consistent with a genuine cross-test-class race, not a deterministic defect. **Never formally registered as its own Technical Debt item before this entry**, despite six years' (in this project's own compressed timeline) worth of narrative disclosure — a genuine, disclosed gap in this register's own completeness, found only because this Work Package's own re-verification pass happened to catch a live instance | WP 6.3 (first informally disclosed)/`WP9.9.0 Release Readiness Report.md` (Second Pass, first formally registered) | A future Work Package either serialising `CompositeLogSinkTests` against the `[Collection("Console output capture")]` tests (via a shared xUnit collection, preventing the two from running concurrently), or replacing `CompositeLogSink`'s own direct `Console.Error.WriteLine` failure report with an injectable sink, removing the shared static stream dependency entirely | **Resolved** (`WP 16.4A`, 2026-09-04) — `CompositeLogSink` takes an injected `TextWriter` (default `Console.Error`); `CompositeLogSinkTests` injects a private writer and asserts the failure message positively; `LoggerTests`, the last redirecting class outside the `Console output capture` collection, joined it. Three consecutive clean full-suite runs. See `docs/releases/v0.16.0/WP16.4A Test Determinism Report.md`. Previously: Open — disclosed explicitly; no data-correctness issue exists — the underlying `CompositeLogSink.Write` behaviour (isolating every child sink's own failure) is correct and already proven by the same test's own repeated, isolated passes; only the test's own incidental dependency on the shared, static `Console.Error` stream is at risk under full-suite parallel execution. **Observed live and extended, `WP 13.11D`.** That Work Package's own full Release regression returned 2,561/2,562 with one failure, and an immediate re-run of the identical binaries returned 2,562/2,562 clean — the exact intermittent, parallelism-only, never-in-isolation signature described above. The failing test's identity was lost to that run's own log filter and is therefore not asserted to have been this one, but no other cause is plausible: `WP 13.11D` changed zero `src/` and zero `tests/` files, so nothing about the binaries differed between the failing and passing runs. Recorded here rather than rounded away, because a suite that intermittently red-flags a clean tree is a real cost to every future Work Package's own verification step, and each such Work Package currently has to re-run and judge for itself whether a failure is genuine. **Sibling hazard named, same root cause, found by `WP 13.11D`'s own Verification reviewer:** `tests/Tempest.Core.Tests/Logging/ConsoleLogSinkTests.cs` calls `Console.SetOut`, redirecting the same process-global stream, while sitting **outside** `[Collection("Console output capture")]` — the collection that serialises the fifty-odd other classes doing exactly this. It is therefore free to race against all of them, and against `CompositeLogSinkTests`. Adding that one class to the existing collection is the smallest available mitigation and is strictly narrower than the proposed remedy above; deliberately not applied by `WP 13.11D`, which was read-only by its own instruction. Neither the flake nor the sibling hazard blocks WP14 — no product behaviour is implicated, only test-harness isolation. **Second sibling named, `WP 13.12.2` (`DNB-11`): `WP 13.11D`'s account was incomplete.** `tests/Tempest.Core.Tests/Logging/LoggerTests.cs` (class declared line 5) calls `Console.SetError` at lines 228 and 236 and likewise carries **no** `[Collection("Console output capture")]` — verified by direct source read. That is a *more direct* collider than the `Console.SetOut` sibling already named, because `Console.Error` is the exact stream `CompositeLogSink.Write` writes to and therefore the exact stream this item's own victim test asserts against. Both unguarded classes must join the collection for the mitigation to be complete; adding only `ConsoleLogSinkTests.cs` would leave the more likely collider in place. Also corrected: the count of guarded classes is **64**, not the "fifty-odd" stated above — verified by direct count of `[Collection("Console output capture")]` declarations in `tests/Tempest.Core.Tests`, excluding five commented-out or doc-comment references. Still test-harness isolation only; still not blocking. |

| TD-35 | `ProjectExplorer.Id`/`PropertyInspector.Id` (`Tempest.App.Workspace`, `WP 8.1A`/`WP 8.1B`) were generated via `Guid.NewGuid()` per instance — fresh and random every `WorkspaceManager.StartAsync` call — meaning `ADR-0064`'s own session-persistence design could never correctly restore either panel's own placement across a genuine process restart, only appear to work within one process's own save/load cycle. Confirmed pre-existing on the unmodified `v0.9.0` tag; never previously detected because no prior test constructed two genuinely independent `WorkspaceManager` instances against the same persisted session state. Found by `WP 10.0B`'s own `PersistentLayouts_HidingAPanelAndRestarting_RestoresTheSameLayout` test, before any commit | WP 8.1A/`WP 8.1B` (introduced, undetected)/`WP 10.0B`/`WP10.0B Technical Debt Review.md` §2 (found and fixed) | N/A — already resolved | **Resolved, `WP 10.0B`** — both panels' own `Id` changed to a fixed, well-known `Guid` constant; the same test that found the defect now proves the fix (`WP10.0B Implementation Report.md` §6.3). |
| TD-36 | `PersistenceStore.DefaultRootPath` (`"persistence-data"`, `Tempest.Core.Persistence`, unmodified) resolves relative to the process's own current working directory at start — a reasonable default for a console tool launched from a known shell location, but launch-context-dependent and potentially unpredictable for a graphical application launched from a shortcut or file manager (`Tempest.Desktop`, `WP 10.0B`). Disclosed directly by `WP10.0B Security Review.md` §6 | WP 10.0B/`WP10.0B Security Review.md` §6 | A future Work Package that owns `Tempest.Core.Persistence` directly deciding on a fixed, per-user profile location (`%AppData%`/`~/.config`-equivalent) as an alternative default, if a real, demonstrated need for one emerges | Open — disclosed explicitly; no data-correctness consequence — session state remains fully functional, only its own on-disk location may be less predictable to an end user than intended. |
| TD-37 | Four `Tempest.Samples` modules (`tempest.samples.engineeringdomain`, `tempest.samples.materials`, `tempest.samples.requirements`, `tempest.samples.requirementsworkspace`) each fail their own `InitialiseAsync` with a duplicate-registration error against their own literal Id (e.g. `EngineeringDomainSampleModule` fails "A material is already registered with Id 'SAMPLE-MAT-001'"). Root cause fully determined `WP 10.1B`: not a double-invocation (`IModuleLifecycleManager` re-confirmed single-pass, no retry) — `IMaterialCatalog`/`IRequirementsService` both index their own uniqueness constraint durably via `IPersistenceStore` (`ADR-0041`), shared by design across every process launched from the same working directory; a first successful launch durably writes each module's own fixed, literal identifier, and every later launch's own re-seeding then collides with its own prior work. Confirmed by direct file-system evidence, not inference alone | WP 10.1A/`WP10.1A Technical Debt Review.md` §1 (found); resolved, `WP 10.1B`/`WP10.1B Root Cause Analysis.md`, `WP10.1B Technical Debt Review.md` §2 | N/A — already resolved | **Resolved, `WP 10.1B`** — each affected module now checks (`FindAsync`/`FindByIdentifierAsync`) before seeding and treats an already-registered identifier as its own prior work, skipping idempotently rather than failing; `Tempest.Desktop.Tests` gained genuine per-test persistence isolation, mirroring `Tempest.Core.Tests`' own `WP 7.3A` precedent. `EngineeringCockpitTests`' own three previously honest-empty assertions (`RiskSummary`/`UpcomingMilestones`/`OpenTaskCount`) now read real, stable, live data. A related, non-crashing finding surfaced during this same investigation — see `TD-38`. |
| TD-38 | `EngineeringObjectFactory<T>.CreateAsync` (`Tempest.Core.EngineeringDomain`, unmodified) enforces no business-identifier uniqueness of its own — every call unconditionally creates a fresh document with a new `Guid`, regardless of whether its own literal display identifier (e.g. `"SAMPLE-PORT-001"`) already exists. Confirmed in `EngineeringDomainSampleModule`'s own first five objects (created before its `TD-37`-affected Material registration) and in `MechanicalProductStructureSampleModule`, independent of the four `TD-37`-affected modules — a real, genuine second launch from the same working directory silently duplicates these objects' own graph, with the same displayed identifier text, rather than crashing. Compounded by a separate, pre-existing, deliberate `WP 8.2C` scope decision, re-confirmed as context here: `IEngineeringObjectRepository`/`IEngineeringRelationshipRepository` are in-memory only, never rehydrated from the durable document store on startup, so even a successfully-created duplicate is invisible to any Workspace read once its own process exits | WP 10.1B/`WP10.1B Root Cause Analysis.md` §2.3, `WP10.1B Technical Debt Review.md` §3 (found while investigating `TD-37`) | A future, dedicated Engineering Domain Work Package adding either real business-identifier uniqueness enforcement to `EngineeringObjectFactory<T>`/`EngineeringObjectBase`, or a durable-store rehydration capability for `IEngineeringObjectRepository` — both explicitly out of `WP 10.1B`'s own "No Engineering Domain changes" scope | Open — disclosed explicitly; no crash, no data-correctness issue in already-shipped Engineering Domain/discipline code — confined to sample/demonstration data silently duplicating across a genuine restart, never surfaced to a real user since every affected object is fictional sample data. |
| TD-39 | `PanelHostControl`'s own opaque `Background` (`Tempest.Desktop.Docking`, `WP 10.2B`) is a fixed `Brushes.White`, not theme-variant-aware — required once a panel can be reparented as an Auto-Hide flyout directly over the Document Area, but visually incorrect (a white panel over a dark theme) if the user has switched to Dark via the Theme Framework (`WP 10.0B`/`WP 10.2A`). Disclosed and accepted at implementation time, not found later: the identical, already-shipped, never-previously-registered limitation `CommandPaletteOverlay` (`WP 10.0B`, `Background = Brushes.Black`) already carries for this platform's only other overlay control — found by direct inspection while designing the flyout, both instances surfaced together here for the first time | WP 10.2B/`WP10.2B Engineering Review.md` §4, `WP10.2B UX Review.md` §5; resolved, `WP 10.5A`/`WP10.5A Technical Debt Review.md` §1 | N/A — already resolved | **Resolved, `WP 10.5A`** — both `PanelHostControl` and `CommandPaletteOverlay` now bind through the new `ThemeReactiveBrush` helper to `ApplicationPalette`'s own explicit Light/Dark resource keys, fixed at exactly the mechanism this entry's own "Revisit trigger" named. Proven by `VisualPolishTests.PanelHostControl_Background_IsARealResolvedThemeBrush_ClosesTD39`/`CommandPaletteOverlay_Background_IsARealResolvedThemeBrush_ClosesTD39`, re-run five consecutive times, zero failures. |
| TD-40 | `ObjectEditorView`'s own new, real, buffered dirty-state (`Tempest.Desktop.Editors`, `WP 10.3A`) is not wired into `IWorkspaceView.CloseAsync`'s own unsaved-edit prompt hook — every concrete `IWorkspaceView.IsDirty` has returned `false` unconditionally since `WP 8.1A` (by design, since no View ever buffered a local edit before this Work Package), so `DocumentAreaView`'s own tab-close path never had a reason to prompt; this Work Package's own new, genuinely buffered edits (Name/Content, before Save) can now be silently discarded by closing a tab, with no confirmation dialog. Disclosed, deliberate scope decision at implementation time, not found later — building a proper confirmation dialog was judged disproportionate scope for a first implementation | WP 10.3A/`WP10.3A UX Review.md` §5, `WP10.3A Implementation Report.md` §4; resolved, `WP 10.5A`/`WP10.5A Technical Debt Review.md` §2 | N/A — already resolved | **Resolved, `WP 10.5A`** — `DocumentAreaView.IsMarkedDirty` (new) + a real `ConfirmationDialog` (new) gate `MainWindow.CloseDocumentAsync`: a dirty tab now prompts "Discard unsaved changes?" before closing, reached from both the tab close button and the keyboard-shortcut close path; cancelling leaves the tab and its edits untouched, fixed at exactly the mechanism this entry's own "Revisit trigger" named. |
| TD-41 | `ObjectEditorView.TryCreate` (`Tempest.Desktop.Editors`, `WP 10.3A`, unmodified logic) gates unconditionally on `EngineeringDomainContext.Repository.FindAsync` resolving a real `IEngineeringObject` — confirmed by direct test, `WP 10.7A`, that this call returns `null` for every real Requirement. Requirements are real `IEngineeringDocument`s (`ADR-0058`), but were never wired into the general `IEngineeringObjectRepository`'s own Kind-to-object materialisation — only reachable through `IRequirementsService` directly, a genuinely different, pre-`WP 10.3A` read path. Consequence: opening a Requirement always falls back to `DocumentAreaView.BuildDefaultBody` (the generic Title/Kind/Id-only body), never the real `ObjectEditorView` — so Requirements' own Rename/Revise (`WP 10.2A`/`WP 10.3A`, both real, registered) and `WP 10.7A`'s own new Owner/Priority section are all correctly implemented but unreachable specifically through the Object Editor for this one discipline. **`WP 10.8A` update**: `PropertyInspectorView.AddValidationSection` (new this Work Package, also gated on the identical `EngineeringDomainContext.Repository.FindAsync` call) is a second, previously-undisclosed consumer of the same gap — a selected Requirement's own Validation section correctly reports "Real validation is not available for this object here" rather than fabricating a result, the same honest degradation the Object Editor already used, not a new instance of the underlying defect. Not a regression — the identical, already-existing `ObjectEditorViewTests.NavigateToObject_ClickedFromARelationshipRow_...` test (`WP 10.3A`) already silently defended against a `null` editor for a Requirement; never previously formally registered | WP 10.3A (introduced, undetected)/`WP 10.7A Implementation Report.md` §5 (found and formally registered)/`WP 10.8A Implementation Report.md` (second consumer disclosed) | A future Work Package either wiring Requirements into `EngineeringDomainContext`'s own general repository materialisation, or giving `ObjectEditorView`/`PropertyInspectorView` a second, parallel code path over `IRequirementsService`'s own `IRequirement` DTO directly — both a genuine architecture decision, out of `WP 10.7A`'s/`WP 10.8A`'s own "no new architecture" scope | Open — disclosed explicitly; no data-correctness issue exists — every real Requirement read/write remains fully correct via `IRequirementsService` and the Ribbon's own `"requirements.set-owner"`/`"requirements.set-priority"`/`"requirements.rename"`/`"requirements.revise"` handlers, all of which dispatch independently of `ObjectEditorView`; only the Object-Editor and Property-Inspector presentation surfaces are affected. |
| TD-42 | `scripts/new-release.ps1`'s `git tag -a "v$Version" ...` and both `git push` invocations (`origin main`, the version tag — `WP 11.1B`) carry **no `$LASTEXITCODE` verification**, unlike the script's own `dotnet build`/`dotnet test` steps, which correctly check it. Empirically confirmed, `WP 11.9.0`: a failing, unredirected `git tag`/`git push` under this environment's `$ErrorActionPreference = "Stop"` does **not** self-promote to a terminating PowerShell error — `git` writes its own `fatal:` message to inherited stderr, sets `$LASTEXITCODE` (128, reproduced), and the script silently continues past it, straight through to an unconditional `RELEASE SUCCESSFUL` banner. This script has never been run for a real release (`WP11.1B Engineering Workflow.md` §6 — every `v0.10.0`/`v0.11.0` tag was created manually) | WP 11.1B (introduced, undetected — build/test paths hardened correctly, the two most consequential steps were not)/`WP 11.9.0` QA Report (found, empirically reproduced) | Add the identical `$LASTEXITCODE -ne 0` → `throw` guard already used for `dotnet build`/`dotnet test`, after both the `git tag` and each `git push` call | Open — disclosed explicitly; no consequence yet realised, since the script has never executed against a real release. Real risk the first time it is: a rejected push (network failure, branch protection, non-fast-forward, permission denied) would report success falsely. Recommended fixed before this script's own first real-world use. |
| TD-43 | `scripts/governance-healthcheck.ps1`'s (`WP 11.2A`) top-level generic exception handler catches any uncaught error from an individual check but reports it under a single hard-coded label (`"Unhandled error in a governance check"`), losing the specific check's own name and the offending file. Empirically reproduced, `WP 11.9.0`, against an isolated fixture (never the real repository): a zero-byte register file (`Get-Content -Raw` → PowerShell `$null` → the check's own regex throws `ArgumentNullException`) and an index document with zero markdown links (`$null` piped into `ForEach-Object`, invoked once with `$_ = $null`, throws on `.ToLowerInvariant()`) both fail safely (non-zero exit, correct `[FAIL]`) but with no indication of which check or file was responsible. Distinct from the three defects `WP 11.2A` itself already found and fixed (multi-line path-extraction crash; git-unavailable crash; PowerShell single-item-count sharp edge) | WP 11.2A (introduced, undetected — outside its own disclosed fixture-testing scope)/`WP 11.9.0` QA Report (found, empirically reproduced) | Pass the specific check's own name into the per-check error handler so a CI-gate failure is diagnosable from the report alone, without log archaeology | Open — disclosed explicitly; the tool still fails safely (correct non-zero exit) today, and is not yet a required `CI Gate` check, so no merge-blocking consequence exists yet. Recommended fixed before the tool is promoted to a required gate. |
| TD-44 | TempestOS's CI pipeline (`.github/workflows/ci.yml`, `WP 11.1A`) had **never executed on real GitHub-hosted infrastructure** for any Work Package of this release, as of `WP 11.9.0`'s own sign-off. **Now executed and independently observed, `WP 11.4A`**: the branch was pushed, and both `v0.11.0`'s own tag push and a disposable verification tag push were independently observed to completion. First real execution (`v0.11.0` tag push, `WP 11.9.0`'s own Release Publication Report) found three genuine, previously-invisible defects — a flaky test (`ObjectEditorViewTests.cs`), a `governance-healthcheck.ps1` path-doubling crash, and a `ci.yml` tag-checkout `fetch-tags` collision — exactly the class of risk this item always named as possible, now replaced with evidence rather than assumption. All three fixed and re-verified on real infrastructure by `WP 11.4A`: `Build & Test` (Debug/Release) and `CI Gate` both now genuinely pass, confirmed on two independent real runs (a branch push and a tag push) | WP 11.1A (disclosed at inception)/`WP 11.2A` (same caveat re-disclosed for its own new job)/`WP 11.9.0` (reconfirmed open at release sign-off; found the three defects on first real execution, disclosed in `Release Publication Report.md`)/`WP 11.4A` (fixed all three, re-verified on real infrastructure) | N/A — resolved | **Resolved, `WP 11.4A`.** One residual, disclosed, unrelated item: `Governance Health Check` itself still reports `failure` on both real runs — not a crash (confirmed from the job log: a clean `Summary: 3 passed, 2 warned, 3 failed` followed by a correct non-zero exit), but genuine, already-tracked, pre-existing governance debt (the Academy Index/Documentation Register gaps `WP 11.2A`/`WP 11.9.0` already disclosed) that this Work Package's own scope explicitly excluded ("no feature work"). This does not affect `CI Gate`, which does not depend on this job. |
| TD-45 | Branch protection on `main` is fully documented (`WP11.1B Engineering Workflow.md` §4: required `CI Gate` status check, required CODEOWNERS review, merge-commit-only) but **not configured in GitHub** — confirmed by the workflow document's own section title, "(Recommended — Not Configured)," and by `PROJECT_STATUS.md`'s own identical disclosure. Today nothing in GitHub itself prevents a direct push to `main`, a merge of a failing PR, or a merge with no Code-Owner review | WP 11.1B (disclosed at inception, deliberately out of that Work Package's own scope)/`WP 11.9.0` (reconfirmed still open at release sign-off) | A repository administrator configures the documented branch-protection rule directly in GitHub Settings — no code change, a one-time repository-configuration action | Open — disclosed explicitly. The entire PR/CI/CODEOWNERS apparatus this release designs is currently voluntary, not mechanically enforced, until this is done. **`WP 16.1A` (2026-09-04) closed the CI-workflow half** — `CI Gate` now depends on `Governance Health Check` passing, per `WP11.1B Engineering Workflow.md` §4 — but the GitHub-settings half (branch protection itself) still cannot be performed from an engineering session; the exact settings to configure are handed to the Product Owner in `docs/releases/v0.16.0/WP16.1A Enforce the Release Gate.md`. Pointer added `WP 16.2A`; status remains Open pending that manual configuration step. |
| TD-46 | `tests/Tempest.Desktop.Tests/ObjectEditorViewTests.cs`'s `VerificationResultSection_RecordPass_ActuallyRecordsARealVerificationRecord` used a fixed `Task.Delay(50)` to wait for an async status-text update before asserting on it — reliable on a quiet local machine, lost its race on a loaded, shared GitHub Actions Windows runner (the test's first-ever real execution there). Confirmed flaky, not a regression: the identical commit passed this same test on a separate, concurrent CI run | `WP 10.3A` (introduced, undetected)/`WP 11.9.0` Release Publication Report (found, first real CI execution) | N/A — resolved | **Resolved, `WP 11.4A`** — replaced the fixed delay with a bounded (2s) poll against the real, live status text, re-read from the visual tree each iteration; confirmed deterministic by 5 consecutive isolated re-runs plus two full-suite runs (Debug/Release, 2228/2228 both), and by a real, independent pass on GitHub-hosted infrastructure. |
| TD-47 | `scripts/governance-healthcheck.ps1`'s `-SummaryPath` resolution used PowerShell's `Join-Path`, which (unlike `[System.IO.Path]::Combine`) does not discard an already-rooted child path — `ci.yml`'s own `$env:RUNNER_TEMP`-based absolute path doubled into `RepoRoot`, tripping the script's own "never write inside the repo" safety check for the wrong underlying reason, on every CI invocation | `WP 11.2A` (introduced, undetected — its own fixture testing never exercised an absolute `-SummaryPath`)/`WP 11.9.0` Release Publication Report (found, first real CI execution) | N/A — resolved | **Resolved, `WP 11.4A`** — guarded with `[System.IO.Path]::IsPathRooted($SummaryPath)`; relative-path behaviour and the genuine in-repo safety check both independently re-verified unchanged; confirmed on two independent real CI runs (branch push, tag push) — no crash, only the same pre-existing, already-tracked governance findings (Academy Index/Documentation Register gaps) reported correctly. |
| TD-48 | `.github/workflows/ci.yml`'s `governance-health-check` job's `Checkout` step set `fetch-tags: true` with no pinned `ref:` — on a tag-triggered push, `actions/checkout@v4`'s own default ref (the tag itself) collided with `fetch-tags: true`'s own fetch of that identical tag, both targeting the same local `refs/tags/<name>` path (`fatal: Cannot fetch both <sha> and refs/tags/<name> to refs/tags/<name>`), failing the job before the governance script ever ran | `WP 11.2A` (introduced, undetected — never exercised by a real tag push before `v0.11.0`'s own)/`WP 11.9.0` Release Publication Report (found, first real tag-push CI execution) | N/A — resolved | **Resolved, `WP 11.4A`** — pinned `ref: ${{ github.sha }}`, removing the competing tag-ref write target; confirmed via a disposable, non-release-pattern tag push (`ci-verify-fix3-<sha>`, deleted after verification, chosen specifically so it would not also trigger `release.yml`) — `Checkout` succeeded. |
| TD-49 | A TOCTOU (time-of-check-to-time-of-use) window exists between Plugin Discovery's own signature verification (`PluginManifestDiscoveryService`'s `AssignTrustTier`/`VerifySignature`, reading manifest and assembly bytes via `File.ReadAllBytes`) and Plugin Loading's own `Assembly.LoadFrom` call (`PluginAssemblyLoader.LoadOne`) — the verified hash is never carried forward, and the assembly bytes on disk are never re-hashed or re-verified immediately before `Assembly.LoadFrom` is called. A file on disk swapped between the two phases (a different Host Lifecycle phase each, `ADR-0026`) would load unverified content under a trust tier assigned to the bytes that were actually checked, not the bytes that were actually loaded | `WP 13.2A` (introduced, by `ADR-0112`'s own design — Discovery verifies "entirely from file bytes... before any `Assembly.LoadFrom` call," a two-phase boundary that structurally creates this window); disclosed, not fixed, `WP 13.2B`'s own commit message ("a TOCTOU window... would require carrying the verified hash forward — a structural change, not a defect fix"); formally registered for the first time, `WP 13.3A` | A future Work Package threading the Discovery-time-computed hash (or the verified bytes themselves) forward into Plugin Loading, re-verifying immediately before `Assembly.LoadFrom`, or a real, demonstrated attack/incident naming this window as its own root cause | Open — disclosed explicitly; not an `ADR-0112` conformance gap (that ADR's own Decision text promises verification "before any assembly is loaded," satisfied literally, and never promises re-verification *at* the load call site itself — see `ADR Register.md`'s own `WP 13.3A` entry for the full clause check). No known exploitation; the window requires an actor with filesystem write access to the plugin candidate folder between two phases of the same process run, already a materially privileged position. |
| TD-50 | TempestOS's own first-party publisher certificate is identified by a fixed filename convention (`PluginTrustStore.FirstPartyCertificateFileName`, the literal file `TrustedPublishers/TempestOS.cer`) rather than by a certificate attribute (Subject, a custom extension, a dedicated field) — any `.cer` file placed in the trust store folder under that exact name is treated as the First-Party trust anchor, regardless of its own certificate content, so First-Party status is a property of *where a file is named*, not of *what the certificate itself asserts* | `WP 13.2A` (introduced, self-disclosed directly in `PluginTrustStore`'s own class-level remarks at the time of writing — not found later); disclosed, not fixed, `WP 13.2B`'s own commit message ("filename-based... first-party trust store detection... not plugin-exploitable"); formally registered for the first time, `WP 13.3A` | A future Work Package moving first-party detection to a certificate-level attribute (a well-known Subject/extension TempestOS's own signing process asserts), if a real, demonstrated need for filename-independent detection emerges | Open — disclosed explicitly; low severity — mitigated today by requiring operator-level filesystem write access to `TrustedPublishers/` already, the identical privileged position `ADR-0112`'s own "no revocation checking" Negative consequence already accepts for the entire trust store. Not exploitable by a plugin itself, only by whoever controls the trust store folder's own contents. |
| TD-51 | `PluginAssemblyLoader.EnforceTrust`'s static constructor-conformance check — `HasCompliantConstructor`, and with it the `NeverEligibleServiceResolveTypes` denylist blocking `CurrentComponentAccessor`/`CurrentPrincipalAccessor` from constructor injection — was invoked only against a plugin's discovered `IModule` types, never its discovered `IHostedService` types. An `IHostedService`-only plugin assembly (zero `IModule` types) hit `moduleTypes.Count == 0` and returned immediately after the (trivially-satisfied, empty) capability-ceiling check — its hosted service's constructor was never inspected. Compounded by `HostedServiceManager` having no `componentScopeProvider`-equivalent hook at all (unlike `ModuleLifecycleManager`), so even a legitimately-passing plugin's own hosted service ran with a `null`, First-Party-treated ambient component principal. Live-PoC confirmed, `WP 13.10A`: an `UnsignedLocal` manifest requesting zero capabilities, one `IHostedService` type whose sole constructor took the denylisted, concrete `CurrentComponentAccessor` — accepted, not denied; since `WP 13.10C`'s own commit, confirmed genuinely denied | `WP 13.2A` (introduced — `EnforceTrust`'s own constructor check was written against `IModule` before `IHostedService` denial-recording existed at all); `WP 13.9.4` extended `hostedServiceTypes` *recording* for the denial path but never extended the *conformance check itself*; found, `WP 13.10A`'s own Security/Trust and Lifecycle/Composition reviewers, independently, each with a live proof-of-concept; fixed, `WP 13.10B` — `HasCompliantConstructor`/the denylist now run identically against `hostedServiceTypes`, not only `moduleTypes`, and `HostedServiceManager` gained an optional `componentScopeProvider` constructor hook mirroring `ModuleLifecycleManager`'s own established ADR-0111 hook; independently re-verified, `WP 13.10C` (four fresh, read-only reviewers, each with its own live proof-of-concept; non-vacuousness confirmed by reverting the fix and observing the targeted tests genuinely fail, then restoring) | N/A — already resolved | **Resolved, `WP 13.11B`; previously Reopened `WP 13.11A`, and before that Resolved `WP 13.10B`, independently re-verified `WP 13.10C`.** Live-PoC-confirmed fixed (retained, unchanged below): the identical `UnsignedLocal`, zero-capability, `IHostedService`-only manifest `WP 13.10A` used to demonstrate the gap is now denied at category 17, exactly like a non-compliant `IModule` type always was, and a legitimately-granted plugin's own hosted service genuinely observes its own correct ambient component principal during `StartAsync`/`StopAsync` (`AmbientPrincipalCaptureProbe`-based regression test). **`WP 13.10B`'s own independent Adversarial Security review found and fixed a compounding regression, mid-implementation, before any commit, disclosed here in full**: `DiscoverModuleTypes`'s own `WP 13.9.3` pre-resolution loop (forcing every discovered type's every constructor parameter to resolve inside the scan's own diff window) originally iterated `moduleTypes` only; once `HasCompliantConstructor` was extended to also check `hostedServiceTypes`, an `IHostedService`-only plugin with a genuinely unresolvable constructor-parameter type hit `HasCompliantConstructor`'s own unguarded `GetParameters()` call as the *first* resolution attempt for that type, with no exception handling anywhere in reach — an uncaught exception propagating out of `LoadPlugins` and `TempestHost.RunAsync` entirely, a Host-wide crash strictly worse than `TD-51`'s own original gap, not merely a denied plugin. Found twice, independently — the `IModule`-only version of this same pre-resolution fix had already landed when a second reviewer reproduced the identical gap for hosted services; a first attempted fix also mis-placed its own `try`/`catch` one level too deep (around the per-parameter body, not `GetParameters()` itself, which eagerly resolves every parameter's own signature the moment it is called) and never actually caught anything for its own documented scenario, corrected before landing. Closed by iterating both `moduleTypes` and `hostedServiceTypes` uniformly in the pre-resolution loop, converting the resulting `TypeLoadException`/`FileNotFoundException`/`FileLoadException`/`BadImageFormatException` into a `PluginTrustDeniedException`, isolating the one plugin exactly like any other trust-check failure, not the whole Host — `WP 13.10C`'s own Verification/RAM reviewer found this specific scenario had only ever been proven via throwaway proof-of-concept code across two independent reviewers, never a permanent regression test; `WP 13.10C` closed that gap directly (`LoadPlugins_FirstHostedServiceOnlyPluginHasUnresolvableConstructorParameterType_IsolatesFailure_SecondLegitimatePluginStillLoads`, non-vacuousness independently confirmed). **One small, low-severity, pre-existing (not `WP 13.10B`-introduced) gap disclosed by `WP 13.10C`'s own Security/Adversarial reviewer, deliberately not fixed here to avoid scope creep**: the "unresolvable constructor parameter type" denial path throws `PluginTrustDeniedException` directly from inside `DiscoverModuleTypes`, before `EnforceTrust` ever reaches a `RecordDenied` call site — so that one specific denied type is never added to `deniedTypeRegistry` (`WP 13.9.4`'s own execution-boundary registry), and a second, independent construction attempt is made during Module/Hosted Service Registration, safely failing for the identical reason (no code execution, `ModuleState.Failed`/hosted-service-equivalent, not a crash). Confirmed pre-existing since `WP 13.9.3` for the `IModule` axis — `WP 13.10B` merely widened this already-existing gap's reach to the `IHostedService` axis too, it did not create a new category of defect. **Correction, `WP 13.11A` Final Hardening Architecture Review — the paragraph above's own "no crash" characterization of this residual gap is confirmed accurate for the `IHostedService` axis only, and is false for the `IModule` axis; reopened as Release Blocking (for third-party plugin support) accordingly, not merely a redundant-construction inefficiency.** Independent Security/Adversarial re-examination traced the same `RecordDenied` gap through to `TempestHost.cs`'s own `WP 13.9.6` Module Discovery filter (`isTypeExcluded: deniedTypeRegistry.IsDenied`, wired specifically to prevent an unattributed module belonging to a denied plugin from ever being constructed during discovery) and found it depends entirely on `deniedTypeRegistry` actually containing the offending type — which, for the "unresolvable constructor parameter type" denial path, it never does. `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own `type.GetConstructor(Type.EmptyTypes)` call (no `[ModuleMetadataAttribute]` present) is not wrapped in any exception handling; empirically confirmed (a real, minimal repro built and torn down during this review, mirroring the CLR's own eager constructor-signature-resolution behaviour already established during `WP 13.10B`'s own `GetParameters()` investigation) that this call throws an uncaught `TypeLoadException`/`FileNotFoundException` when a type's constructor — any constructor, including an otherwise-irrelevant overload — references a parameter type that cannot be resolved, even when a compliant parameterless constructor also exists on the same type. That exception is not caught anywhere in `TempestHost.ExecuteStartupPhasesAsync`'s call to `discovery.DiscoverModules()`, and propagates through `RunAsync`'s own outer `catch (Exception ex) { EnterFaulted(ex); throw; }`, faulting and crashing the whole Host. Reachable by a single, otherwise-inert `IModule` type — any trust tier, zero requested capabilities, no `[ModuleMetadataAttribute]`, any constructor naming an unresolvable parameter type — directly contradicting `ADR-0025`'s own founding guarantee that a plugin-scoped failure must never become a platform-wide outage, the exact guarantee this whole `WP 13` plugin trust chain exists to provide. The `IHostedService` axis is confirmed genuinely safe from this specific path (`HostedServiceManager.StartServiceAsync`'s own generic `try`/`catch` correctly isolates the identical underlying failure as `Failed`, not a crash) — only Module Discovery's independent, plugin-unaware rescan lacks an equivalent guard. Recommended minimum fix, `WP 13.11B`: either (a) have `PluginAssemblyLoader.EnforceTrust`'s unresolvable-constructor-parameter-type denial path call `RecordDenied` (with whatever partial type list `DiscoverModuleTypes` had already scanned) before throwing, closing the root cause so `WP 13.9.6`'s own existing filter correctly excludes the type; and/or (b) wrap `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s `type.GetConstructor`/`Activator.CreateInstance` path in the same `TypeLoadException`/`FileNotFoundException`/`FileLoadException`/`BadImageFormatException` guard already used at three other call sites in this exact codebase [factually corrected, `WP 13.11C`: exactly ONE other call site existed — `DiscoverModuleTypes`' own; `LoadOne`'s neighbouring guard is a different three-exception filter (`BadImageFormatException`/`FileLoadException`/`IOException`) and the remaining reflection guards are single-`ReflectionTypeLoadException` catches], isolating the candidate module rather than propagating — either alone closes the crash; both together match this codebase's own established defense-in-depth convention. Not release-blocking for `v0.13.0` as currently shipped (no real third-party plugin exists in `src/Plugins/` today, so this path is presently dormant, unreachable without a loaded plugin assembly), but must be closed before third-party plugin support is actually enabled or advertised — matching this item's own original `WP 13.10A` framing exactly. See `Plugin Trust & Isolation Architecture.md`'s own updated Risks entry for full detail. **Closed, `WP 13.11B` — both recommended options, (a) and (b), implemented exactly as scoped immediately above, and no wider.** (a), the root cause: `PluginAssemblyLoader.DiscoverModuleTypes`'s own unresolvable-constructor-parameter-type failure no longer throws from inside the scan at all. It is surfaced to `EnforceTrust` through a new `out PluginTrustDeniedException?` parameter, which records every discovered `IModule` and `BackgroundServices.IHostedService` type via `RecordDenied` before throwing it — so this denial path now records exactly what the capability-ceiling and constructor-conformance paths already recorded, and `WP 13.9.6`'s own `isTypeExcluded: deniedTypeRegistry.IsDenied` filter genuinely excludes the offending type. Critically, this is **not** the "whatever partial type list `DiscoverModuleTypes` had already scanned" shape the recommendation above suggested: the fixed-point scan is now allowed to run to completion (the failing type's remaining constructors are skipped via `break`, the enclosing loops continue, and the per-step before/after `AppDomain` diff still executes) rather than aborting mid-traversal. `WP 13.11B`'s own independent Security/Adversarial analysis established that the partial-list shape would have been actively unsafe — an assembly already pulled into the `AppDomain` but not yet dequeued would have gone unscanned and unrecorded, while remaining fully visible to Module Discovery's own deliberately plugin-unaware `AppDomain` scan (ADR-0110); a well-formed module among them (attributed, parameterless constructor) would then have been registered and lifecycle-run with a `null`, and therefore First-Party-treated (`PluginTrustPermission.IsFirstParty`), ambient component principal, trading the Host crash for a silent trust bypass. (b), the backstop: `ReflectionFrameworkDiscoveryService.DiscoverModules` now wraps its own `CreateDescriptor` call in the same four-exception `TypeLoadException`/`FileNotFoundException`/`FileLoadException`/`BadImageFormatException` guard already used elsewhere in this codebase, excluding and logging the candidate rather than faulting the Host — fail-closed, never a silent inclusion, and deliberately narrow: `ModuleDiscoveryException` derives from none of the four, so the long-standing `WP 5.3` "no parameterless constructor and no `[ModuleMetadataAttribute]`" guidance and every `ValidateMetadata` failure still propagate unchanged. That class gains no plugin awareness (ADR-0110) — it is a reflection guard, not a trust decision. No ADR added or amended: `ADR-0111`'s own Decision text already mandates the recording behaviour (a) restores, verbatim, so the defect was an implementation divergence from an already-correct decision. No public API change — every touched member is `private`, except the `internal` `DiscoverModules(IEnumerable<Type>)` test seam, whose signature is unchanged. Seven permanent regression tests added across three files, covering both discovery axes and both directions (denied and legitimate); non-vacuousness confirmed by reverting each half of the fix independently and observing the targeted tests genuinely fail — with both reverted, the end-to-end Host test reproduces the exact `Expected: Running, Actual: Faulted` crash `WP 13.11A` found — then restoring. `TD-53`/`TD-54` deliberately untouched: neither is a variant of this item, and neither is naturally or directly implicated by this fix. |
| TD-52 | `IIdentityService.EstablishCurrentPrincipal` called `ICurrentPrincipalAccessor.SetCurrent` directly, and `IIdentityService` is an ordinary, DI-public, un-denylisted interface — `PluginAssemblyLoader.NeverEligibleServiceResolveTypes` blocks only the two concrete accessor types by name, not any interface that internally reaches the same write surface. A plugin declaring `plugin.services.resolve:Tempest.Core.Identity.IIdentityService` (an innocuous-looking capability request) passed `EnforceTrust` cleanly, then could call `EstablishCurrentPrincipal` for any configured identity at runtime with no ownership check; because `ICurrentPrincipalAccessor` is deliberately not call-chain-scoped (`ADR-0044`), the effect persisted ambiently for every later, unrelated caller in the process. Live-PoC confirmed, `WP 13.10A`; since `WP 13.10C`'s own commit, confirmed genuinely gated | `WP 13.2A` (introduced — the denylist was scoped to concrete accessor types only, `IIdentityService` not considered as an indirect path to the same surface); found, `WP 13.10A`'s own Security/Trust reviewer, with a live proof-of-concept; fixed, `WP 13.10B` — `EstablishCurrentPrincipal` now gates via a new dynamic `IPermissionEvaluator.RequirePermission` check against a new capability key, `plugin.identity.establish` (`PluginCapability.IdentityEstablish`), mirroring `NavigationService.Register`'s own existing ADR-0111 gate exactly; independently re-verified, `WP 13.10C` (four fresh, read-only reviewers, each with its own live proof-of-concept including a real signed multi-tier plugin scenario; non-vacuousness confirmed by reverting the gate and observing the targeted test genuinely fail, then restoring) | N/A — already resolved | **Resolved, `WP 13.10B`; independently re-verified and committed, `WP 13.10C`.** Live-PoC-confirmed fixed: a plugin declaring `plugin.services.resolve:Tempest.Core.Identity.IIdentityService` and calling `EstablishCurrentPrincipal` for an arbitrary identity now requires the separate, explicit `plugin.identity.establish` capability grant, denied by the same static capability-ceiling check (`FindIneligibleCapability`) if requested outside its trust tier's ceiling (absent from `UnsignedLocalCapabilityCeiling` by design — never grantable at the lowest tier), and additionally gated dynamically at the call site itself. Every existing first-party caller (a genuine login/session-establishment flow) observes zero behavioural change, since the check is skipped entirely, not merely satisfied, when the ambient component principal is `null` or First-Party. **Governance note, disclosed by `WP 13.10C`'s own Security/Adversarial reviewer, not a code defect**: `plugin.identity.establish` is grantable to any trust tier above `UnsignedLocal` (including `VerifiedSigned`, not only `FirstParty`), and `EstablishCurrentPrincipal` accepts an arbitrary `identityId` with no ownership check by design (`ADR-0043`, no authentication) — granting this one capability to any signed third-party publisher is effectively full ambient-identity impersonation power (including impersonating a configured administrator identity), a materially broader blast radius than every other v1 capability. This is inherent to the capability's own necessary semantics, not a code defect requiring a fix, but warrants explicit governance sign-off before any third-party publisher is actually granted it. See `Plugin Trust & Isolation Architecture.md`'s own "Closed, `WP 13.10B`" Risks entry and `ADR-0111`'s own "Corrected, `WP 13.10B`" note for full detail. |
| TD-53 | `HostedServiceManager.StartServiceAsync`'s `catch` block classifies a start failure as Host-fatal only if `tracked.Instance is ICriticalBackgroundService` — but if `_serviceProvider.GetService(tracked.ServiceType)` itself throws (construction failure, before `tracked.Instance` is ever assigned), that check evaluates against `null` and is always `false`. A hosted service that would have been `ICriticalBackgroundService` had it constructed successfully is instead silently reclassified as non-critical and merely marked `Failed`, undermining `ADR-0021`/`ADR-0029`'s own guarantee that a critical service's failure faults the whole Host. Not plugin-trust-specific and not introduced by `WP 13.10B` — a general robustness gap in construction-failure handling, present since `HostedServiceManager`'s own original implementation — surfaced by `WP 13.11A`'s own Security/Adversarial reviewer because `TD-51`'s reopened Module-axis crash (above) is one concrete way to trigger a hosted-service construction throw, safely, on this axis | `HostedServiceManager`'s own original implementation (introduced, undetected); found, `WP 13.11A`'s own Security/Adversarial reviewer | A future Work Package capturing criticality from the hosted service's own declared `Type` (via reflection, before construction is attempted) rather than from the constructed `Instance`, so a construction-time throw is classified identically to a start-time throw | Open — disclosed explicitly; low severity, not plugin-trust-specific, no known exploitation. The failure is still isolated (no crash, no unrelated-service impact) — only its own criticality classification is wrong for this one specific throw timing. |
| TD-54 | `ITempestServiceProvider` is never registered back into the `IServiceCollection` it is built from (`TempestHost.cs`), so a plugin cannot presently obtain it via `plugin.services.resolve:*` and use it to reach `CurrentPrincipalAccessor`/`CurrentComponentAccessor` directly, bypassing `IdentityService`'s own capability gate (`TD-52`) entirely. Confirmed correct today, but this closure is *incidental* — a consequence of construction ordering — rather than an explicit, load-bearing entry in `PluginAssemblyLoader.NeverEligibleServiceResolveTypes` the way the two accessor types themselves are. A future, unrelated change registering the provider into the container for a legitimate reason would silently reopen this exact path with no test or review signal naming it | `TempestHost`'s own original composition-root wiring (introduced, undetected as a latent gap); found, `WP 13.11A`'s own Security/Adversarial reviewer, during re-examination of `TD-52`'s own closure | A future Work Package adding `ITempestServiceProvider` (and `IServiceCollection`) to `NeverEligibleServiceResolveTypes` explicitly, as defense-in-depth matching the denylist's own stated rationale — cheap, no behavioural change today, closes only a latent path | Open — disclosed explicitly; not currently exploitable (the provider is not DI-resolvable by any plugin today), a preventive hardening item, not a live gap. |
| TD-55 | `PluginAssemblyLoader.RecordDenied` records every `Type` a denied plugin's own fixed-point transitive scan reached, and `PluginDeniedTypeRegistry` is keyed on `Type` identity alone, with no notion of which plugin caused the recording. Because `DiscoverModuleTypes`'s own scan deliberately follows every assembly that enters the `AppDomain` as a side effect of that plugin's own type and constructor-parameter resolution (`WP 13.9.1`/`WP 13.9.3`), a malformed or deliberately-crafted plugin whose resolution force-loads a *different*, legitimate assembly — a shared dependency, or simply a second plugin's assembly not yet loaded — causes that other assembly's own `IModule`/`IHostedService` types to be recorded denied too. The victim then passes its own trust check and is recorded `PluginRegistryState.Loaded`, yet is silently dropped at `TempestHost.cs`'s own Module Registration and Hosted Service Registration filters, leaving registry state and runtime state inconsistent with only a `Warning` to distinguish them. A denial-scoping mechanism (recording per-plugin, and excluding a type only for the plugin that actually owns it) is the correct closure, but is an architectural change to the `WP 13.9.4` registry's own shape, not a defect fix | `WP 13.9.4` (introduced, latent — `RecordDenied`'s all-discovered-types shape and the registry's `Type`-only keying both date from there); newly *reachable* on the unresolvable-constructor-parameter denial path by `WP 13.11B`, which necessarily records that path's own discovered types where previously it recorded none at all; found, `WP 13.11B`'s own independent Adversarial Review, and disclosed rather than silently accepted | A future Work Package scoping denial recording per-plugin, so a plugin can never suppress another plugin's or a first-party module's types — deliberately not attempted in `WP 13.11B`, whose own commissioning instruction was an explicitly minimal fix stopping short of architectural change beyond existing seams | Open — disclosed explicitly; not currently exploitable in either shipped host, and the pre-existing capability-ceiling and constructor-conformance denial paths already carry the identical hazard. `Tempest.Samples`, the only other assembly carrying `IModule` implementers, is provably safe by composition ordering rather than by design: `WorkspaceManager`'s own constructor force-loads it (`_ = typeof(NavigationSampleModule).Assembly;`) before `RunAsync` is ever called, so it is always inside the scan's own per-step `before` snapshot and can never be enqueued. That is a side effect of App composition, not a property of the loader — which is precisely why this is tracked rather than assumed safe. `src/Plugins/` is empty today, so the multi-plugin case is presently dormant. Fail-closed in direction (a type is wrongly *excluded*, never wrongly included), so the over-recording half is an availability and diagnosability concern, not a trust-boundary escape. **Extended, `WP 13.11C`, to name the complementary under-recording half of the same attribution defect, which is *not* fail-closed and which this item's original wording concealed.** `DiscoverModuleTypes` takes its per-step `before` snapshot only once `LoadOne`'s own `Assembly.LoadFrom` has already returned, and the fixed-point scan enqueues an assembly only if it appears in that step's own before/after diff. An assembly already resident when a given plugin is scanned is therefore never attributed to that plugin at all — `WP 13.9.1` documented this deliberately, reasoning that such an assembly is resident "for an unrelated reason", but a *second plugin* is very much a related reason. Concretely: two plugins sharing a secondary assembly of the same identity, the first scanned pulls it in and (passing trust) binds its own component principal to that assembly's module types; the second, if denied, never re-reaches those types and so never records them denied. `WP 13.11C`'s own adversarial review could not construct an actual privilege escalation from this — the shared assembly's types run under the *passing* plugin's own grants, which is the designed behaviour for a legitimate multi-assembly plugin, and a later plugin cannot substitute its own copy because the default `AssemblyLoadContext` returns the already-loaded instance — so it is recorded here as attribution imprecision of the same root cause, not as a separate exploitable finding. Per-plugin denial scoping closes both halves at once; neither half is closed by anything short of it. |
| TD-56 | A plugin's own **constructor** executes outside its component scope, so plugin code runs during construction with a `null` ambient component principal — which every capability gate treats as genuine first-party and skips entirely. On both axes the scope is pushed *after* the instance is created: `ModuleLifecycleManager` resolves the instance (`tracked.Instance = ResolveInstance(tracked.Descriptor)`) and only then opens `using var scope = _componentScopeProvider?.Invoke(moduleId)` around the lifecycle method; `HostedServiceManager.StartServiceAsync` does the same, resolving via `_serviceProvider.GetService(tracked.ServiceType)` before opening its own scope. `PluginTrustPermission.IsFirstParty(null)` is `true` and `Rank(null)` is `3` — the top tier — so within the constructor window `NavigationService.Register`, `CommandRegistry.RegisterDescriptor`, `IEventBus.PublishAsync`, and `IdentityService.EstablishCurrentPrincipal` all skip their `RequirePermission` call and the plugin additionally wins every ownership-override tie against real first-party components. Exploitation requires the plugin to obtain an abusable collaborator as a constructor parameter, which `HasCompliantConstructor` restricts to the fixed baseline plus its own granted `plugin.services.resolve:*` keys — so an `UnsignedLocal` plugin **cannot** reach it (that tier's ceiling admits only `plugin.navigation.register`/`plugin.commands.register`, no `plugin.services.resolve:*` key at all, and the baseline is `ILogger`/`IConfigurationProvider`/`IDiagnosticsProvider`), but a `VerifiedSigned` or `FirstParty` plugin can: `IIdentityService` is DI-registered (`TempestHost.cs`), is not in `NeverEligibleServiceResolveTypes`, and is a recognised `plugin.services.resolve:*` shape, so a signed plugin granted only `plugin.services.resolve:Tempest.Core.Identity.IIdentityService` — and deliberately *not* `plugin.identity.establish` — can call `EstablishCurrentPrincipal` from its own constructor and set the process-wide current principal, defeating the exact gate `TD-52` exists to enforce, for the exact population `TD-52` constrains. The module must carry `[ModuleMetadataAttribute]` so Module Discovery reads its metadata instead of instantiating it (ADR-0027), leaving the DI-constructed lifecycle path as the first and only construction. **Most severe variant, because it outlives the window:** `EventBus.Subscribe` captures `var owner = _currentComponentAccessor?.Current` at subscribe time, and `PublishAsync` opens a scope only `when subscription.Owner is not null` — so a handler subscribed from a constructor is recorded with a `null` owner and is therefore dispatched with no component scope, running as first-party for the remaining lifetime of the process, long after the constructor returned | `ADR-0111`'s own component-scope hook as originally implemented (`WP 13.2A` for modules, extended to hosted services by `WP 13.10B` for `TD-52`) — both wired the scope around the *lifecycle method* only, which is what the ADR's own "each of that module's own lifecycle calls" wording describes; construction was never in scope on either axis, and no `WP 13.9.x`/`13.10x`/`13.11x` review examined the constructor window because every ambient-principal regression test probes `StartAsync`, which is correctly scoped. Found, `WP 13.11D`'s own Security/Adversarial exit reviewer; every link independently re-confirmed against real source before this entry was written | A future Work Package moving the component scope to enclose construction on both axes — opening `_componentScopeProvider?.Invoke(...)` *before* `ResolveInstance`/`GetService` rather than after, so the constructor runs under the same principal its lifecycle methods already do. The scope key (`moduleId`, `tracked.ServiceType`) is available before construction on both paths, so this is a statement-ordering change within the existing `ADR-0111` hook, not a new seam. `EventBus.Subscribe`'s owner capture then becomes correct for free. Must be paired with regression coverage that probes the **constructor**, not only `StartAsync` — the existing `AmbientPrincipalCaptureProbe` records inside `StartAsync` and so passes today regardless | Open — **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support.** (Wording normalised by `WP 13.12.0`: the earlier grade read "Release Blocking for third-party plugin support", which collided with `ADR-0106` §4's formal term "Release Blocking" — a term that admits no qualifiers and, if it applied, would force a `NOT READY` release verdict. The underlying technical classification is unchanged; this item does not block `v0.13.0` as shipped, and does not block WP14.) Dormant today: `src/Plugins/` contains only `README.md`, no third-party plugin exists, third-party plugin support is neither enabled nor advertised, and the path is unreachable without a plugin signed by a certificate already in `TrustedPublishers/` — an actor who by definition already cleared the platform's own publisher-trust bar. Causes no crash and no instability; the Host runs normally. Entirely orthogonal to UI/UX work, which is why `WP 13.11D` declares the platform ready for WP14 while tracking this: WP14 neither depends on this path nor makes it worse. Must be closed before third-party plugin support is actually enabled or advertised — the same framing `TD-51` carried, and for the same reason. Fail-**open** in direction, unlike `TD-55`: a plugin gains authority it was not granted, rather than losing authority it held, so this is a genuine trust-boundary escape and is graded accordingly. |
| TD-57 | Governance-register currency has drifted across v0.13.x: the Interface Register is missing all 8 `WP 13.x` plugin-trust interfaces (`ICurrentComponentAccessor`, `IPluginTrustStore`, `IPluginRegistry`, `IPluginRegistryRecorder`, `IPluginDeniedTypeRecorder`, `IPluginDeniedTypeRegistry`, `IPluginComponentPrincipalRecorder`, `IPluginComponentPrincipalRegistry`); the Exception Register is missing the six v0.13.0 plugin-trust exception types with no covering staleness disclosure; the DI Register is missing the `ICurrentComponentAccessor` dual registration (`TempestHost.cs:520-521`); the Namespace Register is missing every namespace introduced after WP 6.6 (ten `Tempest.Core.*` Engineering namespaces plus `Tempest.App.Workspace[.*]`) while still claiming "Complete", with wrong file counts on several existing rows; the Platform Services Register has no row for the v0.13.0 plugin-trust platform; and the Validation Register asserts a WP 5.3-era world (`VERSION` "still 0.4.0", 552 tests) as current fact with no staleness disclosure. Numbering note: a parallel Companion work stream (branch `claude/tempestos-companion-mobile-ubznt3`) has independently allocated `TD-57`/`TD-58` on its own copy of this register — the two streams' numbering must be reconciled at merge. | 2026-08-28 independent closure-verification review | `WP 16.2A` (Register and Status Currency) | **Resolved.** Every register named above re-derived directly against source at `WP 16.2A`'s own base commit, not incremented from any prior figure: Interface Register 174 → 188 (15 backfilled rows, including all 8 `v0.13.0` plugin-trust interfaces named here, plus 7 `v0.14.0` durability interfaces; 1 stale row removed); Exception Register 55 → 84 (29 backfilled rows, including the six `v0.13.0` plugin-trust exception types named here); DI Register 44 → 50 named registrations (`ICurrentComponentAccessor`/`CurrentComponentAccessor` dual now present, plus 5 more `v0.14.0` registrations); Namespace Register 29 → 46 namespaces / 272 → 713 files (full re-derivation, first since `WP 6.6`, `Tempest.App.Shell`'s wrongly-claimed "0 — retired" corrected to its real 10-file count); Platform Services Register 32 → 35 entries (Plugin Trust & Capability Enforcement, Engineering Object Durability & Rehydration, Attachment Content Store added); Validation Register's `VERSION 0.4.0`/"552 tests" claim corrected with a "Current State" section stating `VERSION 0.15.0`, 2,725 test-method attributes, and the last real CI totals (3,088/408 at the `v0.15.0` tag, 412 Desktop after `WP 15.2A`). Full derivation: `docs/releases/v0.16.0/WP16.2A Register and Status Currency Report.md`. **Residuals, disclosed rather than silently left:** `docs/architecture/Platform Service Map.md` has not received the three new Platform Services' own detail sections — out of `docs/governance/**`, this Work Package's own ownership. `src/Tempest.Desktop/` remains explicitly excluded from the Namespace Register's declared scope (88 files, 9 namespaces) — a reaffirmed, not revisited, decision. `WP 16.3B` (concurrent, not on this Work Package's own base) will add `IStateMigration`/`IStateMigrationRegistry`/`ISettingsMigration<T>` to the Interface Register only at its own merge. The Companion numbering-collision note above is unresolved by this closure — it depends on a merge this Work Package does not perform. |
| TD-58 | Redundant Desktop rebuilds: every `RibbonView` command click tore down and rebuilt the entire ribbon tab tree (`RecordRecent` → `Rebuild()`) just to update the 5-item "Recently Used" row — destroying keyboard focus and running a second full `RefreshEnablement` pass per click — and every `ActionCompleted` fan-out (`MainWindow` ribbon handler, `WorkspaceViewCoordinator` explorer/inspector/editor handlers, `UndoRedoCoordinator`, Command Palette) reloaded the full Project Explorer and rebuilt the full Cockpit **unconditionally**, including for pure refusals ("needs a selected object first") and failed commands, always with a Success-severity toast; the Cockpit "Favourite Projects" open double-rebuilt all cards through two parallel paths; and mutating flows that *should* refresh did not (macro runs and palette-invoked commands refreshed no Explorer; a deleted object stayed selected with Delete/Rename still enabled; `BackgroundTaskRunner.Changed` and the platform-notification toast bridge had no live subscriber at all). | WP 10.3B–10.7A (accumulated) | 2026-08-28 closure pass | **Resolved** — the Desktop-wide `ActionCompleted` convention now carries an `ActionOutcome` (`Succeeded`, `WorkspaceChanged`): every subscriber reports severity honestly and refreshes dependents only when the workspace actually changed; `RecordRecent` updates the per-tab "Recently Used" row in place (tab instances and focus survive, single enablement pass — pinned by `RibbonViewTests.CommandClicks_DoNotRebuildTabs_AvoidSpuriousEnablementPasses_AndUpdateRecentRowInPlace`, which fails under a restored-`Rebuild()` mutation); the favourite-open inline `Refresh()` was removed (the navigate path already refreshes); `WorkspaceManager.DeleteObjectAsync` clears a deleted current selection (every delete surface converges there); macro/palette successes now reload Explorer+Cockpit; `BackgroundTaskRunner.Changed` drives the Output panel; and the toast bridge is subscribed on `INotificationDispatcher` (the channel producers actually publish through) with UI-thread marshalling — pinned by `MainWindowCompositionTests.PlatformNotification_PublishedThroughTheRealDispatcher_ReachesAVisibleToast`. Remaining refresh-architecture debt (O(everything) Cockpit rebuild internals, editor-tab staleness on external mutation) is tracked separately as `TD-66`. |
| TD-59 | Reserved device-name identifiers silently collapsed into missing records: `PersistenceStore` mapped keys/collections to file names via plain `Uri.EscapeDataString`, which leaves `CON`, `PRN`, `AUX`, `NUL`, `COM1`–`COM9`, `LPT1`–`LPT9` (any casing, with or without extension) untouched — on Windows those writes are routed to devices and `File.Exists` then reports them absent, so e.g. `MaterialCatalog.RegisterAsync("CON", …)` returned success while `FindAsync`/`ListAsync` never saw the record again (orphaning its backing document); trailing-dot keys aliased their dotless siblings (Win32 strips trailing dots); `"."`/`".."` were unrepresentable; and case-variant keys silently overwrote each other's single file on case-insensitive file systems (with the per-key lock not even contending, since `LockKey` used the raw strings). The class remarks' claim that an arbitrary key "can never produce an invalid or unintended file-system path" was false, and no test fed the store a single hostile name. | WP 6.4 (PersistenceStore), surfaced 2026-08-28 independent review | 2026-08-28 closure pass | **Resolved** — at the persistence boundary (protecting every consumer, per the review's "escape/encode safely at persistence boundary" option): `EncodeSegment` percent-escapes the first character of a reserved device stem and any terminal dot (decode remains exactly `Uri.UnescapeDataString`; safe names encode identically to before, keeping existing stores valid; legacy-encoded records remain readable and migrate forward on write); reads/deletes match the stored file name exactly (never a case-variant) and a case-colliding write is refused loudly instead of silently overwriting; the lock key is the encoded, case-folded file identity; writes are temp-file + atomic-rename. Pinned end-to-end over the real file-backed store by `PersistenceStoreHostileNameTests` (full NUL/CON/PRN/AUX/COM1/LPT1/case/extension/trailing-dot/dot-name/traversal matrix) and `MaterialCatalogHostileDataTests`/`RequirementsServiceHostileDataTests` (identifier creation → validation → persistence/index → `ListAsync` → lookup, duplicates, adjacent-valid names); mutation-verified (disabling the reserved-stem escape fails 6 tests). Windows-only aspects (device routing itself, case-insensitive collision at runtime) cannot manifest on the Linux CI host — the tests pin the cross-platform invariants that make those failures impossible (no reserved file stem is ever produced; exact-name matching; refusal path), disclosed here rather than overclaimed. |
| TD-60 | Malformed stored values threw uncontrolled exceptions from passive read paths: a corrupted `Materials.Index`/`Requirements.Index` value hit `Guid.ParseExact` → raw `FormatException` from `FindAsync`/`ListAsync`/`RegisterAsync`'s duplicate check (one poisoned or foreign file — a `.DS_Store` in a registry directory — aborted whole listings); corrupted document/revision/reference/audit/verification JSON leaked raw `JsonException` (and a literal `null` document record was silently misclassified as "does not exist"); and every Desktop state loader (`UserSettings`, `WindowUiState`, `RecentObjectsState`, `FavouriteObjectsState`, `DesktopPanelUiState`, `WorkspaceState`) plus `MacroManager.LoadAsync` threw raw `JsonException` on a torn-write-corrupted value — in the composition root, bricking startup permanently until the file was hand-deleted, despite each loader's own "never an exception" doc contract. | WP 6.4–10.x (accumulated), surfaced 2026-08-28 independent review | 2026-08-28 closure pass | **Resolved** — corruption now surfaces as each framework's own controlled exception type, never a raw BCL exception and never misreported as absence: `Guid.TryParseExact` with a corruption-naming `MaterialsException`/`EngineeringDataException` in both index readers (a corrupt entry can no longer be silently overwritten by re-registration either); non-GUID registry *keys* (foreign files) are skipped with a warning; stale index entries (backing document missing or wrong Kind) read as absent and no longer abort `MaterialCatalog.ListAsync` (ported from `RequirementsService`'s own guard); `JsonException` is wrapped into `EngineeringDataException`/`MaterialsException`/`AuditException` at every store read; Desktop loaders and `MacroManager` degrade to their documented first-run defaults (one corrupt macro entry no longer aborts the healthy rest). Pinned by `MaterialCatalogHostileDataTests`, `RequirementsServiceHostileDataTests`, `EngineeringDocumentStoreCorruptionTests`, `AuditQueryCorruptionTests`, `MacroManagerCorruptionTests`, `CorruptedStateLoadTests`; mutation-verified (reverting to `Guid.ParseExact` fails 6 tests). |
| TD-61 | `PluginManifestDiscoveryService`'s "assembly must stay within its own folder" containment (`IsWithinFolder`) is a purely lexical `StartsWith` check over `Path.GetFullPath`, which does not resolve symlinks — a plugin folder containing `Plugin.dll` as a symlink to a target outside the folder passes containment while both signature verification and `Assembly.LoadFrom` follow the link; combined with `Plugins:AllowUnsignedLoad=true` this pulls arbitrary external DLL content into the process at the clamped tier, and it composes with `TD-49`'s verify/load TOCTOU (the swapped link target need not be inside the folder at all). No symlink test exists. | 2026-08-28 independent security review | Unassigned | **Open** — same "must-fix before third-party plugins are enabled" bar as `TD-49`/`TD-56`; fix shape: resolve the real path (`File.ResolveLinkTarget`) before the containment check. |
| TD-62 | The REST API's OpenAPI document route is served with no identity and no permission check (`RestApiHostedService.InvokeOpenApiAsync`), disclosing the complete route → required-permission map to any unauthenticated loopback caller — a genuine widening of `TD-13`: the caller is told exactly which identity claim unlocks which capability before authenticating at all, and `RestApiHostedServiceTests` locks the unauthenticated 200 in as intended behaviour. | 2026-08-28 independent security review | Unassigned | **Resolved** (`WP 16.4B`, 2026-09-05). The OpenAPI document route now runs through `ApiRequestHandler`'s own identity, permission and audit path — the same `AuthorizeAsync` every command route uses — guarded by a new `ApiRequestHandler.OpenApiDocumentPermission` (`api.openapi.read`), so an unauthenticated caller receives the identical `401` a command-route caller with no identity receives, and a caller lacking the permission the identical `403`. `RestApiHostedServiceTests`' own assertion of the unauthenticated `200` was asserting the defect and now asserts the fix. Separately, and independently, the listener itself is off by default (`D-024`, **Proposed**) so the route is unreachable in a default install at all. Disclosed: no default identity is granted `api.openapi.read`; whether any operator identity should hold it is an Identity/Product question this Work Package did not answer. |
| TD-63 | `TD-40`'s closure (dirty-tab close confirmation) is not pinned on its production path: the tests cover the `ConfirmationDialog` control in isolation and the `MarkDirty` flag, but deleting the `IsMarkedDirty` guard in `WorkspaceViewCoordinator.CloseDocumentAsync` — reintroducing the exact original silent-discard defect — leaves the entire suite green. | 2026-08-28 closure-verification review | Unassigned | **Open** — one headless test driving the real close path over a dirtied editor kills the mutation. |
| TD-64 | `TD-52`'s closure is real at unit level but its composed-host wiring is unpinned: replacing the `IIdentityService` registration with a manually constructed gate-less instance (`TempestHost.cs`) silently disarms the `plugin.identity.establish` gate in production with the whole suite green, and no permanent end-to-end test exercises the original attack shape (a real plugin granted `plugin.services.resolve:…IIdentityService` denied at runtime); `TD-56` additionally documents a live constructor-window bypass of this exact gate, which `TD-52`'s own Resolved status cell does not cross-reference. | 2026-08-28 closure-verification review | Unassigned | **Open** — add the composed-host deny test and the permanent end-to-end scenario; cross-reference `TD-56` from `TD-52`'s row. |
| TD-65 | Systemic Desktop accessibility gaps: the dialog family (`ConfirmationDialog`, `MessageDialog`, `SettingsDialog`, `MacroManagerDialog`, `CommandPaletteOverlay`) are `IsVisible`-toggled `Border`s with no focus move/trap/restore, no Escape/Enter handling (except `InputDialog`'s own), and fully tab-reachable content behind them; every asynchronous feedback channel (StatusBar, toasts, all editor/dialog status `TextBlock`s) is visual-only with no automation live-setting; `AutomationProperties` is used nowhere (icon-only buttons and watermark-only inputs have no accessible names); the Digital Thread graph is entirely pointer-only (no keyboard pan/zoom/node focus, 1.4 px line hit targets); object move is drag-and-drop-only; the declared `FocusRing` design tokens are consumed by no style; several hit targets sit below the codebase's own `MinControlSize` floor; and Cockpit health text is painted in status colours at ~2.1:1 contrast on the light theme. | 2026-08-28 independent accessibility review | Unassigned | **Partially resolved.** `WP 16.5A` delivered the baseline — six genuinely modal dialogs (focus trapped and restored, proved by walking every Tab stop with `KeyboardNavigationHandler.GetNext`), automation names, live regions, Digital Thread graph keyboard operability for nodes/zoom/pan, and measured Cockpit contrast fixes. **`WP 16.5A-R1` then closed six defects in that baseline** found by the `v0.16.0` review board, the blocker being a `:focus-visible` ring painted in a brush resolving to the same colour as the Primary button's own fill — 1.00:1 in both themes, on the shell's single call-to-action — guarded by a test that compared the ring only against its own token and so could not fail. Every treatment × theme × state now clears the 3:1 UI-component floor, independently re-measured at the post-remediation review. **Remaining inventory, each with its own row rather than buried here:** `TD-128` (graph relationship edges keyboard-unreachable), `TD-131` (the focus-ring test cannot see any `Flat`-treatment state), `TD-132` (duplicate "Open" automation names in the Object Editor), `TD-133` (docking rearrangement is mouse-only), plus `WP 16.5A`'s own disclosed residuals (calculation-input textbox name, chrome-button hit targets). This release delivered a baseline, not a conformance claim; a v1.0 claim needs those closed and a full WCAG audit. |
| TD-66 | Remaining refresh-architecture debt beyond `TD-58`'s closure: `CockpitView.Refresh()` rebuilds all ~20 cards with dozens of sync-over-async full-repository scans on the UI thread for every workspace change (per-card/incremental refresh is the fix shape); `ProjectExplorerView.LoadAsync` refetches the whole tree and loses selection/expansion state on every mutation; open editor tabs and tab titles never refresh on external mutation of their object (`NavigationService.OpenAsync` returns the stale view; rename/undo from another surface desyncs `_originalName`); `DigitalThreadGraphView` fully rebuilds per search keystroke/legend toggle/selection; the startup path builds the Cockpit twice; `NavigationService.JumpToAsync` duplicates views for already-open objects; and there is still no domain-mutation event on the bus (`WorkspaceSelectionSetChangedEvent` is published with zero subscribers) — every refresh is hand-wired. | 2026-08-28 independent UI review | Unassigned | **Open** — the outcome-gated `ActionCompleted` convention (`TD-58`) is the substrate an incremental/domain-event refresh model can now build on. |
| TD-67 | Crash-window write ordering creates undetectable orphans: `EngineeringDocumentStore.CreateAsync` writes the document record before revision 1 (a crash between leaves a document whose `GetRevisionHistoryAsync` throws "internally inconsistent"; the reverse order is crash-safe, as `ReviseAsync` already demonstrates); `MaterialCatalog.RegisterAsync`/`RequirementsService.Create*` create the backing document first and index it second, so a crash or index-write failure (disk full, overlong escaped name) strands a permanent, invisible orphan document with no reconcile/repair path; `VerificationService.RecordAsync`'s record-then-N-links sequence can leave a partial evidence graph; `MoveToGroupAsync`/`MoveGroupAsync` append new `GroupedUnder` references without removing prior ones, and `MoveGroupAsync` checks neither parent Kind nor ancestry cycles. | 2026-08-28 independent persistence review | Unassigned | **Partially resolved** (`WP 16.4B`, 2026-09-05) — three of the five defects this row names are closed, one is not a defect, one is genuinely still open. **Closed:** `EngineeringDocumentStore.CreateAsync`'s ordering inverted to write revision 1 before the document record, mirroring `ReviseAsync`, so a crash between the two leaves an invisible orphaned revision rather than a document whose `GetRevisionHistoryAsync` throws; `MoveGroupAsync` gained both a parent-Kind check and an ancestry-cycle check (`RequirementGroupCycleException`); and the reconcile/repair path this row called absent now exists as `IRequirementsReconciliationService` and `IMaterialCatalogReconciliationService`, both registered in `TempestHost` Phase 6 and both explicit `DetectAsync`/`SweepAsync` — nothing runs a sweep automatically. **Not a defect:** the `MoveToGroupAsync`/`MoveGroupAsync` "append without removing" claim — see the correction recorded below, referred to Technical Review. **Still open:** `MaterialCatalog.RegisterAsync`/`RequirementsService.Create*`'s document-then-index ordering cannot be cheaply inverted (unlike `CreateAsync`, each obtains its Id *from* `CreateAsync`, so there is nothing to index beforehand without changing that interface) — the sweep is a mitigation, not a fix; and `VerificationService.RecordAsync`'s partial evidence graph is not mitigated by anything built here. Full disposition: `docs/releases/v0.16.0/WP16.4B-3 Durability.md`. **One of the five defects named above is not a defect — corrected 2026-09-05, `WP 16.4B`, after direct investigation rather than silently.** The claim that `MoveToGroupAsync`/`MoveGroupAsync` "append new `GroupedUnder` references without removing prior ones" describes documented, tested, intentional behaviour: `RequirementDto.GroupId` and `RequirementGroupDto.ParentGroupId` are the **live** membership value (a disclosed `WP 9.1A` fix made specifically to stop resolving membership from the relationship graph, for the very ambiguity this row cites), and the `GroupedUnder` link is deliberately permanent history — `IRequirementsService.cs:96,106` says "permanent" in the contract, `RequirementsLifecycleExtensionsTests.MoveToGroupAsync_ASecondTime_PreservesTheOriginalGroupedUnderLinkAsHistory` asserts it, and `EngineeringObjectBase.MoveAsync` already ships the identical design. Removing the prior link would reintroduce the bug `WP 9.1A` fixed. **This is a conflict between this register and the code's own documented design, referred to Technical Review rather than resolved by either side unilaterally.** |
| TD-68 | `AsyncKeyedLock` grows without bound: `GetOrAdd` creates one `SemaphoreSlim` per key and nothing ever removes entries — five independent instances (PersistenceStore, SettingsProvider, EngineeringDocumentStore, MaterialCatalog, RequirementsService) leak one semaphore per distinct key/document/identifier for process life; `Releaser`'s double-dispose guard is a plain bool (not `Interlocked`), so concurrent disposal can double-release a `SemaphoreSlim(1,1)`. | 2026-08-28 independent architecture review | Unassigned | **Resolved** (`WP 16.4B`, 2026-09-05). Each key's `SemaphoreSlim` now sits behind a reference-counted entry removed from the dictionary and disposed when its last holder releases. The removal race is closed by holding the per-entry lock across both the refcount decrement and the `TryRemove`, so a retired entry's tombstone can never become visible before the entry has already left the dictionary — a concurrent acquirer at worst retries once. A cancelled `WaitAsync` now releases the reservation it took rather than leaking it. The double-dispose guard is `Interlocked.Exchange`. Five tests, including an iteration-bounded (not wall-clock) interleaved stress test. |
| TD-69 | The DI container has two latent defect classes: `ServiceCollection.Add`/`AddInstance` silently overwrite an existing registration for the same service type (last-wins, no exception, no log — a mistaken re-registration of `IEventBus` would silently swap the platform implementation), and `TempestServiceProvider.Construct` resolves every constructor parameter unconditionally, never honouring `ParameterInfo.HasDefaultValue` — platform types declare optional parameters (`EventBus(ILogger? logger = null, …)`) that the container would fail on the moment one's type is unregistered. | 2026-08-28 independent architecture review | Unassigned | **Resolved** (`WP 16.4B`, 2026-09-05). `ServiceCollection.Add`/`AddInstance` throw `DuplicateServiceRegistrationException` on a colliding registration; a deliberate replacement passes `allowReplace: true`, so no existing caller was silently changed in meaning. `TempestServiceProvider.Construct` falls back to `ParameterInfo.DefaultValue` when — and only when — the parameter's own type has no descriptor at all and the parameter is optional; a registered type that fails to resolve for any other reason still propagates exactly as before. The change found a real latent bug the old last-wins behaviour was hiding: eight `Samples` integration fixtures each registered `IPermissionEvaluator` twice, the first registration dead code. |
| TD-70 | The desktop workspace did not adapt to available screen space at all: both side docks were declared `ColumnDefinition(240, GridUnitType.Pixel)` — fixed — with only the centre `Star`, so every pixel a shrinking window lost came out of the Document Area, and at the application's own `MinWidth` of 960 px the two side panels consumed **half the window**; the ribbon held a further fixed 145 px with no minimise affordance. Confirmed categorical by repository-wide search: no `SizeChanged` handler, no `Bounds` observation, no breakpoint and no compact mode existed anywhere in `Tempest.Desktop`. Measured empirically on the real `MainWindow` at seven widths by a headless probe before any change. | 2026-08-28 Product Compliance Audit (brief §4, "major non-negotiable") | 2026-08-28 audit pass | **Resolved** — `DockingGrid.ApplyResponsiveLayout` observes `Bounds` and fits the docks into what remains after reserving `MinDocumentAreaWidth` (420 px)/`MinDocumentAreaHeight` (220 px): unchanged when they already fit (no surprise in the common case), squeezed proportionally when they do not, and collapsed to the 32 px strip rather than squeezed below a readable `MinUsablePanelSize` (140 px). The user's own preferred sizes are held separately from the applied widths and restored in full when the window grows back — a responsive rule never rewrites user intent. `RibbonView.SetCollapsed`/`ToggleCollapsed` minimises the ribbon to its tab strip (double-click a tab, or View ▸ Minimise Ribbon), persisted via `DesktopPanelUiState.RibbonCollapsed`, with every tab header kept reachable so no command is lost. Pinned by `ResponsiveWorkspaceTests` (12 tests incl. a 7-width theory); mutation-verified — disabling the clamp fails 5 tests, a no-op ribbon collapse fails 2. Residual compact/icon-only presentation and the 960×600 floor are tracked separately as `TD-73`, not folded in here. |
| TD-71 | A `GridSplitter` drag mutates the `ColumnDefinition` directly and never calls `SetLeftWidth`, so `DockingGrid`'s preferred-width field was never updated by a drag — hiding and re-showing a panel after dragging it silently reverted to the pre-drag width. Undetected because the existing suite drove `SetLeftWidth`, not the drag path. | 2026-08-28 Product Compliance Audit | 2026-08-28 audit pass | **Resolved** — `NotifyLeftPanelResized`/`NotifyRightPanelResized`/`NotifyBottomPanelResized` record the dragged size as the new preference. Pinned by a test that mutates the `ColumnDefinition` directly (the real drag path); mutation-verified — the first version of that test drove `SetLeftWidth` and the mutation **survived**, which is how the wrong-path assertion was caught. |
| TD-72 | Docking is a fixed three-dock geometry, not a docking system: `DockingGrid` is a compile-time 5-column × 3-row `Grid`, panels are assigned to a dock once at composition time (`SetLeftPanel`/`SetRightPanel`/`SetBottomPanel`) and can never move. No drag-to-re-dock, no tabbed panel groups, no panel reordering, no panel splitting, and no floating panels (`WorkspaceDockPosition` has no `Floating` member, `ADR-0095`). Exactly one `Window` subclass exists, so multi-monitor engineering work — brief §5 — is not supported; the `Screens` API is used only to validate a restored window position. | Brief §3.1/§5; mock-ups 1–2, 5; `FCR-0064` | 2026-08-29 docking pass | **Closed** (2026-08-29 closure verification; implemented as `ADR-0095` — the number `ADR-0092` reserved for exactly this question in 2026-08-05, written here). The compile-time five-column, three-row grid was **replaced**, not decorated: the arrangement is now an immutable `WorkspaceLayoutTree` of arbitrarily nested splits and tab groups, plus floating windows in real screen coordinates, plus per-panel pin/collapse presentation. Every capability the brief and mock-ups require is expressible and implemented — drag-to-dock (five drop zones resolved by pure geometry), tabbed panel groups (the only leaf *is* a tab group, so tabbing is the ordinary insert), arbitrary horizontal/vertical splitting at any depth, floating panels as real top-level windows on any monitor, collapse, auto-hide with flyout, resize as durable proportions, layout persistence and restoration, and responsive behaviour. **The document area is a panel like any other**, so a future surface — the Drawing Viewer (`TD-80`), Materials, Calculations, Tasks — participates by registering one `WorkspacePanelDescriptor`, with no change to the model, the renderer or the drag logic; the reason `DigitalThreadGraphView` recorded for not being a panel ("exactly three physical dock slots, all already occupied") no longer exists. Preserved: `TD-70` responsive behaviour (now expressed against the tree, so it holds for any arrangement rather than three named docks — **and wired to the window's own resize for the first time**, closing the gap `TD-83` recorded), `TD-71` splitter preferences as proportions, ribbon minimisation, collapse/auto-hide, and existing user preferences via `WorkspaceLayoutMigration`. `IWorkspaceLayout` (`WP 8.0B`) is retained as a **live projection** of the tree rather than a stale second account. `DockingGrid`, `PanelHostControl` and `PredefinedLayouts` were deleted with their tests, their guarantees re-proven against the new host. 119 tests (81 model/geometry/serialisation/preset/migration with no UI, 38 renderer and controller); eleven mutations run and all eleven killed — one survived first time and exposed a real coverage gap (nothing exercised a split reducing to a single child) which was closed before it died. Residual, disclosed, all new: `TD-90` (focus across re-render), `TD-91` (the projection is lossy for tabbed/floating panels), `TD-92` (no live drag preview adorner). **Closure verification, 2026-08-29:** the real shell exercises this surface — `MainWindow` constructs `WorkspaceDockingComposer`, adds its `View` to the live visual tree, and drives save/restore on close and open; no `DockingGrid`, `PanelHostControl` or `PredefinedLayouts` type survives anywhere in `src/` or `tests/` (the four remaining mentions are historical prose in comments). All nine required capabilities were traced to production wiring, not test-only entry points: drag-to-dock (`LayoutTabGroupView` tab pointer press to `WorkspaceLayoutController.BeginDrag`, with tunnelled move/release handlers on the host and `DockTargetResolver` deciding the target), tab groups, arbitrary nested horizontal/vertical splits with `GridSplitter` resize, floating windows (`FloatingPanelWindow.Show()`), collapse, auto-hide with flyout, persistence and restoration, and the responsive rule. Six mutations were run against the closure claims and one — deleting the host's own `SizeChanged` subscription — **survived**, leaving all 260 Desktop tests green: `TD-83`'s smell had been reintroduced one level up, because every responsive test called `ApplyResponsiveLayout` directly and nothing proved the rule was wired at all. Closed with `ShrinkingTheWindow_AppliesTheResponsiveRule_WithoutAnyoneInvokingItDirectly`, which drives a real layout pass and never names the method; the mutant then died. The other five mutations were killed as run (drop-zone geometry 7 failures, layout persistence 1, the `IWorkspaceLayout` projection 1, collapse/auto-hide 10). `TD-90`/`TD-91`/`TD-92` were each re-checked against the source and remain accurate, and a search for `TODO`, `FIXME`, `NotImplementedException` and placeholder markers across the docking, layout and composition code returned nothing, so they are the only intentional limitations. |
| TD-90 | A workspace layout change re-renders the whole tree and reparents panel content (`ADR-0095`). Content controls are long-lived singletons, so selection and scroll state survive, but keyboard focus is not explicitly restored across a re-render — docking a panel while typing in another moves focus. | `ADR-0095`; `TD-72` | Unassigned | **Open** — bounded: capture the focused element before a re-render and restore it after, when it still exists. No user has reported it; it is disclosed because it is a real consequence of the render model, not because it was observed. |
| TD-91 | `IWorkspaceLayout` (`WP 8.0B`) speaks in edges, sizes and visibility, and cannot express a tabbed or floating panel. `TD-72` keeps it truthful as a live projection of the layout tree, but that projection is **lossy by construction** for those two arrangements: a tabbed or floating panel reports the nearest honest edge. | `ADR-0095`; `TD-72` | Unassigned | **Open** — the correct fix is to retire the edge-based contract in favour of the tree for the Workspace-layer consumers that still read it, which is a contract change and its own Work Package. Disclosed rather than silently approximated. |
| TD-92 | Drag-to-dock has no live preview adorner: the drop target is resolved on pointer move and applied on release, but nothing follows the pointer and no highlight is drawn over the target zone during the drag (`ADR-0095`). The gesture is complete and correct; the feedback during it is minimal. | `ADR-0095`; `TD-72` | Unassigned | **Open** — purely presentational and additive: `WorkspaceLayoutController.DropTargetChanged` already publishes the resolved target on every move, so an overlay has everything it needs and needs no model change. |
| TD-93 | `Tempest.Samples` sample modules redeclare thirteen canonical vocabulary values as their own `const string` fields — `"calculatedBy"`, `"basedOnCalculation"`, the five Document classifications, `"External Reference"`, and the five Manufacturing classifications. They cannot do otherwise: `Tempest.Samples` references only `Tempest.Core`, while the canonical owners (`DocumentObjectFactoryRegistry`, `ManufacturingObjectFactoryRegistry`, `CalculationTemplateRegistry`) all live in `Tempest.App`, one layer above, so referencing the owning constant would invert the layering. `ADR-0105`'s "declared once" rule is therefore unenforceable across this boundary. | `ADR-0105`; `WP 12.1A`; `TD-57` | Unassigned | **Open** — found on 2026-08-29 while making `EngineeringVocabularyConsistencyTests` deterministic: the check reflected over `AppDomain.CurrentDomain.GetAssemblies()`, so it examined `Tempest.Samples` only on runs where some earlier test had already loaded it, and reported a clean result on the runs where it had not. The scan now names the three production assemblies it covers and force-loads them, with `Tempest.Samples` a disclosed exclusion for the layering reason above and a guard test that fails if the scan's reach ever shrinks. The real fix is a decision about where canonical vocabulary is declared — moving these values into a layer `Tempest.Samples` can see, or accepting sample-local duplication permanently — which is an `ADR-0105` amendment and its own Work Package, not something a sample module can resolve. |
| TD-94 | `PluginTrustStore` enumerated its trust store with `Directory.GetFiles(path, "*.cer")`. That search pattern is matched with the **file system's** case rules, so `Acme.CER` was a trusted publisher on Windows and silently absent from the store on Linux — no exception, nothing logged, and a genuinely trusted publisher reported as untrusted. The type's own documentation already claimed every `*.cer` file is read and that its matching is case-insensitive ordinal; the file system was overriding both. | `ADR-0112`; `TD-59` | 2026-08-29 cross-platform cleanliness pass | **Resolved.** Enumerates unfiltered and matches on `Path.GetExtension` with `StringComparison.OrdinalIgnoreCase`, so the rule is this type's own and identical on every platform. Found while closing the last two environment-dependent test failures: the failing test (`Constructor_TempestOSCerFileNameIsCaseInsensitive_StillFlaggedFirstParty`) was correct and the production code was not — it had been recorded for three commits as a "pre-existing Linux-environment case", which it never was. The general form had no test at all, only the first-party filename incidentally covering it, so `Constructor_OrdinaryCertificateExtensionCasingVaries_IsStillTrusted` now pins `.cer`/`.CER`/`.Cer` for an ordinary publisher. Mutation: restoring the search pattern fails all three plus the original. |
| TD-95 | Attachment content is stored per attachment, not per distinct file: attaching the same document to three objects stores its bytes three times. | `ADR-0114`; `TD-31` | Unassigned | **Open** — deliberate, and disclosed rather than deduplicated. Content-addressed storage (key = content hash) would deduplicate for free, and turns deletion into a reference-counting problem; this platform has no attachment deletion path yet to make that trade against, so the simpler shape was chosen first. Costs disk only. |
| TD-96 | `IBinaryPersistenceStore.ReadBytesAsync` returns `byte[]`, so attachment content is materialised whole in memory. Correct for the document types the workflow names; wrong for a very large file. | `ADR-0114`; `TD-31` | Unassigned | **Open** — no consumer streams today and the Document Viewer (`TD-80`) is not built, so there is nothing yet to size the streaming contract against. A `Stream`-returning read is additive to both the binary store and the content store when a consumer needs one. |
| TD-97 | Nothing collects orphaned attachment content: deleting an engineering object leaves its attachments' bytes in the store, and a crash between the content write and the metadata write leaves a record nothing references. | `ADR-0114`; `TD-31` | Unassigned | **Resolved** (`WP 16.4B`, 2026-09-05). `EngineeringObjectBase.DeleteAsync` releases an object's attachment content after the deletion is durable — never before, preserving the crash-safety direction `ADR-0114` decision 4 deliberately chose, which is unchanged. The sweep this row describes exists as `IAttachmentContentReconciliationService`, comparing the content store's own keys against every attachment Id any persisted `EngineeringObjectState` references; it is explicit `DetectAsync`/`SweepAsync`, registered in `TempestHost` Phase 6, and invoked by nothing automatically. |
| TD-98 | The document viewer has no markup, annotation or rotation. `TD-80`'s own original text lists all three alongside zoom, pan and fit, and the 2026-08-29 work package's scope did not include them. | `ADR-0115`; `TD-80`; mock-ups 2, 3 | Unassigned | **Open** — not a control but a work package: annotations need a model, durable storage of their own, and a decision about whether an annotation is an engineering object (with lifecycle, revisions and traceability) or presentation state attached to one. Rotation is cheap in isolation and belongs with markup rather than shipped alone. `TD-80` is closed against what was built, and this row exists so that what was not built is not quietly absorbed into that closure. |
| TD-99 | DWG and SVG attachments cannot be viewed; both report `Unsupported`. | `ADR-0115`; `TD-80` | Unassigned | **Open** — DWG needs a licensed library and a real evaluation; SVG needs a renderer Avalonia does not provide out of the box. Both are additive: a new `IDocumentPageSource` and one line in the factory, with no change to the viewport, the control or the workspace integration. Reported honestly today rather than opened as a blank page. |
| TD-100 | The automated suite does not verify real image decoding. The headless Avalonia platform the Desktop tests run on substitutes a stub decoder that reports every image as 1x1 and accepts bytes that are not an image at all. | `ADR-0115`; `TD-80` | Unassigned | **Resolved** (`WP 16.4A` part 2, 2026-09-04) — the Desktop test host now renders through Skia (`UseHeadlessDrawing = false`); the 4x3 PNG fixture decodes to 4x3 and its twelve pixels read back non-white, asserted unconditionally; the non-image case asserts `DocumentRenderException` unconditionally. Suite cost +~40%. Previously: **Open** — found by probing what the decoder actually returned, after a test asserting a 4x3 PNG's real size failed against correct code. The tests now detect the stub and state which half of it they are in rather than asserting a falsehood, so the PDF and text paths stay fully verified and the image path is disclosed as unverified here. Closing it needs a rendering-capable test host, not a change to the viewer. |
| TD-101 | A page is rasterised at its full size even when only part of it is visible, so a very large drawing at high zoom rasterises far more than the screen shows. | `ADR-0115`; `TD-80` | Unassigned | **Open** — bounded today by a hard cap on the raster edge, so deep zoom degrades in sharpness rather than in availability, and adequate for the document sizes this workflow names. Tiled rendering — rasterising only the visible region — is the fix, and is a change inside `PdfDocumentPageSource` alone. |
| TD-102 | The Project Workspace's Documents and Requirements areas were declared `NavigationAvailability.Implemented` and rendered a `DeclaredCapabilityView` — a glyph, a title, a paragraph of prose and no content. `DeclaredCapabilityView` shows its "Not yet implemented" badge only for `Declared` areas, so these two carried no badge either: the descriptor table, the governance registers and the surface itself all agreed the capability existed, and it did not. Found by the 2026-08-29 post-`TD-80` product gap reassessment, which read `ProjectWorkspaceView.BuildAreaContent` and found `Overview` was the only area with content of its own. | 2026-08-29 Post-TD-80 Product Gap Reassessment | 2026-08-29 | **Resolved** — both areas now have real, project-scoped surfaces. `ProjectDocumentRegister` lists the project's documents and drawings through `ProjectMembership` (transitively, so a drawing on a Part three levels down is in the project) with each file openable in the `TD-80` viewer through the same `AttachmentViewerLauncher` the object editor uses; `ProjectRequirementRegister` lists the requirements allocated into the project and reports the declared status and the recorded verification outcome side by side, because they can disagree and a reviewer needs to see it. Pinned by 12 register tests and 11 acceptance tests driven through the real `MainWindow` (project → area → document/requirement → action); five mutations run, five killed. Runtime-verified by rendering the real window. |
| TD-103 | The desktop shell establishes no principal. Only sample modules call `IIdentityService.EstablishCurrentPrincipal`, so what permissions a running session holds depends on which sample module initialised last — and permission-gated reads such as `IVerificationService.GetVerificationHistoryAsync` (`verification.read`) therefore succeed or fail for reasons unrelated to the user. Found by `TD-102` when the project Requirements surface reported every requirement's verification as unreadable in a shell with no sample-established principal. | 2026-08-29 `TD-102` | `WP — Production Rehydration & Principal Boundary` | **Resolved** (2026-08-29) — for the product TempestOS currently is: a local, single-user desktop application. `ISessionPrincipalSource` (`Tempest.Core.Identity`) is the one place the running application decides who is using it; `LocalSessionPrincipalSource` answers it from the operating system's own account name, and `WorkspaceHost.StartAsync` publishes that answer into `ICurrentPrincipalAccessor` after module initialisation, so the product's own answer stands rather than whichever sample initialised last. The boundary is `desktop session → ISessionPrincipalSource → ICurrentPrincipalAccessor → services → domain`: no engineering object gained a user field, no username is hard-coded anywhere else, and the shell publishes the source's answer **including null**, so a session that genuinely has no principal reports none rather than inheriting a sample's — the un-fixed form of that was found by mutation `M4` and by the acceptance test itself. `RequirementVerificationState.Unknown` is deliberately kept: it is still the honest answer when no principal can be established. **This is not authentication**, and `PrincipalBoundaryTests.TheBoundaryIsNotAuthentication` asserts the absence — no credentials, no login, no token, no session to sign into. Authentication, real user accounts and a permissions model remain future Administration scope (`TD-81`), and are what this interface exists to be replaced by. See `ADR-0116`. |
| TD-104 | Nine persistable engineering types — `Approval`, `Assumption`, `EngineeringAction`, `Hazard`, `Issue`, `Review`, `Simulation`, `Test`, `Verification` — implemented `IRehydratable<T>` and could be written to disk, but were registered for rehydration **nowhere at all**, in production or in samples. Persisting one and restarting discarded it: it came back as an unknown Kind, was skipped, and the loss was reported only as a `Warning` in a log nobody reads. Found while closing `TD-75`'s rehydration half, by reflecting over every `EngineeringObjectBase` subclass implementing `IRehydratable<>` rather than trusting the registration list. | `WP — Production Rehydration & Principal Boundary` | Unassigned | **Resolved** (2026-08-29) — all nine are registered by `CanonicalObjectKinds`, and each is driven through create → persist → new lifetime → rehydrate with its full captured state compared before and after, so an omission in any one type's rehydration constructor fails as a changed `TypeState` key rather than passing unnoticed. `TheProductionRegistry_CoversEveryPersistableDomainType` is the standing check that would have caught this, and now guards against the next one. Separately, an unknown Kind is now logged at **Error** and surfaced in the running shell as a toast naming what could not be recovered — silent data loss was the worse half of this defect. |
| TD-73 | The navigation rail and ribbon never switch to a compact or icon-only presentation, and `MainWindow.MinWidth`/`MinHeight` of 960 × 600 bars genuinely small displays and tight split-screen. The ribbon degrades by horizontal *scrolling*, never by *compacting*. | Brief §4 | Unassigned | **Open** — the residual half of `TD-70`, deliberately not folded into that closure. |
| TD-74 | No global navigation architecture. The mock-ups' persistent left rail of ten product modules (Home, Projects, Tasks, Engineering, Documents, Commercial, Resources, Reports, Knowledge, Administration) does not exist — there is no navigation-rail control in the codebase. The mock-ups' three-level model (Global module → Project → Engineering Workspace) is collapsed to one level: switching which discipline tree the Project Explorer shows. Verified on the running window: menus are `_View/_Document/_Theme/_Commands/_Help`; ribbon tabs are seven engineering disciplines; explorer areas are the disciplines plus Home/Sample Objects/Settings. | Mock-ups 1, 2, 5 | Unassigned | **Open (P0)** — depends on `TD-76`; the information architecture is the product decision, not an implementation detail.  Grouped under **`TD-84` — P0 Product Spine**: the global rail now exists and is a view over `IShellNavigator`, showing only modules the platform can genuinely serve. Remaining: the mock-ups' other seven modules, which depend on `TD-81`. |
| TD-75 | The sample harness ships inside the product: `Tempest.Desktop` → `Tempest.App` → `Tempest.Samples`, so all 33 fictional sample modules initialise in a real launch and a **"Sample" ribbon tab and "Sample Objects" explorer area are visible to end users**. `ADR-0101` classifies `Tempest.App` as an internal engineering harness, yet the shipped desktop application is built on it. | Brief §23; `ADR-0101`; 2026-08-28 architecture review | Unassigned | **Resolved.** (2026-08-30) Phase 1 moved the product's own content out of the sample assembly and cut the `Tempest.App` → `Tempest.Samples` reference. **Phase 2 closes the row by removing what phase 1 deliberately left: the sample-supporting wiring, and the last edge keeping the sample harness undeletable.** Three things went. (1) The composition root's sample explorer registration — measured before removal and found already dead: its provider and view factory were keyed to the navigation area `tempest.samples.workspace-explorer.objects`, whose only registrar is a module in the assembly phase 1 stopped the product loading, so a real run registered 6 navigation items, all real disciplines, and never that one. (2) `Tempest.App.Workspace.Samples` — the fictional Longeron/Frame/Bracket tree, the never-editable view and its factory — moved to `Tempest.Core.Tests`, the only thing that ever drove it; it could not move to `Tempest.Samples`, which references only `Tempest.Core` and so cannot see the Workspace abstractions these implement. (3) `Tempest.Validation` → `Tempest.Samples`: the fault-injection module read one navigation-id constant from a sample module, so the demo harness could not be deleted without breaking the validation harness; it now collides with whatever `INavigationProvider.Items` already holds, which needs no constant and works with any partner module. The disclosed duplication phase 1 left is therefore **deleted, not guarded** — the id is spelled once in the repository. **Proven by deleting `src/Samples` outright and building the rest of the solution: 0 warnings, 0 errors.** Evidence: `SampleSeparationTests` (29 tests), Architectural Dependency Register (graph re-derived in full). |
| TD-76 | There is no project context anywhere in the application, though all five mock-ups are project-framed. `Project` exists only as an engineering-object `Kind` inside the Mechanical discipline tree (`Portfolio → Programme → Project`), reachable as a tree node; there is no current-project concept, no open/switch, no project-scoped workspace, and none of the mock-up's project tabs (Overview/Tasks/Engineering/Documents/Requirements/Risks/Timeline/Reports/Settings). `StatusBarView.SetProject` is called exactly once — by its own constructor, with `null` — so the shipped status bar permanently reads "📁 No project" (verified on the running window). The `ProjectModel`/`ProjectService`/`IProjectRepository`/`JsonProjectRepository` cluster is dead code referenced by nothing in `Tempest.App` or `Tempest.Desktop` (part of `TD-01`). | All five mock-ups; brief §11 | Unassigned | **Open (P0)** — the single highest-leverage gap: every other missing mock-up surface (`TD-79`, `TD-81`) traces back to the absence of a project aggregate that owns tasks, documents, risks and commercial data.  Grouped under **`TD-84` — P0 Product Spine**: a real, persisted project context now exists and the Status Bar shows it. Remaining: project fields beyond identity and lifecycle (customer, manager, dates, budget). |
| TD-77 | The Command Palette is a searchable list, not the global contextual command framework the brief describes: 96 command descriptors are registered but **only 22 set `CreateDefault`** (all in `Tempest.Samples`), so for every real discipline command the palette raises `CommandUnavailable` and reports that the command needs a selected object or additional input. No contextual commands for the current object, no recent-commands list, and no command can be completed from the palette. | Brief §6; `ADR-0070` | Unassigned | **Open** — honest today (the disclosure was a deliberate `WP 10.3B` fix) but incomplete; needs a parameter-binding/command-context contract, which is the same gap the Ribbon works around with its three Kind-keyed verbs. |
| TD-78 | The TempestOS brand design system is absent from the Desktop: none of Royal Blue `#1E2F97`, Electric Blue `#00AEEF` or Purple `#6C2BD9` appears anywhere in `src/`, and no `FontFamily` is set at all (no Chakra Petch, Inter or Space Mono) — the palette is Avalonia Fluent default. The brand system genuinely exists but only on the unmerged `claude/tempestos-companion-mobile-ubznt3` branch (`BrandPalette.cs`, `Assets/Fonts/ChakraPetch-*.ttf`, `SpaceMono-*.ttf`, `WP 14.1A`); it was never applied to the Desktop. | Brief §22; all five mock-ups | Unassigned | **Open** — bounded and mechanical: port the palette and font assets, then apply them through the existing `ApplicationPalette`/`DesignTokens` seam. |
| TD-79 | The Engineering Workspace has the platform's deepest domain support and its thinnest dedicated UI. Domain services exist for Calculations (19 files), Materials (17), Validation (11) and Units (11), yet `Tempest.Desktop` has **zero** dedicated views for any of them, and none at all for Standards, Reference Data, Profiles, Loads, Environments, Compare, Optimization, Sensitivity or Math Tools. Mock-up 2's multi-pane engineering cockpit (calculation editor, results grid, validation pane, materials table, reference standards, output console with Messages/Warnings/Errors tabs, pinned items) is realised as one generic `ObjectEditorView` of per-Kind `Expander` sections plus the Output panel. | Mock-up 2; brief §8 | Unassigned | **Open** — the generic editor is real and working; the per-discipline workspace surfaces are the gap.  Grouped under **`TD-84` — P0 Product Spine**: Engineering is now entered from, and scoped by, a project. The Project Workspace's Documents and Requirements areas now have real, project-scoped surfaces of their own (`TD-102`) rather than declared-capability cards. Remaining: the per-discipline surfaces themselves. |
| TD-80 | No drawing or document viewer exists. Mock-ups 2 and 3 both centre on one (zoom, pan, fit, rotate, markup, annotations, revision metadata, linked objects); a search across `Tempest.Desktop` for image, PDF, DWG, SVG or bitmap rendering returns nothing, and `ZoomBy`/`PanBy` exist only in the Digital Thread *graph*. Attachments are metadata-only records — filename, content type, size, no bytes (`TD-31`) — so there is nothing to view even in principle. A user cannot open a drawing in TempestOS. | Mock-ups 2, 3; brief §12 | 2026-08-29 document viewer (`ADR-0115`) | **Closed for the scope delivered** (`ADR-0115`), with markup, annotation and rotation explicitly **not** delivered and carried as `TD-98` — this row's own original text lists them, so it is closed against what was built rather than against what it said. Delivered: a real viewer over `TD-31`'s durable content. PDF pages are rasterised by PDFium **on demand at the current zoom**, so zooming into a drawing shows more of the drawing rather than larger pixels — a text-extraction viewer was rejected precisely because it serves a specification and fails at a drawing whose content is vector paths, which is the case these mock-ups are about. Raster images and text are the other two page sources; format is decided by magic bytes first and the declared content type second, because a renamed file is a thing people have. Zoom (anchored, so the point under the pointer stays put), pan (clamped, so content cannot scroll off its own edge), fit-to-view on open and on demand, actual size, and page navigation for multi-page documents, all decided by an immutable `DocumentViewport`/`DocumentViewSession` in `Tempest.App` that runs with no UI in the process. Missing, Corrupt and Unsupported are three distinct on-screen states: telling a user their intact file is damaged would be a false accusation about their data. The viewer is an ordinary `TD-72` panel, so it tabs, splits, floats onto a second monitor and persists with no code for any of it, multiple documents open without a fixed number of slots, and opening one never navigates — the project, the open object and the Explorer selection are exactly where they were when the tab closes. 55 Core and 22 Desktop tests, including a create → attach → restart → open → zoom → page journey through the real `MainWindow`; eight mutations run and eight killed. Residual and disclosed: `TD-98` (markup/annotation/rotate), `TD-99` (DWG/SVG), `TD-100` (image decoding unverified by the headless suite), `TD-101` (no tiled rendering). |
| TD-81 | Whole mock-up modules have no implementation across `Tempest.Core`, `Tempest.App` and `Tempest.Desktop`: engineering/project **Tasks** (only `BackgroundTaskRunner`, i.e. background jobs, exists), **Commercial** (invoices, quotes, cashflow, budget), **Resources**/workload, **Knowledge**, **Administration**, **Kanban**, **Timeline/Gantt**, and managed **Milestone** surfaces. Risks, Issues and Decisions exist as domain objects but have no UI surface at all (`FCR-0056`). | Mock-ups 1, 5 | Mock-ups 1, 5; `WP — Project Tasks & Delivery Workflow` | **Partially resolved** (2026-08-29) — **Tasks is delivered; every other module named here is not.** The Project Workspace's Tasks area is now a real surface: tasks and actions are created, retitled, assigned, prioritised, dated, moved through their own work states, completed and reopened, as a list or a status board, all through `EngineeringTask`/`EngineeringAction` rather than a new model. `TaskWorkState` is the first implementation of the platform's own `IFamilySpecificState` contract — the canonical document lifecycle forbids Released → Draft, correctly, so a task family that can be reopened needed its own vocabulary mapped onto the canonical one rather than a second lifecycle. Membership is `ProjectMembership` transitively, so a task hung three levels down a product structure is a project task and no `ProjectId` field was added. Assignment reads the `ADR-0116` principal boundary. `Milestone` and `Deliverable` are reachable through a `contributesTo` link, which is the extent of scheduling delivered. **Explicitly NOT delivered, and still this row's scope:** Commercial (invoices, quotes, cashflow, budget), Resources/workload, Knowledge, Administration, Gantt/Timeline, managed Milestone surfaces, and resource or capacity planning of any kind. `Kanban` is delivered only as the simple status board above — no swimlanes, no WIP limits, no drag-between-columns. See `ADR-0117`. **(2026-08-30) Risks, Issues and Decisions are now delivered too** (`FCR-0056` Implemented): the Project Workspace's Risks area is a real surface over `Risk`, `Hazard`, `Issue` and `Decision` — raised, retitled, scored, owned, assigned, prioritised, and moved through three family-specific status vocabularies that map onto the canonical lifecycle rather than borrowing it. Membership is `ProjectMembership` transitively, as for tasks; no `ProjectId` was added to any of the three. `Assumption` is deliberately left unsurfaced, and `FCR-0056` says why rather than implying it was missed. **(2026-08-30) The Timeline area and managed Milestone surfaces are now delivered too.** The Project Workspace's Timeline area is a real surface over the existing `Milestone` and `Deliverable` Kinds: milestones are set with a target date, deliverables are added against them, and the tasks and actions contributing to each are shown — reached either directly or through a deliverable, with the register keeping which route the work took. Membership is `ProjectMembership` transitively, as everywhere else; a deliverable is parented to its milestone and reaches the project through it, so the structure shown is the structure the domain holds. **No domain change was made**: the brief asked for the state and date information the model already supports, so `Milestone` keeps its immutable `TargetDate` and the canonical lifecycle, and the register invents no "achieved" state — it reports only that a date has passed, and whether work against it is outstanding or nothing was ever linked. **Explicitly NOT delivered:** no Gantt chart, no time axis, no scheduling engine, no dependency graph, no critical path, no date rollup from tasks to milestones, and no rescheduling — `Milestone.TargetDate` has no setter, so changing a date is not offered rather than faked. **Still this row's scope and still not delivered:** Commercial (invoices, quotes, cashflow, budget), Resources/workload, Knowledge, Administration, Gantt/scheduling proper, and resource or capacity planning of any kind. |
| TD-82 | The Companion (mobile/field) application has zero implementation on this branch — no `Companion`, `Mobile`, `Android` or `iOS` files in `src/` or `tests/` — though mock-ups 3 (tablet SOP/checklist/drawing markup/photos/offline) and 4 (phone home) depend entirely on it. It exists only on the unmerged branch `claude/tempestos-companion-mobile-ubznt3` (`WP 14.0A`/`14.1A`/`14.2A`), which also carries the offline snapshot cache, the Companion REST surface and the brand pack. Journey E (site engineer) is therefore not traceable here, and the desktop/mobile responsibility split cannot be assessed. | Mock-ups 3, 4; brief §13–§15 | Unassigned | **Deferred by decision — `D-022`, ratified by the Product Owner 2026-09-05, v1.1.** The branch `claude/tempestos-companion-mobile-ubznt3` is retained, neither merged nor deleted (`WP 16.0B`, 2026-09-04, verified still present on the remote at ratification); its `TD-57`/`TD-58` numbering collision is reconciled at the v1.1 merge, not now. This row stood as "Deferred... pending PR #6", with an explicit revert-to-Open-(P0) condition, from 2026-09-04 until ratification; that condition is now discharged rather than deleted. Evidence: `docs/releases/v0.16.0/WP16.0A Product Owner Ratification — D-021 to D-026.md`. Previously: **Open (P0)** — merge or formally reject that branch, including reconciling its independent allocation of `TD-57`/`TD-58` (see `TD-57`). It is finished work sitting outside the product. |
| TD-83 | Layout and dialog behaviour is under-proven end to end: no test drives a real window resize through `MainWindow` (the `TD-70` tests drive `DockingGrid` directly), and dialog focus/modality is untested. Related: the new responsive tests initially passed in isolation and failed 15-way in the full suite because they were plain `[Fact]`s constructing Avalonia controls outside the headless application context — caught only by running the whole suite. | Brief §18 | Unassigned | **Resolved** (`WP 16.4A` + `WP 16.5A`, 2026-09-04) — `MainWindowResizeTests` drives a real `MainWindow` through 1920/1280/960 (the window's own `MinWidth`) and asserts the compact threshold and the `TD-70` document-area floor from arranged bounds (mutation-tested); `MainWindowKeyboardModalityTests` proves Tab cannot leave an open dialog and focus returns on close. Previously: **Open** — add `MainWindow`-level resize and dialog-modality coverage. |
| TD-84 | **P0 — PRODUCT SPINE (grouping row).** `TD-74` (no global navigation), `TD-76` (no project context), `TD-79` (no per-discipline engineering surfaces) and `TD-81` (no Tasks/Commercial/Resources/Knowledge/Admin modules) are **not four independent features** — they are four symptoms of one architectural deficiency: **the Product Shell / Project Context layer was missing.** Recorded as a group so they are never worked as unrelated tickets, which would produce a projects screen, a navigation rail, a project dashboard and some engineering links with no relationship between them — the precise failure this row exists to prevent. The authoritative product decision (2026-08-28) is that TempestOS is a **project-centric engineering operating environment**: `TempestOS → Module → Project → Workspace → Engineering Object → Evidence`. | Product Compliance Audit (`e40a3d6`); Product Convergence & Recovery Programme; all five mock-ups | 2026-08-28 convergence pass | **Partially resolved** — the spine itself is built and proven end to end. Delivered: `IProjectDirectory`/`ProjectDirectory` (projects are real `IProject` engineering objects, created through the same `EngineeringObjectFactory` every discipline uses, created through the same `EngineeringObjectFactory` every discipline uses; the durable `Projects.Index` this row originally cited was removed by `TD-85`, which made the objects themselves durable); `IProjectContext`/`ProjectContext` (the current project as real application state — open/switch/close/refresh, an `IEventBus` event, persisted); `IShellNavigator`/`ShellNavigator` + `ShellLocation`/`ShellArea`/`ProjectArea` (`Module → Project → Workspace` as one immutable, persisted, testable value, enforcing the invariant that a project-scoped location and the current project can never disagree); and the Desktop shell they drive — `GlobalNavigationRail`, `ProjectBrowserView`, `ProjectWorkspaceView`, a single module host that renders whatever the navigator reports, and the Status Bar finally showing the real current project instead of a permanent "No project". Engineering was reachable **only** from an open project — **superseded by `TD-89`**, which makes standalone calculation sets and quick calculations a first-class workflow requiring no project, while keeping the project scope real: entering Engineering from a project still carries that project as the scope, and a location that claims a project still cannot disagree with the context. 32 tests including the full Definition-of-Done acceptance journey through the real `MainWindow`, and the three commissioned engineering traces (Project→Component→Material→Calculation→Validation→Result; Project→Requirement→Verification→Evidence; Project→Drawing/Document→Engineering Object); four mutations run and killed. **Remaining under this grouping:** `TD-74` global rail still shows only the three modules the platform can genuinely serve; `TD-79` per-discipline engineering surfaces; `TD-81` the absent modules; `TD-76` residual project fields (customer, manager, dates, budget) beyond identity and lifecycle. **Closed under this grouping since:** the object-graph durability boundary the spine's own restart journey exposed (`TD-85`, resolved by `ADR-0113`) — a recovered project is now a live `IProject` with its real lifecycle state, relationships, revisions and contents, and every engineering object inside it comes back too. |
| TD-85 | The engineering object graph is **not durable**: `TempestHost` registers in-memory `IEngineeringObjectRepository`/`IEngineeringRelationshipRepository` (`ADR-0077`), so although engineering *documents* persist, the reconstructed objects over them do not survive a restart — nothing read only through the object repository is findable in the next session. Found by the Product Spine's own Definition-of-Done journey, which failed at "close and reopen TempestOS" until the boundary was crossed deliberately. | 2026-08-28 convergence pass; `ADR-0077` | 2026-08-28 object rehydration pass | **Resolved** (`ADR-0113`) — fixed at its real source, not worked around. The investigation's decisive finding was that a Kind→constructor map over the discipline factory registries could never have worked: those registries are in `Tempest.App` and explicitly "never a Domain-layer registry contract", and — decisively — `EngineeringObjectFactory<T>.CreateAsync` persisted only the document (`Kind`, created-at, prose), so the constructor arguments such a map would need had never been written to disk at all. Delivered: `EngineeringObjectState` (the one canonical model's own serialisation, never a second model) written through `IEngineeringObjectStateStore` into the same `IPersistenceStore` the document store already uses, at creation and after every state-changing mutation, with no save step; `CaptureTypeState` + `IRehydratable<TSelf>.Rehydrate` so each of the 30 concrete canonical types owns both directions of its own persistence, keyed by `nameof`, with **no central switch over Kind anywhere** and a compile error if a type cannot rebuild itself; `IEngineeringObjectRehydratorRegistry` in the Domain layer, populated by each Kind's own declaring class using the constants it already owns (`ADR-0105`) — 29 Kinds, every Kind with a live write path in `src/`; `EngineeringObjectRehydrationService`, run by `EngineeringWorkspaceComposer` in **both** the desktop and console entry points, which reconstructs objects, restores lifecycle/history/parent/deletion/BOM/attachments, reattaches self-factories, rebuilds the relationship index from durable `DocumentReference`s, and never overwrites an object already live in the process; and durable relationship provenance (`DocumentReference.CreatedByPrincipalId`/`CreatedAt`) so a rebuilt link is not credited to whoever is signed in at startup. **`Projects.Index` was removed** — collection, DTO, read/write paths and `IPersistenceStore` dependency — so exactly one answer to "what projects exist?" remains, and a recovered project is a live `IProject` rather than a name-and-status snapshot. Proven by an 18-step acceptance journey through the real `MainWindow` across three application lifetimes (create → close → relaunch → verify identity, type, lifecycle, history, relationships, BOM, parent and revisions → open through the real project browser → keep working → relaunch again), plus 24 focused tests over the real persistent stores; the Product Spine's own restart tests now run through real rehydration instead of an index. **Closure audit, 2026-08-28 (post-`e752368`):** ten checks run; one material defect found and fixed. `IHasRevisions.ReviseAsync` carried only the structural half of an object's state onto its revised instance (`WP 9.0B`'s `CopyStructuralStateFrom`: rename, parent, delete, BOM line), so a revised object silently reverted to `Draft` with no history and no attachments. Harmless-looking before `TD-85` because it was lost at restart anyway — but `TD-85` made it **durable**: the revised instance's next mutation persisted the reset, destroying a recorded lifecycle state and its entire transition history on disk. Fixed by carrying the full captured state (`revised.RestoreState(CaptureState())`, the same pair rehydration already uses) and deleting the partial copy, so one definition of "this object's state" exists and no future field can be forgotten by one of two copy paths. Four regression tests added; two further mutations run and killed. Every other check passed: one authoritative write path (exactly two call sites, both through `IEngineeringObjectStateStore`); no `Projects.Index` code, DTO, collection or dependency anywhere; no project-specific persistence elsewhere; all 29 live-write-path Kinds registered, with no canonical type constructed outside `EngineeringObjectFactory<T>` in `src/`; the two `Tempest.Core.Tests` failures confirmed identical at `37788a0` and therefore not regressions. Residual, disclosed, all new: `TD-86` (write batching), `TD-87` (state schema versioning), `TD-88` (lazy rehydration); the `WP9.3A`/`TD-32` relationship-index asymmetry stays `TD-32`'s, amended there rather than absorbed here. |
| TD-86 | Engineering object state is written **once per mutation, per object** (`ADR-0113`): each `TransitionAsync`/`RenameAsync`/`MoveAsync`/`DeleteAsync`/`AttachAsync`/`SetBomLineAsync` performs its own small atomic `IPersistenceStore` write. Correct and crash-safe, but a bulk operation over thousands of objects performs thousands of individual writes. No batching, write-behind or transaction scope exists. | `ADR-0113`; `TD-85` | Unassigned | **Open** — bounded and additive: a unit-of-work or debounced flush behind `IEngineeringObjectStateStore`, with the same crash-safety guarantee. No measured workload demands it yet, so it is disclosed rather than pre-optimised. |
| TD-87 | `EngineeringObjectState` is a **versionless schema** (`ADR-0113`). A record that cannot be deserialised is skipped with a warning, and a field a newer type expects but an older record lacks falls back to a default — honest degradation, but not migration. A future breaking change to the shape (a renamed facet, a changed enum ordering) would silently lose the affected state rather than upgrading it. `LifecycleState` in particular serialises as its numeric value, so reordering the enum would silently reinterpret every persisted status. | `ADR-0113`; `TD-85` | Unassigned | **Resolved** (`WP 16.3B`, `ADR-0120`, 2026-09-04) — `EngineeringObjectState` carries `SchemaVersion` (absent normalises to 1); enums serialise as strings and read back either form; a per-Kind migration chain applies on read only inside `EngineeringObjectStateStore`; a record with no path to the store's target version is logged and skipped; a committed v1 golden corpus and a real-host restart proof are permanent tests. `SettingsDocument<T>` joins for its six object-shaped consumers; the three list-shaped ones are disclosed, not versioned (`ADR-0120` Decision 6). See `docs/releases/v0.16.0/WP16.3B Schema Versioning Implementation Report.md`. Previously: **Open** — needs a schema version stamp on the record plus a migration seam, and (cheaply, first) string-valued enum serialisation. |
| TD-88 | Startup rehydration is **eager and linear**: `EngineeringObjectRehydrationService` reads every persisted object state and reconstructs every object before the shell renders (`ADR-0113`). Correct, and necessary for the current read-everything surfaces (`ListAllAsync`, by-Kind listings, the project explorer), but startup cost grows with the size of the estate and nothing is loaded lazily or by project scope. | `ADR-0113`; `TD-85` | Unassigned | **Open** — needs project-scoped or on-demand rehydration, which in turn needs the read-side surfaces to stop assuming the whole graph is resident. Measure before designing. |
| TD-89 | **Project-centric convergence (grouping row, largely resolved).** The Product Spine (`TD-84`) made Engineering reachable *only* from an open project — a faithful but too-strong reading of "TempestOS is project-centric". The authoritative product decision is that projects are the primary organisational context **and** that standalone calculation sets / quick calculations remain a first-class workflow requiring no project. The shell also showed only the three modules the platform could serve, so the product looked smaller than designed, and project membership was answered by direct children only, so a two-level product structure looked almost empty. | Product Convergence & Recovery Programme; Product Compliance Audit (`e40a3d6`); mock-ups 1–5 | 2026-08-29 convergence pass | **Resolved** for the spine itself. Delivered: `ShellArea.Engineering` now legitimately carries a project **or** none, with `ShellLocation.ProjectId` as the scope itself and the shell invariant restated rather than weakened (a location claiming no project cannot disagree with the context); `GoToStandaloneEngineeringAsync`, which never closes the open project; `IEngineeringScope`/`EngineeringScope`, the real derived state the Engineering Workspace reads instead of inferring its scope; `ProjectMembership`, the single, transitive definition of project ownership over the durable `IHasParent` chain, used by both `ListProjectContentsAsync` and the engineering scope so the platform has one answer to "what is in this project"; `ShellAreas`/`ProjectAreas`, which declare the product's designed module and area sets **and** which of them have a capability behind them, as state a test asserts; and `DeclaredCapabilityView`, a real, navigable, project-aware surface for anything unbuilt that names what is missing and the debt tracking it. Five mandatory journeys through the real `MainWindow` (new project; existing project with a real calculation and validation; **standalone calculation with no project, surviving a reopen**; context restoration across a restart; a full navigation circuit) plus 17 integration tests and 3 docking-seam tests; six mutations run and killed. Two tests that encoded the superseded rule were **inverted and strengthened**, not deleted. Remaining under this grouping: the declared-but-unbuilt modules and areas themselves (`TD-79`/`TD-81`/`FCR-0056`/`TD-76`), the drawing viewer (`TD-80`), and full Option-C docking (`TD-72`) — whose seam this pass verified and pinned rather than built. |
| TD-105 | **Command invocation has four parallel mechanisms (grouping row).** `TD-77`'s binding contract became the canonical surface-level path but retired none of what it replaced. Live today: (1) `ICommandRegistry.InvokeAsync(id, context, prompt)` — Ribbon, Palette, Macro Manager; (2) the obsolete Id-only `InvokeAsync(id)`, which throws `CommandException` for all 74 production descriptors — see `TD-106`; (3) typed `ICommandDispatcher.DispatchAsync<T>` — `WorkspaceManager`, `ObjectEditorView` (6 sites), `WorkspaceViewCoordinator`; (4) `WorkspaceManager`'s own rename/delete/revise factory registries with independent per-Kind maps — see `TD-107`. Mechanism (3) is legitimate where a caller already holds typed command data and is **not** to be eliminated mechanically; it requires explicit classification, not removal. Audit finding `F-01`. | Architecture & Dead-Code Audit (2026-08-31) | `WP-A1` (classification + guard rail); `WP-H` (invariant suite) | **Closed — `WP-A2` (2026-09-01).** *Original disposition, retained:* classify, do not consolidate. `WP-A1` installs an allow-list test so no future surface can reach mechanism (2) unnoticed; `WP-H` pins the sanctioned exceptions. Gated **before further feature work**. **Closed, `WP-A2` (2026-09-01).** All four mechanisms are now classified rather than merely listed, which was this row's approved disposition. (1) The canonical `InvokeAsync(id, context, prompt)` path is used by every live surface — Ribbon, Palette, Macro Manager, Cockpit, and now the input binding router. (2) The obsolete Id-only overload has one remaining caller, deliberate and guarded (`AT-10`, `TD-106`). (3) Typed `ICommandDispatcher.DispatchAsync<T>` is legitimate where a caller already holds typed command data and was never eliminated, as ruled. (4) The `WorkspaceManager` factory registries were decided by `ADR-0118` (`WP-B2`) to be a genuinely distinct mechanism rather than a duplicate, with `KindEligibilityInvariantTests` as the permanent control. Nothing was consolidated mechanically; everything is named, guarded and explained. |
| TD-106 | **Three command surfaces still call the obsolete Id-only `InvokeAsync`,** which throws for every descriptor without `CreateDefault` — i.e. all 74 production discipline commands. `EngineeringCockpit.cs:613` is **live**, reached from `WorkspaceShell.cs:313` (the ADR-0101 engineering harness); `ApiRequestHandler.cs:151` and `Core/Input/InputBindingRouter.cs:89` are **latent** — the REST transport is never composed and no keyboard gesture is ever bound in production, so neither fires today, but each becomes live the moment it is switched on. `EngineeringCockpit.AvailableCommands` compounds it: it filters on `CanExecute` only, never `Evaluate`, so it advertises all 74 as available and then throws on each. Audit findings `F-02`, `F-13`, `F-16`, `F-17`. | Architecture & Dead-Code Audit (2026-08-31) | `WP-A1` (live surface); `WP-A2` (dormant surfaces, at switch-on) | **Closed — `WP-A2` (2026-09-01).** *Original disposition, retained:* approved architectural ruling for `WP-A1`: **option (c)** — `WorkspaceShell` owns selection/context construction and supplies `CommandContext` to `EngineeringCockpit`; `ISelectionService` is **not** added to `EngineeringCockpit` and its dependency surface is not widened. `WP-A1` also adds the allow-list test that forces the two dormant surfaces into a recorded decision rather than a hidden defect. Historical note: before `TD-77` Stage 5 all 22 `CreateDefault` descriptors lived in `Tempest.Samples`, which the shipped Desktop does not reference — so the Palette and Macro Manager were structurally incapable of invoking anything at all (audit `F-22`); that half is resolved. **Completed, `WP-A1` (2026-09-01):** the live Cockpit path is migrated — `EngineeringCockpit.AvailableCommands(CommandContext)` now filters on `Evaluate`, `InvokeCommandAsync` takes a context and a prompt, and `WorkspaceShell` builds both through the existing `WorkspaceCommandContext` adapter (option (c), as ruled). **Correction to this row: there were four Id-only surfaces, not three.** The fourth — `MainWindow`'s Macro Manager "Run" path — was missed by the audit and found by the new allow-list test itself; it did not throw (a macro descriptor does carry `CreateDefault`) but ran every step against no context, so it failed quietly rather than loudly. Also migrated, to match the Palette's own macro path. The two dormant surfaces are recorded in the allow-list with their reasons (`AT-10`, `AT-23`) and remain `WP-A2`. `F-17`'s "the REST transport is never composed" is **wrong** and is corrected here: `HostedServiceDiscoveryService` defaults to scanning `AppDomain.CurrentDomain.GetAssemblies()`, so `RestApiHostedService` **is** discovered and started. The transport is live; it is the absence of request-parameter binding (`AT-10`) that makes the Id-only overload correct there rather than leftover. **Closed, `WP-A2` (2026-09-01).** The last of the four surfaces is migrated: `InputBindingRouter` now takes the canonical path and its allow-list entry is gone. The only remaining Id-only caller in the repository is `ApiRequestHandler`, retained deliberately under `AT-10` — which `WP-A2` reclassified from "not yet" to a decided architectural position. No surface reaches the obsolete overload unintentionally, and the guard test proves it. |
| TD-107 | **Two independently hand-maintained encodings of per-Kind rename/revise/delete eligibility.** `CommandBinding.AppliesToKinds` drives `ICommandRegistry.Evaluate` (Ribbon, Palette); `IWorkspaceManager.CanRename`/`CanRevise`/`CanDelete`, backed by the `Register{Rename,Delete,Revise}Factory` Kind maps, drives `ProjectExplorerView` (context menu, inline rename, delete), `PropertyInspectorView` (name-field editability) and `ObjectEditorView` (field enablement and the save path). 12/12/11 factory registrations against 15 manager call sites in three Desktop views. They agree today, and until `WP-B1` nothing detected drift — a divergence would show as the Ribbon and the Explorer disagreeing about the same object. `TD-77` Stage 5 moved the Ribbon from one encoding to the other, creating the split. Audit finding `F-03`. | Architecture & Dead-Code Audit (2026-08-31) | `WP-B1` (invariant test); `WP-B2` (unification — deferred, requires ADR) | **Closed — `WP-B2` (2026-09-01), by decision rather than by change.** *Original disposition, retained:* `WP-B1` adds the cheapest robust invariant test asserting the two encodings agree; `WorkspaceManager` is **not** redesigned and `WP-B2` is **not** implemented. Gated **before further feature work** (test only). **Closed, `WP-B2` (2026-09-01) — decided: two mechanisms, one invariant.** See `ADR-0118` (Kind Eligibility Is Two Mechanisms Held Together by One Invariant, Not One Source of Truth). **Production unification is rejected**, and the audit's premise is corrected: the two encodings do not state the same fact twice. `CanRename`/`CanRevise`/`CanDelete` are `ContainsKey` over the factory maps and answer *"can a command be constructed for this Kind"*; `AppliesToKinds` is one of five terms `Evaluate` computes and answers *"is this command available for this selection"*. A third question — *"does this Kind support this user operation"* — is represented nowhere, and naming it would add a mechanism rather than remove one. **The overlap is 19 commands of 74:** 53 binding sites pass a Kind list and only 18 have a factory counterpart, the other 35 being status transitions, creates, copies, duplicates, bulk operations and `set-bom-line`, which have no manager question to ask. **The Manufacturing asymmetry is load-bearing:** Manufacturing registers Documents'/Verification's commands for `WorkInstruction`/`Inspection` (`WP 9.5A`), so the manager map is deliberately wider than any one descriptor — unifying would either put `documents.rename` on the wrong Ribbon tab or stop the Explorer renaming a `WorkInstruction`. `CalculationTemplate` (synthetic, no domain object) and Requirements (no rename concept at all) are excluded consistently on both sides. Deriving bindings from the manager is additionally blocked by registration order — Documents' descriptors are registered before Manufacturing's factories. **The permanent control is `KindEligibilityInvariantTests`,** which asserts both directions and pins the asymmetry, with `SurfaceCommandPolicyCompletenessTests` guarding the single point where the mechanisms meet. Runtime divergence degrades to an honest `CommandResult.Failure`, never a crash. **`WP-B2` made no production or test change.** Two observations disclosed in `ADR-0118` rather than actioned: the `CanX` names read as capability questions while documenting factory registration, and `CalculationsWorkspaceRegistration.SupportedKinds` is public and wider than what any command can act on. |
| TD-108 | **Blocking `.GetAwaiter().GetResult()` calls, several on the UI thread.** **Re-derived at `WP-E` (HEAD `7de6290`); the original figures were wrong in three ways.** Not 77 but **69 textual occurrences, of which 63 are executable** (six are `<c>…</c>` doc-comment mentions); not "in `Tempest.Desktop`" but **28 Desktop / 35 `Tempest.App` / 0 `Tempest.Core`**; and the cited line numbers (`MainWindow.cs:269,272,545`) were stale from before `WP-G`. *(Split corrected `WP-Z1` (2026-09-01): the row as first written said "26 Desktop / 37 App", which matched neither the textual (28/41) nor the executable (28/35) division — an error in the very amendment that existed to correct wrong figures, found by the pre-release audit and re-derived from source here.)* **At `e4bc3ee`, after `WP-E`: 67 textual, 60 executable — 27 Desktop / 33 `Tempest.App` / 0 `Tempest.Core`**, with file-I/O sites down from 22 to 19 and the in-memory 36 unchanged. The other blocking patterns were also re-derived: **`.Wait()` 0, `Task.Run` 0, `Task.WaitAll/WaitAny` 0**, and of the 13 `.Result` occurrences **none is `Task.Result`** — every one is `CommandInvocation.Result` or `CalculationRecord.Result`, a record property. **No deadlock risk exists.** `Tempest.Core` and `Tempest.App` use `ConfigureAwait(false)` on every awaited statement but one (`ApiRequestHandler`, never reachable from the UI thread), and neither assembly references Avalonia or `Dispatcher.UIThread` at all — so no awaited operation can require the UI dispatcher and no blocking call can hang. **36 of the 63 block on an already-completed `Task`** (`Task.FromResult` from the in-memory object/relationship repositories, the only implementations there are) and therefore do not wait at all. The real and remaining concern is **UI-thread latency on the calls that do reach real file I/O** via the file-backed `EngineeringDocumentStore`/`PersistenceStore`. Audit finding `F-04`. | Architecture & Dead-Code Audit (2026-08-31); re-derived `WP-E` (2026-09-01) | `WP-E` — partially resolved | **Open — narrowed, with the worst case resolved.** `WP-E` removed the two it authorised: the interactive Toggle Favourite save (now awaited) and the Cockpit's repeated reads (now memoised per refresh — measured **1,140 → 104** persistence reads for one Requirements refresh at N=4, and the per-requirement validation pass now runs **once** per refresh instead of about eight times). **Residual, deliberately not changed:** one validation pass is itself `O(N²)`, because `RequirementValidationService.ValidateAsync` calls `ListAsync` for its duplicate-identifier check, so validating one requirement reads every requirement — fixing that means changing a `Tempest.Core` validation service and is a separate decision (pinned by `CockpitReadScopeTests.OneValidationPass_IsItselfQuadratic_WhichThisWorkPackageDeliberatelyDidNotChange`). The remaining executable calls are startup/shutdown (Avalonia's own `void` framework contract, with no window yet constructed), the `IWorkspaceViewFactory.Create` survivors below, one already-completed-task rethrow in `WorkspaceManager`, and the 36 that never wait. Revisit trigger unchanged: **persistence or remote-storage work begins.** |
| TD-109 | **`MainWindow` was a 1,577-line god object** (this row recorded 1,557; it had grown by 20 lines by `WP-D1`) — composition root, event wiring, *and* the complete CRUD interaction logic for projects, tasks, milestones, deliverables, risks, issues and decisions, each with its own `try`/`catch` + toast + `RecordHistory` (~20 `_toastHost.Show` sites in one file). Largest file in the repository by 50%. The extraction pattern already exists (`WorkspaceViewCoordinator`, `UndoRedoCoordinator`). Audit finding `F-05`. | Architecture & Dead-Code Audit (2026-08-31) | `WP-G` — deferred | **Open — deferred by approved decision.** Mechanical but large; ugly rather than dangerous. Sequenced after `WP-D1` so the reporting tail is consolidated before the file is split. **Partially resolved, `WP-G` (2026-09-01).** The CRUD interaction logic this row names is extracted verbatim into two `ADR-0103` collaborators split along the domain service: `ProjectDeliveryCoordinator` (tasks, milestones, deliverables — `IProjectTaskService`/`IProjectMilestoneService`) and `ProjectGovernanceCoordinator` (risks, issues, decisions — `IProjectGovernanceService`). Not one method touched both services, which is what made the seam clean. All 19 methods moved byte-for-byte; the only edits were field access becoming constructor-injected dependencies and `RecordHistory(…)` becoming the injected `Action<string>` the other three collaborators already take. Prompts, messages, identifier schemes, try/catch blocks, refreshes, async shape and ordering are unchanged, and the 21 `_projectWorkspace.*Requested` wiring lambdas stay in the composition root where they belong. Both seams mutation-checked: unwiring either coordinator fails its own acceptance journey. **Deliberately still Open.** `MainWindow` is 1,577 -> **1,052 lines** (1,042 at `WP-G`; `WP-A2` then added the keyboard's context and prompt wiring — figure re-derived `WP-Z1`) — a 34% reduction and still the largest file in the repository. What remains is the composition root proper plus the shell services (`RenderCurrentModuleAsync`, `RefreshStatusBar`, `RecordHistory`, layout save/restore, rehydration reporting), which are genuinely the shell's own and are a different question from this row's stated defect. The 41 CRUD toast sites were **not** routed through `ActionOutcomeReporter` — that family carries no `ActionOutcome` and refreshes no dependent surfaces, which is `TD-111`'s recorded reason for leaving it alone. |
| TD-110 | **The entire pre-`TempestHost` v0.1 architecture is retained in `Tempest.Core`,** unreferenced: `Bootstrap/BootstrapService`, `Hosting/HostingService`, `Configuration/ConfigurationService`, `Logging/LoggingService`, `Projects/ProjectService`, `Projects/ProjectNumberGenerator`, `Repositories/IProjectRepository`, `Repositories/JsonProjectRepository`. `BootstrapService` is the sole consumer of the first three and is itself referenced nowhere; `ProjectService` hard-codes `new JsonProjectRepository()` in its constructor. None carries XML documentation, unlike the rest of Core. The only surviving mentions are comments naming the code as retired — `ProjectWorkspaceView.cs:31` ("the mistake the retired bootstrap `ProjectModel` made") and `ProjectSummary.cs:22` ("Deliberately **not** the pre-platform `Tempest.Core.Models.ProjectModel`"). Broader than, and inclusive of, `TD-01`. Audit finding `F-06`. | Architecture & Dead-Code Audit (2026-08-31) | `WP-C` (Gate 0, approved) | **Resolved — `WP-C`.** *Original disposition, retained:* approved for deletion, Gate 0. Scope is deletion only: dormant future-capability contracts (`Core/EngineeringDomain/Contracts` declared vocabulary, `Core/Api`, the `InputBindingRouter` extension point) and `TD-115`'s three commands are explicitly **out of scope**. `Models/ProjectModel` is retained — it is still referenced by live App/Desktop code. Discharges `TD-01`. **Completed, `WP-C`:** all eight deleted. `ApplicationConfiguration` — the settings record `ConfigurationService` produced and `HostingService`/`BootstrapService` consumed — became unreferenced as a direct consequence and was removed as `WP-C`'s own completion, by explicit approval, rather than left behind as a ninth orphan. |
| TD-111 | **The report-then-refresh tail is reimplemented six times** across `Tempest.Desktop` (Ribbon, Explorer, Inspector, Object Editor, Palette, Undo/Redo), each doing `statusBar.SetText` + `toastHost.Show(severity from succeeded)` + conditional `explorerView.LoadAsync()` + `cockpitView.Refresh()`. Four separate `ActionCompleted +=` subscriptions, 10 explorer reloads, 24 cockpit refreshes. `ActionOutcome` was introduced to standardise exactly this and has no single consumer. Audit finding `F-08`. | Architecture & Dead-Code Audit (2026-08-31) | `WP-D1` — deferred | **Resolved — `WP-D1` (2026-09-01).** *Original disposition, retained:* deferred, deliberately separated from `WP-D2` (`TD-112`): different layer, different risk. Touches six live UI paths and should follow `WP-A1`. **Completed, `WP-D1` (2026-09-01):** one implementation now — `Tempest.Desktop/Composition/ActionOutcomeReporter.cs`, constructed once by `MainWindow` and used by the Ribbon, Explorer, Inspector, Object Editor, drag-and-drop Move and Undo/Redo tails. **Correction to this row's own wording:** `ActionOutcome` did *not* lack a consumer — six views raised it and five subscribers read it. What had no single implementation was the *tail*, which is what was consolidated; the type is unchanged. **The refresh set stayed per-caller, deliberately:** the four sites refresh different dependents (Inspector+Cockpit, Explorer+Cockpit, Explorer+Inspector+Cockpit, Explorer+Cockpit) and those differences are correct — the Explorer does not reload itself after its own action — so the reporter takes the refresh as a delegate and only decides whether to run it. **Gated on `WorkspaceChanged`, never on `Succeeded`:** `ObjectEditorView`'s Owner/Priority save reports a failure that *did* change the workspace, and a success-gated reporter would have left three surfaces showing stale values. **Deliberate unmigrated survivors,** each for a stated reason rather than by omission: the **Command Palette** reports through `RefreshStatusBar(manager)` rather than the action's own message and raises no toast, so converging it would either change what the user sees or add a no-toast mode for one caller — approved as an explicit survivor; the **Digital Thread graph** sets the status bar alone; **`CommandUnavailable`** reports a third severity (Warning); and `MainWindow`'s project/task/milestone/deliverable/risk/issue/decision CRUD is a different shape entirely (no `ActionOutcome`, no dependent refresh) and belongs to `TD-109`. **History semantics unchanged:** drag-and-drop Move still records nothing, carried as an explicit no-history entry point rather than normalised away, and `RecordHistory`'s own success heuristic (`AT-21`) is untouched. Four mutations run, four killed. |
| TD-112 | **The same "load persisted JSON, swallow `JsonException`, fall back to defaults" block appears eight times,** and six of the eight have no logger at all: `UserSettings.cs:91`, `WindowUiState.cs:91`, `RecentObjectsState.cs:91`, `FavouriteObjectsState.cs:96`, `Docking/DesktopPanelUiState.cs:123`, `Workspace/WorkspaceState.cs:95`, `Macros/MacroManager.cs:50` (logger-free); `Projects/ProjectContext.cs:124` and `Shell/ShellNavigator.cs:143` have loggers. Corrupt user settings, window state, recents, favourites, panel layout, workspace state and macros are therefore discarded silently and unobservably. Audit finding `F-09`. | Architecture & Dead-Code Audit (2026-08-31) | `WP-D2` (Gate 0, approved) | **Resolved — `WP-D2` (Gate 0), recorded `WP-G` (2026-09-01).** *Original disposition, retained:* approved, Gate 0. The `TD-60` degrade-to-defaults contract is correct and is **preserved unchanged**; the defect is the silence and the eight-fold duplication. `WP-D2` introduces one shared settings-document helper that logs the corruption once, and is explicitly **not** combined with `WP-D1`. **Completed, `WP-D2` (Gate 0) — recorded here `WP-G` (2026-09-01).** All nine sites migrated to the shared `SettingsDocument<TDocument>` helper (`src/Tempest.Core/Settings/SettingsDocument.cs`); zero `catch (JsonException)` swallows remain at any of them. The `TD-60` degrade-to-defaults contract is unchanged — a corrupt value still falls back to defaults — and the helper now logs the discarded value once, which was the whole of the defect. This row's disposition was simply never updated when `WP-D2` shipped; found and corrected during `WP-G`'s own Stage 1 audit. The audit's own count correction stands: **nine** sites, not eight — `Workspace/WorkspaceState.cs` was omitted from the original finding. |
| TD-113 | **`Tempest.Desktop/Composition/SurfaceCommandPolicy.cs` hard-codes 14 command-id string literals,** duplicating knowledge the `Tempest.App` discipline registrations own. It is the only Desktop file containing production command-id literals in code. If a discipline adds a delete-shaped command and it is not added to `DeleteCommandIds`, the Ribbon silently routes it through its binding instead of `IWorkspaceManager.DeleteObjectAsync`, losing the `TD-58` selection clear — with no failing test. Replacing the previous Id-suffix parser with an explicit list was correct; the absence of a completeness check is not. Audit finding `F-10`. | Architecture & Dead-Code Audit (2026-08-31) | `WP-A1` (completeness test) | **Resolved — `WP-A1` (2026-09-01).** *Original disposition, retained:* keep the explicit list, add the completeness test asserting every delete-shaped descriptor appears in `DeleteCommandIds`. Gated **before further feature work**. **Completed, `WP-A1` (2026-09-01):** `SurfaceCommandPolicyCompletenessTests` asserts set equality between `DeleteCommandIds` and the delete commands the registry actually carries, so all four failure directions now fail the build — a new delete missing from the policy, a policy Id naming no descriptor, a policy entry that is not a delete, and an omitted required delete (each demonstrated by mutation). A delete is recognised structurally, from `WorkspaceCommandBindings.DeleteConfirmation`'s own wording read out of the helper, never by parsing an Id — the defect `TD-77` Stage 5 removed; the recognised set carries a floor so a detector that silently matched nothing cannot pass. The explicit policy itself is unchanged, as ruled. |
| TD-114 | **Test-suite brittleness and weakness introduced or inherited around `TD-77` (grouping row).** Exact-count assertions on 74/56/18/14/42/41/30 spread across four files, so adding one production command breaks ~12 assertions in three files none of which concerns the new command (`F-11`); `SurfaceCommandIntegrationTests` boots a full `WorkspaceHost` nine times in one file, contributing to a ~3-minute Desktop suite (`F-12`); `ServiceProviderExtensions.GetService<T>` is used by 29 test call sites and zero production ones while production writes 35 manual casts (`F-15`); 84 of 2,936 tests (2.9%) carry a single weak assertion, including outright tautologies such as `UnitConverterTests.Constructor_RequiresNoArguments` and `WorkspaceManagerTests.CanRevise_FactoryRegisteredForKind_IsTrue` (`F-18`). | Architecture & Dead-Code Audit (2026-08-31) | `WP-F` — deferred | **Resolved — `WP-F` (2026-09-01).** *Original disposition, retained:* deferred, sequenced after `WP-A1`/`WP-B1` so it does not re-touch files those work packages change. The 8 source-text architectural assertions and the 29 `GetPrivateField` sites are **retained**: they catch what compilation cannot, and no public seam exists for several. **Completed, `WP-F` (2026-09-01), with two of its four premises corrected.** **`F-11` (exact counts):** the six per-discipline command counts became set assertions over declared Ids that fail naming the offending command; `EveryDescriptorRegistrationInSource_DeclaresABinding` compares source-declared Ids to registry Ids as sets rather than counting to 74; the macro-eligibility test's two counts were removed as restatements of the set equality on the line above them; and `EveryDescriptorKeptItsIdentity` now asserts uniqueness rather than restating 74. **One canonical reconciliation is retained and made complete** — `74 = 18 unavailable + 56 bindable`, with all three terms asserted rather than two plus a comment, because that arithmetic is what stops a nineteenth unavailable command hiding in the bindable set. Numeric assertions that carry meaning (HTTP status codes, viewport geometry, byte sizes, page counts) were left alone. **`F-12` (repeated host boot) — the premise was wrong and no work was done.** `SurfaceCommandIntegrationTests` boots 9 times out of 236 across the Desktop suite (~4%); the heavier files are `DigitalThreadGraphTests` (24) and `CockpitViewHonestyTests` (16). The real cost is that the whole collection is serialised, which is mandatory — the headless Avalonia dispatcher is process-wide. A shared-host fixture was **rejected**: 5 of those 9 tests mutate the domain, and `TD-37`/`WP 10.1B` root-caused the rule that each independent test gets a fresh persistence root. The ~3s recoverable from the two provably read-only classes was declined as not worth the churn. **`F-15`:** `ServiceProviderExtensions.GetService<T>()` — a public API on a production assembly with zero production callers and 32 test call sites — was **deleted**, its call sites migrated to the `(T)provider.GetService(typeof(T))` form production already uses in 40 places. **`F-18` — 84 was an undercount and the label was wrong.** 269 of 2,820 test methods carry a single weak-shaped assertion; nearly all are legitimate contract tests. Only 8 assert something that cannot fail by construction, and 3 of those are real lifecycle contracts. Net: one deletion (`UnitConverterTests.Constructor_RequiresNoArguments`) and three strengthenings — `MultiPanelLayout…` now checks the three panels its name promises, the Cockpit refresh test asserts the idempotence its own comment claimed, and the Explorer filter test now filters and counts. `WorkspaceManagerTests.CanRevise_FactoryRegisteredForKind_IsTrue`, named in the audit, is **legitimate** and was kept. The other 265 were not touched. **Also:** the Desktop `EngineeringCockpitTests` was renamed `CockpitViewHonestyTests` (it shares a name with the Core class covering a different subject), and the six Desktop test helpers copied between classes — one of which had been copied in a subtly wrong form — now have one implementation in `DesktopTestHelpers`. |
| TD-115 | **Three commands are fully implemented and registered but have no construction path anywhere in the product:** `AddRequirementToCollectionCommand`, `CompareBaselinesCommand`, `LinkRequirementCommand`. Each has a registered handler and a descriptor whose binding is `CommandBinding.Unavailable(...)`, and no surface, handler or drag-and-drop path constructs it — only tests do. Distinct from the other 15 `U1`/`U2` commands, which all retain a real production construction path (7 `Move*` via drag-and-drop, 5 `Copy*` via the `Duplicate*` handlers, 3 via `ObjectEditorView`). Audit finding `F-07`. | Architecture & Dead-Code Audit (2026-08-31) | `FCR-0073` (Copy/Move Destination-Picker Dialog) | **Open — implemented-but-unavailable capability, retained by approved decision.** These are **not** dead code and are **not** to be deleted: they are complete implementations awaiting the object picker `FCR-0073` will provide. `WP-H` pins their construction-path status so they cannot be mistaken for working features or quietly removed. **Pinned, `WP-H` (2026-09-01):** `FutureCapabilityCommandTests` asserts both halves — each command, its handler and its `RegisterHandler<…>` line still exist (so none is swept up as dead code), and no production code anywhere under `src/` constructs one (so no surface guesses at the second object instead of asking for it). When `FCR-0073` lands and these gain real bindings the test fails, which is the prompt to retire this row rather than an obstacle to the work. |
| TD-116 | **The shipped Desktop application cannot launch on Linux/X11** — `System.TypeLoadException: Could not load type 'Tmds.DBus.Protocol.Connection'` out of `Avalonia.FreeDesktop`'s own `X11DBusImeHelper.DetectAndRegister()`, during `AvaloniaX11Platform.Initialize`, before any window is created. Root cause is a deliberate, documented decision, not an accident: `Tempest.Desktop.csproj` pins `Tmds.DBus.Protocol` to `0.94.2` to remediate advisory `GHSA-xrw6-gwf8-vvr9` in the `0.20.0` that `Avalonia.Desktop 11.2.3` pulls transitively, and `0.94.2` no longer presents the type that `Avalonia.FreeDesktop 11.2.3` binds against. **Windows and macOS are unaffected** — `UsePlatformDetect` selects Win32/AvaloniaNative and never initialises the FreeDesktop/X11 path — and the whole `Tempest.Desktop.Tests` suite passes because `Avalonia.Headless` does not initialise X11 either, which is why nothing in the repository noticed. Reproduced at `9c25223` on Linux both with and without a display (`xvfb-run`); reverting the pin to `0.20.0` in an uncommitted working copy launches the application successfully under `Xvfb`, which isolates the cause exactly. | `WP-REVIEW` Clean-Machine Readiness Audit (2026-09-01) | `WP 16.5B` | **Resolved** (`WP 16.5B`, 2026-09-04) — the timeboxed spike succeeded: `Avalonia`, `Avalonia.Desktop`, `Avalonia.Themes.Fluent` and `Avalonia.Fonts.Inter` upgraded from `11.2.3` to `11.3.20`; `Tmds.DBus.Protocol` repinned from `0.94.2` to `0.21.3` — the `GHSA-xrw6-gwf8-vvr9` advisory's own backported fix on the pre-rewrite API line `Avalonia.FreeDesktop 11.3.x` binds against by member layout, unlike `0.94.2`'s rewritten `0.9x` line, which is what caused this debt in the first place. Reproduced first — the identical `System.TypeLoadException`, verbatim — then verified fixed: `dotnet build src/TempestOS.slnx -c Release -p:TreatWarningsAsErrors=true` is 0 Warnings/0 Errors; the full `Tempest.Desktop.Tests` (408/408) and `Tempest.Core.Tests` (3088/3088) suites pass; the built `Tempest.Desktop.dll` launches under `xvfb-run` on Linux with no exception anywhere in its output and no crash log, completing full service/module/UI initialisation (including binding its REST API on `127.0.0.1:5080`) to a running state that persists until deliberately terminated. A `linux-launch-smoke` job now runs this same launch on every CI push (advisory — not yet in `gate`'s required checks, per this Work Package's own brief). Per `D-025` (**ratified by the Product Owner 2026-09-05**), Linux is explicitly **not** recorded as supported without qualification and is **not** claimed CI-verified — the ratified boundary keeps the distinction between successful launch evidence and formal CI verification. **Not yet CI-verified in the sense Windows is:** the launch was verified once, locally, under `xvfb-run`, and the `linux-launch-smoke` job is advisory and has accumulated no real CI runs (GitHub Actions on this account has been unable to obtain a runner since 2026-09-04 22:34 UTC). Hedged here to match `WP 16.5B`'s own report after the `v0.16.0` review board found this row overstated it. See `docs/releases/v0.16.0/WP16.5B Linux Launch Spike Report.md` for full evidence, an Avalonia 11.3 headless-layout behaviour difference found and fixed along the way in `Tempest.Desktop.Tests`, and exactly what this evidence does and does not establish. |
| TD-117 | **`UndoRedoStack.UndoAsync`/`RedoAsync` raise `Changed` on whatever thread the undone action's continuation landed on,** and `UndoRedoCoordinator.RefreshButtons` — subscribed to that event — sets `IsEnabled` and tooltips on real Avalonia controls. `await action.Undo(ct).ConfigureAwait(false)` means any undo action that genuinely yields resumes on a thread-pool thread, so `Changed?.Invoke()` fires there and the button refresh throws `InvalidOperationException: Call from invalid thread`. **Reproduced**: Ctrl+D (Toggle Favourite) records an undo action whose body is `await favourites.SaveAsync(ct).ConfigureAwait(false)` — a real file write, which does yield — so pressing Undo after favouriting throws. Actions that complete synchronously (every in-memory one, which is why the existing suite is green) are unaffected. **Pre-existing and untouched by `WP-E`**: both the `ConfigureAwait(false)` and the undo action predate it; `WP-E` found it by writing the first test to drive `Stack.UndoAsync()` with a coordinator attached. | `WP-E` Stage 2 (2026-09-01), discovered while testing the async favourite path | `WP-Z2` | **Resolved — `WP-Z2` (2026-09-01), `ADR-0119`.** `UndoRedoCoordinator.RefreshButtons` now marshals: `Dispatcher.UIThread.CheckAccess()` and, when the answer is no, `Post` — the third instance of an idiom `PlatformNotificationToastBridge` and `ThemeService` already used for the identical cause, now written down as `ADR-0119`. `Tempest.App` is untouched: no `IUndoRedoStack` contract change, no `UndoRedoStack` change, no Avalonia reference, and its `ConfigureAwait(false)` discipline intact. Both Undo and Redo are fixed through the one `Changed` subscription. **Two corrections to this row as first written.** *Age:* not new to `v0.13.1` — `UndoRedoStack.cs` has been unmodified since commit `0f125a3` (`v0.10.0`), and both halves of the defect were present at that tag, so it was reachable in **every release from `v0.10.0` to `v0.13.1`**. *Symptom:* not "an error dialog" — the exception escaped through a fire-and-forget `_ = UndoAsync()` into `TaskScheduler.UnobservedTaskException`, which fires only at GC finalisation, so the dialog was nondeterministically delayed or never seen. What happened every time was worse and quieter: the data change landed, then the toast, status bar, history entry, Explorer reload and Cockpit refresh never ran and the buttons went stale — Undo appeared to do nothing while having done something. **Why the suite missed it:** the only test driving the coordinator's `UndoAsync` asserted `CanUndo == false` first, so it returned before any action ran; the others used a bare stack with no coordinator subscribed, or an action completing synchronously. `UndoRedoThreadingTests` closes that gap with a genuinely yielding action on a coordinator-attached stack, and asserts its own premise — that the action really did resume off the UI thread — so it cannot pass vacuously. Two mutations run, two killed: reverting to the direct refresh fails both new tests with the original `Call from invalid thread`; removing the `CheckAccess` fast path fails `MainWindowCompositionTests` too, proving that branch is load-bearing rather than defensive. |
| TD-118 | **The Engineering Cockpit's read surface is synchronous by shape, and cannot become asynchronous without a public API change across two assemblies.** Every discipline read-model exposes its data as properties (`Status`, `KpiCards`, `Count`, `OutstandingActions`), and C# properties cannot be `async` — so the persistence reads behind them are performed with `.GetAwaiter().GetResult()` on the UI thread. Converting them means turning roughly thirty `EngineeringCockpit` properties and six read-models into `Task`-returning methods and reworking `CockpitView`'s render path. | `WP-E` Stage 1 audit (2026-09-01) | Deferred architectural work — not `WP-E` | **Open — deferred by approved decision.** `WP-E` implemented the alternative that addressed the actual harm: per-refresh memoisation (`CockpitReadScope`), which removed the repetition without changing the API. Async conversion would *not* by itself have fixed the repeated reads, and would have reshaped a two-assembly contract immediately before the first physical review. Revisit trigger: **persistence becomes slow or remote**, at which point one bounded blocking read per refresh stops being acceptable and the conversion earns its cost. |
| TD-119 | **`Tempest.Desktop.Tests` used fixed `Task.Delay` waits as synchronisation against asynchronous work the tests never joined.** The acceptance tests raise a UI event (`RaiseEvent(Button.ClickEvent)` and siblings), which returns as soon as the `async void` handler reaches its first `await`, then slept a hard-coded interval — `Task.Delay(20)`, `(50)`, `(60)`, `(80)`, `(100)` — before asserting on state a still-running command and persistence chain was writing. The delay was a guess, not a join, and the state was sampled once, after the guess. **Remediated across `WP-Z4` Stages 8, 11 and 13; 51 of 52 sites converted, test-only, with zero `src/` changes.** The remedy is the bounded, condition-based poll `TD-46`/`WP 11.4A` established and `WP 13.12.9` reused: re-read the real state every iteration, stop on the condition or a two-second deadline, and leave the original assertion untouched so a genuine failure still fails on its own message. **Counts re-derived at `f0fcad6`: 59 executable `await Task.Delay(...)` occurrences across 16 Desktop test files, of which 58 are legitimate — bounded-poll back-offs or deliberate yields — leaving exactly 1 fixed wait**, down from 52. **The sole remaining site is `WorkflowInteractionTests.cs:335`** (`RibbonDelete_ConfirmationDeclined_DoesNotDeleteTheRealObject`, the Stage 10 "site 47" / Class D case): every assertion in it is negative — the object must *not* be deleted — and the declined path is `CommandOutcome.Cancelled`, which raises no completion event at all, so there is nothing to poll for and no signal to join. It is deliberately retained; waiting longer only strengthens a negative assertion, so it cannot produce a false pass. **Incident history — six CI occurrences.** (1) `v0.11.0`, `ObjectEditorViewTests.VerificationResultSection_RecordPass_…`, first real CI execution, fixed by `WP 11.4A` (`TD-46`); (2) `v0.13.0`, `ObjectEditorViewTests.Save_RevisedContent_…`, tag-triggered `release.yml` run `32146823154`, diagnosed by `WP 13.12.8` and fixed by `WP 13.12.9`; (3) `384e47f`, PR run `33545260056`, **two tests in one run** — `ProjectGovernanceAcceptanceTests.Journey_ProposeADecision_…` in Debug (`Assert.Single() Failure: The collection was empty`) and `FeatureCompletionTests.MechanicalDuplicate_…` in Release (`Assert.Equal() Failure: Expected 13, Actual 12`); (4) `b09a620`, push run `33557502598` Release, `SurfaceCommandIntegrationTests.Ribbon_RequirementsDeleteGroup_…` (`Assert.Contains() Failure … Collection: []`); (5) `a1e64d2`, PR run `33565738694` Release, `ProjectTaskAcceptanceTests.Journey_CreateATask_…` (`Assert.Single() Failure: The collection was empty`); (6) `e7357b6`, push run `33644145385` Debug, `ProjectTaskAcceptanceTests.ADueDateSetThroughTheDialog_…` (`No 'Due date' button on this surface. Present: New Task, View as board`) — a new signature, the row-level button not yet rendered rather than a collection read empty. **Seven distinct tests in total, five of them within `WP-Z4`.** A count correction: earlier `WP-Z4` reporting said "six occurrences across five distinct tests", conflating the two figures; six is the number of CI incidents, seven the number of distinct tests. **Three legitimate synchronisation patterns exist in this suite, not one.** The deadline-bounded `while` (`TD-46`/`WP 13.12.9`); the **attempt-bounded `for`**, found by the Stage 10 audit and used six times (`ProjectAreaAcceptanceTests:135, 185, 225, 342, 517`, `DocumentViewerAcceptanceTests:567`), one of which already carried the comment "Fire-and-forget by design, so wait for the open to land" — the suite had diagnosed this mechanism before the register named it; and the deliberate thread-pool yield (`UndoRedoThreadingTests:60`, `WP-Z2`). **Several apparently asynchronous paths are in fact synchronous or deterministically drainable, and their waits were removed outright rather than converted:** `RibbonView.OnCommandButtonClickAsync` refuses an unavailable command and raises `ActionCompleted` before its first `await`; `ProjectExplorerView` wires `_filter.PropertyChanged` straight to a synchronous `ApplyFilter()`; and `ProjectTaskAcceptanceTests.LayOutAsync` now drains the dispatcher with `Dispatcher.UIThread.RunJobs()` (`UndoRedoThreadingTests`, `WP-Z2`) instead of guessing 40 ms. **Two classification errors of this programme's own are disclosed rather than rewritten, and both were caught by the test suite, not by reasoning.** First, this row originally stated "3 legitimate / 61 fixed waits"; the true split at that moment was **10 legitimate / 54 fixed**, because the classification had been derived from the two files already open rather than from all sixteen — corrected at Stage 11. Second, the Stage 10 audit classified three Command Palette sites as needing no wait at all, reasoning that `_query.TextChanged` reaches a synchronous `ApplyFilter`; `TextBox` in fact raises `TextChanged` on a later dispatcher pass, unlike `AvaloniaObject.PropertyChanged`, and four tests failed on the first Stage 11 run. Those sites were reclassified and converted, and the palette's invoke helper now joins on `CommandInvoked`/`CommandUnavailable` — the real completion signal the palette already raises. A third under-join was caught the same way: `Save_RenamedName_…` polled the repository but not `editor.IsDirty`, which clears on a later continuation. | `TD-46`/`WP 11.4A` named the mechanism; `WP 13.12.9` §6 first quantified the scale (28 sites, generalisation explicitly declined); raised as its own row by `WP-Z4` Stage 5 (2026-09-01) after the third incident; remediated by `WP-Z4` Stages 8, 11 and 13 (2026-09-01/02) | `WP-Z4` Stages 8, 11, 13 (remediation); `WP-Z4` Stage 14 (verification) | **Resolved** (`WP 16.4A`, 2026-09-04) — the last fixed wait (`WorkflowInteractionTests.cs`) was dead time: the declined-delete path awaits an already-completed task and completes synchronously inside `RaiseEvent`; removed, 30/30 consecutive passes, rationale pinned in a comment. Previously: **Partially resolved, `WP-Z4` Stages 8/11/13.** 51 of 52 fixed waits are gone and the one that remains is a deliberate, reasoned retention, not an unfinished conversion. **Verification is dual-event and deliberate:** at `f0fcad6` **both** the `push` run `33653617714` and the `pull_request` run `33653636667` passed Build & Test (Debug), Build & Test (Release), Governance Health Check and `CI Gate` against the identical SHA — the first commit in the sequence where the two event runs agree completely, after four consecutive commits (`384e47f`, `b09a620`, `a1e64d2`, `e7357b6`) where they disagreed and the failing side alternated. That agreement is the point: a single green run on a suite of this construction proved nothing, which is why the standard was set at both events on one SHA. Locally: Desktop 372/372 three times in Debug and three times in Release, Core 3,088/3,088 in both, zero warnings under `TreatWarningsAsErrors`. **Mutation evidence, all reverted:** suppressing the task creation reproduced the `e7357b6` message verbatim; suppressing the assign, score, save, lifecycle, delete and milestone actions each failed on the original assertion with its original message; defeating the palette event join failed; forcing five stale observations into the render polls still passed, proving the loops wait and re-observe rather than sampling once; and setting the deadlines to zero failed six of nine tests, the first with that same `e7357b6` message — the wait is what removes the race, not the machine's speed. **Release outcome (`WP-Z4` Stage 24):** the remediated work shipped in **`v0.14.0`**. `f0fcad6` above remains the historical evidence for the Stage 14 dual-event result and is deliberately not restated; the final PR head merged was **`ad7ecf4`** (which added only the `ci.yml` diagnostic-step fix on top of this remediation), and the merge commit `026ed7c` is the §7.3-gated commit, the `v0.14.0` tag target and the published release commit. `release.yml` run `33736273125` re-ran Build and Test in Release at that commit before publishing, so the remediated suite passed a further independent execution at the tag. **The single retained fixed wait shipped as disclosed.** **Revisit trigger:** any further CI recurrence, or work that gives `RibbonDelete_ConfirmationDeclined_…` an observable completion signal for the cancelled path. |
| TD-120 | **The Desktop test suite never removes the isolated persistence roots it creates, so a machine running it repeatedly fills its disk.** Every `WorkspaceHost` test builds a fresh root through `WorkspacePersistenceCollection.NewIsolatedPersistenceRootPath()` — correctly, since isolation is what keeps the suite parallel-safe and order-independent — but nothing deletes it afterwards. `host.ShutdownAsync()`/`DisposeAsync()` close the host, not the directory. **Observed at `WP-Z4` Stage 13** while running the Desktop suite six times over for release verification: the container reached **0 bytes free** mid-run, and the third Debug repetition failed en masse with `No space left on device` — dozens of tests red for want of disk, not for want of correctness, which is a genuinely misleading failure mode. A direct count of the leftovers found **≈9,000 directories** under `/tmp` — 5,535 `TempestOS.Desktop.Tests.*` plus roughly 3,500 `tempest-project-{tasks,milestones,governance,areas}` and `tempestos-tests` roots — accounting for **≈12 GB**; removing them, together with stale task output, took the volume from full to 27 GB free. **This is test-cleanup/environment debt, not a product defect and not a CI failure.** CI runners are ephemeral and start clean, so the hosted runs never accumulate; it bites a developer or agent running the suite many times on one machine, which is exactly what release verification requires. It is **not a merge blocker** for `v0.14.0`: it changed no result once the disk was cleared, and the verification was repeated clean. | Observed and disclosed by `WP-Z4` Stage 13 (2026-09-02) during release verification | Unassigned — no owning Work Package; **deliberately not fixed inside `WP-Z4`**, whose remaining scope is documentation only | **Resolved** (`WP 15.2A`, commit `8b6cc1e`, carried into `feature/v0.16.0` by `WP 16.0B`). `PersistenceRootCleanupFixture` roots every isolated persistence path under one shared, per-test-run parent directory and removes it at the end of the run, so a machine running the suite repeatedly no longer fills its disk. The second option in the original remedy was taken: a failed test's root does not survive for diagnosis, which the Work Package's own report records as a deliberate trade. Evidence: `docs/releases/v0.16.0/WP15.2A Desktop Test Suite Persistence Root Cleanup — Implementation Report.md`. **This row was left reading "Open" by `WP 15.2A`'s own merge and by two subsequent register passes** — found by the `v0.16.0` review board, which noted that `scripts/governance-healthcheck.ps1` Check 12 cannot see it: that check verifies the summary line against a count of the rows' own Status cells, so a row whose status was never updated is self-consistent by construction. Corrected here; the class of gap is recorded as `TD-122` |
| TD-121 | **A real Windows startup crash**: `WorkspaceLayoutController.RestoreAsync` awaited a Core settings read with `ConfigureAwait(false)` and then called `Load(tree)` directly; once that read genuinely completed asynchronously (reliably the case on Windows), the continuation resumed on a thread-pool thread and `Load` synchronously drove the visual tree, throwing `Dispatcher.VerifyAccess`'s `"Call from invalid thread"` unhandled from the `async void Window.Opened` handler, killing the process moments after the window appeared. Found and fixed same-commit, added here retroactively for traceability — added by `WP 15.1A` (2026-09-04), no register entry existed at the time it was found and fixed | `WP 15.0A` (`91cdbff`) | **Resolved.** Only `Load` is marshalled via the same `CheckAccess`/`Invoke` idiom `ThemeService` already established; `ConfigureAwait(false)` is kept on the awaited call itself, which has no UI affinity. Verified on a real Windows machine: the application launches, stays running, and the shell is navigable. |
| TD-122 | **The Ribbon's horizontal scrollbar never rendered at compact window widths (~1000px)** even when its command groups genuinely overflowed: a `ScrollViewer`'s `Auto` visibility only ever occupies room its own final arranged size already has slack for, and nothing upstream gave the Ribbon's `ScrollViewer` more height than its own button rows needed. Found and fixed same-commit, added here retroactively for traceability — added by `WP 15.1A` (2026-09-04), no register entry existed at the time it was found and fixed | `WP 15.0D` (`0451369`) | **Resolved.** A content-driven `MinHeight` reservation (button-row height + `DesignTokens.SpaceXl`) lets `Auto` show the scrollbar exactly when needed, at no cost when nothing overflows. Verified via real Xvfb interaction, 1000×700 and 1600×1000, both themes; two new regression tests assert the real `ScrollBar` control's rendered visibility. |
| TD-123 | **Three governance registers have no machine derivation, so `TD-57`'s failure mode can recur silently — and did.** `scripts/governance-healthcheck.ps1` (16 checks, `WP 16.1B`) derives the Interface, Exception and Namespace registers directly from `src/`, but the **Dependency Injection**, **Platform Services** and **Validation** registers are checked by nothing: they are re-derived only when a human Work Package chooses to. `WP 16.3B` registered `IStateMigrationRegistry` in `TempestHost.cs`'s own Phase 6 block without adding its DI Register row, and that register still stated 55 statements / 50 named rows against a tree carrying 56 / 51 — one Work Package after `TD-57` closed, undetected by every automated gate. | `v0.16.0` review board, 2026-09-05 | Unassigned | **Open.** The DI Register is mechanically derivable from one file by the command the register itself already documents (a single `grep -c` over `src/Tempest.Core/Runtime/TempestHost.cs` counting its `Singleton`/`AddInstance` call sites) and should become health-check Check 17. Platform Services and Validation are prose-heavy and harder; at minimum each needs a check that its stated "Last Reviewed" is not older than the newest commit touching the source it describes. |
| TD-124 | **The health check cannot detect a stale individual Technical Debt row.** Check 12 verifies this register's summary line against a count of each row's own Status cell, so a row whose status was never updated after the work closed is self-consistent by construction and passes. `TD-120` read "Open" from `WP 15.2A`'s merge through two subsequent register passes and two Work Packages built specifically to catch drift, while four other documents — including this register's own "Last Reviewed" narrative — said Resolved. | `v0.16.0` review board, 2026-09-05 | Unassigned | **Open.** A cheap partial check: warn where a row's Status reads Open but the row's own closing-Work-Package cell names a Work Package that has landed. It cannot be complete — the real fix is that a Work Package closing a debt edits the row in the same commit — but it converts a silent failure into a visible warning. |
| TD-125 | **The Rejected Designs Register indexes 45 entries against 65 in its own source log.** Found and disclosed by `WP 16.2A`, which judged it outside its own itemised scope; `docs/governance/Governance Index.md` records the discrepancy. No Work Package owns closing it. | `WP 16.2A`, re-raised by the `v0.16.0` review board, 2026-09-05 | Unassigned | **Open.** Twenty entries need indexing. Nothing machine-checks it today; a count check would be as cheap as the ADR-count check the health check already performs. |
| TD-126 | **`docs/architecture/Platform Service Map.md` has no detail section for the three Platform Services added in `v0.14.0`** (Plugin Trust & Capability Enforcement, Engineering Object Durability & Rehydration, Attachment Content Store). `WP 16.2A` added their rows to the Platform Services Register but recorded the Map itself as a residual outside its own `docs/governance/**` ownership, so nothing owns it. | `WP 16.2A` residual, re-raised by the `v0.16.0` review board, 2026-09-05 | `WP 16.4B-R1` (Architecture remediation) | **Resolved.** `WP 16.4B-R1` added complete Responsibility/Key types/Dependencies/Consumers/Lifecycle/ADR-reference/Academy-reference sections for all three to `docs/architecture/Platform Service Map.md` (and their own "At a Glance" table rows, also missing until now), following the Map's own established shape for the pre-existing services. See `docs/releases/v0.16.0/WP16.4B-R1 Migration Collision Guard and Platform Service Registration.md`. |
| TD-127 | **`v0.16.0`'s own Work Packages have no Academy retrospectives.** Engineering Governance §3/§6 make a retrospective part of each Work Package's own Definition of Done, and release-level Definition of Done item 4 ("Academy updated") was made true for the first time since `v0.10.0` by `WP 16.2B` — which backfilled 41 historical retrospectives but, by its own scope, none of this release's. Closing `v0.16.0` without them re-breaks item 4 the moment it is met. This repository has run one release behind on retrospectives for its entire history, which is why `WP 16.2B` had to exist at all. | `v0.16.0` review board, 2026-09-05 | `WP 16.2C` | **Resolved** (`WP 16.2C`, completed 2026-09-05). Every landed `v0.16.0` Work Package now has a retrospective under Engineering Governance §6's thirteen-section template: the eleven from the original plan, plus `WP 16.4B` and the five remediation packages (`16.1A-R1`, `16.2C` itself, `16.4B-R1`, `16.4B-R2`, `16.5A-R1`) — 217 → 223. **The first pass closed only half of it and this row said so wrongly for a time:** an integration commit's message claimed "closing `TD-127`" while this row still read Open and six Work Packages had no retrospective, found by the independent post-remediation governance audit. Release-level Definition of Done item 4 ("Academy updated") is true for `v0.16.0`. The standing convention question — whether §6 means "within the Work Package" or "before the release closes" — is a governance decision this row does not make and does not pre-empt. |
| TD-128 | **The Digital Thread graph's relationship (edge) lines are keyboard-unreachable.** `line.PointerPressed`/`hitTestLine.PointerPressed` (`DigitalThreadGraphView`) are the only way to select an edge, and `OnGraphKeyDown` has no case for it; node focus (`Border.Focusable`/`TabIndex`) never reaches an edge, which is not a focusable element at all today. `WP 16.5A` claimed comprehensive keyboard operability for this view and delivered it for nodes, zoom and pan only. | `v0.16.0` review board, 2026-09-05; re-confirmed by `WP 16.5A-R1` | Unassigned | **Open.** Needs a real focus model for edges — each relationship line becoming a focusable, tab-reachable element with its own visible focus indicator and an `OnGraphKeyDown` case to activate or inspect it. A design decision, not a defect fix, and out of `WP 16.5A-R1`'s scope. Part of `TD-65`'s remaining inventory. |
| TD-129 | **The REST handler's 404-versus-401 split lets an unauthenticated caller enumerate real routes.** `ApiRequestHandler.HandleAsync` looks the route up before authorising, so an unknown path returns `404` and a known one returns `401` — one probe per candidate path reconstructs the route table. `WP 16.4B-R2` removed the permission key from the `403` body, which was the other half of the disclosure, but deliberately left the status codes alone. | `v0.16.0` review board, 2026-09-05 (`WP 16.4B-R2`) | Unassigned | **Open** — collapsing `404` and `401` into one response for unauthenticated callers would close it, but changing documented status codes is a Product/Security decision, not an engineering one, and the practical exposure is nil while `AT-10` holds (no shipped assembly maps a command onto an HTTP route) and the listener is off by default (`D-024`, Proposed). Revisit the moment either changes. |
| TD-130 | **The three reconciliation services have no authorization seam.** None takes `IIdentityService` or `IPermissionEvaluator`, so nothing in the platform backstops authorization when `DetectAsync`/`SweepAsync` is eventually wired to a command, CLI or route — the caller must remember, and `SweepAsync` deletes data. Nothing wires them today. | `v0.16.0` review board, 2026-09-05 (`WP 16.4B-R2`) | Unassigned | **Open** — deliberately not designed now against a surface that does not exist: guessing at the permission model for an unbuilt caller is worse than naming the requirement. **Whichever Work Package wires a sweep to any surface owns adding the check as part of that work**, and should read `WP16.4B-R2 Attachment Sweep Write-Intent Marker.md` first for why these operations are more dangerous than they look. |
| TD-131 | **The focus-ring contrast test cannot see any `Flat`-treatment state.** `FocusVisibleStyleTests` skips an adjacency whenever the background brush is not a fully opaque solid, and all three `Flat` fills are transparent or translucent washes (`Brushes.Transparent` at rest, a 5% hover wash, a 12% pressed wash). So the treatment used for most toolbar and chrome buttons has **zero** automated contrast coverage in either theme. Measured by hand at the review it is fine today (4.8–10.1:1), but a future change to `HoverBackgroundBrushKey`/`SelectedBackgroundBrushKey` opacity or hue could drop it below 3:1 with nothing to catch it. The skip is disclosed in the test's own comment — this row is about the coverage gap, not a hidden defect. | `v0.16.0` independent post-remediation review, 2026-09-05 | Unassigned | **Open.** The test must resolve the *effective composited* colour by walking up to the real parent background, or assert against each concrete surface `Flat` actually renders on. Part of `TD-65`'s remaining inventory. |
| TD-132 | **Every relationship row in the Object Editor exposes an identically-named "Open" button.** `ObjectEditorView` sets `AutomationProperties.SetName(openButton, "Open")` per row with no per-row differentiation, so a screen-reader user tabbing a multi-relationship list hears "Open, button" repeatedly with no way to tell which relationship each targets. Same class as the ribbon/graph/document-viewer gaps `WP 16.5A-R1` closed; this one was never in that Work Package's six findings. | `v0.16.0` independent post-remediation review, 2026-09-05 | Unassigned | **Open.** Needs a per-row name derived from the relationship's target — which text to use (kind, identifier, display name, or a composite) is a small design decision, which is why this is recorded rather than guessed at. Part of `TD-65`'s remaining inventory. |
| TD-133 | **Docking-panel repositioning and tab reordering are mouse-only.** `WorkspaceLayoutController.BeginDrag` and `LayoutTabGroupView`'s tab-drag handler are wired from `PointerPressed` with no `KeyDown` counterpart anywhere. Opening, closing, collapsing and pinning panels are all keyboard-reachable; *rearranging* the layout is not. Distinct from `TD-65`'s own "object move is drag-and-drop only" clause (that is engineering-object move, not panel layout) and from `TD-128` (graph edges), and disclosed in neither. | `v0.16.0` independent post-remediation review, 2026-09-05 | Unassigned | **Open.** Needs a keyboard model for docking (a move-mode with arrow keys, or a "move panel to…" command), which is a design decision rather than a defect fix. Part of `TD-65`'s remaining inventory. |
| TD-134 | **`SettingsDocument<TDocument>` has no per-consumer notion of "the current version", so it cannot tell a document written by a newer build from one that simply sits past this caller's own chain.** Its migration target is derived from the highest `FromVersion` the caller supplies, which is not a build version — so, unlike `EngineeringObjectStateStore` with its fixed `CurrentSchemaVersion`, it cannot safely discard an "ahead" document. The residual risk: an older build silently reads and uses a settings document that a newer build gave a new meaning to. | `v0.16.0` independent post-remediation review (security), 2026-09-05 — proposed as a symmetric "ahead" discard by analogy with the state store; the analogy was tested, the change written, and it broke `ADocumentAlreadyPastEveryRegisteredMigration_IsLeftAlone`, a pre-existing test encoding the opposite intent, so it was reverted and recorded here instead | Unassigned | **Open.** Closing it properly needs each consumer to declare its own current schema version alongside its chain, so "ahead" has a meaning; approximating it from the chain's reach discards documents no migration ever claimed it could not read. Low severity — this seam holds UI/session state (window geometry, recent files, panel layout), and `TD-60`'s degrade-to-defaults contract already covers unreadable documents. |
| TD-135 | **A lost update in durable object state let a reconciliation sweep permanently delete a live attachment.** `EngineeringObjectBase.PersistStateAsync` captured the live fields and then issued an unconditional whole-record overwrite with nothing serialising the pair, so two concurrent mutations on one object could land their saves in the opposite order to their captures — the later-landing, earlier-captured snapshot silently dropping the other's change from disk. Once that dropped an attachment reference, `AttachmentContentReconciliationService.SweepAsync` deleted its bytes as a genuine orphan, behaving exactly as designed, while the object and the caller who received a successful `IAttachment` still believed the file existed. Reproduced against the real classes (two concurrent `AttachContentAsync` calls, no artificial delays, hit on trial 323 of 500). The `WP 16.4B-R2` write-intent marker does not reach this: both writes complete and clear their markers correctly. | `v0.16.0` independent post-remediation review (persistence), 2026-09-05 | `WP 16.4B-R3` | **Resolved.** `PersistStateAsync` now acquires an `AsyncKeyedLock` keyed by object Id — owned by `EngineeringDomainContext`, not the instance, because `ReviseAsync` proves more than one live instance can answer to one Id — before `CaptureState()` and holds it through `SaveAsync`. An exhaustive re-entrancy audit found no nested capture-then-persist path, so no restructuring was needed. `EngineeringObjectFactory<T>.CreateAsync`'s initial save had the identical unprotected shape and is now routed through the same lock. The underlying lost update predates `v0.16.0`; this release's sweep is what converted it from a dropped reference into permanent deletion, which is why it is closed here. |
| TD-136 | **`ReviseAsync` was the one durable-write path `TD-135`'s fix never routed through the lock, and it lost data the same way.** `EngineeringObjectBase.ReviseAsync` captured this object's state outside the per-object write lock, handed the snapshot to a newly constructed successor instance, and registered that successor in the repository in place of its predecessor — leaving two independently mutable instances mapped onto one durable record. `TD-135`'s remediation keyed the write lock by `Id` rather than by instance *because* of this hazard, and said so in `PersistStateAsync`'s own remarks, but ordering the two writers is not sufficient: the successor's snapshot was taken at revision time, so a predecessor write landing after it is invisible to the successor, and the successor's next mutation overwrites the whole record and discards it. Reproduced by the final release review against the real classes: attach on the predecessor lands durably, an ordinary rename on the live successor follows, and the attachment is gone from disk — and because the predecessor's own persist had already cleared the write-intent marker, the reconciliation sweep then saw content present, unmarked and unreferenced, and deleted the file's bytes as a genuine orphan | Final `v0.16.0` release review board (found); `WP 16.4B-R3`/`TD-135` (the incomplete fix); `WP 16.4B-R4` (closed) | N/A — resolved in the same release | **Resolved, `WP 16.4B-R4`** — `ReviseAsync` now performs its capture, hand-off and repository registration inside the same per-object write lock, and retires the predecessor there; a later durable write through a retired instance throws `SupersededEngineeringObjectException` instead of silently overwriting. The lock is the linearisation point, so a predecessor write that arrives *before* the revision is still carried into the successor — verified, because a fix that simply stopped predecessors writing would have broken the ordinary rename-then-revise sequence every `Revise*Command` in `Tempest.App` performs. Mutation-proven in both directions: with the fix reverted, 3 of the 5 new tests in `RevisedObjectWriteSerializationTests` fail, one of them reporting the attachment collection on disk as empty after the write was accepted without error; with it restored, 5 of 5 pass |

**Disclosed correction (`WP 13.2B` governance review):** this paragraph's
own leading figure had drifted stale at "10 Resolved, 1 Partially
resolved, 37 Open" since `WP 11.4A` — never re-incremented by `WP 13.1A`'s
own `TD-06` resolution or `WP 13.2A`'s own `TD-09`/`TD-10`/`TD-11`
resolutions, even though both Work Packages correctly updated this
register's own `Last Reviewed` narrative above. Re-verified here by a
direct count of every `TD-NN` row's own Status field (48 rows total, no
gaps, no duplicates): 16 Resolved, 1 Partially resolved, 31 Open —
matching the `Last Reviewed` narrative's own running total exactly.
**Total: 116 tracked debt items.** `WP-REVIEW` Clean-Machine Readiness Audit (2026-09-01): one row added (`TD-116`) — the Linux/X11 Desktop launch failure, found by attempting an actual launch from a clean clone rather than by reading code. Updated Architecture & Dead-Code Audit (2026-08-31): eleven rows
added (`TD-105`–`TD-115`) from the full-repository architecture and
dead-code audit, and `TD-01`'s documented revisit trigger recorded as
fired.

**Disclosed correction (Architecture & Dead-Code Audit (2026-08-31)):** this paragraph's own leading
figure was already stale before this pass — it read "92 tracked debt
items" while a direct count of `TD-NN` rows returned **104**
(`TD-01`–`TD-104`, sequential, no gaps, no duplicates), a drift of
twelve rows accumulated since the 2026-08-28 update. The row count is
corrected here to the verified total (104 existing + 11 new = 115). The
Resolved / Partially resolved / Open split is **not** restated, because
it could not be re-derived reliably by inspection: the Status column's
own formatting varies enough across 104 rows that 48 of them do not
begin with a parseable status token. Re-deriving that split is recorded
as outstanding governance work rather than carried forward as a figure
that is known to be wrong. The eleven rows added by this pass are all
**Open**.

**Previously stated (superseded, retained for traceability):** 92
tracked debt items — 26 Resolved, 2 Partially resolved, 64 Open.** Updated 2026-08-28 (Product Convergence & Recovery Programme):
`TD-84` added as an explicit **P0 — Product Spine** grouping row, under
which `TD-74`/`TD-76`/`TD-79`/`TD-81` are now cross-referenced as four
symptoms of one architectural deficiency rather than four independent
features; the spine itself is built and **Partially resolved**, proven by
a full Definition-of-Done acceptance journey. `TD-85` added — the
in-memory object-graph durability boundary the spine's own restart
recovery exposed, worked around for projects and disclosed for everything
else. 83 → 85 tracked, 1 → 2 Partially resolved, 59 → 60 Open, re-derived
by direct row count.

Updated 2026-08-28 (Engineering Object Rehydration, `TD-85`): `TD-85` is
**Resolved** by `ADR-0113` — engineering object state is durable, each
canonical type rehydrates itself, and `Projects.Index` was removed rather
than retained, leaving one object model and one persistence authority.
Three new rows disclose what was deliberately not attempted: `TD-86`
(per-mutation write volume), `TD-87` (versionless state schema), `TD-88`
(eager startup rehydration). 85 → 88 tracked, 23 → 24 Resolved, 60 → 62
Open, re-derived by direct row count (88 `TD-NN` rows). The pre-existing
total below is retained for provenance:
**(previous) Total: 83 tracked debt items — 23 Resolved, 1 Partially resolved, 59
Open.** Updated 2026-08-28 (Product / UX / Functional Compliance Audit
against the original design brief and the five supplied mock-ups): two
items resolved with implementation, tests and run-and-verified mutations
(`TD-70` responsive workspace and ribbon minimisation, `TD-71` splitter
drag as a durable preference), and twelve added (`TD-72`–`TD-83`) —
69 → 83 tracked, 21 → 23 Resolved, 47 → 59 Open, re-derived by direct row
count. The audit's own finding is that the shortfall against the mock-ups
is structural rather than cosmetic — no project context (`TD-76`), no
global navigation (`TD-74`), no drawing viewer (`TD-80`), no Companion on
this branch (`TD-82`) — and is recorded at severity here rather than
softened. Full report:
`docs/governance/Quality/Product Compliance Audit (2026-08-28).md`.
The pre-existing total below is retained for provenance:
**(previous) Total: 69 tracked debt items — 21 Resolved, 1 Partially resolved, 47
Open.** Updated 2026-08-28 (independent closure-verification pass):
three items resolved with source, tests, and mutation evidence
(`TD-58` redundant rebuilds, `TD-59` reserved device-name identifiers,
`TD-60` malformed-value read paths — all three surfaced by the
2026-08-28 independent review and closed in the same pass), and ten
items added from the same review's eight-discipline re-examination
(`TD-57` governance-register currency, `TD-61`–`TD-69` above), 56 → 69
tracked, 18 → 21 Resolved, 37 → 47 Open — re-derived by direct row
count. The pre-existing total below is retained for provenance:
**(previous) Total: 56 tracked debt items — 18 Resolved, 1 Partially resolved, 37
Open.** Updated `WP 13.11D` (`v0.13.0` Plugin Platform Exit Review): one
item added, `TD-56` (55 → 56 tracked; 36 → 37 Open) — a genuine, newly
found trust-boundary escape in which a plugin's own **constructor** runs
outside its component scope and is therefore treated as first-party by
every capability gate, defeating `TD-52`'s own gate for signed plugins.
Graded **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support**, exactly as
`TD-51` was, and explicitly **not** blocking WP14 or `v0.13.0` as
shipped: it is dormant (`src/Plugins/` is empty, third-party support is
neither enabled nor advertised), unreachable below the `VerifiedSigned`
tier, causes no crash, and is orthogonal to UI/UX work. No status
changed; no item resolved or removed. Previously updated `WP 13.11B`: `TD-51` moved **Reopened → Resolved** — the
one Release-blocking finding `WP 13.11A` raised, closed by both of that
review's own recommended minimum-fix options, (a) and (b), and no wider:
the unresolvable-constructor-parameter-type denial path now records every
discovered type in `deniedTypeRegistry` before throwing (so `WP 13.9.6`'s
own Module Discovery filter genuinely excludes it) and Module Discovery's
own `CreateDescriptor` call gained the same fail-closed reflection guard
already used elsewhere in this codebase. One item added by the same Work
Package's own independent Adversarial Review: `TD-55` (54 → 55 tracked;
35 → 36 Open), the `Type`-keyed denial registry's own cross-plugin
suppression hazard — latent since `WP 13.9.4`, newly *reachable* on the
denial path `TD-51` closed, and disclosed rather than silently accepted;
closing it needs a per-plugin denial-scoping mechanism, which is an
architectural change this deliberately minimal Work Package stopped short
of. `TD-53`/`TD-54` deliberately left Open and untouched.
Previously updated `WP 13.11A`: `TD-51` moved Resolved → **Reopened** (its
own disclosed residual gap, previously characterised as safely isolated,
re-examined and found to be a genuine, empirically-confirmed Host crash
on the `IModule` axis — see its own Status cell above for the full
correction); two new items added directly by this same review, `TD-53`
and `TD-54` (both low-severity, disclosed, Open — neither plugin-trust-
specific, neither a variant of `TD-51`/`TD-52`). Two items added
`WP 13.10A` (`TD-51`–`TD-52`, both a genuine,
live-PoC-confirmed gap in the plugin trust platform's own static/dynamic
enforcement, neither a variant of anything the `WP 13.9.1`–`WP 13.9.6`
remediation chain closed), both moved Open → Resolved by `WP 13.10B`'s
own implementation, independently re-verified and committed by
`WP 13.10C` — `TD-51` itself since reopened, `WP 13.11A`, and re-resolved,
`WP 13.11B`, per above. Two further items added `WP 13.3A` (`TD-49`–`TD-50`, both
Open — sourced
directly from `WP 13.2B`'s own commit message, two plausible-but-not-
fixed findings that had never before reached a formal register entry;
`TD-09`/`TD-10`/`TD-11` independently re-confirmed Resolved by direct
source re-read, not taken on citation). Previously: three items added
`WP 11.4A` (`TD-46`–`TD-48`, all immediately
Resolved — the specific defects `WP 11.9.0`'s own Release Publication
Report found on `v0.11.0`'s first real CI execution, now fixed and
independently re-verified on real infrastructure; see `WP11.4A Release
Engineering Corrections.md`), and `TD-44` moved Open → Resolved in the
same Work Package (CI now genuinely executes successfully on real
infrastructure, both branch- and tag-push paths — the precondition
`TD-44` was tracking). Previously: four items added `WP 11.9.0`
(`TD-42`–`TD-45`, all Open at the time — found during `v0.11.0`'s own
release sign-off: two genuine, previously-undisclosed release-tooling
defects found by independent QA re-verification (`TD-42`, `TD-43`,
both still Open — distinct from `TD-46`/`TD-47`, different defects in
the same two files), and two already-disclosed, still-open
release-engineering gaps consolidated into formal register entries for
the first time rather than left as prose-only disclosure (`TD-44`,
`TD-45`) — see `WP11.9.0 Engineering Release Report.md`).
Previously: one item added `WP 10.7A` (`TD-41`, Open — found during
`WP 10.7A`'s own Object Editor test coverage, see `WP10.7A
Implementation Report.md` §5). Previously: one item added `WP 10.3A`
(`TD-40`, Open — see `WP10.3A
Implementation Report.md` §11). Previously: one item added `WP 10.2B`
(`TD-39`, Open — see `WP10.2B
Engineering Review.md` §4). **Disclosed correction, `WP 9.3A`:** this figure had read "29"
(before that Work Package's own `TD-32` addition) — stale since at
least `WP 9.2A`, silently un-recomputed by `WP 9.4A`'s own "29" edit
despite that Work Package's own real row count already being 31 at the
time. Re-verified here by a direct count of every `TD-NN` row actually
present in this file (`TD-01` through `TD-34`, sequential, no gaps, no
duplicates) rather than carried forward — corrected to the true total,
not silently. **Re-verified again, `WP 9.5A`:** a direct count
immediately before this Work Package's own `TD-33` addition confirmed
exactly 32 rows present (`TD-01`–`TD-32`), matching the register's own
stated total exactly — no drift found this time.

## Entries — Disclosed, Accepted Trade-offs (Not Expected to Need Fixing Unless a Real Need Emerges)

| # | Trade-off | Disclosed By | Revisit Trigger |
|---|---|---|---|
| AT-01 | No automatic unsubscription on module stop/dispose (Event Bus) | ADR-0028 / WP 4.4D | A real, demonstrated need |
| AT-02 | Subscriber references held strongly for the Event Bus's whole lifetime | ADR-0028 / WP 4.4D | A real, demonstrated need |
| AT-03 | Exact-event-type-only dispatch, no polymorphic dispatch (Event Bus). **`WP 6.2`**: `NotificationDispatcher` mirrors this same design (`ADR-0046`), so the identical limitation now also applies to Notifications — a caller must publish against the same static type a subscriber subscribed to; found in practice against this Work Package's own sample consumers (see `ADR-0046`) | RD-0021 / ADR-0028; re-confirmed for Notifications, `WP 6.2`/`ADR-0046` | A real, demonstrated need |
| AT-04 | No ongoing supervision/monitoring of a hosted service's own work after `StartAsync` returns | RD-0026 / ADR-0029 | A real, demonstrated need |
| AT-05 | No automatic restart/backoff for an isolated hosted service failure | RD-0029 / ADR-0021/ADR-0029 | A real, demonstrated need |
| AT-06 | `src/Plugins/` remains empty — no real plugin built yet | WP 4.2 (by design) | The first Work Package that ships a real plugin (see `Plugin Register.md`) |
| AT-07 | ~~Zero real hosted services exist beyond the infrastructure~~ | WP 4.5 (by explicit scope decision) | The first Work Package that ships a real hosted service (see `Hosted Services Register.md`) — **Retired, `WP 6.3`:** `RestApiHostedService` is a real, non-infrastructure `IHostedService` — a genuine, listening HTTP server, orchestrated by the exact same `IHostedServiceManager` this trade-off's own revisit trigger named (`ADR-0047`), exactly the Work Package `WP 6.2`'s own disclosure said this assignment belonged to. `NotificationSampleHostedService` (`WP 6.2`) remains real too, but `WP 6.3` is the Work Package whose own revisit trigger this item explicitly named in advance (`Required ADRs.md`'s `ADR-0047` entry), so the retirement is recorded here. |
| AT-08 | No persistent/durable notification model, no history or inbox capability — a notification is not retained after dispatch | WP 6.2 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Persistence Requirements state "None" for Notifications) | A real, demonstrated need for notification history (a UI Shell's own notification centre, an audit-adjacent record of what was shown to a user) |
| AT-09 | No delivery-channel abstraction (email, webhook, push) and no durable report history for Reporting — a generated report is handed back to its caller and is not retained or delivered anywhere by the service itself | WP 6.0 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Future Extension Points name both explicitly as future, not current, scope) | A real, demonstrated need for one or both (a future engineering module or `WP 6.3`'s own REST API naming a concrete delivery or history requirement) |
| AT-10 | `IApiEndpointRegistry.MapCommand` carries no request-parameter binding — every REST-exposed command dispatches only its own parameterless `CreateDefault` factory instance; an inbound request's own body or query string is never threaded into the invoked command | WP 6.3 (by explicit, approved-contract scope decision — `Public Interface Catalogue.md`'s own `MapCommand` signature carries no binding mechanism) | A real, demonstrated need for a REST-exposed command that requires caller-supplied parameters beyond what a fixed default instance provides **Pinned, `WP-H` (2026-09-01):** `IdOnlyInvocationGuardTests.NoShippedAssembly_MapsAnHttpRouteOntoACommand` asserts that no shipped assembly maps a route onto a command — the two `MapCommand` callers are both in `Tempest.Samples`, which the Desktop does not reference. This is the premise the sanctioned Id-only REST call site rests on: a mapped discipline command would dispatch a `CreateDefault` instance it does not have, so mapping one is the trigger for closing this row. **Reclassified, `WP-A2` (2026-09-01) — from "not yet" to decided.** REST is **not** activated, and the reasons are larger than the parameter-binding gap this row records. Established by audit: **no production command is reachable over HTTP at all.** Of 74 descriptors, 18 are declared unavailable and all 56 invocable ones either need a selected object (49) or declare parameters (the 7 creates) — so the set invocable with `CommandContext.Empty` and no prompt is **empty**. Making REST genuinely functional therefore requires three things that do not exist: a request→`CommandContext` contract, a parameter source for the 42 prompt-bearing commands, and an authentication mechanism — `ApiRequestHandler` trusts the `identity` header verbatim (the 401 is a presence check, not authentication) and, by the `ADR-0052` decision, never establishes the current principal, so a REST-invoked domain command would be **authored by whoever the desktop session established, not by the caller**. That misattribution is latent only because no shipped assembly maps a route: both `MapCommand` callers are in `Tempest.Samples`, which the Desktop does not reference, and `IdOnlyInvocationGuardTests.NoShippedAssembly_MapsAnHttpRouteOntoACommand` keeps it that way. The Id-only allow-list entry for `ApiRequestHandler` is **retained** and remains correct. **Activation requires a separate feature/design Work Package** covering request context, parameter binding and authentication together; it is not remediation and is not scheduled here. |
| AT-11 | No compression or encryption of exported artifact content; an `IExportable` implementation is individually responsible for redacting or refusing to export sensitive content — `IExportService`/`IImportService` impose no content-level policy of their own, mirroring how Persistence imposes none on what Settings/Audit choose to store | WP 6.7 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Future Extension Points name both compression and encryption explicitly as future, not current, scope; Security Considerations name redaction as each `IExportable`'s own responsibility) | A real, demonstrated need for either (a concrete deployment scenario naming artifact confidentiality or size as a requirement) |
| AT-12 | `IImportService.ImportAsync` rejects (never upgrades or downgrades) an artifact section whose schema version does not exactly match the currently registered `IImportable` — no migration path exists for an older, still-readable schema version | WP 6.7 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Versioning Policy states an incompatible version "must reject... not silently downgrade or upgrade"; Future Extension Points name a migration path explicitly as future scope) | A real, demonstrated need for reading an older schema version (a concrete, shipped schema version bump with a genuine backward-compatibility requirement) |
| AT-13 | No remote validation/activation, no floating/seat-based licensing, and no license-renewal/grace-period model — `ILicense` is immutable for the life of the running process; a license change requires a full Host restart | WP 6.6 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Future Extension Points name all three explicitly as future, not current, scope) | A real, demonstrated need for any of the three (a concrete deployment scenario naming remote activation, concurrent-seat limits, or graceful expiry handling as a requirement) |
| AT-14 | The compile-time dimension-safety guarantee (`Quantity<TDimension>`'s generic constraint prevents cross-dimension arithmetic/conversion) is verified by direct inspection — a demonstration class documenting the exact `CS1503`/`CS0019` errors reproduced against this repository's own compiler — rather than an automated compiler-error assertion, which would require a new Roslyn-scripting test dependency this framework does not otherwise need | WP 7.1B/`ADR-0054` (`WP7.0C Testing Strategy.md`'s own "Additional: compile-time rejection test" category named the guarantee; this Work Package chose disclosed manual verification over new test infrastructure) | A second framework independently needing the same kind of compile-time guarantee verified, making shared Roslyn-scripting infrastructure worth its own dependency cost | None — by design, this is not expected to change unless a second, independent need for the same kind of test justifies the added infrastructure. |
| AT-15 | `IMaterialCatalog` performs no permission-gating of its own — a caller registering or revising a material is expected to enforce authorization at the calling layer (mirroring `IReportingService`/`INavigationProvider`'s own precedent), even though `WP7.0C Engineering Foundation Contracts.md`'s own Platform Services Consumed line named "Identity & Permissions (registration authorization)" | WP 7.1C/`ADR-0055` | A real, demonstrated need for authorization enforcement specifically inside `IMaterialCatalog` itself, rather than at each caller's own calling layer | A real, demonstrated need for framework-internal authorization, rather than calling-layer enforcement — not assumed to exist yet. |
| AT-16 | `CalculationContext.ReferenceMaterial` accepts any string — the Calculation Framework does not verify a referenced `materialId` actually exists in `Tempest.Core.Materials`, and has no dependency on that framework at all | WP 7.1D/`ADR-0056` Decision 6 | A real, demonstrated need for the Calculation Framework itself to validate material references, rather than trusting the calling definition | A real, demonstrated need for framework-internal reference validation — not assumed to exist yet; mirrors `EngineeringData.DocumentReference.RelationshipKind`'s own open-string precedent. |
| AT-17 | `VerificationContext.ReferenceMaterial` accepts any string — the Verification Framework does not verify a referenced `materialId` actually exists in `Tempest.Core.Materials`, and has no dependency on that framework at all | WP 7.1E/`ADR-0057` Decision 5 | A real, demonstrated need for the Verification Framework itself to validate material references | A real, demonstrated need for framework-internal reference validation — not assumed to exist yet; identical precedent to `AT-16`. |
| AT-18 | Undo/Redo (`IUndoRedoStack`, `ADR-0098`) is wired only for Rename (all six disciplines) and the new Favourite/Un-favourite toggle — Create/Delete/Duplicate/Move and every Set-Status/Set-Priority/Set-Owner command have no Undo entry; Delete specifically has no defined inverse (already a soft delete, no "restore" operation anywhere in this platform) | WP 10.6A/`ADR-0098` (by explicit, disclosed scope decision — this Work Package's own time budget) | A real, demonstrated need for broader Undo coverage, or a real "restore a soft-deleted object" capability being built for an unrelated reason first |
| AT-19 | `IBackgroundTaskRunner` reports coarse state (Running/Succeeded/Failed/Cancelled) and elapsed time only, never a percentage — no `ICommandHandler<TCommand>` anywhere in this platform reports incremental progress; that frozen Command Framework contract carries no `IProgress<T>` parameter | WP 10.6A (by explicit, disclosed scope decision — extending `ICommandHandler<TCommand>` would be a Command Framework-wide change, out of this Work Package's own scope) | A real, demonstrated need for percentage progress on a genuinely long-running command handler — none exists yet |
| AT-20 | ~~A Macro step (`RunMacroCommand`, `ADR-0099`) must be a `CommandDescriptor` with `CreateDefault` set — confirmed by repository-wide `grep`, only `Tempest.Samples` commands qualify today; no real Engineering discipline command is invokable by Id alone.~~ **Superseded, `TD-77` Stage 5.** Macro eligibility is now derived from the command's own `CommandBinding`, not from `CreateDefault`: a step must be a command that needs nobody present, which is exactly what `CommandBinding.RequiresPrompt` already answers. Parameterless, non-interactive bound commands are eligible — in practice the thirteen lifecycle/status transitions plus `mechanical.validate-configuration`. Commands declaring values to collect are excluded, and so are confirmation-gated ones (every Delete and every Duplicate), which is what keeps a destructive command out of an unattended run. `CreateDefault` remains supported for legacy and degenerate callers and is still honoured by the Id-only invocation path, but it is no longer the mechanism that determines whether a real discipline command is available to a macro. | WP 10.6A (by explicit, disclosed scope decision); superseded by `TD-77` Stage 5 | **Retired** — the limitation this row disclosed no longer exists. |
| AT-21 | `CommandHistoryLog` records only what already reaches `MainWindow`'s own existing `ActionCompleted`-shaped UI surfaces (Ribbon, Explorer, Inspector, Object Editor, Undo/Redo), never a global `ICommandDispatcher` interception (frozen, unmodified); `succeeded` is a disclosed heuristic, inferred from whether the recorded message contains "fail," since these surfaces carry only a human-readable string, not a structured `CommandResult` | WP 10.6A (by explicit, disclosed scope decision — every currently-real dispatch path already funnels through one of the recorded surfaces) | A command dispatched through a future path that bypasses every recorded surface, or a real, demonstrated need for audit-grade completeness |
| AT-22 | `IUndoRedoStack`/`CommandHistoryLog` are both session-only — neither is persisted across a restart, matching most desktop applications' own established convention | WP 10.6A (by explicit, disclosed scope decision) | A real, demonstrated need for cross-session Undo/Redo or Command History |
| AT-23 | `KeyboardCommandBindingProvider` (`ADR-0100`) ships with zero default bindings and no end-user remapping UI; `IExternalControllerProvider` has zero real vendor integration (no Stream Deck plugin, no MIDI device support) — proven only against a test-only `StubExternalControllerProvider` | WP 10.6A/`ADR-0100` (by explicit Out-of-Scope instruction — no vendor SDK, no hardware integration) | A real, demonstrated need for either a remapping UI or a real vendor SDK integration — the latter requires its own commissioning decision **Pinned, `WP-H` (2026-09-01):** `DormantKeyboardBindingTests` asserts that no production code calls `KeyboardCommandBindingProvider.Bind(gesture, commandId)`, and that the shell still registers the provider — so the extension point is genuinely wired and genuinely bound to nothing. That is the premise the dormant Id-only call site in `InputBindingRouter` rests on; binding a real gesture is `WP-A2`'s trigger, and now fails the build until `WP-A2` is done rather than failing silently for the user. **Amended, `WP-A2` (2026-09-01) — the dormancy was a defect's shadow; now it is only a product choice.** `InputBindingRouter` routed through the obsolete Id-only `InvokeAsync(id, ct)`, which throws for all 74 production discipline commands; the throw was caught into a log line, so binding any real command would have produced a key that silently did nothing. The router now takes the canonical path — `Evaluate(id, context)` then `InvokeAsync(id, context, prompt, ct)` — with an optional `ContextSource` and `ParameterPrompt` supplied by `MainWindow` from the same `WorkspaceCommandContext.From(workspace.Selection)` and `DesktopCommandPrompt` the Command Palette already uses. Neither is fabricated: with no context source a selection-scoped command is refused with its own declared reason, and with no prompt a parameterised command reports that it needs input rather than running without asking. Its entry in `IdOnlyInvocationGuardTests`' allow-list is **removed**, not re-justified. **What this row now means, unchanged in substance:** the keyboard still ships with zero default bindings, no remapping UI and no binding store, by product choice — `DormantKeyboardBindingTests` still enforces that. The difference is that the first binding anyone adds will now work. Default bindings, persistence and a remapping surface remain feature work, not remediation. |
| AT-24 | `Duplicate*CommandHandler` invokes `Copy*CommandHandler.HandleAsync` directly, outside `CommandHandlerTable` — five sites, one per discipline (`DuplicateMechanicalObjectCommand.cs:57-59` and siblings). A deliberate composition: Duplicate is Copy with the source's own current parent. The consequence is that nothing the shared handler table does — logging, trust checks, any future cross-cutting concern — applies to the inner Copy, so this is a second dispatch path, however small. Audit finding `F-14`. | Architecture & Dead-Code Audit (2026-08-31) | Concrete evidence that the bypass causes a real problem (a cross-cutting concern the inner Copy must observe). Retained as a sanctioned exception by approved decision; `WP-H` pins the five current sites so any spread is visible rather than silent. **Pinned, `WP-H` (2026-09-01):** `DuplicateCopyDelegationTests` asserts set equality between the five documented files and the files that actually call a Copy handler's `HandleAsync` directly, so both proliferation and disappearance fail; Requirements' own non-delegating Duplicate is pinned alongside them so the five read as a bounded list rather than as 'every Duplicate'. Deliberately refactoring the pattern away still fails the test — which is the prompt to retire this row, not a wall. |
| AT-25 | `Tempest.App/AssemblyInfo.cs:17` carries `InternalsVisibleTo("Tempest.Desktop.Tests")` — a test-only concession compiled into a production assembly. Audit finding `F-19`. | Architecture & Dead-Code Audit (2026-08-31) | A real need to narrow the production assembly's surface. Retained as a sanctioned exception by approved decision. |
| AT-26 | **`IWorkspaceViewFactory.Create` is synchronous by contract, so its implementations bridge asynchronous reads with `.GetAwaiter().GetResult()`** — the `WP8.0B` contract, already disclosed in six of the factories' own doc comments. Seven executable call sites depend on it: `RequirementsWorkspaceViewFactory` (three), the Calculations/Documents/Manufacturing/Mechanical/Verification factories' own `Repository.FindAsync` (one each), and transitively `MainWindow`'s Cockpit `ContinueAsync`/`OpenRecentAsync`, which reach a factory through `NavigationService.OpenAsync`. Re-derived and assessed at `WP-E` (2026-09-01). | Architecture & Dead-Code Audit (2026-08-31); assessed `WP-E` (2026-09-01) | **Retained as a deliberate survivor by approved decision (`WP-E`).** Only the three Requirements sites block on real file I/O; the rest read the in-memory repository and complete synchronously, waiting on nothing. Making `Create` asynchronous is a `Tempest.Core` contract change cascading through `NavigationService.OpenAsync`, `DocumentAreaView` and every discipline's own factory — disproportionate to one bounded read, and explicitly out of `WP-E`'s scope. Revisit if `Create` ever needs to do work that is genuinely slow, or alongside `TD-118`. |

**Total: 25 disclosed trade-offs (2 Retired, 23 active), none of the
active twenty-three requiring action absent a real, demonstrated need.**
Updated Architecture & Dead-Code Audit (2026-08-31): `AT-24` and `AT-25` added as sanctioned exceptions
by approved decision.

## Cross-Reference Check

Every item above is traceable to a specific retrospective or ADR cited in
its own row. TD-08's resolution is cross-checked directly against
`Risk Register.md`'s R1/R4 (both Retired on the same date, by the same
Work Package) — consistent, no discrepancy. No debt item was found in any
retrospective that is missing from this consolidated list.
