# Technical Debt Register

## Register Metadata

| Field | Value |
|---|---|
| **Register Name** | Technical Debt Register |
| **Purpose** | The single, consolidated list of every disclosed debt item across the entire Claude-developed history of TempestOS — pulled from each Work Package retrospective's own "Architectural Debt Assessment" section and `WP 4.2D`'s own consolidated review, updated to current status as of this baseline. |
| **Scope** | Every debt item explicitly named in a Work Package retrospective's "Architectural Debt Assessment" section, `docs/releases/v0.4.0/Platform Services Architecture Review.md`'s "Remaining Technical Debt" section, or an ADR's own disclosed, accepted trade-off. |
| **Owner** | Project Maintainer. |
| **Source of Truth** | Every retrospective under `docs/academy/03 Work Packages/`; `docs/releases/v0.4.0/Platform Services Architecture Review.md`. |
| **Review Frequency** | Updated whenever a Work Package resolves, worsens, or discloses a new debt item — every retrospective's own "Architectural Debt Assessment" section is the trigger. |
| **Last Reviewed** | 2026-08-28 (`WP 14.0A`, TempestOS Companion — Mobile Companion Application) — two debt items added, `TD-57` (no Android/iOS platform heads; mobile workloads unobtainable in the build environment) and `TD-58` (Companion off-box reach bounded by `TD-13`/`TD-14`, deliberately not worked around client-side), 56 → 58 tracked (39 Open); one trade-off added, `AT-24` (no offline write queue, by design, `ADR-0115`), 23 → 24 disclosed. All counts re-derived by direct row count in the same change. Previously reviewed 2026-08-18 (`WP 13.11D`, `v0.13.0` Plugin Platform Exit Review — six fresh, read-only sub-agents: Security/Adversarial, Architecture/Runtime, API/Compatibility, Verification/Test and RAM-concurrency, Governance/Documentation, WP14 UI/UX Dependency; every finding independently re-confirmed against real source centrally before this register was touched). Scope was deliberately narrow: determine only whether anything genuinely prevents starting WP14 UI/UX. **All six disciplines returned no blocker, and the platform is declared READY FOR WP14.** Confirmed against real code: `TD-51` remains closed (all four elements of the `WP 13.11B` mechanism intact — the `out` seam, `RecordDenied` before every throw, the completed fixed-point scan, the four-exception `CreateDescriptor` guard); `TD-52` remains closed (hosted-service constructor conformance plus `HostedServiceManager`'s own `componentScopeProvider` hook); `TD-55` correctly classified non-blocking, with an escalation attempt made and failed; no public API, dependency, or ADR regression across the whole of `v0.13.0` (111 ADRs, zero dependency changes, nothing packaged for external consumption); multi-assembly/multi-module behaviour sound; and plugin-facing UI dependencies clearly identified as WP14's own greenfield design scope rather than prerequisites — no existing seam is wrong in a way WP14 would have to break, and every extension WP14 needs is additive. **One genuine new finding, added here as `TD-56` and deliberately not fixed** (this Work Package is read-only by its own instruction): a plugin's own constructor executes outside its component scope, so plugin code runs during construction with a `null` ambient principal, which every capability gate treats as first-party and skips — defeating `TD-52`'s own gate for a signed plugin, and, via `IEventBus.Subscribe`'s owner capture, persisting first-party dispatch for the life of the process. Graded **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support** exactly as `TD-51` was, and explicitly not blocking WP14 or `v0.13.0` as shipped: dormant, unreachable below `VerifiedSigned`, no crash, orthogonal to UI work. Minimum follow-up is defined in its own row and is a statement-ordering change within `ADR-0111`'s existing hook, not a new seam. Three documentation-accuracy defects also corrected: `WP 13.9.0` was genuinely complete yet had no row in `WorkPackages.md`'s delivered table (the only completed `v0.13.0` Work Package missing from it); that table's own preamble still claimed "two ... completed so far" and that `WP 13.0A` "has not yet begun"; and `Plugin Trust & Isolation Architecture.md`'s closing pointer named only `TD-53`/`TD-54`, omitting `TD-55` and now `TD-56`, the two that are actually plugin-trust-specific. A fourth reported defect was **rejected on inspection**: `WP 13.9.0`'s `Not started` cell in the roadmap-*predicted* table is correct and deliberately unchanged, that table being a frozen branch-cut snapshot that is explicitly never re-derived. 55 → 56 tracked items, 18 Resolved unchanged, 1 Partially resolved unchanged, 36 → 37 Open; no existing status changed, `TD-53`/`TD-54` untouched. No ADR added or amended and `ADR Register.md` correctly untouched, mirroring `WP 13.11A`–`WP 13.11C`'s own precedent; no Academy article, mirroring the unbroken precedent since `WP 13.3B`. Zero `src/`/`tests/` files touched. `TD-34` extended, not re-classified: this Work Package's own Release regression reproduced its intermittent failure live (2,561/2,562, then 2,562/2,562 clean on an immediate re-run of identical binaries), and its Verification reviewer named the untracked sibling hazard behind the same root cause — `Logging/ConsoleLogSinkTests.cs` redirects process-global `Console.Out` while sitting outside the collection that serialises the fifty-odd other classes doing the same. Neither blocks WP14; both are test-harness isolation, not product behaviour. Previously reviewed 2026-08-18 (`WP 13.11C`, TD-51 Remediation Review & Trust-Boundary Verification — four fresh, read-only sub-agents: Security/Adversarial, Architecture/Runtime, Verification/Test and RAM-concurrency, Governance/Documentation; all revert experiments and every edit performed centrally). Reviewed `WP 13.11B` against `TD-51`, `ADR-0110`/`ADR-0111`, and the real implementation. **The production fix is clean** — an unresolvable constructor-parameter type cannot crash the Host on any traced path; `RecordDenied` provably precedes every denial `EnforceTrust` surfaces; the scan terminates and its per-step `AppDomain` diff runs on every path; both `IModule` and `IHostedService` denied types are excluded from Discovery, Registration, and Lifecycle, including a type implementing both; the discovery backstop preserves `ModuleDiscoveryException`/`DuplicateModuleIdException`/`TargetInvocationException` propagation (the duplicate-ID throw is outside the guarded block entirely); legitimate multi-assembly plugins are byte-identical on the non-failing path; and no public API, ADR, dependency, or architecture-boundary regression exists. **Two genuine findings, both closed here.** First, and materially: `WP 13.11B`'s own completed-fixed-point-scan decision — the security-critical half of the fix, and the one its rationale argued hardest for — had **no regression coverage whatsoever**. Reverting it to `WP 13.11A`'s own recommended partial-list shape left all 2,561 tests green, so a future "simplification" back to the unsafe shape would have passed the entire suite. Closed by one new test plus one new builder helper, proven non-vacuous in both directions: it fails on its own load-bearing assertion under the partial-list revert and passes with the fix. Second: the "guard already used at three other call sites in this exact codebase" claim, inherited verbatim from `WP 13.11A` and softened rather than corrected by `WP 13.11B`, is factually wrong — exactly **one** other call site carries that four-exception filter (`DiscoverModuleTypes`' own); `LoadOne`'s neighbouring guard is a different three-exception filter and the rest are single-`ReflectionTypeLoadException` catches. Corrected in every document that repeated it and in the production comment that asserted it. `TD-55` extended to name the complementary **under**-recording half of the same attribution defect, which its original wording concealed and which is *not* fail-closed: the per-step `before` snapshot is taken after `Assembly.LoadFrom` returns, so an assembly already resident when a plugin is scanned is never attributed to it at all — adversarial review could not construct a privilege escalation from this, so it is recorded as attribution imprecision of the same root cause, closed by the same per-plugin denial scoping. `ADR-0013` reviewed and found not to require amendment (its "Discovery throws → `Faulted`" clause is narrowed by the backstop, but same-category and fail-closed, and `ADR-0025`'s own category 8 already ratifies this isolation; the residue — per-candidate rather than per-assembly, and covering first-party candidates — is disclosed in `Plugin Trust & Isolation Architecture.md` rather than amended into either ADR). One pre-existing Academy staleness corrected in place: `07-plugin-architecture.md`'s "with zero change to ... Module Discovery, Registration" claim has been false since `WP 13.9.6`, not `WP 13.11B`, and now carries a supersession marker in that article's own established strikethrough style. No new ADR; `ADR Register.md` correctly untouched, mirroring `WP 13.11A`/`WP 13.11B`'s own precedent for a Work Package amending no ADR. 55 tracked items unchanged, 18 Resolved, 1 Partially resolved, 36 Open — no status changed, `TD-53`/`TD-54` deliberately left untouched. Debug and Release both 0 Warnings / 0 Errors; full regression 2,562/2,562 both configurations (2,561 + 1 new). Previously reviewed 2026-08-18 (`WP 13.11B`, Trust-Denial Crash Remediation — four parallel sub-agents, the analysis three read-only: Security/Adversarial, Runtime/Lifecycle, Verification/Test, Governance/Documentation; all production and documentation edits integrated centrally, one owner per file) — closes the single Release-blocking finding `WP 13.11A` raised, by both of that review's own recommended minimum-fix options and no wider. (a) `PluginAssemblyLoader.DiscoverModuleTypes`'s own unresolvable-constructor-parameter-type failure no longer throws from inside the scan; it is surfaced to `EnforceTrust`, which records every discovered `IModule` and `IHostedService` type (`RecordDenied`, `IPluginDeniedTypeRecorder`, `WP 13.9.4`) before throwing — so `WP 13.9.6`'s own `isTypeExcluded: deniedTypeRegistry.IsDenied` Module Discovery filter genuinely excludes the offending type and `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own `type.GetConstructor(Type.EmptyTypes)` call is never reached for it. Deliberately implemented as a **complete** fixed-point scan rather than the partial type list `WP 13.11A` suggested: this Work Package's own Security/Adversarial analysis established that aborting mid-traversal would have left an already-resident, unscanned assembly's module types unrecorded yet fully visible to Module Discovery's plugin-unaware `AppDomain` scan, trading the Host crash for a silent First-Party-treated trust bypass — strictly worse, and not fail-closed. (b) `ReflectionFrameworkDiscoveryService.DiscoverModules` gained the same narrow four-exception reflection guard around `CreateDescriptor` already used at the one other call site in this codebase (`DiscoverModuleTypes`' own; corrected from "other call sites", `WP 13.11C`): excluded and logged, never a crash and never a silent inclusion, with `ModuleDiscoveryException` (including the `WP 5.3` guidance) still propagating unchanged. `TD-51` moved **Reopened → Resolved** accordingly; 54 → 55 tracked items, 17 → 18 Resolved, 1 Partially resolved unchanged, 36 Open unchanged in count but changed in membership (`TD-51` out, `TD-55` in). `TD-55` was added by this Work Package's own independent Adversarial Review, which confirmed the core fix clean on every axis it attacked but found one genuine consequence worth tracking: `RecordDenied`'s all-discovered-types shape combined with `PluginDeniedTypeRegistry`'s `Type`-only keying lets one plugin's denial suppress another assembly's module types, a hazard latent since `WP 13.9.4` on the two existing denial paths and newly reachable on this third one, since it previously recorded nothing at all. Disclosed rather than silently accepted; closing it requires a per-plugin denial-scoping mechanism, which is an architectural change beyond this Work Package's own explicitly minimal remit. `TD-53`/`TD-54` deliberately untouched — both low-severity, non-blocking, and neither naturally nor directly implicated by this fix. No ADR added or amended: `ADR-0111`'s own Decision text already mandates the behaviour (a) restores, verbatim, so the defect was an implementation divergence from an already-correct decision — independently re-confirmed by direct read of that Decision section before this register was updated. No public API change, no new dependency. Seven permanent regression tests added; non-vacuousness confirmed by reverting each half of the fix independently, observing the targeted tests genuinely fail, and — with both reverted — reproducing the exact `Expected: Running, Actual: Faulted` Host crash end-to-end, then restoring. Debug and Release both 0 Warnings / 0 Errors; full regression 2,561/2,561 both configurations (2,554 + 7 new). Committed together with `WP 13.11A`'s own documentation corrections, which stopped before commit by their own explicit instruction. Previously reviewed 2026-08-17 (`WP 13.11A`, Plugin Platform Final Hardening Architecture Review — six parallel, read-only sub-agents: Security/Adversarial, Runtime/Lifecycle Architecture, API/Compatibility, Verification/Test/RAM, Governance/Documentation, UI/UX Dependency) — one **Release-blocking finding** (for third-party plugin support, not `v0.13.0`'s own shipped state): `TD-51`'s own already-disclosed "unresolvable constructor parameter type" residual gap, previously characterised by `WP 13.10C` as safely isolated (no crash), re-examined by this review's Security/Adversarial sub-agent and found to genuinely crash the whole Host on the `IModule` axis — `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own unguarded `type.GetConstructor(Type.EmptyTypes)` call throws uncaught when an unattributed module type's constructor references an unresolvable parameter type, because `WP 13.9.6`'s own crash-prevention filter (`isTypeExcluded: deniedTypeRegistry.IsDenied`) never triggers for a type this specific denial path never records. Independently re-confirmed by direct source read (not taken on the sub-agent's own citation alone) before this register was updated. `TD-51` reopened accordingly; a minimal follow-up Work Package (`WP 13.11B`, recommended, not yet scheduled) is the correct next step, not implemented here per this review's own explicit "review only" scope. Two further, genuinely new, low-severity items found and added directly by the same sub-agent, neither plugin-trust-specific: `TD-53` (`HostedServiceManager`'s own construction-throw critical-service misclassification) and `TD-54` (`ITempestServiceProvider`'s own incidental, not explicit, DI-escape closure). Zero WP14-blocking findings from any of the six disciplines — the UI/UX Dependency sub-agent found two genuine plugin-facing UI/diagnostics gaps (no capability-gated Workspace view/panel extension point exists yet, only Navigation/Commands; no plugin-ownership/trust-tier/capability projection exists on any DI-public diagnostics surface yet), both assessed as WP14's own first-order design scope to build, not a prerequisite hardening gap blocking WP14 from starting. `TD-49`/`TD-50` reconfirmed unchanged. One documentation-accuracy correction made directly by this review, unrelated to the plugin trust boundary: `Future Capability Register.md`'s `FCR-0001`/`FCR-0002` entries had drifted stale since `WP 13.0A`, still citing `WP 13.0B` as "implementation pending" when `WP 13.0B` was in fact repurposed to an independent architecture review and the real implementation shipped in `WP 13.2A` — corrected to cite `WP 13.2A` and `Status: Implemented`, matching this same register's own already-correct `WP 13.3A` narrative. 52 → 54 tracked items; 18 → 17 Resolved (`TD-51` reopened), 1 Partially resolved unchanged, 33 → 36 Open (`TD-51` reopened plus `TD-53`/`TD-54` newly added). Not yet committed at time of writing — this Work Package stops before commit per its own explicit instruction. Previously reviewed 2026-08-17 (`WP 13.10C`, Plugin Trust Hardening Review & Integration — the same integration-and-commit role `WP 13.9.7` established for the `WP 13.9.1`–`WP 13.9.6` chain, applied here to the `WP 13.10A`/`WP 13.10B` chain; four fresh, read-only sub-agents — Security/Adversarial, Architecture/API, Verification/Test + RAM/Concurrency, Governance/Documentation) — `TD-51`/`TD-52` both independently re-confirmed **Resolved** by direct re-read of the real, current source (`PluginAssemblyLoader.EnforceTrust`/`DiscoverModuleTypes`, `BackgroundServices.HostedServiceManager`, `Identity.IdentityService.EstablishCurrentPrincipal`), not taken on either prior Work Package's own citation — each finding's own targeted regression test independently reverted and confirmed to genuinely fail, then restored. One permanent-coverage gap found and closed directly by this review: the twice-found, twice-fixed Host-crash regression (an unresolvable constructor-parameter type on an `IHostedService`-only plugin faulting the whole Host) had only ever been proven fixed via throwaway proof-of-concept code across two independent `WP 13.10B` reviewers — a new permanent test, `LoadPlugins_FirstHostedServiceOnlyPluginHasUnresolvableConstructorParameterType_IsolatesFailure_SecondLegitimatePluginStillLoads`, closes it, non-vacuousness independently confirmed (reverted, observed to fail, restored). One small, low-severity, pre-existing gap disclosed but deliberately not fixed here (a redundant, safely-isolated second construction attempt for the "unresolvable constructor parameter" denial path, since `RecordDenied` is never reached on that specific throw site) — see `TD-51`'s own updated Status cell. One governance-only note disclosed, not a code defect (`plugin.identity.establish`'s own broad blast radius at any tier above `UnsignedLocal`) — see `TD-52`'s own updated Status cell. `ADR-0111` gained a "Corrected, `WP 13.10B`" note; `Plugin Trust & Isolation Architecture.md`'s own "Found, `WP 13.10A`" Risks entry corrected to "Closed, `WP 13.10B`"; `ADR Register.md`'s own `ADR-0111` citation extended to a seventh phase; `WorkPackages.md`/`PROJECT_STATUS.md` gained `WP 13.10A`/`WP 13.10B`/`WP 13.10C` entries. 52 tracked items unchanged; 16 → 18 Resolved, 35 → 33 Open (the two `WP 13.10A` additions moved to Resolved), 1 Partially resolved unchanged. Committed as a single commit alongside the full `WP 13.10B` implementation. Previously reviewed 2026-08-17 (`WP 13.10A`, Plugin Platform Hardening Architecture — a read-only architecture/security review of the complete v0.13.0 plugin platform, `WP13.0A`–`WP13.9.7`, six parallel disciplines: Plugin Runtime Architecture, Security/Trust, Lifecycle/Composition, API/Compatibility, Verification/Testability, Governance/Documentation) — **two new items added, `TD-51`/`TD-52`**, both live-PoC-confirmed against the current, committed code, both a genuinely distinct finding from anything the `WP 13.9.1`–`WP 13.9.6` remediation chain closed: `TD-51` (the static `IModule` constructor-conformance/denylist check was never extended to `IHostedService`, and `HostedServiceManager` has no ambient-identity scope hook at all) and `TD-52` (`IIdentityService.EstablishCurrentPrincipal`, an ordinary DI-public interface, reaches the exact ambient-identity write surface the denylist exists to keep out of plugin hands). Both classified **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support** — not for `v0.13.0`'s own already-shipped, already-signed first-party modules, which this finding does not affect, but for the plugin trust platform's own stated purpose. A dedicated remediation Work Package is recommended before third-party plugin support is considered production-ready; neither item blocks WP14 UI/UX work itself, which does not touch the plugin trust boundary. 50 → 52 tracked items; zero items resolved or worsened beyond the two additions. Full detail: this Work Package's own report (delivered directly in-session, no standalone document) and `Plugin Trust &amp; Isolation Architecture.md`'s own "Found, `WP 13.10A`" Risks entry. Previously reviewed 2026-08-14 (`WP 13.3A`, Plugin Platform Integration & End-to-End Validation) — the plugin platform's own closing integration/validation pass for `v0.13.0`. `TD-09`/`TD-10`/`TD-11` independently re-confirmed **Resolved** by direct re-reading of the real, current code (`PluginAssemblyLoader.EnforceTrust`, `NavigationService.Unregister`'s ownership check, trust-tier-ordered eviction across `NavigationService`/`CommandRegistry`/`CommandHandlerTable`), not taken on `WP 13.2A`'s own citation. **Two new items added, `TD-49`/`TD-50`** — both sourced from `WP 13.2B`'s own commit message, which disclosed each as a genuine, plausible finding it deliberately did not fix ("genuinely out of scope for a defect-fixing pass"), and neither of which had, until now, ever reached a formal register entry: `TD-49` (a TOCTOU window between Discovery-time signature verification and Loading-time `Assembly.LoadFrom`) and `TD-50` (filename-based, not certificate-attribute-based, first-party trust-store detection). Both confirmed still present by direct source re-read (`PluginAssemblyLoader.LoadOne`; `PluginTrustStore`'s own `FirstPartyCertificateFileName` constant and its class-level remarks, which already self-disclose the filename convention). Neither is an `ADR-0112` conformance gap — see `ADR Register.md`'s own updated `Last Reviewed` entry for the clause-by-clause check. 48 → 50 tracked items; zero items worsened, zero resolved by this pass beyond the two re-confirmations above. Full detail: `docs/academy/03 Work Packages/WP13.3A-plugin-platform-integration-and-end-to-end-validation.md`. Previously reviewed 2026-08-13 (`WP 13.2A`, Plugin Trust & Capability Enforcement Implementation) — `TD-09`, `TD-10`, and `TD-11` all moved **Open → Resolved**: this Work Package implements `ADR-0110`–`ADR-0112` exactly as `WP 13.0A` designed and `WP 13.0B` independently audited them — real, tested, working capability enforcement (`PluginAssemblyLoader.EnforceTrust`, closing `TD-09`'s DI-container-trust gap), a real `NavigationService.Unregister` ownership check (closing `TD-10`), and real trust-tier-ordered registration replacing unconditional "first registration wins" across `NavigationService`/`CommandHandlerTable`/`CommandRegistry` (closing `TD-11`). 48 tracked items unchanged in count; zero new items added, zero worsened; 13 → 16 Resolved, 34 → 31 Open (1 Partially resolved item, `TD-05`, unaffected). Full detail: `docs/academy/03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md`. Previously reviewed 2026-08-13 (`WP 13.1A`, Plugin Runtime & Composition Root Implementation) — `TD-06` moved **Open → Resolved**: `Runtime:Plugins:RootDirectory`/`ManifestFileName`/`Disabled` are now real, tested configuration keys, closing the gap `WP 13.0A` had only designed. `TD-09`/`TD-10`/`TD-11` **confirmed unchanged, deliberately** — this Work Package implements only the mechanical, non-trust half of `WP 13.0A`'s design (`ADR-0107`–`ADR-0109`); the trust retrofit those three items still need (`ADR-0110`–`ADR-0112`) remains a genuinely separate, still-unassigned future Work Package's task, not touched here. 48 tracked items unchanged in count; zero new items added, zero worsened; 12 → 13 Resolved, 35 → 34 Open. Zero `docs/` files touched by any of the three production/testing workstreams — only this Work Package's own Governance & Documentation pass, applied after implementation and independent verification both closed (Debug: 2,305/2,305 passing, 0 Warnings/0 Errors; Release: run in progress at time of writing, expected to match). Full detail: `docs/academy/03 Work Packages/WP13.1A-plugin-runtime-and-composition-root-implementation.md`. Previously reviewed 2026-08-13 (`WP 13.0A`, Plugin & Registration Trust Isolation Architecture) — `TD-06`, `TD-09`, `TD-10`, `TD-11` all updated in place, none resolved: each moves from "mechanism exists, not yet applied" to "design resolved, retrofit remains open until implemented" (`TD-09`/`TD-10`/`TD-11`) or "architecture designed, not yet implemented" (`TD-06`), citing the six new ADRs (`ADR-0107`–`ADR-0112`) this Work Package produced. No item moved to Resolved — this Work Package is architecture only; the actual code retrofit is `WP 13.0B`'s own, separately-scoped task. 48 tracked items unchanged in count; zero new items added, zero worsened. Zero `src/`/`tests/` files touched. Full detail: `docs/academy/03 Work Packages/WP13.0A-plugin-and-registration-trust-isolation-architecture.md`. Previously reviewed 2026-08-11 (WP 11.4A, Release Engineering Corrections) — three new items added and immediately resolved (`TD-46`–`TD-48`, the specific defects found on `v0.11.0`'s first real CI execution, per `WP11.9.0`'s own Release Publication Report); `TD-44` moved Open → Resolved in the same pass, its own precondition (CI genuinely executing on real infrastructure) now independently confirmed on two real runs (branch push, tag push). 45 → 48 total; 6 → 10 Resolved; 38 → 37 Open. No production, Domain, or architecture change; three test/tooling/CI-configuration files touched, each narrowly, per `WP11.4A Release Engineering Corrections.md`. Previously reviewed 2026-08-11 (WP 11.9.0, `v0.11.0` Release Preparation & Engineering Sign-Off) — full re-verification as part of this release's own closing sign-off, not merely a currency refresh: an independent QA re-verification of `v0.11.0`'s own two new release-tooling scripts, run against real fixtures (not taken on either script's own prior claim), found two genuine, previously-undisclosed defects — `scripts/new-release.ps1`'s `git tag`/`git push` steps silently swallow a failure and still report `RELEASE SUCCESSFUL` (`TD-42`); `scripts/governance-healthcheck.ps1`'s generic exception handler loses the failing check's own identity on an empty-register or link-less-index input (`TD-43`). Two further items already disclosed in prose across `WP 11.1A`/`WP 11.1B`/`WP 11.2A`'s own reports, but never before consolidated into a formal register entry, are now registered directly: CI has never executed on real GitHub-hosted infrastructure (`TD-44`); branch protection is documented, not configured (`TD-45`). This register itself had drifted stale relative to `v0.11.0`'s own prior seven Work Packages before this review — `WP 10.8A` was its last genuine review, none of `v0.11.0`'s own findings had been entered — corrected here directly rather than deferred again. 41 → 45 total; Open count 34 → 38. `v0.11.0`'s own eighth and closing Work Package. Previously reviewed 2026-08-11 (WP 10.8A, Desktop Feature Completion & Existing Capability Exposure) — reviewed, zero new items added: this Work Package's own `PropertyInspectorView` real-Validation section (reusing `ObjectEditorView.BuildSeverityRow`, an already-established resolution to reuse rather than duplicate) is a second, previously-undisclosed consumer of `TD-41`'s own gap — a Requirement's own Validation now correctly reports "Real validation is not available for this object here" rather than a fabricated result, the identical honest degradation `TD-41`'s original entry already named for the Object Editor, now confirmed to extend to this second surface too; `TD-41`'s own Consequence text updated to name both surfaces. A second, genuine pre-existing defect was found and fixed in place, never reaching Technical Debt status: `RibbonView`'s own final fallback message (reached by every genuinely-unwired verb, including Copy) claimed a command was reachable via "the Command Palette (Ctrl+K)" or "Project Explorer's own context menu" — confirmed false for every command still reaching that branch, since `CreateDefault` is `null` on every real `CommandDescriptor` this platform has ever registered (`26-ribbon-and-command-experience.md` §11, a `WP 10.3B` finding this Work Package's own audit re-confirmed still true), so the Command Palette cannot invoke any real discipline command by Id at all, and the Project Explorer's own context menu offers only Open/Rename/Delete/Favourite. Corrected to name no false alternative — the identical "found and fixed, not merely disclosed" discipline this project has followed since `WP 10.2A`. No genuine new implementation defect found beyond this. 41 tracked items unchanged; Open count 34 unchanged. `v0.10.0`'s own fifteenth Work Package — zero Command Framework/frozen Workspace contract changes; `EngineeringDomainContext` threaded into `PropertyInspectorView` as a third optional constructor parameter, mirroring exactly how `ObjectEditorView` gained `ICommandDispatcher` the Work Package prior. Combined test suite 2266/2266 passing, Debug and Release both clean, 0 Warnings/0 Errors. See `WP10.8A Implementation Report.md`. Previously reviewed 2026-08-10 (WP 10.7A, Feature Completion) — reviewed, one new item added: `TD-41` (`ObjectEditorView.TryCreate` never resolves a real Requirement via `EngineeringDomainContext.Repository`, a genuine, pre-existing, previously-undetected gap since `WP 10.3A`, found while adding this Work Package's own new Requirements Owner/Priority Object Editor section — Open, not caused by this Work Package, not fixable within its own "no new architecture" scope). A second, genuine implementation-time bug was found and fixed in place before any commit, never reaching Technical Debt status: every one of this Work Package's own five new Object Editor sections had its own success/failure status message immediately overwritten by the very `Refresh()` call that reported it, since `Refresh()` re-runs `PopulateFrom`, which unconditionally clears each section's own status text — reordered (`Refresh()` first, status message set after) in all five, the identical "found and fixed, not merely disclosed" discipline this project has followed since `WP 10.2A`. This Work Package closes the WP10.6D-audited placeholder gap directly: real Ribbon status-transition/Create/Duplicate dispatch for the five disciplines beyond Mechanical (`FCR-0075`), real drag-and-drop reparenting (`FCR-0066`), and all five discipline-specific Object Editor sections (`FCR-0068`) — see `WP10.7A Implementation Report.md`. 40 → 41 tracked items; Open count 33 → 34. `v0.10.0`'s own fourteenth Work Package — zero Command Framework/frozen Workspace contract changes (confirmed directly): every new capability dispatches an already-existing, already-registered command through `ICommandDispatcher` exactly as `MainWindow`'s own pre-existing Mechanical Create/Duplicate handlers already did, or raises a new plain event mirroring `ProjectExplorerView`'s own existing `ObjectOpened`/`ToggleFavouriteRequested` shape. Combined test suite 2263/2263 passing (2064 `Tempest.Core.Tests` + 199 `Tempest.Desktop.Tests`), Debug and Release both clean, 0 Warnings/0 Errors. Previously reviewed 2026-08-10 (WP 10.5C, Commercial User Experience & Application Completion) — reviewed, zero new items added: this Work Package's own required-first runtime audit found two genuine, real `TD-39`-class defects (`CockpitCardControl`'s own neutral border, `RibbonView.BuildSectionWithLabel`'s own group divider — both a fixed `Brushes.Gray`, never theme-reactive) — both fixed at their own true source (`ThemeReactiveBrush.Bind`/`ApplicationPalette.PanelBorderBrushKey`, the identical, already-proven fix) before commit, the fourth and fifth real instance of `TD-39`'s own risk class this project has now found and closed. 40 tracked items unchanged; Open count unchanged. `v0.10.0`'s own thirteenth Work Package by completion order — zero `src/Tempest.Core/` files touched; `ProjectExplorerNode`/`CockpitKpiCard` (`Tempest.App.Workspace`) both gained only additive, defaulted trailing parameters. See `WP10.5C Technical Debt Review.md`. Previously reviewed 2026-08-10 (WP 10.6A, Command Execution & Productivity Experience) — zero new `TD-##` items added (no genuine pre-existing defect found; every new capability this Work Package built is genuinely new code, not a fix against already-shipped behaviour); six new Accepted Trade-offs added instead (`AT-18`–`AT-23`): Undo/Redo coverage limited to Rename and Favourite toggle; Background Task Runner reports coarse state only, never a percentage; a Macro step must be `CreateDefault`-eligible (today, only `Tempest.Samples` commands qualify); Command History records only what reaches existing UI `ActionCompleted` surfaces, with a disclosed "contains 'fail'" heuristic for `succeeded`; Undo/Redo and Command History are both session-only; `KeyboardCommandBindingProvider` ships with zero default bindings and no remapping UI, and no real external controller vendor integration exists. 40 tracked `TD` items unchanged; Open count 31 unchanged; 17 → 23 Accepted Trade-offs. `v0.10.0`'s own twelfth Work Package — zero Engineering Domain/Runtime files touched beyond `TempestHost.cs`'s own two new `services.Singleton` lines; every frozen `WP8.0B` Workspace contract and both public Command Framework contracts (`ICommandDispatcher`/`ICommandRegistry`) unmodified. Combined test suite 2238/2238 passing (2062 `Tempest.Core.Tests` + 176 `Tempest.Desktop.Tests`), Debug and Release both clean, 0 Warnings/0 Errors. See `WP10.6A Technical Debt Review.md`. Previously reviewed 2026-08-10 (WP 10.5B, Desktop Workflow & Professional Interaction) — reviewed, zero new items added: two genuine implementation-time findings — a real, pre-existing (`WP 10.2A`) `ProjectExplorerView` filter reliability gap (`TextChanged` not firing for programmatic `.Text =`, the identical class `ObjectEditorView`/`WP 10.3A` already fixed) and a `TD-27`-class test-precision gap in this Work Package's own new Delete-Confirmation tests (a soft-delete assertion error, a non-deterministic "first node found" helper) — both fixed at their true source before commit, neither reaching Technical Debt status, mirroring `WP 10.2A`/`WP 10.3B`'s own "found and fixed, not merely disclosed" precedent. Five new Future Capability entries recorded instead (`FCR-0073`–`FCR-0077`), all disclosed, deliberate scope reductions, none a defect. 40 tracked items unchanged; Open count 31 unchanged. `v0.10.0`'s own eleventh Work Package — zero `src/Tempest.Core/`/`src/Tempest.App/Workspace/` files touched. See `WP10.5B Technical Debt Review.md`. Previously reviewed 2026-08-10 (WP 10.5A, Workspace Visual Polish & Engineering User Experience) — `TD-39` and `TD-40` both moved Open → Resolved, each fixed at exactly the mechanism its own "Revisit trigger" column named (a real `DynamicResource`-bound overlay background; a real "Discard unsaved changes?" confirmation before a dirty tab closes), both proven by dedicated tests, re-run five consecutive times, zero failures. Zero new items added. One genuine, real test-precision defect found in `WP 10.4A`'s own `DigitalThreadGraphModelTests` (an over-broad assertion, intermittently failing depending on `InMemoryEngineeringObjectRepository`'s own unspecified iteration order, `TD-27`'s identical class of risk) — fixed in place, not registered as a new item, since the underlying production behaviour was always correct; `WP10.4A`'s own already-published documents deliberately left untouched, this finding disclosed in `WP10.5A Technical Debt Review.md` §3 instead. 40 tracked items unchanged; Open count 33 → 31. `v0.10.0`'s own tenth Work Package — zero `src/Tempest.Core/`/`src/Tempest.App/Workspace/` files touched. Previously reviewed 2026-08-09 (WP 10.4A, Digital Thread Visualisation) — reviewed, zero new items added: one existing item, `TD-32`, revisited and re-disclosed exactly as `WP10.0A Digital Thread & Relationship Visualisation.md` §4 explicitly called for once a graph view was actually built — disposition unchanged (still Open, no data-correctness issue), but a second, previously-undisclosed consumer of the same underlying gap was found in the process: `ObjectEditorView`'s own Relationship summary (`WP 10.3A`) silently misses the same `"verifiedBy"` link `DigitalThreadGraphModel` now explicitly merges in via `VerificationRecordReader`, made visible for the first time as a real graph node (`WP10.4A Engineering Review.md` §3). 40 tracked items unchanged (33 Open). `v0.10.0`'s own ninth Work Package — zero `src/Tempest.Core/`/`src/Tempest.App/Workspace/` files touched; the new `DigitalThreadGraphModel`/`DigitalThreadGraphView` pair lives entirely in `Tempest.Desktop`, reading exclusively through already-existing, already-permitted reads (`EngineeringDomainContext.Repository`/`RelationshipRepository`, `VerificationRecordReader`) — zero new traversal mechanism, `ADR-0093` honoured directly. Previously reviewed 2026-08-09 (WP 10.3B, Ribbon, Toolbar & Command Experience) — reviewed, zero new items added: this Work Package found two genuine defects (`CommandPaletteOverlay`'s own pre-existing silent no-op on Enter for any command with no `CreateDefault`; `RibbonView.Rebuild()`'s own missed initial `RefreshEnablement()` call) — both fixed in place, before any commit, proven by dedicated tests, never reaching a state warranting a Technical Debt entry (`WP10.3B Engineering Review.md` §2–3), the identical "found and fixed, not merely disclosed" discipline `WP 10.2A`'s own Property Inspector fix established. 40 tracked items unchanged (33 Open). `v0.10.0`'s own eighth Work Package — zero `src/Tempest.Core/`/`src/Tempest.App/Workspace/` files touched. Previously reviewed 2026-08-09 (WP 10.3A, Engineering Object Editors) — one new item added: `TD-40` (`ObjectEditorView`'s own new, real, buffered dirty-state is not wired into `DocumentAreaView`'s own tab-close path — closing a dirty editor tab silently discards unsaved edits, no confirmation prompt, a disclosed, deliberate scope decision). 39 → 40 total (33 Open). Zero pre-existing items worsened; every one of the thirty-nine reviewed and confirmed unaffected by this Work Package's own diff. `v0.10.0`'s own seventh Work Package. Previously reviewed 2026-08-09 (WP 10.2B, Docking & Workspace Layouts) — one new item added: `TD-39` (`PanelHostControl`'s own fixed-brush overlay background, not theme-variant-aware — required once a panel can be reparented as an Auto-Hide flyout, mirroring `CommandPaletteOverlay`'s own identical, previously-unregistered limitation, both surfaced together for the first time). 38 → 39 total (32 Open). Zero Workspace contract changes of any kind (`IWorkspaceLayout`/`WorkspacePanelPlacement`/`IWorkspacePanel` all unmodified — Bottom dock, Collapse, Auto-Hide, predefined/saved/reset layouts, and the new Output panel are all realised additively, entirely in `Tempest.Desktop`/a new `OutputPanel : IWorkspacePanel` implementer, plus a second, sibling `DesktopPanelUiState` Settings key alongside `WorkspaceState`'s own — never a change to any of the twelve frozen `WP8.0B` contracts). Zero Engineering Domain/Runtime files touched. `v0.10.0`'s own sixth Work Package. Previously reviewed 2026-08-07 (WP 10.2A, Workspace Modernisation) — reviewed, zero new items added: one genuine, low-severity implementation defect (Property Inspector Lifecycle-section facet duplication) was found by this Work Package's own required Engineering Review and fixed in place, before any commit, proven by a dedicated regression test — never reaching a state warranting a Technical Debt entry (`WP10.2A Engineering Review.md` §3). Two disciplines' own honestly-incomplete Rename/Delete coverage (Requirements' three Kinds register Delete only; Calculations' synthetic `"CalculationTemplate"` Kind registers neither) are permanent, correct, disclosed-by-design absences, not defects with a future fix trigger — documented directly in `ADR-0096` and the Implementation Report, not registered here. 38 tracked items unchanged. Previously reviewed 2026-08-07 (WP 10.1B, Runtime Host & Module Discovery Hardening) — `TD-26` and `TD-37` both fully investigated and Resolved, not merely re-mitigated: `TD-26` fixed at its own source (`WorkspaceManager.StartAsync` now waits for `IDiagnosticsProvider.HostState == HostState.Running`, replacing the two prior Work Packages' own `Tempest.Desktop`-side mitigations, now removed as redundant); `TD-37`'s true root cause determined via direct file-system evidence — a durable, cross-launch `IPersistenceStore`-backed uniqueness index (`ADR-0041`) colliding with fixed, literal sample-module identifiers on a second real launch, never a double-invocation as its own prior entry had speculated — fixed via idempotent seeding in all four affected modules plus genuine per-test persistence isolation in `Tempest.Desktop.Tests`. `EngineeringCockpitTests`' own three previously honest-empty assertions now read real, stable, live data. One new item added and disclosed, not fixed: `TD-38` (`EngineeringObjectFactory<T>` enforces no business-identifier uniqueness of its own — a genuine Engineering Domain change, explicitly out of this Work Package's own scope). 37 → 38 total (two Open items, `TD-26`/`TD-37`, moved to Resolved; one new Open item, `TD-38`, added): 6 Resolved, 1 Partially resolved, 31 Open. `v0.10.0`'s own fourth Work Package, and its first pure hardening/root-cause pass — zero Engineering Domain files touched, confirmed directly; combined test suite 2051/2051 passing (2029 `Tempest.Core.Tests` + 22 `Tempest.Desktop.Tests`), Debug and Release both clean. Previously reviewed 2026-08-07 (WP 10.1A, Engineering Cockpit Implementation) — TD-37 added, Open: four `Tempest.Samples` modules (`engineeringdomain`, `materials`, `requirements`, `requirementsworkspace`) each fail their own `InitialiseAsync` against their own literal Id, confirmed reproducible, confirmed not caused by this Work Package's own code, root cause not fully diagnosed — found via `IDiagnosticsProvider.Modules` while diagnosing an incomplete `WP 10.0B`-era `TD-26` mitigation. TD-26 reconfirmed Open a third time (`WP 9.0A`, `WP 10.0B`, `WP 10.1A`); its own `Tempest.Desktop` mitigation strengthened from a narrow six-item `NavigationItem` poll to the Runtime Host's own authoritative `IDiagnosticsProvider.HostState == HostState.Running` signal. 36 → 37 total. `v0.10.0`'s own third Work Package, and its second real implementation this release — zero Engineering Domain files touched, confirmed directly. Previously reviewed 2026-08-07 (WP 10.0B, Desktop Application Framework) — TD-35 added and immediately marked Resolved (`ProjectExplorer`/`PropertyInspector`'s own non-deterministic panel Ids broke cross-restart session persistence — a genuine, pre-existing `v0.9.0` defect, found by this Work Package's own real, two-independent-process test before any commit, fixed in the same Work Package); TD-36 added, Open (the Settings persistence store's own working-directory-relative default root path — a new consideration surfaced by a graphical, non-terminal launch path). TD-26 reconfirmed Open, its own "Revisit trigger" now directly, empirically met for the first time — `Tempest.Desktop`'s own headless tests hit its real consequences directly, mitigated one layer up in `Tempest.Desktop` itself, not fixed at the `WorkspaceManager` source; priority reconsideration recommended. 34 → 36 total (34 → 35 → 36, one addition immediately Resolved). This is `v0.10.0`'s own second Work Package, and the first to write real implementation code this release — zero Engineering Domain/discipline files touched, confirmed directly; two small, disclosed, non-contract changes made in `Tempest.App.Workspace` (§TD-35, plus an `InternalsVisibleTo` grant). Previously reviewed 2026-08-07 (WP 9.9.0, Release Preparation & Product Baseline — Second Pass) — TD-34 added: `CompositeLogSinkTests`'s own previously-informally-disclosed, non-reproducible `Console.Out`-capture flake (first named `WP 6.3`, referenced by every subsequent release-closing review's own flake-check, never formally registered) — actually observed for the first time during this Work Package's own fresh test-suite verification (1 failure across 5 full-suite runs, 0 failures across 5 further isolated runs of the same test), formally registered now that real evidence exists. 33 → 34 total. Previously reviewed 2026-08-07 (WP 9.9.0, Release Preparation & Product Baseline — First Pass) — reviewed, zero new items added: verification-only Work Package. All 33 tracked items and 17 disclosed trade-offs re-verified directly against this register's own Entries tables, both counts matching exactly — zero arithmetic drift found this time (unlike `WP 9.3A`'s own disclosed correction). Eight items added across `v0.9.0` (`TD-26`–`TD-33`, one per Work Package), all reconfirmed still Open, zero Release Blocking, zero disposition worsened. See `WP9.9.0 Release Readiness Report.md` §15 (Technical Debt Review). Previously reviewed 2026-08-07 (WP 9.5A, Manufacturing Workspace) — TD-33 added (`EngineeringCockpit.FormatCoverage`'s own zero-denominator text is hardcoded Requirements-specific, already inaccurately reused by two prior Work Packages' own coverage cards — found by direct inspection while designing `ManufacturingKpiCards`, worked around locally, not fixed at the shared source). Seventh consecutive dedicated Security Review (`WP9.5A Security Review Report.md`); zero Domain-layer files touched; one genuine finding (`TD-33`), caught by design-time inspection, not a failing test this time, and not a bug in already-shipped code. Previously reviewed 2026-08-07 (WP 9.3A, Verification Management Workspace) — TD-32 added (`VerificationService.RecordAsync`'s own subject→record link is never visible via `EngineeringDomainContext.RelationshipRepository`, only via the raw document store — found by nine failing tests, fully worked around at the read side, no functional gap remains). Sixth consecutive dedicated Security Review (`WP9.3A Security Review Report.md`); zero Domain-layer/Verification-Framework files touched; one genuine implementation-time finding (`TD-32`), caught by tests before any commit, not a bug in already-shipped code. Previously reviewed 2026-08-06 (WP 9.4A, Engineering Documents Workspace) — TD-31 added (`Attachment`/`IAttachment` carry descriptive metadata only — no file/URL storage capability exists anywhere in this platform, disclosed, not fixed); TD-30 confirmed still open and its consequences extended to a second discipline (no live Verification object exists for Documents↔Verification Digital Thread traceability, a direct consequence of the disclosed `WP 9.3A` numbering gap). Fifth consecutive dedicated Security Review (`WP9.4A Security Review Report.md`); zero Domain-layer files touched, zero genuine implementation defects found. Previously reviewed 2026-08-05 (WP 9.2A, Engineering Calculations Workspace) — TD-29 added (`CalculationRecord`/`CalculationRecordDto` never retain the input that produced them — `RecalculateCalculationCommand` cannot resume from stored input, disclosed, not fixed); TD-30 added (`ICalculationResult`/`IVerificationResult`/`IApprovalGate` family — declared `WP 8.2B` Domain contracts with zero concrete implementations anywhere in the platform, a pre-existing gap now formally registered for the first time; `Evidence`/`Approval` both worked around via direct relationship/status reads, not fixed). Fourth consecutive dedicated Security Review (`WP9.2A Security Review Report.md`); zero Domain-layer files touched, zero genuine implementation defects found — this Work Package's own disclosed items are both pre-existing absences, not bugs in already-real code. Previously reviewed 2026-08-05 (WP 9.1A, Requirements Management Workspace) — TD-28 added (Bulk Requirements commands — `BulkSetRequirementStatusCommand`/Owner/Priority — are deliberately plain `ICommand`, so no open view for any touched requirement is automatically refreshed after a bulk operation succeeds; disclosed, not fixed). Two further, genuine implementation defects (a permission-gated `GetEvidenceAsync` read reachable from three passive surfaces — `RequirementValidationService`, `RequirementsPropertyFacetProvider`, `EngineeringCockpit`; a `RequirementGroupDto` parent-resolution ambiguity once `MoveGroupAsync` made a second `groupedUnder` link possible) were found and fully fixed in place, not registered as debt — neither had ever been part of a commit or tagged release. Previously reviewed 2026-08-05 (WP 9.0B, Product Configuration & BOM Management) — TD-27 added (`InMemoryEngineeringObjectRepository` iteration order is unspecified; a test incorrectly assumed insertion order, found via an intermittent full-suite failure, fixed at the test/documentation level). Second consecutive dedicated Security Review (`WP9.0B Security Review Report.md`). Two further, genuine implementation defects (a `TEMPEST-VAL` validation-code collision `WP 9.0A` itself introduced; `ReviseAsync` silently discarding structural/BOM state) were found and fully fixed in place, not registered as debt — neither had ever been part of a commit or tagged release. Previously reviewed 2026-08-05 (WP 9.0A, Mechanical Product Structure) — TD-26 added (`WorkspaceManager.StartAsync` does not await Runtime Host module initialisation before returning — confirmed pre-existing on the unmodified `v0.8.0` tag, not introduced by this Work Package). First dedicated Security Review since `v0.7.0` (`WP9.0A Security Review Report.md`), closing `WP8.9.0`'s own disclosed "zero dedicated Security Reviews this release" finding; sourced no new debt item of its own beyond `TD-26` (found during Workspace integration verification, not the Security Review itself). Previously reviewed 2026-07-30 (WP 7.3A, Requirements Engine) — TD-25 added (`IRequirementsService.ReviseAsync`/`SetStatusAsync` carry no concurrency-conflict detection). Third Work Package to perform a dedicated Security Review (`WP7.3A Security Review Report.md`), sourcing this new debt item directly from it. Previously reviewed 2026-07-30 (WP 7.1E, Verification Framework) — TD-23 added (`RecordAsync`'s own multi-link sequence is not transactional); TD-24 added (`VerificationContext` imposes no bound on recorded data volume; `GetVerificationHistoryAsync` scales with total reference count); AT-17 added (no dependency on Materials for material-reference validation). Second Work Package to perform a dedicated Security Review (`WP7.1E Security Review Report.md`), sourcing both new debt items directly from it. This is the fifth and final Engineering Foundation framework Work Package. Previously reviewed 2026-07-30 (WP 7.1D, Engineering Calculation Framework) — TD-21 added (no cancellation reaches into `Calculate` once execution has started); TD-22 added (`CalculationContext` imposes no bound on recorded data volume or type fidelity); AT-16 added (no dependency on Materials for material-reference validation — open, unvalidated string references only). First Work Package to also perform a dedicated Security Review (`WP7.1D Security Review Report.md`), sourcing both new debt items directly from it. Previously reviewed 2026-07-30 (WP 7.1C, Materials Framework) — TD-20 added (`MaterialCatalog` reads a material's own full revision history to reconstruct current state — no dedicated "latest revision only" lookup exists on `IEngineeringDocumentStore`); AT-15 added (no permission-gating inside `IMaterialCatalog` itself — calling-layer enforcement expected, mirroring Reporting/Navigation precedent). Previously reviewed 2026-07-30 (WP 7.1B, Units & Quantities Framework) — TD-19 added (no affine/offset unit conversion support — Temperature deliberately deferred); AT-14 added (compile-time dimension-safety guarantee verified by direct inspection, not an automated compiler-error test). Previously reviewed 2026-07-30 (WP 7.1A, Engineering Data Model) — TD-17 added (`Content` remains string-only, no structured/typed payload); TD-18 added (no native concurrent-reference-write isolation test at scale for `LinkAsync`); TD-12 confirmed not inherited for this framework's own revision-history/reference-lookup access pattern (see `ADR-0053`). Previously reviewed 2026-07-29 (WP 6.6, Licensing) — TD-16 added (no cryptographic license file signature verification); AT-13 added (no remote validation/activation, floating/seat-based licensing, or renewal/grace-period model, all explicit approved-contract scope decisions); no existing Technical Debt item required annotation. |
| **Related Documents** | `docs/releases/v0.4.0/Risks.md`; `Validation Register.md`; `Hosted Services Register.md`; `Plugin Register.md`; `docs/security/Platform Security Review v0.5.0.md`; `docs/security/Security Roadmap.md`; `docs/architecture/Command Framework Architecture.md`; `docs/architecture/Diagnostics Architecture.md`; `docs/releases/v0.7.0/WP7.1A Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1B Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1C Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1D Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1D Security Review Report.md`; `docs/releases/v0.7.0/WP7.1E Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.1E Security Review Report.md`; `docs/releases/v0.7.0/WP7.3A Technical Debt Assessment.md`; `docs/releases/v0.7.0/WP7.3A Security Review Report.md`; `docs/releases/v0.9.0/WP9.0A Technical Debt Assessment.md`; `docs/releases/v0.9.0/WP9.0A Security Review Report.md`; `docs/releases/v0.9.0/WP9.0B Technical Debt Assessment.md`; `docs/releases/v0.9.0/WP9.0B Security Review Report.md`. |
| **Related ADRs** | ADR-0009, ADR-0021, ADR-0024, ADR-0025, ADR-0026, ADR-0028, ADR-0029, ADR-0032, ADR-0037, ADR-0038, ADR-0039, ADR-0040, ADR-0044, ADR-0046, ADR-0047, ADR-0048, ADR-0049, ADR-0052, ADR-0053, ADR-0054, ADR-0055, ADR-0056, ADR-0057, ADR-0058, ADR-0059, ADR-0060, ADR-0061, ADR-0107, ADR-0108, ADR-0109, ADR-0110, ADR-0111, ADR-0112. |
| **Related Academy Articles** | Every Work Package retrospective's own "Trade-offs" and "Architectural Debt Assessment" sections. |
| **Coverage Status** | Complete as of this baseline — every debt item disclosed by any retrospective through `WP 4.5`'s implementation is represented below, either as still Open or Resolved with the resolving Work Package named. |

---

## Governing Distinction

This project consistently distinguishes **debt** (something that should
eventually be fixed, currently costing nothing to leave alone) from a
**disclosed, accepted trade-off** (a deliberate design exclusion, not
expected to ever need fixing unless a real consumer need emerges). Both
are tracked below, in separate tables, because conflating them would
misrepresent deliberate, reasoned scope decisions as unaddressed problems.

## Entries — Technical Debt (Expected to Eventually Be Addressed)

| # | Debt Item | Since | Owning Work Package | Status |
|---|---|---|---|---|
| TD-01 | Two logging mechanisms coexist (`ILogger` vs. legacy `LoggingService`) | WP 2.6 | WP 5.2 assessed this (formerly WP 4.8); decided **not** to migrate — re-scoped forward again, no new owning Work Package named | **Open — reassessed, not resolved.** `WP 5.2`'s own investigation confirmed `Program.cs` has not called the legacy path since `WP 5.0D`; migrating code with zero live callers was judged pure risk with no behavioural benefit (`D-020`). Revisit trigger: the legacy bootstrap code is either genuinely revived or deliberately deleted. |
| TD-02 | Single-sink logging (no composite `ILogSink` fan-out) | WP 2.6 | WP 5.2 (formerly WP 4.8) | **Resolved** — `CompositeLogSink` (`ADR-0039`, `Diagnostics Architecture.md`) fans a log entry out to any number of child sinks, isolating one child's own write failure from every other, with no change to `Logger`, `ILoggerFactory`, or any existing `ILogger` consumer. |
| TD-03 | No disposal tracking for `AddInstance`-registered or reflection-constructed singletons implementing `IDisposable` | WP 2.4 / ADR-0009 | None named yet | Open — not urgent; no current platform service is disposable |
| TD-04 | `IHostedService` naming proximity to `Microsoft.Extensions.Hosting.IHostedService` | WP 4.0 / ADR-0024 | Revisit trigger: real usage evidence | **Open — trigger arguably now met, not yet acted on.** `WP 6.3` added a genuine, direct dependency on the ASP.NET Core ecosystem (`ADR-0049`) within the very same codebase that defines this platform's own, differently-shaped `IHostedService` (`Tempest.Core.BackgroundServices.IHostedService`, still unrelated to `Microsoft.Extensions.Hosting.IHostedService` in shape or namespace) — the two same-named-but-unrelated interfaces now coexist in one solution for the first time, exactly the "real usage evidence" this item's own revisit trigger named. No confusion has actually been reported; `RestApiHostedService` itself implements only `Tempest.Core.BackgroundServices.IHostedService`, never the ASP.NET Core one. Still Open, not Resolved — renaming remains a judgment call for a future Work Package, not forced by this one. |
| TD-05 | Parameterless-constructor-only constraint on discovered modules | WP 4.1 | Partially addressed — WP 4.4A/4.4B (ADR-0027) | **Partially resolved.** A module carrying `[ModuleMetadata]` may now declare a DI-resolvable constructor; a module without the attribute remains constrained exactly as before. This is a deliberate, additive lift, not a full removal of the constraint. |
| TD-06 | Plugins root directory (`Plugins/`) and manifest file name (`plugin.manifest.json`) are fixed conventions, not configurable | WP 4.2 | `WP 13.0A` (architecture — `Runtime:Plugins:RootDirectory`/`Runtime:Plugins:ManifestFileName` configuration keys designed, `Plugin Platform Architecture.md`); `WP 13.1A` (implementation, complete) | **Resolved — implemented, `WP 13.1A`.** `Runtime:Plugins:RootDirectory`/`Runtime:Plugins:ManifestFileName`/`Runtime:Plugins:Disabled` are now real, tested configuration keys, consumed in `TempestHost.cs`'s own Plugin Discovery wiring exactly as `WP 13.0A`'s `Plugin Platform Architecture.md` designed, defaulting to the original `WP 4.2` fixed conventions when absent, consulted at the seam ADR-0026 already anticipated — no Host Lifecycle phase change. See `docs/academy/03 Work Packages/WP13.1A-plugin-runtime-and-composition-root-implementation.md`. |
| TD-07 | Navigation's `Tempest.Core` placement is an open architectural question | WP 4.2D (named), pre-existing since v0.4.0 planning | WP 5.0A (formerly WP 4.6A) | **Resolved** — WP 5.0A's own Repository Investigation and `ADR-0031` settle this: the Navigation *model* belongs in `Tempest.Core`; rendering is `Tempest.App`'s own responsibility. Stale "Open"/`WP 4.6A` labels found and corrected here, `WP 5.0D`, having survived unnoticed through three prior Work Packages (`WP 5.0A`–`WP 5.0C`) that each resolved or built on this exact question without this register being updated to match — a disclosed governance-debt finding in its own right, not a new architectural gap. |
| TD-08 | Background Services would need to extend `Host Lifecycle.md`'s phase table a second time | WP 4.2D (named as future work) | WP 4.5 | **Resolved** — WP 4.5 implemented Phases 8.1/10.1 exactly per ADR-0029/ADR-0030, no renumbering. See `Risks.md` R1/R4, both now Retired. |
| TD-09 | No isolation boundary exists between a loaded plugin and a first-party module — a plugin gets identical DI-container trust once its assembly loads. **Scope widened, WP 5.1A**: this same gap now also applies to the Command Framework — a plugin's own command handler/descriptor is indistinguishable from a first-party one once registered (`Command Framework Architecture.md`'s own Security Review). One root cause, two affected surfaces, not two separate debt items. | Implicit since WP 4.2/ADR-0025/ADR-0026; named explicitly as security debt by WP 5.0S; scope widened WP 5.1A | `WP 6.1` built the mechanism (`IPermissionEvaluator`, `ADR-0044`); `WP 13.0A` designed the retrofit (`ADR-0110`/`ADR-0111`/`ADR-0112`, `Plugin Trust & Isolation Architecture.md`); `WP 13.2A` (implementation, complete) | **Resolved — implemented, `WP 13.2A`.** `PluginAssemblyLoader.EnforceTrust` checks every plugin's `RequestedCapabilities` against its assigned `PluginTrustTier`'s ceiling and reflects over every discovered `IModule` implementer's constructor for conformance against a fixed always-allowed baseline (`ILogger`/`IConfigurationProvider`/`IDiagnosticsProvider`) plus its own granted `plugin.services.resolve:*` declarations, denying at category 17 (`PluginTrustDeniedException`, recorded `PluginRegistryState.TrustDenied`) before the plugin is ever `Loaded` — real, tested, working code, not merely a design. See `docs/security/Platform Security Review v0.5.0.md` Finding SEC-01, `Security Roadmap.md` item 1, `ADR-0110`–`ADR-0112`, `docs/academy/03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md`. |
| TD-10 | `NavigationService.Unregister` performs no ownership check — any caller can unregister any other component's navigation item by ID | WP 5.0B/ADR-0032; named explicitly as security debt by WP 5.0S | `WP 6.1` built the mechanism (`IPermissionEvaluator`, `ADR-0044`); `WP 13.0A` designed the retrofit (`ADR-0111`); `WP 13.2A` (implementation, complete) | **Resolved — implemented, `WP 13.2A`.** `NavigationService.Register` now captures the registering component's own principal (`ICurrentComponentAccessor.Current`) out-of-band, alongside `_itemsById`, with no change to `NavigationItem`'s public shape; `Unregister(id)` now compares the caller's current component principal against the stored owner, rejecting a mismatch unless the caller holds the reserved `navigation.unregister.any` permission (held only by First-Party by construction) — real, tested, working code. `null`/First-Party callers proceed unconditionally, exactly as before, zero behavioural change for every actor that existed prior to this Work Package. See `docs/security/Platform Security Review v0.5.0.md` Finding NAV-1, `Security Roadmap.md` item 2, `docs/academy/03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md`. |
| TD-11 | Command and Navigation registration-order squatting: "first registration wins" rejects a *later* duplicate but does not establish the *first* registrant was the intended owner of a well-known Id — a plugin whose module Id sorts earlier (by `ModuleLifecycleManager`'s existing ascending-Id initialisation order) can legitimately claim a well-known command or navigation Id before its real owner registers | Pre-existing for Navigation since WP 5.0B/ADR-0032 (undetected until now); newly designed-in for Commands, WP 5.1A; both disclosed together by WP 5.1A's own Security Review (Finding CMD-1) | `WP 6.1` built the mechanism (`IPermissionEvaluator`, `ADR-0044`); `WP 13.0A` designed the retrofit (`ADR-0111`); `WP 13.2A` (implementation, complete) | **Resolved — implemented, `WP 13.2A`.** `NavigationService.Register`, `CommandHandlerTable.Register`, and `CommandRegistry.RegisterDescriptor` all now apply the trust-tier-ordered rule for real: first-wins only among registrants of the same tier (`PluginTrustPermission.Rank`); a higher-tier registration always evicts and replaces a lower one, regardless of order, logged loudly at `Warning` as an "ownership override," never silent. An Id-namespace-prefix convention was considered and rejected (`RD-0059`) in favour of reusing the trust-tier comparison this design already needs. Real, tested, working code — every registrant that exists today is First-Party, so zero behavioural change was observed. See `docs/architecture/Command Framework Architecture.md` Finding CMD-1, `docs/security/Security Roadmap.md` item 10, `docs/academy/03 Work Packages/WP13.2A-plugin-trust-and-capability-enforcement-implementation.md`. |
| TD-12 | `IPersistenceStore` has no native query or filter capability — only key lookup and full-collection key enumeration. `WP 6.5` (Audit)'s own `IAuditQuery` filters client-side, over `ListKeysAsync` plus a per-key `ReadAsync`; this is fully correct but scales linearly with the total number of stored records | WP 6.4/ADR-0041 (the shape itself); confirmed as a real, not merely anticipated, limitation by WP 6.5's own explicit Persistence Validation (`ADR-0045`) | None named yet — trigger: a real, measured performance problem or a concrete scale requirement, not speculative pre-emptive extension | Open — deliberately not extended by `WP 6.5`; see `docs/releases/v0.6.0/Risk Register.md` `R8` and `ADR-0045`'s own Persistence Validation reasoning |
| TD-13 | The REST API's own identity resolution carries no real authentication — a caller-supplied `X-Identity-Id` header is trusted outright, with no password/token/credential verification of any kind | WP 6.3/ADR-0052 (extends this release's own local-only identity model, `ADR-0043`, over a network boundary for the first time) | A genuine authentication mechanism (API keys, OAuth/OIDC, mutual TLS) being designed for a future release, once this platform is exposed beyond a trusted local/loopback boundary | Open — disclosed explicitly, mitigated (not fixed) by binding to the loopback address only by default (`ADR-0049`); this is this release's own first network-facing attack surface, and `Platform Service Contracts.md`'s own Security Considerations name it as the highest-security-sensitivity service alongside Identity & Permissions itself |
| TD-14 | No TLS is configured for the REST API's own Kestrel listener | WP 6.3/ADR-0049 | A concrete deployment scenario beyond local development naming a real TLS requirement | Open — `Platform Service Contracts.md`'s own Versioning/Security dimensions name TLS as an expectation "beyond local development," not a current-release requirement; disclosed, not silently omitted |
| TD-15 | A command handler that relies on `IAuditRecorder.RecordAsync`'s own ambient-current-principal auto-attribution will record `AuditRecorder.UnknownActorId` ("unknown") when invoked via the REST API, since `ApiRequestHandler` deliberately never establishes the ambient current principal (`ADR-0052`) | WP 6.3/ADR-0052 | A future command handler needing precise per-command Audit attribution specifically when invoked via REST | Open — the caller's own real identity is not lost, only mis-routed: it is carried correctly in the REST API's own `api.request` audit entry's `Detail[CallerIdentityId]`, just not automatically visible to a *different* command handler's own, separate `RecordAsync` call for the same request. See `WP6.3 Technical Debt Assessment.md` and `WP6.3 Future Capability Recommendations.md`. |

| TD-16 | License file contents are trusted at face value — no cryptographic signature or tamper-resistance verification of any kind | WP 6.6/ADR-0050 | A concrete license distribution scenario naming a real tamper/forgery threat model | Open — disclosed explicitly, mirroring `TD-13`'s own precedent for the REST API's undisclosed-authentication gap; extends this release's own local-trust posture (`ADR-0043`) to a second surface. No concrete distribution channel or threat model exists yet in this release's own approved scope. |
| TD-17 | `IDocumentRevision.Content` (Engineering Data Model) is a plain, opaque `string` — no structured/typed payload support; every future consumer must define and enforce its own content schema outside this framework's own contract | WP 7.1A/`WP7.0C Engineering Foundation Contracts.md`'s own disclosed Extension Point | A real, demonstrated need for framework-enforced content structure | Open — disclosed explicitly, an approved-contract scope decision, not a defect. No concrete consumer with a structured-content requirement exists yet. |
| TD-18 | `IEngineeringDocumentStore.LinkAsync`'s own concurrency behaviour under many simultaneous calls against the *same* source document is not tested at the same depth as `ReviseAsync`'s own 20-concurrent-call regression test — only sequential `LinkAsync` calls are tested | WP 7.1A | A real, demonstrated need for high-concurrency reference writes against the same source document | Open — disclosed explicitly; no atomicity concern is believed to exist (each `LinkAsync` call writes an independent, randomly-keyed entry), but this has not been proven under real concurrent load the way `ReviseAsync`'s own atomicity has. |
| TD-19 | `Unit<TDimension>` supports only a single multiplicative `ToBaseUnitFactor` — no offset term. `Temperature` (Celsius↔Fahrenheit, a genuine affine conversion) is therefore deliberately absent from `WP 7.1B`'s own starting catalogue (`Length`, `Mass`, `Duration`, `Force`, `Pressure`, `Area`, `Volume` — all purely multiplicative) | WP 7.1B/`ADR-0054`'s own "Temperature Deliberately Deferred" | A real, demonstrated need for a Temperature dimension (any future Mechanical/HVAC/Materials capability) | Open — disclosed explicitly, an approved-contract-compatible scope decision, not a defect. Resolving it requires either extending `Unit<TDimension>` with an optional offset term (a change to every existing dimension's own conversion arithmetic) or a parallel affine-unit type — a genuine design decision for a future Work Package, not assumed here. |
| TD-20 | `MaterialCatalog.FindAsync`/`ListAsync` read a material's own complete revision history (`IEngineeringDocumentStore.GetRevisionHistoryAsync`) and take only the last entry, since no dedicated "latest revision only" lookup exists on that interface — a material with many revisions pays for reading all of them on every lookup | WP 7.1C/`ADR-0055` | A real, measured performance problem with a many-revision material, or `IEngineeringDocumentStore` gaining a dedicated latest-revision lookup of its own | Open — disclosed explicitly, mirroring `TD-12`'s own "no measured performance problem yet" disclosure discipline; not believed to be a current correctness or performance concern at this framework's own current scale. |
| TD-21 | `ICalculationDefinition.Calculate` carries no `CancellationToken` — a long-running or blocking calculation definition cannot be cancelled once `ExecuteAsync` has dispatched to it | WP 7.1D/`WP7.1D Security Review Report.md` | A real, demonstrated need for cancelling an in-flight calculation | Open — disclosed explicitly; calculation definitions are trusted, first-party, in-process code, the same trust boundary the Command Framework already operates under without cancellation reaching into a handler either. |
| TD-22 | `CalculationContext` imposes no bound on how many intermediate results, constraint checks, or material references a single execution may record; separately, `CalculationIntermediateResult.Value` is not guaranteed to deserialize back to its exact original CLR type if read from durable storage later, only fully inspectable from the in-memory record `ExecuteAsync` returns immediately | WP 7.1D/`ADR-0056`, `WP7.1D Security Review Report.md` | A real, demonstrated need to bound recorded data volume, or to deserialize intermediate results back into their original CLR type from durable storage | Open — disclosed explicitly; no current consumer needs either capability. |
| TD-23 | `VerificationService.RecordAsync`'s own multi-step linking sequence (create the verification record, link it to its subject, link it to each additional document/calculation record) is not transactional — a failure partway through leaves a partially-linked record | WP 7.1E/`ADR-0057`, `WP7.1E Security Review Report.md` | A real, demonstrated need for transactional multi-document operations (`FCR-0036`) | Open — disclosed explicitly; `IEngineeringDocumentStore` itself offers no transactional multi-write primitive to build on. |
| TD-24 | `VerificationContext` imposes no bound on how many criteria, evidence entries, or links a single verification may record; separately, `GetVerificationHistoryAsync` reads every reference from a subject document (not only verification references) plus a full revision history per matching record, scaling linearly with both | WP 7.1E/`WP7.1E Security Review Report.md` | A real, measured performance problem, or a real need for bounded recording | Open — disclosed explicitly; mirrors `TD-12`/`TD-20`/`TD-22`'s own identical "no measured problem yet" disclosure discipline. |
| TD-25 | `IRequirementsService.ReviseAsync`/`SetStatusAsync` carry no compare-and-swap or expected-prior-revision parameter — two concurrent editors of the same requirement can each succeed, with the second silently becoming current and no conflict signalled to either caller. The underlying store's own per-document lock still guarantees no two revisions ever claim the same revision number | WP 7.3A/`ADR-0060`, `WP7.3A Security Review Report.md` | A real, demonstrated multi-author collaborative-editing incident against a shipped requirement set | Open — disclosed explicitly; implementing a fix now would deviate from the approved `WP7.2C Requirements Platform Contracts.md` signature without a genuine implementation defect to justify it. More consequential than the equivalent absence elsewhere, since the Requirements Engine's own target users are more likely to edit the same artefact concurrently as ordinary practice. |
| TD-26 | `WorkspaceManager.StartAsync`'s own `WaitForServicesAsync` returns as soon as `ITempestHost.Services` is non-null, which `TempestHost.cs` sets *before* running module `InitialiseAsync` — a Workspace read taken immediately after `StartAsync` returns can occur before a module-registered `NavigationItem` or seeded Engineering Domain object exists. Confirmed pre-existing on the unmodified `v0.8.0` tag, not introduced by `WP 9.0A`. Mitigated one layer up in `Tempest.Desktop` twice (`WP 10.0B`, `WP 10.1A`) before being fixed at its own true source | WP 9.0A/`WP9.0A Technical Debt Assessment.md`; consequences directly reproduced, `WP 10.0B`/`WP10.0B Technical Debt Review.md` §1; resolved, `WP 10.1B`/`WP10.1B Technical Debt Review.md` §1 | N/A — already resolved | **Resolved, `WP 10.1B`** — `WorkspaceManager.StartAsync`'s own `WaitForServicesAsync` now also waits for `IDiagnosticsProvider.HostState == HostState.Running` before returning, fixed at its own source for the first time; `Tempest.Desktop`'s own now-redundant `WorkspaceHost.WaitForHostRunningAsync` mitigation was removed. Proven by `ModuleLifecycleStabilityTests` (`tests/Tempest.Desktop.Tests`). |
| TD-27 | `InMemoryEngineeringObjectRepository`'s own backing `ConcurrentDictionary`'s `.Values` iteration order is not guaranteed (insertion order or otherwise) — `MechanicalProductStructureNodeProvider.OrderForBom`'s own fallback behaviour for a partially-numbered BOM sibling group is therefore "whatever order the repository happens to return," honest but not especially useful. Found via an intermittent full-suite test failure (passed in isolation, failed roughly one run in several as part of the full 1738-test suite) | WP 9.0B/`WP9.0B Technical Debt Assessment.md` | A real user complaint about unstable/confusing ordering for a partially-numbered BOM, or the in-memory repository being replaced by one with a real ordering guarantee | Open — the test-level and documentation-level inaccuracies that first surfaced this are already fixed (re-run four consecutive times with zero failures since); the underlying "no stable order" characteristic itself is accepted, not solved, since no ordering rule beyond "sorting"/"filtering" generally was ever specified. |
| TD-28 | `IWorkspaceCommand`'s own generic post-dispatch `RefreshAsync` targets exactly one `TargetObjectId` — `BulkSetRequirementStatusCommand`/`BulkSetRequirementOwnerCommand`/`BulkSetRequirementPriorityCommand` are deliberately plain `ICommand` (many targets, none more "the" target than another, mirroring `CompareBaselinesCommand`'s own precedent), so no open view for any requirement touched by a bulk operation is automatically refreshed. Underlying data is always correct immediately; only an already-open view's own cached display can lag | WP 9.1A/`WP9.1A Technical Debt Assessment.md` | A real user report of a stale view after a bulk operation, or a future Work Package extending `IWorkspaceCommand` (or a sibling contract) with a multi-target refresh shape (`FCR-0050`) | Open — disclosed explicitly; no data-correctness issue exists, only a display-freshness one. |
| TD-29 | `CalculationRecord<TResult>`/`CalculationRecordDto<TResult>` (`WP 7.1D`, unmodified) never retain the `TInput` that produced them — `RecalculateCalculationCommand` therefore still requires a fresh `InputJson` from the caller, identical in shape to `ExecuteCalculationCommand`; it cannot offer a parameterless "run it again with the same numbers" gesture. No data-correctness issue — every execution, first or repeated, is a fresh, correctly-recorded, fully evidentiary record | WP 9.2A/`WP9.2A Technical Debt Assessment.md` | A real UI consumer of the Calculations Workspace surface demonstrating a genuine need to re-run with the last input unchanged, or a future Work Package extending `CalculationRecordDto`/`CalculationRecord` to retain it (`FCR-0053`) | Open — disclosed explicitly; no data-correctness issue exists. |
| TD-30 | `ICalculationResult`/`IVerificationResult` (`Contracts/Calculations.cs`/`Contracts/RequirementsVerification.cs`, `WP 8.2B`) and `IApprovalGate`/`IApproval`/`IReview`/`IReviewGate` (`Contracts/Lifecycle.cs`, `WP 8.2B`) have zero concrete implementations anywhere in this platform, confirmed by a direct, whole-repository search — a pre-existing gap across two releases and three Engineering-discipline Workspace integrations, formally registered here for the first time. `EvidenceComposer`/`ITraceable.GetEvidenceAsync` resolves structurally empty for every object as a direct consequence; "Approval State" is necessarily read from `IHasLifecycle.Status` alone, never a governed sign-off record | WP 9.2A/`WP9.2A Technical Debt Assessment.md` | A future Work Package needing governed, queryable Approval/Review records, or needing `IEvidence.CalculationResults`/`VerificationResults` to actually resolve non-empty (`FCR-0051`, `FCR-0052`) | Open — disclosed explicitly; worked around via direct relationship/status reads throughout, not fixed. Confirmed still open by `WP 9.4A`, and additionally consequential for it — no live Verification Domain object exists anywhere in the platform for Documents↔Verification Digital Thread traceability to be demonstrated against (a direct consequence of the disclosed `WP 9.3A` numbering gap, see `PROJECT_STATUS.md`), so `WP9.4A`'s own Test Report instead references the closest real, live anchor available. |
| TD-31 | `Attachment`/`IAttachment` (`Tempest.Core.EngineeringDomain`, `WP 8.2C`, unmodified) carry descriptive metadata only (`FileName`/`ContentType`/`SizeInBytes`) — no actual file bytes, no resolvable file path, no URL-fetch or storage capability exists anywhere in this platform. `AttachDocumentCommand` therefore only ever records metadata about a file, never the file itself; an `"External Reference"` Document's own Content field holds a descriptive placeholder, never resolved or fetched | WP 9.4A/`WP9.4A Technical Debt Assessment.md` | A future Work Package building a real file/URL storage Platform Service (`FCR-0054`), or a real UI consumer demonstrating a genuine need for actual file content | Open — disclosed explicitly; no data-correctness issue exists — every Attachment/External Reference record is exactly what it claims to be. |
| TD-32 | `VerificationService.RecordAsync` (`Tempest.Core.Verification`, `WP 7.1E`, unmodified) links its own subject to a newly-created record via `IEngineeringDocumentStore.LinkAsync` directly, never through `IHasRelationships.LinkAsync` on an `EngineeringObjectBase`-derived object — so, unlike every other relationship a Verification Activity's own subject creates, the Activity→Record `"verifiedBy"` link is invisible to `EngineeringDomainContext.RelationshipRepository`, though fully durable and correctly readable via the raw document store. Found by nine failing tests during `WP 9.3A`'s own implementation, before any commit; fully worked around at the read side (`VerificationRecordReader` reads `IEngineeringDocumentStore.GetReferencesAsync` directly) — no functional gap remains in the shipped Workspace. **Disposition revisited, `WP 10.4A`, as `WP10.0A Digital Thread & Relationship Visualisation.md` §4 explicitly called for once a graph view was actually built**: `DigitalThreadGraphModel`'s own relationship read is the same `RelationshipRepository`-based bidirectional read `ObjectEditorView` (`WP 10.3A`) already established — which, confirmed by direct test (`Recentre_VerificationActivityWithARecordedResult_AddsTheResultAsAVisibleLeafNode`), does **not** surface the `"verifiedBy"` link either, an identical, previously-undisclosed gap already silently present in `ObjectEditorView`'s own Relationship summary since `WP 10.3A`, only now found. `DigitalThreadGraphModel` additionally merges in `VerificationRecordReader`'s own raw-store read for any `"VerificationActivity"` node it expands, rendering the record as a real, visible, non-editable leaf node — the first place in the Workspace/Desktop layer this link is ever shown graphically | WP 9.3A/`WP9.3A Technical Debt Assessment.md`; disposition revisited, `WP 10.4A`/`WP10.4A Engineering Review.md` §3 | A future Work Package extending `VerificationService.RecordAsync` itself to additionally link through `IHasRelationships` (`FCR-0057`), or any new code querying `RelationshipRepository` directly for a Verification result link (now confirmed to include `ObjectEditorView`'s own Relationship summary, `WP 10.3A`, disclosed here for the first time — out of `WP 10.4A`'s own scope to fix) | Open — disclosed explicitly; no data-correctness issue exists — the underlying link is always durable and always correctly read back by `VerificationRecordReader`, now reachable from two consumers (`DigitalThreadGraphModel`, and — indirectly, as the workaround — `VerificationRecordReader` itself) instead of one. |
| TD-33 | `EngineeringCockpit.FormatCoverage` (`Tempest.App.Workspace`, `WP 9.1A`, unmodified) returns a hardcoded, Requirements-specific zero-denominator string (`"— (no requirements yet)"`) regardless of what is actually being measured — already inaccurately reused by `CalculationsKpiCards`'s (`WP 9.2A`) and `VerificationKpiCards`'s (`WP 9.3A`) own "Verification Coverage" cards for a zero-Calculation/zero-Activity denominator. Found by direct inspection while designing `ManufacturingKpiCards`, before any code was written — not by a failing test. Worked around locally: `ManufacturingKpiCards`'s own "Manufacturing Readiness"/"Supplier Status" cards define a local `FormatShare` function with an accurate empty-state message instead of reusing the shared helper, avoiding a third inaccurate instance; the two pre-existing instances are left unfixed | WP 9.5A/`WP9.5A Technical Debt Assessment.md` | A future Work Package touching `EngineeringCockpit.cs` for any reason, which could parameterise `FormatCoverage`'s own empty-state message and retire `ManufacturingKpiCards`'s own local `FormatShare` duplication (`FCR-0061`) | Open — disclosed explicitly; no data-correctness issue exists — every numerator/denominator value is always correct, only the all-zero display text is discipline-mismatched for two pre-existing call sites. |
| TD-34 | `CompositeLogSinkTests.Write_AllSinksThrow_ExceptionNeverPropagatesToTheCaller` (`Tempest.Core.Tests.Logging`, `WP 5.2`, unmodified) intermittently fails when run as part of the full suite, though never in isolation — `CompositeLogSink.Write`'s own per-sink failure handler (`Console.Error.WriteLine`, line 84) races against a concurrently-running test class that temporarily redirects the process-wide `Console.Out`/`Console.Error` streams (`Console.SetOut`/`SetError` — every `[Collection("Console output capture")]`-tagged Workspace integration test does this, `WP 9.0A` onward), since xUnit parallelises test classes by default and `Console.Error` is a single, static, shared stream. First informally disclosed in narrative form since `WP 6.3` ("this project's own previously-disclosed, non-reproducible `Console.Out`-capture flake"), referenced by name in every subsequent release-closing review's own dedicated flake-check run — every one of which, until now, reported zero instances observed. **First actually observed and captured, `WP 9.9.0`'s own second verification pass**: 1 failure across 5 full-suite runs (Debug ×2, Release ×2 plus one re-run), reproduced zero times across 5 further runs scoped to the failing test class alone — consistent with a genuine cross-test-class race, not a deterministic defect. **Never formally registered as its own Technical Debt item before this entry**, despite six years' (in this project's own compressed timeline) worth of narrative disclosure — a genuine, disclosed gap in this register's own completeness, found only because this Work Package's own re-verification pass happened to catch a live instance | WP 6.3 (first informally disclosed)/`WP9.9.0 Release Readiness Report.md` (Second Pass, first formally registered) | A future Work Package either serialising `CompositeLogSinkTests` against the `[Collection("Console output capture")]` tests (via a shared xUnit collection, preventing the two from running concurrently), or replacing `CompositeLogSink`'s own direct `Console.Error.WriteLine` failure report with an injectable sink, removing the shared static stream dependency entirely | Open — disclosed explicitly; no data-correctness issue exists — the underlying `CompositeLogSink.Write` behaviour (isolating every child sink's own failure) is correct and already proven by the same test's own repeated, isolated passes; only the test's own incidental dependency on the shared, static `Console.Error` stream is at risk under full-suite parallel execution. **Observed live and extended, `WP 13.11D`.** That Work Package's own full Release regression returned 2,561/2,562 with one failure, and an immediate re-run of the identical binaries returned 2,562/2,562 clean — the exact intermittent, parallelism-only, never-in-isolation signature described above. The failing test's identity was lost to that run's own log filter and is therefore not asserted to have been this one, but no other cause is plausible: `WP 13.11D` changed zero `src/` and zero `tests/` files, so nothing about the binaries differed between the failing and passing runs. Recorded here rather than rounded away, because a suite that intermittently red-flags a clean tree is a real cost to every future Work Package's own verification step, and each such Work Package currently has to re-run and judge for itself whether a failure is genuine. **Sibling hazard named, same root cause, found by `WP 13.11D`'s own Verification reviewer:** `tests/Tempest.Core.Tests/Logging/ConsoleLogSinkTests.cs` calls `Console.SetOut`, redirecting the same process-global stream, while sitting **outside** `[Collection("Console output capture")]` — the collection that serialises the fifty-odd other classes doing exactly this. It is therefore free to race against all of them, and against `CompositeLogSinkTests`. Adding that one class to the existing collection is the smallest available mitigation and is strictly narrower than the proposed remedy above; deliberately not applied by `WP 13.11D`, which was read-only by its own instruction. Neither the flake nor the sibling hazard blocks WP14 — no product behaviour is implicated, only test-harness isolation. **Second sibling named, `WP 13.12.2` (`DNB-11`): `WP 13.11D`'s account was incomplete.** `tests/Tempest.Core.Tests/Logging/LoggerTests.cs` (class declared line 5) calls `Console.SetError` at lines 228 and 236 and likewise carries **no** `[Collection("Console output capture")]` — verified by direct source read. That is a *more direct* collider than the `Console.SetOut` sibling already named, because `Console.Error` is the exact stream `CompositeLogSink.Write` writes to and therefore the exact stream this item's own victim test asserts against. Both unguarded classes must join the collection for the mitigation to be complete; adding only `ConsoleLogSinkTests.cs` would leave the more likely collider in place. Also corrected: the count of guarded classes is **64**, not the "fifty-odd" stated above — verified by direct count of `[Collection("Console output capture")]` declarations in `tests/Tempest.Core.Tests`, excluding five commented-out or doc-comment references. Still test-harness isolation only; still not blocking. |

| TD-35 | `ProjectExplorer.Id`/`PropertyInspector.Id` (`Tempest.App.Workspace`, `WP 8.1A`/`WP 8.1B`) were generated via `Guid.NewGuid()` per instance — fresh and random every `WorkspaceManager.StartAsync` call — meaning `ADR-0064`'s own session-persistence design could never correctly restore either panel's own placement across a genuine process restart, only appear to work within one process's own save/load cycle. Confirmed pre-existing on the unmodified `v0.9.0` tag; never previously detected because no prior test constructed two genuinely independent `WorkspaceManager` instances against the same persisted session state. Found by `WP 10.0B`'s own `PersistentLayouts_HidingAPanelAndRestarting_RestoresTheSameLayout` test, before any commit | WP 8.1A/`WP 8.1B` (introduced, undetected)/`WP 10.0B`/`WP10.0B Technical Debt Review.md` §2 (found and fixed) | N/A — already resolved | **Resolved, `WP 10.0B`** — both panels' own `Id` changed to a fixed, well-known `Guid` constant; the same test that found the defect now proves the fix (`WP10.0B Implementation Report.md` §6.3). |
| TD-36 | `PersistenceStore.DefaultRootPath` (`"persistence-data"`, `Tempest.Core.Persistence`, unmodified) resolves relative to the process's own current working directory at start — a reasonable default for a console tool launched from a known shell location, but launch-context-dependent and potentially unpredictable for a graphical application launched from a shortcut or file manager (`Tempest.Desktop`, `WP 10.0B`). Disclosed directly by `WP10.0B Security Review.md` §6 | WP 10.0B/`WP10.0B Security Review.md` §6 | A future Work Package that owns `Tempest.Core.Persistence` directly deciding on a fixed, per-user profile location (`%AppData%`/`~/.config`-equivalent) as an alternative default, if a real, demonstrated need for one emerges | Open — disclosed explicitly; no data-correctness consequence — session state remains fully functional, only its own on-disk location may be less predictable to an end user than intended. |
| TD-37 | Four `Tempest.Samples` modules (`tempest.samples.engineeringdomain`, `tempest.samples.materials`, `tempest.samples.requirements`, `tempest.samples.requirementsworkspace`) each fail their own `InitialiseAsync` with a duplicate-registration error against their own literal Id (e.g. `EngineeringDomainSampleModule` fails "A material is already registered with Id 'SAMPLE-MAT-001'"). Root cause fully determined `WP 10.1B`: not a double-invocation (`IModuleLifecycleManager` re-confirmed single-pass, no retry) — `IMaterialCatalog`/`IRequirementsService` both index their own uniqueness constraint durably via `IPersistenceStore` (`ADR-0041`), shared by design across every process launched from the same working directory; a first successful launch durably writes each module's own fixed, literal identifier, and every later launch's own re-seeding then collides with its own prior work. Confirmed by direct file-system evidence, not inference alone | WP 10.1A/`WP10.1A Technical Debt Review.md` §1 (found); resolved, `WP 10.1B`/`WP10.1B Root Cause Analysis.md`, `WP10.1B Technical Debt Review.md` §2 | N/A — already resolved | **Resolved, `WP 10.1B`** — each affected module now checks (`FindAsync`/`FindByIdentifierAsync`) before seeding and treats an already-registered identifier as its own prior work, skipping idempotently rather than failing; `Tempest.Desktop.Tests` gained genuine per-test persistence isolation, mirroring `Tempest.Core.Tests`' own `WP 7.3A` precedent. `EngineeringCockpitTests`' own three previously honest-empty assertions (`RiskSummary`/`UpcomingMilestones`/`OpenTaskCount`) now read real, stable, live data. A related, non-crashing finding surfaced during this same investigation — see `TD-38`. |
| TD-38 | `EngineeringObjectFactory<T>.CreateAsync` (`Tempest.Core.EngineeringDomain`, unmodified) enforces no business-identifier uniqueness of its own — every call unconditionally creates a fresh document with a new `Guid`, regardless of whether its own literal display identifier (e.g. `"SAMPLE-PORT-001"`) already exists. Confirmed in `EngineeringDomainSampleModule`'s own first five objects (created before its `TD-37`-affected Material registration) and in `MechanicalProductStructureSampleModule`, independent of the four `TD-37`-affected modules — a real, genuine second launch from the same working directory silently duplicates these objects' own graph, with the same displayed identifier text, rather than crashing. Compounded by a separate, pre-existing, deliberate `WP 8.2C` scope decision, re-confirmed as context here: `IEngineeringObjectRepository`/`IEngineeringRelationshipRepository` are in-memory only, never rehydrated from the durable document store on startup, so even a successfully-created duplicate is invisible to any Workspace read once its own process exits | WP 10.1B/`WP10.1B Root Cause Analysis.md` §2.3, `WP10.1B Technical Debt Review.md` §3 (found while investigating `TD-37`) | A future, dedicated Engineering Domain Work Package adding either real business-identifier uniqueness enforcement to `EngineeringObjectFactory<T>`/`EngineeringObjectBase`, or a durable-store rehydration capability for `IEngineeringObjectRepository` — both explicitly out of `WP 10.1B`'s own "No Engineering Domain changes" scope | Open — disclosed explicitly; no crash, no data-correctness issue in already-shipped Engineering Domain/discipline code — confined to sample/demonstration data silently duplicating across a genuine restart, never surfaced to a real user since every affected object is fictional sample data. |
| TD-39 | `PanelHostControl`'s own opaque `Background` (`Tempest.Desktop.Docking`, `WP 10.2B`) is a fixed `Brushes.White`, not theme-variant-aware — required once a panel can be reparented as an Auto-Hide flyout directly over the Document Area, but visually incorrect (a white panel over a dark theme) if the user has switched to Dark via the Theme Framework (`WP 10.0B`/`WP 10.2A`). Disclosed and accepted at implementation time, not found later: the identical, already-shipped, never-previously-registered limitation `CommandPaletteOverlay` (`WP 10.0B`, `Background = Brushes.Black`) already carries for this platform's only other overlay control — found by direct inspection while designing the flyout, both instances surfaced together here for the first time | WP 10.2B/`WP10.2B Engineering Review.md` §4, `WP10.2B UX Review.md` §5; resolved, `WP 10.5A`/`WP10.5A Technical Debt Review.md` §1 | N/A — already resolved | **Resolved, `WP 10.5A`** — both `PanelHostControl` and `CommandPaletteOverlay` now bind through the new `ThemeReactiveBrush` helper to `ApplicationPalette`'s own explicit Light/Dark resource keys, fixed at exactly the mechanism this entry's own "Revisit trigger" named. Proven by `VisualPolishTests.PanelHostControl_Background_IsARealResolvedThemeBrush_ClosesTD39`/`CommandPaletteOverlay_Background_IsARealResolvedThemeBrush_ClosesTD39`, re-run five consecutive times, zero failures. |
| TD-40 | `ObjectEditorView`'s own new, real, buffered dirty-state (`Tempest.Desktop.Editors`, `WP 10.3A`) is not wired into `IWorkspaceView.CloseAsync`'s own unsaved-edit prompt hook — every concrete `IWorkspaceView.IsDirty` has returned `false` unconditionally since `WP 8.1A` (by design, since no View ever buffered a local edit before this Work Package), so `DocumentAreaView`'s own tab-close path never had a reason to prompt; this Work Package's own new, genuinely buffered edits (Name/Content, before Save) can now be silently discarded by closing a tab, with no confirmation dialog. Disclosed, deliberate scope decision at implementation time, not found later — building a proper confirmation dialog was judged disproportionate scope for a first implementation | WP 10.3A/`WP10.3A UX Review.md` §5, `WP10.3A Implementation Report.md` §4; resolved, `WP 10.5A`/`WP10.5A Technical Debt Review.md` §2 | N/A — already resolved | **Resolved, `WP 10.5A`** — `DocumentAreaView.IsMarkedDirty` (new) + a real `ConfirmationDialog` (new) gate `MainWindow.CloseDocumentAsync`: a dirty tab now prompts "Discard unsaved changes?" before closing, reached from both the tab close button and the keyboard-shortcut close path; cancelling leaves the tab and its edits untouched, fixed at exactly the mechanism this entry's own "Revisit trigger" named. |
| TD-41 | `ObjectEditorView.TryCreate` (`Tempest.Desktop.Editors`, `WP 10.3A`, unmodified logic) gates unconditionally on `EngineeringDomainContext.Repository.FindAsync` resolving a real `IEngineeringObject` — confirmed by direct test, `WP 10.7A`, that this call returns `null` for every real Requirement. Requirements are real `IEngineeringDocument`s (`ADR-0058`), but were never wired into the general `IEngineeringObjectRepository`'s own Kind-to-object materialisation — only reachable through `IRequirementsService` directly, a genuinely different, pre-`WP 10.3A` read path. Consequence: opening a Requirement always falls back to `DocumentAreaView.BuildDefaultBody` (the generic Title/Kind/Id-only body), never the real `ObjectEditorView` — so Requirements' own Rename/Revise (`WP 10.2A`/`WP 10.3A`, both real, registered) and `WP 10.7A`'s own new Owner/Priority section are all correctly implemented but unreachable specifically through the Object Editor for this one discipline. **`WP 10.8A` update**: `PropertyInspectorView.AddValidationSection` (new this Work Package, also gated on the identical `EngineeringDomainContext.Repository.FindAsync` call) is a second, previously-undisclosed consumer of the same gap — a selected Requirement's own Validation section correctly reports "Real validation is not available for this object here" rather than fabricating a result, the same honest degradation the Object Editor already used, not a new instance of the underlying defect. Not a regression — the identical, already-existing `ObjectEditorViewTests.NavigateToObject_ClickedFromARelationshipRow_...` test (`WP 10.3A`) already silently defended against a `null` editor for a Requirement; never previously formally registered | WP 10.3A (introduced, undetected)/`WP 10.7A Implementation Report.md` §5 (found and formally registered)/`WP 10.8A Implementation Report.md` (second consumer disclosed) | A future Work Package either wiring Requirements into `EngineeringDomainContext`'s own general repository materialisation, or giving `ObjectEditorView`/`PropertyInspectorView` a second, parallel code path over `IRequirementsService`'s own `IRequirement` DTO directly — both a genuine architecture decision, out of `WP 10.7A`'s/`WP 10.8A`'s own "no new architecture" scope | Open — disclosed explicitly; no data-correctness issue exists — every real Requirement read/write remains fully correct via `IRequirementsService` and the Ribbon's own `"requirements.set-owner"`/`"requirements.set-priority"`/`"requirements.rename"`/`"requirements.revise"` handlers, all of which dispatch independently of `ObjectEditorView`; only the Object-Editor and Property-Inspector presentation surfaces are affected. |
| TD-42 | `scripts/new-release.ps1`'s `git tag -a "v$Version" ...` and both `git push` invocations (`origin main`, the version tag — `WP 11.1B`) carry **no `$LASTEXITCODE` verification**, unlike the script's own `dotnet build`/`dotnet test` steps, which correctly check it. Empirically confirmed, `WP 11.9.0`: a failing, unredirected `git tag`/`git push` under this environment's `$ErrorActionPreference = "Stop"` does **not** self-promote to a terminating PowerShell error — `git` writes its own `fatal:` message to inherited stderr, sets `$LASTEXITCODE` (128, reproduced), and the script silently continues past it, straight through to an unconditional `RELEASE SUCCESSFUL` banner. This script has never been run for a real release (`WP11.1B Engineering Workflow.md` §6 — every `v0.10.0`/`v0.11.0` tag was created manually) | WP 11.1B (introduced, undetected — build/test paths hardened correctly, the two most consequential steps were not)/`WP 11.9.0` QA Report (found, empirically reproduced) | Add the identical `$LASTEXITCODE -ne 0` → `throw` guard already used for `dotnet build`/`dotnet test`, after both the `git tag` and each `git push` call | Open — disclosed explicitly; no consequence yet realised, since the script has never executed against a real release. Real risk the first time it is: a rejected push (network failure, branch protection, non-fast-forward, permission denied) would report success falsely. Recommended fixed before this script's own first real-world use. |
| TD-43 | `scripts/governance-healthcheck.ps1`'s (`WP 11.2A`) top-level generic exception handler catches any uncaught error from an individual check but reports it under a single hard-coded label (`"Unhandled error in a governance check"`), losing the specific check's own name and the offending file. Empirically reproduced, `WP 11.9.0`, against an isolated fixture (never the real repository): a zero-byte register file (`Get-Content -Raw` → PowerShell `$null` → the check's own regex throws `ArgumentNullException`) and an index document with zero markdown links (`$null` piped into `ForEach-Object`, invoked once with `$_ = $null`, throws on `.ToLowerInvariant()`) both fail safely (non-zero exit, correct `[FAIL]`) but with no indication of which check or file was responsible. Distinct from the three defects `WP 11.2A` itself already found and fixed (multi-line path-extraction crash; git-unavailable crash; PowerShell single-item-count sharp edge) | WP 11.2A (introduced, undetected — outside its own disclosed fixture-testing scope)/`WP 11.9.0` QA Report (found, empirically reproduced) | Pass the specific check's own name into the per-check error handler so a CI-gate failure is diagnosable from the report alone, without log archaeology | Open — disclosed explicitly; the tool still fails safely (correct non-zero exit) today, and is not yet a required `CI Gate` check, so no merge-blocking consequence exists yet. Recommended fixed before the tool is promoted to a required gate. |
| TD-44 | TempestOS's CI pipeline (`.github/workflows/ci.yml`, `WP 11.1A`) had **never executed on real GitHub-hosted infrastructure** for any Work Package of this release, as of `WP 11.9.0`'s own sign-off. **Now executed and independently observed, `WP 11.4A`**: the branch was pushed, and both `v0.11.0`'s own tag push and a disposable verification tag push were independently observed to completion. First real execution (`v0.11.0` tag push, `WP 11.9.0`'s own Release Publication Report) found three genuine, previously-invisible defects — a flaky test (`ObjectEditorViewTests.cs`), a `governance-healthcheck.ps1` path-doubling crash, and a `ci.yml` tag-checkout `fetch-tags` collision — exactly the class of risk this item always named as possible, now replaced with evidence rather than assumption. All three fixed and re-verified on real infrastructure by `WP 11.4A`: `Build & Test` (Debug/Release) and `CI Gate` both now genuinely pass, confirmed on two independent real runs (a branch push and a tag push) | WP 11.1A (disclosed at inception)/`WP 11.2A` (same caveat re-disclosed for its own new job)/`WP 11.9.0` (reconfirmed open at release sign-off; found the three defects on first real execution, disclosed in `Release Publication Report.md`)/`WP 11.4A` (fixed all three, re-verified on real infrastructure) | N/A — resolved | **Resolved, `WP 11.4A`.** One residual, disclosed, unrelated item: `Governance Health Check` itself still reports `failure` on both real runs — not a crash (confirmed from the job log: a clean `Summary: 3 passed, 2 warned, 3 failed` followed by a correct non-zero exit), but genuine, already-tracked, pre-existing governance debt (the Academy Index/Documentation Register gaps `WP 11.2A`/`WP 11.9.0` already disclosed) that this Work Package's own scope explicitly excluded ("no feature work"). This does not affect `CI Gate`, which does not depend on this job. |
| TD-45 | Branch protection on `main` is fully documented (`WP11.1B Engineering Workflow.md` §4: required `CI Gate` status check, required CODEOWNERS review, merge-commit-only) but **not configured in GitHub** — confirmed by the workflow document's own section title, "(Recommended — Not Configured)," and by `PROJECT_STATUS.md`'s own identical disclosure. Today nothing in GitHub itself prevents a direct push to `main`, a merge of a failing PR, or a merge with no Code-Owner review | WP 11.1B (disclosed at inception, deliberately out of that Work Package's own scope)/`WP 11.9.0` (reconfirmed still open at release sign-off) | A repository administrator configures the documented branch-protection rule directly in GitHub Settings — no code change, a one-time repository-configuration action | Open — disclosed explicitly. The entire PR/CI/CODEOWNERS apparatus this release designs is currently voluntary, not mechanically enforced, until this is done. |
| TD-46 | `tests/Tempest.Desktop.Tests/ObjectEditorViewTests.cs`'s `VerificationResultSection_RecordPass_ActuallyRecordsARealVerificationRecord` used a fixed `Task.Delay(50)` to wait for an async status-text update before asserting on it — reliable on a quiet local machine, lost its race on a loaded, shared GitHub Actions Windows runner (the test's first-ever real execution there). Confirmed flaky, not a regression: the identical commit passed this same test on a separate, concurrent CI run | `WP 10.3A` (introduced, undetected)/`WP 11.9.0` Release Publication Report (found, first real CI execution) | N/A — resolved | **Resolved, `WP 11.4A`** — replaced the fixed delay with a bounded (2s) poll against the real, live status text, re-read from the visual tree each iteration; confirmed deterministic by 5 consecutive isolated re-runs plus two full-suite runs (Debug/Release, 2228/2228 both), and by a real, independent pass on GitHub-hosted infrastructure. |
| TD-47 | `scripts/governance-healthcheck.ps1`'s `-SummaryPath` resolution used PowerShell's `Join-Path`, which (unlike `[System.IO.Path]::Combine`) does not discard an already-rooted child path — `ci.yml`'s own `$env:RUNNER_TEMP`-based absolute path doubled into `RepoRoot`, tripping the script's own "never write inside the repo" safety check for the wrong underlying reason, on every CI invocation | `WP 11.2A` (introduced, undetected — its own fixture testing never exercised an absolute `-SummaryPath`)/`WP 11.9.0` Release Publication Report (found, first real CI execution) | N/A — resolved | **Resolved, `WP 11.4A`** — guarded with `[System.IO.Path]::IsPathRooted($SummaryPath)`; relative-path behaviour and the genuine in-repo safety check both independently re-verified unchanged; confirmed on two independent real CI runs (branch push, tag push) — no crash, only the same pre-existing, already-tracked governance findings (Academy Index/Documentation Register gaps) reported correctly. |
| TD-48 | `.github/workflows/ci.yml`'s `governance-health-check` job's `Checkout` step set `fetch-tags: true` with no pinned `ref:` — on a tag-triggered push, `actions/checkout@v4`'s own default ref (the tag itself) collided with `fetch-tags: true`'s own fetch of that identical tag, both targeting the same local `refs/tags/<name>` path (`fatal: Cannot fetch both <sha> and refs/tags/<name> to refs/tags/<name>`), failing the job before the governance script ever ran | `WP 11.2A` (introduced, undetected — never exercised by a real tag push before `v0.11.0`'s own)/`WP 11.9.0` Release Publication Report (found, first real tag-push CI execution) | N/A — resolved | **Resolved, `WP 11.4A`** — pinned `ref: ${{ github.sha }}`, removing the competing tag-ref write target; confirmed via a disposable, non-release-pattern tag push (`ci-verify-fix3-<sha>`, deleted after verification, chosen specifically so it would not also trigger `release.yml`) — `Checkout` succeeded. |
| TD-49 | A TOCTOU (time-of-check-to-time-of-use) window exists between Plugin Discovery's own signature verification (`PluginManifestDiscoveryService`'s `AssignTrustTier`/`VerifySignature`, reading manifest and assembly bytes via `File.ReadAllBytes`) and Plugin Loading's own `Assembly.LoadFrom` call (`PluginAssemblyLoader.LoadOne`) — the verified hash is never carried forward, and the assembly bytes on disk are never re-hashed or re-verified immediately before `Assembly.LoadFrom` is called. A file on disk swapped between the two phases (a different Host Lifecycle phase each, `ADR-0026`) would load unverified content under a trust tier assigned to the bytes that were actually checked, not the bytes that were actually loaded | `WP 13.2A` (introduced, by `ADR-0112`'s own design — Discovery verifies "entirely from file bytes... before any `Assembly.LoadFrom` call," a two-phase boundary that structurally creates this window); disclosed, not fixed, `WP 13.2B`'s own commit message ("a TOCTOU window... would require carrying the verified hash forward — a structural change, not a defect fix"); formally registered for the first time, `WP 13.3A` | A future Work Package threading the Discovery-time-computed hash (or the verified bytes themselves) forward into Plugin Loading, re-verifying immediately before `Assembly.LoadFrom`, or a real, demonstrated attack/incident naming this window as its own root cause | Open — disclosed explicitly; not an `ADR-0112` conformance gap (that ADR's own Decision text promises verification "before any assembly is loaded," satisfied literally, and never promises re-verification *at* the load call site itself — see `ADR Register.md`'s own `WP 13.3A` entry for the full clause check). No known exploitation; the window requires an actor with filesystem write access to the plugin candidate folder between two phases of the same process run, already a materially privileged position. |
| TD-50 | TempestOS's own first-party publisher certificate is identified by a fixed filename convention (`PluginTrustStore.FirstPartyCertificateFileName`, the literal file `TrustedPublishers/TempestOS.cer`) rather than by a certificate attribute (Subject, a custom extension, a dedicated field) — any `.cer` file placed in the trust store folder under that exact name is treated as the First-Party trust anchor, regardless of its own certificate content, so First-Party status is a property of *where a file is named*, not of *what the certificate itself asserts* | `WP 13.2A` (introduced, self-disclosed directly in `PluginTrustStore`'s own class-level remarks at the time of writing — not found later); disclosed, not fixed, `WP 13.2B`'s own commit message ("filename-based... first-party trust store detection... not plugin-exploitable"); formally registered for the first time, `WP 13.3A` | A future Work Package moving first-party detection to a certificate-level attribute (a well-known Subject/extension TempestOS's own signing process asserts), if a real, demonstrated need for filename-independent detection emerges | Open — disclosed explicitly; low severity — mitigated today by requiring operator-level filesystem write access to `TrustedPublishers/` already, the identical privileged position `ADR-0112`'s own "no revocation checking" Negative consequence already accepts for the entire trust store. Not exploitable by a plugin itself, only by whoever controls the trust store folder's own contents. |
| TD-51 | `PluginAssemblyLoader.EnforceTrust`'s static constructor-conformance check — `HasCompliantConstructor`, and with it the `NeverEligibleServiceResolveTypes` denylist blocking `CurrentComponentAccessor`/`CurrentPrincipalAccessor` from constructor injection — was invoked only against a plugin's discovered `IModule` types, never its discovered `IHostedService` types. An `IHostedService`-only plugin assembly (zero `IModule` types) hit `moduleTypes.Count == 0` and returned immediately after the (trivially-satisfied, empty) capability-ceiling check — its hosted service's constructor was never inspected. Compounded by `HostedServiceManager` having no `componentScopeProvider`-equivalent hook at all (unlike `ModuleLifecycleManager`), so even a legitimately-passing plugin's own hosted service ran with a `null`, First-Party-treated ambient component principal. Live-PoC confirmed, `WP 13.10A`: an `UnsignedLocal` manifest requesting zero capabilities, one `IHostedService` type whose sole constructor took the denylisted, concrete `CurrentComponentAccessor` — accepted, not denied; since `WP 13.10C`'s own commit, confirmed genuinely denied | `WP 13.2A` (introduced — `EnforceTrust`'s own constructor check was written against `IModule` before `IHostedService` denial-recording existed at all); `WP 13.9.4` extended `hostedServiceTypes` *recording* for the denial path but never extended the *conformance check itself*; found, `WP 13.10A`'s own Security/Trust and Lifecycle/Composition reviewers, independently, each with a live proof-of-concept; fixed, `WP 13.10B` — `HasCompliantConstructor`/the denylist now run identically against `hostedServiceTypes`, not only `moduleTypes`, and `HostedServiceManager` gained an optional `componentScopeProvider` constructor hook mirroring `ModuleLifecycleManager`'s own established ADR-0111 hook; independently re-verified, `WP 13.10C` (four fresh, read-only reviewers, each with its own live proof-of-concept; non-vacuousness confirmed by reverting the fix and observing the targeted tests genuinely fail, then restoring) | N/A — already resolved | **Resolved, `WP 13.11B`; previously Reopened `WP 13.11A`, and before that Resolved `WP 13.10B`, independently re-verified `WP 13.10C`.** Live-PoC-confirmed fixed (retained, unchanged below): the identical `UnsignedLocal`, zero-capability, `IHostedService`-only manifest `WP 13.10A` used to demonstrate the gap is now denied at category 17, exactly like a non-compliant `IModule` type always was, and a legitimately-granted plugin's own hosted service genuinely observes its own correct ambient component principal during `StartAsync`/`StopAsync` (`AmbientPrincipalCaptureProbe`-based regression test). **`WP 13.10B`'s own independent Adversarial Security review found and fixed a compounding regression, mid-implementation, before any commit, disclosed here in full**: `DiscoverModuleTypes`'s own `WP 13.9.3` pre-resolution loop (forcing every discovered type's every constructor parameter to resolve inside the scan's own diff window) originally iterated `moduleTypes` only; once `HasCompliantConstructor` was extended to also check `hostedServiceTypes`, an `IHostedService`-only plugin with a genuinely unresolvable constructor-parameter type hit `HasCompliantConstructor`'s own unguarded `GetParameters()` call as the *first* resolution attempt for that type, with no exception handling anywhere in reach — an uncaught exception propagating out of `LoadPlugins` and `TempestHost.RunAsync` entirely, a Host-wide crash strictly worse than `TD-51`'s own original gap, not merely a denied plugin. Found twice, independently — the `IModule`-only version of this same pre-resolution fix had already landed when a second reviewer reproduced the identical gap for hosted services; a first attempted fix also mis-placed its own `try`/`catch` one level too deep (around the per-parameter body, not `GetParameters()` itself, which eagerly resolves every parameter's own signature the moment it is called) and never actually caught anything for its own documented scenario, corrected before landing. Closed by iterating both `moduleTypes` and `hostedServiceTypes` uniformly in the pre-resolution loop, converting the resulting `TypeLoadException`/`FileNotFoundException`/`FileLoadException`/`BadImageFormatException` into a `PluginTrustDeniedException`, isolating the one plugin exactly like any other trust-check failure, not the whole Host — `WP 13.10C`'s own Verification/RAM reviewer found this specific scenario had only ever been proven via throwaway proof-of-concept code across two independent reviewers, never a permanent regression test; `WP 13.10C` closed that gap directly (`LoadPlugins_FirstHostedServiceOnlyPluginHasUnresolvableConstructorParameterType_IsolatesFailure_SecondLegitimatePluginStillLoads`, non-vacuousness independently confirmed). **One small, low-severity, pre-existing (not `WP 13.10B`-introduced) gap disclosed by `WP 13.10C`'s own Security/Adversarial reviewer, deliberately not fixed here to avoid scope creep**: the "unresolvable constructor parameter type" denial path throws `PluginTrustDeniedException` directly from inside `DiscoverModuleTypes`, before `EnforceTrust` ever reaches a `RecordDenied` call site — so that one specific denied type is never added to `deniedTypeRegistry` (`WP 13.9.4`'s own execution-boundary registry), and a second, independent construction attempt is made during Module/Hosted Service Registration, safely failing for the identical reason (no code execution, `ModuleState.Failed`/hosted-service-equivalent, not a crash). Confirmed pre-existing since `WP 13.9.3` for the `IModule` axis — `WP 13.10B` merely widened this already-existing gap's reach to the `IHostedService` axis too, it did not create a new category of defect. **Correction, `WP 13.11A` Final Hardening Architecture Review — the paragraph above's own "no crash" characterization of this residual gap is confirmed accurate for the `IHostedService` axis only, and is false for the `IModule` axis; reopened as Release Blocking (for third-party plugin support) accordingly, not merely a redundant-construction inefficiency.** Independent Security/Adversarial re-examination traced the same `RecordDenied` gap through to `TempestHost.cs`'s own `WP 13.9.6` Module Discovery filter (`isTypeExcluded: deniedTypeRegistry.IsDenied`, wired specifically to prevent an unattributed module belonging to a denied plugin from ever being constructed during discovery) and found it depends entirely on `deniedTypeRegistry` actually containing the offending type — which, for the "unresolvable constructor parameter type" denial path, it never does. `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own `type.GetConstructor(Type.EmptyTypes)` call (no `[ModuleMetadataAttribute]` present) is not wrapped in any exception handling; empirically confirmed (a real, minimal repro built and torn down during this review, mirroring the CLR's own eager constructor-signature-resolution behaviour already established during `WP 13.10B`'s own `GetParameters()` investigation) that this call throws an uncaught `TypeLoadException`/`FileNotFoundException` when a type's constructor — any constructor, including an otherwise-irrelevant overload — references a parameter type that cannot be resolved, even when a compliant parameterless constructor also exists on the same type. That exception is not caught anywhere in `TempestHost.ExecuteStartupPhasesAsync`'s call to `discovery.DiscoverModules()`, and propagates through `RunAsync`'s own outer `catch (Exception ex) { EnterFaulted(ex); throw; }`, faulting and crashing the whole Host. Reachable by a single, otherwise-inert `IModule` type — any trust tier, zero requested capabilities, no `[ModuleMetadataAttribute]`, any constructor naming an unresolvable parameter type — directly contradicting `ADR-0025`'s own founding guarantee that a plugin-scoped failure must never become a platform-wide outage, the exact guarantee this whole `WP 13` plugin trust chain exists to provide. The `IHostedService` axis is confirmed genuinely safe from this specific path (`HostedServiceManager.StartServiceAsync`'s own generic `try`/`catch` correctly isolates the identical underlying failure as `Failed`, not a crash) — only Module Discovery's independent, plugin-unaware rescan lacks an equivalent guard. Recommended minimum fix, `WP 13.11B`: either (a) have `PluginAssemblyLoader.EnforceTrust`'s unresolvable-constructor-parameter-type denial path call `RecordDenied` (with whatever partial type list `DiscoverModuleTypes` had already scanned) before throwing, closing the root cause so `WP 13.9.6`'s own existing filter correctly excludes the type; and/or (b) wrap `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s `type.GetConstructor`/`Activator.CreateInstance` path in the same `TypeLoadException`/`FileNotFoundException`/`FileLoadException`/`BadImageFormatException` guard already used at three other call sites in this exact codebase [factually corrected, `WP 13.11C`: exactly ONE other call site existed — `DiscoverModuleTypes`' own; `LoadOne`'s neighbouring guard is a different three-exception filter (`BadImageFormatException`/`FileLoadException`/`IOException`) and the remaining reflection guards are single-`ReflectionTypeLoadException` catches], isolating the candidate module rather than propagating — either alone closes the crash; both together match this codebase's own established defense-in-depth convention. Not release-blocking for `v0.13.0` as currently shipped (no real third-party plugin exists in `src/Plugins/` today, so this path is presently dormant, unreachable without a loaded plugin assembly), but must be closed before third-party plugin support is actually enabled or advertised — matching this item's own original `WP 13.10A` framing exactly. See `Plugin Trust & Isolation Architecture.md`'s own updated Risks entry for full detail. **Closed, `WP 13.11B` — both recommended options, (a) and (b), implemented exactly as scoped immediately above, and no wider.** (a), the root cause: `PluginAssemblyLoader.DiscoverModuleTypes`'s own unresolvable-constructor-parameter-type failure no longer throws from inside the scan at all. It is surfaced to `EnforceTrust` through a new `out PluginTrustDeniedException?` parameter, which records every discovered `IModule` and `BackgroundServices.IHostedService` type via `RecordDenied` before throwing it — so this denial path now records exactly what the capability-ceiling and constructor-conformance paths already recorded, and `WP 13.9.6`'s own `isTypeExcluded: deniedTypeRegistry.IsDenied` filter genuinely excludes the offending type. Critically, this is **not** the "whatever partial type list `DiscoverModuleTypes` had already scanned" shape the recommendation above suggested: the fixed-point scan is now allowed to run to completion (the failing type's remaining constructors are skipped via `break`, the enclosing loops continue, and the per-step before/after `AppDomain` diff still executes) rather than aborting mid-traversal. `WP 13.11B`'s own independent Security/Adversarial analysis established that the partial-list shape would have been actively unsafe — an assembly already pulled into the `AppDomain` but not yet dequeued would have gone unscanned and unrecorded, while remaining fully visible to Module Discovery's own deliberately plugin-unaware `AppDomain` scan (ADR-0110); a well-formed module among them (attributed, parameterless constructor) would then have been registered and lifecycle-run with a `null`, and therefore First-Party-treated (`PluginTrustPermission.IsFirstParty`), ambient component principal, trading the Host crash for a silent trust bypass. (b), the backstop: `ReflectionFrameworkDiscoveryService.DiscoverModules` now wraps its own `CreateDescriptor` call in the same four-exception `TypeLoadException`/`FileNotFoundException`/`FileLoadException`/`BadImageFormatException` guard already used elsewhere in this codebase, excluding and logging the candidate rather than faulting the Host — fail-closed, never a silent inclusion, and deliberately narrow: `ModuleDiscoveryException` derives from none of the four, so the long-standing `WP 5.3` "no parameterless constructor and no `[ModuleMetadataAttribute]`" guidance and every `ValidateMetadata` failure still propagate unchanged. That class gains no plugin awareness (ADR-0110) — it is a reflection guard, not a trust decision. No ADR added or amended: `ADR-0111`'s own Decision text already mandates the recording behaviour (a) restores, verbatim, so the defect was an implementation divergence from an already-correct decision. No public API change — every touched member is `private`, except the `internal` `DiscoverModules(IEnumerable<Type>)` test seam, whose signature is unchanged. Seven permanent regression tests added across three files, covering both discovery axes and both directions (denied and legitimate); non-vacuousness confirmed by reverting each half of the fix independently and observing the targeted tests genuinely fail — with both reverted, the end-to-end Host test reproduces the exact `Expected: Running, Actual: Faulted` crash `WP 13.11A` found — then restoring. `TD-53`/`TD-54` deliberately untouched: neither is a variant of this item, and neither is naturally or directly implicated by this fix. |
| TD-52 | `IIdentityService.EstablishCurrentPrincipal` called `ICurrentPrincipalAccessor.SetCurrent` directly, and `IIdentityService` is an ordinary, DI-public, un-denylisted interface — `PluginAssemblyLoader.NeverEligibleServiceResolveTypes` blocks only the two concrete accessor types by name, not any interface that internally reaches the same write surface. A plugin declaring `plugin.services.resolve:Tempest.Core.Identity.IIdentityService` (an innocuous-looking capability request) passed `EnforceTrust` cleanly, then could call `EstablishCurrentPrincipal` for any configured identity at runtime with no ownership check; because `ICurrentPrincipalAccessor` is deliberately not call-chain-scoped (`ADR-0044`), the effect persisted ambiently for every later, unrelated caller in the process. Live-PoC confirmed, `WP 13.10A`; since `WP 13.10C`'s own commit, confirmed genuinely gated | `WP 13.2A` (introduced — the denylist was scoped to concrete accessor types only, `IIdentityService` not considered as an indirect path to the same surface); found, `WP 13.10A`'s own Security/Trust reviewer, with a live proof-of-concept; fixed, `WP 13.10B` — `EstablishCurrentPrincipal` now gates via a new dynamic `IPermissionEvaluator.RequirePermission` check against a new capability key, `plugin.identity.establish` (`PluginCapability.IdentityEstablish`), mirroring `NavigationService.Register`'s own existing ADR-0111 gate exactly; independently re-verified, `WP 13.10C` (four fresh, read-only reviewers, each with its own live proof-of-concept including a real signed multi-tier plugin scenario; non-vacuousness confirmed by reverting the gate and observing the targeted test genuinely fail, then restoring) | N/A — already resolved | **Resolved, `WP 13.10B`; independently re-verified and committed, `WP 13.10C`.** Live-PoC-confirmed fixed: a plugin declaring `plugin.services.resolve:Tempest.Core.Identity.IIdentityService` and calling `EstablishCurrentPrincipal` for an arbitrary identity now requires the separate, explicit `plugin.identity.establish` capability grant, denied by the same static capability-ceiling check (`FindIneligibleCapability`) if requested outside its trust tier's ceiling (absent from `UnsignedLocalCapabilityCeiling` by design — never grantable at the lowest tier), and additionally gated dynamically at the call site itself. Every existing first-party caller (a genuine login/session-establishment flow) observes zero behavioural change, since the check is skipped entirely, not merely satisfied, when the ambient component principal is `null` or First-Party. **Governance note, disclosed by `WP 13.10C`'s own Security/Adversarial reviewer, not a code defect**: `plugin.identity.establish` is grantable to any trust tier above `UnsignedLocal` (including `VerifiedSigned`, not only `FirstParty`), and `EstablishCurrentPrincipal` accepts an arbitrary `identityId` with no ownership check by design (`ADR-0043`, no authentication) — granting this one capability to any signed third-party publisher is effectively full ambient-identity impersonation power (including impersonating a configured administrator identity), a materially broader blast radius than every other v1 capability. This is inherent to the capability's own necessary semantics, not a code defect requiring a fix, but warrants explicit governance sign-off before any third-party publisher is actually granted it. See `Plugin Trust & Isolation Architecture.md`'s own "Closed, `WP 13.10B`" Risks entry and `ADR-0111`'s own "Corrected, `WP 13.10B`" note for full detail. |
| TD-53 | `HostedServiceManager.StartServiceAsync`'s `catch` block classifies a start failure as Host-fatal only if `tracked.Instance is ICriticalBackgroundService` — but if `_serviceProvider.GetService(tracked.ServiceType)` itself throws (construction failure, before `tracked.Instance` is ever assigned), that check evaluates against `null` and is always `false`. A hosted service that would have been `ICriticalBackgroundService` had it constructed successfully is instead silently reclassified as non-critical and merely marked `Failed`, undermining `ADR-0021`/`ADR-0029`'s own guarantee that a critical service's failure faults the whole Host. Not plugin-trust-specific and not introduced by `WP 13.10B` — a general robustness gap in construction-failure handling, present since `HostedServiceManager`'s own original implementation — surfaced by `WP 13.11A`'s own Security/Adversarial reviewer because `TD-51`'s reopened Module-axis crash (above) is one concrete way to trigger a hosted-service construction throw, safely, on this axis | `HostedServiceManager`'s own original implementation (introduced, undetected); found, `WP 13.11A`'s own Security/Adversarial reviewer | A future Work Package capturing criticality from the hosted service's own declared `Type` (via reflection, before construction is attempted) rather than from the constructed `Instance`, so a construction-time throw is classified identically to a start-time throw | Open — disclosed explicitly; low severity, not plugin-trust-specific, no known exploitation. The failure is still isolated (no crash, no unrelated-service impact) — only its own criticality classification is wrong for this one specific throw timing. |
| TD-54 | `ITempestServiceProvider` is never registered back into the `IServiceCollection` it is built from (`TempestHost.cs`), so a plugin cannot presently obtain it via `plugin.services.resolve:*` and use it to reach `CurrentPrincipalAccessor`/`CurrentComponentAccessor` directly, bypassing `IdentityService`'s own capability gate (`TD-52`) entirely. Confirmed correct today, but this closure is *incidental* — a consequence of construction ordering — rather than an explicit, load-bearing entry in `PluginAssemblyLoader.NeverEligibleServiceResolveTypes` the way the two accessor types themselves are. A future, unrelated change registering the provider into the container for a legitimate reason would silently reopen this exact path with no test or review signal naming it | `TempestHost`'s own original composition-root wiring (introduced, undetected as a latent gap); found, `WP 13.11A`'s own Security/Adversarial reviewer, during re-examination of `TD-52`'s own closure | A future Work Package adding `ITempestServiceProvider` (and `IServiceCollection`) to `NeverEligibleServiceResolveTypes` explicitly, as defense-in-depth matching the denylist's own stated rationale — cheap, no behavioural change today, closes only a latent path | Open — disclosed explicitly; not currently exploitable (the provider is not DI-resolvable by any plugin today), a preventive hardening item, not a live gap. |
| TD-55 | `PluginAssemblyLoader.RecordDenied` records every `Type` a denied plugin's own fixed-point transitive scan reached, and `PluginDeniedTypeRegistry` is keyed on `Type` identity alone, with no notion of which plugin caused the recording. Because `DiscoverModuleTypes`'s own scan deliberately follows every assembly that enters the `AppDomain` as a side effect of that plugin's own type and constructor-parameter resolution (`WP 13.9.1`/`WP 13.9.3`), a malformed or deliberately-crafted plugin whose resolution force-loads a *different*, legitimate assembly — a shared dependency, or simply a second plugin's assembly not yet loaded — causes that other assembly's own `IModule`/`IHostedService` types to be recorded denied too. The victim then passes its own trust check and is recorded `PluginRegistryState.Loaded`, yet is silently dropped at `TempestHost.cs`'s own Module Registration and Hosted Service Registration filters, leaving registry state and runtime state inconsistent with only a `Warning` to distinguish them. A denial-scoping mechanism (recording per-plugin, and excluding a type only for the plugin that actually owns it) is the correct closure, but is an architectural change to the `WP 13.9.4` registry's own shape, not a defect fix | `WP 13.9.4` (introduced, latent — `RecordDenied`'s all-discovered-types shape and the registry's `Type`-only keying both date from there); newly *reachable* on the unresolvable-constructor-parameter denial path by `WP 13.11B`, which necessarily records that path's own discovered types where previously it recorded none at all; found, `WP 13.11B`'s own independent Adversarial Review, and disclosed rather than silently accepted | A future Work Package scoping denial recording per-plugin, so a plugin can never suppress another plugin's or a first-party module's types — deliberately not attempted in `WP 13.11B`, whose own commissioning instruction was an explicitly minimal fix stopping short of architectural change beyond existing seams | Open — disclosed explicitly; not currently exploitable in either shipped host, and the pre-existing capability-ceiling and constructor-conformance denial paths already carry the identical hazard. `Tempest.Samples`, the only other assembly carrying `IModule` implementers, is provably safe by composition ordering rather than by design: `WorkspaceManager`'s own constructor force-loads it (`_ = typeof(NavigationSampleModule).Assembly;`) before `RunAsync` is ever called, so it is always inside the scan's own per-step `before` snapshot and can never be enqueued. That is a side effect of App composition, not a property of the loader — which is precisely why this is tracked rather than assumed safe. `src/Plugins/` is empty today, so the multi-plugin case is presently dormant. Fail-closed in direction (a type is wrongly *excluded*, never wrongly included), so the over-recording half is an availability and diagnosability concern, not a trust-boundary escape. **Extended, `WP 13.11C`, to name the complementary under-recording half of the same attribution defect, which is *not* fail-closed and which this item's original wording concealed.** `DiscoverModuleTypes` takes its per-step `before` snapshot only once `LoadOne`'s own `Assembly.LoadFrom` has already returned, and the fixed-point scan enqueues an assembly only if it appears in that step's own before/after diff. An assembly already resident when a given plugin is scanned is therefore never attributed to that plugin at all — `WP 13.9.1` documented this deliberately, reasoning that such an assembly is resident "for an unrelated reason", but a *second plugin* is very much a related reason. Concretely: two plugins sharing a secondary assembly of the same identity, the first scanned pulls it in and (passing trust) binds its own component principal to that assembly's module types; the second, if denied, never re-reaches those types and so never records them denied. `WP 13.11C`'s own adversarial review could not construct an actual privilege escalation from this — the shared assembly's types run under the *passing* plugin's own grants, which is the designed behaviour for a legitimate multi-assembly plugin, and a later plugin cannot substitute its own copy because the default `AssemblyLoadContext` returns the already-loaded instance — so it is recorded here as attribution imprecision of the same root cause, not as a separate exploitable finding. Per-plugin denial scoping closes both halves at once; neither half is closed by anything short of it. |
| TD-56 | A plugin's own **constructor** executes outside its component scope, so plugin code runs during construction with a `null` ambient component principal — which every capability gate treats as genuine first-party and skips entirely. On both axes the scope is pushed *after* the instance is created: `ModuleLifecycleManager` resolves the instance (`tracked.Instance = ResolveInstance(tracked.Descriptor)`) and only then opens `using var scope = _componentScopeProvider?.Invoke(moduleId)` around the lifecycle method; `HostedServiceManager.StartServiceAsync` does the same, resolving via `_serviceProvider.GetService(tracked.ServiceType)` before opening its own scope. `PluginTrustPermission.IsFirstParty(null)` is `true` and `Rank(null)` is `3` — the top tier — so within the constructor window `NavigationService.Register`, `CommandRegistry.RegisterDescriptor`, `IEventBus.PublishAsync`, and `IdentityService.EstablishCurrentPrincipal` all skip their `RequirePermission` call and the plugin additionally wins every ownership-override tie against real first-party components. Exploitation requires the plugin to obtain an abusable collaborator as a constructor parameter, which `HasCompliantConstructor` restricts to the fixed baseline plus its own granted `plugin.services.resolve:*` keys — so an `UnsignedLocal` plugin **cannot** reach it (that tier's ceiling admits only `plugin.navigation.register`/`plugin.commands.register`, no `plugin.services.resolve:*` key at all, and the baseline is `ILogger`/`IConfigurationProvider`/`IDiagnosticsProvider`), but a `VerifiedSigned` or `FirstParty` plugin can: `IIdentityService` is DI-registered (`TempestHost.cs`), is not in `NeverEligibleServiceResolveTypes`, and is a recognised `plugin.services.resolve:*` shape, so a signed plugin granted only `plugin.services.resolve:Tempest.Core.Identity.IIdentityService` — and deliberately *not* `plugin.identity.establish` — can call `EstablishCurrentPrincipal` from its own constructor and set the process-wide current principal, defeating the exact gate `TD-52` exists to enforce, for the exact population `TD-52` constrains. The module must carry `[ModuleMetadataAttribute]` so Module Discovery reads its metadata instead of instantiating it (ADR-0027), leaving the DI-constructed lifecycle path as the first and only construction. **Most severe variant, because it outlives the window:** `EventBus.Subscribe` captures `var owner = _currentComponentAccessor?.Current` at subscribe time, and `PublishAsync` opens a scope only `when subscription.Owner is not null` — so a handler subscribed from a constructor is recorded with a `null` owner and is therefore dispatched with no component scope, running as first-party for the remaining lifetime of the process, long after the constructor returned | `ADR-0111`'s own component-scope hook as originally implemented (`WP 13.2A` for modules, extended to hosted services by `WP 13.10B` for `TD-52`) — both wired the scope around the *lifecycle method* only, which is what the ADR's own "each of that module's own lifecycle calls" wording describes; construction was never in scope on either axis, and no `WP 13.9.x`/`13.10x`/`13.11x` review examined the constructor window because every ambient-principal regression test probes `StartAsync`, which is correctly scoped. Found, `WP 13.11D`'s own Security/Adversarial exit reviewer; every link independently re-confirmed against real source before this entry was written | A future Work Package moving the component scope to enclose construction on both axes — opening `_componentScopeProvider?.Invoke(...)` *before* `ResolveInstance`/`GetService` rather than after, so the constructor runs under the same principal its lifecycle methods already do. The scope key (`moduleId`, `tracked.ServiceType`) is available before construction on both paths, so this is a statement-ordering change within the existing `ADR-0111` hook, not a new seam. `EventBus.Subscribe`'s owner capture then becomes correct for free. Must be paired with regression coverage that probes the **constructor**, not only `StartAsync` — the existing `AmbientPrincipalCaptureProbe` records inside `StartAsync` and so passes today regardless | Open — **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support.** (Wording normalised by `WP 13.12.0`: the earlier grade read "Release Blocking for third-party plugin support", which collided with `ADR-0106` §4's formal term "Release Blocking" — a term that admits no qualifiers and, if it applied, would force a `NOT READY` release verdict. The underlying technical classification is unchanged; this item does not block `v0.13.0` as shipped, and does not block WP14.) Dormant today: `src/Plugins/` contains only `README.md`, no third-party plugin exists, third-party plugin support is neither enabled nor advertised, and the path is unreachable without a plugin signed by a certificate already in `TrustedPublishers/` — an actor who by definition already cleared the platform's own publisher-trust bar. Causes no crash and no instability; the Host runs normally. Entirely orthogonal to UI/UX work, which is why `WP 13.11D` declares the platform ready for WP14 while tracking this: WP14 neither depends on this path nor makes it worse. Must be closed before third-party plugin support is actually enabled or advertised — the same framing `TD-51` carried, and for the same reason. Fail-**open** in direction, unlike `TD-55`: a plugin gains authority it was not granted, rather than losing authority it held, so this is a genuine trust-boundary escape and is graded accordingly. |
| TD-57 | `Tempest.Companion` ships with no Android/iOS platform heads — the single-view shell (`ISingleViewApplicationLifetime`-ready, `ADR-0113`) runs today only through its phone-frame desktop head, and the mobile form factor is proven by headless viewport tests (320dp–large) rather than on a physical device. The Work Package's own build environment could not acquire the .NET mobile workloads/Android SDK (network policy), so heads could not have been compiled or tested honestly. | WP 14.0A | WP that adds the mobile platform heads (requires a build environment with .NET mobile workloads) | **Open** — the shell, lifetimes, and `Program.BuildAvaloniaApp` are already shaped for heads to plug in. |
| TD-58 | The Companion cannot securely reach an off-box TempestOS host in production: it inherits the REST API's own disclosed identity/transport limitations unchanged (`TD-13` unverified `X-Identity-Id` header, `TD-14` no TLS, loopback-only bind, all deliberate, `ADR-0052`), so its real deployable reach today is the platform host's own machine or an operator-managed tunnel. Deliberately NOT worked around client-side — widening the bind or inventing client-side auth would create the exact unauthenticated off-box exposure `TD-13`/`TD-14` exist to prevent. | WP 14.0A | `FCR-0003` (real authentication) + `FCR-0004` (TLS) — the same triggers `TD-13`/`TD-14` already carry | **Open** — bounded by design; see `ADR-0113` and the `WP 14.0A` Companion Security Review. |

**Disclosed correction (`WP 13.2B` governance review):** this paragraph's
own leading figure had drifted stale at "10 Resolved, 1 Partially
resolved, 37 Open" since `WP 11.4A` — never re-incremented by `WP 13.1A`'s
own `TD-06` resolution or `WP 13.2A`'s own `TD-09`/`TD-10`/`TD-11`
resolutions, even though both Work Packages correctly updated this
register's own `Last Reviewed` narrative above. Re-verified here by a
direct count of every `TD-NN` row's own Status field (48 rows total, no
gaps, no duplicates): 16 Resolved, 1 Partially resolved, 31 Open —
matching the `Last Reviewed` narrative's own running total exactly.
**Total: 58 tracked debt items — 18 Resolved, 1 Partially resolved, 39
Open.** Updated `WP 14.0A` (TempestOS Companion): two items added,
`TD-57` (no mobile platform heads yet — environment could not acquire
mobile workloads) and `TD-58` (Companion off-box reach bounded by the
REST API's own deliberate `TD-13`/`TD-14` limitations until
`FCR-0003`/`FCR-0004`), 56 → 58 tracked, 37 → 39 Open — re-derived by a
direct count of every `TD-NN` row's own Status field, per this
paragraph's own standing recomputation rule. Previously: Updated `WP 13.11D` (`v0.13.0` Plugin Platform Exit Review): one
item added, `TD-56` (55 → 56 tracked; 36 → 37 Open) — a genuine, newly
found trust-boundary escape in which a plugin's own **constructor** runs
outside its component scope and is therefore treated as first-party by
every capability gate, defeating `TD-52`'s own gate for signed plugins.
Graded **Disclosed, Non-Blocking for `v0.13.0`; mandatory precondition to enabling third-party plugin support**, exactly as
`TD-51` was, and explicitly **not** blocking WP14 or `v0.13.0` as
shipped: it is dormant (`src/Plugins/` is empty, third-party support is
neither enabled nor advertised), unreachable below the `VerifiedSigned`
tier, causes no crash, and is orthogonal to UI/UX work. No status
changed; no item resolved or removed. Previously updated `WP 13.11B`: `TD-51` moved **Reopened → Resolved** — the
one Release-blocking finding `WP 13.11A` raised, closed by both of that
review's own recommended minimum-fix options, (a) and (b), and no wider:
the unresolvable-constructor-parameter-type denial path now records every
discovered type in `deniedTypeRegistry` before throwing (so `WP 13.9.6`'s
own Module Discovery filter genuinely excludes it) and Module Discovery's
own `CreateDescriptor` call gained the same fail-closed reflection guard
already used elsewhere in this codebase. One item added by the same Work
Package's own independent Adversarial Review: `TD-55` (54 → 55 tracked;
35 → 36 Open), the `Type`-keyed denial registry's own cross-plugin
suppression hazard — latent since `WP 13.9.4`, newly *reachable* on the
denial path `TD-51` closed, and disclosed rather than silently accepted;
closing it needs a per-plugin denial-scoping mechanism, which is an
architectural change this deliberately minimal Work Package stopped short
of. `TD-53`/`TD-54` deliberately left Open and untouched.
Previously updated `WP 13.11A`: `TD-51` moved Resolved → **Reopened** (its
own disclosed residual gap, previously characterised as safely isolated,
re-examined and found to be a genuine, empirically-confirmed Host crash
on the `IModule` axis — see its own Status cell above for the full
correction); two new items added directly by this same review, `TD-53`
and `TD-54` (both low-severity, disclosed, Open — neither plugin-trust-
specific, neither a variant of `TD-51`/`TD-52`). Two items added
`WP 13.10A` (`TD-51`–`TD-52`, both a genuine,
live-PoC-confirmed gap in the plugin trust platform's own static/dynamic
enforcement, neither a variant of anything the `WP 13.9.1`–`WP 13.9.6`
remediation chain closed), both moved Open → Resolved by `WP 13.10B`'s
own implementation, independently re-verified and committed by
`WP 13.10C` — `TD-51` itself since reopened, `WP 13.11A`, and re-resolved,
`WP 13.11B`, per above. Two further items added `WP 13.3A` (`TD-49`–`TD-50`, both
Open — sourced
directly from `WP 13.2B`'s own commit message, two plausible-but-not-
fixed findings that had never before reached a formal register entry;
`TD-09`/`TD-10`/`TD-11` independently re-confirmed Resolved by direct
source re-read, not taken on citation). Previously: three items added
`WP 11.4A` (`TD-46`–`TD-48`, all immediately
Resolved — the specific defects `WP 11.9.0`'s own Release Publication
Report found on `v0.11.0`'s first real CI execution, now fixed and
independently re-verified on real infrastructure; see `WP11.4A Release
Engineering Corrections.md`), and `TD-44` moved Open → Resolved in the
same Work Package (CI now genuinely executes successfully on real
infrastructure, both branch- and tag-push paths — the precondition
`TD-44` was tracking). Previously: four items added `WP 11.9.0`
(`TD-42`–`TD-45`, all Open at the time — found during `v0.11.0`'s own
release sign-off: two genuine, previously-undisclosed release-tooling
defects found by independent QA re-verification (`TD-42`, `TD-43`,
both still Open — distinct from `TD-46`/`TD-47`, different defects in
the same two files), and two already-disclosed, still-open
release-engineering gaps consolidated into formal register entries for
the first time rather than left as prose-only disclosure (`TD-44`,
`TD-45`) — see `WP11.9.0 Engineering Release Report.md`).
Previously: one item added `WP 10.7A` (`TD-41`, Open — found during
`WP 10.7A`'s own Object Editor test coverage, see `WP10.7A
Implementation Report.md` §5). Previously: one item added `WP 10.3A`
(`TD-40`, Open — see `WP10.3A
Implementation Report.md` §11). Previously: one item added `WP 10.2B`
(`TD-39`, Open — see `WP10.2B
Engineering Review.md` §4). **Disclosed correction, `WP 9.3A`:** this figure had read "29"
(before that Work Package's own `TD-32` addition) — stale since at
least `WP 9.2A`, silently un-recomputed by `WP 9.4A`'s own "29" edit
despite that Work Package's own real row count already being 31 at the
time. Re-verified here by a direct count of every `TD-NN` row actually
present in this file (`TD-01` through `TD-34`, sequential, no gaps, no
duplicates) rather than carried forward — corrected to the true total,
not silently. **Re-verified again, `WP 9.5A`:** a direct count
immediately before this Work Package's own `TD-33` addition confirmed
exactly 32 rows present (`TD-01`–`TD-32`), matching the register's own
stated total exactly — no drift found this time.

## Entries — Disclosed, Accepted Trade-offs (Not Expected to Need Fixing Unless a Real Need Emerges)

| # | Trade-off | Disclosed By | Revisit Trigger |
|---|---|---|---|
| AT-01 | No automatic unsubscription on module stop/dispose (Event Bus) | ADR-0028 / WP 4.4D | A real, demonstrated need |
| AT-02 | Subscriber references held strongly for the Event Bus's whole lifetime | ADR-0028 / WP 4.4D | A real, demonstrated need |
| AT-03 | Exact-event-type-only dispatch, no polymorphic dispatch (Event Bus). **`WP 6.2`**: `NotificationDispatcher` mirrors this same design (`ADR-0046`), so the identical limitation now also applies to Notifications — a caller must publish against the same static type a subscriber subscribed to; found in practice against this Work Package's own sample consumers (see `ADR-0046`) | RD-0021 / ADR-0028; re-confirmed for Notifications, `WP 6.2`/`ADR-0046` | A real, demonstrated need |
| AT-04 | No ongoing supervision/monitoring of a hosted service's own work after `StartAsync` returns | RD-0026 / ADR-0029 | A real, demonstrated need |
| AT-05 | No automatic restart/backoff for an isolated hosted service failure | RD-0029 / ADR-0021/ADR-0029 | A real, demonstrated need |
| AT-06 | `src/Plugins/` remains empty — no real plugin built yet | WP 4.2 (by design) | The first Work Package that ships a real plugin (see `Plugin Register.md`) |
| AT-07 | ~~Zero real hosted services exist beyond the infrastructure~~ | WP 4.5 (by explicit scope decision) | The first Work Package that ships a real hosted service (see `Hosted Services Register.md`) — **Retired, `WP 6.3`:** `RestApiHostedService` is a real, non-infrastructure `IHostedService` — a genuine, listening HTTP server, orchestrated by the exact same `IHostedServiceManager` this trade-off's own revisit trigger named (`ADR-0047`), exactly the Work Package `WP 6.2`'s own disclosure said this assignment belonged to. `NotificationSampleHostedService` (`WP 6.2`) remains real too, but `WP 6.3` is the Work Package whose own revisit trigger this item explicitly named in advance (`Required ADRs.md`'s `ADR-0047` entry), so the retirement is recorded here. |
| AT-08 | No persistent/durable notification model, no history or inbox capability — a notification is not retained after dispatch | WP 6.2 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Persistence Requirements state "None" for Notifications) | A real, demonstrated need for notification history (a UI Shell's own notification centre, an audit-adjacent record of what was shown to a user) |
| AT-09 | No delivery-channel abstraction (email, webhook, push) and no durable report history for Reporting — a generated report is handed back to its caller and is not retained or delivered anywhere by the service itself | WP 6.0 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Future Extension Points name both explicitly as future, not current, scope) | A real, demonstrated need for one or both (a future engineering module or `WP 6.3`'s own REST API naming a concrete delivery or history requirement) |
| AT-10 | `IApiEndpointRegistry.MapCommand` carries no request-parameter binding — every REST-exposed command dispatches only its own parameterless `CreateDefault` factory instance; an inbound request's own body or query string is never threaded into the invoked command | WP 6.3 (by explicit, approved-contract scope decision — `Public Interface Catalogue.md`'s own `MapCommand` signature carries no binding mechanism) | A real, demonstrated need for a REST-exposed command that requires caller-supplied parameters beyond what a fixed default instance provides |
| AT-11 | No compression or encryption of exported artifact content; an `IExportable` implementation is individually responsible for redacting or refusing to export sensitive content — `IExportService`/`IImportService` impose no content-level policy of their own, mirroring how Persistence imposes none on what Settings/Audit choose to store | WP 6.7 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Future Extension Points name both compression and encryption explicitly as future, not current, scope; Security Considerations name redaction as each `IExportable`'s own responsibility) | A real, demonstrated need for either (a concrete deployment scenario naming artifact confidentiality or size as a requirement) |
| AT-12 | `IImportService.ImportAsync` rejects (never upgrades or downgrades) an artifact section whose schema version does not exactly match the currently registered `IImportable` — no migration path exists for an older, still-readable schema version | WP 6.7 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Versioning Policy states an incompatible version "must reject... not silently downgrade or upgrade"; Future Extension Points name a migration path explicitly as future scope) | A real, demonstrated need for reading an older schema version (a concrete, shipped schema version bump with a genuine backward-compatibility requirement) |
| AT-13 | No remote validation/activation, no floating/seat-based licensing, and no license-renewal/grace-period model — `ILicense` is immutable for the life of the running process; a license change requires a full Host restart | WP 6.6 (by explicit, approved-contract scope decision — `Platform Service Contracts.md`'s own Future Extension Points name all three explicitly as future, not current, scope) | A real, demonstrated need for any of the three (a concrete deployment scenario naming remote activation, concurrent-seat limits, or graceful expiry handling as a requirement) |
| AT-14 | The compile-time dimension-safety guarantee (`Quantity<TDimension>`'s generic constraint prevents cross-dimension arithmetic/conversion) is verified by direct inspection — a demonstration class documenting the exact `CS1503`/`CS0019` errors reproduced against this repository's own compiler — rather than an automated compiler-error assertion, which would require a new Roslyn-scripting test dependency this framework does not otherwise need | WP 7.1B/`ADR-0054` (`WP7.0C Testing Strategy.md`'s own "Additional: compile-time rejection test" category named the guarantee; this Work Package chose disclosed manual verification over new test infrastructure) | A second framework independently needing the same kind of compile-time guarantee verified, making shared Roslyn-scripting infrastructure worth its own dependency cost | None — by design, this is not expected to change unless a second, independent need for the same kind of test justifies the added infrastructure. |
| AT-15 | `IMaterialCatalog` performs no permission-gating of its own — a caller registering or revising a material is expected to enforce authorization at the calling layer (mirroring `IReportingService`/`INavigationProvider`'s own precedent), even though `WP7.0C Engineering Foundation Contracts.md`'s own Platform Services Consumed line named "Identity & Permissions (registration authorization)" | WP 7.1C/`ADR-0055` | A real, demonstrated need for authorization enforcement specifically inside `IMaterialCatalog` itself, rather than at each caller's own calling layer | A real, demonstrated need for framework-internal authorization, rather than calling-layer enforcement — not assumed to exist yet. |
| AT-16 | `CalculationContext.ReferenceMaterial` accepts any string — the Calculation Framework does not verify a referenced `materialId` actually exists in `Tempest.Core.Materials`, and has no dependency on that framework at all | WP 7.1D/`ADR-0056` Decision 6 | A real, demonstrated need for the Calculation Framework itself to validate material references, rather than trusting the calling definition | A real, demonstrated need for framework-internal reference validation — not assumed to exist yet; mirrors `EngineeringData.DocumentReference.RelationshipKind`'s own open-string precedent. |
| AT-17 | `VerificationContext.ReferenceMaterial` accepts any string — the Verification Framework does not verify a referenced `materialId` actually exists in `Tempest.Core.Materials`, and has no dependency on that framework at all | WP 7.1E/`ADR-0057` Decision 5 | A real, demonstrated need for the Verification Framework itself to validate material references | A real, demonstrated need for framework-internal reference validation — not assumed to exist yet; identical precedent to `AT-16`. |
| AT-18 | Undo/Redo (`IUndoRedoStack`, `ADR-0098`) is wired only for Rename (all six disciplines) and the new Favourite/Un-favourite toggle — Create/Delete/Duplicate/Move and every Set-Status/Set-Priority/Set-Owner command have no Undo entry; Delete specifically has no defined inverse (already a soft delete, no "restore" operation anywhere in this platform) | WP 10.6A/`ADR-0098` (by explicit, disclosed scope decision — this Work Package's own time budget) | A real, demonstrated need for broader Undo coverage, or a real "restore a soft-deleted object" capability being built for an unrelated reason first |
| AT-19 | `IBackgroundTaskRunner` reports coarse state (Running/Succeeded/Failed/Cancelled) and elapsed time only, never a percentage — no `ICommandHandler<TCommand>` anywhere in this platform reports incremental progress; that frozen Command Framework contract carries no `IProgress<T>` parameter | WP 10.6A (by explicit, disclosed scope decision — extending `ICommandHandler<TCommand>` would be a Command Framework-wide change, out of this Work Package's own scope) | A real, demonstrated need for percentage progress on a genuinely long-running command handler — none exists yet |
| AT-20 | A Macro step (`RunMacroCommand`, `ADR-0099`) must be a `CommandDescriptor` with `CreateDefault` set — confirmed by repository-wide `grep`, only `Tempest.Samples` commands qualify today; no real Engineering discipline command is invokable by Id alone (the identical, pre-existing platform-wide limitation `CommandPaletteOverlay`'s own remarks already document) | WP 10.6A/`ADR-0099` (pre-existing platform-wide limitation, not introduced by this Work Package; `AT-10`'s own identical root cause) | A real, demonstrated need for real discipline commands to become Id-invokable — its own, larger design/implementation effort |
| AT-21 | `CommandHistoryLog` records only what already reaches `MainWindow`'s own existing `ActionCompleted`-shaped UI surfaces (Ribbon, Explorer, Inspector, Object Editor, Undo/Redo), never a global `ICommandDispatcher` interception (frozen, unmodified); `succeeded` is a disclosed heuristic, inferred from whether the recorded message contains "fail," since these surfaces carry only a human-readable string, not a structured `CommandResult` | WP 10.6A (by explicit, disclosed scope decision — every currently-real dispatch path already funnels through one of the recorded surfaces) | A command dispatched through a future path that bypasses every recorded surface, or a real, demonstrated need for audit-grade completeness |
| AT-22 | `IUndoRedoStack`/`CommandHistoryLog` are both session-only — neither is persisted across a restart, matching most desktop applications' own established convention | WP 10.6A (by explicit, disclosed scope decision) | A real, demonstrated need for cross-session Undo/Redo or Command History |
| AT-23 | `KeyboardCommandBindingProvider` (`ADR-0100`) ships with zero default bindings and no end-user remapping UI; `IExternalControllerProvider` has zero real vendor integration (no Stream Deck plugin, no MIDI device support) — proven only against a test-only `StubExternalControllerProvider` | WP 10.6A/`ADR-0100` (by explicit Out-of-Scope instruction — no vendor SDK, no hardware integration) | A real, demonstrated need for either a remapping UI or a real vendor SDK integration — the latter requires its own commissioning decision |
| AT-24 | The Companion has no offline write queue — a quick action either reaches the authoritative platform now or fails visibly (`ADR-0115`): the platform carries no synchronisation/idempotency/conflict semantics to replay against, and a client-side queue would manufacture a second source of truth on the least trusted device | WP 14.0A (by explicit, disclosed design decision) | Real platform synchronisation semantics (`FCR-0022`/`FCR-0023`) |

**Total: 24 disclosed trade-offs (1 Retired, 23 active), none of the
active twenty-three requiring action absent a real, demonstrated need.**
Updated `WP 14.0A`: `AT-24` added (no Companion offline write queue, by
design — `ADR-0115`), 23 → 24 disclosed, re-derived by direct row count.

## Cross-Reference Check

Every item above is traceable to a specific retrospective or ADR cited in
its own row. TD-08's resolution is cross-checked directly against
`Risk Register.md`'s R1/R4 (both Retired on the same date, by the same
Work Package) — consistent, no discrepancy. No debt item was found in any
retrospective that is missing from this consolidated list.
