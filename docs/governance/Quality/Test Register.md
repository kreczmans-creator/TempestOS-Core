# Test Register

## Register Metadata

| Field | Value |
|---|---|
| **Register Name** | Test Register |
| **Purpose** | The index of the test suite by subsystem — file counts, test-method counts, and which platform capability each directory covers. |
| **Scope** | Every `.cs` file under `tests/Tempest.Core.Tests/`. |
| **Owner** | Project Maintainer. |
| **Source of Truth** | `tests/Tempest.Core.Tests/` (direct inspection); `dotnet test` output. |
| **Review Frequency** | Updated whenever the test suite's total count changes materially (in practice, every Work Package). |
| **Last Reviewed** | 2026-08-30 (Product Gap Reconciliation audit — no feature work) — **`Tempest.Core.Tests` 2773/2773**, **`Tempest.Desktop.Tests` 325/325**, Debug and Release, 0 warnings, 0 errors. **+6 new**, all audit evidence, in `tests/Tempest.Desktop.Tests/ProductGapReconciliationAuditTests.cs` (new file): every project area declared `Implemented` is driven through the real shell and asserted to render something other than a `DeclaredCapabilityView` and to name no tracking debt; every `Declared` area is asserted to render the card and to name what tracks it; the same check one level up for shell modules; the shell Tasks descriptor is asserted not to deny the project Tasks surface that exists; the `TD-75` coupling is stated as it is — six discipline explorer areas and five engineering calculations declared in `Tempest.Samples`; and fictional `SAMPLE-*` objects are confirmed present in the user's own object graph in a real launch. The tables are read at run time, so an area added later is covered without anyone remembering. **Three mutations run, three killed**: a `Declared` area re-labelled `Implemented` (the `TD-102` defect re-introduced), an `Implemented` area losing its surface, and an unbuilt area tracked by nothing. Two of the six tests failed on first run and both were the test's own precondition, not a product defect — Project Explorer root nodes are scoped to the selected navigation area, and `NavigationService` is not resolvable by its concrete type — corrected rather than asserted around. Zero tests weakened or deleted. Previously reviewed 2026-08-29 (Project Tasks & Delivery Workflow, `TD-81` partially) — **`Tempest.Core.Tests` 2773/2773**, **`Tempest.Desktop.Tests` 319/319**, Debug and Release, 0 warnings, 0 errors. **+34 new:** 25 in `tests/Tempest.Core.Tests/Projects/ProjectTaskTests.cs` (new file — the task family's own work state and its mapping onto the canonical lifecycle, reopening finished work **asserted alongside the canonical table's refusal of Released → Draft** so the test fails if anyone loosens that rule, project membership by the parent chain including three levels of nesting, project isolation, an empty project keeping every board column, standalone tasks staying out of every project's register, assignment through the principal boundary with two different principals, unassignment, overdue only while open, editing keeping prior wording as history, milestone/deliverable links and the date a deliverable reports, and **a test that reads back what the state store actually holds** after every kind of edit), and 9 in `tests/Tempest.Desktop.Tests/ProjectTaskAcceptanceTests.cs` (new file — the full journey through the real `MainWindow`: project → Tasks → create through the button and dialog → assign → move → **relaunch into a second `WorkspaceHost` over the same store** → the task is still there with its state and assignee → reopen → edit; plus a due date surviving a relaunch and reaching the Cockpit's Overdue Actions card, project scoping across two projects, a task nested three levels down, only-permitted-moves offered as buttons, Documents and Requirements still working, and a **rendered-layout test** that shows the window, runs a real layout pass and asserts the task card and every action button have non-zero bounds). **Thirteen mutations run, thirteen killed** across project filtering, persistence, assignment, status transitions, task creation, rehydration, the board and the milestone link. One survived first time and was the most useful result in the pass: making `ChangeWorkStateAsync` skip its own persist call broke nothing, because every assertion read the in-memory object it had just changed — which cannot distinguish "saved" from "set on this instance". Closed by the state-store read-back test above, after which the mutation is killed. Two test-fragility findings fixed rather than worked around: an assertion held a stale object reference across `ReviseAsync` (which returns a new instance by design), and `OfType<T>().Single()` fails on a **shown** window because a `TabControl` materialises the selected tab's content through its presenter as well — one control enumerated twice, not two controls. Zero tests weakened or deleted; one existing Cockpit test renamed, because its name called Overdue Actions an honest placeholder and that is no longer true. Previously reviewed 2026-08-29 (Production Rehydration & Principal Boundary, `TD-103`/`TD-104`/`TD-75`) — **`Tempest.Core.Tests` 2748/2748**, **`Tempest.Desktop.Tests` 310/310**, Debug and Release, 0 warnings, 0 errors. **+74 new in this Work Package:** 42 in `tests/Tempest.Core.Tests/EngineeringDomain/ProductionRehydrationTests.cs` (new file — every one of the 38 production Kinds resolving to the right type through a registry built from the production composition path alone, the registration set proven to declare no type from `Tempest.Samples`, a reflection sweep asserting that every `EngineeringObjectBase` implementing `IRehydratable<>` has a rehydrator — the check that would have caught `TD-104`'s nine orphans — and the whole registration run twice to prove no two paths claim the same Kind), 24 in `CanonicalKindRoundTripTests.cs` (new file — all 21 canonical Kinds driven create → persist → **a second lifetime over a real `PersistenceStore` on disk registering only the production path** → rehydrate, each comparing the **full captured state** before and after including every `TypeState` key, plus a Project → Part → Risk nesting whose parent edges survive, all 21 recovered together in one restart, and an unregistered Kind reported as unknown rather than silently dropped), and 8 in `tests/Tempest.Desktop.Tests/PrincipalBoundaryTests.cs` (new file — the real `WorkspaceHost` establishing a principal and publishing it to `ICurrentPrincipalAccessor`, work the user creates attributed to it rather than to `unknown`, `verification.read` held so first-party surfaces read instead of refusing, a substituted source replacing the principal with no change to the domain, a null-returning source leaving authorship honestly `unknown`, and two standing guards that no authentication system and no per-object user field have appeared). **Seven mutations run, seven killed** — dropping one orphan Kind's registration, binding a Kind to the wrong type, discarding unknown Kinds silently, never establishing a principal, inventing a person when the OS gives no name, and dropping `verification.read`. The seventh, `M4`, reproduced a **real defect this work had introduced**: publishing only a non-null principal left a sample module's principal standing in a session that should have had none. The acceptance test caught it first, against correct-looking code; the fix was to make the boundary authoritative in both directions. Zero tests weakened or deleted. **Register gap corrected here:** this register's previous figures (2670 Core, 287 Desktop) were `TD-80`'s — the intervening Project Documents & Requirements Work Package (commit `4bd6140`) updated `PROJECT_STATUS.md` but not this register, and contributed +12 Core and +15 Desktop. The measured pre-work baseline was therefore 2682/302, confirmed by running the suite with this Work Package's two new Core files removed, and this Work Package's own contribution is +66 Core and +8 Desktop. Reconciled against a direct run rather than by trusting the arithmetic. Previously reviewed 2026-08-29 (Drawing / Document Viewer, `TD-80`) — **`Tempest.Core.Tests` 2670/2670**, **`Tempest.Desktop.Tests` 287/287**, Debug and Release, 0 warnings, 0 errors. **+77 new:** 19 in `tests/Tempest.Core.Tests/Workspace/Viewing/DocumentViewportTests.cs` (new file — fit on the constraining axis, centring, edge clamping, anchored zoom keeping the point under the pointer, zoom limits, resize keeping a fitted view fitted and a zoomed view where the user put it, and degenerate/NaN sizes), 36 in `DocumentViewSessionTests.cs` (new file — one-based page numbers, clamping at both ends, re-fit on page turn, the three unavailable states, and format detection preferring magic bytes over the declared content type), 14 in `tests/Tempest.Desktop.Tests/DocumentPageSourceTests.cs` (new file — real page counts and per-page sizes from a three-page PDF with different page geometries, rasterisation at the requested scale, **non-white pixel counts proving the vector content actually rendered**, the raster cap, and text pagination), and 8 in `DocumentViewerAcceptanceTests.cs` (new file — attach → restart → open → zoom → pan → page through, two documents as ordinary panels, reopening bringing the tab forward, project context preserved across open and close, and the Missing/Corrupt/Unsupported states each reached through the real launcher). **Eight mutations run, eight killed.** Two tests failed against correct code and were fixed by probing rather than by relaxing the assertion: the headless platform decodes every image to 1x1 including garbage (`TD-100`), and `RenderTargetBitmap.Save` writes zero bytes headlessly, so a generated "PNG" helper was an empty array. Zero tests weakened or deleted. Previously reviewed 2026-08-29 (Attachment Content Storage, `TD-31`) — **`Tempest.Core.Tests` 2615/2615**, **`Tempest.Desktop.Tests` 265/265**, Debug and Release, 0 warnings, 0 errors, and **no environment carve-outs**: the two long-standing Linux-only failures were cleared in the preceding pass, one of them (`TD-94`) having turned out to be a real production defect rather than an environment case at all. **+47 new:** 23 in `tests/Tempest.Core.Tests/Persistence/BinaryPersistenceStoreTests.cs` (new file — byte fidelity for every document type the workflow names, a 4 MB record, every one of the 256 byte values, empty-versus-absent, reserved device names inherited from `TD-59`, overwrite leaving no tail of a longer previous value, concurrency, and the architectural assertion that one store instance satisfies both shapes), 20 in `tests/Tempest.Core.Tests/EngineeringDomain/AttachmentContentStoreTests.cs` (new file — round-trip and verification per file type, tampered content, truncated content, a metadata size that disagrees with the bytes, the unverifiable pre-`TD-31` record, replacement, deletion, isolation between attachments, and survival across a new store instance), and 4 in `tests/Tempest.Desktop.Tests/AttachmentContentAcceptanceTests.cs` (new file — create → attach → persist → restart → retrieve through the real `WorkspaceHost` and the real `AttachDocumentCommand`, a three-lifetime journey attaching a second file to a rehydrated object, the metadata-only attachment that must keep working and report its content missing, and the absent-attachment read). Payloads are structurally real — a PDF with its high-byte comment line, a PNG with real CRCs, the ZIP container a `.docx` is, a JPEG, a CSV with a BOM — plus a blob carrying every byte value twice, because an empty array round-trips perfectly through a store that would lose every real file. **Seven mutations run, seven killed**, including "record the metadata but never store the bytes", which every Core test passed and only the restart journey caught. Zero tests weakened or deleted. Previously reviewed 2026-08-29 (Full Workspace Layout & Docking, `TD-72`) — **`Tempest.Core.Tests` 2567/2569**, **`Tempest.Desktop.Tests` 259/259**, 0 warnings, 0 errors. The workspace docking replacement is proven by 119 tests: 81 in `tests/Tempest.Core.Tests/Workspace/Layout/` running with **no UI in the process** (the layout tree's structure, operations, normalisation and invariants; drop-zone geometry; versioned serialisation and every way a stored layout can be wrong; the tree-based presets; and preference migration for a returning user), plus 25 in `WorkspaceLayoutHostTests.cs` over the renderer and 13 in `WorkspaceLayoutControllerTests.cs` over drag-to-dock, floating windows and persistence. Three suites were **deleted with the types they tested** — `DockingGridTests`, `PanelHostControlTests`, `PredefinedLayoutsTests` — and every guarantee they held was re-proven against the new host rather than carried across, which is deliberately more work than adapting the assertions and the only way to know the behaviour survived; `ResponsiveWorkspaceTests` was trimmed to its ribbon half, its `TD-70` side-dock guarantee moving to `WorkspaceLayoutHostTests` where it now holds for any arrangement rather than three named docks. **Eleven mutations run, eleven killed** — one survived the first attempt and exposed a real coverage gap (no test exercised a split reducing to a single child, the case that accumulates one-child wrappers over a session); three tests were added and the mutant then died. The two `Tempest.Core.Tests` failures remain the pre-existing environment cases, unchanged and unrelated. Previously reviewed 2026-08-29 (Project-Centric Convergence, `TD-89`) — **`Tempest.Core.Tests` 2486/2488**, **`Tempest.Desktop.Tests` 262/262**, 0 warnings, 0 errors. +28 net new: 17 in `tests/Tempest.Core.Tests/Shell/ProductConvergenceTests.cs` (new file — the navigation model's declared-vs-implemented invariants, declared modules as real destinations that never close the open project, transitive project membership, project isolation, both engineering scopes, project switching, and the deliberate separation of project domain data from application session state), 7 in `tests/Tempest.Desktop.Tests/ProductConvergenceAcceptanceTests.cs` (the five mandatory journeys through the real `MainWindow`, including the standalone-calculation journey that was impossible before this pass, plus rail and project-tab surface checks), 3 in `tests/Tempest.Desktop.Tests/ShellDockingSeamTests.cs` (Phase 7 — the module-host seam, and the docking grid's absence from the tree outside Engineering), and 1 further `ProductSpineTests` case. Two tests that encoded the superseded "Engineering only from a project" rule were **inverted and strengthened**, not deleted: `EngineeringWithNoProjectOpen_EntersTheStandaloneScope_NeverThrows` and `TheRailEntersStandaloneEngineering_WhenNoProjectIsOpen`. Six mutations run and all six killed. The two `Tempest.Core.Tests` failures remain the pre-existing environment cases (POSIX advisory locking; a case-sensitive filesystem), unchanged and unrelated. Previously reviewed 2026-08-28 (`TD-85` closure audit) — **`Tempest.Core.Tests` 2468/2470**, **`Tempest.Desktop.Tests` 252/252**, 0 warnings, 0 errors. +4 over the implementation pass: four regression tests in `EngineeringObjectRehydrationTests` covering the one material defect the audit found — `IHasRevisions.ReviseAsync` carried only the structural half of an object's state onto its revised instance, which `TD-85` had turned from an in-memory reset into durable lifecycle-history loss (lifecycle and history carried; attachments carried; the `WP 9.0B` structural behaviour unchanged; and a further mutation after a revision never overwriting the persisted lifecycle). Two further mutations run and killed, bringing this work's total to fifteen run and fifteen killed. The two `Tempest.Core.Tests` failures were confirmed to fail identically at `37788a0` in an isolated worktree, and are therefore pre-existing environment cases, not regressions. Previously reviewed 2026-08-28 (Engineering Object Rehydration, `TD-85`) — counts re-verified directly via a fresh `dotnet test` run against both suites: **`Tempest.Core.Tests` 2464/2466**, **`Tempest.Desktop.Tests` 252/252**, 0 warnings, 0 errors. The two `Tempest.Core.Tests` failures are the same two pre-existing, environment-dependent Linux cases this branch has carried since the finding-closure pass (`PersistenceStoreTests.DeleteAsync_FileLockedByAnotherHandle_...` — POSIX advisory locking does not make an open handle block a delete; `PluginTrustStoreTests.Constructor_TempestOSCerFileNameIsCaseInsensitive_...` — a case-sensitive filesystem), unrelated to this pass and unchanged by it. **+27 net new:** 24 in `tests/Tempest.Core.Tests/EngineeringDomain/EngineeringObjectRehydrationTests.cs` (new file — durable state at creation, every mutable facet restored, attachment identity preserved, lifecycle history in order with actor and time, deletion, revision history, a rehydrated object still revising itself into its own type, relationships re-indexed with durable provenance, every Mechanical Kind round-tripping to its own registered type, type-specific state for SubAssembly/Configuration/metadata, and the four honest-failure paths: unregistered Kind, orphaned state, corrupt record, already-live object) and 2 in `tests/Tempest.Desktop.Tests/ObjectRehydrationAcceptanceTests.cs` (new file — the 18-step Definition-of-Done journey through the real `MainWindow` across three application lifetimes, and the relaunch case proving a Verification record stays a result leaf once its link is re-indexed); the remaining net change is the Product Spine's own restart tests, rewritten in place to run through real rehydration rather than the removed `Projects.Index`. Zero tests were weakened or deleted to obtain a green result. Thirteen mutations were run against the new persistence and rehydration paths and **all thirteen were killed** (see this pass's own report). Previously reviewed 2026-08-12 (WP 12.3B, Fault-Injection Validation Framework Implementation) — counts re-verified directly via a fresh `dotnet test` run against `tests/Tempest.Core.Tests/`: 2031/2031 passing (Debug), +5 net new since this field's own last recorded total (2026): 2 new (`FaultInjectionModuleDiscoveryTests.cs`, new file, `Modules/`) + 3 new (`ReflectionFrameworkDiscoveryServiceTests.cs`, extended in place, `Modules/`) proving the `IFaultInjectionModule` default-exclusion filter (ADR-0102); zero test methods removed — `NavigationSampleModuleIntegrationTests.cs`/`ClockModuleDiscoveryTests.cs` were edited in place (updated assertions/counts for the moved module), not shrunk. This register's own per-directory breakdown table's broader staleness (disclosed below, unchanged since `WP 5.3`) is out of this Work Package's own scope, per the same precedent. Previously reviewed 2026-08-11 (WP 11.3B, Presentation Strategy Implementation) — **narrow correction only, not a full re-derivation**: the `Shell/` row in the already-disclosed-stale (since `WP 5.3`) per-directory breakdown table below marked retired, reflecting `TempestShell`/`PlaceholderPage`'s removal (`ADR-0101`). The table's own broader staleness (frozen at a `WP 5.3`-era snapshot while real executed-test counts have since reached 2,228 — confirmed by a real `dotnet test` run this Work Package) is unchanged and remains out of this Work Package's own scope, per the prior disclosure below. Previously reviewed 2026-08-05 (WP 9.1B, Development Baseline Consolidation) — counts re-verified directly via a fresh `dotnet test` run against the consolidated working tree; 1808/1808. **Disclosed correction**: this field was not updated by either `WP 9.0B` or `WP 9.1A` at the time — a genuine omission during those two Work Packages, found and corrected here as part of this Work Package's own governance-consistency verification, not a new staleness introduced by `WP 9.1B` itself. `WP 9.0B` added 43 tests (1695 → 1738): 16 `BillOfMaterialsTests.cs`, 4 more `StructuralMutationTests.cs` (`ReviseAsync` regression), 10 for three new commands, 7 for extended node/facet providers, 6 more `MechanicalWorkspaceIntegrationTests.cs`. `WP 9.1A` added 70 tests (1738 → 1808): 32 Domain (`RequirementsLifecycleExtensionsTests.cs`, `RequirementValidationServiceTests.cs`, both new), 12 `SelectionServiceTests.cs` (multi-selection), 10 `EngineeringCockpitTests.cs` (Requirements KPIs), 16 `RequirementsWorkspaceIntegrationTests.cs` (new). **Disclosed, not fixed**: this register's own per-directory breakdown table below has not been re-derived since `WP 5.3` (552 tests) — a large, genuine staleness spanning `v0.6.0`/`v0.7.0`/`v0.8.0`/`v0.9.0` in their entirety, out of this Work Package's own scope to backfill; only this field's own top-line total is corrected here. Recommended as a future governance health-check candidate (`FCR-0005`). Previously reviewed 2026-08-05 (WP 9.0A, Mechanical Product Structure) — counts re-verified directly via four fresh `dotnet test` runs (two Debug, two Release, both via `src/TempestOS.slnx`); 1695/1695, 64 new tests: `EngineeringDomain/` extended with `StructuralMutationTests.cs` (13); `Workspace/` extended with `MechanicalNodeProviderAndFacetsTests.cs` (17), `MechanicalCommandsTests.cs` (21), `MechanicalWorkspaceIntegrationTests.cs` (7), and 6 new `WorkspaceManagerTests.cs` cases (`RegisterFacetProvider`); `Samples/` unaffected in count (`ClockModuleDiscoveryTests.cs` updated in place, module count 22→24, no new test method). |
| **Related Documents** | `docs/releases/v0.4.0/Testing.md`; `Validation Register.md`; `Repository Metrics Register.md`. |
| **Related ADRs** | None directly. |
| **Related Academy Articles** | `docs/academy/06 Engineering Standards/02-testing-strategy.md`. |
| **Coverage Status** | Complete. |

---

## Entries

| Directory | Files | `[Fact]`/`[Theory]` Attributes | Covers |
|---|---|---|---|
| `BackgroundServices/` | 4 | 37 | Hosted service discovery, manager, fixtures (WP 4.5) |
| `Commands/` | 5 | 54 | `ICommand` contract (WP 4.0); `CommandDispatcher`/`CommandRegistry`/`CommandHandlerTable` — registration, duplicate rejection, dispatch, failure propagation, cancellation, logging, thread safety, DI lifetime, shared-state sharing (WP 5.1B) |
| `Configuration/` | 4 | 31 | Configuration Framework (WP 2.5) |
| `DependencyInjection/` | 3 | 22 | Custom DI container (WP 2.4) |
| `Diagnostics/` | 1 | 9 | `IDiagnosticsProvider`/`DiagnosticsProvider` — live accessor projection, empty-before-attached/populated-after-attached, construction validation (WP 5.2) |
| `Events/` | 4 | 27 | Event Bus (WP 4.0 contracts, WP 4.4D bus) |
| `Logging/` | 10 | 53 | Logging & Diagnostics Framework (WP 2.6); extended `WP 5.2` with `CompositeLogSinkTests.cs` — fan-out, per-child failure isolation, `Logger` integration |
| `Modules/` | 13 | 74 | Discovery, Registration, Lifecycle, Module SDK, `ModuleMetadataAttribute` (WP 2.1–2.3, WP 4.1, WP 4.4A/B); extended `WP 5.3` with 2 tests proving a module with no `[ModuleMetadata]` and no parameterless constructor now fails with a clear `ModuleDiscoveryException`, not a raw `MissingMethodException` |
| `Navigation/` | 2 | 31 | `NavigationItem`, `NavigationService` — registration, ordering, hierarchy, visibility, events, DI (WP 5.0B) |
| `Plugins/` | 6 | 21 | Plugin manifest, discovery, loading (WP 4.2); extended `WP 5.0B` with a Navigation-registering dynamic plugin assembly builder; extended `WP 5.0S` with 2 `AssemblyFileName` path-containment regression tests |
| `Runtime/` | 5 | 50 | `TempestHost`, `TempestHostBuilder`, plugin/hosted-service Host integration; extended `WP 5.0D` with `ITempestHost.Services` availability, resolution, and Discovery/Registration/Lifecycle non-exposure tests |
| `Samples/` | 7 | 55 | `ClockModule`/`ClockLifecycleObserverModule` pipeline and event integration (WP 4.3, WP 4.4E); `NavigationSampleModule` and companions, module/host/plugin integration (WP 5.0B); `CommandSampleModule`, module/host/plugin integration and Navigation-integration proof (WP 5.1B); `DiagnosticsSampleModule`, module/host/plugin integration and the disclosed "zero hosted services during Initialise" finding (WP 5.2) |
| `Shell/` | 0 — **retired, `WP 11.3B`** | 0 | Formerly `TempestShell`, `PlaceholderPage` — composition, Navigation/Content rendering, page selection, unknown-page placeholder, real Host/sample-module integration, full interactive sessions (WP 5.0D). Both the production classes and this entire test directory were retired `WP 11.3B` (`ADR-0101`) — `TempestShell` had been unreachable from any running entry point since `ADR-0068` (`WP 8.1A`, `v0.8.0`), confirmed dead before removal. |
| `Templates/` | 3 | 6 | The `dotnet new tempest-module` template (`WP 5.3`) — `template.json` manifest validity; the template's own file content, substituted, built with the real compiler, and proven discoverable by the real, unmodified `ReflectionFrameworkDiscoveryService` |
| `Versioning/` | 2 | 17 | Platform Version infrastructure (WP 4.2A) |

**Total: 71 test files, 518 `[Fact]`/`[Theory]` attribute occurrences.**

## Reconciling Attribute Count Against Executed Test Count

`dotnet test` reports **552** executed tests, 34 more than the 518 raw
`[Fact]`/`[Theory]` attribute occurrences above. This difference is
**Verified** to be `[Theory]` methods with multiple `[InlineData]` rows
executing as multiple tests at runtime from a single attribute occurrence
— for example, the Plugin Manifest test suite's missing-required-field
theory (one `[Theory]` attribute, five `InlineData` rows, one per
required field), `NavigationItemTests`' own two `[Theory]` methods
(invalid `Id`/`Title`, three `InlineData` rows each), `PlaceholderPageTests`'
own two `[Theory]` methods (invalid title/message, three `InlineData`
rows each), `TempestShellTests.HandleInputAsync_InvalidSelection_ReportsInvalid_AndReturnsTrue`
(one `[Theory]` attribute, four `InlineData` rows), `CommandDescriptorAndResultTests`'
own three `[Theory]` methods (invalid `Id`/`DisplayName`/failure message,
three `InlineData` rows each, `WP 5.1B`), and, as of `WP 5.3`,
`ModuleTemplateManifestTests.TemplateManifest_DeclaresEachDocumentedSymbol_WithItsDefaultValue`
(one `[Theory]` attribute, three `InlineData` rows, one per template
symbol — a net new gap of 2). No discrepancy or missing test was found;
the two counts measure different things (source attributes vs.
runtime-executed cases) and both are reported here to avoid the false
impression that they should match.

## Historical Test Count Progression (Verified from CHANGELOG.md / Testing.md / Retrospectives)

| Milestone | Test Count |
|---|---|
| v0.3.0 (Runtime Foundation baseline) | 164 |
| WP 4.1 (Module SDK) | Unknown exact figure at this point — not stated as a standalone total in any reviewed document |
| WP 4.2 (Plugin Manifest implementation) | 242 (215 pre-existing + 27 new) |
| WP 4.3 (Sample Module implementation) | Unknown exact running total — 18 new tests added, base not restated in that retrospective |
| WP 4.4D (Event Bus implementation) | 302 (278 pre-existing + 24 new) |
| WP 4.5 (Background Services implementation) | 355 (313 pre-existing + 42 new) |
| WP 4.5A (Governance Register Baseline) | 355 — re-verified directly, 0 failures, no code change |
| WP 5.0B (Navigation Framework implementation) | 400 (355 pre-existing + 45 new) |
| WP 5.0D (Shell & Composition Framework implementation) | 446 (400 pre-existing + 46 new) |
| WP 5.0S (Platform Security Baseline Audit) | 448 (446 pre-existing + 2 new) |
| WP 5.1B (Command Framework Implementation) | 514 (448 pre-existing + 66 new) |
| WP 5.2 (Diagnostics Improvements) | 542 (514 pre-existing + 28 new) |
| WP 5.3 (Developer Experience Improvements) | 552 (542 pre-existing + 10 new) |
| **Current (WP 5.3)** | **552** — re-verified directly, 0 failures |

Gaps in this progression are recorded as **Unknown**, not interpolated —
several retrospectives report only the tests *they* added, not a running
total, and reconstructing every intermediate total would require
re-running `dotnet test` against 43 individual historical commits, out of
this Work Package's own scope.

## Cross-Reference Check

The 552 figure above is cross-checked directly against
`Validation Register.md`'s own Test Gate row (also 552, from the same
`dotnet test` run performed as part of this Work Package) — consistent,
no discrepancy.
