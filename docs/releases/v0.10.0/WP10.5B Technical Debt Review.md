# WP 10.5B — Technical Debt Review

## Purpose

This Work Package's own explicitly-named "Technical Debt Review"
deliverable — reviews every one of the register's 40 tracked items
against this Work Package's own diff, and gives the formal record for
the two genuine implementation-time findings this Work Package fixed
in place rather than merely disclosed.

## 1. Zero New Items — Both Genuine Findings Were Fixed, Not Left Open

This Work Package found two real, genuine defects while implementing
and testing (§9/§10 of the Implementation Report):

1. **`ProjectExplorerView`'s own `TextChanged` reliability gap**
   (pre-existing since `WP 10.2A`) — a real, shipped-code reliability
   issue, fixed at its own source (`PropertyChanged`, filtered to
   `TextBox.TextProperty`), the identical fix `ObjectEditorView`
   (`WP 10.3A`) already established for its own fields. Proven by
   `WorkflowInteractionTests`'s own Recent Searches tests, re-run three
   consecutive times, zero failures after the fix.
2. **A test-precision gap in this Work Package's own new tests**
   (never shipped, never part of any prior commit) — an incorrect
   assumption that `IDeletable.DeleteAsync` removes an object from the
   repository (it soft-deletes, `IsDeleted`, unchanged, correct,
   pre-existing behaviour), and a `TD-27`-class non-determinism risk in
   a test-only node-finder. Both fixed before this Work Package's own
   commit.

Neither reached "Technical Debt" status — both were found and fixed in
the same Work Package that found them, before any commit, mirroring
this project's own established "found and fixed, not merely disclosed"
discipline (`WP 10.2A`'s Property Inspector fix, `WP 10.3B`'s two
defects, `WP 10.5A`'s two theme-binding findings — all the identical
pattern).

## 2. `TD-39`/`TD-40` — Reviewed, Confirmed Still Correctly Resolved

Both closed by `WP 10.5A`, immediately prior to this Work Package.
Re-confirmed unaffected: `PanelHostControl`/`CommandPaletteOverlay`'s
own theme-reactive backgrounds and `DocumentAreaView.IsMarkedDirty`
(`TD-40`'s own closure) are both untouched by this Work Package's own
diff, except that `TD-40`'s own `IsMarkedDirty` member is now also read
by the new `HasAnyDirtyTab` property — an additive, non-breaking
extension, confirmed by direct re-read.

## 3. Every Other Tracked Item (`TD-01`–`TD-38`) — Reviewed, Confirmed Unaffected

Direct review of all 38 remaining entries against this Work Package's
own diff: none names any file this Work Package changed as its own
revisit trigger; none is touched by any file this Work Package changed.
Zero pre-existing item worsened.

## 4. New Future Capability Entries, Not Duplicated Here

Real, disclosed scope reductions (Copy/Move/Export/Import dialogs;
extending Create/Duplicate beyond Mechanical; a startup splash screen)
are recorded as new Future Capability Register entries, not this
register — none is a defect, all are disclosed, deliberate reductions
named directly in the Implementation Report §8.

## 5. Register Totals After This Review

40 tracked items total (unchanged — zero new items added, zero
removed). Open count: 31 (unchanged from `WP 10.5A`'s own closing
figure — this Work Package closed no additional Open item, since its
own two genuine findings were fixed in place rather than registered).

## 6. Recommendation

**Accept.** Both genuine findings from this Work Package's own
implementation were correctly diagnosed and fixed at their own true
source before commit — one closing a real, pre-existing shipped-code
reliability gap, one correcting this Work Package's own new tests —
neither needed, nor received, a new open Technical Debt entry.
