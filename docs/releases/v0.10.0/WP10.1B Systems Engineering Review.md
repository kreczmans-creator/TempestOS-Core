# WP 10.1B — Systems Engineering Review

## Purpose

The required, dedicated Systems Engineering Review — verifying this
Work Package's own explicit "Guarantee" list is genuinely met, each
item traced to a specific test, not merely asserted in prose.

## 1. Guarantee-by-Guarantee Verification

| Guarantee | Evidence |
|---|---|
| Deterministic module discovery | `ModuleLifecycleStabilityTests.ModuleDiscoveryAndInitialisation_IsDeterministic_SameModuleSetEveryLaunch` — two independent, freshly-seeded hosts produce the identical, identically-ordered module Id set. |
| Deterministic registration | `RuntimeModuleManager.Register` re-read directly (`WP10.1B Root Cause Analysis.md` §2.1) — confirmed single-pass, order-preserving, unchanged by this Work Package. |
| Deterministic initialisation | `ModuleLifecycleManager`'s own ordinal-by-Id snapshot re-confirmed unchanged; `ModuleLifecycleStabilityTests`' three tests each independently confirm the same four previously-affected modules reach `ModuleState.Running`, not a nondeterministic mix of `Running`/`Failed` across launches. |
| No duplicate registrations | `MaterialsSampleModuleIntegrationTests`/`EngineeringDomainSampleModuleIntegrationTests`/`RequirementsSampleModuleIntegrationTests`'s own new idempotent-restart tests — a second pipeline against the same store reaches `Initialised`, not a `DuplicateMaterialException`/duplicate-identifier failure. |
| Correct module isolation | `ModuleLifecycleStabilityTests`' own explicit exclusion and separate handling of `tempest.samples.navigation.zzz-duplicate` — its permanent, by-design failure is confirmed isolated (Runtime Host still reaches `Running`) in every one of this Work Package's own three new Desktop-level tests, the same guarantee `WP 10.1A` first observed directly. |
| Stable startup | Every test in the full suite (2051/2051) constructs and starts at least one real Runtime Host/`WorkspaceHost`; none fail. |
| Stable shutdown | `ShutdownAsync`/`DisposeAsync` unchanged by this Work Package; exercised in every test's own `finally` block, all passing, across Debug and Release. |
| Stable restart | `ModuleLifecycleStabilityTests.StableRestart_TwoSequentialHostsAgainstTheSameStore_BothReachRunningWithNoModuleFailures`/`StableRestart_ThirdSequentialHost_...` — the direct, named proof this Work Package exists to deliver. |

## 2. Traceability to the Controlling Instruction

Every named review item ("Review and harden: TempestHost; Module
Discovery; Module Registration; Module Initialisation; Module
Lifecycle; Dependency Injection; Sample Module loading; Diagnostics")
was independently re-read against its own current source as part of
`WP10.1B Root Cause Analysis.md` §2.1/§1: `TempestHost.cs`,
`RuntimeModuleManager.cs`, `ModuleLifecycleManager.cs`,
`ServiceCollection`/`TempestServiceProvider` (Dependency Injection —
unchanged, confirmed no new registration pattern needed), the four
`Tempest.Samples` modules (Sample Module loading — the one area
actually changed), and `IDiagnosticsProvider`/`DiagnosticsProvider`
(Diagnostics — unchanged in shape, now consumed one layer earlier by
`WorkspaceManager` itself, the same signal it always exposed).

## 3. Verified Against All Six Engineering Disciplines

Mechanical, Requirements, Calculations, Verification, Documents, and
Manufacturing each have their own dedicated `Tempest.Core.Tests`
namespace, all 2026 pre-existing tests re-run unmodified as part of the
combined 2051/2051 pass. None of the six disciplines' own Workspace
registrations, commands, or sample data were touched by this Work
Package — confirmed by `git diff --stat`. `ModuleLifecycleStabilityTests`
additionally proves all six load correctly across repeated,
sequential `WorkspaceHost` launches (not merely a single launch), the
scenario this Work Package specifically targets.

## 4. Cockpit — Live Data After Successful Initialisation

The controlling instruction's own explicit Cockpit requirement
("Ensure all Cockpit cards receive stable live data after successful
module initialisation") is met and directly demonstrated:
`EngineeringCockpitTests`' own three rewritten assertions
(`RiskSummary`/`UpcomingMilestones`/`OpenTaskCount`) now read real,
stable, seeded data on every run — not an honest-empty fallback path —
because the module initialisation those cards depend on now succeeds
stably, not because any Cockpit code changed.

## 5. Recommendation

**Sound, complete.** Every named Guarantee independently traced to a
specific, passing test; every named review area independently re-read
against current source; all six Engineering disciplines confirmed
unaffected and independently re-verified across repeated launches.
