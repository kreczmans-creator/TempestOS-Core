# TempestOS v0.13.0 — Work Packages

## Status

**In progress.** `v0.13.0` — "Trust & Deployment Hardening" — branch
`feature/v0.13.0`, cut from the released `v0.12.0` tag (`13a6ce3`), not
from any working branch, per this release's own explicit `WP 13.0.0`
Branch Establishment instruction — confirmed directly, `git merge-base
feature/v0.13.0 v0.12.0` resolves to `13a6ce3` exactly, the identical
commit `v0.12.0` itself points to. This document is created now, per
this project's own established convention ("each release's own
`WorkPackages.md` is created when that release's branch is cut,"
`WP11.4B Release Process Correction Report.md` §10), seeded from
`WP11.0B Architecture Roadmap.md` §3's own predicted `v0.13.0` table.

**This release is conditional, and the condition is now confirmed
triggered.** `WP11.0B Architecture Roadmap.md` §3 is explicit:
`v0.13.0` "only enters the plan if `WP 11.2A` / the Product Owner
commits `v1.0` scope to third-party plugins and/or REST deployment
beyond a trusted local network. Otherwise… this release is skipped
entirely — the plan collapses to `v0.11.0` → `v0.12.0` → `v1.0.0`."
Directly confirmed, at this Work Package's own commissioning: neither
trigger had fired as of `v0.12.0`'s own close (`src/Plugins/` remains
empty — `WP 12.9.1`'s own tracked marker `README.md` — and no
`v0.12.0` Work Package touched REST API scope). The Product Owner has
now explicitly made that commitment, confirmed directly, commissioning
this branch — recorded here as the trigger event itself, not assumed
or inferred from the branch's mere existence.

**Predicted scope**, per `WP11.0B Architecture Roadmap.md` §3: Work
Package, Scope, and Type columns reproduced verbatim; Status updated
from that table's own `Conditional` to `Not started` now that the
condition has fired — no `v0.13.0` Work Package has yet begun:

| Work Package | Scope | Type | Status |
|---|---|---|---|
| `WP 13.0A` | Plugin & Registration Trust Isolation Architecture (`A-3`/`FCR-0001`) | Architecture | **Complete** |
| `WP 13.0B` | ~~Plugin & Registration Trust Isolation Implementation~~ — **actually commissioned as** Plugin & Trust Isolation Architecture Review and Baseline (independent audit of `WP 13.0A`, not the implementation this table originally predicted — disclosed, not silently reconciled; see below) | ~~Implementation~~ Governance/Process | **Complete** |
| `WP 13.1A` | ~~REST API Authentication & TLS Architecture (`A-4`/`FCR-0003`/`FCR-0004`)~~ — **actually commissioned as** Plugin Runtime & Composition Root Implementation (the real Trust Isolation Implementation successor `WP 13.0B`'s own row disclosed as "not yet commissioned, and does not yet have a number" — assigned this label directly by the Product Owner, not by roadmap prediction; disclosed, not silently reconciled; see below). REST API Authentication & TLS Architecture remains a real, distinct, still-uncommissioned Work Package that will need its own number when it is actually commissioned. | ~~Architecture~~ Implementation | **Complete** |
| `WP 13.1B` | ~~REST API Authentication & TLS Implementation~~ — **actually commissioned as** Plugin Runtime & Composition Root Implementation Review (an independent review of `WP 13.1A`'s own implementation before it becomes a commit — mirroring `WP 13.0B`'s own review-before-baseline role one Work Package earlier — not the REST API implementation this table originally predicted; disclosed, not silently reconciled; see below). REST API Authentication & TLS Implementation remains a real, distinct, still-uncommissioned Work Package that will need its own number when it is actually commissioned. | ~~Implementation~~ Governance/Process | **Complete** |
| `WP 13.3A` | Plugin Platform Integration & End-to-End Validation | Verification/Governance | **Complete** |
| `WP 13.9.0` | `v0.13.0` Release Preparation & Engineering Sign-Off | Verification only | Not started |

**Not re-derived or re-scoped here** — this document records the
roadmap's own predicted plan at the moment this branch was cut; the
first substantive Work Package (`WP 13.0A`, expected) is where a real,
independent architectural investigation of `A-3`/`FCR-0001` belongs,
mirroring exactly how `WP 12.0A` opened `v0.12.0`'s own first roadmap-
predicted item rather than re-litigating it here.

**Disclosed divergence from this table's own prediction, found when
`WP 13.0B` was actually commissioned:** the roadmap predicted `WP 13.0B`
would be the Trust Isolation *Implementation* Work Package, directly
following `WP 13.0A`'s architecture. What was actually commissioned
under that exact label was instead an independent **Architecture Review
and Baseline** — auditing `WP 13.0A`'s own design before it becomes a
commit, not implementing it. This is recorded plainly, not silently
reconciled with the original prediction, mirroring `WP 12.2A`'s own
precedent of disclosing exactly this class of roadmap-vs-reality
divergence rather than quietly renumbering around it. **The real Trust
Isolation Implementation Work Package the roadmap originally predicted
remains not yet commissioned, and does not yet have a number** — it is
not `WP 13.0B` (that label is now taken by the review Work Package
above, already Complete) and this document does not invent one
speculatively; the next Work Package to take up that scope names its own
number when it is actually commissioned, per this project's own
established "do not pre-number a Work Package that has not yet been
briefed" discipline.

**Second disclosed divergence, found when `WP 13.1A` was actually
commissioned:** the real Trust Isolation Implementation successor named
above did, in fact, get commissioned — but not as its own, freshly-minted
number. The Product Owner, asked directly, assigned it the `WP 13.1A`
label instead of a new one, even though this table's own row for that
exact label already predicted a different, unrelated Work Package —
"REST API Authentication & TLS Architecture" (`A-4`/`FCR-0003`/
`FCR-0004`), itself still genuinely uncommissioned. This is recorded
plainly, mirroring the identical disclosure discipline `WP 13.0B`'s own
divergence above already established, not silently reconciled either
way: **`WP 13.1A` now means Plugin Runtime & Composition Root
Implementation** (`Plugin Platform Architecture.md`'s own mechanical,
non-trust half — `ADR-0107`–`ADR-0109` — built, not the Trust &
Isolation retrofit `ADR-0110`–`ADR-0112` still needs). REST API
Authentication & TLS Architecture remains real, unrelated, and
uncommissioned; it will need a new number of its own — genuinely a
*second* instance of "do not pre-number a Work Package that has not yet
been briefed" colliding with a Work Package that, once briefed, was
deliberately assigned a label this table had already reserved for
something else. See `WP13.1A-plugin-runtime-and-composition-root-implementation.md`
("A Note on This Work Package's Own Number") for the full disclosure.

**Third disclosed divergence, found when `WP 13.1B` was actually
commissioned:** the identical pattern recurred one Work Package later.
`WP 13.1B` was commissioned as an independent review of `WP 13.1A`'s own
implementation — mirroring `WP 13.0B`'s own review-before-baseline role
for `WP 13.0A` — not as "REST API Authentication & TLS Implementation,"
this table's own original prediction for that exact label. Recorded
plainly, the same discipline both prior divergences already established:
**`WP 13.1B` now means Plugin Runtime & Composition Root Implementation
Review.** REST API Authentication & TLS Implementation remains real,
unrelated, and uncommissioned — now the *second* roadmap-predicted REST
API row this release's own numbering has had to disclose as reserved-but-
reassigned, alongside `WP 13.1A`'s own identical fate one row above. Both
real REST API Work Packages (Architecture and Implementation) will need
new numbers of their own when actually commissioned; this document does
not invent them speculatively. See
`WP13.1B-plugin-runtime-and-composition-root-implementation-review.md`
for the full disclosure.

## Branch Discipline

Per this release's own explicit `WP 13.0.0` instruction — stricter
than every prior release's own convention (`v0.11.0`/`v0.12.0` each
used multiple parallel `feature/vX.Y.0-*` branches, one per Work
Package or Work Package pair):

- `feature/v0.13.0` is the **sole** integration branch for every
  `v0.13.0` Work Package. No additional feature branches without
  explicit, separate authorisation.
- Every Work Package commits **directly** to `feature/v0.13.0`.
- **Never rebase. Never squash.** History stays linear through
  sequential commits on this one branch.
- Merge commits only when integrating `feature/v0.13.0` back to `main`
  at this release's own close (mirroring Engineering Governance §7's
  own merge-commit-only rule, applied here one level earlier, to the
  Work-Package-to-branch relationship as well as the branch-to-`main`
  one).

## Work Packages

The Work Packages actually delivered on this branch, in order — many of
them additive and never named in the roadmap-predicted table above
(mirroring how `v0.12.0`'s own `WP 12.9.1`/`WP 12.9.2`/`WP 12.9.3B` were
real, additive Work Packages never named in that release's own original
roadmap prediction either). Unlike the predicted table above, which is
frozen at branch-cut and never re-derived, this table is the live
delivered record and every Work Package appends to it.

**Corrected, `WP 13.11D`:** this preamble previously read "Two
process/governance Work Packages completed so far ... `WP 13.0A` (the
roadmap's own first predicted item) has not yet begun." That was written
when the table held two rows and was never updated as it grew; `WP 13.0A`
has in fact been complete since commit `0ac1bdc`, and the table below now
carries every Work Package through `WP 13.11D`. Corrected rather than
left to contradict the rows immediately beneath it. The roadmap-predicted
table above is deliberately *not* corrected — being a frozen snapshot of
the plan at branch-cut is its entire purpose.

| Work Package | Scope | Type | Status |
|---|---|---|---|
| `WP 13.0.0` | `v0.13.0` Branch Establishment — creates `feature/v0.13.0` directly from the released `v0.12.0` tag (`13a6ce3`), confirmed the exact branch point six independent ways (Architecture Agent). Two real conflicts resolved with the Product Owner before proceeding, not silently applied: `VERSION → 0.13.0-dev` would have broken `governance-healthcheck.ps1`'s own version-token parsing (`[version]"0.13.0-dev"` throws) and has no precedent in this project's documented versioning policy — `VERSION` kept at `0.12.0`, mirroring how it stayed `0.11.0` throughout all of `v0.12.0`'s own development; `v0.13.0`'s own roadmap-conditional scope confirmed now explicitly triggered by the Product Owner. Establishes stricter branch discipline than any prior release: `feature/v0.13.0` is the sole integration branch, commit directly to it, never rebase, never squash. Three parallel verification agents (Architecture/Repository/Governance) all Pass; Governance Agent's one minor finding (imprecise "verbatim" wording) fixed same session, second sequential commit. See `WP13.0.0-v0.13.0-branch-establishment.md` (Academy retrospective). | Governance/Process | **Complete** |
| `WP 13.0.0A` | Release Register Reconciliation — closes the one finding `WP 13.0.0` disclosed rather than fixed: `docs/governance/Delivery/Release Register.md` had no row for the `v0.12.0` tag. Independently confirmed pre-existing, dating to `v0.12.0`'s own close (`git show 13a6ce3:...` — already missing at the exact commit the tag points to), not caused by any `v0.13.0` Work Package. `v0.12.0` row added, matching the established evidence/density convention; all eleven real tags (`v0.3.0`–`v0.12.0`) independently re-verified present in the Entries table exactly once each. `governance-healthcheck.ps1` re-confirmed clean: 7 passed, 1 warned, 0 failed, exit 0. Governance/documentation only; zero `src/`/`tests/` files touched, zero architecture/implementation/release behaviour changed. See `WP13.0.0A-release-register-reconciliation.md` (Academy retrospective). | Governance/Process | **Complete** |
| `WP 13.0A` | Plugin & Registration Trust Isolation Architecture — this release's own first roadmap-predicted Work Package (`A-3`/`FCR-0001`). Four parallel architecture sub-agents (Plugin Architecture; Security & Trust; Governance & ADR Review; Documentation & Academy), each writing new, non-overlapping files, reconciled into one coherent design by a single integrating pass rather than each editing the project's own shared registers directly. **Plugin Architecture** (`docs/architecture/Plugin Platform Architecture.md`): manifest v2 (`Dependencies`, plus shape-only `RequestedCapabilities`/`Publisher`/`Signature` fields whose semantics the Security & Trust document owns), dependency-graph resolution inside the existing Phase 3.1 (no new Host Lifecycle phase), a new Host-owned `IPluginRegistry` projected read-only via `IDiagnosticsProvider.Plugins`, a configurable plugins root/manifest convention (closes `FCR-0010`/`TD-06`), and an explicit, defended decision that live in-process plugin unload remains a non-goal for `v0.13.0` (`ADR-0015` reaffirmed) while reserving an unused lifecycle seam contingent on a future isolation-mechanism change. `ADR-0107`–`ADR-0109`. **Security & Trust** (`docs/security/Plugin Trust & Isolation Architecture.md`): a four-tier trust model; a capability model extending `IPermissionEvaluator` (`ADR-0044`) via a new, `AsyncLocal<T>`-backed `ICurrentComponentAccessor` distinct from the existing ambient `ICurrentPrincipalAccessor`; a detached SHA-256 manifest+assembly signature verified entirely at Plugin Discovery, no new NuGet dependency; and the central isolation-boundary decision — capability-scoped, in-process enforcement, **not** a separate `AssemblyLoadContext` (confirmed, on direct technical grounds, not a security boundary in modern .NET) and **not** process separation (disproportionate to the disclosed threat: vetted, signed, commercial plugins, not an open marketplace). This design directly closes the architecture gap behind `TD-09`/`TD-10`/`TD-11` — a trust-tier-ordered registration rule replaces unconditional "first registration wins" for Navigation/Command Id ownership, closing `Security Roadmap.md` items 1, 2, and 10 together, as that roadmap's own item 10 recommended. `ADR-0110`–`ADR-0112`. Both documents were reconciled directly against each other during drafting (matching manifest field shapes; failure-classification categories numbered 12–14 and 15–18 to avoid collision; the Security & Trust document's own isolation decision confirmed against, and consciously leaves unused, the Plugin Architecture document's reserved lifecycle seam) — one integration gap both sides explicitly deferred (`PluginRegistryState.TrustDenied`, a sixth enum value referenced but not literally declared) found and closed directly in the same file both agents already owned. Nineteen new Rejected Designs entries (`RD-0046`–`RD-0064`) and one addendum (`RD-0009`, reaffirmed not reversed). **Governance & ADR Review** independently re-derived the full governance landscape before either architecture agent's output existed, confirmed a `governance-healthcheck.ps1` clean baseline (7 passed, 1 warned, 0 failed), and — critically — found that this Work Package's own true scope (per its own title, "Plugin **& Registration** Trust Isolation Architecture") is broader than `FCR-0001`'s own narrow three-call-site framing alone suggests; verified in a second pass, after integration, that none of the six new ADRs contradicts an existing Accepted ADR and that the numbering/register updates are internally consistent, re-running the health check against the fully integrated state. **Documentation & Academy** extended `docs/academy/02 Runtime Architecture/07-plugin-architecture.md` in place (not a new article, per Engineering Governance §6's own "fit an existing document first" discipline), wrote this Work Package's own 13-section Academy retrospective, and updated `WP6.1-permissions-and-identity-implementation.md`'s own Future Evolution section to connect its prediction to what this Work Package actually did. **Zero `src/`/`tests/` files touched, zero code written, zero implementation introduced** — confirmed directly at every stage; a real, still-unnumbered implementation Work Package remains the roadmap's own next step (see `WP 13.0B`'s own row, below, for the disclosed divergence — `WP 13.0B` itself became an independent architecture review, not the implementation this row originally anticipated). See `WP13.0A-plugin-and-registration-trust-isolation-architecture.md` (Academy retrospective). | Architecture | **Complete** |
| `WP 13.0B` | Plugin & Trust Isolation Architecture Review and Baseline — an independent review of `WP 13.0A`, not the implementation the roadmap-predicted table originally named this number for (disclosed divergence, see the note above that table). Three parallel, genuinely independent audit sub-agents — Architecture Audit, Governance Audit, Documentation Audit — none of which authored any `WP 13.0A` material, each strictly read-only, each reporting file-and-line findings rather than editing anything themselves. **Architecture Audit** (PASS): all six new ADRs re-checked against Engineering Governance §5 criteria; a full-corpus sweep of all 111 ADRs for contradiction (wider than any single prior pass); all nineteen Rejected Designs entries spot-checked against their cited sources. One blocking fix (a copy-paste failure-category range in `Plugin Trust & Isolation Architecture.md`'s Logging & Telemetry section, "12–15" corrected to "15–18") and one citation-completeness fix (`ADR-0111` now explicitly acknowledges and cites `ADR-0032`/`ADR-0037`, whose already-Accepted registration behaviour its trust-ordered rule additively revises). **Governance Audit** (FAIL → fixed): re-derived every count independently (ADR total, Rejected Designs total, Academy/Documentation Register counts) and re-ran `governance-healthcheck.ps1` — found `ADR Register.md`'s own separate `**Total: 104 ADRs**` paragraph had drifted stale independently of the `Last Reviewed`/`Related ADRs` fields `WP 13.0A` had already corrected, an internal self-contradiction within the same register; found `WP13.0A Governance Landscape Brief.md`'s own closing verdict still read "cannot yet be called complete" after its underlying findings had in fact already been fixed. Both corrected — the Brief via an appended addendum, not a rewritten verdict, per this project's own "disclose a finding's history, don't edit it away" discipline. A double-encoded `&amp;amp;` typo in this very document's own `WP 13.0A` row also found and fixed. **Documentation Audit** (FAIL → fixed): confirmed the retrospective's all-thirteen-section completeness and fact-checked it against the architecture source documents; found its own §12 Future Evolution falsely claimed `Host Lifecycle.md`/`Failure Behaviour.md` were untouched by `WP 13.0A`, when `git diff` shows both carry substantial, already-merged "Extended, `WP 13.0A`" content — corrected to state the true documentation-vs-code distinction (the *prose* extension is done; the *code* it describes is `WP 13.0B`'s — now this Work Package's own — remaining, still-unassigned successor's task). The retrospective's own incomplete "Files changed" list was also corrected. A wrong section citation in the `WP6.1` retrospective's own `WP 13.0A` addition (§7 → §10/§12) was fixed. **Every fix applied was a defect `WP 13.0A` itself introduced — nothing pre-existing was touched or re-flagged** (`Rejected Designs Register.md`/`Governance Index.md`'s own long-stale counts, already disclosed by `WP 13.0A` as out of scope, confirmed still untouched). `governance-healthcheck.ps1` re-run clean after every fix: 7 passed, 1 warned, 0 failed, exit 0 — identical shape to `WP 13.0A`'s own closing baseline. No new ADR, no new architecture, no broadened scope. Zero `src/`/`tests/` files touched throughout. `WP 13.0A`'s and this Work Package's combined material committed as a single architecture baseline immediately following this Work Package's own close. See `WP13.0B-plugin-and-trust-isolation-architecture-review-and-baseline.md` (Academy retrospective). | Governance/Process | **Complete** |
| `WP 13.1A` | Plugin Runtime & Composition Root Implementation — the real Trust Isolation Implementation successor `WP 13.0B`'s own row disclosed as "not yet commissioned, and does not yet have a number," now commissioned and assigned the `WP 13.1A` label directly by the Product Owner, colliding with — and repurposing — that same label's own roadmap-predicted "REST API Authentication & TLS Architecture" row (disclosed divergence, see the note above the roadmap-predicted table). Builds the mechanical, non-trust half of `WP 13.0A`'s design (`ADR-0107`–`ADR-0109`) only; the sibling Trust & Isolation retrofit (`ADR-0110`–`ADR-0112`, `TD-09`/`TD-10`/`TD-11`) is explicitly out of scope, untouched, and remains a genuinely separate, still-unassigned future Work Package's task. Four parallel workstreams: **Runtime Implementation** (`src/Tempest.Core/Plugins/`) built the fixed-point dependency-graph reduction (repeated missing/incompatible-version removal, three-colour cycle detection, repeat until stable, then Kahn's-algorithm topological sort) and the three new isolated failure categories (`MissingPluginDependencyException`/`IncompatiblePluginDependencyVersionException`/`CircularPluginDependencyException`, ADR-0107 categories 12–14), plus `PluginRegistry`/`IPluginRegistry`/`IPluginRegistryRecorder` (a Host-owned `Record`/`Entries` split, never DI-public, mirroring `ADR-0017`). **Dependency Injection & Composition** (`TempestHost.cs`, `Diagnostics/*.cs`) wired `Runtime:Plugins:RootDirectory`/`ManifestFileName`/`Disabled` at the seam `ADR-0026` already anticipated (no Host Lifecycle phase change), and extended `IDiagnosticsProvider` with a `Plugins` property backed by a direct (non-`Func<T>`) `IPluginRegistry` reference — the Plugin Registry is always already constructed by Plugin Discovery (Phase 3.1), strictly before `DiagnosticsProvider` itself (Phase 6). **Verification & Testing** added 45 new test methods across six new test files (`PluginManifestV2FieldsTests.cs`, `PluginDependencyGraphResolutionTests.cs`, `PluginDisabledConfigurationTests.cs`, `PluginRegistryTests.cs`, `TempestHostPluginConfigurationTests.cs`, plus a fixture helper) and extended two existing files (`PluginAssemblyLoaderTests.cs` +2, `TempestHostPluginLifecycleTests.cs` +4); found two bugs in its own new test assertions during authoring, both fixed before its final report — **zero genuine production-code defects found**. Debug and Release builds both 0 Warnings/0 Errors (`-p:TreatWarningsAsErrors=true`); full regression Debug 2,305/2,305 passing (2,084 `Tempest.Core.Tests` + 221 `Tempest.Desktop.Tests`), Release confirmed to match. **Governance & Documentation** confirmed directly (via `git diff`) that trust enforcement, capability enforcement, signature verification, REST, installer, hot reload, dynamic unload, and marketplace/commercial licensing are all absent from this Work Package's own diff, exactly as scoped; confirmed no new ADR was warranted (implementation of already-ratified `ADR-0107`–`ADR-0109`, no undisclosed deviation found); closed `TD-06`/`FCR-0010` (12 → 13 Resolved, 35 → 34 Open in the Technical Debt Register; both now read Implemented/Resolved); updated `docs/academy/02 Runtime Architecture/07-plugin-architecture.md`'s Status line and two Future Evolution bullets from "architecture only"/"Designed… Awaiting implementation" to reflect real, built code, correcting each stale `WP 13.0B` implementation citation along the way; confirmed `Plugin Register.md`'s own "Not Yet Applicable" status is correctly unaffected (`src/Plugins/` remains empty — infrastructure, not a real shipped plugin). 10 new + 8 modified production files under `src/Tempest.Core/Plugins/`, `src/Tempest.Core/Runtime/TempestHost.cs`, `src/Tempest.Core/Diagnostics/` — independently re-verified via `git status`/`git diff --stat` directly, one file more than the orchestrator's own initial count, disclosed plainly (see the retrospective's own Trade-offs section). See `WP13.1A-plugin-runtime-and-composition-root-implementation.md` (Academy retrospective). | Implementation | **Complete** |
| `WP 13.1B` | Plugin Runtime & Composition Root Implementation Review — an independent review of `WP 13.1A` before it becomes a commit, mirroring `WP 13.0B`'s own role for `WP 13.0A`, applied to real production code for the first time this release. Four parallel, genuinely independent audit sub-agents — Code Review, Architecture Compliance, Verification & Test Audit, Governance & Documentation Audit — none of which authored any `WP 13.1A` material, each instructed to assume defects exist until personally disproven. **Architecture Compliance**: PASS, zero blocking findings — confirmed `ADR-0107`–`ADR-0109` point-by-point (no new Host Lifecycle phase, all three new failure categories isolated by construction, no live-unload capability introduced) and the single most important boundary in this Work Package, `ADR-0017`/`ADR-0109`'s DI-registration rule — `PluginRegistry` confirmed never added to the `ServiceCollection` anywhere; a full 111-ADR contradiction sweep found no drift. **Code Review**: FAIL, **two genuine, empirically-confirmed production bugs**, both found via direct repro before any fix existed: (1) `PluginManifestDiscoveryService.TopologicalSort`'s `remainingDependencyCount` was keyed on raw `Dependencies.Count`, including duplicate target declarations, but decremented once per distinct target removed — a manifest declaring the same dependency `Id` twice (legal, unrejected input) silently vanished from the topological sort with no isolation, no log line, and no registry entry, violating `ADR-0025`/`ADR-0107`'s own "always logged, never silently swallowed" guarantee; fixed by keying the count on distinct targets. (2) `TempestHost.cs`'s configuration-precedence `??` chains for `Runtime:Plugins:RootDirectory`/`ManifestFileName` only substituted on `null`, not on a blank configured value — a single empty configuration entry reached `PluginManifestDiscoveryService`'s own `ArgumentException.ThrowIfNullOrWhiteSpace` guard uncaught, **faulting the entire Host**, not merely isolating one plugin; fixed by treating a blank configured value as absent at both precedence chains. **Verification & Test Audit**: independently re-ran the full build/test matrix itself and, judging test completeness against the real algorithm's edge cases rather than reading the Code Review findings first, converged on the identical two gaps — strong triangulated evidence both bugs were real; also surfaced and correctly diagnosed, via two clean isolated/full-suite reruns, a genuinely pre-existing, unrelated `CompositeLogSinkTests` `Console.Error`-race flake (disclosed, not fixed — predates and is unrelated to `WP 13.1A`), and two non-blocking coverage gaps in `DiagnosticsProviderTests.cs` (no dedicated unit-level `Plugins`-property coverage, no null-registry constructor guard, breaking that file's own established per-property convention). **Governance & Documentation Audit**: PASS, zero defects — independently re-ran `governance-healthcheck.ps1` and its own full Release regression, fact-checked twenty specific claims against real source (double the required ten), and correctly declined to flag a genuinely pre-existing, unrelated stale paragraph in the Technical Debt Register as a finding. Eight new regression tests added closing every real gap found (2 proving the duplicate-dependency fix, 2 proving the blank-configuration-value fix, 4 closing the `DiagnosticsProviderTests.cs` gap) — every fix a defect `WP 13.1A` itself introduced, nothing pre-existing touched or re-flagged. `governance-healthcheck.ps1` re-run clean: 7 passed, 1 warned, 0 failed, exit 0 — identical shape to `WP 13.1A`'s own closing baseline. Debug and Release builds both 0 Warnings/0 Errors, independently re-run by the orchestrator after every fix; full regression, both configurations, independently re-run: **2,313/2,313 passing** (2,092 `Tempest.Core.Tests` + 221 `Tempest.Desktop.Tests` — 2,305 from `WP 13.1A`'s own baseline plus these 8 new tests). No new Technical Debt introduced; no ADR changes required or made, confirmed independently by two of the four audit workstreams. Zero new production/test files — every fix and every new test landed inside files `WP 13.1A` had already created or modified. This Work Package's own Governance & Documentation pass also found and fixed a genuine, disclosed gap `WP 13.1A` itself left: `Academy Register.md`'s own `## 03 Work Packages` header/Total counts had never been incremented for `WP 13.1A`'s own retrospective at all (not merely missing its table row) — corrected by independent file recount, a two-file jump (108 → 110, 173 → 175), not the one-file jump a naive "add one for this retrospective" edit would have produced. `WP 13.1A`'s and this Work Package's combined material committed as a single reviewed implementation baseline immediately following. See `WP13.1B-plugin-runtime-and-composition-root-implementation-review.md` (Academy retrospective). | Governance/Process | **Complete** |
| `WP 13.2A` | Plugin Trust & Capability Enforcement Implementation — the trust-half implementation successor to `WP 13.1A`/`WP 13.1B`'s own mechanical half, implementing `ADR-0110`–`ADR-0112` exactly as `WP 13.0A` designed and `WP 13.0B` independently audited them. **Not a roadmap-table numbering collision** — this label was not predicted by the roadmap-predicted table above at all (that table stops at `WP 13.1B`/`WP 13.9.0`), the same "real, additive Work Package outside the original prediction" shape `WP 13.0.0`/`WP 13.0.0A` already established for this release. Trust tier assignment and detached SHA-256/RSA-PSS signature verification (`PluginSignatureEnvelope`, `PluginSignatureVerifier`) run entirely within the existing Plugin Discovery phase, from file bytes, before any `Assembly.LoadFrom` call; a new `Plugins:AllowUnsignedLoad` configuration key governs whether an unsigned candidate loads at all, defaulting to `false` (fail-closed, ADR-0112's own named safe default) — absent or unparseable resolves to `false`, isolating an unsigned candidate at category 16 rather than silently permitting it. Capability enforcement (`PluginAssemblyLoader.EnforceTrust`) checks a plugin's `RequestedCapabilities` against its assigned trust tier's ceiling and reflects over every discovered `IModule` implementer's constructor for conformance against a fixed always-allowed baseline plus its own granted `plugin.services.resolve:*` declarations, denying at category 17 (`PluginTrustDeniedException`, recorded `PluginRegistryState.TrustDenied`, the sixth registry state) before the plugin is ever `Loaded`. A new `ICurrentComponentAccessor`/`CurrentComponentAccessor` (`AsyncLocal<T>`-backed, mirroring `ICurrentPrincipalAccessor`'s exact shape, a second identity axis per ADR-0111) is pushed around each module lifecycle call by `ModuleLifecycleManager`'s new, plugin-unaware `componentScopeProvider` hook. `NavigationService`, `CommandRegistry`, `CommandHandlerTable`, and `IEventBus` each gained a trust-ordered registration/capability-gated-publish retrofit — a contested Id is resolved by trust-tier rank (a higher tier always evicts and replaces a lower one, logged loudly, never silently); `NavigationService.Unregister` now performs a real ownership check; every check is skipped, not merely satisfied, for a `null`/First-Party actor, so every registrant that exists today observes zero behavioural change. **Directly closes `TD-09`, `TD-10`, and `TD-11`** (moved Open → Resolved in the Technical Debt Register) and updates `Security Roadmap.md` items 1, 2, and 10 from "resolved at the architecture level, not yet implemented" to implemented. 15 new + 11 modified production files (`src/Tempest.Core/Plugins/`, `src/Tempest.Core/Identity/`, plus `ModuleLifecycleManager.cs`/`TempestHost.cs`/`NavigationService.cs`/`CommandRegistry.cs`/`CommandHandlerTable.cs`/`EventBus.cs`); 2 new test files (`PluginSignatureVerifierTests.cs`, 15 test methods; `PluginSigningTestHelper.cs`, an RSA test-key helper) plus 31 modified test files — 23 pre-existing fixtures needed a two-line `ICurrentComponentAccessor`/`IPermissionEvaluator` dual-registration update, the direct, foreseeable consequence of this platform's DI container requiring every constructor parameter type to be independently resolvable, once `NavigationService`/`CommandRegistry`/`CommandHandlerTable`/`EventBus` each gained new, optional trust-related constructor parameters. One genuine found-and-corrected test-fixture finding, never a production defect: `PluginManifestV2FieldsTests.cs` had, since `WP 13.0A`/`WP 13.1A`, asserted a placeholder `Signature` string round-tripped verbatim through discovery — correct before this Work Package, since `Signature` was "read, not interpreted"; now that it is actively parsed as a verification envelope and cryptographically checked, the placeholder correctly fails envelope parsing rather than round-tripping, so the fixture was corrected to omit the field and pass `allowUnsignedLoad: true` instead, taking the deliberate unsigned path its own remaining assertions actually concern. Confirmed absent from this Work Package's own diff, matching its explicit out-of-scope list: certificate chains, online revocation, a marketplace, remote trust services, a REST API, authentication, authorisation beyond plugin capabilities, dynamic trust changes, live plugin reload. **Governance & Documentation** confirmed no new ADR was warranted (implementation of already-ratified `ADR-0110`–`ADR-0112`, no undisclosed deviation found); `ADR Register.md`'s own Originating Work Package field for all three ADRs updated to cite this Work Package's implementation alongside `WP 13.0A`'s own design, mirroring `ADR-0105`'s own established design/implementation-citation precedent. See `WP13.2A-plugin-trust-and-capability-enforcement-implementation.md` (Academy retrospective). | Implementation | **Complete** |
| `WP 13.2B` | Plugin Trust & Capability Enforcement Review — an independent review of `WP 13.2A`, mirroring `WP 13.0B`/`WP 13.1B`'s own review-before-baseline discipline one Work Package later, by four parallel, genuinely independent audit sub-agents (Architecture Compliance, Security Review, Verification Review, Governance Review), fixes applied centrally to avoid file collisions. **Disclosed governance gap, found by `WP 13.3A`, not backfilled by it**: this Work Package's own real, substantial findings are recorded in full only in its own commit message (`25df570`) — it has no Academy retrospective file, no row of its own in this table until now, and no `PROJECT_STATUS.md` entry; its only prior trace anywhere in this project's governance documentation was one citation inside `Technical Debt Register.md`'s own narrative. Security Review found one **Critical**, confirmed privilege-escalation defect: `PluginAssemblyLoader.EnforceTrust`'s own capability-eligibility check had no denylist for Host-authority concrete types, letting a Verified-Signed/First-Party plugin request `plugin.services.resolve:*` naming `Identity.CurrentComponentAccessor`/`Identity.CurrentPrincipalAccessor` and forge an arbitrary component principal or hijack the ambient user principal — fixed with a `NeverEligibleServiceResolveTypes` denylist checked in both `FindIneligibleCapability` and `HasCompliantConstructor`. Architecture Compliance found one **Blocking** deviation: `CommandHandlerTable.DispatchAsync` never pushed a component scope around handler invocation, unlike `EventBus`/`ModuleLifecycleManager`'s own per-call push — fixed by giving it the same `Identity.CurrentComponentAccessor` collaborator and a `BeginScope` wrap. Security Review also found (Medium, confirmed) that First-Party plugins were never actually exempted from capability checks, contrary to `ADR-0111`'s own stated design — the guard checked `registrant is not null` instead of `PluginTrustPermission.IsFirstParty`, fixed identically across all four registries. Two further, lower-severity, plausible-not-confirmed findings were disclosed, not fixed, as genuinely out of scope for a defect-fixing pass: a TOCTOU window between Discovery-time signature verification and Loading-time `Assembly.LoadFrom`; filename-based (not certificate-attribute-based) first-party trust store detection — both formally registered for the first time by `WP 13.3A` as `TD-49`/`TD-50`. This entry was reconstructed directly from the commit message and independently re-verified against real, current source by `WP 13.3A` (`Identity.CurrentComponentAccessor.SetCurrent`/`BeginScope` denylist; `CommandHandlerTable`'s `BeginScope` wrap; `PluginTrustPermission.IsFirstParty` checks — all confirmed present); a full 13-section Academy retrospective for this Work Package remains a genuine, still-open gap, deliberately not fabricated here (`WP 13.3A`'s own Trade-offs section explains why). | Governance/Process | **Complete** |
| `WP 13.3A` | Plugin Platform Integration & End-to-End Validation — the plugin platform's own closing integration/validation pass for `v0.13.0`, run alongside three parallel technical sub-agents (End-to-End Integration; Fault Injection & Resilience; Performance & Scalability, each scoped to `src/`/`tests/`) and this Governance & Documentation workstream (`docs/`/`PROJECT_STATUS.md` only). Independently re-verified `ADR-0107`–`ADR-0112` fully implemented against real, current source, not any prior Work Package's own citation; confirmed `TD-09`/`TD-10`/`TD-11` remain genuinely Resolved. Checked `WP 13.2B`'s own two disclosed-but-unfixed findings directly against each ADR's own literal Decision text and found neither is an ADR conformance gap — both are real, disclosed engineering debt, formally registered for the first time as `TD-49` (Discovery-to-Loading TOCTOU window) and `TD-50` (filename-based first-party trust-store detection). Raised four new Future Capability entries (`FCR-0085`–`FCR-0088`: certificate-chain validation/revocation checking; per-plugin hot/live reload and unload; process-separated marketplace isolation; a per-plugin unsigned-load allow-list), each already named out of scope by the ADRs themselves but never before given a roadmap-facing entry. **Found and fixed, this Work Package's own independent audit**: `ADR-0107`/`ADR-0108`/`ADR-0109`'s own `ADR Register.md` citation had never been updated to reflect `WP 13.1A`'s own implementation, unlike the sibling `ADR-0110`–`ADR-0112` citation `WP 13.2A` correctly fixed — corrected to the same two-phase citation shape. Also found, disclosed, and deliberately not backfilled: `WP 13.2B` had no row in this table, no Academy retrospective, and no `PROJECT_STATUS.md` entry — added a row above from its own commit message, but did not fabricate a full retrospective for a Work Package it did not execute. Zero `src/`/`tests/` files touched by this workstream. `governance-healthcheck.ps1` re-confirmed clean. See `WP13.3A-plugin-platform-integration-and-end-to-end-validation.md` (Academy retrospective). | Verification/Governance | **Complete** |
| `WP 13.3B` | Plugin Platform Integration & End-to-End Validation Review — an independent review of `WP 13.3A`, mirroring `WP 13.0B`/`WP 13.1B`'s own review-before-baseline discipline, by four parallel, genuinely independent audit sub-agents (Architecture & Integration Review, Security & Reliability Review, Verification Review, Governance Review), instructed to assume defects exist until disproven. **Committed together with `WP 13.3A` as a single combined commit (`88e41a2`)** — the same pattern `WP 13.0A`/`WP 13.0B` and `WP 13.1A`/`WP 13.1B` each already established — rather than as a separate, later commit, which is the direct reason this Work Package's own governance footprint (this row, its Academy retrospective, its `PROJECT_STATUS.md` entry) went missing until `WP 13.9.1` closed the gap: the most-corroborated finding in `WP13.9.0 Engineering Release Report.md`, found independently by four of that review's six disciplines. Architecture & Integration Review and Security & Reliability Review independently converged on the same genuine, blocking defect in `WP 13.3A`'s own `InvalidPluginManifestException.PluginId` fix: `DuplicatePluginIdException` fires only for candidates that pass complete manifest validation, so a manifest failing on any other field could carry its own self-declared, uniqueness-unverified `Id` into `PluginRegistryEntry.Id`, letting a malformed manifest spoof or duplicate a genuine, unrelated, already-`Loaded` plugin's registry entry. Fixed: the declared `Id` is now surfaced only as free text inside `PluginRegistryEntry.Detail`, never as the structured `Id`, confirmed still present exactly this way by direct source re-read; a dedicated regression test proves the spoof is no longer possible. Architecture & Integration Review additionally reported verifying `WP 13.3A`'s `TopologicalSort` optimization via a "20,000-trial differential fuzz harness" — a claim `WP13.9.0`'s own Implementation-readiness review (Finding F2) searched for directly and found no corresponding artefact anywhere in this repository, disclosed here rather than silently repeated (see the Academy retrospective's own dedicated section for the full account). Verification Review independently re-verified the performance claim (confirming the constant-factor win, correcting an inaccurate doc comment about Kahn's-algorithm complexity) and added regression tests — the commit message's own two paragraphs disclose an internal count inconsistency (15 vs. 12) not resolved here. Also flagged, not fixed: four distinct signature failure causes collapse to one generic `Detail` message. Governance Review fixed one stale `Academy Register.md` count and reconfirmed `TD-49`/`TD-50`/`FCR-0085`–`FCR-0088`. `governance-healthcheck.ps1`: 7 passed, 1 warned, 0 failed. Debug/Release both 0 Warnings/0 Errors; full regression both configurations 2,519/2,519 passing. This entry, and its full Academy retrospective, were reconstructed directly from `88e41a2`'s own commit message by `WP 13.9.1`, independently cross-checked against real, current source wherever directly checkable. See `WP13.3B-plugin-platform-integration-and-end-to-end-validation-review.md` (Academy retrospective, added `WP 13.9.1`). | Governance/Process | **Complete** |
| `WP 13.9.0` | `v0.13.0` Engineering Readiness Review — the release-readiness review that opened the `WP 13.9.x` hardening chain, producing `WP13.9.0 Engineering Release Report.md`. Its own Security/Trust reviewer empirically demonstrated, against this project's own compiled binary, that `PluginAssemblyLoader.EnforceTrust` scanned only the one manifest-declared assembly's own types — so a plugin could package a second, wholly undeclared assembly in its own candidate folder, have its primary assembly's own type inherit from a type declared there, and that second assembly's `IModule` implementers would reach Module Discovery (deliberately plugin-unaware, ADR-0110) with zero trust checking of any kind. That finding is what `WP 13.9.1` remediated with the fixed-point, breadth-first transitive scan, and what every subsequent `WP 13.9.x`/`13.10x`/`13.11x` Work Package built on. **Row added by `WP 13.11D`** — this Work Package was genuinely completed (commit `0b8726f`) but was never given a row in this table, the only completed `v0.13.0` Work Package missing from it; disclosed and corrected here rather than left silently absent. Note its `Not started` cell in the roadmap-predicted table above is **correct and deliberately unchanged**: that table records the plan as predicted at branch-cut and is explicitly never re-derived. | Verification/Governance | **Complete** |
| `WP 13.9.1` | `v0.13.0` Readiness Remediation — closes `WP13.9.0`'s four named findings only, nothing broader (Security, Implementation/API, Verification/Test, Governance & Documentation sub-agents, each stopped before commit, not merged). **Security**: the multi-assembly trust-boundary bypass `WP13.9.0` found and empirically demonstrated — `PluginAssemblyLoader.EnforceTrust`'s own construction-time conformance check scanned only the one manifest-declared assembly, while Module Discovery (deliberately plugin-unaware, `ADR-0110`) scans the entire process `AppDomain` regardless — closed by giving `DiscoverModuleTypes` the identical fixed-point, breadth-first AppDomain-diff idiom `PluginManifestDiscoveryService`'s own dependency-graph resolution already uses, following every assembly that enters the process as a direct or transitive consequence of examining the plugin's own declared assembly; `ADR-0111` gained a "Corrected, `WP 13.9.1`" Status-section note and Decision-text amendment. **API**: `PluginManifest`'s undisclosed five-new-required-parameter public API break resolved asymmetrically, not simply reverted — `TrustTier` stays required (a genuine security-critical field with no safe implicit default, mirroring the `AssemblyPath` precedent; documented via new Rejected Designs entry `RD-0065`), while `Dependencies`/`RequestedCapabilities`/`Publisher`/`Signature` are restored to optional, defaulted, trailing constructor parameters — all six call sites (`PluginManifestDiscoveryService` plus five test files) updated. **Verification**: the unsubstantiated "20,000-trial differential fuzz harness" claim in `WP13.3B`'s own commit message replaced with a real, committed, reproducible one — `TopologicalSort_RandomizedTrials_MatchesNaiveFullRescanReference` (`PluginPlatformPerformanceTests.cs`), 10,000 seeded trials (seed `20130`), each trial a freshly-random acyclic dependency graph checked against `NaiveFullRescanTopologicalSort`'s own independent reimplementation. **Governance**: `WP13.3B` (real, already committed `88e41a2`, the single most-corroborated `WP13.9.0` finding — independently found by four of six disciplines) backfilled with a full 13-section Academy retrospective and this table's own `WP 13.3B` row, immediately above; `Architecture Document Register.md`/`Documentation Register.md` counts corrected; `Plugin Platform Architecture.md`/`Plugin Trust & Isolation Architecture.md` Status headers corrected from stale `WP13.0A`-era "architecture only"/design-pending text to reflect real implementation and review. Earliest scenarios in new file `PluginAssemblyLoaderMultiAssemblyTrustTests.cs` (a base-type-inheritance reproduction of `WP13.9.0`'s own proof-of-concept) plus two new `DynamicPluginAssemblyBuilder.cs` builder methods (`BuildSecondaryAssemblyWithBaseTypeAndModule`, `BuildPrimaryPluginAssemblyDerivingFromExternalBaseType`); `[Collection("Console output capture")]` added to four further real-assembly-loading test classes (`PluginAssemblyLoaderTests`, `PluginAssemblyLoaderEnforceTrustTests`, `PluginSignatureVerifierTests`, `PluginTrustTierAssignmentTests`) to serialise a test-harness-only AppDomain race the new scan introduced (no real-world counterpart — Plugin Loading is single-threaded, sequential Host startup in production). Full regression re-run fresh after all four fixes: 2,524/2,524 passing, both configurations (2,303 `Tempest.Core.Tests` + 221 `Tempest.Desktop.Tests`); `governance-healthcheck.ps1`: 7 passed, 1 warned (pre-existing, unrelated), 0 failed. **Does not rewrite `WP13.9.0`'s own verdict** — the CI/branch-push/merge gap (Verification/Release readiness) remains entirely unaddressed, out of this Work Package's own engineering scope. See `PROJECT_STATUS.md`'s own `WP 13.9.1` entry for the full account. | Implementation/Governance | **Complete** |
| `WP 13.9.2` | `v0.13.0` Readiness Re-Execution — verification only; no code, documentation, or Academy artefact of its own beyond this row and its own conversational report to the commissioning session (this table's own row is retroactive, added by `WP 13.9.7`). Re-verified `WP 13.9.1`'s own multi-assembly fix directly against a fresh live proof-of-concept and found it still incomplete: `DiscoverModuleTypes`'s own fixed-point scan diffed the `AppDomain` only around `Assembly.GetTypes()`/`IsAssignableFrom` (the touchpoint resolving a discovered type's own base-type chain), not around the independent CLR lazy-load trigger a discovered module's own constructor parameter types represent (`ConstructorInfo.GetParameters()`/`ParameterInfo.ParameterType`). The proof-of-concept showed a second, wholly undeclared assembly reachable only through a non-compliant constructor's own parameter type — never through any base type — again reached Module Discovery with zero trust checking, including the more severe variant where the same module also exposes an alternate, individually-compliant constructor and so is not even rejected on its own conformance check. This finding is the direct commissioning trigger for `WP 13.9.3`, immediately below. Zero `src/`/`tests/`/`docs/` files touched by this Work Package itself. | Verification only | **Complete** |
| `WP 13.9.3` | Multi-Assembly Trust-Boundary Remediation — closes `WP 13.9.2`'s own finding in full. `PluginAssemblyLoader.DiscoverModuleTypes`'s own fixed-point scan widened so each step's own `AppDomain` diff is taken around forcing resolution of every discovered module type's every public constructor's every parameter's `ParameterType`, not only around `GetTypes()`/`IsAssignableFrom` — `HasCompliantConstructor` itself is unchanged; only the point at which its own unavoidable reflection side effects are allowed to fire moved earlier, into this method's own already-diffed scan window. New file `tests/Tempest.Core.Tests/Plugins/PluginAssemblyLoaderMultiAssemblyTrustTests.cs` (8 tests): the direct case, the alternate-compliant-constructor case (the more severe variant `WP 13.9.2` found), a three-assembly transitive chain proving the fix generalises beyond one hop, and a benign multi-assembly-with-granted-capability case proving no legitimate plugin regresses. One new `DynamicPluginAssemblyBuilder.cs` builder method, `BuildPrimaryPluginAssemblyWithExternalConstructorParameter`. `ADR-0111` gained a "Corrected, `WP 13.9.3` Multi-Assembly Trust-Boundary Remediation" Status-section note and Decision-text amendment — every scan step's own "examining an assembly" now explicitly means forcing resolution of both base-type chains and constructor parameter types, the `AppDomain` diffed around both, not `GetTypes()` alone. No new isolation mechanism, no `AssemblyLoadContext`, no process separation — widens the existing capability-scoped enforcement mechanism's own coverage only; the fixed-point traversal's own termination guarantee (a visited-assembly `HashSet`) is unchanged. See `Plugin Trust & Isolation Architecture.md`'s own Risks section for the full before/after account. | Implementation | **Complete** |
| `WP 13.9.4` | Trust-Denial Execution Boundary Remediation — found and closed a separate, more severe defect than the multi-assembly scan gap above: `PluginTrustDeniedException` isolated a denied plugin only from `PluginAssemblyLoader.LoadPlugins`'s own returned list and `PluginRegistryState.Loaded` — nothing stopped the plugin's already-loaded assembly (`Assembly.LoadFrom` runs before this check; `ADR-0015`: that step cannot be undone) from being separately, redundantly rediscovered by Module Discovery and Hosted Service Discovery (both deliberately plugin-unaware, `ADR-0110`) and fully lifecycle-run — a denied module's ambient component principal is always `null`, and `null` is treated as First-Party (`PluginTrustPermission.IsFirstParty`). True of every denial reason, not only the multi-assembly case; true since `WP 13.2A` first introduced trust denial. Closed via three new files (`src/Tempest.Core/Plugins/IPluginDeniedTypeRecorder.cs`, `IPluginDeniedTypeRegistry.cs`, `PluginDeniedTypeRegistry.cs` — a Host-owned registry mirroring `PluginComponentPrincipalRegistry`'s own established read/write-split pattern), `EnforceTrust` reordered (`PluginAssemblyLoader.cs`) so `DiscoverModuleTypes` runs unconditionally before either static trust check (not only ahead of the constructor-conformance check, so a capability-ceiling denial also has its full type set recorded), and two new filters entirely within `TempestHost.cs`'s own orchestration, at Module Registration and Hosted Service Registration. **This Work Package's own Adversarial Review found its first pass (Module Registration only) itself incomplete before this Work Package concluded** — broadened mid-WP to also cover Hosted Service Registration, since a single `Type` implementing both `IModule` and `IHostedService` could otherwise still reach `StartAsync` unfiltered via the second, wholly independent pipeline — hence the registry/interfaces are named `IPluginDeniedTypeRecorder`/`Registry` (not `...ModuleType...`), reflecting that final, broadened scope; one registry, keyed on `Type` alone, covers both pipelines from the one recording pass. New tests in `tests/Tempest.Core.Tests/Runtime/TempestHostPluginTrustTests.cs` and `PluginAssemblyLoaderMultiAssemblyTrustTests.cs`; one new `DynamicPluginAssemblyBuilder.cs` builder method, `BuildDualModuleAndHostedServiceAssembly`. `ADR-0111` gained a "Corrected, `WP 13.9.4` Trust-Denial Execution Boundary Remediation" section — the Decision's own original text asserted denial "isolates the whole plugin before Module Discovery ever sees it… no change to Module Discovery, Registration, or Lifecycle," which was never actually true (only the second half was); the correction makes that first-stated intent actually true, rather than widening what "isolates" means. No new isolation mechanism, no `AssemblyLoadContext`, no process separation. | Implementation | **Complete** |
| `WP 13.9.5` | `v0.13.0` Trust & Security Final Adversarial Review — six parallel, read-only reviewers (Security, Architecture, Runtime/Lifecycle, Verification, Governance, Adversarial), each performed before seeing the others' conclusions, per `Engineering Readiness Review Architecture.md` §5's own independence discipline. Found the combined `WP 13.9.3`+`WP 13.9.4` boundary defensible for its own literal claims, but three independent reviewers (Runtime/Lifecycle, Security, Adversarial) each separately live-PoC-reproduced a further, distinct bug: `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own metadata-reading convention (`ADR-0027`) called `Activator.CreateInstance` for any `IModule` candidate type lacking `[ModuleMetadataAttribute]`, unconditionally, during Module Discovery — which runs strictly before the `WP 13.9.4` Module Registration filter is ever consulted — so a denied plugin's unattributed module constructor genuinely executed: real, verified code execution. A more severe variant was also confirmed: a denied, unattributed module with no public parameterless constructor hit `CreateDescriptor`'s own `ModuleDiscoveryException` guard, uncaught inside the Discovery loop — Host-fatal, crashing `RunAsync` entirely, a denial-of-service any plugin author could trigger deliberately, requiring no constructor trickery beyond a bare capability-ceiling denial. This Work Package produced no code changes of its own — a read-only review; its report was delivered conversationally to the commissioning session, not saved as a standalone file (this table's own row is retroactive, added by `WP 13.9.7`). Directly commissions `WP 13.9.6`, immediately below. | Verification/Governance | **Complete** |
| `WP 13.9.6` | Module Discovery Trust Boundary Remediation — closes the `WP 13.9.5` finding: four parallel Round-1 read-only reviewers converged on one design, then one implementation agent, then a fresh, independent adversarial review. `ReflectionFrameworkDiscoveryService` gained one optional constructor parameter, `Func<Type, bool>? isTypeExcluded = null`, consulted inside its existing candidate-scanning loop immediately after the existing `IsValidModuleType` check and strictly before `CreateDescriptor` is ever called — `TempestHost` supplies `deniedTypeRegistry.IsDenied` (the same, unmodified `WP 13.9.4` registry, already fully populated by Plugin Loading before Module Discovery ever runs). Defaults to `null` (never-excluded) for every existing caller/test, leaving every existing caller's behaviour completely unchanged; `Modules` gains no reference to `Plugins` (the predicate is generic), preserving `ADR-0110`'s "deliberately plugin-unaware" status at the type-reference level exactly as the `WP 13.9.4` filter already did. New test infrastructure: `tests/Tempest.Core.Tests/Plugins/ConstructorExecutionProbe.cs` (new file — a GUID-keyed, thread-safe static invocation counter an emitted plugin module's own IL calls into, proving whether `Activator.CreateInstance` genuinely ran for that exact type); one new `DynamicPluginAssemblyBuilder.cs` builder method, `BuildUnattributedPluginModuleWithConstructorProbe`; one new `ModuleFixtures.cs` fixture, `ConstructorTrackingModule`; new tests in `ReflectionFrameworkDiscoveryServiceTests.cs` (2) and `TempestHostPluginTrustTests.cs` (7). `ADR-0111` gained a "Corrected, `WP 13.9.6` Module Discovery Trust Boundary Remediation" section — the third and, per this Work Package's own fresh, independent Adversarial Review (mutation-tested against the actual test suite twice, independently, plus a fully independent standalone proof-of-concept project), final correction closing the "isolates the whole plugin" execution boundary `ADR-0111`'s Decision first stated; `Plugin Trust & Isolation Architecture.md`'s own Status header and Risks section updated with the full `WP 13.9.0`→`WP 13.9.6` chain. The existing `WP 13.9.4` Registration-time filter remains in place, unchanged, as harmless defense-in-depth for the module pipeline and still fully load-bearing for Hosted Service Registration (confirmed to need no equivalent fix — `HostedServiceDiscoveryService` never instantiates a candidate at all). Verified non-vacuous by mutation testing (temporarily disabling the guard reproduced both the quiet-execution and the `Faulted`-crash failure modes exactly) and by the fresh adversarial review, finding no remaining or newly-introduced gap. | Implementation | **Complete** |
| `WP 13.9.7` | Trust Boundary Integration & Commit — sole implementation/integration agent, no parallel writers. Integrates, reconciles, and commits the entire `WP 13.9.1`–`WP 13.9.6` remediation chain (six rows above) as one commit on `feature/v0.13.0` — the net effect: the plugin trust platform's execution boundary is now closed end-to-end, no denied plugin's code executes at any point from Plugin Discovery through Module/Hosted-Service Discovery, Registration, and Lifecycle, for any denial reason, across single- or multi-assembly plugins, for either `IModule` or `IHostedService`. Verified the changeset directly against real, current source before touching anything (not the commissioning summary alone); confirmed `ADR-0111` and `Plugin Trust & Isolation Architecture.md` each accurately describe the final code state, with no "not yet implemented"/"pending"/"to be closed" language remaining for this chain's own scope. **Found and fixed the one genuine governance staleness `WP 13.9.5`'s own Governance reviewer had flagged as needing to close before commit**: `ADR Register.md`'s own `ADR-0111` Entries-table citation had accumulated only a `WP 13.9.1 (corrected)` phase while three further corrections (`WP 13.9.3`/`WP 13.9.4`/`WP 13.9.6`) had already landed directly in `ADR-0111`'s own file without this register's own citation field ever being extended to match — corrected to a full six-phase citation, each phase carrying its own short parenthetical. Added this row and the five immediately above it (`WP 13.9.2`–`WP 13.9.6`, retroactively, plus this one); prepended six matching entries to `PROJECT_STATUS.md`. Confirmed, by direct convention check against `Academy Index.md`'s own stated maintenance rule and the `WP 13.2B`/`WP 13.9.1` precedent (neither has an Academy retrospective, both closed with only a `WorkPackages.md` row and a `PROJECT_STATUS.md` entry), that `WP 13.9.1`–`WP 13.9.6` do not require new Academy retrospective files — not scope creep to omit them. `governance-healthcheck.ps1`: 7 passed, 1 warned (pre-existing `v0.9.0`/`v0.10.0` informational, unrelated), 0 failed, confirmed identical before and after every documentation edit — zero new drift introduced. Debug and Release builds both 0 Warnings/0 Errors (`-p:TreatWarningsAsErrors=true`); full regression, both configurations, independently re-run fresh: **2,538/2,538 passing** (2,317 `Tempest.Core.Tests` + 221 `Tempest.Desktop.Tests`). Working tree confirmed, directly via `git status --short`, to contain only files accounted for by the commissioned changeset or this Work Package's own governance edits, before staging. Not pushed, not tagged, no release created, per this Work Package's own explicit instruction. | Governance/Process | **Complete** |
| `WP 13.10A` | Plugin Platform Hardening Architecture — a read-only architecture/security review of the complete `v0.13.0` plugin platform (`WP 13.0A`–`WP 13.9.7`), six parallel disciplines (Plugin Runtime Architecture, Security/Trust, Lifecycle/Composition, API/Compatibility, Verification/Testability, Governance/Documentation), no implementation. Found and formally registered two new, genuinely distinct Release-Blocking findings, both live-PoC-confirmed against the current, committed code, neither a variant of anything the `WP 13.9.1`–`WP 13.9.6` remediation chain closed: `TD-51` (`PluginAssemblyLoader.EnforceTrust`'s static constructor-conformance/denylist check — `HasCompliantConstructor`, and with it the `NeverEligibleServiceResolveTypes` denylist — was invoked only against a plugin's discovered `IModule` types, never its discovered `IHostedService` types, so an `IHostedService`-only plugin's constructor was never inspected at all; compounded by `HostedServiceManager` having no `componentScopeProvider`-equivalent hook at all, unlike `ModuleLifecycleManager`, so even a legitimately-passing plugin's hosted service ran with a `null`, First-Party-treated ambient component principal) and `TD-52` (`IIdentityService.EstablishCurrentPrincipal` calls `ICurrentPrincipalAccessor.SetCurrent` directly, and `IIdentityService` is an ordinary, DI-public, un-denylisted interface — a plugin declaring `plugin.services.resolve:Tempest.Core.Identity.IIdentityService` passes `EnforceTrust` cleanly, then can call `EstablishCurrentPrincipal` for any configured identity at runtime, with no ownership check). Found by `WP 13.10A`'s own Security/Trust and Lifecycle/Composition reviewers independently, each with a live proof-of-concept. Classified Release Blocking for third-party plugin support only — `v0.13.0`'s own already-shipped, already-signed first-party modules are unaffected, and WP14 UI/UX work is untouched, since it does not touch the plugin trust boundary. Both findings judged closable via existing mechanisms, requiring no new isolation mechanism and no new ADR — only a Corrected note to `ADR-0111`, the already-established amendment pattern `WP 13.9.1`/`WP 13.9.3`/`WP 13.9.4`/`WP 13.9.6` each used. `Technical Debt Register.md` gained `TD-51`/`TD-52` (50 → 52 tracked items); `Plugin Trust & Isolation Architecture.md`'s own Status header and Risks section gained a "Found, `WP 13.10A`" entry. No standalone report file — delivered directly, in-session, to the commissioning conversation. Directly commissions `WP 13.10B`, immediately below. | Architecture/Security | **Complete** |
| `WP 13.10B` | Plugin Trust Hardening Implementation — closes both `WP 13.10A` findings in full. `PluginAssemblyLoader.HasCompliantConstructor`/`DiscoverModuleTypes` now run the constructor-conformance/denylist check identically against every discovered `hostedServiceTypes` entry, not only `moduleTypes` (`TD-51`, first half); `BackgroundServices.HostedServiceManager` gained a new, optional `Func<Type, IDisposable?>` `componentScopeProvider` constructor parameter, mirroring `Modules.ModuleLifecycleManager`'s own established `ADR-0111` hook (keyed by `Type`, since a hosted service carries no `Id` the way a module does), established immediately before and disposed immediately after each `StartServiceAsync`/`StopServiceAsync` call — `TempestHost` supplies a non-null provider closing over the same `ICurrentComponentAccessor`/plugin component-principal registry `ModuleLifecycleManager`'s own hook already uses (`TD-51`, second half). `Identity.IdentityService.EstablishCurrentPrincipal` gained a new dynamic `IPermissionEvaluator.RequirePermission` gate, checked against a new, sixth capability key, `plugin.identity.establish` (`Plugins.PluginCapability.IdentityEstablish`) — mirroring `NavigationService.Register`'s own existing `ADR-0111` gate exactly, skipped (not merely satisfied) when the caller's ambient component principal is `null` or First-Party, so every existing first-party caller observes zero behavioural change (`TD-52`). **This Work Package's own independent Adversarial Security review found and fixed a compounding regression, mid-implementation, before any commit, disclosed here in full rather than left to its own commit message alone**: `DiscoverModuleTypes`'s own `WP 13.9.3` pre-resolution loop originally iterated `moduleTypes` only; once `HasCompliantConstructor` was extended to also check `hostedServiceTypes`, an `IHostedService`-only plugin with a genuinely unresolvable constructor-parameter type reached `HasCompliantConstructor`'s own `GetParameters()` call as the first resolution attempt for that type, with no exception handling anywhere in reach — throwing uncaught out of `LoadPlugins` and `TempestHost.RunAsync` entirely, a Host-wide crash strictly worse than `TD-51`'s own original gap. Found independently twice: an `IModule`-only version of this exact pre-resolution fix had already landed when a second reviewer reproduced the identical gap for hosted services; a first attempted fix also placed its own `try`/`catch` one level too deep and never actually caught anything for its own documented scenario — corrected before landing. Closed by iterating both `moduleTypes` and `hostedServiceTypes` uniformly, converting the resulting `TypeLoadException`/`FileNotFoundException`/`FileLoadException`/`BadImageFormatException` into a `PluginTrustDeniedException`, isolating the one plugin exactly like any other trust-check failure. Implemented via strict per-file ownership across three parallel production agents (`PluginAssemblyLoader.cs`; `HostedServiceManager.cs`; `IdentityService.cs`/`PluginCapability.cs`), centrally integrated (`TempestHost.cs`), then three test-writing agents (sequenced to avoid a shared-builder-file conflict), verified by an independent Adversarial Security review and an independent Architecture/API review — both found the same compounding regression above, independently, and confirmed it fixed after a second correction. `ADR-0111` gained a "Corrected, `WP 13.10B` Hosted Service & Identity Trust Boundary Remediation" Status-section note and matching Decision-text amendment (finalized, `WP 13.10C`). No new isolation mechanism, no ALC, no process separation. Debug/Release builds both 0 Warnings/0 Errors; full regression both configurations 2,553/2,553 passing; `governance-healthcheck.ps1` 7 passed/1 pre-existing warned/0 failed. Not committed by this Work Package itself — integrated and committed by `WP 13.10C`, immediately below, alongside one further permanent-test-coverage correction that review found. | Implementation | **Complete** |
| `WP 13.10C` | Plugin Trust Hardening Review & Integration — the same integration-and-commit role `WP 13.9.7` established for the `WP 13.9.1`–`WP 13.9.6` chain, applied here to the `WP 13.10A`/`WP 13.10B` chain. Four fresh, read-only sub-agents (Security/Adversarial, Architecture/API, Verification/Test + RAM/Concurrency, Governance/Documentation) independently re-verified `WP 13.10B`'s own fix directly against real, current source, not the commissioning summary alone — all four converged clean on `TD-51`/`TD-52` themselves (non-vacuous, reverted-and-restored on the two most load-bearing tests independently by two separate reviewers), no public API break, no unintended ADR/dependency change, and definitively confirmed no test-suite parallelism/RAM configuration was ever changed by `WP 13.10B` (every new real-assembly-loading test correctly reuses the existing `[Collection("Console output capture")]` convention; the session's own repeatedly-observed "Test host process crashed" pattern was independently traced to external, concurrent multi-agent process contention, not a test-suite design defect). **One permanent-coverage gap found and closed directly by this review**: the twice-found, twice-fixed Host-crash regression (an unresolvable constructor-parameter type on an `IHostedService`-only plugin faulting the whole Host) had only ever been proven fixed via throwaway proof-of-concept code across two independent `WP 13.10B` reviewers — a new permanent test, `LoadPlugins_FirstHostedServiceOnlyPluginHasUnresolvableConstructorParameterType_IsolatesFailure_SecondLegitimatePluginStillLoads`, closes it, non-vacuousness independently confirmed (reverted, observed to fail with the exact uncaught `FileNotFoundException`, restored). **One small, low-severity, pre-existing gap disclosed but deliberately not fixed, to avoid scope creep beyond this review's own remit**: the "unresolvable constructor parameter type" denial path throws directly from inside `DiscoverModuleTypes`, before `EnforceTrust` ever reaches a `RecordDenied` call site, so that one specific type is never added to `deniedTypeRegistry` — a second, independent construction attempt is made during Registration, safely failing for the identical reason (no code execution, not a crash); confirmed pre-existing since `WP 13.9.3` for the `IModule` axis, `WP 13.10B` merely widened its reach. **One governance-only note disclosed, not a code defect**: `plugin.identity.establish` is grantable to any trust tier above `UnsignedLocal`, and `EstablishCurrentPrincipal` accepts an arbitrary identity with no ownership check by design (`ADR-0043`) — a materially broader blast radius than every other v1 capability, warranting explicit governance sign-off before any third-party publisher is actually granted it. **Governance & Documentation workstream**: moved `TD-51`/`TD-52` from Open to Resolved in `Technical Debt Register.md`, each entry's own Status field extended to disclose the mid-`WP 13.10B` regression and both further disclosed items in full, mirroring `TD-49`'s own "disclose the full discovery history" convention; corrected the register's own stale `Total` summary line (last incremented `WP 13.3A`, never updated for `WP 13.10A`'s own `TD-51`/`TD-52` addition) to 52 tracked items — 18 Resolved, 1 Partially resolved, 33 Open. `Plugin Trust & Isolation Architecture.md`'s own "Found, `WP 13.10A` — NOT YET CLOSED" Risks entry corrected to "Closed, `WP 13.10B`," the regression and both further items disclosed there too; Status-header narrative corrected to match, including the now-superseded "third and final correction" claim in `ADR-0111`. `ADR-0111` gained the "Corrected, `WP 13.10B`" Status-section note and Decision-text amendment (static check's stated scope extended to `IHostedService`; "Dynamic, at each call site" list extended to `IIdentityService.EstablishCurrentPrincipal`) — not a new ADR, per both `WP 13.10A` reviewing disciplines' own independent assessment; `ADR-0111`'s own Related Documents extended to cite `TD-51`/`TD-52`; `ADR Register.md`'s own `ADR-0111` citation extended to a seventh phase. Added this row and the two immediately above it; prepended a matching entry to `PROJECT_STATUS.md`. Confirmed, by direct convention check against `Academy Index.md`'s own stated maintenance rule and the `WP 13.2B`/`WP 13.9.1`–`WP 13.9.6` precedent, that none of `WP 13.10A`/`WP 13.10B`/`WP 13.10C` requires a new Academy retrospective file; confirmed `Security Roadmap.md` items 1/2/10 require no update, matching the identical precedent the `WP 13.9.1`–`WP 13.9.6` chain's own integration already set. `governance-healthcheck.ps1`, Debug/Release builds, and full regression all independently re-run fresh after every documentation correction — see `PROJECT_STATUS.md`'s own matching entry for the exact final figures. Working tree confirmed, directly via `git status --short`, to contain only files accounted for by the commissioned changeset or this Work Package's own governance edits and one new permanent test, before staging. Committed as a single commit alongside the complete `WP 13.10B` implementation. Not pushed, not tagged, no release created. | Governance/Process | **Complete** |
| `WP 13.11A` | Plugin Platform Final Hardening Architecture Review — read-only, six parallel disciplines (Security/Adversarial, Runtime/Lifecycle Architecture, API/Compatibility, Verification/Test/RAM, Governance/Documentation, UI/UX Dependency), determining whether anything genuinely blocks WP14 UI/UX after `WP 13.0A`–`WP 13.10C`. **One Release-blocking finding** (for third-party plugin support, not `v0.13.0`'s own currently-shipped state): the Security/Adversarial reviewer re-examined `TD-51`'s own already-disclosed "unresolvable constructor parameter type" residual gap — `WP 13.10C` had characterised it uniformly as safely isolated, no crash — and found that characterisation false for the `IModule` axis. `PluginAssemblyLoader`'s denial path for this specific case throws before `EnforceTrust` ever reaches `RecordDenied`, so the offending type is never recorded in `deniedTypeRegistry`; `TempestHost.cs`'s own `WP 13.9.6` Module Discovery crash-prevention filter (`isTypeExcluded: deniedTypeRegistry.IsDenied`) therefore never excludes it; `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own unguarded `type.GetConstructor(Type.EmptyTypes)` call then throws an uncaught `TypeLoadException`/`FileNotFoundException`, propagating through `TempestHost.RunAsync`'s own outer catch to a genuine Host crash — reachable by a single, otherwise-inert `IModule` type, any trust tier, zero requested capabilities. Empirically confirmed (a real, minimal repro built and torn down during the review) and independently re-confirmed by direct source read before any documentation was updated. `TD-51` reopened in `Technical Debt Register.md` accordingly (Resolved → Reopened, Release Blocking for third-party plugin support); a minimal follow-up Work Package, `WP 13.11B`, is recommended (not yet scheduled, not implemented here — this Work Package is read-only by its own explicit instruction). **Two further, genuinely new, low-severity, non-blocking findings** by the same reviewer, neither plugin-trust-specific: `TD-53` (`HostedServiceManager.StartServiceAsync`'s own critical-service-failure classification is silently wrong if `_serviceProvider.GetService` itself throws before `tracked.Instance` is assigned) and `TD-54` (`ITempestServiceProvider`'s own DI-escape closure is correct today only incidentally, not as an explicit `NeverEligibleServiceResolveTypes` denylist entry). **Zero WP14-blocking findings** from any of the six disciplines — Runtime/Lifecycle, API/Compatibility, Verification/Test/RAM, and Governance/Documentation each independently converged clean (full regression independently re-run fresh, 2,554/2,554 both configurations, matching the documented figure exactly); the UI/UX Dependency reviewer found two genuine gaps in the plugin platform's own UI-facing surface — no capability-gated Workspace view/panel extension point exists yet for a plugin to contribute real UI content (only Navigation/Commands, which are proven end-to-end today), and no plugin-ownership/trust-tier/granted-capability projection exists yet on any DI-public diagnostics surface a future plugin-management UI would need — but assessed both as WP14's own first-order design scope to build, not a prerequisite hardening gap blocking WP14 from starting. **One documentation-accuracy correction, unrelated to the plugin trust boundary**, made directly by the Governance/Documentation reviewer and integrated centrally: `Future Capability Register.md`'s `FCR-0001`/`FCR-0002` entries had drifted stale since `WP 13.0A` itself, still citing `WP 13.0B` as "implementation pending" through `WP 13.1A`, `WP 13.2A`, and `WP 13.3A` (which reconfirmed **Implemented** in this same register's own "Last Reviewed" narrative without ever correcting the table cells it contradicted) — corrected to cite `WP 13.2A` as the real implementer and `Status: Implemented`. `Technical Debt Register.md` updated: `TD-51` reopened, `TD-53`/`TD-54` added (52 → 54 tracked items; 18 → 17 Resolved, 1 Partially resolved unchanged, 33 → 36 Open); `Plugin Trust & Isolation Architecture.md`'s own Status header and Risks section gained a "Reopened, `WP 13.11A`" entry with the full technical account. `governance-healthcheck.ps1`, Debug/Release builds, and full regression all independently re-run fresh after every documentation edit. No standalone report file — delivered directly, in-session, to the commissioning conversation. Stopped before commit, per this Work Package's own explicit instruction — the working tree is left with only these documentation corrections; `WP 13.11B`'s own implementation is a separate, not-yet-commissioned future Work Package. | Architecture/Security | **Complete** |
| `WP 13.11B` | TD-51 Trust-Denial Crash Remediation — closes `WP 13.11A`'s single Release-blocking finding (`TD-51`, reopened) by that review's own recommended minimum fix, taking both of its options and no wider. Four parallel sub-agents (Security/Adversarial, Runtime/Lifecycle, Verification/Test, Governance/Documentation), the three analysis disciplines strictly read-only, every production and documentation edit integrated centrally with one owner per file. **(a) The root cause.** `PluginAssemblyLoader.DiscoverModuleTypes`'s own "unresolvable constructor parameter type" failure no longer throws `PluginTrustDeniedException` from inside the scan. It is surfaced to `EnforceTrust` through a new `out PluginTrustDeniedException?` parameter, which calls `RecordDenied` for every discovered `IModule` and `BackgroundServices.IHostedService` type (`IPluginDeniedTypeRecorder`, `WP 13.9.4`) before throwing it — previously the throw escaped `DiscoverModuleTypes` before `EnforceTrust` could reach either existing `RecordDenied` call site, so `deniedTypeRegistry` stayed empty for this one denial reason, `TempestHost.cs`'s own `WP 13.9.6` Module Discovery filter (`isTypeExcluded: deniedTypeRegistry.IsDenied`) had nothing to exclude, and `ReflectionFrameworkDiscoveryService.CreateDescriptor`'s own unguarded `type.GetConstructor(Type.EmptyTypes)` call threw uncaught through `TempestHost.RunAsync`, crashing the whole Host. **Deliberately implemented as a complete fixed-point scan, not the partial type list `WP 13.11A` suggested** — this Work Package's own Security/Adversarial analysis found the partial-list shape actively unsafe: aborting mid-traversal leaves an assembly already resident in the `AppDomain` (enqueued but not yet dequeued, or pulled in earlier in the same step) unscanned and unrecorded, yet fully visible to Module Discovery's own deliberately plugin-unaware `AppDomain` scan (ADR-0110), and a well-formed module among those would have been registered and lifecycle-run with a `null`, and therefore First-Party-treated (`PluginTrustPermission.IsFirstParty`), ambient component principal — a silent trust bypass in place of a crash, strictly worse and not fail-closed. The failing type's remaining constructors are skipped via `break`, the enclosing loops continue, and the per-step before/after `AppDomain` diff still executes. **(b) The backstop.** `ReflectionFrameworkDiscoveryService.DiscoverModules` now wraps its own `CreateDescriptor` call in the same narrow `TypeLoadException`/`FileNotFoundException`/`FileLoadException`/`BadImageFormatException` guard already used at the one other call site in this codebase (`DiscoverModuleTypes`' own; corrected from "other call sites", `WP 13.11C`) — the candidate is excluded and logged, never a crash and never a silent inclusion. That class gains no plugin awareness (ADR-0110): it is a reflection guard, not a trust decision, and `ModuleDiscoveryException` derives from none of the four, so the long-standing `WP 5.3` "no parameterless constructor and no `[ModuleMetadataAttribute]`" guidance and every `ValidateMetadata` failure still propagate unchanged. **No ADR added or amended** — `ADR-0111`'s own Decision text already mandates the recording behaviour (a) restores, verbatim ("every `Type` this scan discovered for it … is recorded denied"), so this was an implementation divergence from an already-correct decision, independently re-confirmed by direct read of that Decision section before any edit; `ADR Register.md` therefore untouched, mirroring `WP 13.11A`'s own precedent. **No public API change and no new dependency** — every touched member is `private`, except the `internal` `DiscoverModules(IEnumerable{Type})` test seam, whose signature is unchanged. **Seven permanent regression tests** added across three files (four in `PluginAssemblyLoaderMultiAssemblyTrustTests.cs` covering both discovery axes and the recorded-type-is-excluded boundary plus the legitimate-plugin no-overreach direction; two in a new `ModuleDiscoveryUnresolvableConstructorTests.cs` covering the guard and its narrowness; one end-to-end Host test in `TempestHostPluginTrustTests.cs`). Non-vacuousness confirmed by reverting each half of the fix independently and observing exactly the predicted targeted failures — with `RecordDenied` reverted, the three recording tests fail; with the discovery guard reverted, the guard test fails on the raw reflection exception; with **both** reverted, the end-to-end Host test reproduces `WP 13.11A`'s crash exactly (`Assert.Equal() Failure: Expected: Running, Actual: Faulted`) — then restoring and confirming both production files byte-identical to their fixed versions. **Independent adversarial review after implementation** attacked the fix on ten axes — `break` scoping and loop termination, the unguarded `GetConstructors()` header, `HasCompliantConstructor` reachability after continuation, message and denial-set equivalence, `RecordDenied` blast radius, guard narrowness and definite assignment, fail-closedness, and test non-vacuity — and found the core fix clean on all of them. Two corrections applied: a stray deleted `/// </summary>` tag in a test helper's doc comment (invisible to the build, `GenerateDocumentationFile` being `false`) and an incomplete `<exception>` element on `EnforceTrust`, now that this denial is raised there rather than inside `DiscoverModuleTypes`. One genuine consequence tracked rather than silently accepted, now **`TD-55`**: `RecordDenied` records every type a denied plugin's transitive scan reached and `PluginDeniedTypeRegistry` is keyed on `Type` identity alone, so one plugin's denial can suppress another assembly's module types — latent since `WP 13.9.4` on the two existing denial paths, newly *reachable* on this third one because it previously recorded nothing at all. Fail-closed in direction (wrongly excluded, never wrongly included), presently dormant (`src/Plugins/` is empty), and `Tempest.Samples` provably unaffected by composition ordering rather than by design — which is precisely why it is tracked. Closing it needs per-plugin denial scoping, an architectural change beyond this Work Package's own explicitly minimal remit, so it was deliberately not attempted. `TD-51` moved **Reopened → Resolved** in `Technical Debt Register.md` and `TD-55` added (54 → 55 tracked; 17 → 18 Resolved, 1 Partially resolved unchanged, 36 Open unchanged in count but changed in membership), including the buried mid-paragraph counter-narrative cross-reference that would otherwise have gone stale; `Plugin Trust & Isolation Architecture.md`'s own Status header and Risks entry corrected "Not yet closed" → "Closed, `WP 13.11B`". `TD-53`/`TD-54` deliberately untouched — both low-severity, non-blocking, neither a variant of `TD-51`, and neither naturally nor directly implicated by this fix; left tracked. New test cost measured, not assumed: no parallelism increase (every new test sits in the existing `"Console output capture"` serialisation collection, mandatory here because these tests load real assemblies and `DiscoverModuleTypes` diffs the process-global `AppDomain`), no processes spawned, six collectible reflection-only load contexts created and unloaded, and roughly 7 small dynamic assemblies permanently resident in the default, non-unloadable `AssemblyLoadContext` (ADR-0015) — under ~2 MB, against 141 such existing calls in the suite. Debug and Release builds both 0 Warnings / 0 Errors; full regression 2,561/2,561 both configurations (2,554 documented baseline + 7 new); `governance-healthcheck.ps1` re-run fresh after every edit. No standalone report file — delivered directly, in-session, to the commissioning conversation. Committed together with `WP 13.11A`'s own documentation corrections, which stopped before commit by their own explicit instruction: the finding and its closure form a single commit. | Implementation | **Complete** |
| `WP 13.11C` | TD-51 Remediation Review & Trust-Boundary Verification — read-only review of `WP 13.11B` (commit `8341438`) against `TD-51`, `ADR-0110`/`ADR-0111`, and the real implementation, across four fresh sub-agents (Security/Adversarial, Architecture/Runtime, Verification/Test and RAM-concurrency, Governance/Documentation), with every revert experiment and every edit performed centrally. **The production fix is clean on every axis it was asked to prove**: an unresolvable constructor-parameter type cannot crash the Host on any traced path (full reflection inventory taken from `LoadPlugins` through Discovery, Registration, DI, Lifecycle, and both hosted-service pipelines); `RecordDenied` provably precedes every denial `EnforceTrust` surfaces, on all three denial paths — `DiscoverModuleTypes` now contains no `throw` at all; the `break` exits only the per-constructor loop, termination is bounded by the finite `AppDomain` assembly set, and the per-step before/after diff runs on every path; denied `IModule` **and** `IHostedService` types are excluded from Discovery, Registration, and Lifecycle, including a single type implementing both; the discovery backstop preserves `ModuleDiscoveryException`, `DuplicateModuleIdException` (thrown outside the guarded block entirely), `ValidateMetadata` failures, and `TargetInvocationException`; legitimate multi-assembly/multi-module plugins are byte-identical on the non-failing path (the only three divergence points are all `catch`-filter or null-check evaluations that do not fire); and there is no public API, ADR, dependency, or architecture-boundary regression — `ReflectionFrameworkDiscoveryService` references no `Tempest.Core.Plugins` type, directly or transitively. **Two genuine findings, both closed by this Work Package.** (1) **Material: `WP 13.11B`'s completed-fixed-point-scan decision had no regression coverage whatsoever.** That decision — deliberately rejecting `WP 13.11A`'s own recommended partial-list shape, on the grounds it would trade the Host crash for a silent trust bypass — is the security-critical half of the remediation and the part its rationale argued at greatest length, yet reverting it to the partial-list shape left **all 2,561 tests green**, because every existing unresolvable-parameter test deliberately places its secondary assembly where the default `AssemblyLoadContext` can never probe, so no assembly ever becomes resident mid-scan and the two shapes are indistinguishable. A future "simplification" back to the unsafe shape would have passed the entire suite. Closed by one new test and one new `DynamicPluginAssemblyBuilder` helper carrying both transitive-load mechanisms at once: a *reachable* secondary assembly pulled in by an anchor type's base-type-chain resolution during the scan step, plus an *unreachable* tertiary supplying the offending constructor parameter that trips the denial mid-step, before the step's own diff is reached. Proven non-vacuous in both directions — it fails on its own load-bearing assertion under the partial-list revert and passes with the fix — which also converts `WP 13.11B`'s trust-bypass argument from a stated rationale into a demonstrated one. (2) **A factually wrong documented claim**: "guard already used at three other call sites in this exact codebase", inherited verbatim from `WP 13.11A` and softened to an unquantified plural rather than corrected by `WP 13.11B`. Exactly **one** other call site carries that four-exception filter (`DiscoverModuleTypes`' own); `LoadOne`'s neighbouring guard is a different three-exception filter (`BadImageFormatException`/`FileLoadException`/`IOException`, omitting `TypeLoadException`) and the remaining reflection guards are single-`ReflectionTypeLoadException` catches. Corrected in all four documents that repeated it and in the production comment that asserted it. **All three defences proven non-vacuously by individual reversion** (the review brief's own test discipline): reverting `RecordDenied` fails 4 tests; reverting the discovery guard fails exactly 1; reverting the completed-scan semantics fails exactly the new test; reverting the first two together reproduces `TD-51`'s Host crash end-to-end. `TD-55` **extended** to name the complementary **under**-recording half of the same attribution defect, which its original "never wrongly included" wording concealed and which is genuinely not fail-closed: `DiscoverModuleTypes` takes its per-step `before` snapshot only after `LoadOne`'s `Assembly.LoadFrom` has returned, so an assembly already resident when a given plugin is scanned is never attributed to that plugin — `WP 13.9.1` reasoned such an assembly is resident "for an unrelated reason", but a second plugin is a related reason. Adversarial review could not construct a privilege escalation from it (the shared assembly's types run under the *passing* plugin's own grants, which is designed behaviour for a legitimate multi-assembly plugin, and a later plugin cannot substitute its own copy because the default context returns the already-loaded instance), so it is recorded as attribution imprecision of the same root cause, closed by the same per-plugin denial scoping. **`ADR-0013` reviewed, no amendment required** — its "Discovery throws → `Faulted`" clause is narrowed by the backstop, but same-category and fail-closed, and `ADR-0025`'s own category 8 already ratifies exactly this isolation for reflection/type-load failures while explicitly "extending `ADR-0013`'s existing boundary rather than complicating it"; the genuine residue (category 8 is written per-*assembly*, this guard is per-*candidate*, and it also covers first-party candidates, so a first-party module with a missing dependency assembly now disappears with a `Warning` rather than faulting loudly) is disclosed in `Plugin Trust & Isolation Architecture.md` rather than amended into either ADR. **Test parallelism deliberately unchanged** — the `"Console output capture"` collection is genuinely required for the new file (it is a *producer* side of the process-global `AppDomain`-diff race via `Assembly.LoadFrom`, even though it never invokes `PluginAssemblyLoader` itself), and no suite was globally serialised. Two pre-existing, non-blocking observations recorded but deliberately not acted on: that collection has become a catch-all conflating two distinct shared-state hazards (`Console.Out` redirection and `AppDomain` assembly loading), serialising ~32% of the Core suite, with `Logging/ConsoleLogSinkTests.cs` redirecting `Console.Out` while sitting *outside* it; and `PluginPlatformPerformanceTests.cs` is the suite's dominant RAM consumer at roughly 1,700 permanently-resident dynamic assemblies against this Work Package's 1 added. One pre-existing Academy staleness corrected in place: `07-plugin-architecture.md`'s "with zero change to `TempestServiceProvider`, Module Discovery, Registration, or Lifecycle" claim has been false since `WP 13.9.6`, not `WP 13.11B`, and now carries a supersession marker in that article's own established strikethrough style; `TempestServiceProvider` and Lifecycle genuinely remain unchanged. No new ADR and no `ADR Register.md` touch, mirroring `WP 13.11A`/`WP 13.11B`'s own precedent for a Work Package amending no ADR; no Academy retrospective article, mirroring the unbroken precedent across `WP 13.9.x`–`WP 13.11B`. 55 tracked debt items unchanged, 18 Resolved, 1 Partially resolved, 36 Open — no status changed; `TD-53`/`TD-54` deliberately untouched. `governance-healthcheck.ps1`, Debug/Release builds, and full regression all re-run fresh after every edit: 0 Warnings / 0 Errors both configurations, 2,562/2,562 both configurations (2,561 + 1 new). No standalone report file — delivered directly, in-session, to the commissioning conversation. | Architecture/Security | **Complete** |
| `WP 13.11D` | `v0.13.0` Plugin Platform Exit Review — read-only review of the complete plugin platform through `WP 13.11C` (`c0f3a19`), across six fresh sub-agents (Security/Adversarial, Architecture/Runtime, API/Compatibility, Verification/Test and RAM-concurrency, Governance/Documentation, WP14 UI/UX Dependency), scoped deliberately to one question: does anything genuinely prevent starting WP14 UI/UX? **All six returned no blocker — the platform is declared READY FOR WP14.** Verified against real source, centrally re-confirmed before anything was written: `TD-51` remains closed (all four elements of the `WP 13.11B` mechanism intact — the `out PluginTrustDeniedException?` seam, `RecordDenied` preceding all three throws, the completed fixed-point scan, and the four-exception `CreateDescriptor` guard); `TD-52` remains closed (hosted-service constructor conformance plus `HostedServiceManager`'s `componentScopeProvider` hook, with `IdentityService`'s own `plugin.identity.establish` gate live); `TD-55` correctly classified non-blocking, the Security reviewer having attempted an escalation from both its halves and failed — the over-recording half is fail-closed, and the under-recording half binds a shared assembly to whichever plugin legitimately loaded it first, which requires filesystem write access to exploit; no public API, dependency, or ADR regression anywhere in `v0.13.0` (111 ADRs matching 111 register rows, **zero** `.csproj`/`Directory.Build.props`/lock-file changes across the whole release, `TargetFramework` unchanged, and nothing packaged for external consumption — so the several binary-breaking-but-source-compatible constructor additions are inert); multi-assembly/multi-module behaviour sound, with the fixed-point scan proven terminating and complete and sequential plugin loads proven non-interfering; test-suite RAM/concurrency acceptable and structurally capped (every heavy dynamic-assembly emitter already sits inside the single serialised collection, so concurrent peak is bounded by construction); and plugin-facing UI dependencies clearly identified. **The UI dependency finding, stated plainly:** a plugin can contribute Navigation items and Commands — both capability-gated and both proven end-to-end against real signed plugin assemblies — but **cannot contribute any renderable UI content at all**. `NavigationItem` carries no rendering concern by explicit design, and the real UI seams (`IWorkspaceViewFactory`, `IWorkspacePanel`, `IWorkspaceView`) live in `Tempest.App`, which plugin projects do not and structurally cannot reference. No trust-tier, granted-capability, or ownership projection exists on any DI-public diagnostics surface either. Both gaps assessed as **WP14's own first-order design scope, not prerequisites**: there is no plugin-facing UI seam to break because none exists, and every extension WP14 would need (`IDiagnosticsProvider` members, `PluginRegistryEntry` fields, new `PluginCapability` keys, a new UI-contract type) is purely additive to contracts explicitly documented as extensible. First-party UI is confirmed *not* accidentally blocked by the new gates — `IsFirstParty(null)` is true and `Rank(null)` is the top rank, so WP14's own UI code skips every `RequirePermission` rather than merely satisfying it. **One genuine new finding, added as `TD-56`, deliberately not fixed** (this Work Package is read-only by its own instruction): a plugin's own **constructor** executes outside its component scope — `ModuleLifecycleManager` and `HostedServiceManager` both create the instance before opening the `componentScopeProvider` scope — so plugin code runs during construction with a `null` ambient principal, which every capability gate treats as genuine first-party and skips entirely, at the top trust rank. A `VerifiedSigned` plugin granted only `plugin.services.resolve:Tempest.Core.Identity.IIdentityService`, and deliberately *not* `plugin.identity.establish`, can therefore call `EstablishCurrentPrincipal` from its constructor and defeat the exact gate `TD-52` exists to enforce; worse, a handler subscribed to `IEventBus` from a constructor is recorded with a `null` owner and is dispatched unscoped, as first-party, for the remaining life of the process. Every link independently re-confirmed against real source, and the reason no prior review caught it established: the existing `AmbientPrincipalCaptureProbe` records inside `StartAsync`, which *is* correctly scoped, so it passes regardless. Graded **Release Blocking for third-party plugin support**, exactly as `TD-51` was, and explicitly **not** blocking WP14 or `v0.13.0` as shipped — dormant (`src/Plugins/` holds only `README.md`), unreachable below `VerifiedSigned` (that tier's ceiling admits no `plugin.services.resolve:*` key at all), causes no crash, and is entirely orthogonal to UI/UX work. This is the one place `v0.13.0`'s trust boundary is fail-**open** rather than fail-closed, which is why it is graded above `TD-55` despite both being dormant. Minimum follow-up defined in its own register row: move the scope to enclose construction on both axes — the scope key is available before construction on each — which is a statement-ordering change within `ADR-0111`'s existing hook, not a new seam, and must be paired with coverage that probes the constructor rather than only `StartAsync`. **Three documentation-accuracy defects corrected**, all disclosed rather than silently fixed: `WP 13.9.0` was genuinely complete (commit `0b8726f`, with its own Engineering Release Report) yet had no row in the delivered table below — the only completed `v0.13.0` Work Package missing from it; that table's preamble still read "Two process/governance Work Packages completed so far ... `WP 13.0A` ... has not yet begun", written when it held two rows and never updated as it grew past twenty; and `Plugin Trust & Isolation Architecture.md`'s closing pointer named only `TD-53`/`TD-54`, omitting `TD-55` and `TD-56`, the two items that are actually plugin-trust-specific and therefore belong in that document. **A fourth reported defect was rejected on inspection**: `WP 13.9.0`'s `Not started` cell in the roadmap-*predicted* table above is correct and deliberately left alone — that table is a frozen branch-cut snapshot which the document itself states is never re-derived, and "correcting" it would destroy its only purpose. 55 → 56 tracked debt items; 18 Resolved and 1 Partially resolved both unchanged; 36 → 37 Open. No existing item's status changed; `TD-53`/`TD-54` untouched. No ADR added or amended and `ADR Register.md` correctly untouched, mirroring `WP 13.11A`–`WP 13.11C`; no Academy article, mirroring the unbroken precedent since `WP 13.3B`. **Zero `src/` and `tests/` files touched** — this Work Package implemented nothing, fixed nothing, and created no speculative ADR or Work Package, per its own explicit instruction. `governance-healthcheck.ps1` and Debug/Release builds re-run fresh after every documentation edit, all clean (0 Warnings / 0 Errors; 7 passed, 1 warned, 0 failed). Full regression: Debug **2,562/2,562**; Release returned **2,561/2,562** on its first run and **2,562/2,562** on an immediate re-run of the identical binaries — recorded as observed rather than rounded up. The failing test's identity was lost to that run's log filter and is deliberately not asserted, but it cannot be a regression from this Work Package (zero `src/` and `tests/` files changed) and matches the known shape of `TD-34`, whose entry has been extended to record it and to name the untracked sibling hazard this Work Package's own Verification reviewer found. No standalone report file — delivered directly, in-session, to the commissioning conversation. | Governance/Process | **Complete** |

## Related Documents

`docs/releases/v0.11.0/WP11.0B Architecture Roadmap.md` §3 (this
release's own originally-predicted scope, now confirmed triggered);
`docs/releases/v0.12.0/WorkPackages.md` (the immediately preceding
release, and this document's own format precedent); `PROJECT_STATUS.md`;
`docs/governance/Future Capability Register.md` (`FCR-0001`–`FCR-0004`).
