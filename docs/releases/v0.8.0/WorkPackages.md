# TempestOS v0.8.0 — Work Packages

## Status

**Complete — recommended APPROVED by `WP 8.9.0`.** The Status text
below this line, and the single-row table beneath it, are this
document's own **original text**, retained for historical record, not
deleted, per this project's "never delete, mark superseded" convention.
It was accurate at the time it was written (`WP 8.0A` genuinely was
this release's own first, and at that time only, Work Package) but was
never updated across the eight further Work Packages that followed it,
since none of them had this specific document in its own scope — the
identical documentation-currency gap `WP 7.4.0` found in `v0.7.0`'s own
`WorkPackages.md`, now recurring here, found and closed by `WP 8.9.0`.

### What `v0.8.0` Actually Delivered

Nine Work Packages, two sequential tracks, both following this
project's own architecture-then-contracts-then-implementation
discipline (`FOUNDATION.md` §1) throughout:

| Work Package | Scope | Type | Status |
|---|---|---|---|
| `WP 8.0A` | Engineering Workspace Architecture | Architecture only | Complete |
| `WP 8.0B` | Workspace Contracts | Contract review only | Complete |
| `WP 8.1A` | Workspace Shell | Implementation | Complete |
| `WP 8.0C` | Engineering Workspace UX Specification | Product/UX only | Complete |
| `WP 8.1B` | Navigation & Project Explorer | Implementation | Complete |
| `WP 8.1C` | Engineering Cockpit | Implementation | Complete |
| `WP 8.2A` | Engineering Domain Architecture | Architecture only | Complete |
| `WP 8.2B` | Engineering Domain Contracts | Contract review only | Complete |
| `WP 8.2C` | Engineering Domain Implementation | Implementation | Complete |
| `WP 8.9.0` | Release Preparation & Product Baseline | Verification only | Complete (this Work Package) |

See `docs/releases/v0.8.0/ReleaseNotes.md`, `docs/releases/v0.8.0/
Retrospective.md`, and `PROJECT_STATUS.md` for what each Work Package
actually shipped.

### Original Text (Historical, Not Corrected Below This Line)

**In progress.** `feature/v0.8.0-engineering-workspace` was cut from
`main` at the `v0.7.0` tag, mirroring `v0.6.0`'s own "prepare next
branch and release documentation" precedent (`18e61d5`). `WP 8.0A`
(Engineering Workspace Architecture) is this release's own first Work
Package — an architecture and design phase only, no implementation —
following this project's own standing discipline (`FOUNDATION.md` §1:
architecture precedes implementation for anything non-trivial).

## Work Packages

| Work Package | Scope | Status |
|---|---|---|
| `WP 8.0A` | Engineering Workspace Architecture — workspace philosophy, user journeys, main window layout, navigation model, Project Explorer, engineering object hierarchy, docking strategy, view architecture, digital thread visualisation, state management, extensibility model, interaction patterns. Architecture and design only; no implementation. | In progress |

Further Work Packages (contract review, implementation) are not yet
scoped — each must pass through this project's own standing discipline
before any further phase begins, per `WP 8.0A`'s own explicit "no
implementation" constraint.

## Related Documents

`docs/releases/v0.8.0/ReleaseNotes.md`; `docs/releases/v0.8.0/
Retrospective.md`; `docs/releases/v0.8.0/WP8.9.0 Release Readiness
Report.md`; `docs/releases/v0.7.0/ReleaseNotes.md` (the immediately
preceding release); `VISION.md`; `docs/governance/Future Capability
Register.md`.
