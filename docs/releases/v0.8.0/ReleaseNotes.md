# TempestOS v0.8.0 — Release Notes

## Status

**Not yet written.** `v0.8.0` ("Engineering Workspace," working name) is
in progress — `WP 8.0A` (Engineering Workspace Architecture) is its
first Work Package. This document is a prepared skeleton — mirroring
the shape every prior release's own `ReleaseNotes.md` took once its
release actually shipped — and will be populated once `v0.8.0`
substantially delivers, not written in advance as a prediction.

## What This Document Will Be

A user/operator-facing summary of what changed in `v0.8.0`: what
capability shipped, what changed for an existing consumer, what remains
accepted technical debt, and the release's own certification outcome —
in the same register as `docs/releases/v0.7.0/ReleaseNotes.md`.

## Carried Forward From `v0.7.0`

`v0.7.0` ("Engineering Foundation") was recommended **APPROVED**
(`docs/releases/v0.7.0/WP7.4.0 Product Approval Report.md`), tagged, and
merged to `main`. 25 disclosed Technical Debt items and 17 trade-offs
remain open at the start of this release cycle — see `docs/governance/
Quality/Technical Debt Register.md`. `FCR-0005` (Governance Register
Health-Check Tooling) remains unbuilt, having recurred five times across
three releases. Two disclosed Future Capability candidates
(`FCR-0037`, `FCR-0038`) remain unscheduled.

## Related Documents (Prepared in Advance)

`docs/releases/v0.8.0/WorkPackages.md`; `docs/releases/v0.8.0/
Retrospective.md`; `docs/releases/v0.7.0/ReleaseNotes.md` (the
immediately preceding release, for comparison once this one ships).
