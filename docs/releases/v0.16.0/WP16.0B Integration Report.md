# WP 16.0B — Integrate Off-`main` Work

**Type.** Governance/Integration. One merge, two file moves, one folder
deletion, four register/status edits. No production code change of its
own; the test-infrastructure change it carries is `WP 15.2A`'s.
**Branch.** `feature/v0.16.0-wp16.0b-integration`, cut from
`feature/v0.16.0` at `47efc37`; merged into `feature/v0.16.0` as
`a4f891b`.
**Commits.** `1e44daf` (non-fast-forward merge of
`origin/feature/wp15.2a-td120-persistence-root-cleanup`, `8b6cc1e`),
`4198289` (the fold and the register notes).
**Applies.** `D-026` (fold the former `v0.15.1` folder into `v0.16.0`)
and `D-022` (Companion deferred to v1.1) — **both Proposed** (`WP 16.0A`),
ratification pending PR #6. Every edit below says so; none claims
approval. **Closes** `TD-120` (via `WP 15.2A`). **Cites** `D-021`
(Proposed) as the v1.0 scope this release serves.

---

## What was done

1. **`WP 15.2A` merged.** `feature/wp15.2a-td120-persistence-root-cleanup`
   (one commit, CI green on its own push, run `33895008891`) merged
   non-fast-forward. `Tempest.Desktop.Tests` 412/412 in Release on the
   merged tree, locally.
2. **The former `v0.15.1` folder folded** (`D-026`). `WP 15.2A`'s
   Implementation Report moved to `docs/releases/v0.16.0/` with a
   "Location" note; that folder's `WorkPackages.md` — which itself stated
   "Not a release" — deleted. No `v0.15.1` tag or Release Register row
   existed or will.
3. **Every reference repointed** so the health check's literal path checks
   stay green: `Documentation Register.md` (its `v0.15.1` row became the
   `docs/releases/v0.16.0/` row; one backticked historical path that
   Check 4 reads literally rewritten as prose), `PROJECT_STATUS.md`, and
   the `WP 15.2A` retrospective's Related Documents.
4. **Companion disposition** (`D-022`). `TD-82` moved to "Deferred to v1.1
   per `D-022` (Proposed)" — branch `claude/tempestos-companion-mobile-ubznt3`
   retained, not merged, not deleted; its `TD-57`/`TD-58` numbering
   collision is reconciled at the v1.1 merge. `Future Capability
   Register.md` records `FCR-0092` as a Companion-scoped number outside
   its own `FCR-0001`–`FCR-0088` range; no entry added.
5. **`WorkPackages.md`**: the `WP 15.2A` row carried in as Complete —
   merged.

## Disclosed for later Work Packages

- **The Core test suite leaks temp directories** (the question `WP 15.2A`
  left open): 65 `tempest-project-*` roots per full run, from four
  fixture factories with no disposal. Handed to `WP 16.4A`.
- **`SampleSeparationTests` fails in any checkout that carries extra
  git worktrees** (it enumerates every `.csproj` under the repository
  root, including `.claude/worktrees/**`); passes in CI. Handed to
  `WP 16.4A` as an environment-dependency defect.
- This report was written after the merge, at the `WP 16.2B` closure,
  because `WP 16.2A`'s status rewrite correctly found no standalone
  `WP 16.0B` report at its base — the work itself is in the two commits
  above and their messages.

## Validation

- Governance Health Check: 7 passed, 1 pre-existing informational warn,
  0 failed, after the repoint.
- `Tempest.Desktop.Tests` 412/412 (Release, local). Core suite unchanged
  by this Work Package.
- CI: the push of `4198289` (run `33926053093`) failed to obtain a runner
  within 8 seconds, as did every run on this account from 22:34 UTC —
  see `WP 16.9.0`'s CI disclosure; the branch's content was validated
  locally as above.

## Related Documents

`docs/releases/v0.16.0/v0.16.0 Release Plan.md` (`WP 16.0B`);
`docs/releases/v0.16.0/WP16.0A v0.16.0 Scope Decision.md` (`D-022`,
`D-026`); `docs/releases/v0.16.0/WP15.2A Desktop Test Suite Persistence
Root Cleanup — Implementation Report.md`; `docs/governance/Quality/
Technical Debt Register.md` (`TD-82`, `TD-120`); `docs/governance/Future
Capability Register.md`.
