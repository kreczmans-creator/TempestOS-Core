# v0.16.0 — Consolidated Review Board Disposition

**Scope.** Every finding raised by the **six** review boards conducted on
this release, and by the independent verification that followed the sixth
(§9), with its severity, evidence, disposition, remediation and
verification. This is the consolidated record `WP 16.9.0`'s Engineering
Readiness Review draws on; the ERR states verdicts, this states findings.
*(Corrected `WP 16.4B-R6` round 2, 2026-09-06: this read "the two review
boards", which stopped being true at board 3 and had not been updated
through three further boards.)*

**Two boards, deliberately** — *and then four more.* Board 1 ran nine
perspectives against the integrated tree. Board 2 ran six **fresh**
perspectives against a pinned, immutable checkout, mandated to assume
board 1 had missed something and that the remediation for board 1's
findings might itself have introduced defects. No board 2 reviewer had
written the code it examined. Boards 3, 4, 5 and 6 followed on the same
mandate against successive remediated candidates (§5, §6, §7, §8).
**Boards 2, 3, 4, 5 and 6 each found a defect inside the previous board's
remediation** — see the tables in §7 and §8. This document is therefore
written so that a superseded disposition is struck through and annotated
where a later board falsified it, never deleted.
*(Counts updated `WP 16.4B-R6` round 2, 2026-09-06: this paragraph read
"three more" and stopped at board 5.)*

**Scope note, `WP 16.4B-R6` round 2 (2026-09-06, updated after
integration).** §7 records board 5, §8 records board 6, and **§9 records
Agent C's independent adversarial verification of board 6's remediation at
`261ba34`** — one verification, not a board. Board 5's remediation is what
board 6 failed; board 6's remediation is what Agent C partly falsified
(`C-R1`, plus four open defects). **Updated 2026-09-06 after the round
completed:** the seventh board did then review this round and returned
PASS with six AMBERs and no RED; those six are closed at `888ef63`, and a
five-run determinism matrix has been obtained against that commit and is
recorded in the Engineering Release Report §4.4. **None of that is a
release verdict**, which
remains NOT READY. Two dispositions moved on
proof this round and are marked as such (`TD-140`, for the
supersession-refusal route only; the register rows re-derived against the
integrated tree). Everything else in §7, §8 and §9 stands unclosed.
*(This note read "No board 6 has sat, and no record for one exists here."
Board 6 has since sat, at `3dd74d8`, and returned FAIL on four findings —
§8.)*

**Candidate, updated `WP 16.4B-R7` governance closure, 2026-09-07.** The
candidate is **`d6af7ec7bd2e2d182eba5ef77bbe703a8a38134c`** on
`feature/v0.16.0-wp16.4b-r6` — **not merged** (`main` is `58c4cba`;
`git merge-base --is-ancestor d6af7ec 58c4cba` fails), not tagged, not
published, no Product Approval verdict. *(This sentence read "The
candidate is `888ef63`, not `261ba34`", and stopped being true the moment
`WP 16.4B-R7` landed: `888ef63..d6af7ec` is eight commits changing three
files under `src/` and five under `tests/`. `888ef63` is retained above as
the commit the seventh board's six AMBERs were closed at and the fourth
determinism matrix was obtained against, and is **not** relabelled — that
matrix covers `888ef63` and no later tree, so **`d6af7ec` is not
matrix-certified** and ERR §4.4 is superseded. **No branch named
`feature/v0.16.0-wp16.4b-r7` exists**; `WP 16.4B-R7` was developed on the
`-r6` branch. Neither `WP 16.4B-R7` nor any finding raised inside it is
recorded anywhere in `docs/` at `d6af7ec`, and **no board disposition for
`d6af7ec` exists in this document or any other** — §8 and §9 below are
scoped to the commits their own headings name and none of them is this
one. The words "NOT READY on `TD-143`" have been reduced to "NOT READY"
here for one reason only: `TD-143`'s register row and the tree at
`d6af7ec` disagree, and **naming the blocker is the Technical Debt
Register pass's act and the chair's**, not this document's. The verdict
itself is unchanged and is not being softened.)*

*(**Blocker now named again — seam reconciliation, `WP 16.4B-R7`
governance closure, 2026-09-07.** The paragraph above is retained unedited.
The register pass has run and is integrated, so the deliberate omission it
records can be lifted: the verdict at `d6af7ec` is **`NOT READY` on
`TD-147`**. `TD-143` is Resolved for the seven mutators on
`EngineeringObjectBase` and for nothing else; `TD-147` — an object creation
whose initial durable state write fails still registers the object, whose
next successful write makes it real — carries the identical harm on an
eighth path and was opened in the same pass, Release Blocking. **The
verdict is unchanged, the blocker count is unchanged at one, and the
blocker MOVED rather than disappeared.** This is the third identity this
one blocker has had — `TD-140` → `TD-143` → `TD-147` — and on each occasion
the count and the verdict stayed exactly where they were.)*

**Severity vocabulary.** RED — blocks the release. AMBER — must be
resolved before v1.0.0. YELLOW — worthwhile improvement or tracked debt.
GREEN — observation, no action.

---

## 1. RED findings — all four closed and verified

### R1. The attachment sweep could permanently delete a live file
*Board 1, security; independently reproduced by board 1, persistence.*

**Evidence.** `AttachContentAsync` wrote content then object state
(`ADR-0114` decision 4, deliberate). The sweep read states, then content
keys, and deleted any content nothing referenced. A sweep between those
two writes deleted the bytes; the state write then recorded an attachment
whose hash pointed at content that no longer existed. Reading states
*first* made the window wider, not narrower. The persistence reviewer
reproduced the same shape in the **Materials** sweep with a worse
consequence: a fully-registered material's index entry deleted, leaving
the document intact but unfindable, and its duplicate check then blind.

**Aggravating.** `TD-97` was already marked **Resolved** against this
code, and the release plan (§3) had specified "a write-intent marker
before multi-file writes" — the implementing stream substituted a simpler
comparison, and that substitution opened the race.

**Disposition.** Fix required, in scope.
**Remediation.** `WP 16.4B-R2` — write-intent marker plus read reordering
across all three sweeps. **The first attempt was rejected** for sampling
the marker *after* the state read, which narrowed the race rather than
closing it. Correct order is content keys → markers → states; the
*position* of the sample is what makes it airtight.
**Verification.** The corrected fix was proven by reverting to the broken
order and watching the new test fail, then restoring it.

### R2. A lost update let the sweep delete a live file anyway
*Board 2, persistence.*

**Evidence.** `PersistStateAsync` captured live state then issued an
unconditional whole-record overwrite, with nothing serialising the pair.
Two concurrent mutations could land their saves in the opposite order to
their captures; the later-landing, earlier-captured snapshot silently
dropped the other's change. The sweep then deleted the orphaned bytes,
behaving exactly as designed. **Reproduced against the real classes on
trial 323 of 500, no artificial delays.** The `WP 16.4B-R2` marker cannot
reach this — both writes complete and clear their markers correctly. Its
proof assumed a single writer per object, and that assumption is false.

**Disposition.** Fix required, in scope. The lost update predates
v0.16.0, but this release's sweep is what converts it from a dropped
reference into permanent deletion.
**Remediation.** `WP 16.4B-R3` — `AsyncKeyedLock` keyed by object Id,
owned by `EngineeringDomainContext`, held from before `CaptureState()`
through `SaveAsync`. Keyed by Id rather than per instance because
`ReviseAsync` demonstrably constructs a second live instance for one Id;
an instance lock would have closed nothing. An exhaustive re-entrancy
audit found no nested path. The same unprotected shape was found and
fixed in `EngineeringObjectFactory.CreateAsync`.
**Verification.** Deterministic test, verified by stashing the fix and
watching it fail immediately. `TD-135` Resolved. 3,204 tests.

### R3. Four tests guarding that code could not fail
*Board 2, testing — every finding proven by mutation.*

**Evidence.** The Materials and Requirements sweep race tests gated on a
collection *name* rather than call order, so reverting the production fix
moved the watched collection to the first call and the interleaving never
occurred — all nine tests passed against genuinely broken code. The
double-dispose test caught its bug in **0 of 20 runs** (0.45% per trial
over 2,000). The golden corpus's "every field is compared" docstring
omitted `Metadata`, so wiping it on every read passed all nine fixtures.

**Disposition.** Fix required, in scope. A test that cannot fail is worse
than none, because it buys false confidence in code that can destroy data.
**Remediation.** `WP 16.4A-R1` — a shared order-agnostic gate; the
double-dispose race looped 2,000× (arithmetic stated: p=0.0045 ⇒ N≥1021
for 99%); the corpus comparison extended to `Metadata`, `BomLine`,
`History` and `Attachments.ContentType`; gates bounded at 10s and
`--blame-hang-timeout` added to CI.
**Verification.** Every fix mutation-proven in both directions. Reverting
both sweeps failed all three race tests (24/24 restored); the looped
double-dispose test detected a reverted guard 10/10.

### R4. Governance documents claimed completeness they did not have
*Board 2, governance.*

**Evidence.** The DI and Platform Services registers both claimed
complete coverage while omitting `IAttachmentWriteIntentStore` — the
service that fixed R1 — left out by the very commit that added it to the
Interface Register. `WP 16.4A` was marked flatly Complete despite its own
5×CI acceptance never being met. `WP 16.2C` was marked Complete having
left six Work Packages without retrospectives, including `WP 16.4B`.
`PROJECT_STATUS.md`'s headline metrics were stale by material margins.

**Disposition.** Fix required, in scope.
**Remediation.** Both register rows added and totals re-derived from
source; both status rows corrected to state their shortfall; the six
retrospectives written (217 → 223); metrics re-derived. `TD-127` Resolved.
**Verification.** `governance-healthcheck.ps1` 14 passed, 2 warned,
0 failed; register totals re-derived by direct grep, not incremented.

---

## 2. AMBER findings

| # | Finding | Board | Disposition |
|---|---|---|---|
| A1 | Two decisions shipped without a governance-required ADR | 1, architecture | Fixed — `ADR-0121`, `ADR-0122` |
| A2 | A third (the migration collision guard) closed with an explicit "No ADR" §5 did not permit | 2, architecture | Fixed — `ADR-0123` |
| A3 | Three new Platform Services absent from Register and Map | 1, architecture | Fixed; also closed `TD-126` |
| A4 | `release.yml` never ran the governance check — a tag could publish drift the merge gate rejects | 1, build/CI | Fixed — `WP 16.1A-R1` |
| A5 | `new-release.ps1` warned on un-green CI and continued; a run completing `cancelled` would have passed a "block on failure" check | 1, build/CI | Fixed — requires literal `success` |
| A6 | `linux-launch-smoke` treated "still running" as success; a post-init deadlock passed identically | 1, build/CI | Fixed — requires a `Host -> Running.` marker, verified against a real launch |
| A7 | 403 body handed an unauthenticated caller the required permission key | 1, security | Fixed — generic body, detail to the log |
| A8 | `PHYSICAL_REVIEW.md` self-contradicted on Linux; later, asserted Windows launch with no citation while holding Linux to a spike report | 1 and 2, UI/platform | Fixed both times; the asymmetry is now stated outright |
| A9 | `PersistenceStore` writes are crash-safe but not power-loss durable (no `fsync`) | 1, persistence | **Recorded, not fixed** — changing the most load-bearing write path in a hygiene release is the wrong trade |
| A10 | The focus-ring test cannot see any `Flat`-treatment state | 2, UI | Recorded — `TD-131` |
| A11 | Duplicate "Open" automation names in the Object Editor | 2, UI | Recorded — `TD-132` |
| A12 | Docking rearrangement is mouse-only | 2, UI | Recorded — `TD-133` |
| A13 | Review target was a live mutable tree | 1, build/CI | Fixed — board 2 ran against a pinned tag |

---

## 3. One finding rejected, with reasoning

**`SettingsDocument` "ahead of target" discard** *(board 2, security —
graded YELLOW by its reporter).*

The reviewer proposed a symmetric "ahead" discard by analogy with
`EngineeringObjectStateStore`. **The analogy does not hold.** That store
has a fixed, platform-wide `CurrentSchemaVersion`, so "ahead" genuinely
means "written by a newer build". This seam derives its target from *the
calling consumer's own supplied chain*, which is not a build version at
all — a document past that target is not from the future, it merely sits
beyond what this caller claims to transform.

The change was written anyway, and it broke
`ADocumentAlreadyPastEveryRegisteredMigration_IsLeftAlone`, a pre-existing
test encoding the opposite intent. **Reverted**, the reasoning put in the
code so the next reader does not re-propose it, and the real residual risk
the reviewer correctly identified recorded as `TD-134` rather than
approximated with a target that means something else.

Recorded here because a review board that only ever agrees with its
reviewers is not a review board.

---

## 4. What the boards confirmed sound

Stated because a disposition listing only defects misrepresents the tree.

- The REST enable gate is genuinely fail-closed and `RestApiHostedService`
  is the only socket-binding path in the tree — verified by exhaustive
  grep, and `D-024`'s claim holds.
- Nothing reachable today can invoke a data-deleting sweep.
- `Tmds.DBus.Protocol` 0.94.2 → 0.21.3 is a real security improvement
  despite the lower number — two independently patched release lines,
  verified against GHSA-xrw6-gwf8-vvr9's actual affected ranges.
- No committed secrets, in the working tree **or** in 321 commits of
  history; no `pull_request_target`; no fork-PR secret exposure.
- Layering intact: `Tempest.Core` has zero project references; ASP.NET
  Core is confined to one type per `ADR-0049`.
- The migration chain's loop is provably bounded and cannot cycle.
- All six modal dialogs genuinely trap and restore focus, proved by
  walking every Tab stop.
- **No document anywhere claims, implies or relies on Product Owner
  approval that has not been given.** Checked exhaustively across every
  citation of `D-021`–`D-026`. The Decision Register genuinely stops at
  `D-020`.

## 5. Board 3 — the final release review (2026-09-05, at `4e91ea8`)

Seven fresh reviewers, run against the release PR's own tree after gate 1
ratified `D-021`–`D-026`. None had written the code it examined. Their
mandate was the same as board 2's: assume the earlier boards missed
something.

They did.

### 5.1 RED — one, closed and mutation-proven

**`R5`. `ReviseAsync` was the one durable-write path `WP 16.4B-R3` never
routed through its own lock, and it lost data the same way.** Found by the
persistence reviewer, who built an executable reproduction against the
real classes rather than arguing from inspection.

`ReviseAsync` captured this object's state *outside* the write lock, handed
the snapshot to a newly constructed successor, and registered that
successor in place of its predecessor — two independently mutable
instances, one durable record. `WP 16.4B-R3` keyed the lock by `Id`
rather than by instance **because of this exact hazard**, and
`PersistStateAsync`'s own remarks named it. But ordering two writers is not
the same as preventing one from discarding the other: the successor's
snapshot was taken at revision time, so a predecessor write that lands
after it is invisible, and the successor's next mutation overwrites the
whole record.

The sequence, reproduced: attach on the predecessor lands durably; an
ordinary rename on the live successor follows; the attachment is gone from
disk. With real content attached, the predecessor's own persist has
already cleared the write-intent marker, so the reconciliation sweep then
sees content present, unmarked and unreferenced, and **deletes the file's
bytes as a genuine orphan.**

*Disposition:* fixed, `WP 16.4B-R4`. `ReviseAsync` performs its capture,
hand-off and registration inside the per-object write lock and retires the
predecessor there; a later durable write through a retired instance throws
`SupersededEngineeringObjectException`. The lock is the linearisation
point, so a predecessor write arriving *before* the revision is still
carried into the successor — which matters, because a fix that simply
stopped predecessors writing would have silently broken the
rename-then-revise sequence every `Revise*Command` in `Tempest.App`
performs, and two of the five new tests exist to catch exactly that
over-correction. `TD-136`.

*Verification:* mutation-proven in both directions. With the fix reverted,
**3 of 5 fail**, one reporting the on-disk attachment collection as `[]`
after the write was accepted without error. With it restored, **5 of 5
pass**.

*What this finding says about the process:* this is the second time a
board has found a data-loss defect inside the previous board's own
remediation, and the second time the remediation's own doc comment
correctly identified the hazard it then under-solved. That pattern is the
argument for running the board again rather than trusting the fix.

### 5.2 AMBER — two, both fixed

| # | Finding | Board | Disposition |
|---|---|---|---|
| A14 | `.github/workflows/ci.yml` claimed Linux becomes "CI-verified in effect once this job exists" — contradicting `D-025`'s ratified "do not claim Linux is CI-verified" | 3, platform | Fixed, `WP 16.5B-R1`. Written by `WP 16.5B` before ratification; every document was hedged afterwards and this comment was missed |
| A15 | The Object Editor's attachment "Open" button set no accessible name at all, relying on `ContentControlAutomationPeer`'s `Content?.ToString()` fallback — undisclosed, and not covered by `TD-132`, whose text is scoped to relationship rows | 3, UI | Fixed, `WP 16.5A-R2`. Named per file (`"Open {FileName}"`) rather than per verb, which also avoids reproducing `TD-132`'s duplicate-name problem here |

### 5.3 YELLOW — documentation currency, all fixed

Ratifying `D-021`–`D-026` made a set of pre-ratification comments false,
and nothing swept for them. Twelve sites across `src/`, `tests/`,
`scripts/`, `.github/`, `PHYSICAL_REVIEW.md` and `WorkPackages.md` still
described ratified decisions as "Proposed — awaiting Product Owner
approval". All corrected. Two register summaries had also drifted from
their own authoritative rows — the ADR Register's narrative cell stopped
at `ADR-0120` while its table correctly held 123, and the Technical Debt
Register said "`TD-01`–`TD-128` contiguous" in the same sentence as "135
tracked". Both corrected by direct recount, and both noted rather than
quietly amended.

### 5.4 What board 3 confirmed sound

Recorded because a disposition listing only defects misrepresents the
tree. Independently re-derived, not taken from prior reports: `Tempest.Core`
has zero project references; `src/Plugins/` is empty, so `D-023` holds in
code; the REST enable key is evaluated before any `WebApplication` object
exists and the bind address is a hardcoded `127.0.0.1` literal, so `D-024`
holds in code; **no `appsettings*.json` ships at all**, so the listener is
off by absence as well as by default; `TD-62` is genuinely fixed, with
401/403/200 tests, not merely claimed; `AT-10`'s premise was re-derived
from scratch and is guarded by a test; `AsyncKeyedLock` has no lock-order
inversion, no re-entrancy and a sound double-dispose guard; the sweep's
content→markers→states read order is correct and its proof holds; all 16
new production files were individually judged for smuggled capability and
none is reachable from any command, route or UI; 123 ADRs match 123 rows;
26 decisions with no invented `D-027+`; 135 TD rows summing exactly to
their stated breakdown, with four "Resolved" rows spot-checked against
source; and **no live document anywhere claims v0.16.0 is approved,
certified, tagged or published.**

### 5.5 Not fixed, and why

`PersistenceStore` still has no `fsync` — process-crash-safe, not
power-loss-safe. The persistence reviewer confirmed the file is
**byte-for-byte unchanged by this release** (`git diff` empty), so it is
pre-existing rather than introduced, and changing the most load-bearing
write path in a hygiene release remains the wrong trade. `TD-129`,
`TD-130`, `TD-131`, `TD-132`, `TD-133`, `TD-134` all verified present in
code exactly as their register rows describe, and left as recorded debt.

## 6. Board 4 — the fresh post-remediation board (2026-09-06, at `9bba720`)

Four reviewers, briefed with the fact that three consecutive boards had
each found the defect *inside the previous board's remediation*, and told
to look for the fourth rather than confirm the third. They found it, and
they found that three of this release's own evidence documents were wrong.

### 6.1 The fourth defect — `WP 16.4B-R5`

**`A16` (AMBER, blocking). `AttachContentAsync` stranded its write-intent
marker when the state write was refused.** `WP 16.4B-R4` taught
`PersistStateAsync` to throw when a concurrent `ReviseAsync` retires the
instance. That throw landed inside a four-step durable sequence — mark,
write content, write state, clear marker — that had **no exception
handling**, because when it was written only a process crash could
interrupt it. Its own comment reasoned from exactly that premise, and the
remediation silently falsified it.

Reproduced against the real classes: marker set, bytes durably written,
state write refused, `ClearAsync` never reached. The marker stays set for
ever; the sweep, correctly refusing to collect marked content, can never
reclaim the bytes. **A permanent leak reachable without any crash.**

*Disposition:* fixed. The state write is now compensated — in-memory
attachment removed, content deleted, marker cleared, **in that order**, so
that a failure inside the compensation leaves a bounded leak rather than
content the sweep would delete while something still believed it existed.
A failure inside the compensation is suppressed so the original exception
still reaches the caller. `TD-139`. Mutation-proven: **3 of the 5 new
tests fail against the reverted code.**

**The first version of this fix was wrong, and the existing suite caught
it.** It caught *every* exception, not just the supersession one, and
compensated identically — which meant that when a failure occurred
**after** the state write had already landed durably, it deleted the bytes
of an attachment that durable state still referenced. That is not cleanup;
it is data loss, introduced by a fix for a leak.
`SweepAsync_AStaleMarker_LeavesContentUncollectedRatherThanErroring`
failed immediately: it simulates precisely that case, asserts the content
survives, and found the collection empty.

The correction is a one-word narrowing with the whole safety argument
resting on it: the compensation catches
`SupersededEngineeringObjectException` **and nothing else**, because that
guard throws strictly *before* `store.SaveAsync`, so on that path alone —
it was argued — nothing was written and deletion is a true rollback. Every
other exception has ambiguous save state, and for those the original
conservative behaviour stands — stale marker, content left uncollected, no
deletion.

> **THAT SAFETY ARGUMENT IS FALSE OF THE SHIPPED CODE. Recorded
> `WP 16.4B-R6`, 2026-09-06, from board 5's findings (`F1`/`P2-1`,
> confirmed by the chair and proved executably by two reviewers
> independently).** The guard does throw before `store.SaveAsync` *on
> this call*. What does not follow is that nothing was written **at
> all**: `AttachContentAsync` adds to `_attachments`
> (`EngineeringObjectBase.cs:538`) **before** taking the per-object write
> lock that `PersistStateAsync` (`:290`) acquires, and a concurrent
> `ReviseAsync` copies `_attachments` into the successor under that same
> lock (`:431`–`:433`). So the successor's durable state can already
> reference the attachment at the moment the predecessor's write is
> refused — and the compensation then deletes the bytes it references.
> **A durable data-loss regression, introduced by the fix for a bounded
> leak.** The window is real, not theoretical.
>
> The claim above is left standing, struck through by this note rather
> than edited away, because it is the record of the reasoning that
> shipped the regression, and that reasoning is the finding. **Nothing
> in this note asserts a fix.** `WP 16.4B-R6` is remediating it in
> `src/` and `tests/` concurrently with this document; the disposition
> of that work, and of `TD-136`/`TD-139`, is decided when it is proven,
> not when the code changes.

> **What `WP 16.4B-R6` actually did, recorded `WP 16.4B-R6` round 2,
> 2026-09-06, read directly from the tree — first at `3dd74d8` and then
> re-read at `261ba34`, the integrated round-2 candidate, because the
> merge moved these line numbers by roughly 120 lines. The mechanism is
> unchanged between the two commits; the numbers below are `261ba34`'s.
> Re-reading rather than relabelling is the point: this note records what
> the code *is*, and Agent C failed the previous revision of these
> documents (`C-R1`) for carrying a superseded SHA on exactly this kind
> of claim.** The
> compensation described in the *Disposition* paragraph above — remove
> the in-memory attachment, delete the content, then clear the marker —
> **no longer exists**. `grep -c "catch (SupersededEngineeringObjectException)"`
> on `EngineeringObjectBase.cs` returns **0**. `WP 16.4B-R6` removed the
> window instead of compensating for it: `AttachContentAsync` acquires
> the per-object write lock *before its first durable step*
> (`EngineeringObjectBase.cs:853`–`:855` at `261ba34`) and holds it
> across all four, and `ThrowIfSuperseded()` runs inside that acquisition
> and before the marker is set (`:862`, marker at `:868`), so a refused
> attach — and a cancelled wait for the lock, which is board 5's `P2-2`
> — throws while nothing durable exists and there is nothing to roll
> back. The state write is `:926` and the success-path marker clear
> `:940`. One `catch` remains (`:878`); it surrounds the content write
> only and **deletes nothing**, clearing a marker for a `Guid` that has
> never left the method. Agent C's
> `AttachContentAsync_WritesItsContent_WhileHoldingTheObjectWriteLock`
> now pins the hold as a fact rather than a reading. The
> paragraph above is therefore doubly superseded — its safety argument
> was false, and the mechanism it describes has since been deleted — and
> both facts are recorded here rather than by editing it. `TD-139`'s
> register row is re-derived to match, twice. **This note still asserts no
> disposition:** it says what the code is, at one named commit, and
> nothing about whether the round-2 remediation is sound as a whole —
> Agent D has not reviewed this round, and the determinism matrix has not
> run.
>
> *(**Both halves of that last sentence have since been overtaken —
> `WP 16.4B-R7` governance closure, 2026-09-07; the sentence is retained as
> what was true when it was written.** A seventh independent board HAS since
> reviewed the round-2 remediation, returning **PASS with five conditions
> and no RED**, closed at `888ef63`; and the determinism matrix HAS since
> run, **5/5 on `888ef63`**, runs 253–257, ERR §4.4. **Neither reaches
> `d6af7ec`**: that matrix is superseded, because `888ef63..d6af7ec` is 8
> commits touching 3 files under `src/` and 5 under `tests/`, so `d6af7ec`
> is **not matrix-certified** and a **fifth** matrix is outstanding; and
> **no board disposition for `d6af7ec` exists in this repository.** This
> note still asserts no disposition.)*

This was the fourth time in this release that a remediation introduced a
defect, and the first time an *existing* test caught one before any board
had to. That is what the suite is for. **It was not the last: board 5
found the fifth, inside this very fix, and the existing suite did not
catch that one — see §7.**

**`A17` (YELLOW, pre-existing). `CaptureState` read `_attachments` under
the wrong monitor.** Writers used `lock (_attachments)`; the projection ran
under `_structuralLock`. Not synchronised at all — a torn read of the
backing array mid-resize, reproduced as a `NullReferenceException` from
inside the projection, twice in three hundred attempts, with no revision
involved. `git log -p` traces it to `TD-85`'s original `CaptureState`, so
it predates the whole `WP 16.4B` chain — but it also made `WP 16.4B-R4`'s
claim of "an atomic capture" untrue in general, which is why it was closed
here rather than deferred. `TD-138`.

**The first version of that test could not fail, and the record says so.**
It attached four hundred files to one object and passed three times out of
three against the defect. A `List<T>` only tears while *resizing*, and four
hundred sequential adds resize roughly nine times — so the vulnerable
window was entered about nine times in the whole test. Raising the
attachment count would have made it worse, not better. Rebuilt around
resize density (many short trials, fresh object each time) it detected
**3 times in 10**; the per-trial rate was then solved from that
measurement — `1 − 0.70^(1/120) = 0.00297`, giving 1,550 trials for 99%
confidence — and the loop set to 2,000. Re-measured: **10 detections out
of 10.** The same arithmetic `WP 16.4A-R1` applied to the double-dispose
test.

> **The detection rate is hardware-dependent, and the measurements
> disagree. All of them are recorded; none is being averaged away.**
> *(Added `WP 16.4B-R6` round 2, 2026-09-06, from board 5 `F6` and the
> sixth board's `D-A2`. The derivation above is left exactly as it is.)*
>
> | Measured by | Hardware / occasion | Against the reverted code | Against the fixed code |
> |---|---|---|---|
> | `WP 16.4A-R1`-era rebuild (the derivation above) | the machine of the day | **3 in 10** at 120 trials; then **10 of 10** at 2,000 | — |
> | Board 5 `F6` | 4-core runner | **1, 4 and 1** detections in three 2,000-trial runs (3 runs of 3 detected it); **34** detections at 20,000 trials → **0.17 %** per trial → ≈ **96.7 %** at 2,000, not 99.74 % | 2,000 trials: **0** detections |
> | `WP 16.4B-R6` | its own container | **5 in 5**, stated flatly, with no hardware caveat | **10 passes in 10** |
> | Sixth independent board (`D-A2`) | a different container | **8 in 9** — one clean pass against defective code | — |
>
> **What is stable across every one of them** is the qualitative result:
> the test detects the reverted code, and never fires against the fixed
> code. **What is not stable is the per-trial rate**, including the
> `0.00297` the trial count was derived from. Board 5 established that
> the `TD-138` evidence is *sound* and that its measured detection rate
> *differs by hardware*; that nuance is the finding, and replacing one
> figure with another would destroy it. The 2,000-trial count stands on
> the original derivation. Raising it is the obvious remedy if a suite run
> ever goes green against the reverted code; it is not being done on the
> strength of four disagreeing measurements, which would only substitute
> one unreproducible constant for another.

### 6.2 Three RED evidence findings — all in this release's own records

| # | Finding | Disposition |
|---|---|---|
| `R6` | `Release Register.md` stated the release was "Not merged". It **was** merged — `main` is the merge commit. The row was written before the merge and never updated, so the canonical release-status register asserted the opposite of the truth | ~~Fixed. The row now records both merges, and says plainly what is still *not* done: not tagged, not published, no verdict~~ **PARTIAL — falsified by board 5, corrected `WP 16.4B-R6`.** The *row* was fixed and nothing else was. `git show 58c4cba -- "…/Release Register.md"` is a one-line diff. Five further documents went on saying not-merged / on-its-own-branch: `Release Notes.md:3–4` (`P4-04`), `WorkPackages.md:19` (`P4-05`), **the same register's own Total line** (`P4-07`, wrapped across a line break), `PROJECT_STATUS.md:83` (`P4-09`), `Documentation Register.md:52` (`P4-15`). All six are now corrected |
| `R7` | The real determinism evidence existed in **no document**. `grep -rln 'f593e5c\|fce2166' --include=*.md .` returned **zero files**. Every document still cited the superseded `d7d3f3b` matrix, and the ERR promised run IDs "appended in §4.2" — **there was no §4.2** | ~~Fixed. §4.2 written, recording the genuine failure (run 214) *before* the five passes, because the failure is the reason to trust the passes~~ **NOT FIXED — the fix reproduced the defect. Falsified by board 5, corrected `WP 16.4B-R6`.** §4.2 was written and closed with *"Its evidence is §4.3."* — **there was no §4.3** (`P4-03`): the identical defect, one section along, created by the fix for it, and propagated into `PROJECT_STATUS.md`'s "§4.1–§4.3" (`P4-10`). The second half was equally untrue: at `58c4cba` **no document mentioned the RC4 matrix, `58c4cba`, `rc4`, or runs 231–235 at all** (`P4-17`) — the same "the real evidence is in no document" shape, one commit later — while `WorkPackages.md`, `Academy Register.md`, `Engineering Status Report.md` and `PROJECT_STATUS.md:131` still cited the superseded `d7d3f3b` matrix. §4.3 is now written and all four documents corrected |
| `R8` | `Exception Register`'s headline read **"Total: 89"** in the same sentence as *"returns exactly 90 matches, matching the 90 rows"*; Coverage Status said "89 of 89"; `PROJECT_STATUS` propagated 89. Actual: 90 rows, 90 classes | ~~Fixed by direct recount. The health check never caught it because it compares rows-to-source (90 = 90, passes) and does not parse the free-text headline~~ **PARTIAL — falsified by board 5, corrected `WP 16.4B-R6`.** Two of the register's *three* statements of the number were fixed. The **Distribution by Root Category** table still read Engineering Domain 9 and totalled *"= 89, matching the Entries table above"* against 10 rows and 90 (`P4-01`) — the arithmetic internally correct, correctly summing a table short by one row. And the sibling register was worse: `WP 16.4B-R5` rewrote the **Namespace Register**'s Coverage Status *announcing it as a re-derivation* and produced 46 namespaces / 724 files where source says **47 / 728** and the same file's own Total line already said 47 / 728 (`P4-02`) |

All three were mine, and all three are the same defect class this release
has now produced four times: **a free-text summary drifting from the rows
it claims to summarise.** `R8` is the sharpest instance — the register that
exists to be authoritative contradicted itself *within a single sentence*.

> **Correction to the paragraph above, `WP 16.4B-R6` (2026-09-06).**
> "Fixed" was asserted, not demonstrated, in all three rows, and board 5
> falsified all three — the strike-throughs above are that board's
> findings, not a re-grading. The count is therefore not four but
> **six**: `R8`'s class recurred twice more (`P4-01`, `P4-02`), `R7`'s
> twice more (`P4-03`, `P4-17`), and `R6`'s five times over. Note where
> two of them came from: `P4-01` and `P4-02` are defects **inside the
> remediation of `R8`**, and `P4-03` is a defect **inside the remediation
> of `R7`** — created by the fix, in the same document, in the same pass.

### 6.3 What board 4 confirmed sound

`P1` re-ran the `WP 16.4B-R4` mutation proof itself rather than accepting
the commit message: 3 of 5 failing reverted, 5 of 5 restored, full suite
3,209/3,209 — matching the claim number for number. `P4` re-verified all
eleven prior RED/AMBER findings **in code**, not from this document, and
found every one genuinely closed; it also confirmed the integration merge
introduced zero divergence (`git diff f593e5c 9bba720` empty), deleted no
test, and that both configurations build 0/0. `P3` re-derived every
governance count from source and confirmed ~~the multi-line
stale-reference sweep is complete~~, `D-025` compliance holds everywhere,
and **no live document claims v0.16.0 is approved, certified, tagged or
published.**

> **The sweep was not complete. Struck through and recorded
> `WP 16.4B-R6`, 2026-09-06.** Board 5 falsified that claim with
> `P4-04`, `P4-05`, `P4-06`, `P4-07`, `P4-14` and `P4-15`. `P4-07` is
> the instructive one: `Release Register.md`'s Total line said v0.16.0
> was "on its own branch, / not `main`" **across a line break**, which a
> line-oriented `grep` cannot see — the exact class of miss the Product
> Owner had already caught personally once in this campaign. The two
> claims that did survive independent re-checking, and still do, are
> `D-025`/Linux compliance and the absence of any
> approved/certified/tagged/published claim; board 5 re-derived both and
> confirmed them, including checking that it was not mis-flagging the
> *correct* negations.

`P1` also asserted `CaptureState` was covered by the object's own locks.
It was not; `P2` was right and `P1` was wrong, resolved against source. A
board whose reviewers never disagree is not being read carefully enough.

---

## 7. Board 5 — the fifth board (2026-09-06, at `58c4cba`)

**Status of this section: findings recorded, remediation IN PROGRESS, and
NOTHING here is verified closed.** `WP 16.4B-R6` is the remediation and it
is under way as this is written. Nothing below may be read as fixed. The
one thing this release has demonstrated repeatedly is that "Fixed" written
next to a finding is worth nothing until someone independent has proved
it — §6.2's three rows said "Fixed" and board 5 falsified all three.

**The pattern, stated plainly, because it is the most important fact
about this release.** Boards 2, 3, 4 and 5 have now **each** found a
defect inside the *previous* board's remediation:

| Board | Found a defect inside | Nature |
|---|---|---|
| 2 | board 1's remediation | Four tests guarding the highest-risk code could not fail (`R3`, §1) |
| 3 | board 2's remediation | A second lost update the `WP 16.4B-R2` marker could not reach (`§5.1`) |
| 4 | board 3's remediation | `AttachContentAsync` stranded its marker — a permanent leak `WP 16.4B-R4` put inside a window whose own comment reasoned it could not exist (`§6.1`) |
| 5 | board 4's remediation | **The fix for that leak deletes durably-referenced attachment bytes.** A data-loss regression introduced by a fix for a bounded leak — plus defects inside the remediation of `R7` and `R8` (see §6.2) |

The failure mode of this release is *the fix introducing the next bug*.
Four consecutive boards is not bad luck.

### 7.1 RED — code (board 5 `F1`–`F3`, chair-confirmed)

Owned by `WP 16.4B-R6`'s implementation stream, **not by this document**,
and recorded here so the findings are visible while that work proceeds:

- **`F1`.** `AttachContentAsync`'s `_attachments.Add`
  (`EngineeringObjectBase.cs:538`) precedes the per-object write lock
  `PersistStateAsync` (`:290`) takes, and `ReviseAsync` (`:431`–`:433`)
  copies `_attachments` into the successor under that same lock. So the
  supersession compensation added at `WP 16.4B-R5` can delete content the
  successor's durable state already references. **Durable data loss, two
  independent executable proofs.** The safety argument that shipped it is
  struck through in §6.1 above.
- **`F2`.** `ReviseAsync:434` assigns `_supersededBy` unconditionally —
  no guard against an existing successor.
- **`F3`.** `RestoreState` (`:159`–`:188`) never reads
  `state.TypeState`, which `CaptureState` (`:89`) does populate, so
  `ReviseAsync` drops all type-specific fields — and falsifies the
  `ReviseAsync` comment's claim that "a field added to it can never again
  be forgotten by one of two copy paths".

AMBER, also chair-confirmed: an `OperationCanceledException` awaiting the
write lock is thrown before `SaveAsync` and is not compensated, so
`TD-139`'s stranded marker is still reachable (`P2-2`); the success path
passes `cancellationToken` where the compensation path correctly passes
`CancellationToken.None` (`P2-3`); `EngineeringDomainContext` holds zero
`ILogger` references, so a bounded leak is unobservable (`P2-4`).

**`TD-136` and `TD-139` are deliberately not re-graded here.** Their
status is decided when `WP 16.4B-R6`'s fixes are *proven*, not when the
code changes.

### 7.2 RED and AMBER — governance evidence (board 5 `P4`, chair `C-01`)

Every one is a document contradicting either source, or itself, or a
sibling document. All are addressed by `WP 16.4B-R6`'s governance stream;
each correction is recorded in place, in the document corrected, with the
superseded statement left visible:

| ID | Where | The contradiction | Derived from | Result |
|---|---|---|---|---|
| `P4-01` | `Exception Register.md` | Distribution table: Engineering Domain 9, total "= 89, matching the Entries table above" | The Entries rows themselves, and `grep -rEn "^public (sealed \|abstract )?class \w+Exception\b" src/Tempest.Core` | **10 / 90** |
| `P4-02` | `Namespace Register.md` | Coverage Status "46 … 724 files", *announced by `WP 16.4B-R5` as a re-derivation*, against the same file's own Total line of 47 / 728 | The four in-scope roots, BOM-stripped, `bin/`/`obj/` pruned | **47 namespaces / 728 `.cs` files** (725 + 3 global) |
| `P4-03`, `P4-10` | ERR `:334`, `PROJECT_STATUS.md` | "Its evidence is §4.3" / "§4.1–§4.3" — no §4.3 existed | The file's own headings | **§4.3 written** |
| `P4-04`, `P4-05`, `P4-07`, `P4-09`, `P4-15` | Release Notes, WorkPackages, Release Register Total line, PROJECT_STATUS, Documentation Register | "not merged" / "on its own branch" | `git merge-base --is-ancestor 58c4cba origin/main` succeeds | **Merged** — and still not tagged, not published, no verdict |
| `P4-06`, `P4-11`, `P4-13`, `P4-14` | WorkPackages `:8`/`:43`, PROJECT_STATUS `:131`, ERR `:6`/`:184`/`:190`/`:196`/`:250`, Academy Register `:307` | The twice-superseded `d7d3f3b` matrix presented as current acceptance evidence; stale test counts | The RC4 matrix (below) and `58c4cba`'s own commit message | **Acceptance points at the RC4 matrix; `Tempest.Core.Tests` 3,214/3,214** |
| `P4-12` | `Release Register.md:49` | `5/5 on f593e5c` offered as this release's evidence with no supersession note; 3,204 tests | as above | **Superseded, noted; 3,214** |
| `P4-16` | ERR `:392` + Release Register `:49` | The merge recorded as performed *and* as an unauthorised outstanding gate, simultaneously, with no deviation disclosed | Governance §7 item 6, and the Product Owner's own instruction of 2026-09-05 | **The §7.6 per-occasion approval record written** — ERR §7 item 2 |
| `P4-17` | whole tree | The RC4 matrix appeared in **no document**; nothing mentioned `58c4cba`, `rc4` or runs 231–235 | GitHub Actions | **Recorded in ERR §4.3**, with the superseded matrices and run 214's failure kept distinct |
| `P4-18` | this document | §6.2 and §6.3 asserted "Fixed" and "sweep complete" | board 5's own re-derivation | **Struck through above, with what each actually left undone** |
| `P4-08` | `PROJECT_STATUS.md:44` | A sentence left truncated mid-clause by `WP 16.4B-R5` | `git show 58c4cba -- PROJECT_STATUS.md`'s `-` side | **Completed** |
| `P4-20` | `PROJECT_STATUS.md` metrics | A "snapshot" table with some rows silently refreshed and others stale | direct re-derivation at `58c4cba` | **All rows re-derived and dated together** |
| `C-01` (chair) | `v0.16.0 Engineering Status Report.md` | `D-021`–`D-026` recorded **Proposed**; "not merged" | `Decision Register.md` — **Ratified 2026-09-05 (Product Owner)** | **Reconciled**, with the frozen snapshot retained unedited and marked superseded |

`P4-19` (`TD-121`/`TD-122` carrying 4 columns where the table declares 5)
is GREEN and deliberately **not** changed: Check 12 takes the last
non-empty cell precisely to survive it, and its own comment documents the
two rows.

**The chair recorded a failure of the chair's own pre-board check**, and
it is repeated here rather than left in the chair's notes: the pre-board
sweep read the Namespace Register's Coverage Status figure and matched it
against itself, never against the register's own Total line. That check
was insufficient, and `P4-02` is what it missed.

### 7.3 The determinism evidence for `58c4cba`

The RC4 matrix — **5/5, runs 231–235, IDs `34023326177`, `34023942958`,
`34024438521`, `34025045650`, `34026440586`, every one
`workflow_dispatch` at `run_attempt: 1` against `58c4cba`**, with
`Build & Test (Debug)`, `Build & Test (Release)`,
`Governance Health Check` and `CI Gate` each `success` at job level — is
recorded in full in `WP16.9.0 Engineering Release Report.md` §4.3. The
advisory `Linux Launch Smoke` job is outside `CI Gate`'s `needs` and is
not part of the gate. Run 230 (`34023322455`) was a `push` run,
`cancelled` before any test ran, and is excluded and preserved.

**It covers `58c4cba` and no later tree.** `WP 16.4B-R6` changes `src/`
and `tests/`, so a **fourth** matrix is outstanding for the next
candidate, on exactly the reasoning that retired the three before it.
*(Discharged, and superseded again — `WP 16.4B-R7` governance closure,
2026-09-07. The fourth matrix ran 5/5 on `888ef63`, runs 253–257, ERR
§4.4; it is now itself superseded, because `WP 16.4B-R7` changed `src/`
and `tests/` between `888ef63` and the candidate `d6af7ec`, so a **fifth**
is outstanding. The RC4 evidence in this section stays scoped to
`58c4cba`, which is what it is evidence for, and is not relabelled.)*

### 7.4 What is still true, and is not being softened

`v0.16.0` is **not tagged, not published, and carries no Product Approval
verdict.** Gate 1 (scope ratification) and gate 2 (the merge) are closed;
gates 3 and 4 are not. Linux is **not** CI-verified and is not to be
recorded as "supported" without qualification — ratified `D-025`, and
`ci.yml`'s job graph agrees with the prose. Nothing in this section, and
nothing written by `WP 16.4B-R6`, approves, certifies or grades anything.

---

## 8. Board 6 — the independent board on `WP 16.4B-R6` (2026-09-06, at `3dd74d8`)

**This heading's SHA is history and is correct: board 6 really did sit at
`3dd74d8`.** It is not a reference to the current candidate and must not
be relabelled — see §8.7, which previously classified it wrongly and is
corrected there.

**Status of this section as originally written: findings recorded,
remediation IN PROGRESS, and NOTHING here verified closed.** That was the
same wording as §7, for the same reason, one board later. The round-2
remediation has since been integrated at `261ba34` and independently
verified by Agent C; **what that verification found, and what it did not
reach, is §9.** Each subsection below carries its own disposition; none of
them was updated by the act of merging.

**The pattern held for a fifth consecutive board.** §7's table listed
boards 2 through 5, each finding a defect inside the previous board's
remediation. Board 6 makes it five:

| Board | Found a defect inside | Nature |
|---|---|---|
| 6 | board 5's remediation (`WP 16.4B-R6`) | The remediation **widened** the window on a defect it fixed for only two of seven mutators, and shipped two code comments the new code falsifies — plus two register rows and this release's own readiness verdict left asserting things the tree contradicts |

Board 6 verified board 5's three code REDs (`F1`, `F2`, `F3`) closed,
independently and by mutation, and re-derived every governance number
`WP 16.4B-R6` produced. Its conclusion was nonetheless **FAIL**, on four
findings.

### 8.1 `D-R1` (RED) — the readiness report still recommended acceptance

`WP16.9.0 Engineering Release Report.md` §3 read "**Release Blocking —
none remain**", and §6 derived **ACCEPT WITH OBSERVATIONS** from it — a
day after board 5 raised three chair-confirmed REDs, one a permanent
data-loss regression, against `58c4cba`, the exact commit that report's
own header names. Its two clauses did not agree with each other either
(§3 said three blockers existed; §6 said "both that existed"), and its
"Disclosed, Non-Blocking — 13" omitted `TD-137` although `TD-137`'s own
register row cites that report as a place it is disclosed. `WP 16.4B-R6`
had edited that file in twelve places and touched neither section. This
document said the opposite in §7 the whole time.

*Disposition:* **revised, upward, in place, `WP 16.4B-R6` round 2.** The
recommended verdict is now **`NOT READY`**, derived in ERR §6.2 from one
open Release Blocking finding (`TD-140`). The old text and the old
derivation are retained verbatim in ERR §3.1 and §6.1. The report's own
principle — *a readiness review that quietly revises its own blocker
count is worth nothing* — is why the count was revised openly rather than
the report left standing. **`NOT READY` is a recommendation not to
proceed. It is not a rejection, not a verdict, and not a decision.**

> **Blocker identity, twice superseded since this disposition was written —
> `WP 16.4B-R7` governance closure, 2026-09-07.** The paragraph above is a
> scoped board record and keeps its original wording; this note exists
> because a **present-tense** blocker identity that is now two moves out of
> date is the isolated-read hazard this campaign keeps being bitten by — a
> reader arriving here alone would follow `TD-140` to a row that reads
> **Resolved** and could conclude the recommendation had been discharged.
> The chain, in full: **`TD-140` → `TD-143` → `TD-147`.** `TD-140` is
> Resolved for the supersession-refusal route only; `TD-143` is Resolved for
> the seven mutators on `EngineeringObjectBase` only and closes no harm
> class; **`TD-147` is the one open Release Blocking finding at `d6af7ec`**
> — an object creation whose initial durable state write fails still
> registers the object, and its next successful write makes a creation the
> caller was told had failed into a real one. **On each of the two moves the
> count stayed at one and the verdict stayed `NOT READY`. The blocker moved;
> it has never disappeared, and none of the three moves is progress toward
> acceptance.**

### 8.2 `D-R2` and `D-R3` (RED) — two register rows asserting closures the tree falsifies

`TD-139` described, **in the present tense**, the `WP 16.4B-R5`
compensation that `WP 16.4B-R6` had already deleted — presenting the
release's own data-loss regression as this defect's closure. `TD-136` read
"Resolved, `WP 16.4B-R4`" and cited `WP 16.4B-R4`'s mutation evidence,
although board 5's `F2` had falsified that fix's sufficiency. Board 5's
`F5` required both rows to move; they had not moved, and nothing anywhere
labelled either claim superseded.

*Disposition:* **both rows re-derived directly from the code at
`3dd74d8` and again at `261ba34` after integration**, each now naming `WP 16.4B-R6` as what closed it, what
`WP 16.4B-R4`/`-R5` did and did not achieve, and the evidence that stands
today — with the superseded text kept inside each cell rather than
deleted. §6.1 above carries the same correction for this document's own
`A16` paragraph. Both rows state the commit they were read at and that
they must be re-derived against the integration SHA.

### 8.3 `D-R4` (RED) — an operation reported as *failed* still becomes durable, and it was recorded nowhere

Board 5 raised this as `F4` and graded it AMBER. Board 6 re-graded it
**Release Blocking**, on the invariant rather than the reproduction, and
explicitly recorded that it was disagreeing with board 5's reviewer and
with Agent C, both of whom graded it AMBER knowing the `DeleteAsync`
consequence. **Both gradings are recorded here and this document settles
neither.** The facts under them, **as they stood at `3dd74d8` — every verb
in this list is past tense because the round-2 remediation has since
changed the code, see the second disposition below** — were not in
dispute:

- `TransitionAsync`, `RenameAsync`, `MoveAsync`, `DeleteAsync` and
  `SetBomLineAsync` mutat**ed** their in-memory field **before** calling
  `PersistStateAsync`, which is where the lock and the supersession check
  lived. A concurrent `ReviseAsync` captured the mutation into the
  successor and then made the mutator's own write throw. The caller was
  told the write did not happen. It had.
- **`WP 16.4B-R6` fixed this for two of the seven mutators and widened the
  window for the other five.** `AttachAsync` and `AttachContentAsync` now
  check supersession and mutate inside one hold of the lock. The exposure
  for the remaining five is as long as the current longest lock hold — and
  `WP 16.4B-R6` extended that from capture-plus-save to the whole of
  `AttachContentAsync`'s four durable steps, including an arbitrarily
  large content byte write, and additionally moved the document store's
  own file writes inside the object lock. Both changes are correct for
  what they were made to fix. Both make this defect more reachable than it
  was at `58c4cba`. That is the campaign's signature failure mode
  occurring again, and it is recorded as such.
- A refused `DeleteAsync` leaves the object **durably soft-deleted**, with
  no undelete anywhere in the platform and twenty-one `Tempest.App` sites
  filtering deleted objects out, and skips the `TD-97` byte release. A
  refused `TransitionAsync` writes a `LifecycleTransitionRecord` carrying
  a real actor principal id and timestamp into the **append-only**
  transition history.
- It had **no register row**. The disclosure lived in test XML comments
  and in agent reports that are not part of this repository.

*Disposition when this section was written:* **recorded, not fixed, and
not closed.** `TD-140` carried it, Open, with its evidence
(`RefusedMutationSuccessorLeakageTests` — five deterministic facts, one
per mutator, each asserting the leaked value on disk) and with the
explicit conditions under which it could be moved: the guard and the
mutation inside one hold of the lock for each of the five, **the
characterisation facts inverted rather than removed**, and independent
verification by someone who did not write the fix.

*Disposition now, added after integration at `261ba34`, 2026-09-06:*
**`TD-140` is Resolved for the supersession-refusal route, and the same
harm is still open by another route.** All three conditions were met and
checked, not assumed — `MutateAndPersistAsync`
(`EngineeringObjectBase.cs:462`–`:479`) refuses before mutating, the five
facts were inverted rather than deleted, and Agent C verified the fix
adversarially. **But Agent C also established that the invariant covers
`SupersededEngineeringObjectException` and nothing else**: a mutation whose
durable write *fails* still leaves the fabricated audit entry and the
unrecoverable soft delete, on all seven mutators. That is `TD-143`, Open,
and it — not `TD-140` — is now the open Release Blocking finding the
Engineering Readiness Review derives `NOT READY` from. See §9.

> **Superseded in fact at `d6af7ec`; retained unedited as board 6's record
> at `3dd74d8` and Agent C's at `261ba34`.** *(`WP 16.4B-R7` governance
> closure, 2026-09-07.)* At `d6af7ec` a mutation whose durable write fails
> **no longer** leaves the fabricated audit entry or the unrecoverable soft
> delete on the seven mutators of `EngineeringObjectBase`:
> `WP 16.4B-R7` added `RollBackOnFailureAsync`, which undoes the in-memory
> mutation — on evidence, only when a re-read of the durable record does
> not already show it — at four wiring sites covering all seven, inside the
> existing hold of the object write lock. The three characterisation facts
> in `MutatorRefusalAdversarialTests` §3 were **inverted, not deleted**,
> and each records the inversion in its own remarks. **This note changes no
> verdict and names no blocker.** The verdict remains `NOT READY`;
> `TD-143`'s register row still reads *"Open — reproduced, not fixed"* at
> `d6af7ec`; and reconciling the row with the tree, and settling which
> finding the `NOT READY` derivation now rests on, is the Technical Debt
> Register pass's act and the chair's. See ERR §2 and §3.2.
>
> *(**Settled — seam reconciliation, 2026-09-07.** The two sentences above
> are retained unedited as what was true earlier the same day, before the
> register pass ran. `TD-143`'s row no longer reads *"Open — reproduced,
> not fixed"*; that wording is retained inside the row as its own superseded
> text. The row is **Resolved for the seven mutators on
> `EngineeringObjectBase` and for nothing else**, and states in its first
> sentence that it does **not** close the harm class. The `NOT READY`
> derivation now rests on **`TD-147`**, Open, Release Blocking, opened in
> the same pass. **The verdict is unchanged and the count is unchanged at
> one; the blocker moved and did not disappear.**)*

### 8.4 Board 6's AMBERs, and what was done with each

| ID | Finding | Disposition |
|---|---|---|
| `D-A1` | Two `WP 16.4B-R6` comments claim `ReviseAsync`, `AttachAsync` and `AttachContentAsync` all call `PersistStateHoldingWriteLockAsync`. Only `AttachContentAsync` does; `ReviseAsync` performs no durable state write at all | Owned by the round-2 **implementation** stream (the comments are in `src/`). The governance sweep searched every live `.md` for the same claim, flattened across line breaks, and **found none** — the only governance statement on the subject, in `WP16.4B-R3 Per-Object Write Serialisation.md`, correctly says `ReviseAsync` never calls `PersistStateAsync`. Recorded here so the negative result is on the record |
| `D-A2` | The new race-test measurement (**5 detections in 5 runs**) is stated flatly where board 5's `F6` was raised for exactly that; board 6 measured **8 in 9** | **Both figures recorded, with all four measurements and the hardware caveat**, in §6.1 above and in `TD-138`'s register row. Neither replaces the other. The original `1 − 0.70^(1/120) = 0.00297` derivation and the 2,000-trial count are intact |
| `D-A3` | `WorkPackages.md` — the register `PROJECT_STATUS.md` calls authoritative per Work Package — has rows for `WP 16.4B-R3` and `-R4` and none for `-R1`, `-R2`, `-R5` or `-R6`, while its Status header reads "Every row below is delivered and validated". Confirmed absent at `9bba720`, `58c4cba`, `3dd74d8` and, re-checked this round, at `261ba34` — the table still has rows for `WP 16.4B` , `-R3` and `-R4` only, so `-R1`, `-R2`, `-R5` and `-R6` (both passes) remain unrepresented; **pre-existing** | **Recorded, not fixed** — outside this round's itemised scope, which is `D-R1`–`D-R4`, board 5's `F4`/`F5`/`F6` and SHA currency. Raised here so it is not found for the first time by a seventh board |
| `D-A4` | Agent C's `C-F3`/`C-F5` — a bounded attachment leak on an I/O failure of the state write, and a fully-committed attach reported as failed when the marker clear fails. Real, bounded, disclosed in test remarks only | **Recorded, not fixed.** Same register gap as `D-R4` at lower severity; no row is created for them this round and that is a deliberate limitation of this sweep, stated so it is visible |

### 8.5 A defect recorded by reading, not by running

`EngineeringObjectBase.LinkAsync` writes durably
(`_context.Store.LinkAsync`) with **no supersession guard at all**, no
object write lock, and no call to `PersistStateAsync` — so none of the
protections `WP 16.4B-R3` through `-R6` added reach it, and `MoveAsync`
reaches that path. Enumerated from the code by the round-2 chair while
scoping the seven mutators, and **deliberately held out of that scope so
that it is characterised before it is changed** — the `WP 16.4B-R2`
precedent, where a first fix was rejected for narrowing a hole rather than
closing it, is the reason for that order.

*Disposition:* `TD-141`, **Open, and no longer merely read.** *(Updated
after integration, 2026-09-06.)* When this subsection was written no test
exercised it and the row said so. Agent C has since pinned it executably —
`LinkAsync_OnARetiredInstance_IsNotRefused_AndWritesThePermanentRelationship`
and `AKindsOwnRelationshipApi_ReachesTheUnguardedLinkPath` — and found a
**second unguarded site this section missed**:
`EngineeringRelationshipFactory.CreateAsync`
(`EngineeringObjectFactory.cs:87`–`:98` at `261ba34`, durable write at
`:92`), which structurally cannot take the same guard because it is a
free-standing factory holding only Ids and never consults a live instance.
Verified against the code and added to the row. Still **not fixed**, and
the fix for the second site is a design question rather than three lines.

### 8.6 What is still true, and is not being softened

`v0.16.0` is **not tagged, not published, and carries no Product Approval
verdict.** `main` remains at `58c4cba`. The candidate under review is
**`d6af7ec7bd2e2d182eba5ef77bbe703a8a38134c`** on
`feature/v0.16.0-wp16.4b-r6`, **not merged**. *(Updated 2026-09-06: this
paragraph named `3dd74d8` and said the SHA would change at integration. It
did — the two round-2 streams were integrated at `ae2b7f0` and `8465945`
and extended by `2192e5b` and `261ba34` — and the update was not made at
the time, which is Agent C's `C-R1` and is corrected here and in §8.7.)*
*(Updated again `WP 16.4B-R7` governance closure, 2026-09-07: this
paragraph still read `261ba34` — **two candidates out of date**. It was
not updated when the candidate moved to `888ef63`, even though `0935c64`
updated the ERR header and this document's own scope note in the same
commit; §8.7's row 5b was written for exactly this substitution and was
not executed. That is `C-R1` recurring, one cycle later, in the paragraph
that records `C-R1`. It is corrected here and the omission is recorded in
§8.7 rather than tidied away.)*
**No five-run determinism matrix exists for `d6af7ec`.** *(Corrected
`WP 16.4B-R7` governance closure, 2026-09-07: this read "**No five-run
determinism matrix exists for `261ba34` or for any tree after
`58c4cba`** — the fourth matrix ERR §7 item 6 requires has not run",
which became false at `0935c64`. **The fourth matrix did run** — 5/5 on
`888ef63`, runs 253–257, ERR §4.4 — and is itself now superseded, because
`WP 16.4B-R7` changed `src/` and `tests/` after it. A **fifth** matrix is
outstanding, against `d6af7ec`. This pass did not run it.)*
The only regression evidence for `261ba34` is a local run on one machine
(builds 0/0 both configurations under `-p:TreatWarningsAsErrors=true`,
Core 3,286/3,286, Desktop 474/474, both configurations, governance check
14/2/0), and it is not offered as anything else — and the equivalent at
`d6af7ec`, measured for the `WP 16.4B-R7` governance closure, is a Debug
build 0 warnings / 0 errors under `-p:TreatWarningsAsErrors=true` with
`Tempest.Core.Tests` 3,330/3,330 and `Tempest.Desktop.Tests` 474/474,
which is likewise a local run on one machine and is likewise not offered
as anything else. ~~**Agent D has not
reviewed this round.**~~ **A seventh board did review the `WP 16.4B-R6`
round-2 remediation and returned PASS with six AMBERs and no RED, those
six closed at `888ef63`** — see the scope note at the head of this
document. *(Struck through, not deleted, `WP 16.4B-R7` governance closure,
2026-09-07: the sentence was true when written and false from `0935c64`,
which corrected the same claim in the scope note and left it standing
here. **No board has reviewed `d6af7ec`, and no disposition for `d6af7ec`
exists in this repository.**)* Agent C's verification is recorded in §9; a single
independent verification is not a board. Nothing in this section, and
nothing written by `WP 16.4B-R6` round 2 or by the `WP 16.4B-R7`
governance closure, approves, certifies, or grades
anything.

### 8.7 Where the candidate SHA is written, so updating it is mechanical

*(Written `WP 16.4B-R6` round 2, 2026-09-06, when the candidate was
`3dd74d8`. **EXECUTED — and corrected — 2026-09-06 after integration**,
against `261ba344cb8504e0fc7af2223597f9232df8eb7d`.)*

**This list was written and then not executed when the two round-2
branches were merged.** Agent C failed the candidate on that (`C-R1`):
`grep -rc 3dd74d8 docs/ PROJECT_STATUS.md` returned 40 hits and the
integrated SHA returned none, while `TD-140`'s row still asserted in the
present tense a mutate-then-persist shape the merged fix falsifies. Having
a correct list and not running it is worth no more than not having one.
The list is retained below, with its own error corrected, so that the next
integration has a checked instrument rather than a fresh hunt.

**The one classification error in the original list, corrected here rather
than deleted.** Row 5 read "§8 heading and §8.6". The **§8 heading is
history** — board 6 really did sit at `3dd74d8` — and relabelling it would
have falsified the record in the act of tidying it. Only §8.6 named the
candidate. The two are separated below.

| # | Where | Kind | Done |
|---|---|---|---|
| 1 | ERR header, **Candidate under review** | substitute | ✔ → `261ba34`, with the old SHA and the reason retained |
| 2 | ERR §2, Verification readiness | substitute **and re-measure** | ✔ → `261ba34`, and the figures **re-run on that commit**: Core 3,286/3,286 (was 3,267 at `3dd74d8`), Desktop 474/474, both configurations |
| 3 | ERR §4, scoping line | substitute | ✔ → `261ba34` |
| 4 | ERR §7 item 6 | substitute | ✔ → `261ba34`, with the full chain of candidates retained |
| 5a | **This document, §8 heading** | **HISTORY — do not change** | ✔ left as `3dd74d8`, and annotated to say why |
| 5b | This document, §8.6 | substitute | ✔ → `261ba34` |
| 6 | This document, §6.1 addendum | **RE-READ** | ✔ re-read at `261ba34`; mechanism unchanged, all six line numbers moved (`:734`→`:853`, `:741`→`:862`, `:753`→`:878`, plus `:869`, `:926`, `:940`) |
| 7 | `Technical Debt Register.md`, `TD-136` | **RE-DERIVE** | ✔ re-derived; `ReviseAsync` lock `:646`, guard `:648`, hand-off `:650`–`:661` |
| 8 | `Technical Debt Register.md`, `TD-139` | **RE-DERIVE** | ✔ re-derived; numbers as row 6 |
| 9 | `Technical Debt Register.md`, `TD-140` | **RE-DERIVE** | ✔ re-derived — **and the mechanism itself changed.** The row's present-tense claim was falsified by the merged fix. Rewritten around `MutateAndPersistAsync` (`:462`–`:479`) and moved to Resolved for the supersession route only |
| 10 | `Technical Debt Register.md`, `TD-141` | **RE-DERIVE** | ✔ re-derived; `LinkAsync` `:678`–`:688`, write `:683`; **second site added** |
| 11 | `Technical Debt Register.md`, `Last Reviewed` | **RE-DERIVE** | ✔ rewritten; counts re-derived to 146 / 77 Open |
| 12 | `Release Register.md`, `v0.16.0` row | substitute | ✔ → `261ba34` |
| 13 | `PROJECT_STATUS.md`, Current Development Branch | substitute | ✔ → `261ba34` |
| 14 | `Technical Debt Register.md`, `TD-142` | **RE-DERIVE** *(added; the row post-dated the list)* | ✔ line numbers re-verified unchanged at `261ba34`, enumeration re-derived and proved exhaustive, and its two arithmetic errors corrected |

**Why rows 6 to 11 and 14 could not be a search-and-replace, demonstrated
rather than asserted.** Row 9 is the proof: between `3dd74d8` and
`261ba34` the five mutators stopped mutating before their persist and
started refusing before mutating. A relabelled row would have read
correctly, cited the right commit, and described code that does not exist
— which is `D-R2` exactly, one cycle after `D-R2` was closed. Every one of
these cells was re-read from `src/` at `261ba34`.

**Every other occurrence of a SHA in the `v0.16.0` record is scoped
evidence for the commit it names and must NOT be changed.** Verified
individually this round and left as history: the RC4 matrix and the two
superseded matrices (`58c4cba`, `f593e5c`, `d7d3f3b`, and run 214's
failure on `fce2166`); the merge history (`cc7ef4d`, `9bba720`,
`58c4cba`); the `PROJECT_STATUS.md` and Repository Metrics snapshots at
`58c4cba`; every board heading naming the commit that board sat at
(including §8's `3dd74d8`); and every correction note that names the SHA it
is correcting *from*. Rewriting those would destroy the evidence trail
three boards have already had to rebuild.

~~**For the next integration.** The candidate is `261ba34`. If it moves,
rows 1–5b, 12 and 13 are a substitution; rows 6–11 and 14 must be re-read
from `src/`; and the §2 figures in row 2 must be re-measured, not carried
forward.~~ *(Struck through, not deleted, `WP 16.4B-R7` governance
closure, 2026-09-07: the candidate has moved twice since — see immediately
below. The instruction itself was right; it was not followed.)*

> **THE LIST WAS AGAIN WRITTEN AND AGAIN NOT EXECUTED — TWICE.**
> *(`WP 16.4B-R7` governance closure, 2026-09-07. Recorded here rather
> than quietly fixed, because "having a correct list and not running it is
> worth no more than not having one" is this section's own sentence.)*
>
> **Run 1 — `261ba34` → `888ef63`.** `0935c64` moved rows 1 (ERR header)
> and the scope note at the head of this document, and stopped. **Rows 5b
> (§8.6), 12 (`Release Register.md`) and 13 (`PROJECT_STATUS.md`) were left
> at `261ba34`**, and rows 2, 3, 4 and 6–11/14 were neither substituted nor
> re-derived. From `0935c64` until this pass the release record named the
> candidate as `888ef63` in two documents and `261ba34` in three.
>
> **Run 2 — `888ef63` → `d6af7ec`.** `WP 16.4B-R7` changed three files
> under `src/` and five under `tests/` and touched **no** document under
> `docs/` (`git diff --name-only 0935c64..d6af7ec | grep '^docs/'` is
> empty). At `d6af7ec`, before this pass, `d6af7ec` appeared **0** times in
> `docs/`.
>
> **What this pass did, and what it did not.** Rows 1, 5b and 3/4 are
> substituted to `d6af7ec` here and in the ERR, with the old SHAs and the
> reasons retained. **Rows 2 and the §2 figures are re-measured, not
> carried forward** — Debug build 0/0 under `-p:TreatWarningsAsErrors=true`,
> `Tempest.Core.Tests` 3,330/3,330, `Tempest.Desktop.Tests` 474/474 at
> `d6af7ec`. **Rows 7–11, 12, 13 and 14 are NOT done by this pass**: rows
> 7–11 and 14 are Technical Debt Register cells and row 12 is
> `Release Register.md`, both of which belong to the register pass and must
> be **re-read from `src/` at `d6af7ec`, not relabelled** — row 9 is the
> standing proof of why. `WP 16.4B-R7` changed `EngineeringObjectBase.cs`
> substantially, so **every `EngineeringObjectBase.cs:NNN` line number in
> those rows is stale for `d6af7ec`**, including the `:462`–`:479` citation
> repeated in §8.3 and in the ERR. Row 13 (`PROJECT_STATUS.md`) is done by
> this pass.
>
> **Row 15, new.** `docs/architecture/Engineering Readiness Review
> Architecture.md`'s `NOT READY` precedent cell names the blocking finding
> and belongs on this list. It was not on it, and it was not updated in
> either run.
>
> **For the next integration the candidate is `d6af7ec`.** If it moves,
> the same split applies: substitutions for rows 1–5b, 12, 13 and 15;
> re-reads from `src/` for rows 6–11 and 14; a re-measurement for row 2.

---

## 9. Agent C — independent adversarial verification of the round-2 remediation (2026-09-06, at `261ba34`)

**Status: this is one independent verification, not a board.** Agent D has
not reviewed this round. Nothing here may be read as a board disposition,
and nothing here approves, certifies or grades anything.
*(Scoped, `WP 16.4B-R7` governance closure, 2026-09-07: the second
sentence was true at `261ba34`, which is the commit this section's heading
names, and is retained as that record. **A seventh board did subsequently
review the `WP 16.4B-R6` round-2 remediation and returned PASS with six
AMBERs and no RED, those six closed at `888ef63`** — see the scope note at
the head of this document. **No board has reviewed the candidate under
review, `d6af7ec`.** The first and third sentences are unaffected: §9 is
still one verification and still grades nothing.)*

Agent C was tasked to **falsify** the round-2 remediation rather than
demonstrate it, and wrote its facts into
`tests/Tempest.Core.Tests/EngineeringDomain/MutatorRefusalAdversarialTests.cs`
in two explicitly separated kinds: **guard-rails**, which assert what the
remediation claims and each name the mutant that kills them, and
**characterisations**, which assert what the platform currently does
including where that is wrong, each naming the register row that owns it
and carrying the instruction to **invert, not delete**, when fixed.

### 9.1 What it confirmed

The seven-mutator refusal invariant holds for
`SupersededEngineeringObjectException`: `MutateAndPersistAsync` refuses
before mutating, the durable state write happens under the object write
lock, `MoveAsync`'s link write happens inside the same hold,
`AttachContentAsync`'s content write happens inside the hold, concurrent
mutators and revisions all complete without deadlock, and a retired
instance refuses *before* it adjudicates a lifecycle transition. The five
characterisation facts in `RefusedMutationSuccessorLeakageTests` were
inverted rather than deleted, exactly as `TD-140` required. On that
evidence, and on its own three conditions stated in advance, **`TD-140` is
Resolved for the supersession route.**

### 9.2 `C-R1` (RED) — the register described code that does not exist, again

`TD-140` still asserted, in the present tense, that five mutators "mutate
their in-memory field first and only then call `PersistStateAsync`". The
merged fix falsifies that. Forty references across `docs/` and
`PROJECT_STATUS.md` still named `3dd74d8`; the integrated SHA appeared
nowhere. **This is `D-R2` recurring one cycle later, in rows that had just
been corrected for it** — and the instrument written to prevent it, §8.7,
existed and was not run.

*Disposition:* **closed by executing §8.7**, both halves — the
substitutions and the re-derivations — against `261ba34`, with §8.7 itself
corrected (row 5's misclassification) and extended (row 14). Verification
that it is closed belongs to the next reviewer, not to the pass that did
the work.

### 9.3 `C-F2`–`C-F5` — four open defects, all reproduced, none fixed

Recorded because recording is in scope for a governance pass and fixing is
not. Each now has a register row, Open, citing the fact that pins it.

| ID | Finding | Row |
|---|---|---|
| `C-F2` | The refusal invariant covers `SupersededEngineeringObjectException` **and nothing else**. A mutation whose durable write *fails* keeps its in-memory mutation on all seven mutators — the fabricated append-only audit entry and the unrecoverable soft delete survive by that route — and the next successful write of any kind makes it durable | **`TD-143`, Open.** The open Release Blocking finding the ERR now derives `NOT READY` from: the same demonstrated harm as `TD-140`, by another route |
| `C-F3` | A move whose durable `groupedUnder` link write fails still reparents the instance; the next successful write makes it durable. `MoveAsync` is the one mutator kept inline, so its failure lands between the mutation and the persist | **`TD-144`, Open.** Pre-existing, and *narrower* than before round 2, because the lock hold now stops a concurrent revision adopting the orphan parent |
| `C-F4` | Two concurrent moves can each pass `GuardAgainstCircularParentAsync` — which runs outside the write lock by deliberate, sound design — and form a parent cycle. Five `Tempest.App` ancestry walks then never terminate | **`TD-145`, Open.** The design choice is not disputed; closing it needs a lock over the hierarchy, which is outside this Work Package |
| `C-F5` | `DeleteAsync`'s live-children check also runs outside the lock, so a delete can commit while a concurrent move gives it a live child — leaving a live child of a parent every read model filters out | **`TD-146`, Open.** A hole in the check, not the absence of one: the same pair in the other order is refused |

> **`C-F2` and `C-F3` are superseded in fact at `d6af7ec`; the table is
> retained unedited as Agent C's record at `261ba34`.** *(`WP 16.4B-R7`
> governance closure, 2026-09-07, derived from the tree rather than from any
> report.)*
>
> - **`C-F2` / `TD-143`.** The failing-durable-write route on the seven
>   mutators of `EngineeringObjectBase` is remediated at `d6af7ec` by
>   `RollBackOnFailureAsync`, which undoes the in-memory mutation on
>   evidence. The facts that pinned it were **inverted, not deleted**.
> - **`C-F3` / `TD-144` is only half true now.** A move whose durable
>   `groupedUnder` link write *fails* no longer reparents the instance —
>   `AMoveWhoseDurableLinkWriteFails_LeavesTheParentWhereItWas_AndTheNextWriteCarriesNothing`
>   asserts so, and the fact's own remarks say `TD-144` is closed here "as a
>   consequence of `TD-143`'s fix". What remains is the *durable* half: a
>   move whose link write **succeeded** and whose state write then failed
>   restores `ParentId` but leaves the `groupedUnder` edge on disk, pinned
>   by `AMoveWhoseStateWriteFailsAfterItsLinkWrite_RestoresTheParent_ButLeavesTheGroupedUnderLink`.
> - **`C-F4` / `TD-145` and `C-F5` / `TD-146` were not re-derived by this
>   pass** and are stated as unverified at `d6af7ec` rather than carried
>   forward as verified.
> - **No register row is changed by this note.** Every row named in this
>   table still reads as it did; reconciling them with the tree is the
>   Technical Debt Register pass's act and the chair's.
>   *(**Done — seam reconciliation, 2026-09-07.** That pass has since run
>   and is integrated. `C-F2`'s row `TD-143` is **Resolved for the seven
>   mutators on `EngineeringObjectBase` and for nothing else**, and the harm
>   class it names is **not** closed: it stands on an eighth path and is
>   **`TD-147`**, Open, **Release Blocking**, which is now the finding the
>   Engineering Readiness Review derives `NOT READY` from. `C-F3`'s row
>   `TD-144` is **Partially resolved** — the in-memory half closed, the
>   durable `groupedUnder` residue open, exactly as this note describes.
>   `C-F4`/`TD-145` and `C-F5`/`TD-146` are unchanged and still Open. **The
>   verdict and the blocker count are unchanged.**)*

### 9.4 What this round still does not have

**No determinism matrix.** **No Agent D.** **No board.** The pattern this
release has repeated five times — a defect found inside the previous
remediation — held again in round 2: Agent C found `C-R1` inside the
governance half and `C-F2` inside the code half. Assume the same of this
pass.

> **All three were subsequently obtained, for `888ef63` — and none of them
> covers `d6af7ec`.** *(`WP 16.4B-R7` governance closure, 2026-09-07. The
> paragraph above is retained unedited as the record of what was true at
> `261ba34`, which is the commit §9's own heading names; it was not marked
> as scoped when it stopped being current, and a reader arriving at it
> today would take it as current state.)*
>
> - **Determinism matrix:** obtained, 5/5 on `888ef63`, runs 253–257,
>   ERR §4.4, recorded by `0935c64`. **Superseded** — `WP 16.4B-R7` changed
>   `src/` and `tests/` after it. A fifth is outstanding for `d6af7ec`.
> - **Board:** a seventh board reviewed the round-2 remediation and
>   returned PASS with six AMBERs and no RED, closed at `888ef63` — see the
>   scope note at the head of this document. **No board has reviewed
>   `d6af7ec`, and no disposition for it exists in this repository.**
> - **`C-F2`/`TD-143`:** a remediation for its failing-durable-write route
>   on `EngineeringObjectBase` landed in `WP 16.4B-R7` and is in the tree at
>   `d6af7ec` — see §8.3's supersession note. **`TD-143`'s register row is
>   not updated to match**, and reconciling the two is the register pass's
>   act and the chair's, not this document's.
>   *(**Reconciled — seam reconciliation, 2026-09-07.** The sentence above
>   is retained as written; the register pass has since run and is
>   integrated. `TD-143` is Resolved for the seven mutators only, `TD-147`
>   is opened Release Blocking for the harm class that remains open on the
>   creation path, and `TD-144` moves to Partially resolved. **The verdict
>   stays `NOT READY` and the blocker count stays one.** What is still
>   missing at `d6af7ec` is unchanged by any of this: **no determinism
>   matrix, and no board disposition for this commit in this repository.**)*
> - The closing instruction — *"Assume the same of this pass"* — held
>   again: this closure found that §8.7's own list was written and then only
>   partially executed for a second time (§8.7), and that §8.6, the
>   `Release Register` and `PROJECT_STATUS.md` sat two candidates behind
>   while two other documents sat one behind.

---

## Related Documents

`docs/releases/v0.16.0/WP16.9.0 Engineering Release Report.md`;
`docs/releases/v0.16.0/v0.16.0 Engineering Status Report.md`;
`docs/releases/v0.16.0/Release Notes.md`;
`docs/adr/ADR-0106-...md` (the ERR model and its taxonomy);
`docs/governance/Quality/Technical Debt Register.md` (`TD-123`–`TD-135`).
