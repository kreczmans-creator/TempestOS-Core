# TempestOS v0.12.0 — Work Packages

## Status

**In progress.** `v0.12.0` — "Desktop Composition & Domain Vocabulary
Hardening" — branch `feature/v0.12.0-desktop-composition-domain-vocabulary-hardening`,
cut from `main` at `b203aef` (the `v0.11.0` merge commit's own successor
on `main`). This document is created now, per this project's own
established convention ("each release's own `WorkPackages.md` is
created when that release's branch is cut," `WP11.4B Release Process
Correction Report.md` §10), seeded from `WP11.0B Architecture Roadmap.md`
§3's own predicted `v0.12.0` table.

**`WP 12.3A`/`WP 12.3B`** were this release's own first completed Work
Packages — a directly-commissioned pair, not named in the roadmap's own
predicted `12.0`–`12.2` slots, the identical pattern `WP 11.3A`/`WP
11.3B` and `WP 11.4A`/`WP 11.4B` each already established for
`v0.11.0`. **`WP 12.0A`/`WP 12.0B`** are the roadmap's own first
*predicted* `v0.12.0` Work Package pair, now both complete — `WP 12.0A`
designed `ADR-0103`/`Desktop Composition Architecture.md`; `WP 12.0B`
implements it exactly, closing Finding `A-1` in full: `MainWindow`
(1,556 → 544 lines) into nine `Tempest.Desktop.Composition`
collaborators, `EngineeringCockpit` (1,398 → 575 lines) into six
per-discipline collaborators. **`WP 12.4A`** (Desktop Command & Event
Wiring Architecture) is a second directly-commissioned Work Package,
following the identical `WP 12.3A`/`WP 12.3B` precedent — reviews the
Desktop layer's own command/event wiring after `WP 12.0B`'s
decomposition and produces `ADR-0104` (Desktop cross-collaborator
communication: direct delegates or typed callback interfaces only,
never a Desktop-local Mediator/Command Dispatcher/Event Dispatcher);
architecture only, on its own branch
(`feature/v0.12.0-desktop-command-event-wiring`), not merged into this
one. **Disclosed naming note**: this Work Package's own scope was
initially proposed under the label `WP 12.1A` before being corrected —
`WP 12.1A` already names the roadmap-predicted "Classification &
Relationship Vocabulary Safety Net Architecture" item, below, unrelated
and untouched by this renumbering. **`WP 12.4B`** (Desktop Command &
Event Wiring Implementation), on the same branch, realises `ADR-0104`
exactly: consolidates `RibbonObjectActionHandlers`'s own 16 duplicated
report-then-refresh tails into one local function, and closes `WP
12.0B`'s own architecture review Finding 5 in both `UndoRedoCoordinator`
and `WorkspaceViewCoordinator` (a two-phase-constructed `CockpitView`
object reference replaced with a plain `Action` delegate). **`WP 12.1A`**
(Classification & Relationship Vocabulary Safety Net Architecture) is
the roadmap's own next predicted item, realising `WP11.0A` Finding
`A-6` — audits every Kind/`Classification`/`RelationshipKind`
vocabulary platform-wide, confirms a real cross-layer duplicate
(`VerifiedByRelationshipKind`) and Mechanical's own complete absence of
declared Kind constants, and produces `ADR-0105` (declare-once
discipline, a new Engineering Vocabulary Register, an additive
consistency test); architecture only, on its own new branch
(`feature/v0.12.0-classification-relationship-vocabulary-safety-net`),
not merged into this one. Includes a same-Work-Package architecture
review follow-up (Architecture Document Register arithmetic corrected;
Component 3's own proposed test location moved to
`Tempest.Desktop.Tests`; two `ADR-0105` clarifications). **`WP 12.1B`**
(Classification & Relationship Vocabulary Safety Net Implementation),
on the same branch, realises `ADR-0105` exactly: every `FactoryRegistry`
retrofitted with named Kind constants, `DigitalThreadGraphModel`'s own
confirmed duplicate closed, the Engineering Vocabulary Register
populated (46 values), and the consistency test added and verified to
actually catch a deliberately-reintroduced rogue duplicate. **`WP
12.2A`** (Presentation Strategy Execution) — discovery finds this
roadmap-predicted item's entire scope already delivered, in `v0.11.0`,
by `WP 11.3A`/`WP 11.3B` (the Work Package pair that actually ratified
and executed the Desktop & Console Presentation Strategy Decision `WP
11.2A` was originally meant to make, since `WP 11.2A`'s own label was
reused for Governance Health-Check Automation instead). Every claim
independently re-verified directly against live source; no ADR, no
new architecture document — neither is justified. Disposed as
**Delivered by `WP 11.3A`/`WP 11.3B`**, mirroring this document's own
identical treatment of the sibling `WP 11.1B`/`WP 11.2A` renumbering
(`docs/releases/v0.11.0/WorkPackages.md`'s own precedent). **`WP
12.9.0`** (Release Preparation & Engineering Sign-Off) — architecture
phase complete, including its own same-day architecture review and
follow-up (two blocking findings — an incomplete ADR Register
reconciliation, and an internally contradictory verdict-derivation
model — both closed): designs the permanent Engineering Readiness
Review (five readiness categories, a written blocking taxonomy, a
fixed verdict vocabulary derived by one explicit, priority-ordered
rule, `ADR-0106`), on its own new branch
(`feature/v0.12.0-release-preparation-and-signoff`), and performs a
direct Phase A repository assessment. **Explicitly not release
engineering** — no `v0.12.0` verdict is rendered; that execution
remains a distinct, later, separately-commissioned Work Package. See
both rows, below, for the full account, including the one genuine,
`v0.12.0`-caused governance gap `WP 12.9.0` found (the Engineering
Vocabulary Register's own missing `Governance Index.md` link) and the
one stale Architecture Document Register row it found and corrected.
Both Work Packages are now merged into `main`. **Release preparation,
2026-08-13**: `docs/releases/v0.12.0/Release Notes.md` produced;
the actual `v0.12.0` Engineering Readiness Review executed for real,
under the corrected `ADR-0106` model — `docs/releases/v0.12.0/WP12.9.0
Engineering Release Report.md`. Verdict: **`NOT READY`**, for a single
procedural reason — `main` has not yet been pushed to `origin`, so
real CI has not run against the pre-tag commit (Verification
readiness's own explicit requirement, `Engineering Readiness Review
Architecture.md` §2.3). All other readiness categories Pass or
Pass-with-observations. No tag created; no push performed; pushing
`main` awaits separate, explicit authorisation.

## Work Packages

| Work Package | Scope | Type | Status |
|---|---|---|---|
| `WP 12.3A` | Fault Injection & Validation Framework Architecture — traces `Tempest.App`'s/`Tempest.Desktop`'s real, unrestricted `TempestHostBuilder()` composition path end to end and finds a genuine, previously-undisclosed defect: `Tempest.Samples.DuplicateNavigationSampleModule`, a deliberately-always-failing module proving `ModuleLifecycleManager`'s per-module isolation (ADR-0013), was discovered and initialised on **every real launch** of either presentation layer, permanently leaving one module `ModuleState.Failed` — confirmed directly against `tests/Tempest.Desktop.Tests/ModuleLifecycleStabilityTests.cs`'s own pre-existing special-case exclusion. Designs `ADR-0102`: a new project, `Tempest.Validation` (namespace `Tempest.Validation.FaultInjection`), chosen over the brief's own suggested `Tempest.Diagnostics`/`Tempest.Samples.Diagnostics` naming specifically to avoid colliding with `IDiagnosticsProvider`/`DiagnosticsSampleModule`, plus a default-excluded discovery-time marker (`IFaultInjectionModule`) — project isolation alone assessed and found insufficient on its own, since `ReflectionFrameworkDiscoveryService`'s unrestricted overload scans the whole process's `AppDomain`, not only directly-referenced assemblies. Architecture only; no code. See `WP12.3A Fault Injection & Validation Framework Architecture.md` (Academy retrospective — this release currently has no dedicated `docs/releases/v0.12.0/` architecture-phase document beyond this `WorkPackages.md` row and the Academy retrospective; the full design lives in `docs/architecture/Fault Injection & Validation Architecture.md`, this platform's own established location for standing architecture documents). | Architecture | **Complete** |
| `WP 12.3B` | Fault Injection & Validation Framework Implementation — realises `WP 12.3A`'s design unchanged, with one disclosed addition found during implementation: `Tempest.Validation` also references `Tempest.Samples` (downward only, no cycle), so the moved module references `NavigationSampleModule.NavigationItemId` directly rather than duplicating it as a second string literal. `DuplicateNavigationSampleModule` moved out of `Tempest.Samples`, renamed `DuplicateNavigationModule`; new marker interface `IFaultInjectionModule` (`Tempest.Core.Modules`); `ReflectionFrameworkDiscoveryService` gains a defaulted `includeFaultInjectionModules` constructor parameter (default `false`, zero behavioural change for any existing caller); `ITempestHostBuilder`/`TempestHostBuilder` gain one new fluent method, `EnableFaultInjectionModules()`. **The actual defect closed, verified directly, not merely asserted**: `ModuleLifecycleStabilityTests.cs`'s special-case exclusion deleted, not updated — a real `WorkspaceHost`, composed through the identical production path `Tempest.App` itself uses, now genuinely reaches `Running` with zero modules `Failed`. New end-to-end proof (`FaultInjectionModuleDiscoveryTests.cs`): a real `TempestHostBuilder` discovers zero modules from `Tempest.Validation` without `.EnableFaultInjectionModules()`, and discovers + correctly isolates the fault-injection module with that one call added. Full regression re-verified directly, both configurations, immediately before this Work Package's own documentation phase and again as its closing gate: 2,233/2,233 passing (2,031 `Tempest.Core.Tests` + 202 `Tempest.Desktop.Tests`), 0 Warnings/0 Errors, both Debug and Release. No production code, ADR, or architecture change beyond what is disclosed above. See `WP12.3B Fault Injection & Validation Framework Implementation.md`, `docs/architecture/Fault Injection & Validation Architecture.md`, `ADR-0102`. | Implementation | **Complete** |
| `WP 12.0A` | Desktop Composition Root Decomposition Architecture (`A-1`) — this roadmap's own first predicted `v0.12.0` Work Package. Re-verifies `MainWindow.cs` (1,556 lines, a ~1,000-line constructor spanning platform-service resolution, five desktop-local state loads, all docking/panel wiring, view construction, ~450 lines of per-discipline Ribbon object-action dispatch, cross-view coordination, undo/redo, window lifecycle) and `EngineeringCockpit.cs` (1,398 lines, a flat six-Engineering-Discipline read-model) unchanged since `WP11.0A`. Designs `ADR-0103` — "Composition Roots Own Collaborators": a general, platform-wide pattern (not scoped to either file) extending `ADR-0009`'s own composition-root principle one layer down — composition-root/collaborator responsibilities, ownership/lifetime, construction/dependency rules, and an explicit prohibition on DI-registering an extracted collaborator (reusing `ADR-0013`'s own classification test verbatim); rejects a `partial class` split (`ADR-0017`'s own structural-over-conventional precedent) and a declarative/reflective composition framework (`ADR-0032`'s own "no genuine extensibility trigger" precedent) as alternatives. `docs/architecture/Desktop Composition Architecture.md` records the pattern, with `MainWindow`/`EngineeringCockpit` as motivating, explicitly non-binding illustrative evidence — the real, final collaborator boundaries are `WP 12.0B`'s own job. Architecture only; zero `src/`/`tests/` files touched, confirmed directly. See `WP12.0A Desktop Composition Root Decomposition Architecture.md` (Academy retrospective), `docs/architecture/Desktop Composition Architecture.md`, `ADR-0103`. | Architecture | **Complete** |
| `WP 12.0B` | Desktop Composition Root Decomposition Implementation (`A-1`) — implements `ADR-0103`/`Desktop Composition Architecture.md` exactly, refactor only: zero public API changes, zero behavioural changes, zero new Platform Services, zero new DI registrations, no `partial class`. `MainWindow.cs` (1,556 → 544 lines; ~1,000 → ~370-line constructor) decomposes into nine `Tempest.Desktop.Composition` collaborators — `DesktopCompositionRoot` (Platform Service resolution), `DesktopSessionState` (five session-state loads), `WorkspaceDockingComposer` (panel construction/resize/hide/collapse/pin/flyout), `RibbonObjectActionHandlers` (the ~450-line, ~29%-of-file per-discipline handler population), `WorkspaceViewCoordinator` (Explorer/Inspector/Document-Area cross-view wiring, `ToggleFavourite`, dirty-aware document close), `UndoRedoCoordinator` (the stack, its two buttons, `Undo`/`RedoAsync`, now fully reactive via `IUndoRedoStack.Changed` rather than several explicit refresh call sites), `MainMenuFactory`/`QuickAccessToolbarFactory` (stateless static build functions), `WorkspaceLayoutPresetCoordinator` (the three named presets). `EngineeringCockpit.cs` (1,398 → 575 lines) decomposes into six per-discipline read-model collaborators (Mechanical/Requirements/Calculations/Documents/Verification/Manufacturing), one per existing `Tempest.App.Workspace.{Discipline}` namespace; genuinely cross-discipline aggregation (`Health`, `KpiCards`, `AttentionItems`, `OpenActions`, `BlockedItems`) and the Governance & Risk family (Decisions/Risks/Milestones/Tasks — none of the six named disciplines) stay directly on the composition root, per `ADR-0103`'s own Composition-root Responsibility #4. Every collaborator is `new`-constructed, never DI-registered, never references a sibling collaborator directly — two genuine construction-order cycles (`DocumentAreaView`/`CockpitView` needing `WorkspaceViewCoordinator`'s own `BuildDocumentContent`/`NavigateToObject`, which in turn need them back) resolved via the identical "field assigned after construction, read lazily by a deferred delegate" two-phase sequencing the pre-decomposition source already used, one collaborator boundary away, not a new pattern. 12 new characterization tests added before their own respective extractions (5 `EngineeringCockpit` — `AttentionItems`/`OpenActions`/per-discipline KPI cards/Mechanical reads/cross-cutting Workspace reads had zero prior direct coverage; 7 `MainWindow` — Menu System View/Layout/Document menus and Quick Access Toolbar Reset Layout/View Relationships/Undo-Redo reactivity/Macros had zero prior coverage), both real, confirmed-by-direct-search gaps, not a speculative addition. Full Debug+Release regression: 2,245/2,245 passing, 0 Warnings/0 Errors, both configurations. No new ADR; `ADR-0103` unmodified — no factual correction was exposed by implementation. **Governance follow-up, same Work Package**: a read-only architecture/code review found ten findings, none blocking except one (Finding 3) — `Desktop Composition Architecture.md`'s own prior "Documentation Impact" section had named `Namespace Register.md` as this Work Package's own obligation, but that register's own declared Scope has never covered `src/Tempest.Desktop/` at all; resolved by explicit disclosure and reconciliation across `Namespace Register.md`, `Desktop Composition Architecture.md`, and the `WP12.0B` Academy retrospective (all three now agree) rather than by unilaterally extending that register's scope as a side effect of this Work Package — see `Namespace Register.md`'s own `WP 12.0B` (follow-up) "Last Reviewed" entry for the complete reasoning. See `WP12.0B Desktop Composition Root Decomposition Implementation.md`, `docs/architecture/Desktop Composition Architecture.md`, `Namespace Register.md`, `ADR-0103`. | Implementation | **Complete** |
| `WP 12.4A` | Desktop Command & Event Wiring Architecture — a second directly-commissioned Work Package (following `WP 12.3A`/`WP 12.3B`'s own precedent), reviewing the Desktop layer's own command, event, and UI-wiring architecture as it stands after `WP 12.0B`'s decomposition, and formally evaluating how Desktop collaborators should communicate going forward. Catalogues every event in `Tempest.Desktop` directly (25, all plain `Action`/`Action<T>` delegates; zero unsubscription, zero `IDisposable` on any View or collaborator — found safe today given exactly one `MainWindow` is ever constructed per process, recorded as a disclosed design characteristic, not silently left unaddressed). Documents four distinct, already-individually-ADR-governed command mechanisms now composing together for the first time in one place (`ICommandRegistry`/`ICommandDispatcher`, `RibbonView.ObjectCreationHandlers`, `KeyboardShortcuts`, `KeyboardCommandBindingProvider`) and confirms none is redundant with another. Finds and quantifies real, measured duplication — a four-statement "report via StatusBar/Toast, then refresh Explorer/Cockpit" tail repeated 42/25/25/19 times respectively across six files, concentrated in `RibbonObjectActionHandlers` and `WorkspaceViewCoordinator` — named as a real implementation-hygiene opportunity for a future `WP 12.4B`, not designed in full here. Re-confirms `WorkspaceViewCoordinator` as the strongest remaining decomposition candidate (`WP 12.0B`'s own architecture review Finding 4, unchanged); confirms neither it nor `RibbonObjectActionHandlers` nor `MainWindow`'s own constructor meets this project's own "God Method" bar (`FOUNDATION.md` non-negotiable 2) today. Evaluates six candidate cross-collaborator communication mechanisms in full (advantages/disadvantages/layering/ownership/lifetime/testing each) — direct delegates, typed callback interfaces, a Desktop-local Mediator, a Desktop-local Command Dispatcher, a Desktop-local Event Dispatcher, and reuse of the existing platform Command/Event Framework. **`ADR-0104` produced**: direct delegates remain the default; typed callback interfaces sanctioned narrowly, at three or more bundled callbacks (directly answering `WorkspaceViewCoordinator`'s own 18-parameter constructor); a Desktop-local Mediator, Command Dispatcher, and Event Dispatcher are each explicitly rejected as Desktop composition mechanisms, closing three concrete future temptations by name rather than by inference — meets Engineering Governance §5's own ADR-creation criteria (a genuine, evaluated either/or choice with a rationale needing to be recorded), applying `ADR-0103`'s own principles more specifically rather than replacing or narrowing them. Architecture only; zero `src/`/`tests/` files touched, confirmed directly. See `WP12.4A Desktop Command & Event Wiring Architecture.md` (Academy retrospective), `docs/architecture/Desktop Command & Event Wiring Architecture.md`, `ADR-0104`. | Architecture | **Complete** |
| `WP 12.4B` | Desktop Command & Event Wiring Implementation — a third directly-commissioned Work Package, on the same branch as `WP 12.4A`, realising `ADR-0104` exactly as approved. Three characterization tests added before any refactor (`MechanicalCreate_OnARealPart_ActuallyCreatesARealObject`, `MechanicalDuplicate_OnARealPart_ActuallyCreatesARealCopy`, `DocumentsApprove_OnAFreshDraftDocument_IsRejectedByRealValidation_NeverThrows`), closing two real, confirmed-by-direct-search gaps: zero direct Ribbon-handler coverage for the Mechanical discipline, and the shared `statusHandler` factory's own failure branch had never been exercised. Two direct-delegate changes, neither a typed callback interface. Before this Work Package, `WorkspaceViewCoordinator`'s own genuine, delegate-shaped callback-parameter count was 2 (`refreshStatusBar`, `recordHistory`) — below `ADR-0104`'s own 3-callback threshold. This Work Package introduces a third callback, `refreshCockpit`, bringing the count to 3 and reaching that threshold; despite reaching it, introducing a typed callback interface in the same change that first crosses the threshold was assessed and deferred as an explicit, disclosed engineering judgement — not applied speculatively — even though `WorkspaceViewCoordinator` is `ADR-0104`'s own named motivating example: (1) `RibbonObjectActionHandlers`'s own 16 duplicated report-then-refresh tails consolidated into one local function, `ReportAsync`, defined inside the existing constructor — pure extract-method, zero new abstraction, zero constructor-signature change; every handler's own message text preserved unchanged. (2) `WP 12.0B`'s own architecture review Finding 5 closed in both `UndoRedoCoordinator` and `WorkspaceViewCoordinator` — a two-phase-constructed `CockpitView` object reference (a nullable field plus an `Attach`/`AttachCockpitView` method) replaced with a plain `Action refreshCockpit` constructor parameter, wired from `MainWindow` via the same field-closure lazy-capture pattern already established there for `DocumentAreaView`; `WorkspaceViewCoordinator`'s own two-phase `Attach` now takes only `DocumentAreaView` — the one genuine remaining construction-order cycle. Zero Desktop mediator, Desktop-local command dispatcher, or Desktop-local event dispatcher introduced; the platform Command/Event Framework untouched. `ADR-0103`'s ownership rules preserved throughout — every collaborator remains `new`-constructed by `MainWindow`, never DI-registered, never referencing a sibling collaborator directly. All public APIs preserved; behaviour verified identical — the three new characterization tests pass with identical assertions against both the pre-refactor and post-refactor code. Full Debug+Release regression: 2,248/2,248 passing (2,031 `Tempest.Core.Tests` + 217 `Tempest.Desktop.Tests`, net +3 from the new characterization tests, zero regressions), 0 Warnings/0 Errors, both configurations. No new ADR — `ADR-0104` unmodified, implemented as designed with no factual correction exposed. See `WP12.4B Desktop Command & Event Wiring Implementation.md` (Academy retrospective), `docs/architecture/Desktop Command & Event Wiring Architecture.md` (Status: Implemented), `ADR-0104`. | Implementation | **Complete** |
| `WP 12.1A` | Classification & Relationship Vocabulary Safety Net Architecture (`A-6`) — the roadmap-defined Work Package, realising `WP11.0A Platform Architecture Review.md` Finding `A-6` ("Domain classification/relationship vocabulary is entirely stringly-typed, by deliberate design, with no compile-time safety net"). Audits every Kind/`Classification`/`RelationshipKind` vocabulary across `Tempest.Core`/`Tempest.App`/`Tempest.Desktop`. Confirms five prior ADRs (`ADR-0072`/`ADR-0073`/`ADR-0076`/`ADR-0088`/`ADR-0091`) already, independently, settle the open-string-vs-closed-type question — none reopened — while `ADR-0090` shows `Classification`-tagging is not universally the right sub-typing axis (Verification deliberately reuses `LifecycleState` instead). Finds the confirmed gap precisely bounded: four `Tempest.Core` frameworks and two Workspace disciplines (Documents/Manufacturing) already declare their own vocabulary values as named constants; Mechanical declares none. Quantifies real duplication — the literal Kind string `"Part"` written out 14 times across 5 files with no shared constant, and a confirmed cross-layer duplicate (`Tempest.Desktop.DigitalThread.DigitalThreadGraphModel`'s own `VerifiedByRelationshipKind` independently redeclaring `Tempest.Core.Verification.VerificationService`'s own identical constant). Finds and generalises three already-shipped, independently-proven instances of the eventual safety-net shape (`RequirementRelationshipKinds`, `RelationshipKindCategoryMap`, `DocumentCategory.Of`/`ManufacturingCategory.Of`). **`ADR-0105` produced**: every live vocabulary value declared once, as a named constant, on its owning class; a new Engineering Vocabulary Register tracks every value platform-wide; one additive xUnit test catches exact-value collisions and register/code drift — never validation at write time. Four alternatives evaluated and rejected: a closed enum (already rejected five times independently), a strongly-typed value object, a runtime DI-resolved registry service, a fully free-form metadata model. Digital Thread roadmap compatibility confirmed low-risk directly. Architecture only; zero `src/`/`tests/` files touched, confirmed directly. `WP 12.1B`'s own minimum scope named directly: populate the Engineering Vocabulary Register, add the consistency test, retrofit the two confirmed defects. **Architecture review follow-up, same Work Package**: a read-only review found two findings requiring closure — `Architecture Document Register.md`'s own Total/Coverage sections fully re-derived (26 real entries, not 24; corrects a pre-existing, compounding miscount); Component 3's own proposed test location moved from `Tempest.Core.Tests` to `Tempest.Desktop.Tests`, the only project that can reflect across all three layers a cross-layer vocabulary duplicate might span — plus two `ADR-0105` clarifications (ownership is determined by which component writes a value, not which project compiles its type; why declared constants are `public`, not `internal` as `A-6`'s own illustrative suggestion named). Documentation only; zero `src/`/`tests/` files touched. See `WP12.1A Classification & Relationship Vocabulary Safety Net Architecture.md` (Academy retrospective), `docs/architecture/Classification & Relationship Vocabulary Safety Net Architecture.md`, `ADR-0105`. | Architecture | **Complete** |
| `WP 12.1B` | Classification & Relationship Vocabulary Safety Net Implementation (`A-6`) — realises `ADR-0105` exactly, on the same branch as `WP 12.1A`; implements the architecture only, does not revisit or extend the decision. Every `FactoryRegistry` retrofitted: `MechanicalObjectFactoryRegistry` (the largest confirmed gap) gains all eight Kind constants, used internally throughout its own `SupportedKinds`/switch/factory-argument sites; `DocumentObjectFactoryRegistry`/`ManufacturingObjectFactoryRegistry` gain their own base Kind constants alongside their already-disciplined `Classification` sub-values; `CalculationObjectFactoryRegistry` gains `CalculationKind`/`CalculationSetKind`. `DigitalThreadGraphModel`'s own confirmed duplicate closed — found, while directly reading the file, to be three local constants, not one (`VerificationActivityKind`/`VerificationRecordKind`/`VerifiedByRelationshipKind`), all now referencing their real owning constants instead. New Engineering Vocabulary Register (`docs/governance/Engineering/`) populated from a full repository scan — 46 declared values across 11 declaring classes, plus 7 further conventional values honestly marked Undeclared; one genuine, pre-existing dual-ownership case found and disclosed (`references`, independently, legitimately owned by both `RequirementRelationshipKinds` and `VerificationService`), mirroring `ADR-0073`'s own already-accepted vocabulary-drift risk, not treated as a defect. New `EngineeringVocabularyConsistencyTests` (`Tempest.Desktop.Tests`, per the corrected `WP 12.1A` architecture review) — four tests covering register/code drift, cross-owner duplication, register self-consistency, and a rogue-duplicate scan across all four layer assemblies; **verified to actually work**, not merely assumed — a rogue duplicate was deliberately, temporarily reintroduced mid-implementation, confirmed to fail the test with a precise message naming both classes, then reverted. Three characterization tests added before refactoring (`MechanicalCommandsTests`'s own `Configuration`/`Baseline`/`Release` `InlineData` cases, closing a real, confirmed-by-direct-search gap `WP 12.1A`'s own literal-string grep had missed, since the existing coverage used `[Theory]`/`[InlineData]`). No write-time validation, no runtime registry, no enum, no value object introduced anywhere — the open-vocabulary philosophy fully preserved. Full Debug+Release regression: 2,255/2,255 passing (2,034 `Tempest.Core.Tests` + 221 `Tempest.Desktop.Tests`, net +7 new tests, zero regressions), 0 Warnings/0 Errors, both configurations. No new ADR — `ADR-0105` unmodified, implemented as designed. **Architecture review, same Work Package**: a read-only architecture/code review found two non-blocking documentation findings, no implementation defects — the register's own declaring-class count of "12" narrative over-counted `VerificationService` (which appears in both the Kind and RelationshipKind tables but is one class; true count 11), propagated into six further documents; and the architecture doc's Component 3 description named only two of the four detection checks the implemented test actually provides. Verdict: Approved with documentation follow-up. **Documentation follow-up, same Work Package**: the propagated "12" corrected in all seven affected documents, confirmed by repository-wide search that no occurrence remains; `Classification & Relationship Vocabulary Safety Net Architecture.md`'s own Component 3 section updated to name all four implemented checks. Documentation only; `ADR-0105` not reopened; zero `src/`/`tests/` files touched. See `WP12.1B Classification & Relationship Vocabulary Safety Net Implementation.md` (Academy retrospective), `docs/governance/Engineering/Engineering Vocabulary Register.md`, `ADR-0105`. | Implementation | **Complete** |
| `WP 12.2A` | Presentation Strategy Execution (realises `WP 11.2A`'s decision) — discovery (Phase A) finds `WP 11.2A` itself was never commissioned as predicted; its label was reused for Governance Health-Check Automation instead, and the real Desktop & Console Presentation Strategy Decision (`WP11.0A` finding `A-2`) was ratified and executed under a different pair, `WP 11.3A` (review)/`WP 11.3B` (implementation), both `Complete` in `v0.11.0`, before `v0.12.0` even branched. Every claim independently re-verified directly against live source, not carried forward: `TempestShell`/`IPage`/`PlaceholderPage` confirmed retired (zero references anywhere in `src/`/`tests/`); `ADR-0101` confirmed Accepted, unmodified, formally classifying `Tempest.App`/`WorkspaceShell` as TempestOS's Internal Engineering Harness, not a shipped product; `README.md`/`Contributor Learning Path.md` confirmed corrected; `ci.yml`/`release.yml` confirmed packaging `Tempest.Desktop`/`Tempest.App` as two separate, clearly-labelled artifacts; `Shell & Composition Framework Architecture.md` confirmed already reflecting the final state via its own `WP 11.3B` addendum. `WP11.3A`'s own five-stage roadmap's Stage 5 (further `WorkspaceShell` test/feature trimming) was deliberately not executed by `WP 11.3B` — confirmed, not assumed, to be a formal, disclosed deferral (`WP11.9.0 Engineering Release Report.md`'s own "Product Debt... deferred by design" entry), gated on "a real, demonstrated cost problem" that a direct search of both the Technical Debt Register and the Future Capability Register confirms has still not occurred. Phase B (Architectural Analysis) evaluates four disposition alternatives and concludes, honestly, neither a new ADR nor a new/updated architecture document is justified — Engineering Governance §5's own ADR-creation criteria is not met (no new either/or choice exists to record), and `Shell & Composition Framework Architecture.md` already documents the current state correctly. Architecture only; zero `src/`/`tests/` files touched, confirmed directly; no ADR produced. See `WP12.2A-presentation-strategy-execution-architecture.md` (Academy retrospective), `docs/releases/v0.11.0/WP11.3A Presentation Strategy Review.md`, `ADR-0101`. | Implementation | **Delivered by `WP 11.3A`/`WP 11.3B` — no remaining scope** |
| `WP 12.9.0` | `v0.12.0` Release Preparation & Engineering Sign-Off — Architecture phase. Designs the permanent TempestOS Engineering Readiness Review (ERR): five readiness categories (Architecture/Implementation/Verification/Governance/Release), each with defined required evidence, pass criteria, and blocking conditions; a written blocking taxonomy (Release Blocking / Disclosed Non-Blocking / Pre-Existing Unaffected — no prior sign-off ever wrote this down); a fixed, four-value verdict vocabulary (`CERTIFIED`; `CERTIFIED WITH ACCEPTED TECHNICAL DEBT`; `ACCEPT WITH OBSERVATIONS`; `NOT READY`); executed via the six-discipline Programme Review `WP 10.9A`/`WP 11.9.0` each already, independently, converged on. **`ADR-0106` produced** — three alternatives evaluated and rejected (continue the ad hoc checklist; a single-reviewer sign-off; a purely automated gate), each on this project's own concrete evidence, most directly `WP 11.9.0`'s own independently-found `TD-42`/`TD-43` as the case against collapsing multi-discipline review. Phase A repository assessment performed directly, not carried forward: `main`'s own merge state confirmed via `git log` (four of five roadmap WP pairs merged); `WP 12.2A`'s own disposition, above, was committed and merged as part of this same overall closure; Debug+Release builds and full regression freshly re-run (0 Warnings/0 Errors both, 2,255/2,255 passing both); `governance-healthcheck.ps1` run directly (3 passed/1 warned/4 failed of 8 — one genuine, `v0.12.0`-caused gap found: `Engineering Vocabulary Register.md` never linked from `Governance Index.md`; remainder pre-existing/tool false-positives); Technical Debt (48) and Future Capability (84) registers independently re-derived, nothing newly blocking. **Found and fixed, this Work Package's own Governance-readiness review**: `Architecture Document Register.md`'s own `Classification & Relationship Vocabulary Safety Net Architecture.md` row corrected Designed → Implemented (`WP 12.1B` had already implemented `ADR-0105`), plus that register's own stale `Related ADRs` field. This is explicitly not release engineering — no verdict is rendered for `v0.12.0` itself here; the actual `v0.12.0` Engineering Readiness Review execution is a distinct, later, separately-commissioned Work Package. **Architecture review, same day**: a read-only review found two blocking findings — the ADR Register's own Entries table had never been updated with `ADR-0106`'s own row (`governance-healthcheck.ps1`: 105 files, 104 register rows); and the blocking taxonomy/category-status/verdict-vocabulary relationship was internally contradictory, making `CERTIFIED WITH ACCEPTED TECHNICAL DEBT` an unreachable verdict as originally written. **Follow-up, same day**: the Entries table row added (`governance-healthcheck.ps1` now `[PASS]`, 105/105); §4 of the architecture document rewritten as the single, explicit, priority-ordered derivation from finding kind → category status → release verdict, with §2/§6 and `ADR-0106`'s own Decision section both corrected to cite it rather than each restate their own version — the minimum change that makes all four verdicts simultaneously reachable, mutually exclusive, and objectively derivable. No other section of the model, and no `src/`/`tests/` file, touched at either pass. Architecture only. See `WP12.9.0-release-preparation-and-engineering-sign-off-architecture.md` (Academy retrospective, §8 Addendum for the full account), `docs/architecture/Engineering Readiness Review Architecture.md`, `ADR-0106`. | Architecture | **Complete (architecture phase, including its own review and follow-up) — execution not yet commissioned** |
| `WP 12.9.1` | Governance Health Check Remediation — repairs the four genuine `[FAIL]` results `governance-healthcheck.ps1` reports against `main`, per `WP 12.9.0`'s own Phase A finding above. Runs the checker directly against `main`'s actual tip first (not the stale commit the controlling brief's own evidence cited), which immediately removes one named finding — missing version tags — already resolved by intervening work. Root-causes all four remaining failures by reading the checker's own logic line by line, not by guessing: `Academy Index.md`'s "Work Package Walkthroughs" section, silently unmaintained since `WP 7.3A`, gains 63 real links (the `v0.7.0` tail, all of `v0.8.0`/`v0.9.0`/`v0.10.0`, and `v0.12.0`'s ten completed Work Packages) plus two new concept-guide topic sections; `Documentation Register.md`/`PROJECT_STATUS.md` corrected — every bare numbered-Academy-folder shorthand reference fully qualified (a documented, deliberate checker scope limit per `WP11.2A Governance Health-Check Tool.md`'s own verification section, confirmed, not assumed, and left untouched), three hyphen-hard-wrap defects rewrapped, and `docs/diagrams/`, `docs/roadmap/`, `src/Plugins/` each given a tracked, honestly-disclosed `README.md` marker closing the exact gap `WP 11.2A` named and explicitly deferred (git cannot track an empty directory); `docs/releases/v0.2.0` (a stray, never-completed release-notes file, not an empty directory as `WP 11.2A`'s own finding had conflated it) renamed to `docs/releases/v0.2.0.md`, matching `v0.3.0.md`'s own convention; `Governance Index.md` gains the one missing link `WP 12.9.0` itself had already found (`Engineering Vocabulary Register.md`). `scripts/governance-healthcheck.ps1` left unmodified throughout — every finding traced to genuine repository/documentation drift or an already-documented checker scope limit, never a checker defect. `governance-healthcheck.ps1` re-run clean after every fix: 7 passed, 1 warned (informational, already accepted by `WP 11.2A`'s own design), 0 failed. Documentation/governance only; zero `src/`/`tests/` files touched, confirmed directly; no ADR required (no new either/or architectural choice). See `WP12.9.1-governance-health-check-remediation.md` (Academy retrospective, full investigation report). | Documentation/Governance | **Complete** |

## Related Documents

`docs/releases/v0.11.0/WP11.0B Architecture Roadmap.md` §3, §5, §6 (this
release's own originally-predicted scope, estimates, dependencies, and
sequence — `WP 12.3A`/`WP 12.3B` are additive to it, not a substitute for
it; `WP 12.0A` is its own first predicted item to complete);
`docs/releases/v0.11.0/WP11.0A Platform Architecture Review.md` (Finding
`A-1`); `docs/academy/03 Work Packages/WP12.3A-fault-injection-validation-framework-architecture.md`;
`docs/academy/03 Work Packages/WP12.3B-fault-injection-validation-framework-implementation.md`;
`docs/academy/03 Work Packages/WP12.0A-desktop-composition-root-decomposition-architecture.md`;
`docs/academy/03 Work Packages/WP12.0B-desktop-composition-root-decomposition-implementation.md`;
`docs/academy/03 Work Packages/WP12.4A-desktop-command-and-event-wiring-architecture.md`;
`docs/academy/03 Work Packages/WP12.4B-desktop-command-and-event-wiring-implementation.md`;
`docs/academy/03 Work Packages/WP12.1A-classification-and-relationship-vocabulary-safety-net-architecture.md`;
`docs/academy/03 Work Packages/WP12.1B-classification-and-relationship-vocabulary-safety-net-implementation.md`;
`docs/academy/03 Work Packages/WP12.2A-presentation-strategy-execution-architecture.md`;
`docs/academy/03 Work Packages/WP12.9.0-release-preparation-and-engineering-sign-off-architecture.md`;
`docs/releases/v0.11.0/WP11.3A Presentation Strategy Review.md`;
`docs/releases/v0.11.0/WP11.2A Governance Health-Check Tool.md`;
`docs/releases/v0.11.0/WP11.9.0 Engineering Release Report.md`;
`docs/governance/Engineering/Engineering Vocabulary Register.md`;
`docs/architecture/Fault Injection & Validation Architecture.md`;
`docs/architecture/Desktop Composition Architecture.md`;
`docs/architecture/Desktop Command & Event Wiring Architecture.md`;
`docs/architecture/Classification & Relationship Vocabulary Safety Net Architecture.md`;
`docs/architecture/Engineering Readiness Review Architecture.md`;
`ADR-0101`; `ADR-0102`; `ADR-0103`; `ADR-0104`; `ADR-0105`; `ADR-0106`; `docs/releases/v0.11.0/WorkPackages.md` (the
immediately preceding release, and the direct precedent for inserting a
directly-commissioned Work Package pair outside a predicted roadmap
slot — `WP 11.3A`/`B`, `WP 11.4A`/`B`); `PROJECT_STATUS.md`.
