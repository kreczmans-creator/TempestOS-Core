# WP 16.9.0 — Engineering Release Report (v0.16.0)

**Type.** Verification and release preparation. The Engineering Readiness
Review required by `ADR-0106`, executed by the Programme Review structure
that ADR mandates.
**Release candidate this report was written against — SUPERSEDED, see
the next line.** `58c4cbaa1affec1cddb01a089fc5cbc0547634a4`, on
`release/v0.16.0-rc4`. *(Corrected `WP 16.4B-R6`, 2026-09-06: this line
read `d7d3f3b`, frozen on `release/v0.16.0-rc`, which was the candidate
when this report was first written and has since been superseded twice —
by `fce2166`/`f593e5c`, then by `58c4cba`. The determinism evidence for
each is preserved in §4.1, §4.2 and §4.3 respectively.)*
**Candidate under review.** `d6af7ec7bd2e2d182eba5ef77bbe703a8a38134c`,
on `feature/v0.16.0-wp16.4b-r6`. **Not merged, not tagged, not
published.** *(Added `WP 16.4B-R6` round 2, 2026-09-06; updated the same
day from `3dd74d8` — the pre-integration candidate — once round 2's two
streams were integrated at `ae2b7f0` and `8465945` and extended by
`2192e5b` and `261ba34`; and updated again to `888ef63`, which closes the
six AMBERs the sixth board raised. Agent C failed an earlier version of
this report for still naming `3dd74d8` after the merge, and it was right
to; the same defect would have recurred here had this line been left at
`261ba34`. Evidence citations that name `261ba34` or an earlier commit
are scoped to what was read there and are deliberately left alone.)*
*(Updated `WP 16.4B-R7` governance closure, 2026-09-07: this line read
`888ef63`, which stopped being the candidate the moment `WP 16.4B-R7`
landed. `888ef63..d6af7ec` is eight commits and changes three files under
`src/` and five under `tests/` — `git diff --name-only 888ef63 d6af7ec`
lists them. `888ef63` is retained above, and in §4.4, as the commit the
fourth determinism matrix was obtained against and **must not** be
relabelled: that matrix covers `888ef63` and no later tree. **No branch
named `feature/v0.16.0-wp16.4b-r7` exists** — checked with `git branch -a`;
`WP 16.4B-R7` was developed on the `-r6` branch, and the SHA governs.
`main` is `58c4cba`, and `git merge-base --is-ancestor d6af7ec 58c4cba`
fails, so **not merged** above is correct and is not being softened.)*
`58c4cba` above
is the commit this report was written against and is the commit `main`
points at; it is **no longer the candidate**, because `WP 16.4B-R6`
changed `src/` and `tests/` after it. Every piece of evidence in §4 is
scoped to the commit it names and stays scoped to it. **This SHA changes
on any further change to `src/` or `tests/`**; the complete, enumerated
list of the places that carry it is in `v0.16.0 Review Board
Disposition.md` §8.7, so updating it is mechanical rather than a fresh
hunt. *(Corrected `WP 16.4B-R7` governance closure, 2026-09-07: this read
"changes when the round-2 remediation is integrated", which was discharged
twice over — at `261ba34` and again at `888ef63` — and had become a
forward promise about an event already in the past.)*

**Recommended verdict.** **NOT READY** — derived in §6, not chosen.
*(Revised `WP 16.4B-R6` round 2, 2026-09-06. This line read **ACCEPT WITH
OBSERVATIONS** from the day this report was written until now. That
recommendation rested on §3's claim that no Release Blocking finding
remained, which was false about `58c4cba` itself: the fifth review board
raised three chair-confirmed REDs against that exact commit, one of them a
permanent data-loss regression, and a sixth independent board then raised
four more. The old verdict, and the derivation that produced it, are
retained in §6.1 rather than deleted.)*

> **This document recommends. It does not certify.** Engineering
> Governance §9 reserves the verdict to Product Approval, and `ADR-0106`
> repeats it: "the Programme Review recommends, it never decides."
> `D-021`–`D-026` were **ratified by the Product Owner on 2026-09-05**
> (gate 1 of 4, scope only). `v0.16.0` **has been merged to `main`** —
> first at `cc7ef4d` on 2026-09-05, most recently at `58c4cba` on
> 2026-09-06, on explicit Product Owner authorisation recorded in §7. It is
> **not tagged, not published, and no Product Approval verdict is
> recorded.** *(Corrected `WP 16.4B-R6`: this paragraph read "`main` is
> untouched. Nothing is merged, tagged or published". The merge had
> already happened; the other three negations were and remain true.)*

---

## 1. How this review was conducted

`ADR-0106` requires five categories, independently reviewed by a
multi-discipline Programme Review, findings classified by a fixed
three-kind taxonomy, and a verdict derived by a single exclusive priority
order.

This release ran that structure **twice**, because the first pass found
defects in the release's own work and the remediation then needed
reviewing in its own right:

- **Board 1 — nine perspectives** against the integrated tree: governance,
  documentation/registers, build-CI-release, accessibility, product
  scope, architecture, security, persistence, test quality.
- **Board 2 — six fresh perspectives** against a pinned, immutable
  checkout, with an explicit mandate to assume board 1 had missed
  something and that **the remediation itself may have introduced
  defects**. No reviewer on board 2 had written the code it examined.

Board 2 was not a formality. It found the most serious defect of the
release, which board 1 had missed and which the remediation for board 1's
own finding did not close.

## 2. The five categories

### Architecture readiness — **Pass, with observations**

The two substantive mechanisms — schema-versioned durable state
(`ADR-0120`) and the DI registration contract (`ADR-0122`) — were
independently re-derived and hold. Layering is intact: `Tempest.Core`
carries zero project references, and ASP.NET Core remains confined to
`RestApiHostedService` per `ADR-0049`, verified by direct grep rather
than assumed. The composition-root carve-out (`ADR-0009`/`ADR-0017`) is
correctly applied.

Three decisions shipped without the ADR §5 requires. All three are now
written: `ADR-0121` (a test-only construction seam is an `internal`
constructor), `ADR-0122` (registering a service twice is an error), and
`ADR-0123` (a migration-chain collision is a registration error, with no
opt-out). `ADR-0123` was written only after board 2 rejected a Work
Package's own explicit "No ADR" judgement.

*Observations:* the migration-walk algorithm is duplicated across the two
migration seams rather than shared; the document-partition-by-Kind loop
now appears four times. Both recorded for a future release rather than
refactored — this release forbids scope creep, and neither is a defect.

### Implementation readiness — **Not Ready**

*(Re-derived `WP 16.4B-R6` round 2, 2026-09-06. This heading read
**Pass, with observations**. The category carries a Release Blocking
finding — `TD-143` — set out at the end of this subsection and in §3.2.
The finding named here was `TD-140` until the round-2 remediation was
integrated at `261ba34`; `TD-140` is now closed for the route it named and
the same demonstrated harm stands, by another route, as `TD-143`.)*

*(**Blocker identity re-derived at `d6af7ec`, `WP 16.4B-R7` governance
closure — seam reconciliation, 2026-09-07.** The sentence above is retained
unedited as the record of what was true at `261ba34`. The Technical Debt
Register pass has since run and is integrated: **`TD-143` is now Resolved
for the seven mutators on `EngineeringObjectBase` and for nothing else**,
and the harm class it was raised for is **not** closed — it stands on an
eighth path, `EngineeringObjectFactory<T>.CreateAsync`, which registers the
instance (`EngineeringObjectFactory.cs:46`) before
`await instance.PersistInitialStateAsync(…)` (`:63`), and is opened as
**`TD-147`, Open, Release Blocking**. **This heading stays `Not Ready`, the
blocker count stays one, and the verdict in §6 stays `NOT READY`: the
blocker MOVED, it did not disappear.** Read as progress toward acceptance,
this change would be read wrongly. Third time this row has changed identity
without changing the count — `TD-140` → `TD-143` → `TD-147` — and each time
because a closure of the narrower thing was refused permission to read as a
closure of the wider one.)*

**One data-loss path was found and closed during this review, and it is
the most important fact in this report.** `EngineeringObjectBase.
PersistStateAsync` captured live state and then issued an unconditional
whole-record overwrite with nothing serialising the pair. Two concurrent
mutations on one object could land their saves in the opposite order to
their captures; the later-landing, earlier-captured snapshot silently
dropped the other's change from disk. Once that dropped an attachment
reference, the reconciliation sweep deleted its bytes as a genuine
orphan — while the caller who received a successful `IAttachment` still
believed the file existed.

Reproduced against the real classes on trial 323 of 500, with no
artificial delays. Closed by `WP 16.4B-R3` with an `AsyncKeyedLock` keyed
by object Id, held from before `CaptureState()` through `SaveAsync`, after
an exhaustive re-entrancy audit found no nested path. The lock is keyed
by Id rather than held per instance because `ReviseAsync` demonstrably
constructs a second live instance for the same Id — an instance lock would
have closed nothing. `TD-135`, Resolved.

The lineage matters for judging this release's process: `WP 16.4B` shipped
an attachment-sweep race → board 1 caught it → `WP 16.4B-R2`'s first fix
was **rejected** for narrowing rather than closing it → the corrected fix
landed → board 2 then found the marker's proof rested on a false
single-writer assumption → `WP 16.4B-R3` closed the real defect. Four
passes. Each found what the previous missed.

*Observations:* `PersistenceStore`'s atomic writes are crash-safe but not
power-loss durable (no `fsync`) — `TD-137`; recorded, not fixed, because
changing the most load-bearing write path in a hygiene release is the
wrong trade.

**What makes this category Not Ready, added `WP 16.4B-R6` round 2,
2026-09-06.** The lineage above stopped at board 2. It continued: board 3
found a second lost update (`TD-136`), board 4 found a stranded marker
(`TD-139`), board 5 found that `TD-139`'s fix **deleted attachment bytes a
live successor durably referenced**, and a sixth independent board found
that an operation the platform reports as *failed* still becomes durable
on five of the seven mutators. That was `TD-140`, and it is **now closed**
for the route it named: `MutateAndPersistAsync`
(`EngineeringObjectBase.cs:462`–`:479` at `261ba34`) refuses before
mutating, the five characterisation facts in
`tests/Tempest.Core.Tests/EngineeringDomain/RefusedMutationSuccessorLeakageTests.cs`
were **inverted rather than deleted**, and Agent C — who did not write the
fix — verified it adversarially.

**The category is Not Ready anyway, and this is the important part.** The
invariant that was closed covers `SupersededEngineeringObjectException` and
nothing else. A mutation whose durable write simply **fails** still keeps
its in-memory mutation, on all seven mutators, and the object's next
successful write of any kind carries it to disk. That is `TD-143`, it is
**Open**, and it is Release Blocking on `ADR-0106`'s own trigger — a
Technical Debt item with real, demonstrated evidence of harm. The evidence
is in this repository and runs:
`tests/Tempest.Core.Tests/EngineeringDomain/MutatorRefusalAdversarialTests.cs`
§3, three deterministic characterisation facts. A `DeleteAsync` the caller was told had failed leaves the object
durably soft-deleted, with no undelete anywhere in the platform and
twenty-one `Tempest.App` sites filtering deleted objects out; a
`TransitionAsync` the caller was told was rejected writes a
`LifecycleTransitionRecord` with a real actor id and timestamp into the
append-only transition history. `WP 16.4B-R6`'s first pass closed this
pattern for two of the seven mutators and, by lengthening the longest lock
hold, widened the window for the remaining five. **Its round-2 pass closed
all seven**, and this document records that at `:147` and in §3.2 row 8.
What remains open is narrower and is why the grade still stands: the
refusal invariant covers `SupersededEngineeringObjectException` only, so a
*failing durable write* still leaves the fabricated audit entry and the
unrecoverable soft delete. That is `TD-143`, and
`TD-143`'s own row states what closing it would require — a decision about
failure semantics, not the three-line reordering that closed `TD-140`.

> **SUPERSEDED IN FACT AT `d6af7ec` — the paragraph above is retained
> unedited as the record of what was true at `261ba34`, and every
> present-tense claim in it is now false of the failing-durable-write route
> on `EngineeringObjectBase`.** *(Written by the `WP 16.4B-R7` governance
> closure, 2026-09-07, from the tree at `d6af7ec` rather than from any
> agent's report.)*
>
> - **The seven mutators no longer keep their mutation when the durable
>   write fails.** `WP 16.4B-R7` added `MutationRollbackPoint`,
>   `CaptureRollbackPoint()`, `RollBackTo(point)`,
>   `RollBackOnFailureAsync(store, operation)` and
>   `DurableRecordAlreadyShowsThisStateAsync(store)` to
>   `EngineeringObjectBase.cs` and wired them at four sites inside the
>   existing hold of the object write lock —
>   `MutateAndPersistAsync` (covering `TransitionAsync`, `RenameAsync`,
>   `DeleteAsync`, `SetBomLineAsync`) plus inline calls in `AttachAsync`,
>   `MoveAsync` and `AttachContentAsync`. The undo is conditional on
>   evidence: it runs only when a re-read of the durable record does **not**
>   already show the mutation.
> - **The three facts this paragraph cites by name no longer exist under
>   those names, and the citation above is a dangling reference.** They were
>   **inverted, not deleted**, and each records the inversion in its own
>   remarks (`INVERTED by WP 16.4B-R7 (TD-143)`):
>   `ATransitionWhoseDurableWriteFails_StillStampsTheAuditEntry` →
>   `ATransitionWhoseDurableWriteFails_StampsNoAuditEntry_AndTheNextWriteMakesNothingDurable`;
>   `ADeleteWhoseDurableWriteFails_StillSoftDeletesTheInstance_AndTheNextWriteMakesItDurable`
>   →
>   `ADeleteWhoseDurableWriteFails_LeavesTheInstanceUndeleted_AndTheNextWriteMakesNothingDurable`;
>   `TheOtherFourMutators_AlsoKeepTheirMutation_WhenTheDurableWriteFails` →
>   `TheOtherFourMutators_AlsoUndoTheirMutation_WhenTheDurableWriteFails_AndDeleteNothingDurable`.
> - **What is deliberately *not* undone, and is not a regression.**
>   `AttachContentAsync`'s content bytes and its write-intent marker are
>   left exactly where they were — deleting them is the `WP 16.4B-R5`
>   compensation the fifth review board proved destroys content a live
>   successor references — and a `MoveAsync` whose *state* write fails after
>   its `groupedUnder` link write committed restores `ParentId` but leaves
>   the durable link edge.
> - **This note changes no verdict, no grade and no blocker.** The
>   recommended verdict stays **NOT READY**, §6 is unchanged, and this
>   category stays **Not Ready**. `TD-143`'s register row still reads
>   *"Open — reproduced, not fixed"* at `d6af7ec`, and **its disposition,
>   its scope, and the identity of the open Release Blocking finding are the
>   Technical Debt Register pass's act and the chair's, not this document's.**
>   They are flagged here so that the two records can be reconciled in one
>   pass rather than drifting apart again, which is `D-R2`.
>   *(**Superseded by the register pass, seam reconciliation 2026-09-07.**
>   The two sentences above are retained unedited as what was true when this
>   note was written, earlier the same day and before the register pass had
>   run. They are now false in one respect and complete in another:
>   `TD-143`'s row no longer reads *"Open — reproduced, not fixed"* — that
>   text is retained inside the row itself as its own superseded wording —
>   and the identity of the open Release Blocking finding **has** now been
>   settled. `TD-143` is **Resolved for the seven mutators on
>   `EngineeringObjectBase` and for nothing else**, with the non-closure of
>   the harm class stated in its own first sentence; the blocker is
>   **`TD-147`**, Open, Release Blocking, opened in the same pass so that
>   the closure could not read as the harm class going away. **The verdict
>   is still `NOT READY` and the blocker count is still one.**)*
> - **Measured here, independently, at `d6af7ec`:**
>   `dotnet build src/TempestOS.slnx -c Debug -p:TreatWarningsAsErrors=true`
>   → 0 Warning(s), 0 Error(s); `Tempest.Core.Tests` **3,330/3,330**;
>   `Tempest.Desktop.Tests` **474/474**. Neither figure is a determinism
>   matrix and neither is offered as one — see §4.4 and §7 item 6.

### Verification readiness — **Pass, with observations**

The five-run CI determinism matrix required by `WP 16.4A`'s own acceptance
line has been obtained on each successive candidate as each superseded the
last: §4.4 (`888ef63`, five for five, the most recent), §4.3 (`58c4cba`,
five for five, superseded), §4.1 (`d7d3f3b`, superseded), §4.2
(`f593e5c`, superseded, and run 214's genuine failure before it).
*(Corrected `WP 16.4B-R6`: this sentence pointed at §4.1 alone. Corrected
again `WP 16.4B-R7` governance closure, 2026-09-07: it still named §4.3 as
the most recent matrix after §4.4 had been written into this same
document.)*

**No matrix exists for the candidate under review.** *(Corrected
`WP 16.4B-R6` round 2, 2026-09-06: this passage called `58c4cba` "the
current candidate". It is not, and has not been since `WP 16.4B-R6`
changed `src/` and `tests/`.)* The RC4 matrix in §4.3 covers `58c4cba` and
no later tree, and the fourth matrix in §4.4 covers `888ef63` and no later
tree, on exactly the rule that retired the two matrices before them.
*(Corrected `WP 16.4B-R7` governance closure, 2026-09-07: this passage read
"A fourth matrix against whatever commit becomes the next candidate is
required and **has not run**". **The fourth matrix has run** — 5/5 on
`888ef63`, runs 253–257, §4.4, recorded by `0935c64`. It is a **fifth**
matrix that is now outstanding, because `WP 16.4B-R7` changed three files
under `src/` and five under `tests/` between `888ef63` and `d6af7ec`. See
§7 item 6, corrected in the same pass.)* **`d6af7ec` is not
matrix-certified**, and nothing in §4.1–§4.4 may be read as covering it.
The only regression evidence for
`261ba34` is a local run, not CI, re-measured on that commit for this
revision rather than carried forward: Debug and Release builds 0 warnings /
0 errors with `-p:TreatWarningsAsErrors=true`, `Tempest.Core.Tests`
**3,286/3,286** and `Tempest.Desktop.Tests` **474/474** in both
configurations, and `governance-healthcheck.ps1` 14 passed / 2 warned /
0 failed. That is a local regression on one machine and is **not** the
five-run matrix, is not offered as one, and does not discharge §7 item 6.
*(Figures re-derived `WP 16.4B-R6` round 2 at the integrated SHA,
2026-09-06. They read 3,267/3,267 at `3dd74d8`; the round-2 code stream
and the independent adversarial verification added facts between the two
commits. The SHA was relabelled and the numbers re-measured together —
relabelling one without re-running the other is the defect Agent C's
`C-R1` names.)*
*(Re-measured, not relabelled, `WP 16.4B-R7` governance closure,
2026-09-07: the figures above stay scoped to `261ba34` as history. **At
`d6af7ec` the equivalent local run is Debug build 0 warnings / 0 errors
with `-p:TreatWarningsAsErrors=true`, `Tempest.Core.Tests` 3,330/3,330 and
`Tempest.Desktop.Tests` 474/474**, executed in a `git worktree` at that
commit for this pass. It is a local run on one machine, it is **not** the
five-run matrix, it is not offered as one, and it does not discharge §7
item 6.)*

Board 2 proved, by mutation, that four tests guarding this release's
highest-risk code **could not fail**: two sweep race tests whose gates
paused on a collection name rather than call order (reverting the
production fix moved the watched collection to the first call, so the
interleaving never occurred), a double-dispose test with a measured 0.45%
detection rate, and a golden corpus whose "every field is compared" claim
omitted `Metadata`. All four are fixed and each fix is hand-mutation-checked
(not tool-produced) in both directions — demonstrated failing against reverted code and passing
against restored code. `WP 16.4A-R1`.

*Observations:* the focus-ring contrast test cannot see any
`Flat`-treatment state (`TD-131`); the attachment sweep's own gates are
not yet timeout-bounded, disclosed rather than silently skipped.

### Governance readiness — **Pass, with observations**

`scripts/governance-healthcheck.ps1`: **14 passed, 2 warned, 0 failed**
of 16. Both warnings are informational and pre-existing.

Board 2's governance audit searched every citation of `D-021`–`D-026`
across the tree and found **no document claiming, implying or relying on
approval that has not been given**. The Decision Register genuinely stops
at `D-020`.

It also found four register/status falsehoods, all now corrected: the DI
and Platform Services registers each claimed complete coverage while
omitting `IAttachmentWriteIntentStore`; `WP 16.4A` was marked flatly
Complete despite its own acceptance being unmet; `WP 16.2C` was marked
Complete having left six Work Packages without retrospectives; and
`PROJECT_STATUS.md`'s headline metrics were stale by material margins.

**Register drift recurred three times inside this release**, each time in
one of the three registers no machine check derives. That is `TD-123`, and
it is the most important governance lesson here: the checks that were
extended caught things, and the registers left un-derived drifted again
within days.

*Observations:* `TD-123`, `TD-124`, `TD-125` remain open and are the
mechanism by which this class of drift will recur.

**It recurred again, in the registers this release itself edited.** *(Added
`WP 16.4B-R6` round 2, 2026-09-06.)* A sixth independent board found
`TD-136` crediting a Work Package whose fix board 5 falsified, and citing
that fix's evidence; and `TD-139` describing, in the present tense, a
compensation `WP 16.4B-R6` had already deleted — presenting the release's
own data-loss regression as that defect's closure. Both rows are
re-derived directly from the code at `3dd74d8`, with the superseded text
kept inside each cell — and **re-derived a second time at `261ba34`**,
because the integration merge moved the code they describe and the
SHA-update list written for exactly that purpose was not executed when the
branches were merged. Agent C failed the candidate on that (`C-R1`): a
register row describing code that does not exist is `D-R2` recurring, one
cycle later, in the rows just corrected for it. The same board found this report's own §3 and §6
asserting that no Release Blocking finding remained, a day after board 5
raised three REDs against the commit this report names. That is the
finding recorded above as `TD-123`'s class, occurring in the two documents
that carry the release's own recommendation.

### Release readiness — **Pass, with observations**

`VERSION` is `0.16.0`. Release Notes are written, including the platform
matrix and the verified one-way downgrade door. All 18 Work Package rows
carry truthful status. The release path was itself hardened this release
(`WP 16.1A-R1`): `release.yml` now runs the governance check, and
`new-release.ps1` refuses to tag a commit whose CI did not conclude
`success` — a check that previously warned and continued, and which board
1 caught waving through a run that completed as `cancelled` rather than
`failure`.

*Observations, and these are Product Owner preconditions, not engineering
gaps:* §7.3's pre-tag Build/Test Gate run on `main` has not occurred;
GitHub branch protection remains unconfigured (`TD-45`), so the gate
exists in the workflow but nothing on GitHub enforces it. *(Corrected
`WP 16.4B-R6`, 2026-09-06: this sentence also said "`main` is untouched"
and that "`D-021`–`D-026` are unratified". Both were true when written
and are now false — the merge happened 2026-09-05, and all six decisions
were ratified by the Product Owner on 2026-09-05, as §7 item 1 of this
same document already records. The §7.3 pre-tag run genuinely remains
outstanding and is not affected by the merge having occurred.)*

## 3. Findings by `ADR-0106`'s three-kind taxonomy

> **REVISED `WP 16.4B-R6` round 2, 2026-09-06 — openly, upward, and with
> the whole of the superseded text kept below.** The paragraph in §3.1
> states the principle this revision obeys: *a readiness review that
> quietly revises its own blocker count is worth nothing*. It was written
> when the count went from two to three. The count has since gone up
> again, and this section had not moved: it still read "Release Blocking —
> none remain" a day after the fifth review board raised three
> chair-confirmed REDs — one of them a permanent data-loss regression —
> against `58c4cba`, the very commit this report's own header names. A
> sixth independent board raised four more, and found this section's
> stated "13" short of its own rows. §3.1 is the old text, unedited.
> §3.2–§3.4 are the current classification.

### 3.1 The superseded text, retained verbatim

> **Release Blocking — none remain.** Three existed and all three were
> closed during this review: the lost-update data-loss path (`TD-135`), the
> tests that could not fail, and — found by the third and final board, after
> this report first claimed the count was two — the *second* lost update, on
> `ReviseAsync`, the one durable-write path `TD-135`'s own fix never routed
> through its lock (`TD-136`, `WP 16.4B-R4`). All three had real,
> demonstrated evidence of harm: reproduced against the real classes, not
> argued. The count in this paragraph read "two" until board 3 ran, and the
> correction is left visible rather than smoothed over, because a readiness
> review that quietly revises its own blocker count is worth nothing.
>
> **Disclosed, Non-Blocking — 13.** `TD-45` (branch protection),
> `TD-123`–`TD-134`. (`TD-136` is not among them: it was raised and closed
> inside this review, like `TD-135` before it.) Each is real, tracked, newly
> raised or newly relevant, and named rather than absorbed.
>
> **Pre-Existing, Unaffected — the standing Open debt** this release neither
> caused, worsened, nor re-raised, disclosed for completeness in the
> Technical Debt Register's 70 Open rows.

Three things in that text are false as it stands, and are corrected below
rather than edited out of it: "none remain"; the count "three"; and "13",
which omits `TD-137` although `TD-137`'s own register row cites this
report as one of the three places it is disclosed. Separately, the closing
figure of "70 Open rows" was **correct when it was written** — the
register held 135 rows and 70 Open at `cc7ef4d` — and had drifted by one
without being noticed: 139 rows and **71** Open at `58c4cba`, the commit
this report names.

### 3.2 Release Blocking — the full sequence, and what is open now

Every finding this review has classified Release Blocking, or which a
board graded RED or blocking against a named candidate, with its current
disposition. Nothing here is graded closed because code changed; each
closure names the evidence, and the two closures re-derived this round
name the commit they were read at.

| # | Finding | Raised by | Disposition, and the evidence for it |
|---|---|---|---|
| 1 | The lost-update data-loss path — `PersistStateAsync` captured then wrote with nothing serialising the pair (`TD-135`) | Independent post-remediation review, 2026-09-05 | **Closed**, `WP 16.4B-R3`. Reproduced against the real classes before the fix (trial 323 of 500) |
| 2 | Four tests guarding the highest-risk code could not fail | Board 2 | **Closed**, `WP 16.4A-R1`, each fix hand-mutation-checked (not tool-produced) in both directions |
| 3 | The second lost update, on `ReviseAsync` (`TD-136`) | Board 3 | **Closed — by `WP 16.4B-R6`, not by `WP 16.4B-R4`.** `WP 16.4B-R4` was necessary and insufficient: board 5's `F2` showed `_supersededBy` was assigned unconditionally, so one predecessor could mint two live successors and the second silently overwrote the first's accepted writes. `ThrowIfSuperseded()` inside `ReviseAsync`'s lock closes it; removing it kills 5 facts. Re-derived at `261ba34`; see `TD-136`'s row |
| 4 | `AttachContentAsync` stranded its write-intent marker — a permanent leak reachable without a crash (`TD-139`) | Board 4 (`A16`, AMBER-blocking) | **Closed — by `WP 16.4B-R6`, not by `WP 16.4B-R5`.** The lock is taken before the first durable step and the refusal happens before the marker exists. `WP 16.4B-R5`'s compensation was **deleted**, not credited. Re-derived at `261ba34`; see `TD-139`'s row |
| 5 | **`WP 16.4B-R5`'s compensation permanently destroyed attachment content a live successor durably referenced** — a data-loss regression introduced by the fix for finding 4 (board 5 `F1`) | Board 5, chair-confirmed, two independent executable proofs | **Closed**, `WP 16.4B-R6`, by removing the window rather than compensating for it. Confirmed line by line by the sixth board at `3dd74d8`, and re-read at `261ba34` for this revision — the ordering is unchanged and `grep -c "catch (SupersededEngineeringObjectException)"` still returns 0 |
| 6 | `ReviseAsync` minted a second live successor and never retired the first (board 5 `F2`) | Board 5, chair-confirmed | **Closed**, `WP 16.4B-R6` — same guard as finding 3. Mutant kills 5 facts |
| 7 | `ReviseAsync` silently dropped every type-specific field; the comment asserting it could not was false (board 5 `F3`) | Board 5, chair-confirmed | **Closed**, `WP 16.4B-R6` — the self-factory takes the captured state and both attachment points supply the Kind's own rehydrator. Mutant kills 10 facts across four test classes and a Samples integration test |
| 8 | **A mutation REFUSED with `SupersededEngineeringObjectException` still became durable, on five of the seven mutators** (`TD-140`; board 5 `F4`, re-graded by the sixth board) | Board 5 (AMBER); re-graded Release Blocking by the sixth independent board, 2026-09-06 | **Closed**, `WP 16.4B-R6` round 2, **for that route only**, against the three closure conditions `TD-140` had stated in advance: `MutateAndPersistAsync` refuses before mutating (`EngineeringObjectBase.cs:462`–`:479` at `261ba34`); the five characterisation facts in `RefusedMutationSuccessorLeakageTests` were **inverted, not deleted**; and Agent C, who did not write the fix, verified it with guard-rail facts each naming the mutant that kills it. **This closure does not reach finding 9** |
| 9 | **The same harm by another route: the refusal invariant covers `SupersededEngineeringObjectException` and nothing else, so a mutation whose durable write FAILS still keeps the fabricated audit entry and the unrecoverable soft delete, on all seven mutators** (`TD-143`) | Agent C's independent adversarial verification, 2026-09-06 (`C-F2`) | **OPEN.** Executable, deterministic evidence in the repository: `MutatorRefusalAdversarialTests` §3 — the failed transition that still stamps the audit entry, `ADeleteWhoseDurableWriteFails_StillSoftDeletesTheInstance_AndTheNextWriteMakesItDurable`, and `TheOtherFourMutators_AlsoKeepTheirMutation_WhenTheDurableWriteFails`. The product consequences are the ones that made finding 8 Release Blocking, and they are properties of the fields rather than of the exception that interrupted the write. **Not fixed, and no remediation attempted at `261ba34`.** *(Superseded in fact `WP 16.4B-R7` governance closure, 2026-09-07, and left otherwise unedited. **At `d6af7ec` a remediation has been attempted and has landed for this route on `EngineeringObjectBase`**: `RollBackOnFailureAsync` undoes the in-memory mutation, on evidence, at all four wiring sites covering all seven mutators. The three facts named in this cell no longer exist under those names — they were **inverted, not deleted**, and each says so in its own remarks; the current names are in the §2 Implementation-readiness supersession note. **This cell's classification is not changed here.** Whether the finding is Resolved, Resolved-and-scoped, or replaced by a successor row is the Technical Debt Register pass's act and the chair's; it is flagged, not decided, by this pass.)* |

**Release Blocking, still open: one — and it is now `TD-143`, not
`TD-140`.** *(Re-derived `WP 16.4B-R6` round 2 against the integrated tree
`261ba34`.)* The count did not change and neither did the verdict: the
demonstrated harm — a fabricated entry in the append-only audit trail, and
a soft delete with no undelete and twenty-one read models filtering the
object out — is identical, and only the route to it moved. Recording
finding 8 as closed while finding 9 stands is the whole point of splitting
them: a closure of the narrower claim must not be readable as a closure of
the wider one. That is what makes Implementation readiness **Not Ready** in
§2 and drives the derivation in §6.

Also open, recorded in the register this round and **deliberately not
graded here**, because grading is the next board's and this report will not
inflate its own blocker count any more than it will deflate it: `TD-141`
(two durable relationship-write paths with no supersession guard at all —
now characterised executably, where the previous revision of this report
correctly said no reproduction existed), `TD-142` (the same
refuse-after-mutate shape on twelve type-specific mutators of the concrete
Kinds), `TD-144` (a move whose durable link write fails still reparents),
`TD-145` (two concurrent moves form a parent cycle over which five
`Tempest.App` ancestry walks never terminate) and `TD-146` (a delete can
commit while a concurrent move gives it a live child).

> **Currency of this subsection at `d6af7ec`, `WP 16.4B-R7` governance
> closure, 2026-09-07. Nothing above is edited; this states what has and
> has not been re-derived.**
>
> - **The blocker count and the blocker's identity have NOT been re-derived
>   at `d6af7ec`.** The parenthesis above says "Re-derived … against the
>   integrated tree `261ba34`", and that is where the derivation stopped.
>   `TD-143`'s register row still reads *"Open — reproduced, not fixed"* at
>   `d6af7ec` while the code at that commit falsifies the row's
>   present-tense mechanism for the seven mutators on
>   `EngineeringObjectBase` — the two records disagree, which is the `D-R2`
>   shape. **Reconciling them is the Technical Debt Register pass's act and
>   the chair's**; this pass flags it rather than deciding it, so that the
>   register and this report move in one step instead of drifting apart.
>   *(**Now re-derived — seam reconciliation, 2026-09-07.** The bullet above
>   is retained as written. The register pass has run: the sentence "Release
>   Blocking, still open: one — and it is now `TD-143`, not `TD-140`" above
>   is **superseded**. At `d6af7ec` it reads: **Release Blocking, still
>   open: one — and it is now `TD-147`, not `TD-143`.** `TD-143` is Resolved
>   for the seven mutators on `EngineeringObjectBase` and for nothing else;
>   `TD-147` carries the identical harm on an eighth path,
>   `EngineeringObjectFactory<T>.CreateAsync`, which registers the instance
>   before its initial durable state write, so a create the caller was told
>   had failed leaves a registered object whose next successful write makes
>   it real. **The count did not change and neither did the verdict** — this
>   is the same sentence the paragraph above makes about `TD-140` → `TD-143`,
>   for the same reason, one round later. Finding 9's own row-9 wording in
>   the table above is likewise superseded for the seven mutators and is
>   retained unedited.)*
> - **`TD-144` is half-falsified at `d6af7ec` and its wording above is
>   stale.** A move whose durable `groupedUnder` link write *fails* no
>   longer reparents the instance — the undo covers it, and
>   `AMoveWhoseDurableLinkWriteFails_LeavesTheParentWhereItWas_AndTheNextWriteCarriesNothing`
>   asserts so. What remains is narrower and is the *durable* half: a move
>   whose link write **succeeded** and whose state write then failed
>   restores `ParentId` but leaves the `groupedUnder` edge on disk, pinned
>   by `AMoveWhoseStateWriteFailsAfterItsLinkWrite_RestoresTheParent_ButLeavesTheGroupedUnderLink`.
>   The row's re-wording is the register pass's act.
> - **`TD-141`, `TD-142`, `TD-145` and `TD-146` were not re-derived by this
>   pass** and are stated here as unverified at `d6af7ec` rather than
>   carried forward as verified.
> - **`WP 16.4B-R7` appears in no document in `docs/` at `d6af7ec`**
>   (`grep -rc 'WP 16.4B-R7' docs/ PROJECT_STATUS.md` → 0 before this pass),
>   and neither the round nor any finding raised inside it has a register
>   row. That is the omission this closure exists to correct, and the rows
>   themselves are the register pass's.

### 3.3 Disclosed, Non-Blocking — 14

`TD-45` (branch protection), `TD-123`–`TD-134`, and **`TD-137`**
(`PersistenceStore`'s writes are crash-safe but not power-loss durable).
1 + 12 + 1 = 14. *(Corrected `WP 16.4B-R6` round 2: this read "13" and
omitted `TD-137`, whose own register row states that it is disclosed in
`Release Notes.md`, this report §2 and the Review Board Disposition §5.5 —
a claim this report did not make true until now. §2's Implementation
readiness observation now names it.)*

`TD-135`, `TD-136`, `TD-138` and `TD-139` are not among them: each was
raised and closed inside this review, as is `TD-140`. `TD-143` is not among
them either — it is Release Blocking, §3.2. `TD-141`, `TD-142`, `TD-144`,
`TD-145` and `TD-146` are unclassified here, §3.2.

> **Re-derived at `d6af7ec`, `WP 16.4B-R7` governance closure, 2026-09-07.
> The paragraph above is retained unedited; it was true at `261ba34` and two
> of its statements are now false.**
>
> - **`TD-143` is no longer Release Blocking and is no longer Open.** It is
>   **Resolved for the seven mutators on `EngineeringObjectBase` and for
>   nothing else**, and its own row says in its first sentence that it does
>   not close the harm class. It is therefore not among the Disclosed,
>   Non-Blocking findings for a different reason than the one given above:
>   not because it is Release Blocking, but because it is closed for what it
>   closes and its residue is carried by other rows.
> - **`TD-147` is the Release Blocking finding**, §3.2's re-derivation note
>   and §6.2 step 1.
> - **Five rows exist that this section does not classify at all**, because
>   they were opened after it was written: `TD-147` (Release Blocking),
>   `TD-148`, `TD-149`, `TD-150` and `TD-151` — all Open, none of them
>   Disclosed, Non-Blocking, and none of them graded by this report.
>   `TD-151` is explicitly **not** release blocking; the other three are
>   ungraded here for the same reason `TD-141`, `TD-142`, `TD-144`, `TD-145`
>   and `TD-146` are — **grading is the next board's, and this report will
>   not inflate its own blocker count any more than it will deflate it.**
> - **The count of Disclosed, Non-Blocking findings is unchanged at 14** and
>   the verdict is unchanged at `NOT READY`. Nothing in this note moves a
>   grade; it records which rows exist and which sentence above has been
>   overtaken.

### 3.4 Pre-Existing, Unaffected

The standing Open debt this release neither caused, worsened, nor
re-raised, disclosed for completeness in the Technical Debt Register's
**77 Open rows** of 146. *(Corrected `WP 16.4B-R6` round 2: this read "70
Open rows", which was true at `cc7ef4d` (135 rows / 70 Open) and had gone
stale by one at `58c4cba` (139 / 71) without anyone re-deriving it. The
register held 141 / 73 Open at `3dd74d8` and holds **146 / 77 Open** at
`261ba34`, after `TD-140`–`TD-146` were added across this round and
`TD-140` moved to Resolved; its own summary line and
`governance-healthcheck.ps1` Check 12 both agree at 146 tracked / 77 Open.
Re-derived at each step, not carried forward — this is the `TD-123` drift
class, in the same paragraph that names it.)*

*(**Re-derived again, `WP 16.4B-R7` governance closure, 2026-09-07 — and the
`TD-123` drift class this paragraph names had caught it again in the two days
since.** The figures above are correct for `261ba34` and are retained. At
`d6af7ec` the register holds **151 rows / 80 Open**, re-derived by a direct
row-by-row count and not by arithmetic on `146`: `TD-147`–`TD-151` were added
by this pass, `TD-143` moved Open → Resolved and `TD-144` moved Open →
Partially resolved, so the Open figure is 77 − 2 + 5 = 80 and the total is
146 + 5 = 151. The register's own summary line and
`governance-healthcheck.ps1` Check 12 agree at **151 tracked / 80 Open**
("151 stated, 151 rows present"). `TD-01`–`TD-151` contiguous, no gaps, no
duplicates.)*

## 4. Validation evidence

Measured on `58c4cba` (`release/v0.16.0-rc4`) unless stated. **`58c4cba`
is not the candidate under review** — see the header. Every row below is
evidence for the commit it names and for no later tree; the candidate
under review, `d6af7ec`, has the local-only regression figures recorded in
§2's Verification readiness paragraph and **no CI matrix at all**.
*(Corrected `WP 16.4B-R7` governance closure, 2026-09-07: this named
`261ba34` as the candidate — two candidates out of date, because the ERR
header had already moved to `888ef63` and then `WP 16.4B-R7` moved past
that. `261ba34`'s own figures stay recorded, scoped to `261ba34`, in §2.)*
*(Scoping clarified `WP 16.4B-R6` round 2, 2026-09-06; this line read
"Measured on the release candidate `58c4cba`", which was true when written
and became false when `WP 16.4B-R6` changed `src/` and `tests/`.)* *(Corrected `WP 16.4B-R6`, 2026-09-06: this line read
"Measured on the frozen candidate `d7d3f3b` unless stated", governing the
whole table below, two candidates after `d7d3f3b` was superseded. The
test counts and the determinism row below are corrected with it.)*

| Check | Result |
|---|---|
| `dotnet build src/TempestOS.slnx -c Release -p:TreatWarningsAsErrors=true` | 0 warnings, 0 errors |
| Same, `-c Debug` | 0 warnings, 0 errors |
| `Tempest.Core.Tests` (Release) | **3,214 / 3,214** (3,204 + `WP 16.4B-R4`'s five + `WP 16.4B-R5`'s five) |
| `Tempest.Desktop.Tests` (Release) | **474 / 474** |
| `governance-healthcheck.ps1` | 14 passed, 2 warned, **0 failed** (of 16) |
| `dotnet list package --vulnerable --include-transitive` | clean, all projects |
| Secrets scan, working tree **and** full history | nothing found |
| Local determinism (board 2) | 16/16 runs clean — Core ×5 Release, ×5 Debug; Desktop ×3 Release, ×3 Debug |
| **CI determinism matrix** | **5 / 5** on `888ef63` (runs 253–257) — the most recent matrix, **§4.4**, and **`888ef63` is not the candidate under review either**. Superseded predecessors retained: 5/5 on `58c4cba` (§4.3), 5/5 on `d7d3f3b` (§4.1), and, after run 214's genuine failure on `fce2166`, 5/5 on `f593e5c` (§4.2). **No matrix covers `d6af7ec`.** *(Corrected `WP 16.4B-R7` governance closure, 2026-09-07: this row named the `58c4cba` RC4 matrix and §4.3 alone, and did not mention §4.4 at all, although §4.4 sits in this same document.)* |

> **A note on the warning gate, because it caught a real defect twice.**
> `Directory.Build.props` sets `TreatWarningsAsErrors` to `false`; CI
> applies it only at its own build step. A plain local `dotnet build`
> therefore does **not** reproduce the gate. Every figure above passes the
> flag explicitly.

### 4.1 CI evidence — the five-run determinism matrix

`WP 16.4A`'s acceptance line requires five consecutive clean CI runs, both
configurations, on one immutable commit. Local repetition does not satisfy
it and was not substituted for it. All five ran on GitHub-hosted runners
against SHA `d7d3f3b093061f177a37cd2bf17a06cb8e55dd94`, on branch
`release/v0.16.0-rc`, which exists solely to hold the candidate still.
The workflow's `concurrency` group is `ci-<workflow>-<ref>` with
`cancel-in-progress: true`: runs sharing a ref cancel each other, runs on
different refs do not. Had the matrix been dispatched on
`feature/v0.16.0`, every `WP 16.9.0` documentation commit pushed while it
ran would have killed the run in flight — which is not hypothetical, it is
what runs 191, 192 and 193 each did to their predecessor. Giving the
candidate a ref of its own is what made five *consecutive* runs obtainable
rather than merely five attempted.

| # | Run | ID | Started (UTC) | Duration | Conclusion |
|---|---|---|---|---|---|
| 1 | 195 | `33958003532` | 09:26:48 | 10m 55s | **success** |
| 2 | 197 | `33958632949` | 09:40:43 | 8m 44s | **success** |
| 3 | 199 | `33959162460` | 09:52:54 | 9m 18s | **success** |
| 4 | 201 | `33959683856` | 10:04:48 | 9m 47s | **success** |
| 5 | 202 | `33960215260` | 10:16:41 | 7m 59s | **success** |

Each run is five jobs, and in all five runs every one of the five
concluded `success` — no cancellation, no retry, no re-run of a failed
job, and no `continue-on-error` masking a failure:

| Job | Runner | Runs green |
|---|---|---|
| `Build & Test (Release)` | `windows-2022` | 5 / 5 |
| `Build & Test (Debug)` | `windows-2022` | 5 / 5 |
| `Governance Health Check` | `windows-2022` | 5 / 5 |
| `CI Gate` (needs both configurations + governance) | `windows-2022` | 5 / 5 |
| `Linux Launch Smoke` (advisory) | `ubuntu-latest` | 5 / 5 |

Two disclosures, so the record is not read as tidier than it is.

**The run numbers are not contiguous** (195, 197, 199, 201, 202), and it
is worth saying exactly what is in the gaps rather than waving at them.
`run_number` is per-workflow, so 196, 198 and 200 are `ci.yml` runs too —
each the automatic `push` run for one of the `WP 16.9.0` documentation
commits landing on `feature/v0.16.0` while the matrix was running on its
own frozen ref. That the two sequences interleave without interfering is
the separate-ref decision working as intended. All three concluded
`success`, and one of them matters to the question §4.1 closes on:
**run 200 (`33959195477`) is `c6a16a5`**, which was the branch tip when
this section was written, and it passed a full CI run of its own. The
applicability argument below does not rest on that — it rests on the
diff — but that tip had independently been through CI, not merely argued
to be equivalent. *(Corrected `WP 16.4B-R6`, 2026-09-06: this sentence
called `c6a16a5` "the same commit this evidence is being read against".
It no longer is, and has not been since `fce2166`. The candidate is
`58c4cba`, whose own determinism evidence is §4.3; this paragraph is
retained as the record of why the `d7d3f3b` matrix was accepted as
covering `c6a16a5` at the time.)*

**Two runs at `d7d3f3b` were cancelled, and neither is counted here.**
Run 193 was the `push` run on `feature/v0.16.0`; run 194 was the `push`
run created seconds later when the same commit was pushed to
`release/v0.16.0-rc`. Both were cancelled by the `concurrency` rule as
designed — 194 three seconds after it started, by the dispatch of run 195.
They are excluded because neither executed a test, not because either
failed: a cancelled run is evidence of nothing, and is counted here as
neither a pass nor a failure. Every run in the matrix table is a
`workflow_dispatch`, every one is after both cancellations, and no run in
the table was re-run or retried.

**This evidence was invalidated, and re-obtained rather than argued
around.** The matrix above ran on `d7d3f3b`. For as long as every later
commit touched only `docs/`, it covered the tree being released, and this
section said so with a mechanical check behind it.

That stopped being true. The final release review (board 3, §5 of the
Review Board Disposition) found a data-loss defect and two further
findings whose remediation changes `src/`, `tests/`, `scripts/` and
`.github/`. The determinism acceptance is an acceptance on *a commit*, and
the commit changed. Nothing about a fix to `ReviseAsync` is likely to
perturb test determinism — and that reasoning is exactly what a
determinism gate exists to refuse. `WP 16.4A`'s own history is the
argument: an independent reviewer refused 16/16 clean local runs as
insufficient, citing `WP 13.12.9`, where a flake reproduced zero times in
25 local attempts and still failed a real release run.

So the matrix was re-run in full on the remediated candidate. Both results
are recorded below rather than the first being deleted, because the
superseded run is what makes the re-run's necessity legible.

The re-run's evidence is §4.2. It is not a tidier version of §4.1 — it
records a genuine failure before the five passes, because that failure is
the reason to trust the passes.

### 4.2 CI evidence — the re-run, on the remediated candidate

The first attempt at the re-run **failed**, and the failure is kept here
rather than quietly replaced by the successful matrix that followed.

| Run | ID | SHA | Conclusion | What happened |
|---|---|---|---|---|
| 214 | `33985405343` | `fce2166` | **failure** | `Governance Health Check`: *"Technical Debt Register summary matches its own rows (135 stated, 136 rows present)"*. Both builds and the advisory Linux smoke passed; `CI Gate` failed only because it correctly depends on the governance check. |

That failure was real, deterministic, and self-inflicted: the remediation
added `TD-136` and did not update the register's own summary beside it —
precisely the drift the check exists to catch, caught on the first run
against the remediated tree. It was fixed by re-deriving the figures from
the rows (136 rows; 54 + 6 + 70 + 5 + 1 = 136) in `f593e5c`, and **no run
against `fce2166` counts toward acceptance**.

The matrix then ran clean on `f593e5c`:

| # | Run | ID | Event | Attempt | Conclusion |
|---|---|---|---|---|---|
| 1 | 218 | `33987082765` | `workflow_dispatch` | 1 | **success** |
| 2 | 220 | `33990823823` | `workflow_dispatch` | 1 | **success** |
| 3 | 223 | `33991318290` | `workflow_dispatch` | 1 | **success** |
| 4 | 226 | `33991814168` | `workflow_dispatch` | 1 | **success** |
| 5 | 227 | `33992329624` | `workflow_dispatch` | 1 | **success** |

Every one is a separate dispatch at `run_attempt: 1` — no re-run of a
failed job, no re-triggered attempt, and no `push` run substituted for a
dispatched one. `Build & Test (Release)`, `Build & Test (Debug)`,
`Governance Health Check` and `CI Gate` each concluded `success` in all
five, verified at job level rather than inferred from the run conclusion.
The advisory `Linux Launch Smoke` also passed in all five and is recorded
for completeness only — it is outside `CI Gate`'s `needs`, and ratified
`D-025` forbids reading it as CI verification.

Two further runs on `f593e5c` are excluded and preserved: run 213 and run
217, both `cancelled` by the workflow's own `concurrency` rule before any
test executed. A cancelled run is evidence of nothing.

**A third matrix is required.** `WP 16.4B-R5` — the remediation of the
findings in §5 of the Review Board Disposition — again changes `src/` and
`tests/`, so the `f593e5c` matrix no longer covers the tree being
released, on exactly the reasoning that invalidated the `d7d3f3b` one.
That third matrix was obtained on `58c4cba` and **its evidence is §4.3
below**, written by `WP 16.4B-R6` on 2026-09-06. *(Until then this
sentence was a forward promise to a section that did not exist — the
identical defect the fourth review board found in the promise to a §4.2,
recreated by the fix for it, and found again by the fifth board. §4.3 is
now written; the promise is discharged, not restated.)*

None of this is a substitute for §7.3's pre-tag Build/Test Gate run on
`main` itself, which remains outstanding and belongs to engineering
*after* approval.

The advisory `Linux Launch Smoke` result carries independent weight: this
is the first live CI confirmation of `WP 16.1A-R1`, which strengthened the
smoke assertion from a bare exit code to the `Host -> Running.` line. It
passing five times on `ubuntu-latest` is evidence the Linux launch path
actually reaches a running host — but it remains a *launch* smoke, not
Linux support, and §5 says so.

### 4.3 CI evidence — the RC4 matrix, on candidate `58c4cba`

**This is the determinism evidence for release candidate
`58c4cbaa1affec1cddb01a089fc5cbc0547634a4`, and for that commit only.**
It is complete: **5/5 qualifying independent passes**, recorded here
2026-09-06 by `WP 16.4B-R6`. The section §4.2 promised and did not write.

*Qualification rule, applied without substitution:* `event =
workflow_dispatch`; `run_attempt = 1`; `head_sha = 58c4cba`; run
conclusion `success`; and every gate job individually `success`, read at
job level rather than inferred from the run conclusion.

| # | Run | ID | SHA | Event | Attempt | Build & Test (Debug) | Build & Test (Release) | Governance Health Check | CI Gate | Conclusion |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 231 | `34023326177` | `58c4cba` | `workflow_dispatch` | 1 | success | success | success | success | **success** |
| 2 | 232 | `34023942958` | `58c4cba` | `workflow_dispatch` | 1 | success | success | success | success | **success** |
| 3 | 233 | `34024438521` | `58c4cba` | `workflow_dispatch` | 1 | success | success | success | success | **success** |
| 4 | 234 | `34025045650` | `58c4cba` | `workflow_dispatch` | 1 | success | success | success | success | **success** |
| 5 | 235 | `34026440586` | `58c4cba` | `workflow_dispatch` | 1 | success | success | success | success | **success** |

**The gate is those four jobs, and only those four.** `ci.yml`'s
`gate` job declares `needs: [build-and-test, governance-health-check]`;
`linux-launch-smoke` is **not** in that list. The advisory
`Linux Launch Smoke` job also concluded `success` in all five runs
(job `101462787332` in run 233, `101464411420` in run 234,
`101468133970` in run 235, and likewise in runs 231 and 232) and is
recorded **for completeness only**. Ratified `D-025` forbids reading it
as CI verification, and this section does not.

Job-level identifiers, for the three runs captured in full: run 233 —
Debug `101462787224`, Release `101462787318`, Governance
`101463917127`, CI Gate `101463952485`; run 234 — Debug `101464411455`,
Release `101464411452`, Governance `101465469826`, CI Gate
`101465516665`; run 235 — Debug `101468134130`, Release
`101468134216`, Governance `101469485953`, CI Gate `101469521504`.
Test-step durations across the matrix ranged Debug 5m28s–9m06s and
Release 5m23s–7m12s: wide but non-failing variance, recorded rather than
smoothed away.

**Excluded and preserved, not deleted.** Run 230 (`34023322455`,
`58c4cba`) was the automatic **`push`** run for the candidate. It was
**cancelled** by the workflow's own `concurrency` rule before any test
executed, and it is therefore **not counted**: it is neither a
`workflow_dispatch` run nor a run that produced a test result. It is
listed here so that a reader reconciling GitHub's run list against this
table can see why the qualifying runs begin at 231.

**The three earlier matrices remain in §4.1 and §4.2 and are not
superseded away silently.** For the avoidance of any doubt about which
evidence covers which commit:

| Matrix | Commit | Result | Standing |
|---|---|---|---|
| §4.1 | `d7d3f3b` | 5/5 (runs 195/197/199/201/202) | **Superseded** — `fce2166` changed `src/`, `tests/`, `scripts/`, `.github/` |
| §4.2, first attempt | `fce2166` | **failure** (run 214) | **A genuine failure, retained.** Governance Health Check: Technical Debt Register summary stated 135 against 136 rows |
| §4.2 | `f593e5c` | 5/5 (runs 218/220/223/226/227) | **Superseded** — `58c4cba` changed `src/` and `tests/` |
| §4.3 | `58c4cba` | 5/5 (runs 231–235) | **Superseded** — `WP 16.4B-R6` changed `src/` and `tests/` |
| §4.4 | `888ef63` | **5/5 (runs 253–257)** | **Superseded** — `WP 16.4B-R7` changed `src/` and `tests/` between `888ef63` and `d6af7ec`. *(Corrected `WP 16.4B-R7` governance closure, 2026-09-07: this cell read "**Current** — the determinism evidence for this candidate", which was true at `0935c64` and false from `501a4a7` onward.)* |
| — | **`d6af7ec`** (the candidate under review) | **none — not run** | **Outstanding.** A **fifth** matrix is required: five qualifying `workflow_dispatch` runs at `run_attempt: 1` against `d6af7ec`, on exactly the rule that retired the four matrices above. **This pass did not run it and does not promise it.** §7 item 6 |

**And that is exactly what happened.** `WP 16.4B-R6` — the remediation
of the fifth review board's findings, and then of the sixth board's —
again changed `src/` and `tests/`. On exactly the reasoning that
invalidated the `d7d3f3b` and `f593e5c` matrices, the RC4 matrix does
not cover any tree later than `58c4cba`, and nothing in §4.3 may be read
as validating the `WP 16.4B-R6` tree. **The fourth matrix has now been
obtained, against `888ef63`, and is recorded in §4.4 below.**

### 4.4 CI evidence — the fourth matrix, on candidate `888ef63`

Five qualifying independent runs, every one `workflow_dispatch`,
attempt 1, against exactly
`888ef63bd9c0ed23ce34bcf40df8918d3c765dbe`. The candidate was
re-verified unmodified and the working tree confirmed clean before every
dispatch.

| # | Run ID | № | Build & Test (Debug) | Build & Test (Release) | Governance Health Check | CI Gate | Conclusion |
|---|---|---|---|---|---|---|---|
| 1 | `34064407123` | 253 | pass | pass | pass | pass | **success** |
| 2 | `34065511477` | 254 | pass | pass | pass | pass | **success** |
| 3 | `34066082260` | 255 | pass | pass | pass | pass | **success** |
| 4 | `34066637682` | 256 | pass | pass | pass | pass | **success** |
| 5 | `34067176363` | 257 | pass | pass | pass | pass | **success** |

Every row was verified at **job** level, not merely by the run's own
conclusion. `Linux Launch Smoke` also passed in all five, and is
deliberately excluded from the table: it is advisory and is not among
`CI Gate`'s `needs`, so counting it would overstate what the gate
actually requires.

Runs were dispatched strictly one at a time. `ci.yml` sets
`concurrency: cancel-in-progress` keyed on the ref, so parallel
dispatches would cancel one another — and a cancelled run recorded as a
pass is precisely the error run 230 stands in the record to prevent.

Test-step duration spread across the five: Debug 5m03s to 8m35s, Release
5m18s to 6m09s. No failure, no re-run, no cancellation, and nothing
dispatched to replace a failed run.

**Why this section does not invalidate its own evidence.** Writing §4.4
necessarily produces a commit later than `888ef63`, which by the rule
above would appear to supersede the matrix it records. It does not. The
rule that invalidated `d7d3f3b`, `f593e5c` and `58c4cba` is that each
successor changed `src/` and `tests/` — the executable tree the matrix
measures. This section is documentation only; it changes no source and no
test, so the tree the five runs exercised is byte-for-byte the tree that
follows. The same reasoning let §4.3 record the `58c4cba` matrix from a
later commit. Stated here rather than left implicit, because a reader
applying the rule mechanically would otherwise conclude this evidence is
already stale. If any later commit touches `src/`, `tests/`, `scripts/`
or `.github/`, this matrix is superseded on the ordinary rule and a fifth
is required.

> **That condition has been met, and this matrix is superseded.**
> *(`WP 16.4B-R7` governance closure, 2026-09-07.)* `WP 16.4B-R7` landed
> seven commits after `0935c64`, changing
> `src/Tempest.Core/EngineeringDomain/Contracts/IEngineeringObjectStateStore.cs`,
> `src/Tempest.Core/EngineeringDomain/Implementation/EngineeringObjectBase.cs`,
> `src/Tempest.Core/Persistence/PersistenceStore.cs` and five files under
> `tests/Tempest.Core.Tests/EngineeringDomain/`. **The §4.4 matrix covers
> `888ef63` and no later tree, `d6af7ec` is not matrix-certified, and a
> fifth matrix is required against it.** This section is retained in full
> and is not relabelled: it is the correct, complete evidence for
> `888ef63`. Neither this note nor any part of this pass runs, promises or
> substitutes for that fifth matrix.

## 5. What this release did not verify

Stated plainly, because a readiness review that omits this is worthless:

- **macOS is not verified.** There is no macOS CI. It is expected to work
  and is untested.
- **No CI step on any platform launches the real windowed application** —
  the suites are Avalonia headless. Linux's launch is evidenced by an
  advisory smoke job asserting `Host -> Running.`; Windows's is asserted
  from development experience with no citation, which board 2 flagged and
  `PHYSICAL_REVIEW.md` now says outright.
- **Downgrade is a one-way door.** After running v0.16.0, any object it
  re-saves is unreadable to v0.15.0, whose read path logs a warning and
  returns `null` — so the object silently disappears from that workspace.
  Verified empirically. In the Release Notes.

## 6. Verdict derivation

### 6.1 The superseded derivation, retained verbatim

> `ADR-0106` §4's priority order, applied once over every finding:
>
> 1. Any category **Not Ready**? **No** — no Release Blocking finding
>    remains; both that existed were closed and verified.
> 2. Any **Disclosed, Non-Blocking** finding anywhere? **Yes** — 13.
>
> → **ACCEPT WITH OBSERVATIONS.**

Row 1's premise was false, and its own two clauses did not agree with §3's:
§3 said three Release Blocking findings existed, row 1 said "both that
existed". Neither count survives §3.2. This derivation is retained because
it is the reasoning that carried a recommendation of acceptance for a day
after board 5 failed the commit it was derived against.

### 6.2 The current derivation

*(Re-derived `WP 16.4B-R6` round 2, 2026-09-06, over §3.2–§3.4.)*

`ADR-0106` §4's priority order, applied once over every finding:

1. Any category **Not Ready**? **Yes** — Implementation readiness (§2).
   It carries one open Release Blocking finding, **`TD-143`**: the round-2
   refusal invariant covers `SupersededEngineeringObjectException` and
   nothing else, so a mutation whose durable write *fails* still keeps its
   in-memory mutation on all seven mutators — leaving a fabricated entry in
   the append-only transition history, and an object soft-deleted with no
   undelete and twenty-one read models filtering it out, after the caller
   was told the operation failed. Executable, deterministic evidence in
   this repository (`MutatorRefusalAdversarialTests` §3). `ADR-0106`'s own
   trigger — "a Technical Debt or Future Capability item with *real,
   demonstrated* evidence of harm" — is met.

   *(Re-derived `WP 16.4B-R6` round 2 against the integrated tree
   `261ba34`, 2026-09-06. This step named `TD-140` before the round-2
   remediation was integrated. `TD-140` is now closed for the route it
   named, and the verdict is unchanged, because the harm it described is
   still demonstrated — by the failure route rather than the refusal route.
   The blocker count went neither up nor down; only the row did.)*

   *(**Not re-derived at `d6af7ec`. `WP 16.4B-R7` governance closure,
   2026-09-07.** Step 1's supporting sentence — "still keeps its in-memory
   mutation on all seven mutators" — is **false at `d6af7ec`** for the
   seven mutators on `EngineeringObjectBase`, and its citation
   (`MutatorRefusalAdversarialTests` §3) now points at facts that were
   **inverted, not deleted**, and that assert the opposite; §2's
   Implementation-readiness supersession note gives their current names and
   the mechanism. **The verdict below is deliberately left unchanged at
   `NOT READY`, and this pass neither derives a new one nor names a
   replacement blocker.** Re-deriving step 1 requires the Technical Debt
   Register to be reconciled with the tree first — that is the register
   pass's act and the chair's — and it is flagged here so the derivation
   and the register move together. Independently of step 1, the two
   preconditions in §6.3 are **both still unmet at `d6af7ec`**, so no
   reading of this section supports acceptance.)*

   *(**Step 1 now re-derived — seam reconciliation, 2026-09-07.** The two
   notes above are retained unedited. The register pass has run and is
   integrated, so step 1 can be completed rather than only flagged.
   **Step 1 at `d6af7ec` reads: any category Not Ready? Yes —
   Implementation readiness (§2). It carries one open Release Blocking
   finding, `TD-147`, not `TD-143`.** An object creation whose initial
   durable state write fails still leaves the object registered and its
   document durable, and the object's next successful write of any kind
   makes a creation the caller was told had failed into a real, durable
   object — `EngineeringObjectFactory<T>.CreateAsync` calls
   `_context.Repository.Register(instance)` (`EngineeringObjectFactory.cs:46`)
   before `await instance.PersistInitialStateAsync(…)` (`:63`), and
   `IEngineeringObjectRepository` has no unregister operation anywhere.
   Executable, deterministic evidence in this repository:
   `R7RegressionProofTests.ACreateWhoseInitialStateWriteFails_StillRegistersTheObject_AndItsNextWriteMakesItDurable`,
   against the real durable stack. `ADR-0106`'s own trigger — "a Technical
   Debt or Future Capability item with *real, demonstrated* evidence of
   harm" — is met by `TD-147` exactly as it was by `TD-143` and by `TD-140`
   before it. **The count did not change, the category did not change, and
   the verdict did not change: one open Release Blocking finding, and
   `NOT READY`. The blocker MOVED; it did not disappear, and nothing in
   this change is progress toward acceptance.** `TD-143` is Resolved for the
   seven mutators on `EngineeringObjectBase` and for nothing else, and its
   own row says in its first sentence that it does not close the harm
   class.)*

→ **NOT READY.**

Rows 2, 3 and 4 are not reached. `ADR-0106` §4 is a strict priority order,
not four independent conditions, so exactly one verdict is reachable and
the fourteen Disclosed, Non-Blocking findings in §3.3 do not need to be
weighed against it.

### 6.3 What would change this, and what would not

Stated explicitly, because this report has twice been read as recommending
acceptance on the strength of code having changed.

- **A commit landing does not change it, and this has now been tested in
  practice.** `TD-140` stated three closure conditions in advance —
  guard-before-mutation inside one lock hold for each of the five mutators,
  the characterisation facts **inverted rather than removed**, and
  independent verification by someone who did not write the fix — and it
  moved only when all three were met and checked. It did **not** carry the
  verdict with it, because the same demonstrated harm survives by another
  route as `TD-143`. `TD-143` in turn will move only on a decision about
  failure semantics, evidence in both directions, and independent review.
- **Two further preconditions bar a recommendation of acceptance
  regardless of `TD-143`**, and are recorded separately so that closing it
  does not silently discharge them:
  1. **No five-run determinism matrix exists for the candidate under
     review.** §7 item 6 requires one before approval. It has not run
     against `d6af7ec`. This
     is not one of §4's three Release Blocking triggers and is therefore
     not what produces the verdict above; it is an unmet acceptance
     condition of `WP 16.4A` in its own right. *(Re-derived
     `WP 16.4B-R7` governance closure, 2026-09-07. This bullet was true
     when written, then briefly false — the fourth matrix ran 5/5 on
     `888ef63`, §4.4 — and is true again of `d6af7ec`, for a different
     reason: `WP 16.4B-R7` changed `src/` and `tests/` after `888ef63`. It
     is now a **fifth** matrix that is outstanding, not the fourth. Stated
     rather than left to coincidence, because a claim that is accidentally
     true is not evidence.)*
  2. **No independent board has reviewed the candidate under review.**
     Every board in this release has found a defect inside the previous
     board's remediation — five consecutive times, including a data-loss
     regression created by the fix for a bounded leak. A remediation that
     has not been independently attacked is not evidence of anything, and
     this report will not treat it as such. *(Re-derived `WP 16.4B-R7`
     governance closure, 2026-09-07. **A seventh board did sit** — on the
     `WP 16.4B-R6` round-2 remediation, returning PASS with six AMBERs and
     no RED, those six closed at `888ef63`; `v0.16.0 Review Board
     Disposition.md`'s scope note records it. That board reviewed
     `888ef63`, **not** `d6af7ec`, and **no board disposition for
     `d6af7ec` exists in this repository** — `grep -rc 'WP 16.4B-R7' docs/`
     returned 0 before this pass. The bullet therefore stands as written of
     the candidate under review, and the correction is to the reasoning
     rather than to the conclusion: the reason it stands is no longer "the
     round-2 remediation has not been attacked", which it has.)*
- **Whether the verdict becomes `ACCEPT WITH OBSERVATIONS` is not decided
  here.** It is derived, again, from `ADR-0106` §4 over whatever findings
  stand once the above exists. It could as easily stay `NOT READY`.

This is a recommendation to Product Approval. It is not certification, and
this document does not record a verdict in `Release Register.md` —
that field is Product Approval's to fill, and leaving it blank is
deliberate. **`NOT READY` is a recommendation not to proceed; it is not a
rejection of the release and it is not a decision of any kind.**

## 7. Outstanding, and whose they are

**Product Owner — four separate gates, of which two are now closed:**

1. ~~Ratify or reject `D-021`–`D-026`.~~ **DONE — all six ratified
   2026-09-05** by explicit Product Owner instruction, and entered in
   `Decision Register.md` with their proposed-then-ratified history
   intact. The irreversible act this gate had to absorb (`D-026`'s folder
   deletion, already performed) was disclosed as such at the gate and
   ratified in that knowledge. `D-025` was ratified with an explicit
   constraint: Linux is not to be recorded as "supported" without
   qualification, nor claimed CI-verified, and the distinction between
   launch evidence and formal CI verification is to be retained.
   Evidence: `docs/releases/v0.16.0/WP16.0A Product Owner Ratification — D-021 to D-026.md`.
2. ~~Authorise the merge of `feature/v0.16.0` to `main` (§7.6).~~
   **DONE — authorised and performed 2026-09-05.**
   **§7.6 per-occasion approval record, written `WP 16.4B-R6`
   (2026-09-06):** Engineering Governance §7 item 6 requires explicit,
   per-occasion Product Owner approval for a merge into `main`, and item
   5 the same for each push. That approval was **given in terms by the
   Product Owner in the release session of 2026-09-05** — *"AUTHORISE
   THE MAIN INTEGRATION — EXECUTE NOW"* — and the merge was executed on
   it. The integration landed on `main` as `cc7ef4d` (2026-09-05), was
   extended by the merge commit `9bba720` (2026-09-05) and by the
   remediation commit `58c4cba` (2026-09-06); `git merge-base
   --is-ancestor 58c4cba origin/main` succeeds.
   **Why this is being written now, and what the defect was.** No such
   record existed in any document until this one. For a day, this
   report listed the merge as an unauthorised outstanding gate while
   `Release Register.md` recorded the merge as performed — the release
   documentation asserting simultaneously that the merge had happened
   and that it had never been authorised, with no deviation disclosed
   either way. The fifth review board raised that contradiction
   (`P4-16`); it is resolved here in the direction the facts support, by
   writing the approval record §7.6 requires rather than by deleting
   either half of the contradiction. Gate 1 (scope ratification) and
   this gate 2 are the only two gates closed. **Gates 3 and 4 below
   remain open, and this record authorises neither.**
3. **Outstanding —** authorise the tag and its push, separately (§7.5).
4. **Outstanding —** record the Product Approval verdict in
   `Release Register.md`, the field every release since `v0.12.0` has
   left blank.

Gate 1 settles scope and nothing else. It is not merge authorisation, not
tag authorisation, and not a release verdict.

**Product Owner / repository administrator, not release-blocking:**
5. Configure GitHub branch protection on `main` (`TD-45`). Exact settings
   are in `WP16.1A Enforce the Release Gate.md`.

**Engineering, before approval:**
6. **A ~~fourth~~ FIFTH determinism matrix.** *(Re-derived
   `WP 16.4B-R7` governance closure, 2026-09-07.)* **The fourth matrix was
   obtained** — 5/5 on `888ef63`, runs 253–257, recorded in §4.4 by
   `0935c64`. It is now **superseded**, because `WP 16.4B-R7` changed three
   files under `src/` and five under `tests/` between `888ef63` and
   `d6af7ec`, on exactly the rule that retired §4.1, §4.2 and §4.3. Five
   qualifying `workflow_dispatch` runs at `run_attempt: 1` are therefore
   required **against `d6af7ec`**, or against whatever commit succeeds it.
   **Not obtained. This pass did not run it and does not promise it.**
   *(The superseded text, retained: "**A fourth determinism matrix.**
   `WP 16.4B-R6` changes `src/` and `tests/` again, so the RC4 matrix in
   §4.3 covers `58c4cba` and no later tree. Five qualifying
   `workflow_dispatch` runs at `run_attempt: 1` are required against
   whatever commit becomes the next candidate, on the same rule §4.3
   states. **Not obtained.** *(Unchanged and restated `WP 16.4B-R6`
   round 2: the candidate has moved again — to `3dd74d8`, then to
   `261ba34` when round 2's two streams were integrated — and will move
   again on any further change. **No matrix exists for `261ba34`.** The
   local regression recorded in §2 is not one and is not offered as
   one.)*" That text was false from `0935c64` onward, in the same document
   that recorded the matrix it denied. The local regression figures in §2
   are still not a matrix and are still not offered as one.)*
7. **Close `TD-143`, or have it explicitly accepted.** *(Added
   `WP 16.4B-R6` round 2, 2026-09-06; this item named `TD-140` until that
   round's remediation was integrated and `TD-140` closed for the route it
   named.)* It is the one open Release Blocking finding and it is what
   makes §6's verdict `NOT READY`. Its register row states what closing it
   would require — a decision about failure semantics, not a reordering.
   Accepting it instead is a decision that must be recorded in the
   governance record, not assumed — an unresolved finding that has been
   accepted nowhere is, on this review's own standing rule, a failure by
   itself.
   *(**Flagged, not re-derived, `WP 16.4B-R7` governance closure,
   2026-09-07.** `WP 16.4B-R7` landed a remediation for `TD-143`'s
   failing-durable-write route on `EngineeringObjectBase`, at `d6af7ec`
   — see §2's supersession note — while `TD-143`'s register row still reads
   *"Open — reproduced, not fixed"* at that same commit. **This item names
   the blocker and is therefore left exactly as it stands until the
   Technical Debt Register pass has settled `TD-143`'s disposition and the
   identity of the open Release Blocking finding.** It is listed in this
   pass's report so the chair can confirm this item, §2, §3.2 row 9, §6.2
   step 1, the register row, `v0.16.0 Review Board Disposition.md` §8.3 and
   §9.3, `Release Register.md`'s `v0.16.0` cell,
   `docs/architecture/Engineering Readiness Review Architecture.md`'s
   `NOT READY` precedent cell, and `PROJECT_STATUS.md` all name the same
   blocker after that pass. They do not all name the same thing today.)*

   *(**Settled — seam reconciliation, 2026-09-07.** The item heading and the
   two notes above are retained unedited. The register pass has run and is
   integrated. **This item now reads: close `TD-147`, or have it explicitly
   accepted.** `TD-143` was moved to **Resolved for the seven mutators on
   `EngineeringObjectBase` and for nothing else**, and it was moved only in
   a pass that opened `TD-147` **simultaneously**, as an independent Release
   Blocking row, precisely so that the narrower closure could not read as
   the harm class going away — the condition the independent board set for
   permitting the closure at all. `TD-143`'s row no longer reads *"Open —
   reproduced, not fixed"*; that wording is retained inside the row as its
   own superseded text. **`TD-147` is now the one open Release Blocking
   finding and it is what makes §6's verdict `NOT READY`.** Its register row
   states four closure conditions written in advance so that later code
   landing cannot move it without evidence, and — exactly as this item says
   of `TD-143` — accepting it instead is a decision that must be recorded in
   the governance record, not assumed. **The verdict is unchanged and the
   blocker count is unchanged at one.** The seven documents this note listed
   are reconciled by the same pass and now name the same finding; the one
   place that names a verdict without a finding, `PROJECT_STATUS.md:54`, is
   left as it stands because it names no blocker to correct.)*
8. **Dispose of the five further open rows this round recorded**, none of
    which is graded here: `TD-141` (two unguarded relationship-write paths,
    now characterised executably), `TD-142` (twelve type-specific mutators
    with the pre-round-2 shape), `TD-144` (a failed move link write still
    reparents), `TD-145` (concurrent moves form a parent cycle over which
    five `Tempest.App` ancestry walks never terminate) and `TD-146` (a
    delete commits while a concurrent move gives it a live child). Each has
    executable evidence and none has a remediation. They are listed here so
    they are not found for the first time by a seventh board.
9. **An independent board on the candidate under review.** Five boards had
    each found a defect inside the previous board's remediation when this
    item was written. *(Re-derived `WP 16.4B-R7` governance closure,
    2026-09-07. This item read "on the integrated candidate … The round-2
    remediation has not been independently attacked", which stopped being
    true when a **seventh board** reviewed the round-2 remediation and
    returned PASS with six AMBERs and no RED, those six closed at
    `888ef63`; `v0.16.0 Review Board Disposition.md`'s scope note records
    it. The item nevertheless **stands**, for the candidate that is
    actually under review: **no board disposition for `d6af7ec` exists in
    this repository**, and `WP 16.4B-R7` — the round that produced
    `d6af7ec` — is recorded in no document in `docs/`. Recording that round
    and any board on it is governance work this pass has flagged and not
    performed.)*

**Engineering, after approval:**
10. §7.3's Build/Test Gate run on `main` itself, immediately pre-tag.
11. `scripts/new-release.ps1` from `main`; `release.yml` publish.

*(Renumbered `WP 16.4B-R6` round 2, 2026-09-06: items 7 and 8 above were
the two after-approval steps and are now 10 and 11; three new
before-approval items were added as 7, 8 and 9. No item was removed.)*

## Related Documents

`docs/adr/ADR-0106-release-sign-off-is-governed-by-a-permanent-five-category-engineering-readiness-review.md`;
`docs/architecture/Engineering Readiness Review Architecture.md`;
`docs/releases/v0.16.0/Release Notes.md`;
`docs/releases/v0.16.0/WorkPackages.md`;
`docs/releases/v0.16.0/v0.16.0 Release Plan.md`;
`docs/releases/v0.16.0/v0.16.0 Engineering Status Report.md`;
`docs/academy/06 Engineering Standards/Engineering Governance.md` §7, §9;
`docs/governance/Delivery/Release Register.md`.
