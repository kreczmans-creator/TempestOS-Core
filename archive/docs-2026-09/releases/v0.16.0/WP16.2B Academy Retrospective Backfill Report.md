# WP 16.2B — Academy Retrospective Backfill

**Type.** Documentation only. No production code, test, ADR, or
architecture document.
**Branches.** Three parallel authors, each on its own branch from
`feature/v0.16.0` at `8b4c394`, merged non-fast-forward in this order:
`feature/v0.16.0-wp16.2b-retros-v0.11.0` (`b7ceada` → merge `bfbe09d`),
`feature/v0.16.0-wp16.2b-retros-v0.15.0` (`17cad3f` → `17509e7`),
`feature/v0.16.0-wp16.2b-retros-v0.14.0` (`214bc76` → `dce992a`); this
closure commit reconciles the shared counts the three could not touch
without colliding.
**Closes.** The retrospective gap disclosed by `WP 11.9.0`, re-disclosed
by `WP 12.3A`/`12.3B`, `WP-Z3` §12, `WP 11.5A` §10 and `WP 15.1A`.
Release-level Definition of Done item 4 ("Academy updated") is true for
the first time since `v0.10.0`. **Cites** `D-021` (Proposed, `WP 16.0A`).

---

## What was written

| Set | Files | Sources |
|---|---|---|
| `v0.11.0`'s ten Work Packages (`WP 11.0A`, `11.0B`, `11.1A`, `11.1B`, `11.2A`, `11.3A`, `11.3B`, `11.4A`, `11.4B`, `11.9.0`) | 10 | Each Work Package's own report under `docs/releases/v0.11.0/` and its commit(s) |
| `WP 15.1A`, `WP 15.1B` | 2 | `docs/releases/v0.15.0/` reports, commits `d2f1fae`/`32abf64` |
| `v0.14.0` pre-programme engineering body of work (`docs/releases/v0.14.0/WorkPackages.md` §B) | 29 — one per row, re-derived as exactly 29 rows (the plan's "~20" and `WP-Z3`'s "~34 commits" both mis-stated it) | Each row's own commit message(s), the ADRs it raised (`ADR-0095`, `0113`–`0117`), its TD rows |

Total **41** new retrospectives; `docs/academy/03 Work Packages/` 165 → **206**
files. All follow Engineering Governance §6's thirteen-section template
plus Related Documents; every one is linked from `Academy Index.md`
(Check 2: 280 files / 373 links, 0 orphans, 0 broken).

**Naming rule for the 29 unnumbered rows** (a Chief Engineer decision,
applied once): `WP-Z3` §6 rejected inventing `WP14.x` numbers for work
that carries none anywhere in the repository. These rows carry no `WP`
identifier at all, so the files are named
`v0.14.0-<anchor>-<kebab-title>.md`, where `<anchor>` is the row's own
identifier when it has one (`TD-59-TD-60`, `TD-84`, `ADR-0116`,
`TD-77-stage-2`) and a short topic slug otherwise. The rule is stated in
the retrospective for the row that commissioned `WP-Z3` itself
(`v0.14.0-architecture-audit-becomes-tracked-debt.md`).

## Register work

- `Academy Register.md` `## 03 Work Packages`: 10 + 2 + 29 rows for the
  new files, plus **21 backfill rows** for retrospectives that existed on
  disk without a row (`WP 9.3A`, `9.4A`, `9.5A`, `9.8B`, `9.9.0` ×2,
  `10.0A`, `10.0B`, `10.1A`, `10.1B`, `10.2A`, `12.9.1`, `12.9.2`,
  `13.0.0`, `13.0.0A`, `11.5A`, `15.0A`–`15.0D`, `15.2A`) — re-derived
  by diffing the table against `ls`, four more than the plan named. The
  section header count and "Last Reviewed" narrative are reconciled in
  this closure commit (142 → 206).
- `docs/releases/v0.15.0/WorkPackages.md`: the `WP 15.1B` row that was
  missing (described only in prose) added.
- `Contributor Learning Path.md`: the closing pointer, stale since
  `v0.5.0` through eleven releases, now names `docs/releases/v0.16.0/
  WorkPackages.md`, with a "Correction, `WP 16.2B`" note in the file's
  own style.
- `Documentation Register.md` and `Repository Metrics Register.md`
  Academy counts reconciled here (165 → 206 retrospectives; 240 → 281
  `.md` files under `docs/academy/`).

## Validation

- `governance-healthcheck.ps1` after each merge and after this closure:
  7 passed, 1 pre-existing informational warn, 0 failed.
- Per-set scripts: every `WP 11.x` row has a file (10/10); every §B row
  has exactly one file, linked (29/29); the register table diffed against
  disk shows zero unregistered files.
- Two of the three authors could not invoke PowerShell directly in their
  worktrees (a sandbox guard); one reimplemented Check 2's algorithm in
  Python, one used a shell wrapper. The real script was run by the
  integrator on the merged tree in every case above.

## Related Documents

`docs/releases/v0.16.0/v0.16.0 Release Plan.md` (`WP 16.2B`);
`docs/academy/Academy Index.md`; `docs/governance/Documentation/Academy
Register.md`; `docs/academy/03 Work Packages/WP-Z3-programme-academy-retrospective-completion.md`
§5/§6/§12; `docs/academy/06 Engineering Standards/Engineering Governance.md`
§6.
