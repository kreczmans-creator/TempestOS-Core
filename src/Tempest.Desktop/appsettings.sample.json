{
  "//": "TempestOS sample operator configuration (WP 17.2A, ADR-0146). Copy this file",
  "// 1": "to 'appsettings.json' next to the running executable (or in the current",
  "// 2": "directory) and edit the values you need; every key here can also be set",
  "// 3": "with a TEMPEST_-prefixed environment variable ('__' as the section",
  "// 4": "separator, e.g. TEMPEST_Runtime__Logging__MinimumLevel=Debug) or on the",
  "// 5": "command line ('--Runtime:Logging:MinimumLevel=Debug'). Precedence, lowest",
  "// 6": "to highest: this file next to the executable, this file in the current",
  "// 7": "directory, environment variables, the command line. Remove every '//'",
  "// 8": "comment key below before using this as a real appsettings.json - JSON has",
  "// 9": "no native comment syntax, and these keys are illustrative only.",

  "Persistence": {
    "//RootPath": "Where the persistence database (and logs/, see Runtime:Logging below) is written. Defaults to a working-directory-relative folder (SqlitePersistenceStore.DefaultRootPath) when unset.",
    "RootPath": "C:/ProgramData/TempestOS",

    "//Backend": "'sqlite' - the default, and the only recognised value. The file-per-key backend this key once could also select ('files') is deleted (v0.18.0, ADR-0144); any other value is a fatal configuration error.",
    "Backend": "sqlite"
  },

  "Runtime": {
    "Logging": {
      "//MinimumLevel": "One of Trace, Debug, Information, Warning, Error, Critical, None. Defaults to Information.",
      "MinimumLevel": "Information"
    },

    "Plugins": {
      "//RootDirectory": "Where the Host's Plugin Discovery phase looks for manifests. Defaults to a 'Plugins' folder next to the executable.",
      "RootDirectory": "C:/ProgramData/TempestOS/Plugins",

      "//ManifestFileName": "The manifest file name Plugin Discovery looks for inside each plugin's own folder. Defaults to the platform's own convention.",
      "ManifestFileName": "plugin.manifest.json",

      "//Disabled": "A comma-separated list of plugin ids Plugin Discovery records as disabled without removing their files.",
      "Disabled": ""
    }
  },

  "Identity": {
    "//DisplayName": "Overrides the session principal's display name (otherwise the OS account name). Never changes the underlying identity id, which is always read from the OS.",
    "DisplayName": "",

    "//Role": "Engineer (the default) or Checker - governs which commands the session is offered. Never changes the identity id.",
    "Role": "Engineer"
  }
}
